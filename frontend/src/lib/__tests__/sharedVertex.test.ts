/* Wave 0 stub for Phase 08 — implemented in plan 08-06b.
 * Covers REQ-IDs: TOPO-03, TOPO-04, EDIT-VERTEX-01
 * See: .planning/phases/08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr/08-VALIDATION.md
 */
import { describe, it } from 'vitest';

describe('Phase 08 — sharedVertex utility (TOPO-03, TOPO-04)', () => {
  it.skip('TODO plan 08-06b: findSharedVertices returns all vertices shared by two adjacent polygons', () => {});
  it.skip('TODO plan 08-06b: propagateVertexMove updates matching coordinate in all sharing polygons', () => {});
  it.skip('TODO plan 08-06b: propagateVertexMove on non-shared vertex leaves neighbour polygons unchanged', () => {});
  it.skip('TODO plan 08-06b: after propagation, Shapely distance between updated polygons is 0.0 (no gap)', () => {});
});
