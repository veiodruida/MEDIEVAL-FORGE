/* Wave 0 stub for Phase 08 — implemented in plan 08-06a.
 * Covers REQ-IDs: EDIT-VERTEX-04, UX-02
 * See: .planning/phases/08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr/08-VALIDATION.md
 */
import { describe, it } from 'vitest';

describe('Phase 08 — VertexCapBadge (EDIT-VERTEX-04, UX-02)', () => {
  it.skip('TODO plan 08-06a: badge shows current vertex count and configured cap (e.g. "42 / 100")', () => {});
  it.skip('TODO plan 08-06a: badge turns red when vertex count reaches cap', () => {});
  it.skip('TODO plan 08-06a: badge is hidden when no territory is selected in editor mode', () => {});
  it.skip('TODO plan 08-06a: simplify button is enabled only when vertex count exceeds cap', () => {});
});
