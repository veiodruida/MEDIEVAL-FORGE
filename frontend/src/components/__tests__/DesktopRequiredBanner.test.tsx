/* Wave 0 stub for Phase 08 — implemented in plan 08-05.
 * Covers REQ-IDs: UX-01, UX-02
 * See: .planning/phases/08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr/08-VALIDATION.md
 */
import { describe, it } from 'vitest';

describe('Phase 08 — DesktopRequiredBanner (UX-01, UX-02)', () => {
  it.skip('TODO plan 08-05: banner renders when viewport width < 1024px', () => {});
  it.skip('TODO plan 08-05: banner is hidden when viewport width >= 1024px', () => {});
  it.skip('TODO plan 08-05: banner message contains actionable text directing user to a desktop browser', () => {});
  it.skip('TODO plan 08-05: vertex-edit mode is blocked (editor toolbar hidden) when banner is visible', () => {});
});
