---
phase: quick-260507-g1v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v1-archive/ROADMAP.md
  - .planning/v1-archive/PROJECT.md
  - .planning/v1-archive/REQUIREMENTS.md
  - .planning/v1-archive/STATE.md
  - .planning/v1-archive/SESSION-NOTES.md
  - .planning/v1-archive/STACK_RESEARCH.md
  - .planning/v1-archive/phases/
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/backlog.md
  - .claude/skills/karpathy/SKILL.md
  - CLAUDE.md
  - .github/workflows/ci.yml
  - pyproject.toml
  - frontend/package.json
  - .planning/quick/260507-g1v-phase-00-v3-archive-milestone-reset/V1_DELETION_CANDIDATES.md
autonomous: true
requirements:
  - V3-PHASE-00
must_haves:
  truths:
    - "ROADMAP.md v1 e os 4 demais artefatos de planejamento v1 estão arquivados em .planning/v1-archive/"
    - "CLAUDE.md tem seção 'v3 Pipeline Contract' (substituindo Constraints) e seções 'Architecture', 'Conventions' e 'What v3 explicitly is NOT'; tabelas de research v1 (Validated Choices, Potential Issues, Recommendations, Confidence Levels, Sources) foram removidas"
    - ".claude/skills/karpathy/SKILL.md existe e o diretório legado Skill/ foi removido"
    - "Tag git v1.0-archive existe apontando para o commit anterior ao reset"
    - "CI scaffolding existe em .github/workflows/ci.yml com 4 jobs (pytest-unit, pytest-parity, vitest, playwright-uat) e markers parity/integration/uat estão registrados em pyproject.toml"
    - "Servidor medieval-forge ainda boota (smoke 200 OK em http://localhost:8765/)"
  artifacts:
    - path: ".planning/v1-archive/ROADMAP.md"
      provides: "Snapshot do roadmap v1.0 (preservado)"
    - path: ".planning/v1-archive/PROJECT.md"
      provides: "Snapshot do PROJECT.md v1.0"
    - path: ".planning/v1-archive/REQUIREMENTS.md"
      provides: "Snapshot dos requisitos v1.0 (62 reqs)"
    - path: ".planning/v1-archive/STATE.md"
      provides: "Snapshot do STATE.md v1.0 (97% complete)"
    - path: ".planning/v1-archive/SESSION-NOTES.md"
      provides: "Snapshot das notas de sessão v1.0"
    - path: ".planning/v1-archive/STACK_RESEARCH.md"
      provides: "Tabelas Validated Choices + Potential Issues + Recommendations + Confidence Levels + Sources extraídas do CLAUDE.md anterior"
    - path: ".planning/PROJECT.md"
      provides: "Visão v3 (novo PROJECT.md)"
    - path: ".planning/ROADMAP.md"
      provides: "Roadmap v3 com 8 phases (00–07)"
    - path: ".planning/STATE.md"
      provides: "Estado v3 (milestone=v3, current_phase=00)"
    - path: ".planning/backlog.md"
      provides: "Backlog v3.1 com 6 itens deferidos"
    - path: "CLAUDE.md"
      provides: "Reescrita v3 (Pipeline Contract + Architecture + Conventions + What v3 is NOT)"
    - path: ".claude/skills/karpathy/SKILL.md"
      provides: "Skill movida para local padrão de auto-discovery"
    - path: ".github/workflows/ci.yml"
      provides: "CI scaffolding (4 jobs: pytest-unit, pytest-parity, vitest, playwright-uat)"
    - path: ".planning/quick/260507-g1v-phase-00-v3-archive-milestone-reset/V1_DELETION_CANDIDATES.md"
      provides: "Lista conservadora de candidatos a deleção para review humano (em vez de deletar agressivamente)"
  key_links:
    - from: "[path]C:\\\\Users\\\\veio_\\\\.claude\\\\plans\\\\ok-vamos-partir-do-smooth-kurzweil.md"
      to: "this PLAN.md"
      via: "PLANO MASTER aprovado pelo usuário — fonte canônica de verdade para Phase 00 v3"
    - from: "[path]inicio/map_generator.py"
      to: "CLAUDE.md (v3 Pipeline Contract)"
      via: "Pipeline canônico de 15 estágios — referência inegociável"
    - from: "[path]inicio/licoes/JORNADA_CRIACAO_MAPA.md"
      to: "CLAUDE.md (v3 Pipeline Contract — 11 arquivos Unity)"
      via: "Documenta cada decisão e bug do pipeline; ground-truth de regras inegociáveis (NEAREST upscale, σ ∈ [3.0, 4.5], KD-trees por país, masks 2x independentes)"
---

<objective>
Arquivar o milestone v1.0 do Medieval Forge e estabelecer a infraestrutura inicial do milestone v3 ("Reset to Roots") sem quebrar o servidor que está funcionando.

Purpose: O v1.0 chegou a 97% mas com complexidade demais (Phase 4 com bugs, Phase 5–6 nunca iniciadas) e o usuário decidiu reiniciar focando no pipeline canônico de 15 estágios em `inicio/map_generator.py`. Esta task quita a dívida documental — limpa worktrees stale, arquiva planning v1, reescreve CLAUDE.md (que estava poluído com 100+ linhas de research notes), e estabelece o esqueleto de CI que a Phase 01 vai consumir.

Output:
- Tag git `v1.0-archive` no HEAD anterior
- 5 arquivos de planejamento v1 movidos para `.planning/v1-archive/` + STACK_RESEARCH.md extraído do CLAUDE.md
- 6 worktrees stale removidos
- Skill Karpathy movida para `.claude/skills/karpathy/SKILL.md`
- Novos `.planning/{PROJECT,ROADMAP,STATE,backlog}.md` v3
- CLAUDE.md reescrito (Project + Constraints curtas + GSD + v3 Pipeline Contract + Architecture + Conventions + What v3 is NOT + Skills + Profile)
- `.github/workflows/ci.yml` com 4 jobs paralelos placeholder
- Markers `parity/integration/uat` em `pyproject.toml`
- Script `e2e:playwright` em `frontend/package.json`
- `V1_DELETION_CANDIDATES.md` com candidatos a deleção para review (sem deletar agressivamente)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
**PLANO MASTER (fonte canônica de verdade — leia INTEIRO antes de começar):**
@C:\Users\veio_\.claude\plans\ok-vamos-partir-do-smooth-kurzweil.md

**Estado v1 a ser arquivado:**
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/PROJECT.md

**Referência canônica do pipeline v3 (CLAUDE.md vai apontar para estes):**
@inicio/map_generator.py
@inicio/licoes/JORNADA_CRIACAO_MAPA.md

**Skill a ser movida:**
@Skill/SKILL-karpathy.md

**CLAUDE.md atual (a ser reescrito):**
@CLAUDE.md

**Arquivos com config a ser ajustada:**
@pyproject.toml
@frontend/package.json

<interfaces>
<!-- Worktrees stale (verificados em git worktree list em 2026-05-07): -->
<!-- Todos LOCKED + dirs já listadas em git status como untracked. Auditoria pré-remoção exigida. -->
- D:/Projetos_Jogo/Medieval_Forge/.claude/worktrees/agent-a1e3903b @ c092f0f
- D:/Projetos_Jogo/Medieval_Forge/.claude/worktrees/agent-a4a1804e @ f01c6cf
- D:/Projetos_Jogo/Medieval_Forge/.claude/worktrees/agent-a5ad6a02 @ 79193b6
- D:/Projetos_Jogo/Medieval_Forge/.claude/worktrees/agent-ad649f86 @ 2fe0028
- D:/Projetos_Jogo/Medieval_Forge/.claude/worktrees/agent-ae007eb2 @ cb5d0e1
- D:/Projetos_Jogo/Medieval_Forge/.claude/worktrees/agent-ae81ed71 @ b17333e

<!-- pyproject.toml já tem [tool.pytest.ini_options] em linhas ~67-73 com 1 marker (slow). -->
<!-- ESTENDER markers (não substituir): adicionar parity, integration, uat preservando slow. -->

<!-- frontend/package.json já tem scripts test (vitest), test:e2e (playwright test), test:e2e:update. -->
<!-- Adicionar APENAS o alias e2e:playwright (apontando para playwright test) — não duplicar test:e2e. -->

<!-- backend/ NÃO existe na raiz como package — pyproject.toml está em D:/Projetos_Jogo/Medieval_Forge/pyproject.toml -->
<!-- código Python vive em backend/medieval_forge/ (subdir, não package separado) -->
<!-- testes em backend/tests/ -->

<!-- Frontend package está em D:/Projetos_Jogo/Medieval_Forge/frontend/ — npm ci roda lá -->

<!-- 11 arquivos do export Unity (do inicio/licoes/JORNADA_CRIACAO_MAPA.md linhas 42-55): -->
1. lookup_barony.png         (1920×1080, hit detection baronias)
2. lookup_condado.png        (1920×1080, hit detection condados)
3. lookup_barony_colors.json (RGB → barony id)
4. lookup_condado_colors.json (RGB → condado id)
5. terrain_lookup.png        (1920×1080, tipo terreno por pixel)
6. terrain_types.json        (RGB → {movement, defense, attack})
7. territory_metadata.json   (hierarquia completa com original_idx)
8. visual_condado.png        (3840×2160, placeholder visual)
9. visual_barony.png         (3840×2160, placeholder visual)
10. mountains_mask.png       (3840×2160, branco=impassável)
11. rivers_overlay.png       (3840×2160, PNG transparente)
+ mountain_river_data.json   (coordenadas geo)
<!-- Total = 12 arquivos. O CLAUDE.md deve listar os 12 (o plano master diz "11" mas a contagem real do JORNADA é 12). -->
<!-- Referenciar como "12-file Unity export contract" na reescrita. -->
</interfaces>
</context>

<tasks>

<!-- ============================================================ -->
<!-- TASK 1 — Archive & quarantine                                -->
<!-- Commit: chore(v3): archive v1.0 planning artifacts and prepare v3 milestone -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task 1: Tag v1.0, audit & remove stale worktrees, move planning + skill artifacts</name>

  <files>
    - .planning/v1-archive/ (criar diretório)
    - .planning/v1-archive/ROADMAP.md (mover de .planning/ROADMAP.md)
    - .planning/v1-archive/PROJECT.md (mover de .planning/PROJECT.md)
    - .planning/v1-archive/REQUIREMENTS.md (mover de .planning/REQUIREMENTS.md)
    - .planning/v1-archive/STATE.md (mover de .planning/STATE.md)
    - .planning/v1-archive/SESSION-NOTES.md (mover de .planning/SESSION-NOTES.md)
    - .planning/v1-archive/phases/ (mover de .planning/phases/)
    - .claude/skills/karpathy/SKILL.md (mover de Skill/SKILL-karpathy.md)
    - Skill/ (remover diretório se vazio)
    - .claude/worktrees/agent-{a1e3903b,a4a1804e,a5ad6a02,ad649f86,ae007eb2,ae81ed71}/ (remover via git worktree remove --force)
  </files>

  <action>
**ORDEM CIRÚRGICA — não pule etapas. Cada bloco tem seu próprio guard.**

### 1.1 Tag v1.0-archive ANTES de qualquer mudança
```bash
cd /d/Projetos_Jogo/Medieval_Forge
git tag -a v1.0-archive -m "Archive v1.0 milestone before v3 reset"
git tag --list | grep -x "v1.0-archive"   # GUARD: deve retornar exatamente "v1.0-archive"
```
Se a tag já existe (ex.: re-execução), abortar com mensagem ao usuário — **não** sobrescrever.

### 1.2 Auditar worktrees stale
```bash
git worktree list --porcelain
```
Para cada worktree em `.claude/worktrees/agent-*`:
1. Cd para o worktree e rodar `git status --porcelain` + `git log --oneline main..HEAD`
2. **Se** retornar QUALQUER linha (uncommitted changes OU commits não-mergeados em main):
   - **ABORTAR** o task. Imprimir nome do worktree + saída do status/log + a mensagem:
     `"WORKTREE STALE COM TRABALHO: <path>. Investigue manualmente antes de continuar Phase 00."`
   - NÃO deletar nada, NÃO continuar. Pedir input do usuário.
3. **Se** worktree está clean (sem uncommitted, sem commits ahead): seguro para remover.

```bash
# Para cada worktree limpo (esperado: todos os 6 listados em <interfaces>):
git worktree unlock .claude/worktrees/agent-a1e3903b 2>/dev/null || true
git worktree remove --force .claude/worktrees/agent-a1e3903b
# Repetir para a4a1804e, a5ad6a02, ad649f86, ae007eb2, ae81ed71
git worktree prune
git worktree list   # GUARD: só deve listar D:/Projetos_Jogo/Medieval_Forge (main)
```

### 1.3 Criar diretório v1-archive e mover planning v1
```bash
mkdir -p .planning/v1-archive
git mv .planning/ROADMAP.md       .planning/v1-archive/ROADMAP.md
git mv .planning/PROJECT.md       .planning/v1-archive/PROJECT.md
git mv .planning/REQUIREMENTS.md  .planning/v1-archive/REQUIREMENTS.md
git mv .planning/STATE.md         .planning/v1-archive/STATE.md
git mv .planning/SESSION-NOTES.md .planning/v1-archive/SESSION-NOTES.md
git mv .planning/phases           .planning/v1-archive/phases
```
**NÃO** mover: `.planning/quick/`, `.planning/research/`, `.planning/notes/`, `.planning/sessions/`, `.planning/debug/`, `.planning/HANDOFF.json`, `.planning/config.json`. Esses ficam no root .planning/.

### 1.4 Mover Skill Karpathy para local padrão
```bash
mkdir -p .claude/skills/karpathy
git mv Skill/SKILL-karpathy.md .claude/skills/karpathy/SKILL.md
# Verificar se o diretório Skill/ ficou vazio:
ls Skill/ 2>/dev/null
# Se vazio, remover (Windows-safe):
rmdir Skill 2>/dev/null || rm -rf Skill
```
Se houver outros arquivos em `Skill/` (não esperado conforme audit), **não** deletar — listar e pedir input.

### 1.5 NÃO commitar ainda — Tasks 2 e 3 fazem parte do mesmo trabalho mas commits separados
A Task 1 termina com working tree contendo:
- 5 renames + 1 dir rename em .planning/
- 1 rename Skill→.claude/skills/karpathy
- (worktree dirs já removidos pelo git worktree remove)

Commit final desta task:
```bash
git add -A .planning/ .claude/skills/karpathy/ Skill/
git commit -m "chore(v3): archive v1.0 planning artifacts and prepare v3 milestone

- Tag v1.0-archive on previous HEAD before reset
- Move .planning/{ROADMAP,PROJECT,REQUIREMENTS,STATE,SESSION-NOTES}.md to .planning/v1-archive/
- Move .planning/phases/ to .planning/v1-archive/phases/
- Move Skill/SKILL-karpathy.md to .claude/skills/karpathy/SKILL.md (auto-discovery)
- Remove 6 stale worktrees (agent-{a1e3903b,a4a1804e,a5ad6a02,ad649f86,ae007eb2,ae81ed71})

Refs: C:/Users/veio_/.claude/plans/ok-vamos-partir-do-smooth-kurzweil.md (Phase 00, Bloco 1)"
```
  </action>

  <verify>
    <automated>
cd /d/Projetos_Jogo/Medieval_Forge && \
git tag --list | grep -qx "v1.0-archive" && \
test -d .planning/v1-archive && \
test -f .planning/v1-archive/ROADMAP.md && \
test -f .planning/v1-archive/PROJECT.md && \
test -f .planning/v1-archive/REQUIREMENTS.md && \
test -f .planning/v1-archive/STATE.md && \
test -f .planning/v1-archive/SESSION-NOTES.md && \
test -d .planning/v1-archive/phases && \
test -f .claude/skills/karpathy/SKILL.md && \
test ! -d Skill && \
[ "$(git worktree list | wc -l)" -eq "1" ] && \
echo "TASK1_OK"
    </automated>
  </verify>

  <done>
- `git tag --list` contém `v1.0-archive`
- `.planning/v1-archive/` contém os 5 arquivos + diretório `phases/`
- `.claude/skills/karpathy/SKILL.md` existe; diretório `Skill/` removido
- `git worktree list` mostra apenas o worktree principal (1 linha)
- 1 commit `chore(v3): archive v1.0 planning artifacts...` no HEAD
  </done>
</task>

<!-- ============================================================ -->
<!-- TASK 2 — Reescrever planning docs e CLAUDE.md                -->
<!-- Commit: docs(v3): rewrite CLAUDE.md and seed v3 planning docs -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task 2: Extrair STACK_RESEARCH, reescrever CLAUDE.md, criar planning v3 (PROJECT, ROADMAP, STATE, backlog)</name>

  <files>
    - .planning/v1-archive/STACK_RESEARCH.md (CRIAR — extraído do CLAUDE.md atual)
    - CLAUDE.md (REESCREVER — substituição completa)
    - .planning/PROJECT.md (CRIAR — visão v3)
    - .planning/ROADMAP.md (CRIAR — 8 phases v3)
    - .planning/STATE.md (CRIAR — milestone=v3, current_phase=00)
    - .planning/backlog.md (CRIAR — 6 itens v3.1)
  </files>

  <action>
**Executar em ordem. Cada arquivo é auto-suficiente — escreva conteúdo completo, não diff.**

### 2.1 Criar `.planning/v1-archive/STACK_RESEARCH.md`

Extrair do CLAUDE.md ATUAL (já foi lido em <context>) as seguintes seções e copiá-las INTEGRAIS para o novo arquivo, preservando markdown:
- `## Technology Stack` (linha 23)
- `## Validated Choices (from briefing)` + `### Frontend` table + `### Backend` table + `### Packaging` table (linhas 25–56)
- `## Potential Issues` (1–7) (linhas 57–81)
- `## Recommendations` + `### Adjust These Versions` + `### Packaging Approach` + `### SQLite Is Appropriate for This Use Case` (linhas 82–93)
- `## Confidence Levels` table (linhas 94–105)
- `## Sources` list (linhas 106–126)

Adicionar header no topo do novo arquivo:
```markdown
# Stack Research — v1.0 archive

> Extracted from CLAUDE.md (v1.0) on 2026-05-07 as part of Phase 00 v3 reset.
> Preserved for historical reference. The v3 milestone treats `inicio/map_generator.py`
> + `inicio/licoes/JORNADA_CRIACAO_MAPA.md` as the canonical reference instead.
> See `C:/Users/veio_/.claude/plans/ok-vamos-partir-do-smooth-kurzweil.md` for v3 plan.
```

Acceptance: `grep -c "Validated Choices" .planning/v1-archive/STACK_RESEARCH.md` ≥ 1; `grep -c "Confidence Levels" .planning/v1-archive/STACK_RESEARCH.md` ≥ 1.

### 2.2 REESCREVER `CLAUDE.md` (substituição completa)

**O conteúdo completo a escrever está abaixo entre os marcadores `===CLAUDE_MD_START===` e `===CLAUDE_MD_END===`.** Use o tool `Write` (que sobrescreve) para reescrever o arquivo do zero. Preservar EXATAMENTE os marcadores `<!-- GSD:*-start -->` / `<!-- GSD:*-end -->` para não quebrar `generate-claude-profile`.

```
===CLAUDE_MD_START===
<!-- GSD:project-start source:PROJECT.md -->
## Project

**Medieval Forge — Milestone v3 ("Reset to Roots")**

Medieval Forge is a local web tool for Game Designers that automates the creation of historically-accurate medieval maps for strategy games. The v3 milestone resets the project to focus on faithfully reproducing the canonical 15-stage pipeline documented in `inicio/map_generator.py` (620 lines, fruit of 25 chat iterations) which produced the maps currently shipping in the Reconquista game.

**Core Value:** A Game Designer goes from "country + historical period" to a validated, Unity-ready map package without manual pixel editing — driven by geometry, with LLM as opt-in metadata layer (never required).

### Constraints

- **Tech Stack**: Python 3.11+ / FastAPI / SQLite (backend); React 19 + TypeScript + Vite 6 + Konva.js (frontend)
- **State**: Zustand v5 + zundo 2.3.0 `temporal` middleware; TanStack Query v5
- **Styling**: Tailwind CSS v4 (`@tailwindcss/vite`) + Radix UI Themes 3.x
- **Packaging**: pip-installable Python package; `medieval-forge start` CLI
- **Geometry**: scipy Voronoi + Shapely for boolean ops; GeoJSON storage
- **LLM**: opt-in only — pipeline runs end-to-end with zero LLM calls

> Full historical stack research (versions, gotchas, peer-dep analysis) is preserved in
> `.planning/v1-archive/STACK_RESEARCH.md` and is **not** required for daily v3 work.
<!-- GSD:project-end -->

<!-- GSD:stack-start source:research/STACK.md -->
## v3 Pipeline Contract

The v3 pipeline produces a **12-file Unity export** that must match the Reconquista game's
`Assets/StreamingAssets/Maps/` artifacts byte-for-byte (lookup PNGs) or SSIM ≥ 0.98 (visual PNGs).

**Output contract (12 files):**

| # | File | Resolution | Purpose |
|---|------|-----------|---------|
| 1 | `lookup_barony.png` | 1920×1080 | hit detection (barony) |
| 2 | `lookup_condado.png` | 1920×1080 | hit detection (condado) |
| 3 | `lookup_barony_colors.json` | — | RGB → barony id |
| 4 | `lookup_condado_colors.json` | — | RGB → condado id |
| 5 | `terrain_lookup.png` | 1920×1080 | terrain type per pixel |
| 6 | `terrain_types.json` | — | RGB → `{movement, defense, attack}` |
| 7 | `territory_metadata.json` | — | full hierarchy with `original_idx` |
| 8 | `visual_condado.png` | 3840×2160 | visual placeholder (condado) |
| 9 | `visual_barony.png` | 3840×2160 | visual placeholder (barony) |
| 10 | `mountains_mask.png` | 3840×2160 | white = impassable |
| 11 | `rivers_overlay.png` | 3840×2160 | transparent PNG overlay |
| 12 | `mountain_river_data.json` | — | geo coordinates of mountains/rivers |

**Canonical references (read-only, gold standard):**
- [inicio/map_generator.py](inicio/map_generator.py) — 620-line reference pipeline
- [inicio/licoes/JORNADA_CRIACAO_MAPA.md](inicio/licoes/JORNADA_CRIACAO_MAPA.md) — every decision and bug, documented
- [inicio/territory_data_v3.py](inicio/territory_data_v3.py) — curated territory data for Iberia 868
- `D:\Projetos_Jogo\Reconquista\Assets\StreamingAssets\Maps\*` — ground truth for parity tests

**Non-negotiable rules (lessons paid for in hours of debugging):**

1. **NEAREST upscale only** — never BICUBIC/BILINEAR for lookup PNGs. BICUBIC spreads border colors and breaks Unity's `byOriginalIdx` shader.
2. **σ ∈ [3.0, 4.5]** for the per-territory Gaussian blur in the smoothing stage. Outside this range either fragments territories (too low) or merges unrelated ones (too high).
3. **KD-trees per country** — never a single global KD-tree across PT/ES boundary. Each country gets its own tree built from its own baronies; the PT/ES border polygon (38 points) routes municipalities to the correct tree.
4. **`original_idx` in every territory** — the metadata JSON must include `original_idx` for every condado/barony so Unity's `byOriginalIdx` lookup never throws on indices > 44 (Nájera bug).
5. **`ocean=-1` and `ignore=9999` in the median pass** — these sentinel values are part of the contract; the median filter must skip them, not blur them.
6. **2x masks are independent renders** — `mountains_mask.png` and `rivers_overlay.png` at 3840×2160 are rendered fresh, NOT upscaled from the 1x lookup. Upscaling masks introduces fractional pixels at boundaries that look wrong on the final map.
7. **`byOriginalIdx` on the Unity side** — the game uses `original_idx` as the canonical key; pipeline must guarantee its uniqueness and stability across re-runs.

> The `RegionConfig` pydantic model is the **only** mutable input to the pipeline. Anything else (boundary polygons, terrain rules, render constants) lives as data files under `data/regions/<region>.yaml`.
<!-- GSD:stack-end -->

<!-- GSD:conventions-start source:CONVENTIONS.md -->
## Conventions

- **Module layout** — `backend/medieval_forge/services/pipeline/` houses individually-callable submodules: `config.py`, `landmask.py`, `border.py`, `voronoi.py`, `cleanup.py`, `render.py`, `lookup.py`, `export.py`, `contracts.py`, plus `adapters/` for ingestion translation.
- **Single mutable input** — the only thing the user/UI changes is a `RegionConfig` pydantic instance. No `importlib.reload`, no `sys.modules` patching, no global state.
- **No LLM in the geometric path** — `RESEARCH-*` features populate a sidecar `research_overlay.json` that the export merges if present. The pipeline must produce a valid 12-file export with zero LLM calls.
- **Atomic commits per task** — each `/gsd-execute-phase` task produces ≤1 commit; messages follow `type(phase-plan): subject` convention used in v1 history.
- **Three-layer test pyramid** — every phase delivers (1) `tests/unit/` pytest + vitest, (2) `tests/parity/` non-skippable parity vs. Reconquista (≥85% backend / ≥80% frontend coverage in `v3/`), (3) `tests/uat/playwright/` Playwright UAT scenario for any UI surface.
- **Determinism** — `np.random.default_rng(42)` is locked in `RegionConfig`. Seed changes break parity tests.
<!-- GSD:conventions-end -->

<!-- GSD:architecture-start source:ARCHITECTURE.md -->
## Architecture

The v3 pipeline is a **directed acyclic graph** of 11 stages, each with a `version_token` so Phase 04 (parameter studio) can re-run only downstream stages on slider change:

```
land mask → border → barony assign (Voronoi+KD-tree per country)
         → median cleanup (8 passes: 11,11,9,9,7,7,5,5)
         → smooth (per-mask Gaussian σ ∈ [3.0, 4.5], winner-takes-all)
         → merge (<200px blob removal)
         → hierarchy (barony → condado → duchy → kingdom via constrained clustering)
         → render (paint + tier borders + 2x mask independent + NEAREST upscale + coast outline)
         → lookup (RGB hash deterministic)
         → metadata (territory_metadata.json with original_idx)
         → export (12-file Unity ZIP + manifest)
```

**Data flow:**
- Input: `ProjectDataset` contract (PT-equivalent GeoJSON, ES-equivalent GeoJSON/TopoJSON, optional mountain/river JSON, optional DEM raster)
- Stage caching: in-memory arrays per project, keyed by `(project_id, stage_name, version_token)`
- Output: 12-file ZIP matching the Unity contract above
<!-- GSD:architecture-end -->

<!-- GSD:skills-start source:skills/ -->
## Project Skills

- [karpathy-guidelines](.claude/skills/karpathy/SKILL.md) — Behavioral guidelines to reduce common LLM coding mistakes (think before coding, simplicity first, surgical changes, verifiable success). Auto-discovered from `.claude/skills/karpathy/`.
<!-- GSD:skills-end -->

<!-- GSD:workflow-start source:GSD defaults -->
## GSD Workflow Enforcement

Before using Edit, Write, or other file-changing tools, start work through a GSD command so planning artifacts and execution context stay in sync.

Use these entry points:
- `/gsd-quick` for small fixes, doc updates, and ad-hoc tasks
- `/gsd-debug` for investigation and bug fixing
- `/gsd-execute-phase` for planned phase work

Do not make direct repo edits outside a GSD workflow unless the user explicitly asks to bypass it.
<!-- GSD:workflow-end -->

## What v3 explicitly is NOT

These are **rejected designs** — if a future plan suggests any of them, push back:

- **No LLM-mandatory pipeline.** The v1.0 Phase 3 LLM dependency is gone; LLM is opt-in metadata only.
- **No stepper UI.** The 697-line `frontend/src/pages/ProjectDetail.tsx` stepper is being replaced (Phase 03) with a single-canvas Figma/Mapbox-style workspace.
- **No `sys.modules` patching.** `RegionConfig` is the only mutable input; no `importlib.reload` games.
- **No upscale interpolation.** Lookup PNGs use `Image.NEAREST`, never `BICUBIC` / `BILINEAR`.
- **No global Voronoi.** KD-trees are always per-country; the PT/ES border is data, not assumption.
- **No hand-rolled compound undo.** zundo `temporal` with `partialize` + `diff` is the contract; alternatives need a written justification.

<!-- GSD:profile-start -->
## Developer Profile

> Profile not yet configured. Run `/gsd-profile-user` to generate your developer profile.
> This section is managed by `generate-claude-profile` -- do not edit manually.
<!-- GSD:profile-end -->
===CLAUDE_MD_END===
```

Acceptance: `grep -c "v3 Pipeline Contract" CLAUDE.md` deve retornar `1`; `grep -c "Validated Choices" CLAUDE.md` deve retornar `0`; `grep -c "What v3 explicitly is NOT" CLAUDE.md` deve retornar `1`.

### 2.3 Criar `.planning/PROJECT.md` (visão v3)

```markdown
# Project: Medieval Forge — v3

## Name
Medieval Forge

## Vision
A local web tool for Game Designers that automates the creation of historically-accurate medieval maps for strategy games, faithfully reproducing the canonical 15-stage pipeline documented in `inicio/map_generator.py` and shipping a 12-file Unity export package per project.

## Value Proposition
A Game Designer goes from "country + historical period" to a validated, Unity-ready map package — driven entirely by geometry — without manual pixel editing or blind LLM iteration. LLM research is opt-in metadata only; the pipeline runs end-to-end with zero API calls.

## Constraints
- Python 3.11+ / FastAPI / SQLite (backend), React 19 + Vite 6 + Konva (frontend)
- pip-installable; single `medieval-forge start` CLI
- 12-file Unity export contract (see CLAUDE.md > v3 Pipeline Contract)
- Pixel parity with `D:/Projetos_Jogo/Reconquista/Assets/StreamingAssets/Maps/` for lookup PNGs; SSIM ≥ 0.98 for visual PNGs
- Determinism: `np.random.default_rng(42)` locked in `RegionConfig`

## Out of Scope (v3)
- Multi-language UI (PT-BR only for now)
- Vector border editor with manual point drag
- Heightmap import (SRTM) for paint-brush mountains
- Compound cross-stage undo for parameter studio (deferred from v1 Phase 4)
- Full historical research database for non-Iberian regions (Phase 05 ships geometry only; data deferred to v3.1)
- Kuwahara filter alternative to Gaussian-per-mask smoothing

## Key Decisions
| ID | Decision | Source |
|----|----------|--------|
| D-V3-01 | Archive v1.0 entirely; restart milestone v3 from roots | AskUserQuestion 2026-05-07 |
| D-V3-02 | Ignore the off-PC v2 attempt; do not reconcile | AskUserQuestion 2026-05-07 |
| D-V3-03 | Move `Skill/SKILL-karpathy.md` → `.claude/skills/karpathy/SKILL.md` (auto-discovery) | AskUserQuestion 2026-05-07 |
| D-V3-04 | Delete obsolete v1 routes/stores rather than namespace them | Karpathy #2/#3 — dead code is regression risk |
| D-V3-05 | Treat `inicio/map_generator.py` as gold-standard reference; `RegionConfig` is the only mutable input | Plano master |
| D-V3-06 | "Smooth Kurzweil" term resolved to Gaussian-blur-per-territory + winner-takes-all (σ ∈ [3.0, 4.5]) | `inicio/map_generator.py:469-483` |
| D-V3-07 | LLM research becomes opt-in sidecar (`research_overlay.json`); zero LLM in geometric path | Phase 07 contract |
```

### 2.4 Criar `.planning/ROADMAP.md` (8 phases v3)

```markdown
# Roadmap: Medieval Forge v3

## Overview

Eight phases reset Medieval Forge to its roots. Phase 00 archives v1.0 and bootstraps v3 infrastructure (CI, planning docs, skill move). Phase 01 ports `inicio/map_generator.py` as a parametrized library and proves byte-equivalence with the Reconquista exports. Phase 02 wires the existing v1 ingestion into the new pipeline contract. Phase 03 replaces the v1 stepper UI with a single-canvas read-only workspace. Phase 04 turns that canvas into a parameter studio with live re-render. Phase 05 generalizes Iberia from hard-coded to config-driven. Phase 06 adds export validation gates. Phase 07 reintroduces LLM as opt-in metadata.

## Phases

### Phase 00: Archive v1.0 + bootstrap v3 infrastructure
**Goal:** v3 starts from a clean base without breaking the working v1 server.
**Depends on:** Nothing
**Status:** in-progress
**Success criteria:**
1. `git tag --list` contains `v1.0-archive`
2. `.planning/v1-archive/` contains 5 archived planning docs + `STACK_RESEARCH.md`
3. CLAUDE.md reflects v3 (Pipeline Contract + Architecture + Conventions + What v3 is NOT)
4. `.claude/skills/karpathy/SKILL.md` exists and is auto-discoverable
5. `.github/workflows/ci.yml` exists with 4 jobs; pytest markers + frontend e2e script in place
6. `medieval-forge start` still boots and serves 200 OK on `/`

### Phase 01: Pipeline parity (port + harness together)
**Goal:** Port `inicio/map_generator.py` as a deterministic, parametrized library and prove byte-equivalence with the Reconquista exports for Iberia 868.
**Depends on:** Phase 00
**Status:** planned
**Success criteria:**
1. `pytest tests/parity/test_iberia_868.py` passes (pixel-perfect for lookup PNGs; SSIM ≥ 0.98 for visual PNGs; deep-equal for JSONs)
2. Pipeline runs standalone (`python -m medieval_forge.services.pipeline --region iberia_868 --out /tmp/out`) without FastAPI
3. CI blocks merges on parity break (non-skippable from this phase forward)

### Phase 02: Ingestion adapter
**Goal:** The existing v1 ingestion (Wikidata/OSM/Overpass/DEM/HydroSHEDS) produces inputs that the v3 pipeline accepts unchanged.
**Depends on:** Phase 01
**Status:** planned
**Success criteria:**
1. Phase 01 parity test stays green when input is "live ingestion" instead of fixture snapshot
2. `services/pipeline/contracts.py` defines `ProjectDataset` consumed by both fixture and live paths
3. Adapter functions wrap (don't rewrite) `ingest_wikidata`, `ingest_osm`, `overpass_client`, `ingest_terrain`

### Phase 03: Read-only canvas redesign
**Goal:** Single-canvas Figma/Mapbox workspace replaces the v1 stepper (697 lines), but read-only.
**Depends on:** Phase 02
**Status:** planned
**Success criteria:**
1. v3 user opens project generated by Phase 01 and pans/zooms/clicks territories
2. Inspector populates on click; layer toggles work
3. Old stepper invisible; no console errors
4. Runs against Phase 01 artifacts directly

### Phase 04: Parameter studio (live re-render)
**Goal:** The same canvas drives the pipeline. Sliders for σ, median passes, fragment threshold, blob-merge threshold; toggles for per-stage outputs; incremental re-render.
**Depends on:** Phase 03
**Status:** planned
**Success criteria:**
1. Explicit DAG with `version_token` per stage drawn BEFORE first slider (Karpathy: avoid v1's compound-undo gap)
2. Backend incremental endpoint per stage; in-memory cache of intermediate arrays
3. Move σ from 3.0 → 4.5 reformats territories visibly in <500ms without full re-run
4. Cancel restores prior state; Konva `clearCache()` after every geometric mutation

### Phase 05: Region generalization
**Goal:** Iberia is a config, not a hard-coded path. Other regions/periods supported.
**Depends on:** Phase 04
**Status:** planned
**Success criteria:**
1. `data/regions/iberia_868.yaml` externalizes the config currently in code
2. `france_1066.yaml` + `england_1216.yaml` ship as templates (geometry only — historical research deferred to v3.1)
3. France 1066 with toy synthetic dataset → ingest → generate → export produces 12 well-formed files (parity to Reconquista NOT required; file contract IS)

### Phase 06: Export contract + validation gate (merged)
**Goal:** Strict 12-file Unity export with manifest, schema validation, and a gate on minimum pixels per territory + color-collision check.
**Depends on:** Phase 05
**Status:** planned
**Success criteria:**
1. All JSON outputs schema-validated via pydantic
2. Export blocked on: territory <200px, lookup color collision, ocean leak, missing `original_idx`, `pixel_center` Y-axis check failure
3. Manifest matches Reconquista structure
4. Iberia + France + a deliberately-broken project all pass through `/api/v3/export`; broken is blocked with structured error list

### Phase 07: LLM research as opt-in metadata layer
**Goal:** Optional research dialog populates `name`, `kingdom_owner`, `historical_notes` on geometrically-fixed territories. Never mandatory.
**Depends on:** Phase 06
**Status:** planned
**Success criteria:**
1. Project without API key generates and exports successfully (zero LLM calls)
2. With research run, territories show historical names instead of `Condado_001`
3. `services/research_runner.py`, `services/llm/`, `services/research_cache.py`, `components/research/` are reused (moved into `v3/` namespace)

---

## Requirement coverage (v3)

| Requirement ID | Phase | Notes |
|----------------|-------|-------|
| V3-PHASE-00 | Phase 00 | This phase (covered by quick task 260507-g1v) |
| V3-PIPELINE-PARITY | Phase 01 | Pixel parity vs. Reconquista |
| V3-INGEST-ADAPTER | Phase 02 | v1 ingestion → v3 contract |
| V3-CANVAS-RO | Phase 03 | Read-only single-canvas |
| V3-CANVAS-PARAM | Phase 04 | Live parameter studio |
| V3-REGION-CONFIG | Phase 05 | Iberia → config |
| V3-EXPORT-GATE | Phase 06 | 12-file validation gate |
| V3-LLM-OPT-IN | Phase 07 | Sidecar metadata |

---

## Backlog (v3.1 — deferred, not discarded)

See `.planning/backlog.md`.
```

### 2.5 Criar `.planning/STATE.md`

```markdown
---
gsd_state_version: 1.0
milestone: v3
milestone_name: reset-to-roots
status: in-progress
stopped_at: Phase 00 — archive + bootstrap (quick task 260507-g1v)
last_updated: "2026-05-07T00:00:00.000Z"
last_activity: 2026-05-07
progress:
  total_phases: 8
  completed_phases: 0
  total_plans: 1
  completed_plans: 0
  percent: 0
---

# Project State (v3)

## Project Reference

See: .planning/PROJECT.md

**Core value:** A Game Designer goes from "country + historical period" to a validated, Unity-ready map package — driven by geometry, with LLM as opt-in metadata.
**Current focus:** Phase 00 — archive v1 + bootstrap v3 infrastructure.

## Current Position

Phase: 00 — archive v1.0 + bootstrap v3 (in-progress)
Plan: quick/260507-g1v-phase-00-v3-archive-milestone-reset
Status: planning complete, executing
Last activity: 2026-05-07 — Started Phase 00

Progress: [░░░░░░░░░░] 0% (0 of 8 phases complete)

## Accumulated Context

### Decisions
See `.planning/PROJECT.md` Key Decisions table (D-V3-01 through D-V3-07).

### Blockers/Concerns
- v1 archive holds the lessons that v3 must NOT relearn. Before Phase 01 planning, re-read `.planning/v1-archive/STATE.md` for the 30+ pitfalls discovered during v1.0.
- 6 stale worktrees were removed in Phase 00 — verify `git worktree list` returns 1 line before any future agent work.

### Pending Todos
None yet.

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
|   |             |      |        |           |

## Session Continuity

Last session: 2026-05-07T00:00:00.000Z
Stopped at: Phase 00 in progress
Resume file: None
```

### 2.6 Criar `.planning/backlog.md` (6 itens v3.1)

```markdown
# Backlog — v3.1 (deferred, not discarded)

Items captured during v3 planning that are deliberately deferred to v3.1 to keep the v3 milestone shippable. Promote with `/gsd-review-backlog` when v3 is in maintenance mode.

| # | Item | Why deferred | Source |
|---|------|--------------|--------|
| 1 | Full historical research database for France 1066 / England 1216 | Phase 05 ships geometry only; historical data is a research project, not a coding task | Plano master, Phase 05 DoD |
| 2 | Evaluate Kuwahara filter as alternative to Gaussian-per-mask smoothing | "Smooth Kurzweil" investigation; current Gaussian σ ∈ [3.0, 4.5] is good enough for v3 | Plano master, Context |
| 3 | Compound undo cross-stage (Ctrl+Z for parameter studio) | Inherited gap from v1 Phase 4; needs DAG version-token design first (Phase 04 builds the foundation but does not implement undo) | Plano master, Phase 04 risks |
| 4 | Vector border editor (drag points to manually refine borders) | Out of scope for v3; pipeline is geometry-driven, manual edits would compete with re-runs | Plano master, Bloco 6 |
| 5 | Heightmap import (SRTM) for mountain paint-brush | Phase 5 v1 was advanced canvas editing; v3 paint stays on terrain types only, no heightmap | Plano master, Bloco 6 |
| 6 | Multi-language UI (currently PT-BR) | Single-user local tool; i18n is premature optimization | Plano master, Bloco 6 |
```

### 2.7 Verificações pós-arquivos

```bash
grep -c "v3 Pipeline Contract"           CLAUDE.md           # esperado: 1
grep -c "Validated Choices"              CLAUDE.md           # esperado: 0
grep -c "What v3 explicitly is NOT"      CLAUDE.md           # esperado: 1
grep -c "Validated Choices"              .planning/v1-archive/STACK_RESEARCH.md   # esperado: ≥1
grep -cE "^### Phase 0[0-7]:"            .planning/ROADMAP.md  # esperado: 8
grep -c "milestone: v3"                  .planning/STATE.md  # esperado: 1
grep -c "Kuwahara"                       .planning/backlog.md  # esperado: ≥1
```

### 2.8 Commit
```bash
git add CLAUDE.md .planning/PROJECT.md .planning/ROADMAP.md .planning/STATE.md .planning/backlog.md .planning/v1-archive/STACK_RESEARCH.md
git commit -m "docs(v3): rewrite CLAUDE.md and seed v3 planning docs

- Extract Validated Choices/Potential Issues/Recommendations/Confidence/Sources to .planning/v1-archive/STACK_RESEARCH.md
- Rewrite CLAUDE.md: v3 Pipeline Contract (12-file Unity export + 7 non-negotiable rules) + Architecture (DAG) + Conventions + What v3 is NOT
- Seed v3 PROJECT.md (vision/value/constraints/out-of-scope/decisions D-V3-01..07)
- Seed v3 ROADMAP.md (8 phases 00-07 with success criteria)
- Seed v3 STATE.md (milestone=v3, current_phase=00)
- Seed backlog.md with 6 deferred items (research, Kuwahara, compound undo, vector editor, heightmap, i18n)

Refs: C:/Users/veio_/.claude/plans/ok-vamos-partir-do-smooth-kurzweil.md (CLAUDE.md reescrita necessária)"
```
  </action>

  <verify>
    <automated>
cd /d/Projetos_Jogo/Medieval_Forge && \
test -f .planning/v1-archive/STACK_RESEARCH.md && \
test -f .planning/PROJECT.md && \
test -f .planning/ROADMAP.md && \
test -f .planning/STATE.md && \
test -f .planning/backlog.md && \
[ "$(grep -c 'v3 Pipeline Contract' CLAUDE.md)" = "1" ] && \
[ "$(grep -c 'Validated Choices' CLAUDE.md)" = "0" ] && \
[ "$(grep -c 'What v3 explicitly is NOT' CLAUDE.md)" = "1" ] && \
[ "$(grep -c 'Validated Choices' .planning/v1-archive/STACK_RESEARCH.md)" -ge "1" ] && \
[ "$(grep -cE '^### Phase 0[0-7]:' .planning/ROADMAP.md)" = "8" ] && \
grep -q "milestone: v3" .planning/STATE.md && \
grep -q "Kuwahara" .planning/backlog.md && \
echo "TASK2_OK"
    </automated>
  </verify>

  <done>
- `.planning/v1-archive/STACK_RESEARCH.md` existe com tabelas v1 preservadas
- `CLAUDE.md` reescrito sem Validated Choices/Potential Issues/Confidence/Sources mas com `v3 Pipeline Contract`, `Architecture`, `Conventions`, `What v3 explicitly is NOT`, e Skills apontando para `.claude/skills/karpathy/SKILL.md`
- `.planning/PROJECT.md`, `ROADMAP.md` (8 phases), `STATE.md` (milestone=v3), `backlog.md` (6 itens) criados
- 1 commit `docs(v3): rewrite CLAUDE.md and seed v3 planning docs` no HEAD
  </done>
</task>

<!-- ============================================================ -->
<!-- TASK 3 — CI bootstrap + V1 deletion candidates list           -->
<!-- Commit: chore(v3): bootstrap CI scaffolding and remove v1 obsolete routes -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task 3: Bootstrap CI workflow, register pytest markers, add e2e:playwright script, list v1 deletion candidates</name>

  <files>
    - .github/workflows/ci.yml (CRIAR)
    - pyproject.toml (EDITAR — adicionar markers parity/integration/uat ao bloco existente; preservar marker `slow`)
    - frontend/package.json (EDITAR — adicionar script `e2e:playwright` apontando para `playwright test`)
    - .planning/quick/260507-g1v-phase-00-v3-archive-milestone-reset/V1_DELETION_CANDIDATES.md (CRIAR)
  </files>

  <action>
**Política de deleção (decisão D-V3-04 confirmada): SER CONSERVADOR.** Em vez de deletar agressivamente código v1 nesta task, listar candidatos em `V1_DELETION_CANDIDATES.md` para review humano. Phase 02 e Phase 07 do v3 ainda vão reusar bastante código (ingest_*, llm/, research_*). Deletar agora arrisca quebrar reuso planejado.

### 3.1 Criar `.github/workflows/ci.yml`

Conteúdo exato:

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  pytest-unit:
    name: pytest unit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install backend deps
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev]
      - name: Run unit tests
        # Coverage gate starts at 60% for Phase 00; Phase 01 raises to 85%.
        run: pytest backend/tests/unit/ -v --cov=medieval_forge --cov-fail-under=60

  pytest-parity:
    name: pytest parity + integration
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install backend deps
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev]
      - name: Run parity + integration tests
        # Non-skippable from Phase 01 forward. Phase 00 may have no parity tests yet (will pass empty).
        run: pytest backend/tests/parity/ backend/tests/integration/ -v -m "parity or integration" --no-header || (echo "No parity/integration tests yet (Phase 00) — passing as placeholder"; exit 0)

  vitest:
    name: vitest
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm run test -- --run

  playwright-uat:
    name: playwright UAT
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium
      - run: npm run e2e:playwright
        # Phase 00: smoke test scaffold may not exist yet — script returns 0 if no tests configured.
```

`mkdir -p .github/workflows` antes de criar o arquivo se necessário.

### 3.2 Editar `pyproject.toml` — estender markers

Em `pyproject.toml`, no bloco existente `[tool.pytest.ini_options]` (linhas ~67-73), o array `markers` atualmente contém apenas `slow`. Substituir o bloco `markers = [...]` por:

```toml
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "parity: parity tests against Reconquista exports (non-skippable from Phase 01)",
    "integration: integration tests across services",
    "uat: user acceptance tests via Playwright",
]
```

NÃO alterar `asyncio_mode`, `testpaths`, ou `pythonpath` no bloco — preservar.

Verify imediato: `python -c "import tomllib; print(tomllib.loads(open('pyproject.toml','rb').read().decode())['tool']['pytest']['ini_options']['markers'])"` deve listar 4 markers.

### 3.3 Editar `frontend/package.json` — adicionar `e2e:playwright`

O `package.json` atual já tem `test:e2e: "playwright test"`. Adicionar um alias `e2e:playwright` que aponta para o mesmo comando (a constraint pediu o nome exato `e2e:playwright`). Inserir no bloco `"scripts"` entre `"test:e2e"` e `"test:e2e:update"`:

```json
"e2e:playwright": "playwright test",
```

Resultado final do bloco scripts (ordem preservada):
```json
"scripts": {
  "dev": "vite",
  "build": "tsc -b && vite build",
  "preview": "vite preview",
  "test": "vitest",
  "test:e2e": "playwright test",
  "e2e:playwright": "playwright test",
  "test:e2e:update": "playwright test --update-snapshots"
}
```

NÃO instalar nada novo (`@playwright/test` e `vitest` já estão em devDependencies — verificado).

### 3.4 Identificar candidatos a deleção e criar `V1_DELETION_CANDIDATES.md`

Rodar greps de pesquisa (informacionais — NÃO deletar):

```bash
# 1. codex_runner.py — usado por que outros módulos?
grep -rn "codex_runner\|from.*codex" backend/ --include="*.py" | grep -v __pycache__ | head -30

# 2. ProjectDetail.tsx (697 linhas, stepper) — referenciado por?
grep -rn "ProjectDetail" frontend/src/ --include="*.ts" --include="*.tsx" | head -30

# 3. Stepper / pipeline UI — quem usa?
grep -rn "Stepper\|StepCard\|usePipelineStore" frontend/src/ --include="*.ts" --include="*.tsx" | head -20

# 4. Confirmar reuso planejado — NÃO mexer:
ls backend/medieval_forge/services/{ingest_wikidata,ingest_osm,overpass_client}.py
ls backend/medieval_forge/services/llm/
ls backend/medieval_forge/services/research_runner.py backend/medieval_forge/services/research_cache.py
```

Criar `.planning/quick/260507-g1v-phase-00-v3-archive-milestone-reset/V1_DELETION_CANDIDATES.md`:

```markdown
# v1 Deletion Candidates — Phase 00 Review

> **Policy (D-V3-04, applied conservatively):** delete obsolete v1 code rather than namespace it.
> But Phase 00 does NOT delete — it lists candidates for human review. Phase 02 (ingest adapter)
> and Phase 07 (LLM opt-in) reuse a lot of v1 services; deleting now risks breaking planned reuse.
>
> **Action required:** review this list with the user before Phase 03 (canvas redesign). Deletions
> happen as part of the phase that replaces the obsolete code, not Phase 00.

## Confirmed candidates (DELETE in Phase 03 or later)

| Path | Lines | Reason | Replaced by | Phase |
|------|-------|--------|-------------|-------|
| `frontend/src/pages/ProjectDetail.tsx` | 697 | v1 stepper UI; v3 uses single-canvas Figma-style workspace | New `frontend/src/v3/pages/ProjectWorkspace.tsx` | Phase 03 |
| `frontend/src/components/pipeline/Stepper.tsx` | ~? | Stepper component — only used by ProjectDetail | (nothing — no stepper in v3) | Phase 03 |
| `frontend/src/components/pipeline/StepCard.tsx` | ~? | Stepper card component | (nothing — no stepper in v3) | Phase 03 |
| `frontend/src/stores/usePipelineStore.ts` | ~? | Stepper state | (nothing — pipeline state lives in v3 ParameterPanel) | Phase 04 |

## Possible candidates (REVIEW before deletion)

| Path | Used by | Decision |
|------|---------|----------|
| `backend/medieval_forge/services/codex_runner.py` | grep result: __FILL_FROM_GREP__ | If only used by v1 LLM-mandatory pipeline → delete in Phase 07 (when LLM moves to opt-in). If reused → keep. |
| `backend/medieval_forge/services/territory_builder.py` | v1 territories.geojson generator | Keep until Phase 02.2 v3 (geometry-first) ports the logic; then delete. |
| `backend/medieval_forge/services/territories_geojson.py` | v1 frontend adapter | Keep until Phase 02 wraps it in `pipeline/adapters/`; then maybe delete. |
| `backend/medieval_forge/lib/map_generator.py` (if exists) | v1 wrapper around `inicio/map_generator.py` | DO NOT DELETE — Phase 01 ports this to `services/pipeline/`. Phase 01 may delete the v1 wrapper after parity is proven. |

## DO NOT delete (reused by v3)

| Path | Why reused | Phase that uses it |
|------|------------|---------------------|
| `backend/medieval_forge/services/ingest_wikidata.py` | Wikidata SPARQL ingestion | Phase 02 (wraps it in adapter) |
| `backend/medieval_forge/services/ingest_osm.py` | OSM Overpass ingestion | Phase 02 |
| `backend/medieval_forge/services/overpass_client.py` | Shared 3-mirror Overpass client | Phase 02 |
| `backend/medieval_forge/services/ingest_terrain/` | DEM + ridges + HydroSHEDS pipeline | Phase 02 |
| `backend/medieval_forge/services/llm/` | Multi-provider LLM adapter (Claude/OpenAI/Gemini/Ollama) | Phase 07 (opt-in) |
| `backend/medieval_forge/services/research_runner.py` | LLM research orchestration | Phase 07 (opt-in) |
| `backend/medieval_forge/services/research_cache.py` | SQLite cache for research | Phase 07 (opt-in) |
| `backend/medieval_forge/services/credential_store.py` | In-memory credential store | Phase 07 (opt-in) |
| `frontend/src/components/research/` | Research UI dialogs | Phase 07 (opt-in, moved to `v3/`) |
| `backend/medieval_forge/db.py` + `models.py` + `auth/` | Project CRUD foundation | All v3 phases |

## Greps captured during this Phase 00 task (for the record)

(Paste actual `grep -rn` outputs here from the commands in section 3.4. Do this BEFORE writing the task 3 commit so the file is committed with real data, not placeholders.)
```

**Critical:** rode os greps reais e cole as saídas no final do markdown ANTES do commit. Não commite com `__FILL_FROM_GREP__`.

### 3.5 Smoke test que servidor ainda boota

```bash
# (Em foreground, com timeout — para não travar o task em background)
medieval-forge start &
SERVER_PID=$!
sleep 5
HTTP=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8765/ || echo "000")
kill $SERVER_PID 2>/dev/null
wait $SERVER_PID 2>/dev/null
echo "HTTP_STATUS=$HTTP"
```

Aceitação: `$HTTP` ∈ {200, 304} (304 if static cache headers).
Se diferente: imprimir últimas 50 linhas do log do uvicorn e abortar — Phase 00 não pode quebrar o servidor.

### 3.6 Commit
```bash
git add .github/workflows/ci.yml pyproject.toml frontend/package.json .planning/quick/260507-g1v-phase-00-v3-archive-milestone-reset/V1_DELETION_CANDIDATES.md
git commit -m "chore(v3): bootstrap CI scaffolding and list v1 deletion candidates

- Add .github/workflows/ci.yml: 4 parallel jobs (pytest-unit with cov-fail-under=60, pytest-parity+integration non-skippable from Phase 01, vitest, playwright-uat)
- Extend pyproject.toml [tool.pytest.ini_options] markers: parity, integration, uat (preserve slow)
- Add frontend/package.json script: e2e:playwright -> playwright test (alias of test:e2e per master plan naming)
- Create V1_DELETION_CANDIDATES.md (conservative — no deletion in Phase 00; deletions happen in the phase that replaces the obsolete code)
- Smoke-tested medieval-forge start: 200 OK on http://localhost:8765/

Refs: C:/Users/veio_/.claude/plans/ok-vamos-partir-do-smooth-kurzweil.md (Bloco 2 — CI bootstrap)"
```
  </action>

  <verify>
    <automated>
cd /d/Projetos_Jogo/Medieval_Forge && \
test -f .github/workflows/ci.yml && \
grep -q "pytest-unit:" .github/workflows/ci.yml && \
grep -q "pytest-parity:" .github/workflows/ci.yml && \
grep -q "vitest:" .github/workflows/ci.yml && \
grep -q "playwright-uat:" .github/workflows/ci.yml && \
grep -q '"parity:' pyproject.toml && \
grep -q '"integration:' pyproject.toml && \
grep -q '"uat:' pyproject.toml && \
grep -q '"slow:' pyproject.toml && \
grep -q '"e2e:playwright"' frontend/package.json && \
test -f .planning/quick/260507-g1v-phase-00-v3-archive-milestone-reset/V1_DELETION_CANDIDATES.md && \
! grep -q "__FILL_FROM_GREP__" .planning/quick/260507-g1v-phase-00-v3-archive-milestone-reset/V1_DELETION_CANDIDATES.md && \
echo "TASK3_OK"
    </automated>
  </verify>

  <done>
- `.github/workflows/ci.yml` existe com 4 jobs nomeados (pytest-unit, pytest-parity, vitest, playwright-uat) e triggers push/pull_request to main
- `pyproject.toml` tem 4 markers: slow, parity, integration, uat
- `frontend/package.json` tem script `e2e:playwright` (apontando para `playwright test`)
- `V1_DELETION_CANDIDATES.md` existe sem placeholders, com greps reais coladas e seções "Confirmed", "Possible (REVIEW)", "DO NOT delete"
- `medieval-forge start` retorna 200 em smoke test
- 1 commit `chore(v3): bootstrap CI scaffolding...` no HEAD
  </done>
</task>

</tasks>

<verification>
Após todas as 3 tasks, verificar end-to-end:

```bash
cd /d/Projetos_Jogo/Medieval_Forge

# 1. Tag + archive
git tag --list | grep -qx "v1.0-archive"
ls .planning/v1-archive/{ROADMAP,PROJECT,REQUIREMENTS,STATE,SESSION-NOTES,STACK_RESEARCH}.md

# 2. Skill movida
test -f .claude/skills/karpathy/SKILL.md
test ! -d Skill

# 3. Worktrees limpos
[ "$(git worktree list | wc -l)" = "1" ]

# 4. CLAUDE.md reescrito
grep -c "v3 Pipeline Contract" CLAUDE.md  # =1
grep -c "Validated Choices" CLAUDE.md      # =0

# 5. Planning v3
test -f .planning/PROJECT.md
test -f .planning/ROADMAP.md
test -f .planning/STATE.md
test -f .planning/backlog.md

# 6. CI scaffolding
test -f .github/workflows/ci.yml
grep -q '"parity:' pyproject.toml
grep -q '"e2e:playwright"' frontend/package.json

# 7. Deletion candidates documentado
test -f .planning/quick/260507-g1v-phase-00-v3-archive-milestone-reset/V1_DELETION_CANDIDATES.md

# 8. Servidor ainda boota
medieval-forge start &
SERVER_PID=$!; sleep 5
HTTP=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8765/)
kill $SERVER_PID; wait $SERVER_PID 2>/dev/null
[ "$HTTP" = "200" ] || [ "$HTTP" = "304" ]

# 9. 3 commits Phase 00 acima da tag
[ "$(git rev-list v1.0-archive..HEAD --count)" = "3" ]

echo "PHASE_00_VERIFIED"
```
</verification>

<success_criteria>
1. **Tag git `v1.0-archive`** existe apontando para o commit anterior ao primeiro commit desta task
2. **`.planning/v1-archive/`** contém os 5 arquivos v1 + STACK_RESEARCH.md + diretório `phases/`
3. **`.claude/skills/karpathy/SKILL.md`** existe; `Skill/` removido
4. **`git worktree list`** retorna 1 linha (só main)
5. **`CLAUDE.md`** contém exatamente 1 ocorrência de "v3 Pipeline Contract" e 0 de "Validated Choices"; tem seções Project, Constraints, v3 Pipeline Contract, Conventions, Architecture, Project Skills (apontando para `.claude/skills/karpathy/SKILL.md`), GSD Workflow Enforcement, What v3 explicitly is NOT, Developer Profile
6. **`.planning/PROJECT.md`** com Vision/Value/Constraints/Out-of-Scope/Key Decisions D-V3-01..07
7. **`.planning/ROADMAP.md`** com 8 phases (00 in-progress, 01–07 planned), success criteria por phase, mapping de requirement IDs
8. **`.planning/STATE.md`** com `milestone: v3`, `current_phase: 00`, `last_activity: 2026-05-07`
9. **`.planning/backlog.md`** com 6 itens (research histórica, Kuwahara, compound undo, vector editor, heightmap, i18n)
10. **`.github/workflows/ci.yml`** com 4 jobs paralelos (`pytest-unit` + cov-fail-under=60, `pytest-parity` non-skippable, `vitest`, `playwright-uat`); triggers push + PR para main; setup-python 3.11, setup-node 20
11. **`pyproject.toml`** com markers parity/integration/uat (slow preservado)
12. **`frontend/package.json`** com script `e2e:playwright`
13. **`V1_DELETION_CANDIDATES.md`** existe sem placeholders, listando candidatos confirmados (4 frontend), possíveis (review humano), e DO-NOT-DELETE (reuso planejado)
14. **`medieval-forge start`** retorna 200 OK em http://localhost:8765/ (servidor não quebrou)
15. **3 commits** entre `v1.0-archive` e `HEAD` (um por task, ordem fixa)
</success_criteria>

<output>
Após completar, criar `.planning/quick/260507-g1v-phase-00-v3-archive-milestone-reset/SUMMARY.md` com:
- Resumo das 3 tasks executadas
- Lista de arquivos criados/movidos/editados
- Tag git criada (v1.0-archive)
- 3 commits SHAs
- Saída do smoke test (HTTP 200)
- Próximo passo: `/gsd-discuss-phase 01` para iniciar Phase 01 (Pipeline parity)
</output>
