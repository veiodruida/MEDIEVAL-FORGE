---
phase: quick-260422-f0s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
autonomous: true
requirements:
  - QUICK-260422-f0s
must_haves:
  truths:
    - "`anthropic` is a declared runtime dependency in pyproject.toml"
    - "`anthropic` is installed in the active environment and importable"
    - "Running `medieval-forge start` (or equivalent backend import) no longer raises ModuleNotFoundError for `anthropic`"
  artifacts:
    - path: "pyproject.toml"
      provides: "anthropic SDK pinned in [project].dependencies"
      contains: "anthropic>="
  key_links:
    - from: "pyproject.toml dependencies"
      to: "installed site-packages"
      via: "pip install -e ."
      pattern: "anthropic"
---

<objective>
Add the Anthropic Python SDK as a runtime dependency so the backend startup path (which imports `anthropic` for the Claude LLM adapter) stops failing with `ModuleNotFoundError: No module named 'anthropic'`.

Purpose: Phase 3 (LLM Research Integration) scaffolding already imports `anthropic` at startup; currently the package is missing from `pyproject.toml`, so fresh installs crash. Pinning it now fixes the CLI and aligns the declared dependency set with actual imports.

Output: Updated `pyproject.toml` with `anthropic` pinned at `>=0.94,<1.0` (per CLAUDE.md validated stack; current stable is 0.94.1), the package installed into the dev env, and a verified clean import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@pyproject.toml

<!-- Precedent: quick task 260422-eue added google-auth-oauthlib with the same shape (pin + install + verify). Follow that pattern. -->
<!-- CLAUDE.md (Technology Stack / Backend) confirms: Anthropic Python SDK current stable 0.94.1, full async via AsyncAnthropic, no gotchas. Pin as `>=0.94,<1.0`. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin anthropic in pyproject.toml and install</name>
  <files>pyproject.toml</files>
  <action>
    Edit `pyproject.toml` and add `"anthropic>=0.94,<1.0",` to the `[project].dependencies` array. Place it immediately after the `"httpx>=0.27,<0.30",` line (keeping the existing loose grouping of HTTP/LLM-adjacent deps together, above `"google-auth-oauthlib"`). Do NOT modify any other dependency or pin. Do NOT add it to `[project.optional-dependencies]`.

    Then install the updated dependency set into the active environment:
    - Run `pip install -e .` from the repo root so the editable install picks up the new dep.
    - If pip reports conflicts, capture the error and stop — do not force-resolve.

    Rationale for the pin: CLAUDE.md validated-stack table lists Anthropic Python SDK stable at 0.94.1 with no migration issues. A `>=0.94,<1.0` range matches the conservative caret style already used for other deps (httpx, click, shapely) and avoids locking into a single patch release.
  </action>
  <verify>
    <automated>python -c "import anthropic; from anthropic import AsyncAnthropic; print(anthropic.__version__)"</automated>
  </verify>
  <done>
    - `pyproject.toml` contains `"anthropic>=0.94,<1.0",` inside `[project].dependencies`.
    - `pip install -e .` completes without errors.
    - `python -c "import anthropic; from anthropic import AsyncAnthropic"` exits 0 and prints a 0.94.x version.
    - No other lines in `pyproject.toml` changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Smoke-test backend startup import chain</name>
  <files></files>
  <action>
    Verify the original failure path is resolved. From the repo root:

    1. Run `python -c "from medieval_forge.cli import cli"` — this is the CLI entry point (`[project.scripts]`) and transitively imports the LLM adapter that was failing.
    2. If the backend has a known LLM adapter module (e.g. `medieval_forge.services.llm` or similar), also run `python -c "import medieval_forge; import importlib; [importlib.import_module(m) for m in ('medieval_forge',)]"` as a lightweight sanity check. If a dedicated anthropic-using module path is discoverable via `grep -r "import anthropic" backend/`, import that module directly too.

    Both invocations MUST succeed with exit code 0 and emit no `ModuleNotFoundError`. If any other missing dependency surfaces, STOP and report it — do not fix unrelated missing deps in this quick task (open a separate quick task), matching the scope discipline of the 260422-eue precedent.
  </action>
  <verify>
    <automated>python -c "from medieval_forge.cli import cli; print('cli import ok')"</automated>
  </verify>
  <done>
    - `from medieval_forge.cli import cli` succeeds without ModuleNotFoundError for `anthropic`.
    - Any anthropic-importing backend module imports cleanly in isolation.
    - No unrelated dependencies were modified.
  </done>
</task>

</tasks>

<verification>
Post-plan checks (both must pass):
1. `grep -n "anthropic" pyproject.toml` shows exactly one new line in `[project].dependencies` with the `>=0.94,<1.0` pin.
2. `python -c "import anthropic; from anthropic import AsyncAnthropic; from medieval_forge.cli import cli"` exits 0.
</verification>

<success_criteria>
- `anthropic>=0.94,<1.0` is a runtime dependency in `pyproject.toml`.
- The package is installed and importable, including `AsyncAnthropic`.
- `medieval_forge.cli` imports cleanly — the original ModuleNotFoundError is gone.
- No other dependency pins were touched.
</success_criteria>

<output>
After completion, create `.planning/quick/260422-f0s-add-anthropic-sdk-to-pyproject-toml-depe/260422-f0s-SUMMARY.md` summarizing:
- Exact line added to `pyproject.toml`
- Installed anthropic version (from `pip show anthropic`)
- Verification command output
- STATE.md "Quick Tasks Completed" row to append
</output>
