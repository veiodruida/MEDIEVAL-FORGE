# Quick Task 260422-f6f: Add all missing LLM provider deps to pyproject.toml

## Objective
Add google-auth-oauthlib, google-genai, ollama, openai to pyproject.toml [project].dependencies and install.

## Tasks
1. Edit pyproject.toml — add 4 missing packages after anthropic entry
2. Run pip install -e . — verify all imports resolve
