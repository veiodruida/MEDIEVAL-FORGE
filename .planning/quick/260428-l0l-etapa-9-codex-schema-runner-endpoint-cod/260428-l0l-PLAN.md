---
quick_id: 260428-l0l
type: execute
mode: quick
wave: 1
depends_on: []
autonomous: true
files_modified:
  - backend/medieval_forge/services/llm/schemas.py
  - backend/medieval_forge/services/llm/prompt.py
  - backend/medieval_forge/models.py
  - alembic/versions/0003_create_codex_cache.py
  - backend/medieval_forge/services/codex_cache.py
  - backend/medieval_forge/services/codex_runner.py
  - backend/medieval_forge/api/codex.py
  - backend/medieval_forge/main.py
  - backend/tests/services/test_codex_schema.py
  - backend/tests/services/test_codex_prompt.py
  - backend/tests/services/test_codex_runner.py
  - backend/tests/api/test_codex_endpoints.py
requirements:
  - HAZY-9-CODEX-SCHEMA
  - HAZY-9-CODEX-RUNNER
  - HAZY-9-CODEX-API
must_haves:
  truths:
    - "CodexResult Pydantic schema validates a 12-category medieval narrative payload (currency, attributes, health, traits, feudal, politics, dynasty, religion, culture, economy, military, events) with extra='forbid'."
    - "build_codex_prompt produces a prompt string mentioning the country, period range, all 12 category headers, and a JSON example matching CodexResult shape."
    - "CodexCache table exists in models.py + alembic migration; codex_cache.py exposes compute/get/set helpers analogous to research_cache.py."
    - "codex_runner.run_codex orchestrates: load project → build prompt → cache lookup → run_with_retry → cache result, emitting SSE strings to an asyncio.Queue and ALWAYS terminating with None sentinel."
    - "POST /api/projects/{id}/codex streams SSE chunks ending with 'data: DONE'; GET /api/projects/{id}/codex/cached returns 200 with the cached payload or 404 when missing."
    - "All ~12 new tests pass; existing 246 backend tests + 191 frontend tests do not regress."
  artifacts:
    - path: "backend/medieval_forge/services/llm/schemas.py"
      provides: "CodexResult + 12 category submodels (CodexCurrency, CodexAttributes, CodexHealth, CodexTraits, CodexFeudal, CodexPolitics, CodexDynasty, CodexReligion, CodexCulture, CodexEconomy, CodexMilitary, CodexEvents) and CodexEntity base."
      contains: "class CodexResult"
    - path: "backend/medieval_forge/services/llm/prompt.py"
      provides: "build_codex_prompt(country_name, period_start, period_end, map_summary, focus_sections=None)"
      contains: "def build_codex_prompt"
    - path: "backend/medieval_forge/models.py"
      provides: "CodexCache ORM table with cache_key_hash PK and JSON payload column."
      contains: "class CodexCache"
    - path: "alembic/versions/0003_create_codex_cache.py"
      provides: "Alembic migration creating codex_cache table; revision='0003', down_revision='0002'."
      contains: "op.create_table(\"codex_cache\""
    - path: "backend/medieval_forge/services/codex_cache.py"
      provides: "compute_codex_cache_key, get_codex_cached, set_codex_cached"
      contains: "def compute_codex_cache_key"
    - path: "backend/medieval_forge/services/codex_runner.py"
      provides: "async run_codex(project_id, provider_id, queue, ...) producer task; mirrors research_runner."
      contains: "async def run_codex"
    - path: "backend/medieval_forge/api/codex.py"
      provides: "POST /projects/{id}/codex (SSE stream), GET /projects/{id}/codex/cached, GET /projects/{id}/codex/prompt."
      contains: "router = APIRouter"
    - path: "backend/medieval_forge/main.py"
      provides: "include_router(codex_router) so endpoints are reachable."
      contains: "from .api.codex"
  key_links:
    - from: "backend/medieval_forge/api/codex.py"
      to: "backend/medieval_forge/services/codex_runner.py"
      via: "asyncio.create_task(run_codex(...)) inside StreamingResponse generator"
      pattern: "asyncio\\.create_task\\(run_codex"
    - from: "backend/medieval_forge/services/codex_runner.py"
      to: "backend/medieval_forge/services/codex_cache.py"
      via: "compute_codex_cache_key + get_codex_cached + set_codex_cached"
      pattern: "compute_codex_cache_key|get_codex_cached|set_codex_cached"
    - from: "backend/medieval_forge/services/codex_runner.py"
      to: "backend/medieval_forge/services/llm/prompt.py"
      via: "build_codex_prompt import"
      pattern: "build_codex_prompt"
    - from: "backend/medieval_forge/services/codex_runner.py"
      to: "backend/medieval_forge/services/llm/schemas.py"
      via: "CodexResult schema passed to run_with_retry"
      pattern: "CodexResult"
    - from: "backend/medieval_forge/main.py"
      to: "backend/medieval_forge/api/codex.py"
      via: "FastAPI include_router"
      pattern: "include_router\\(codex"
---

<objective>
Etapa 9 do plano hazy-hatching-abelson: Codex Histórico Medieval — schema com 12 categorias, runner espelhando research_runner com cache, API SSE, e tabela CodexCache via Alembic.

Purpose: Habilitar a 5ª etapa do pipeline (Codex) com endpoint independente de /research, suportando geração estruturada+markdown de narrativa medieval rica (dinastias, fortuna, piedade, tradições, batalhas, etc.) com cache persistente e streaming SSE.

Output: 3 commits atômicos (TDD: RED → GREEN por bloco), ~12 testes novos, infra de Codex pronta para o frontend (Etapa 10) consumir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@C:\Users\veio_\.claude\plans\hazy-hatching-abelson.md

# Mirror these files (read-only references during execution):
@backend/medieval_forge/services/research_runner.py
@backend/medieval_forge/api/research.py
@backend/medieval_forge/services/llm/schemas.py
@backend/medieval_forge/services/llm/prompt.py
@backend/medieval_forge/services/research_cache.py
@backend/medieval_forge/models.py
@alembic/versions/0002_widen_country_qid_multi_country.py

<interfaces>
<!-- Key contracts the executor needs. Already extracted — do NOT explore further. -->

From backend/medieval_forge/services/llm/__init__.py (re-exports):
```python
PROVIDERS: dict[str, Provider]
ResearchValidationError
async def run_with_retry(provider, prompt, schema_cls, credentials, queue, max_retries=3) -> BaseModel
```

From backend/medieval_forge/services/llm/auth.py:
```python
def resolve_credentials(provider_id: str, app_state) -> dict | None
```

From backend/medieval_forge/services/llm/model_routing.py:
```python
def resolve_model(provider_id: str, task_type: str, effort_override: str | None) -> str
# Recognized task_types include: codex_genealogy (high), codex_wars_battles (high),
# codex_culture_religion (medium), codex_population_economy (low), codex_personalities (medium).
# For Etapa 9 default we use task_type="codex_genealogy" (high) unless caller overrides.
```

From backend/medieval_forge/services/research_cache.py (mirror exactly for codex_cache.py):
```python
def compute_cache_key(country_qid, period_start, period_end, provider, model) -> str   # SHA-256 hex
async def get_cached(session, cache_key_hash) -> dict | None
async def set_cached(session, cache_key_hash, payload, provider, model, country_qid, period_start, period_end) -> None
```

From backend/medieval_forge/services/paths.py:
```python
def is_valid_uuid(s: str) -> bool
def project_dir(project_id: str) -> Path
```

From backend/medieval_forge/database.py:
```python
AsyncSessionLocal: async_sessionmaker
async def get_db() -> AsyncSession  # FastAPI dependency
```

From backend/medieval_forge/models.py (existing):
```python
class Base(DeclarativeBase): ...
class Project(Base):
  id: str; name: str; country_qid: str; period_start: int; period_end: int; ...
class ResearchCache(Base):
  cache_key_hash: str (PK); payload: dict; provider: str; model: str;
  country_qid: str; period_start: int; period_end: int; created_at: datetime
```

From backend/medieval_forge/main.py (assumed pattern — mirror existing include_router calls):
```python
from .api import research, ingest, ...
app.include_router(research.router, prefix="/api")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CodexResult schema + build_codex_prompt + CodexCache model + Alembic migration (RED → GREEN)</name>
  <files>
    backend/medieval_forge/services/llm/schemas.py,
    backend/medieval_forge/services/llm/prompt.py,
    backend/medieval_forge/models.py,
    backend/medieval_forge/services/codex_cache.py,
    alembic/versions/0003_create_codex_cache.py,
    backend/tests/services/test_codex_schema.py,
    backend/tests/services/test_codex_prompt.py
  </files>
  <behavior>
    Tests FIRST (RED commit). Use descriptive names + explicit fixtures (per memory feedback).

    test_codex_schema.py (5 tests):
      - test_codex_result_accepts_minimal_payload_with_all_12_category_keys
        Fixture: CodexResult with each of the 12 category objects empty-but-present
        (currency={}, attributes={}, ..., events={}). Assert .currency, .events exist.
      - test_codex_result_rejects_unknown_top_level_key
        Fixture: payload with extra key "weather" → expect ValidationError mentioning extra='forbid'.
      - test_codex_entity_requires_id_name_description_markdown
        Fixture: dynasty entity {"id": "DYN_HOUSE_OF_LEON", "name": "Casa de Leão",
        "description": "**Fundada** em 910 por Garcia I."} parses; missing description fails.
      - test_codex_dynasty_category_holds_list_of_entities_with_markdown_descriptions
        Fixture: 2 dynasty entities; assert isinstance(result.dynasty.entries, list) and len == 2.
      - test_codex_result_round_trips_through_model_dump_and_validate
        Fixture: full payload with 1 entry per category (12 entries total) →
        dump → re-validate → equality.

    test_codex_prompt.py (3 tests):
      - test_build_codex_prompt_includes_country_period_and_all_12_categories
        Fixture: country="Ibéria", period_start=868, period_end=1100,
        map_summary="3 reinos, 12 condados, 250 baronies".
        Assert all 12 category labels appear: "currency", "attributes", "health",
        "traits", "feudal", "politics", "dynasty", "religion", "culture",
        "economy", "military", "events".
        Assert "868" and "1100" both appear; "Ibéria" appears.
      - test_build_codex_prompt_focus_sections_filters_when_provided
        Fixture: focus_sections=["dynasty", "religion"] → assert prompt explicitly
        names ONLY those two as the active focus, while still listing the full
        schema for shape reference.
      - test_build_codex_prompt_includes_json_example_matching_codex_result_shape
        Assert "EXAMPLE OUTPUT" present and that the example contains the literal
        keys "currency" and "events" (sentinels for first/last category).

    GREEN: implement schemas + prompt + model + migration + cache helper.

    schemas.py additions (append, do not break existing exports):
      - class CodexEntity(BaseModel, extra='forbid'):
          id: str; name: str; description: str  # description is markdown free text
      - class CodexCategory(BaseModel, extra='forbid'):
          entries: list[CodexEntity] = Field(default_factory=list)
          summary: str = ""  # category-level markdown overview
      - 12 category aliases: CodexCurrency, CodexAttributes, CodexHealth,
        CodexTraits, CodexFeudal, CodexPolitics, CodexDynasty, CodexReligion,
        CodexCulture, CodexEconomy, CodexMilitary, CodexEvents — all = CodexCategory
        (Pydantic equality fine; can be `CodexCurrency = CodexCategory` aliases or
        12 distinct subclasses if helpful for prompt clarity — choose subclasses
        so __name__ differs and example output is more grounded).
      - class CodexResult(BaseModel, extra='forbid'):
          currency: CodexCurrency
          attributes: CodexAttributes
          health: CodexHealth
          traits: CodexTraits
          feudal: CodexFeudal
          politics: CodexPolitics
          dynasty: CodexDynasty
          religion: CodexReligion
          culture: CodexCulture
          economy: CodexEconomy
          military: CodexMilitary
          events: CodexEvents

    prompt.py additions:
      - SYSTEM_INSTRUCTIONS_CODEX = system role for Codex generation
        (similar tone to existing SYSTEM_INSTRUCTIONS but emphasizes free-form
        markdown inside entity.description).
      - EXAMPLE_OUTPUT_CODEX = JSON literal with all 12 keys, each as
        {"summary": "...", "entries": [{"id": "...", "name": "...", "description": "**markdown** ok"}]}.
      - RULES_CODEX listing the 12 allowed top-level keys + "no other keys".
      - def build_codex_prompt(country_name, period_start, period_end, map_summary,
        focus_sections=None) -> str. When focus_sections is provided (subset of the
        12 keys), include a "FOCUS:" line listing them; when None, prompt asks for all.

    models.py addition:
      - class CodexCache(Base):
          __tablename__ = "codex_cache"
          cache_key_hash: Mapped[str] = mapped_column(String(64), primary_key=True)
          payload: Mapped[dict] = mapped_column(JSON, nullable=False)
          provider: Mapped[str] = mapped_column(String(50), nullable=False)
          model: Mapped[str] = mapped_column(String(100), nullable=False)
          country_qid: Mapped[str] = mapped_column(String(200), nullable=False)
          period_start: Mapped[int] = mapped_column(nullable=False)
          period_end: Mapped[int] = mapped_column(nullable=False)
          focus_sections: Mapped[str | None] = mapped_column(String(500), nullable=True)  # csv of focus section names; "" or NULL = full
          created_at: Mapped[datetime] = mapped_column(DateTime, default=_utcnow, nullable=False)

    codex_cache.py (new file — mirror research_cache.py shape):
      - compute_codex_cache_key(country_qid, period_start, period_end, provider, model, focus_sections=None) -> str
        Key formula: SHA-256 of f"{country_qid}:{period_start}:{period_end}:{provider}:{model}:codex:{focus_csv}"
        where focus_csv = "" when focus_sections is None else ",".join(sorted(focus_sections)).
      - async get_codex_cached(session, cache_key_hash) -> dict | None
      - async set_codex_cached(session, cache_key_hash, payload, provider, model, country_qid, period_start, period_end, focus_sections=None) -> None

    alembic/versions/0003_create_codex_cache.py:
      - revision="0003", down_revision="0002"
      - upgrade(): op.create_table("codex_cache", ...) with the columns above.
      - downgrade(): op.drop_table("codex_cache").

    Commit 1 (RED): "test(quick-260428-l0l-01): add 8 failing tests for CodexResult schema + build_codex_prompt"
    Commit 2 (GREEN): "feat(quick-260428-l0l-01): CodexResult schema + build_codex_prompt + CodexCache model + 0003 migration"
  </behavior>
  <action>
    1. Write the 8 tests above (RED). Run: cd backend && python -m pytest tests/services/test_codex_schema.py tests/services/test_codex_prompt.py -q. Confirm 8 fail. Commit RED.
    2. Implement schemas.py additions, prompt.py additions, models.py CodexCache, codex_cache.py helpers, alembic 0003 migration.
    3. Run the same pytest command — confirm 8 green.
    4. Run full backend suite to confirm no regression: cd backend && python -m pytest -q. Expect 246 + 8 = 254 passing.
    5. Commit GREEN with message above.

    Honour CLAUDE.md: do NOT introduce new dependencies; reuse SQLAlchemy 2.0 typing pattern already in models.py; use UTF-8 encoding (memory feedback) when writing any Path.write_text.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -m pytest tests/services/test_codex_schema.py tests/services/test_codex_prompt.py -q</automated>
  </verify>
  <done>
    8 new tests pass. Existing 246 backend tests pass. CodexResult, build_codex_prompt, CodexCache, codex_cache.py helpers, and 0003 migration committed in two atomic commits (RED + GREEN).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: codex_runner.py with cache + retry + SSE producer (RED → GREEN)</name>
  <files>
    backend/medieval_forge/services/codex_runner.py,
    backend/tests/services/test_codex_runner.py
  </files>
  <behavior>
    Tests FIRST (RED commit). Mirror test_research_runner / test_research_sse patterns.

    test_codex_runner.py (5 tests):
      - test_run_codex_emits_starting_then_result_then_done_on_happy_path
        Fixture: stub provider whose research() returns a CodexResult with
        12 minimal categories. Drive run_codex with an in-memory queue;
        drain queue. Assert order: "starting <provider>", "RESULT: <json>",
        "DONE", then None sentinel.
      - test_run_codex_returns_cached_payload_without_calling_provider_when_cache_hit
        Fixture: pre-populate codex_cache via set_codex_cached with payload
        {"currency": {"summary": "pre-cached", "entries": []}, ...12 keys...}.
        Stub provider raises if research() called. Assert run_codex emits
        "data: cached" + RESULT containing "pre-cached" + DONE + None.
      - test_run_codex_force_refresh_bypasses_cache
        Fixture: same cache row pre-populated AS ABOVE; pass force_refresh=True.
        Stub provider returns CodexResult with summary="fresh-result".
        Assert payload emitted contains "fresh-result", and DB row was UPSERTED
        (provider.research WAS called exactly once).
      - test_run_codex_emits_error_and_sentinel_on_unknown_provider
        Fixture: provider_id="nonexistent". Drain queue. Assert single
        "data: ERROR: unknown provider nonexistent" message followed by None sentinel.
      - test_run_codex_terminates_with_none_sentinel_even_on_internal_exception
        Fixture: stub provider whose research() raises RuntimeError("boom").
        Drain queue. Assert last item is None and at least one ERROR message
        was emitted before sentinel. (Mirrors Pitfall 6 from research_runner.)

    Use the existing test fixtures pattern: AsyncSessionLocal override via
    db_session_factory parameter; in-memory aiosqlite engine; project row
    inserted with country_qid="Q29", period_start=868, period_end=1100;
    PROVIDERS dict monkeypatched with stub.

    GREEN: implement codex_runner.py — direct mirror of research_runner.run_research.

    codex_runner.py shape (mirror research_runner.py exactly, with these differences):
      - import: build_codex_prompt, CodexResult, compute_codex_cache_key,
        get_codex_cached, set_codex_cached
      - Function signature:
          async def run_codex(
              project_id: str,
              provider_id: str,
              queue: asyncio.Queue[str | None],
              *,
              force_refresh: bool = False,
              focus_sections: list[str] | None = None,
              db_session_factory: async_sessionmaker | None = None,
              app_state: Any = None,
              task_type: str | None = "codex_genealogy",
              effort_override: str | None = None,
          ) -> None
      - Body steps:
          1. validate provider_id in PROVIDERS, else ERROR + None.
          2. resolve model via PROVIDER_DEFAULT_MODEL (reuse the dict from
             research_runner — import it) or via resolve_model when task_type is set.
             Default task_type="codex_genealogy" so multi-modelo routing kicks in
             out-of-the-box per master plan section B.1.
          3. load Project from DB (via factory) → ERROR + None if not found.
          4. compute_codex_cache_key(country_qid, period_start, period_end, provider_id, model, focus_sections).
          5. unless force_refresh: get_codex_cached → if hit, emit "cached" + RESULT + DONE + None and return.
          6. build map_summary string from project (e.g.
             f"{country_name} ({period_start}-{period_end} AD)") since the full
             baronies/territories aren't required for Codex narrative.
             Optional: if generated/territories.geojson exists, include feature_count.
          7. resolve_credentials; ERROR + None if missing for non-Ollama.
          8. emit "starting {provider_id} ({model})".
          9. prompt = build_codex_prompt(country_name, period_start, period_end, map_summary, focus_sections).
         10. result = await run_with_retry(provider, prompt, CodexResult, credentials, queue, max_retries=3).
         11. await set_codex_cached(...) with payload = result.model_dump().
         12. emit "RESULT: {json.dumps(payload)}" + "DONE".
         13. on Exception: log + emit ERROR.
         14. finally: await queue.put(None).  # Pitfall 6 — ALWAYS sentinel.

    Commit 3 (RED): "test(quick-260428-l0l-02): add 5 failing tests for codex_runner SSE producer"
    Commit 4 (GREEN): "feat(quick-260428-l0l-02): codex_runner.run_codex with cache + retry + SSE"
  </behavior>
  <action>
    1. Write 5 tests above. Run: cd backend &amp;&amp; python -m pytest tests/services/test_codex_runner.py -q. Confirm 5 fail. Commit RED.
    2. Implement codex_runner.py per shape above. Re-import and re-export PROVIDER_DEFAULT_MODEL from research_runner (do not duplicate the dict).
    3. Re-run pytest — confirm 5 green. Run full suite: cd backend &amp;&amp; python -m pytest -q. Expect 254 + 5 = 259 passing.
    4. Commit GREEN.

    Notes:
      - Reuse the _ValidatingWrapper pattern? NO — Codex has no cross-ref validator yet (Etapa 10+ may add marriages-reference-existing-persons checks). Keep it simple: pass provider directly to run_with_retry. Pydantic extra='forbid' is the only validation.
      - Use the same logger pattern: logger = logging.getLogger(__name__).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -m pytest tests/services/test_codex_runner.py -q</automated>
  </verify>
  <done>
    5 new codex_runner tests pass. Full backend suite at 259 passing. Two atomic commits (RED + GREEN). codex_runner.run_codex contractually mirrors run_research's queue-producer + sentinel-finally semantics.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: api/codex.py SSE endpoint + GET /cached + main.py wiring (RED → GREEN)</name>
  <files>
    backend/medieval_forge/api/codex.py,
    backend/medieval_forge/main.py,
    backend/tests/api/test_codex_endpoints.py
  </files>
  <behavior>
    Tests FIRST (RED commit). Mirror test_research_sse + test_research_endpoints patterns.

    test_codex_endpoints.py (4 tests):
      - test_post_codex_streams_sse_chunks_through_done
        Fixture: project row with id=fixed UUID, country_qid="Q29",
        period_start=868, period_end=1100. Stub PROVIDERS["claude"] returns
        CodexResult with summary="Test codex". POST /api/projects/{id}/codex?provider=claude.
        Assert HTTP 200, content-type "text/event-stream",
        body contains "data: starting", "data: RESULT", "data: DONE".
      - test_post_codex_returns_400_for_invalid_uuid
        POST /api/projects/not-a-uuid/codex?provider=claude → 400 "Invalid project_id".
      - test_post_codex_returns_404_for_unknown_provider_or_project
        a) POST /api/projects/{valid-uuid-no-row}/codex?provider=claude → 404 "Project not found".
        b) POST /api/projects/{existing-id}/codex?provider=ghost → 404 "Unknown provider: ghost".
      - test_get_codex_cached_returns_payload_when_cached_else_404
        a) Pre-populate codex_cache for (project, provider="claude", model=default, no focus).
           GET /api/projects/{id}/codex/cached?provider=claude → 200 + JSON payload.
        b) Different provider → 404 "No cached codex for this project/provider/model".

    GREEN: implement api/codex.py — direct mirror of api/research.py with these
    endpoints:
      - POST /projects/{project_id}/codex
          Query: provider: str (required), force_refresh: bool=False,
          focus: str|None (csv of focus_sections, e.g. "dynasty,religion").
          Same UUID + provider + project validations as research.py BEFORE streaming.
          StreamingResponse driving asyncio.create_task(run_codex(...)).
          Cancel task in finally (T-3-09 mirror).
      - GET /projects/{project_id}/codex/cached
          Query: provider, model (optional), focus (optional csv).
          Compute key with compute_codex_cache_key; return 200 with payload or 404.
      - GET /projects/{project_id}/codex/prompt
          Returns {"prompt": build_codex_prompt(...)} for the manual paste flow
          (parallel to GET /research/prompt). map_summary=f"{country} ({start}-{end} AD)".

    main.py wiring:
      - from .api import codex as codex_router_module
      - app.include_router(codex_router_module.router, prefix="/api")
        immediately after the existing research router include.

    Commit 5 (RED): "test(quick-260428-l0l-03): add 4 failing tests for /codex endpoints"
    Commit 6 (GREEN): "feat(quick-260428-l0l-03): /codex SSE + cached + prompt endpoints; wire router"

    NOTE on commit count: master plan says "~3 commits", but TDD discipline
    (RED + GREEN per behavior) yields 6 atomic commits across 3 tasks. This is
    intentional and aligns with prior etapas (260428-jug, 260428-h1t both used
    RED+GREEN per task). Each task remains a single logical unit.
  </behavior>
  <action>
    1. Write 4 endpoint tests. Use existing test fixture/conftest pattern — copy from backend/tests/api/test_research_endpoints.py if it exists (otherwise from test_research_sse.py). Use httpx AsyncClient with the FastAPI app.
    2. Run: cd backend &amp;&amp; python -m pytest tests/api/test_codex_endpoints.py -q. Confirm 4 fail (likely with 404 for the route). Commit RED.
    3. Implement api/codex.py. Reuse imports/patterns from api/research.py:
       - APIRouter(tags=["codex"]); same is_valid_uuid + PROVIDERS guards.
       - SSE generator captures app.state._test_session_factory or AsyncSessionLocal.
       - Pass focus_sections (parsed from comma-separated `focus` query param, or None).
    4. Wire router in main.py — locate `app.include_router(research.router, prefix="/api")` and add `app.include_router(codex.router, prefix="/api")` directly below.
    5. Re-run endpoint tests — confirm 4 green.
    6. Run full backend suite: cd backend &amp;&amp; python -m pytest -q. Expect 263 passing (246 + 8 + 5 + 4).
    7. Run frontend suite to confirm no regression: cd frontend &amp;&amp; npm test. Expect 191/191 still passing (no frontend changes this etapa).
    8. Commit GREEN.

    Final check: confirm `cd backend &amp;&amp; alembic upgrade head` runs clean against an empty DB (the 0003 migration applies on top of 0002).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -m pytest tests/api/test_codex_endpoints.py tests/services/test_codex_runner.py tests/services/test_codex_schema.py tests/services/test_codex_prompt.py -q &amp;&amp; python -m pytest -q</automated>
  </verify>
  <done>
    POST /api/projects/{id}/codex streams SSE; GET /codex/cached returns payload or 404; GET /codex/prompt returns built prompt. Router wired in main.py. 4 endpoint tests pass; full backend suite ≥ 263 passing; frontend 191/191 unchanged. Two atomic commits (RED + GREEN).
  </done>
</task>

</tasks>

<verification>
Phase-level verification (after all 3 tasks complete):

```bash
cd backend && python -m pytest -q
# Expected: 263 passed (246 baseline + 17 new across 3 tasks: 5+3+5+4)
# Note: 17 new tests slightly exceeds the ~12 estimate in master plan because
# schema + prompt are split into focused tests per memory feedback (descriptive
# names + explicit fixtures). Acceptable upward variance.

cd backend && alembic upgrade head     # Confirms 0003 migration applies cleanly
cd frontend && npm test                 # Expected: 191/191 (no frontend changes)
```

Manual sanity check (optional, requires backend running):
  curl -X POST 'http://localhost:8000/api/projects/<id>/codex/prompt' → JSON with all 12 category labels visible.
</verification>

<success_criteria>
- All 3 commit pairs (RED + GREEN) landed on main with descriptive messages.
- backend suite: 263 passing (or higher with descriptive test split), 0 failing.
- frontend suite: 191 passing (unchanged).
- alembic upgrade head succeeds against empty DB.
- New endpoints reachable: POST/GET /api/projects/{id}/codex*.
- CodexResult schema enforces 12 categories with extra='forbid'.
- run_codex producer ALWAYS terminates queue with None sentinel (Pitfall 6 honored).
- No regressions in research/, ingest/, edit/ existing endpoints.
</success_criteria>

<output>
After completion, create `.planning/quick/260428-l0l-etapa-9-codex-schema-runner-endpoint-cod/260428-l0l-SUMMARY.md` documenting:
  - Files changed with line counts
  - Test count delta (per task and total)
  - Commit hashes (6 commits expected: 3 RED + 3 GREEN)
  - Confirmation that alembic upgrade head applies cleanly
  - Pointer back to master plan: Etapa 9 in C:\Users\veio_\.claude\plans\hazy-hatching-abelson.md
  - Next etapa: 10 (Codex viewer frontend — tabs por categoria, render markdown)
</output>
