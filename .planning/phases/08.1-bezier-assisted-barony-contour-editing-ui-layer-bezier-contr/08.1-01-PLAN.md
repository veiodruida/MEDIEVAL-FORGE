---
phase: 08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/lib/bezierFit.ts
  - frontend/src/lib/bezierFlatten.ts
  - frontend/src/lib/__tests__/bezierFit.test.ts
  - frontend/src/lib/__tests__/bezierFlatten.test.ts
  - frontend/src/components/canvas/__fixtures__/iberiaBaronyRing.ts
  - frontend/vitest.config.ts
autonomous: true
requirements: [BEZ-FIT-01, BEZ-INDEX-01, BEZ-FLATTEN-01, BEZ-FLATTEN-02]
must_haves:
  truths:
    - "fitPolygonToBezier(coords, 30) returns between 3 and 40 cubic segments for a ~100-vertex polygon"
    - "buildPolyIndexMap returns one contiguous {rangeStart,rangeEnd} per cubic covering all input indices with no gap/overlap"
    - "flattenSegment(cubic, M, projection) returns exactly M [lon,lat] pairs"
    - "fit happens in canvas-pixel space; geo coords only materialize at flatten output"
  artifacts:
    - path: "frontend/src/lib/bezierFit.ts"
      provides: "fitPolygonToBezier + buildPolyIndexMap + BezierCubic/PolyRange types"
      exports: ["fitPolygonToBezier", "buildPolyIndexMap"]
    - path: "frontend/src/lib/bezierFlatten.ts"
      provides: "cubicBezierAt + flattenSegment (inline de Casteljau, no bezier-js dep)"
      exports: ["cubicBezierAt", "flattenSegment"]
    - path: "frontend/src/components/canvas/__fixtures__/iberiaBaronyRing.ts"
      provides: "real σ=3.0 Voronoi barony ring as explicit numeric fixture for calibration"
  key_links:
    - from: "frontend/src/lib/bezierFit.ts"
      to: "fit-curve"
      via: "default import fitCurve(points, error)"
      pattern: "import fitCurve from 'fit-curve'"
---

<objective>
Build the two pure, side-effect-free geometry libraries that the Bézier editor depends on, plus the calibration fixture and the fit-curve dependency. No React, no Konva, no store — just functions and tests. This is the foundation wave: everything downstream imports from these two files.

Purpose: Isolate the numerically-sensitive curve-fitting and flatten logic behind unit-tested pure functions, and empirically calibrate the fit-curve error tolerance against a real Iberia barony ring (RESEARCH Open Q1, Assumption A1). The split-index gap (RESEARCH §Split-Index Gap — fit-curve does NOT expose which polygon vertices each cubic spans) is closed here with `buildPolyIndexMap`, a mandatory deliverable.
Output: `bezierFit.ts`, `bezierFlatten.ts`, the `iberiaBaronyRing` fixture, fit-curve@0.2.0 in package.json, and passing unit tests covering BEZ-FIT-01 / BEZ-INDEX-01 / BEZ-FLATTEN-01 / BEZ-FLATTEN-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-RESEARCH.md
@.planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-VALIDATION.md

<interfaces>
<!-- Projection helpers the flatten path consumes. Extracted from frontend/src/lib/projection.ts -->
From frontend/src/lib/projection.ts:
```typescript
export interface ProjectionConfig {
  lonMin: number; lonMax: number; latMin: number; latMax: number;
  mapW: number; mapH: number; lonScale: number;
}
export function geoToCanvas(lon: number, lat: number, c: ProjectionConfig): [number, number];
export function canvasToGeo(x: number, y: number, c: ProjectionConfig): [number, number];
```

<!-- fit-curve@0.2.0 API. CJS package, ships fit-curve.d.ts. Vite auto-transforms; vitest MAY need deps.inline. -->
fit-curve signature:
```typescript
// fitCurve(points: number[][], maxError: number): number[][][]
// each returned curve = [p0, c1, c2, p3]; each point = [x, y]
import fitCurve from 'fit-curve';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fit-curve dep + bezierFit.ts (fitPolygonToBezier + buildPolyIndexMap) + fixture + tests</name>
  <files>frontend/package.json, frontend/src/lib/bezierFit.ts, frontend/src/lib/__tests__/bezierFit.test.ts, frontend/src/components/canvas/__fixtures__/iberiaBaronyRing.ts, frontend/vitest.config.ts</files>
  <read_first>
    - frontend/package.json (add the dependency to the correct block)
    - frontend/src/lib/projection.ts (ProjectionConfig, geoToCanvas signatures — fit works in px space)
    - .planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-RESEARCH.md §Split-Index Gap (Option A recommended), §Pattern 1 (closed-ring), §Pitfall 3 (fewer/more curves), §Pitfall 4 (CJS in vitest), Open Q1 (error calibration)
    - .planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-VALIDATION.md (Wave 0 Requirements, calibration note)
    - frontend/src/components/canvas/VertexEditLayer.tsx lines 763-790 (existing geoToCanvas usage pattern; the geoRing shape SelectionBridge produces)
  </read_first>
  <behavior>
    - Test BEZ-FIT-01: fitPolygonToBezier(ring100, 30) returns ≥3 and ≤40 cubic segments where ring100 is a ~100-vertex closed ring; each cubic is [[x,y],[x,y],[x,y],[x,y]].
    - Test BEZ-INDEX-01: buildPolyIndexMap(inputPxPts, cubics) returns one {rangeStart, rangeEnd} per cubic; ranges are contiguous (cubics[j].rangeEnd === cubics[j+1].rangeStart), rangeStart of first === 0, rangeEnd of last === inputPxPts.length-1, no gaps, no overlap.
    - Test (calibration, informational): assert the iberiaBaronyRing fixture fits to a count in the 4..30 "normal" band at error=30; if it lands outside, the test documents the tuned constant (adjust BEZ_FIT_ERROR and re-assert). This is the empirical lock for Assumption A1.
    - Test (guard): fitPolygonToBezier(coordsWith2Points, 30) returns [] (the <3-point security guard, T-08.1-01-01) and does NOT throw.
  </behavior>
  <action>
    1. In `frontend/package.json`, add `"fit-curve": "0.2.0"` to `dependencies` (MIT, zero deps). Run `cd frontend && npm install` so the lockfile updates and `fit-curve.d.ts` types resolve.
    2. Create `frontend/src/lib/bezierFit.ts` exporting:
       - `export type BezierCubic = [[number, number], [number, number], [number, number], [number, number]];` (p0, c1, c2, p3 in canvas px)
       - `export interface PolyRange { rangeStart: number; rangeEnd: number; }`
       - `export const BEZ_FIT_ERROR = 30;` (calibrated px error tolerance — RESEARCH §6; adjust only if the fixture calibration test in Task forces it, document the change in SUMMARY)
       - `export function fitPolygonToBezier(coords: Array<[number, number]>, projection: ProjectionConfig, error: number = BEZ_FIT_ERROR): BezierCubic[]`:
         (a) SECURITY GUARD (T-08.1-01-01, V5): `if (coords.length < 3) return [];` — fit-curve throws on degenerate input.
         (b) Map `coords` ([lon,lat]) to canvas px via `geoToCanvas(lon, lat, projection)` BEFORE fitting (UI-SPEC Note #3 — fit is in px space, non-negotiable).
         (c) Drop the closing duplicate if `pts[0]` equals `pts[last]` (Approach A, RESEARCH §Pattern 1 — open ring).
         (d) SECURITY GUARD (over-dense, RESEARCH §Security): `if (open.length > 1500) { use error = error * 4 }` — D-06's 1000-vertex cap already bounds this; raise tolerance rather than freeze.
         (e) `return fitCurve(open, error) as BezierCubic[];`
       - `export function buildPolyIndexMap(inputPxPts: Array<[number, number]>, cubics: BezierCubic[]): PolyRange[]`:
         Option A (RESEARCH §Split-Index Gap): O(N) post-fit scan. Walk a cursor `prevEnd = 0`; for each cubic `j`, its endpoint is `cubics[j][3]`; scan `inputPxPts` from `prevEnd` forward to the index `k` whose point is within 1e-4 px of `cubics[j][3]` (Euclidean). Push `{ rangeStart: prevEnd, rangeEnd: k }`; set `prevEnd = k`. Force the last cubic's `rangeEnd` to `inputPxPts.length - 1` to guarantee full coverage even under float drift.
    3. Create `frontend/src/components/canvas/__fixtures__/iberiaBaronyRing.ts` exporting `export const IBERIA_BARONY_RING: Array<[number, number]> = [ ... ]` — a real ~100-vertex σ=3.0-smoothed Voronoi barony ring in [lon,lat]. Source it by running the seeded Iberia 868 pipeline locally and copying a single barony's geoRing (or extract from an existing canvas-sidecar fixture under frontend/src or backend fixtures). Use explicit numeric literals (memory: descriptive fixtures with explicit numbers). Also export a synthetic `export const RING_100: Array<[number, number]>` (deterministic 100-point closed ring, e.g. a sampled circle) for the BEZ-FIT-01 count assertion when a real fixture is impractical.
    4. Create `frontend/src/lib/__tests__/bezierFit.test.ts` with descriptive test names covering all four behaviors above. Build a `ProjectionConfig` test fixture inline (use `buildProjectionConfig` from projection.ts with the Iberia bounds). If `import fitCurve from 'fit-curve'` throws `require is not defined` / `Cannot find module` in vitest (RESEARCH Pitfall 4), AND ONLY THEN, add `deps: { inline: ['fit-curve'] }` to the `test` config in `frontend/vitest.config.ts`. Do not add it speculatively (karpathy #2).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/__tests__/bezierFit.test.ts</automated>
  </verify>
  <done>
    - `frontend/package.json` contains `"fit-curve"` in dependencies; `npm install` succeeded.
    - `bezierFit.ts` exports `fitPolygonToBezier`, `buildPolyIndexMap`, `BEZ_FIT_ERROR`, `BezierCubic`, `PolyRange`.
    - `iberiaBaronyRing.ts` exports `IBERIA_BARONY_RING` and `RING_100` as explicit numeric arrays.
    - `npx vitest run src/lib/__tests__/bezierFit.test.ts` exits 0 with BEZ-FIT-01, BEZ-INDEX-01, calibration, and <3-point guard tests passing.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bezierFlatten.ts (cubicBezierAt + flattenSegment) + density-match tests</name>
  <files>frontend/src/lib/bezierFlatten.ts, frontend/src/lib/__tests__/bezierFlatten.test.ts</files>
  <read_first>
    - frontend/src/lib/projection.ts (canvasToGeo — only used at flatten output)
    - frontend/src/lib/bezierFit.ts (BezierCubic type — created in Task 1)
    - .planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-RESEARCH.md §Pattern 2 (Flatten Resolution — density matching, the cubicBezierAt + flattenSegment reference code), §Pitfall 2 (float drift — only dirty segments flatten)
  </read_first>
  <behavior>
    - Test BEZ-FLATTEN-01: flattenSegment(cubic, M, projection) returns exactly M [lon,lat] pairs for M = 8 and M = 25.
    - Test BEZ-FLATTEN-02: take a real cubic fitted from IBERIA_BARONY_RING for an unedited segment, flatten it with targetCount = original range size, and assert each flattened [lon,lat] is within 1e-4° of the corresponding original ring vertex (density-match fidelity; values won't be byte-identical — that's why only DIRTY segments flatten at commit, per RESEARCH §Pitfall 2).
    - Test (endpoint): flattenSegment returns p0 (mapped to geo) at k=0 and p3 (mapped to geo) at k=M-1 (within 1e-9° of canvasToGeo(p0/p3)).
  </behavior>
  <action>
    Create `frontend/src/lib/bezierFlatten.ts` exporting:
    - `export function cubicBezierAt(p0: [number,number], c1: [number,number], c2: [number,number], p3: [number,number], t: number): [number, number]` — inline de Casteljau, 4-line uniform-t evaluator exactly as RESEARCH §Pattern 2 (mt=1-t; mt3*p0 + 3*mt2*t*c1 + 3*mt*t2*c2 + t3*p3 per axis). No bezier-js dependency.
    - `export function flattenSegment(cubic: BezierCubic, targetCount: number, projection: ProjectionConfig): Array<[number, number]>` — for k in [0, targetCount): t = targetCount > 1 ? k/(targetCount-1) : 0; evaluate `cubicBezierAt` in px space; map back to geo via `canvasToGeo(x, y, projection)`; push [lon, lat]. Returns exactly `targetCount` pairs. Geo coords materialize ONLY here (UI-SPEC Note #3).
    Create `frontend/src/lib/__tests__/bezierFlatten.test.ts` with the three behaviors above, using the IBERIA_BARONY_RING fixture and an inline Iberia-bounds ProjectionConfig. To produce the "real cubic for an unedited segment", call `fitPolygonToBezier` + `buildPolyIndexMap` from bezierFit.ts in the test setup.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/__tests__/bezierFlatten.test.ts</automated>
  </verify>
  <done>
    - `bezierFlatten.ts` exports `cubicBezierAt` and `flattenSegment`; no bezier-js import.
    - `flattenSegment(cubic, M, projection)` returns exactly M pairs (verified for M=8, M=25).
    - Unedited-segment flatten within 1e-4° of original vertices (BEZ-FLATTEN-02).
    - `npx vitest run src/lib/__tests__/bezierFlatten.test.ts` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| polygon ring → fitCurve | Polygon vertex array (derived from store geometry) crosses into the fit-curve algorithm; malformed/over-dense input could crash or freeze the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-01-01 | Denial of Service | `fitPolygonToBezier` in bezierFit.ts | mitigate | `if (coords.length < 3) return []` guard before fitCurve — degenerate ring (< 3 pts) would throw inside fit-curve. Covered by the <3-point guard unit test. |
| T-08.1-01-02 | Denial of Service | `fitPolygonToBezier` over-dense path | accept (mitigate-by-design) | `if (open.length > 1500) error *= 4` raises tolerance rather than freezing; D-06's 1000-vertex polygon cap already bounds input size. Low risk, UI-only, no remote input. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/__tests__/bezierFit.test.ts src/lib/__tests__/bezierFlatten.test.ts` exits 0.
- `cd frontend && npx vitest run` (full suite) stays green — these are additive pure-function files with zero existing-module edits.
- Grep gate: `grep -rn "bezier_drag\|useEditorStore" frontend/src/lib/bezierFit.ts frontend/src/lib/bezierFlatten.ts` returns nothing — libs are store-free and op-free.
</verification>

<success_criteria>
- BEZ-FIT-01, BEZ-INDEX-01, BEZ-FLATTEN-01, BEZ-FLATTEN-02 all have passing automated tests.
- fit-curve@0.2.0 installed; error tolerance empirically calibrated against the real Iberia fixture (constant documented in SUMMARY if tuned away from 30).
- Both libraries are pure (no React, no Konva, no store imports).
- Full vitest suite green; no backend changes.
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-01-SUMMARY.md`. Record the final calibrated `BEZ_FIT_ERROR` value and the anchor count it produced on the real Iberia barony fixture, and whether `deps.inline: ['fit-curve']` was needed in vitest.config.ts.
</output>
