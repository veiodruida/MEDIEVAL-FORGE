---
phase: 08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr
plan: 03
subsystem: ui
tags: [bezier, konva, drag-commit, flatten, snap, shared-vertex, identity-contract, vitest]

# Dependency graph
requires:
  - phase: 08.1
    plan: 01
    provides: "flattenSegment (bezierFlatten.ts); BezierCubic/PolyRange types; IBERIA_BARONY_RING fixture"
  - phase: 08.1
    plan: 02
    provides: "BezierEditLayer.tsx render+activate host; deriveAnchors/buildPathData; __forgeBezierState stub; BEZ-IDENTITY-01 guard"
  - phase: 08-border-vertex-editor
    provides: "snapToNeighbour (snap.ts); buildSharedVertexIndex/getCoupledVertices (sharedVertex.ts); VertexEditLayer handleDragEnd pattern"
provides:
  - "BezierEditLayer drag-commit path: anchor + control-handle drag → dirty-segment flatten → setVerticesAndLog(op:'move')"
  - "deriveAnchorsFromStore(vertices, projection) — pure store→anchors helper (drag flatten + test expected ranges)"
  - "Live dirtySegmentCount in window.__forgeBezierState (Plan 04 Playwright reachability)"
affects: [08.1-04, CanvasViewer]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Drag preview held in component-local previewRef; the ONLY store write is the single setVerticesAndLog(op:'move') call on dragEnd"
    - "Only DIRTY segments flatten; non-dirty index ranges copied verbatim from store.vertices (no float recompute) — the identity mechanism"
    - "Anchor drag rigidly translates BOTH control handles by the same px delta (preserves local curve shape, no cusp); control-handle drags move only the dragged handle and never snap"
    - "Phase 08 snapToNeighbour + getCoupledVertices reused verbatim; sharedIndex/snapCandidates rebuilt on entry + after each commit"

key-files:
  created:
    - frontend/src/components/canvas/__tests__/BezierEditLayer.drag.test.tsx
  modified:
    - frontend/src/components/canvas/BezierEditLayer.tsx

key-decisions:
  - "[Rule 1 fix] deriveAnchors cp2 contract changed from Plan 02's c2 to c1. fit-curve convention: c1 sits near p0 (this anchor), c2 sits near p3 (next anchor). Plan 02's SUMMARY key-decision 'cp2 is this cubic's c2' was geometrically wrong — measured on IBERIA_BARONY_RING the outgoing-handle distance |p0→c2| was 65–92px vs |p0→c1| 9–13px, so c2 actually leaves toward the NEXT anchor. Using c2 made rigid handle translation on anchor drag incoherent. Corrected to c1; buildPathData stays correct (C c1_i c2_i p3_i)."
  - "Snap candidates = vertices NOT belonging to the active barony (keyBarony prefix split); coupling index built over ALL store vertices. V1 in practice is same-session coupling — SelectionBridge loads one barony at a time, so a neighbour vertex is only present when explicitly coincident (e.g. the ring closing-duplicate, or a seeded shared partner)."
  - "Dirty-segment mapping: anchor i drag → segments i-1 and i; cp1 drag → segment i-1 only (incoming); cp2 drag → segment i only (outgoing). The i<N-1 guard keeps the closing segment N-1 non-editable (V1)."
  - "fit-curve poly ranges overlap at boundaries (range j ends and range j+1 starts at the same shared index). Flattening a dirty segment writes its full inclusive range including the shared endpoint — correct, since that endpoint is the anchor being moved."

requirements-completed: [BEZ-DRAG-01]

# Metrics
duration: 14min
completed: 2026-05-28
---

# Phase 08.1 Plan 03: Bézier drag-commit (anchor + control-handle) Summary

**`BezierEditLayer` gains the actual editing capability: dragging an anchor (or a control handle of the active anchor) flattens ONLY the adjacent dirty segments back to polygon vertices, copies every non-dirty range byte-verbatim from the store, applies snap (anchors only) + shared-vertex coupling, and commits via a single `setVerticesAndLog(op:'move')` — with the Plan 02 empty-log identity contract still green.**

## What landed

- **Anchor drag** marks BOTH adjacent segments (i-1 and i) dirty with boundary guards (i=0 → only segment 0; i=N-1 → only segment N-2; closing segment N-1 stays non-editable). It rigidly translates both control handles of the dragged anchor by the same px delta so the local curve shape is preserved (no cusp). Anchor px → geo, then `snapToNeighbour` snaps to a neighbour-barony vertex (Alt disables); when snapped, the handle translation is re-derived from the snapped position.
- **Control-handle drag** (active anchor only) moves only the dragged handle, never snaps (RESEARCH constraint 2), and marks only the segment that handle shapes dirty (cp1 → incoming segment i-1; cp2 → outgoing segment i).
- **Dirty-only flatten**: for each dirty segment the cubic is rebuilt in px from the (possibly moved) anchor/handle positions and `flattenSegment(cubic, targetCount, projection)` produces `[lon,lat]` at the original density. `nextVertices` starts as `{ ...store.vertices }` and only the dirty index ranges are overwritten — non-dirty ranges are the verbatim store values (the identity mechanism, RESEARCH Pitfall 2).
- **Shared-vertex coupling**: each changed vertex id is passed to `getCoupledVertices`; coupled partners in neighbour baronies are moved to the same coord before commit (mirrors VertexEditLayer:560-579).
- **Commit**: one `setVerticesAndLog(nextVertices, { op:'move', ts, vertexIds })`. After commit, display state is recomputed from the updated store and the shared/snap indices are rebuilt.
- `__forgeBezierState.dirtySegmentCount` is now live (was hardcoded 0 in Plan 02).

## Identity contract (BEZ-IDENTITY-01) — still GREEN

The Plan 02 byte-identity guard against the REAL store was re-run alongside the new drag tests and stays green: enter+exit with zero drags leaves `useEditorStore.vertices` byte-identical and `editLog.length` unchanged. The dirty-only flatten is what preserves this — no drag means `dirtySegments` is empty means `setVerticesAndLog` is never called.

## Task Commits

1. **Task 1 (TDD test + impl): anchor + control-handle drag → dirty-segment flatten → setVerticesAndLog(op:'move')** — `931405f` (feat)
2. **Follow-up (review fixes): anchor center-offset (+5,+5) + live preview during drag** — see git log (fix) — addresses deviations #3 and #4 surfaced in post-implementation review.

The TDD RED→GREEN cycle was committed atomically (≤1 commit per task per CLAUDE.md) because the test and the production drag path are a single coupled unit; the RED state (6 failing assertions: missing `deriveAnchorsFromStore` export + no drag handlers) was verified before GREEN. The follow-up commit lands the two review-driven Rule-1/Rule-2 fixes (NEW commit, not an amend, per GSD protocol).

## Files Created/Modified

- `frontend/src/components/canvas/BezierEditLayer.tsx` — drag handlers, dirty-segment flatten, snap, coupling, `deriveAnchorsFromStore`, deriveAnchors cp2 Rule-1 fix (modified)
- `frontend/src/components/canvas/__tests__/BezierEditLayer.drag.test.tsx` — BEZ-DRAG-01 + handle-carry + boundary (i=0, i=N-1) + coupling + control-handle op:'move' (created)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] deriveAnchors outgoing-handle (cp2) used the wrong control point**
- **Found during:** Task 1 (before writing the rigid-handle-translation logic — the action's "translate both handles by the same delta, preserves local curve shape" is geometrically incoherent if cp2 doesn't actually leave this anchor).
- **Issue:** Plan 02 set `cp2Px = cubic[j][2]` (c2). In fit-curve's `[p0,c1,c2,p3]`, c1 is near p0 and c2 is near p3 — so c2 is the handle leaving toward the NEXT anchor, not this one. Measured on IBERIA_BARONY_RING: per-cubic `|p0→c1|` ≈ 9–13px vs `|p0→c2|` ≈ 65–92px (and `|c2→p3|` ≈ 9–17px, confirming c2≈p3's handle).
- **Fix:** `cp2Px = cubic[j][1]` (c1). `buildPathData` remains correct because the cubic from anchor i→i+1 emits `C cp2Px[i]=c1_i  cp1Px[i+1]=c2_i  anchorPx[i+1]=p3_i`.
- **Files modified:** frontend/src/components/canvas/BezierEditLayer.tsx (deriveAnchors)
- **Verification:** Plan 02 render test (counts only) stays green; new handle-carry test passes; full suite 494/494.
- **Committed in:** 931405f

**2. [Rule 3 - Blocking] Fit effect must read the subscribed `vertices`, not `getState()`**
- **Found during:** Task 1 (first full run: Plan 02 render test + identity test failed `useEditorStore.getState is not a function`).
- **Issue:** An interim version read `useEditorStore.getState().vertices` inside the fit effect. The Plan 02 render-test mock only provides the selector form of `useEditorStore` (no `getState`), so the effect crashed — violating "do NOT rewrite Plan 02's render path."
- **Fix:** Fit effect reads the already-subscribed `vertices` selector value and filters to the active barony by `keyBarony` prefix (with a fallback to all keys if no prefix matches, preserving Plan 02 behavior). `getState()` is used only in the drag-commit handler, where the drag-test mock supplies it.
- **Files modified:** frontend/src/components/canvas/BezierEditLayer.tsx (fit useEffect)
- **Verification:** render + identity tests green again.
- **Committed in:** 931405f

**3. [Rule 1 - Bug] Anchor drag landed 5px off-center (Rect top-left vs center)**
- **Found during:** post-implementation review (advisor). The anchor is a 10×10 Rect positioned at `anchorPx-5` so it visually centers on the anchor. On dragEnd Konva reports the new TOP-LEFT via `e.target.x()/y()`, but the handler read it as the new anchor CENTER — so every anchor drag would land 5px up-left of the cursor and compound on each subsequent drag/refit.
- **Issue:** The unit harness masked it: the mock div forwards `dragEventAt(x,y)` straight through as `e.target.x()/y()` with no Konva offset semantics.
- **Fix:** Added `draggedCenterPx(kind, x, y)` → `[x+5, y+5]` for anchors, `[x, y]` for handles (Circles are center-anchored). Used in both `onDragMove` and `onDragEnd`. Added a dedicated test that fires a top-left and asserts the committed vertex equals `canvasToGeo(center)`.
- **Files modified:** frontend/src/components/canvas/BezierEditLayer.tsx (draggedCenterPx + handlers); BezierEditLayer.drag.test.tsx (offset test)
- **Verification:** new offset test passes; full suite 496/496.
- **Committed in:** (follow-up commit, see Task Commits)

**4. [Rule 2 - Missing critical functionality] Live preview during drag**
- **Found during:** post-implementation review (advisor). The initial `onDragMove` only updated `previewRef` + `dirtyCount`, so during a real drag only the dragged Konva node moved while the curve outline + tethers stayed frozen until commit — the UI-SPEC §Anchor step 1 / plan `<action>` live-preview behavior was missing (not a perf-only stub).
- **Fix:** `onDragMove` now sets a `previewAnchors` state via the pure `applyDragToAnchors` helper (rigid handle translation for anchor drags, single-handle for cp drags); the curve `<Path>` and active-anchor handles/tethers render from `previewAnchors ?? anchors`. Cleared on dragEnd. No store write during move. Added a test asserting the curve `data-d` changes mid-drag with zero `setVerticesAndLog` calls.
- **Files modified:** frontend/src/components/canvas/BezierEditLayer.tsx (previewAnchors state + applyDragToAnchors + render); BezierEditLayer.drag.test.tsx (live-preview test)
- **Verification:** live-preview test passes; BEZ-IDENTITY-01 still green (preview is component-local, never touches the store).
- **Committed in:** (follow-up commit, see Task Commits)

**Total deviations:** 4 auto-fixed (3 Rule 1 production bugs, 1 Rule 2 missing live-preview behavior). All surfaced before declaring done; no scope creep beyond the plan's stated drag-commit behavior.

## Dirty-segment boundary handling note (per <output>)

- Anchor i=0 → only segment 0 dirty (no segment -1). The ring's closing-duplicate vertex (coincident with anchor 0) may still move via shared-vertex coupling — this is correct and intended; the closing SEGMENT (its interior vertices) is never flattened.
- Anchor i=N-1 → only segment N-2 dirty; segment N-1 (the closing segment) has no fit-curve cubic and stays non-editable (V1 limitation, RESEARCH §Pattern 1).
- Control-handle drag respects the same closing-segment guard: dragging cp2 of the last anchor targets segment N-1 (skipped) → no commit fires.

## Verification

- `npx vitest run src/components/canvas/__tests__/BezierEditLayer.drag.test.tsx src/components/canvas/__tests__/BezierEditLayer.test.tsx src/stores/__tests__/useEditorStore.bezierIdentity.test.ts` → 14/14 pass (8 drag + 4 render + 2 identity).
- Full suite: **496/496 green** (488 pre-existing + 8 new); tsc `--noEmit` exit 0.
- Grep gate: `snapToNeighbour` + `getCoupledVertices` both present (reused, not reimplemented); `flattenSegment` called per dirty segment; the sole `setVerticesAndLog` call uses `op:'move'`; `bezier_drag` appears only in a guardrail comment (no op token).

## Known Stubs

None. Live preview during drag IS implemented (deviation #4): the curve outline + handles
repaint from a component-local `previewAnchors` state on every `onDragMove`. The only deferred
polish is using a `Konva.Shape sceneFunc ctx.bezierCurveTo` for the single dragged segment
instead of recomputing the full `Konva.Path data` string (UI-SPEC Note #9, a 60fps optimization
— committed geometry is identical either way). Not a stub: the live-preview feature works; only
its rendering primitive is the simpler one. A future perf pass can swap in the sceneFunc.

## Next Phase Readiness

- Plan 04 (Playwright reachability) can read `window.__forgeBezierState().dirtySegmentCount` (now live) and drive anchor drags via the `bezier-anchor` / `bezier-handle` test-ids (now `draggable`).
- CanvasViewer mount wiring (mutual exclusion with VertexEditLayer at z=5) remains a later-plan concern.

## Self-Check: PASSED

Both files exist on disk; task commit 931405f present in git history.

---
*Phase: 08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr*
*Completed: 2026-05-28*
