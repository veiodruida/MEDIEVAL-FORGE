---
phase: 08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/useZoomPan.ts
  - frontend/src/components/canvas/BezierEditLayer.tsx
  - frontend/src/components/canvas/CanvasViewer.tsx
  - frontend/src/components/canvas/__tests__/BezierEditLayer.test.tsx
  - frontend/tests/uat/playwright/08.1-bezier-usable-targets.spec.ts
autonomous: true
gap_closure: true
requirements: [BEZ-RENDER-01, BEZ-RENDER-02, BEZ-DRAG-01, BEZ-UAT-01]

user_setup: []

must_haves:
  truths:
    - "Anchors, control handles, and the dbl-click hit-path render at a CONSTANT on-screen size regardless of stage zoom: size = BASE / currentScale, so the visible square stays ~10 CSS px at any scale (G5)"
    - "The designer can zoom into a single barony: MAX_SCALE_MULTIPLIER is raised from 4 to 16 so wheel-zoom reaches 16x fit (G5)"
    - "currentScale is threaded into BezierEditLayer as a reactive prop (mirroring DecorationsLayer), so handle/anchor size updates LIVE on every wheel tick without remount (G5)"
    - "The anchor Rect half-size offset, draggedCenterPx center recovery, AND the __forgeBezierTriggerDrag DEV hook offset all use the SAME live halfSize = (BASE/currentScale)/2 — they stay in sync so a drag commits to the correct center (G5 correctness trap)"
    - "A REAL page.mouse double-click on the curve at a usable zoom inserts an anchor (anchorCount increments) — driven by real Konva hit-testing, NOT __forgeBezierTriggerInsert (G7)"
    - "BEZ-IDENTITY-01 stays green: G5/G7 are display-only (sizing + zoom cap + real-event wiring) and add ZERO store writes on entry/exit/insert"
  artifacts:
    - path: "frontend/src/hooks/useZoomPan.ts"
      provides: "MAX_SCALE_MULTIPLIER raised 4 -> 16"
      contains: "MAX_SCALE_MULTIPLIER = 16"
    - path: "frontend/src/components/canvas/BezierEditLayer.tsx"
      provides: "currentScale prop + screen-space sizing for anchor Rect, control-handle Circle, hit-path stroke, tether stroke; live halfSize in Rect offset + draggedCenterPx + DEV drag hook"
      contains: "currentScale"
    - path: "frontend/src/components/canvas/CanvasViewer.tsx"
      provides: "currentScale + minScale threaded into BezierEditLayer (the prop already feeds DecorationsLayer)"
      contains: "currentScale={currentScale}"
    - path: "frontend/tests/uat/playwright/08.1-bezier-usable-targets.spec.ts"
      provides: "REAL page.mouse drag at zoom + REAL dblclick insert at zoom; explicit ban on __forgeBezierTrigger* for the claim"
  key_links:
    - from: "BezierEditLayer anchor Rect / Circle / hit-path / DEV drag hook"
      to: "currentScale prop"
      via: "size = BASE / currentScale, halfSize = (BASE/currentScale)/2"
      pattern: "/ currentScale|/ *currentScale"
    - from: "CanvasViewer <BezierEditLayer>"
      to: "currentScale state"
      via: "currentScale={currentScale} prop (same value passed to DecorationsLayer)"
      pattern: "currentScale=\\{currentScale\\}"
    - from: "08.1-bezier-usable-targets.spec.ts"
      to: "real Konva hit-testing (page.mouse / dblclick)"
      via: "page.mouse.move/down/up + page.mouse.dblclick at zoomed scale; NO __forgeBezierTrigger* for the reachability claim"
      pattern: "page.mouse|dblclick"
---

<objective>
Fechar G5 (alvos interativos pequenos demais + zoom máximo baixo demais) e G7 (não é possível adicionar pontos de controle com duplo-clique real). Ambos os gaps editam `BezierEditLayer.tsx` — o fix de hit-path em SCREEN space que G5 entrega É o que torna o duplo-clique real do G7 alcançável, então G7 vira uma asserção extra de evento real no spec, não um plano separado.

Purpose: tornar a edição Bézier USÁVEL no browser real. Hoje todos os nós interativos (anchor Rect 10×10, handle Circle r=4, hit-path strokeWidth=12) são dimensionados em MAP space; o zoom do Stage os encolhe para ~3px na tela no fit-to-view de Iberia ("~3px anchor square at Iberia fit-to-view scale", BezierEditLayer.tsx:620), e `MAX_SCALE_MULTIPLIER = 4` (useZoomPan.ts:5) limita o zoom a 4× fit — baixo demais para trabalhar numa baronia.

Output: anchors/handles/hit-path dimensionados em SCREEN space (tamanho constante na tela = BASE / currentScale, espelhando o padrão de `DecorationsLayer` que já escala labels/capitais por currentScale); `MAX_SCALE_MULTIPLIER` elevado para 16; e um spec Playwright NOVO que dirige um drag REAL com `page.mouse` e um duplo-clique REAL num zoom utilizável, banindo explicitamente `__forgeBezierTrigger*` para a alegação.

**ARMADILHA DE CORREÇÃO (LOCKED — escrever no código):** o meio-tamanho `5` (metade de 10) aparece em TRÊS lugares, não dois. (1) o offset top-left do anchor Rect (`x = anchorPx[0] - 5`, linhas 750-751); (2) `draggedCenterPx` que recupera o centro (`x + 5`, linha 184); (3) o DEV hook `__forgeBezierTriggerDrag` que computa o alvo (`a.anchorPx[0] - 5 + dx`, ~linha 629). Quando o tamanho vira `S = ANCHOR_BASE_PX / currentScale`, os TRÊS precisam usar o MESMO `halfSize = S / 2` calculado a partir do `currentScale` VIVO. Se qualquer um ficar com o `5` fixo, o drag commita no centro errado e o spec existente (que ainda usa o DEV hook) quebra silenciosamente.

**CONTRATO DE IDENTIDADE (LOCKED — não negociável):** G5 e G7 são display-only — mudança de tamanho, teto de zoom, e fiação de evento real. NENHUM write novo no store em entrada/saída/insert. As mudanças de sizing tocam APENAS props visuais do Konva (width/height/radius/strokeWidth/offset). O insert por duplo-clique já é NO-OP identity-safe (entregue por 08.1-06). Os testes `useEditorStore.bezierIdentity` continuam verdes; o spec novo re-afirma a identidade (entrar+sair com zero drags → editLog inalterado).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-CONTEXT.md
@.planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-HUMAN-UAT.md

<interfaces>
<!-- Extracted from the codebase. The executor should use these directly — no exploration needed. -->

From frontend/src/hooks/useZoomPan.ts (line 5) — the zoom cap to raise:
```typescript
export const MAX_SCALE_MULTIPLIER = 4   // → change to 16
```
makeWheelHandler clamps newScale to [minScale, maxScale]; CanvasViewer builds maxScale as `minScale * MAX_SCALE_MULTIPLIER` (CanvasViewer.tsx:566). No other change needed in useZoomPan to raise the cap.

From frontend/src/components/canvas/DecorationsLayer.tsx — the SCREEN-space-sizing pattern to MIRROR (already shipping):
```typescript
// Props include: currentScale: number; minScale: number
const safeScale = props.scale > 0 ? props.scale : 1
const fontSize = TARGET_LABEL_SCREEN_PX / safeScale     // invert stage scale → constant on-screen px
const shadowBlur = 6 / safeScale
const strokeWidth = 2 / safeScale
```
CanvasViewer already passes `currentScale={currentScale}` and `minScale={minScale}` to DecorationsLayer (CanvasViewer.tsx:754-755). `currentScale` is React state updated by handleWheel (`setCurrentScale(s)`, CanvasViewer.tsx:590) and by fitToView (CanvasViewer.tsx:419) — so a prop wired the same way re-renders BezierEditLayer LIVE on every wheel tick.

From frontend/src/components/canvas/BezierEditLayer.tsx — current MAP-space literals to convert (G5):
```typescript
export interface BezierEditLayerProps {
  projection: ProjectionConfig
}
// draggedCenterPx — the +5 center recovery (line 183-185):
function draggedCenterPx(kind: DragKind, x: number, y: number): [number, number] {
  return kind === 'anchor' ? [x + 5, y + 5] : [x, y]
}
// anchor Rect (lines 748-764): x={a.anchorPx[0] - 5} y={a.anchorPx[1] - 5} width={10} height={10}
// hit-path (lines 722-737): strokeWidth={12}
// control handle Circle (lines 791-808): radius={4}
// tether Line (lines 775-790): strokeWidth={1}
// DEV drag hook (line 629): const targetX = a.anchorPx[0] - 5 + dx   // ← the silent third "5"
```

From frontend/src/components/canvas/CanvasViewer.tsx — mount site (lines 771-773):
```tsx
{bezierActive ? (
  <BezierEditLayer projection={projection} />
) : (
  <VertexEditLayer ... />
)}
// currentScale + minScale are in scope here (useState, lines 139-140); passed to DecorationsLayer at 754-755.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise MAX_SCALE_MULTIPLIER 4 → 16</name>
  <files>frontend/src/hooks/useZoomPan.ts</files>
  <read_first>
    - frontend/src/hooks/useZoomPan.ts (line 5 — the constant; makeWheelHandler clamp at line 67)
    - frontend/src/components/canvas/CanvasViewer.tsx (line 566 — `makeWheelHandler(minScale, minScale * MAX_SCALE_MULTIPLIER, ...)`)
  </read_first>
  <action>
    In `frontend/src/hooks/useZoomPan.ts` change the literal on line 5 from `export const MAX_SCALE_MULTIPLIER = 4` to `export const MAX_SCALE_MULTIPLIER = 16`. Update the inline comment if present to read "16× fit — raised from 4 (Phase 08.1 G5: 4× too low to edit one barony)". Do NOT touch SCALE_BY (1.05), the clamp math in makeWheelHandler, or applyPanClamp — the cap flows through `minScale * MAX_SCALE_MULTIPLIER` at CanvasViewer.tsx:566 with no other change. This is the single-line zoom-cap fix; surgical, no adjacent edits.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/__tests__/useZoomPan.test.ts 2>NUL || rg -n "MAX_SCALE_MULTIPLIER = 16" src/hooks/useZoomPan.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "MAX_SCALE_MULTIPLIER = 16" frontend/src/hooks/useZoomPan.ts` returns exactly one hit
    - `rg -n "MAX_SCALE_MULTIPLIER = 4" frontend/src/hooks/useZoomPan.ts` returns zero hits
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>MAX_SCALE_MULTIPLIER is 16; wheel-zoom now reaches 16× fit; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Screen-space sizing in BezierEditLayer (currentScale prop) — anchors, handles, hit-path, tether, AND the three "5" sites</name>
  <files>frontend/src/components/canvas/BezierEditLayer.tsx</files>
  <read_first>
    - frontend/src/components/canvas/BezierEditLayer.tsx (FULL — lines 75-78 props, 183-185 draggedCenterPx, 622-639 DEV drag hook, 701-816 render)
    - frontend/src/components/canvas/DecorationsLayer.tsx (lines 42-67 — the `/ safeScale` inversion pattern to mirror)
    - frontend/src/components/canvas/CanvasViewer.tsx (lines 138-140 currentScale/minScale state; 747-784 mount site)
  </read_first>
  <action>
    Make every interactive Bézier node a CONSTANT on-screen size by dividing its map-space size by the live stage scale, mirroring DecorationsLayer's `value / safeScale` inversion.

    1) Add `currentScale: number` to `BezierEditLayerProps` (after `projection`). Destructure it in the component signature: `({ projection, currentScale })`.

    2) At the top of the component body, declare base constants and a safe scale (guard against 0/NaN exactly like DecorationsLayer's `safeScale`):
    ```typescript
    const ANCHOR_BASE_PX = 10      // on-screen square size (was the map-space width/height)
    const HANDLE_BASE_PX = 9       // on-screen handle diameter → radius = HANDLE_BASE_PX / 2 / scale
    const HITPATH_BASE_PX = 24     // on-screen dbl-click target width (was 12; widen for real dblclick — G7)
    const TETHER_BASE_PX = 1.5     // on-screen tether stroke width
    const CURVE_BASE_PX = 2        // on-screen curve outline stroke width
    const safeScale = currentScale > 0 ? currentScale : 1
    const anchorSize = ANCHOR_BASE_PX / safeScale
    const anchorHalf = anchorSize / 2
    const handleRadius = (HANDLE_BASE_PX / 2) / safeScale
    const hitPathWidth = HITPATH_BASE_PX / safeScale
    const tetherWidth = TETHER_BASE_PX / safeScale
    const curveWidth = CURVE_BASE_PX / safeScale
    ```

    3) anchor Rect (lines 748-764): replace the four hard-coded literals:
       - `x={a.anchorPx[0] - 5}` → `x={a.anchorPx[0] - anchorHalf}`
       - `y={a.anchorPx[1] - 5}` → `y={a.anchorPx[1] - anchorHalf}`
       - `width={10}` → `width={anchorSize}`
       - `height={10}` → `height={anchorSize}`

    4) draggedCenterPx (line 183-185): it currently hard-codes `+5`. It must use the LIVE half-size. Change its signature to accept the half-size and recover the center with it:
    ```typescript
    function draggedCenterPx(kind: DragKind, x: number, y: number, anchorHalf: number): [number, number] {
      return kind === 'anchor' ? [x + anchorHalf, y + anchorHalf] : [x, y]
    }
    ```
    Update BOTH call sites inside handlers (handleDragMove line ~424, handleDragEnd line ~444) to pass `anchorHalf`. Because handleDragMove/handleDragEnd are useCallback closures, `anchorHalf` must be reachable: read it from a `useRef` updated each render (e.g. `anchorHalfRef.current = anchorHalf`) OR add `anchorHalf` to the useCallback dep arrays and pass it explicitly. PREFER the ref approach (mirrors how `anchorsRef.current = anchors` is already done at line 256) to avoid re-binding every render. Declare `const anchorHalfRef = useRef(anchorHalf); anchorHalfRef.current = anchorHalf` near anchorsRef, and call `draggedCenterPx(kind, ..., anchorHalfRef.current)`.

    5) DEV drag hook `__forgeBezierTriggerDrag` (line 629) — THE SILENT THIRD "5". It computes `const targetX = a.anchorPx[0] - 5 + dx`. Change to `const targetX = a.anchorPx[0] - anchorHalfRef.current + dx` and the same for `targetY`. This keeps the existing 08.1-bezier-edit.spec.ts DEV-hook drag committing to the correct center after sizing changes. (The handle-drag hook at ~line 652 uses NO offset — center-anchored Circles — leave it unchanged.)

    6) control handle Circles (lines 791-808, both cp1 and cp2): `radius={4}` → `radius={handleRadius}`.

    7) tether Lines (lines 775-790, both): `strokeWidth={1}` → `strokeWidth={tetherWidth}`.

    8) curve outline Path (line 713): `strokeWidth={2}` → `strokeWidth={curveWidth}`. Hit-path Path (line 725): `strokeWidth={12}` → `strokeWidth={hitPathWidth}` (HITPATH_BASE_PX=24 → wider real-dblclick target, G7).

    Do NOT add any setVerticesAndLog / store write. Do NOT change the flatten/commit logic, the dirty-segment routing, or the color constants. Surgical: every changed line traces to sizing or the threaded prop. Keep `data-testid` attributes intact (the spec + DEV hook rely on `bezier-anchor`, `bezier-hit-path`, `data-anchor-idx`).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/canvas/__tests__/BezierEditLayer.test.tsx src/components/canvas/__tests__/BezierEditLayer.drag.test.tsx src/components/canvas/__tests__/BezierEditLayer.addpoint.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "currentScale" frontend/src/components/canvas/BezierEditLayer.tsx` shows currentScale in the props interface AND the component signature
    - `rg -n "anchorHalf|handleRadius|hitPathWidth" frontend/src/components/canvas/BezierEditLayer.tsx` returns the screen-space size variables in use
    - `rg -n "- 5 \+ dx|x \+ 5|anchorPx\[0\] - 5" frontend/src/components/canvas/BezierEditLayer.tsx` returns ZERO hits (all three "5" sites converted to anchorHalf)
    - `rg -n "width=\{10\}|radius=\{4\}|strokeWidth=\{12\}" frontend/src/components/canvas/BezierEditLayer.tsx` returns ZERO hits (literals replaced by /scale expressions)
    - BezierEditLayer.test.tsx, BezierEditLayer.drag.test.tsx, BezierEditLayer.addpoint.test.tsx all pass (the drag tests prove draggedCenterPx still recovers the correct center via anchorHalf)
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Anchors/handles/hit-path/tether/curve all size as BASE/currentScale; the three "5" sites use the live anchorHalf; existing unit + drag + addpoint tests green; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Thread currentScale into BezierEditLayer at the mount site + extend the unit render test</name>
  <files>frontend/src/components/canvas/CanvasViewer.tsx, frontend/src/components/canvas/__tests__/BezierEditLayer.test.tsx</files>
  <read_first>
    - frontend/src/components/canvas/CanvasViewer.tsx (lines 138-140 state; 747-757 DecorationsLayer props; 771-773 BezierEditLayer mount)
    - frontend/src/components/canvas/__tests__/BezierEditLayer.test.tsx (existing render tests — must pass currentScale now)
  </read_first>
  <action>
    1) In `CanvasViewer.tsx` at the BezierEditLayer mount (line ~772) pass the SAME `currentScale` value already fed to DecorationsLayer:
    ```tsx
    {bezierActive ? (
      <BezierEditLayer projection={projection} currentScale={currentScale} />
    ) : (
      <VertexEditLayer ... />
    )}
    ```
    `currentScale` is the existing useState at CanvasViewer.tsx:140, kept in sync by handleWheel (setCurrentScale, line 590) and fitToView (line 419). No new state, no new effect — reuse the value DecorationsLayer already consumes. Do NOT pass minScale unless a sizing rule needs it (the sizing here is pure `BASE/currentScale`, so currentScale alone suffices — keep it minimal per karpathy simplicity).

    2) In `BezierEditLayer.test.tsx`, every `<BezierEditLayer .../>` render now requires the `currentScale` prop. Add `currentScale={1}` to each existing render (scale 1 = sizes equal the BASE px, so existing assertions on render presence stay valid). Add ONE new descriptive test: `it('sizes the anchor square in screen space: width = ANCHOR_BASE_PX / currentScale', ...)` — render with `currentScale={4}` and assert the first `bezier-anchor` Rect's `width` prop equals `10 / 4 = 2.5` (use the explicit numeric fixture 2.5, not a computed expression, per the descriptive-tests memory). If the Konva mock does not expose the width prop directly, assert via the rendered node's attribute the test harness already inspects in the file (match the existing assertion style in BezierEditLayer.test.tsx — do NOT invent a new query mechanism).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/canvas/__tests__/BezierEditLayer.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "currentScale=\{currentScale\}" frontend/src/components/canvas/CanvasViewer.tsx` returns at least one hit on the BezierEditLayer mount (in addition to the existing DecorationsLayer one)
    - `rg -n "currentScale=\{1\}|currentScale=\{4\}" frontend/src/components/canvas/__tests__/BezierEditLayer.test.tsx` shows the prop passed in tests
    - The new screen-space-sizing test asserting `2.5` (= 10/4) is present and passes
    - BezierEditLayer.test.tsx all green; `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>currentScale flows CanvasViewer → BezierEditLayer; unit test proves screen-space sizing math (10/4=2.5); tests green; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 4: Playwright UAT — REAL page.mouse drag + REAL dblclick insert at usable zoom (G5 + G7), real-event-only</name>
  <files>frontend/tests/uat/playwright/08.1-bezier-usable-targets.spec.ts</files>
  <read_first>
    - frontend/tests/uat/playwright/08.1-bezier-edit.spec.ts (FULL — reuse loadProjectInfo, discoverBaronyId, navigateToWorkspace, readBezierState helpers; this NEW spec is a sibling file, not an edit of that one)
    - frontend/tests/uat/playwright/globalSetup.ts (seeds the Iberia 868 project; UAT_PROJECT_INFO_PATH)
    - frontend/src/components/canvas/CanvasViewer.tsx (lines 490-498 — __forgeStageScale DEV hatch exposing the live stage scale to Playwright; line 588-593 handleWheel)
  </read_first>
  <action>
    Create `frontend/tests/uat/playwright/08.1-bezier-usable-targets.spec.ts`. This spec closes the FALSE-GREEN gap (RESEARCH Pitfall 5): it MUST drive REAL Konva events at a zoomed-in scale and MUST NOT call `__forgeBezierTrigger*` for the reachability/insert claim. Reuse the helper functions from 08.1-bezier-edit.spec.ts (copy `loadProjectInfo`, `discoverBaronyId`, `navigateToWorkspace`, `readBezierState`, `BezierState` — they are small and self-contained; do not import across spec files).

    Shared setup (per test): navigate to the seeded project, click `edit-tool-v` via the visible palette, `__forgeSelectBarony(id)` with a runtime-discovered barony id (selection is allowed via DEV hatch — it is NOT the claim under test), poll until `readBezierState(page).anchorCount >= 4`.

    Zoom in to a usable scale with REAL wheel events (NOT a store write):
    - Get the canvas bounding box: `const box = await page.getByTestId('canvas-stage').boundingBox()`.
    - Center the pointer over the barony, then dispatch several real wheel-up ticks: loop ~12 times `await page.mouse.move(cx, cy); await page.mouse.wheel(0, -100)` (deltaY<0 = zoom in per makeWheelHandler). Poll `__forgeStageScale` until it is at least ~6× the initial fit scale (read initial via `await page.evaluate(() => (window as any).__forgeStageScale)` before zooming). This proves the raised MAX_SCALE_MULTIPLIER=16 lets the user zoom in (G5).

    TEST 1 — REAL drag commits (G5 reachability via real mouse):
    - Read `firstAnchor` from `readBezierState(page)` AFTER zooming (its x/y are PAGE coords computed from getClientRect, so they reflect the zoomed scale). Assert `firstAnchor` is non-null.
    - Capture `editLog0 = readBezierState().editLogLength`.
    - Drive a REAL drag: `await page.mouse.move(firstAnchor.x, firstAnchor.y); await page.mouse.down(); await page.mouse.move(firstAnchor.x + 30, firstAnchor.y + 30, { steps: 8 }); await page.mouse.up()`.
    - Poll `readBezierState().editLogLength` until `> editLog0` (timeout 8000). This is the durable proof the user can GRAB and DRAG a usable-size anchor → op:'move' commits, with NO DEV trigger hook.
    - EXPLICIT BAN comment + guard: add a header comment "ANTI-PATTERN GUARD: this spec MUST NOT call __forgeBezierTriggerDrag / __forgeBezierTriggerInsert / __forgeBezierTriggerHandleDrag for the claim. The commit/insert must flow through real page.mouse events only." (Grep gate in acceptance asserts zero hits.)

    TEST 2 — REAL dblclick insert (G7):
    - After zooming, capture `anchorCount0` and `editLog0` from readBezierState.
    - Find a point ON the curve outline to dblclick. Use `firstAnchor` (which sits exactly on the ring) as a reliable on-curve target, OR compute a mid-curve page point: but the simplest robust target is `firstAnchor.x, firstAnchor.y` (the hit-path passes through every anchor). Dispatch a REAL double-click: `await page.mouse.dblclick(firstAnchor.x, firstAnchor.y)`.
    - Poll `readBezierState().anchorCount` until `=== anchorCount0 + 1` (timeout 8000) — proves the real Konva onDblClick on the widened (24/scale) hit-path fired and inserted an anchor (G7).
    - Assert `readBezierState().editLogLength === editLog0` immediately after the insert (insert is identity-safe NO-OP, BEZ-IDENTITY-01 extended — no store write on bare insert).

    TEST 3 — empty-log identity re-assertion (BEZ-IDENTITY-01 through the real UI):
    - Select the barony (BezierEditLayer mounts, fits anchors), capture `editLog0`. Do NOT drag, do NOT insert. Concretely: right after the anchors mount with zero interaction, assert `readBezierState().editLogLength === editLog0` (it was captured at mount). This re-affirms entry+exit with zero drags adds no editLog entry. Then deselect by clicking an empty corner of the canvas box away from the barony (`page.mouse.click`) and assert the BezierState is null (layer unmounted) OR editLogLength is still editLog0.

    Use the same describe/test structure and timeouts (15_000 mount poll, 8_000 commit poll) as 08.1-bezier-edit.spec.ts. Tests must be deterministic — poll, never fixed sleeps.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test tests/uat/playwright/08.1-bezier-usable-targets.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/tests/uat/playwright/08.1-bezier-usable-targets.spec.ts` exists
    - `rg -n "__forgeBezierTrigger" frontend/tests/uat/playwright/08.1-bezier-usable-targets.spec.ts` returns ZERO hits (no DEV trigger hook used for the claim)
    - `rg -n "page.mouse.down|page.mouse.up|page.mouse.dblclick|page.mouse.wheel" frontend/tests/uat/playwright/08.1-bezier-usable-targets.spec.ts` returns hits (real events drive the drag, dblclick, and zoom)
    - The 3 tests pass: real-drag commits (editLogLength grows), real-dblclick inserts (anchorCount +1, editLog unchanged), empty-log identity (editLog unchanged at mount)
    - `rg -n "setVerticesAndLog|moveVertex" frontend/tests/uat/playwright/08.1-bezier-usable-targets.spec.ts` returns ZERO hits (no direct store action)
  </acceptance_criteria>
  <done>A real-event Playwright spec proves the designer can zoom in, grab+drag a usable-size anchor (commit), and double-click to insert — all via real Konva hit-testing; identity contract re-asserted; __forgeBezierTrigger* banned for the claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user pointer → Konva Stage | Mouse/wheel events drive zoom and node hit-testing; all client-side, no server crossing |
| BezierEditLayer → useEditorStore | Only the drag-commit path writes (op:'move'); sizing/zoom changes never write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-07-01 | Tampering | draggedCenterPx + DEV drag hook half-size desync | mitigate | The three "5" sites all read the SAME live `anchorHalf` (anchorHalfRef.current); unit drag tests assert the committed center is correct, catching desync regression |
| T-08.1-07-02 | Denial of Service | currentScale = 0 or NaN → division by zero in sizing | mitigate | `safeScale = currentScale > 0 ? currentScale : 1` guard mirrors DecorationsLayer; no NaN sizes reach Konva |
| T-08.1-07-03 | Tampering | DEV hooks (__forge*) shipping in production | accept | All __forge* hooks remain `import.meta.env.DEV`-gated (pre-existing, unchanged by this plan); never in prod bundle |
</threat_model>

<verification>
- `cd frontend && npx vitest run` — full suite green (no regression from prop threading / sizing)
- `cd frontend && npx vitest run src/stores/__tests__/useEditorStore.bezierIdentity.test.ts` — identity contract still green (display-only changes add no store writes)
- `cd frontend && npx playwright test tests/uat/playwright/08.1-bezier-usable-targets.spec.ts` — 3 real-event tests pass
- `cd frontend && npx tsc --noEmit` — clean
- Grep gates: zero `width={10}`/`radius={4}`/`strokeWidth={12}` literals in BezierEditLayer.tsx; zero `__forgeBezierTrigger` in the new spec; `MAX_SCALE_MULTIPLIER = 16`
</verification>

<success_criteria>
- Anchors, handles, and hit-path stay a constant on-screen size at any zoom (size = BASE / currentScale), mirroring DecorationsLayer
- MAX_SCALE_MULTIPLIER = 16 lets the designer zoom into a single barony
- The three half-size "5" sites (Rect offset, draggedCenterPx, DEV drag hook) all use the live anchorHalf — drag commits to the correct center
- A real page.mouse drag at usable zoom commits op:'move' (G5); a real dblclick at usable zoom inserts an anchor (G7) — both via real Konva hit-testing, NOT DEV hooks
- BEZ-IDENTITY-01 stays green; zero new store writes on entry/exit/insert
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-07-SUMMARY.md`
</output>
