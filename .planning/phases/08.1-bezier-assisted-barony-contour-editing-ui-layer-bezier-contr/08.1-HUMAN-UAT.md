---
status: partial
phase: 08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr
source: [08.1-VERIFICATION.md]
started: 2026-05-28T16:28:29Z
updated: 2026-05-29T16:10:00Z
---

## Current Test

[2026-05-29 HUMAN re-verify DONE. G5 (grabbable anchors at 16× zoom) RESOLVED, G7 (real dblclick insert) RESOLVED — designer confirmed in browser. G6 PARTIAL: amber overlay delivered + confirmed moving, but designer judged it insufficient — the colored barony boundary itself does not change ("nada mudou nos limites das baronias"). Root cause traced: backend manual_edit.py:128 never implemented move/add/delete replay, so Bézier op:'move' edits are saved to the branch edit-log but dropped by the render pipeline → colored map + export never converge. Tracked as G8, DEFERRED to new phase 08.2 (backend move-replay + render cascade) per user decision. 08.1 closes on its UI-only charter; end-to-end edit→map convergence is 08.2's goal.]

## Tests

### 1. Bézier handle usability vs. raw per-vertex dots
expected: Selecting a smoothed Voronoi barony under the V-tool shows ~10-20 anchor squares + active-anchor handles instead of hundreds of raw vertex dots. The control-point editor is subjectively usable for shaping a contour.
result: fail — anchors render but are far too small to see/grab at fit-to-view zoom; not usable (see G5).

### 2. Re-flattened contour does not visibly thicken the map
expected: After dragging an anchor and committing, the re-flattened barony contour does not visibly thicken or distort the rendered map.
result: fail — "edit não persistiu, nada mudou depois de alterar as béziers." Drag produces no visible change to the map (see G5 grab-failure + G6 render-feedback).

### 3. Closed ring + readable palette
expected: Bézier outline forms a visibly closed loop (G1); curve/anchor/active/handle/tether distinguishable (G4).
result: pending — could not properly evaluate; elements too small to read at usable zoom (gated by G5).

### 4. Add control point by double-click
expected: Double-clicking the curve outline inserts a new anchor at the nearest point, increasing contour detail; subsequent drag persists (G3).
result: fail — "não consegui adicionar novos pontos de controle." Dbl-click hit-path is ~3px wide on screen at fit zoom; misses (see G5).

## Summary

total: 4
passed: 0
issues: 3
pending: 1
skipped: 0
blocked: 0

## Gaps

### G1. Bézier curve not closed (open ring outline)
status: resolved
severity: high
expected: The Bézier outline forms a closed loop around the barony; every segment of the ring is editable.
actual: Some baronies show an open/unclosed Bézier. `buildPathData` (BezierEditLayer.tsx:96) iterates `i = 0 .. anchors.length-2`, drawing only anchor[0]→anchor[N-1]; it never emits the closing cubic anchor[N-1]→anchor[0]. The closing segment N-1 is also force-excluded from editing (`addSeg` guard `s < N-1`, line 387). Net: the closing arc is neither drawn nor editable, so the contour reads as open.
fix_hint: Emit the closing cubic in buildPathData (wrap N-1 → 0) and make segment N-1 editable (remove/relax the `s < N-1` guard), threading the closing range through buildPolyIndexMap.

### G2. Edited curve not saved (silent drop on commit)
status: resolved
severity: high
expected: Dragging any anchor or control handle and releasing persists the edit (store + render).
actual: WR-01 (08.1-REVIEW.md). For handles whose only affected segment is the non-editable closing segment — cp1 of anchor 0 (`addSeg(-1)` rejected) and cp2 of the last anchor (`addSeg(N-1)` rejected) — `dirty` ends empty, `changedIds.length === 0`, and handleDragEnd early-returns (line 426) AFTER the live preview already repainted (line 341). The user sees the curve follow the drag, releases, and the change vanishes. Tied to G1 — fixing the closing segment removes most of this dead zone.
fix_hint: After G1 makes segment N-1 editable, the empty-dirty case largely disappears. Also reconcile activeAnchorIdx after refit (WR-02) and harden zero-length flatten (WR-03).

### G3. Cannot add new control points
status: resolved
severity: medium
expected: User can add a new anchor/control point on the curve to increase contour detail.
actual: No add-point affordance exists. V1 fit is fixed at the cubic count fit-curve returns; the only edits are dragging existing anchors/handles.
fix_hint: New feature — double-click on the curve outline inserts an anchor at the nearest curve parameter, splitting the underlying segment's poly range. Out of original V1 scope; confirm priority before planning.

### G4. No color differentiation between lines, points, contour, controls
status: resolved
severity: low
expected: Distinct colors for curve outline vs. anchor points vs. control handles vs. tethers, so the edit surface is readable.
actual: Curve outline (CURVE_STROKE #4a9eff) is the SAME blue as inactive anchors (ANCHOR_INACTIVE_FILL #4a9eff). Handles + tethers share #94a3b8. Hard to distinguish elements at a glance.
fix_hint: Pick a distinct palette — e.g. contour one hue, anchors another, active anchor a third, handles/tethers a muted fourth. Update the UI-SPEC color table + the hex literals at BezierEditLayer.tsx:49-54.

### G5. Interactive targets too small to use at working zoom + max zoom too low
status: resolved
closure: Plan 08.1-07 (commits 367ee79, a928d0f, 31585fc, adefb6d) — screen-space sizing (ANCHOR/HANDLE/HITPATH = BASE/currentScale, constant ~10px at any zoom), MAX_SCALE_MULTIPLIER 4→16, currentScale threaded reactively. Playwright G5 real-drag 3/3 green; verifier 17/17 code-level. AWAITING human browser re-verify (grab feel).
severity: high
source: 2026-05-29 human browser UAT
expected: Anchors, control handles, and the dbl-click hit-path stay a usable on-screen size at any zoom, and the user can zoom in far enough to comfortably edit a single small barony.
actual: All interactive targets are sized in MAP space — anchor `Rect width/height={10}` (BezierEditLayer.tsx:750-753), control handle `Circle radius={4}` (791-808), dbl-click `bezier-hit-path` `strokeWidth={12}` (723-728). Konva scales the whole layer by the stage zoom, so at Iberia fit-to-view scale these render at only a few screen px (the code itself documents "~3px anchor square at Iberia fit-to-view scale", line 620). The user: "as âncoras são muito pequenas, não se consegue ver." Sub-px targets cannot be grabbed (→ no drag → G6) or double-clicked (→ G7). Compounded by `MAX_SCALE_MULTIPLIER = 4` (useZoomPan.ts:5) capping zoom at 4× fit — too low to work on one territory ("deve permitir um zoom maior para se trabalhar nos territórios").
fix_hint: Size anchors/handles/hit-path in SCREEN space — divide width/radius/strokeWidth by the current stage scale (e.g. `10 / currentScale`) so they stay constant on screen, mirroring DecorationsLayer's `currentScale`/`minScale` scaling. AND raise `MAX_SCALE_MULTIPLIER` (e.g. 12–16) so the designer can zoom into a single barony. Re-test with REAL mouse, not DEV hooks.

### G6. Edits do not visibly change the map (render-feedback / persistence path unverified)
status: partial
disposition: Overlay (option a) DELIVERED + human-confirmed working. Full goal "colored barony boundary changes" requires backend move-replay → tracked as G8, deferred to NEW phase 08.2 (user decision 2026-05-29).
human_verdict: 2026-05-29 human re-verify — G5 (grab) + G7 (insert) CONFIRMED working, amber overlay CONFIRMED appears/moves. BUT designer judged the overlay INSUFFICIENT: "nada mudou nos limites das baronias" — the colored barony boundary (the real map) must change, not just an overlay. Colored-raster convergence re-opens as new work → see G8.
closure: Plan 08.1-08 (commits ee22de9, 53dc9ab, 8cef67b) — locked decision (option a): live amber-dashed edited-contour overlay (`bezier-edited-contour`) renders the active barony's flattened store.vertices read-only, so committed drags immediately change the map. Reproduction discriminator proves G6 real (colored backend ring stays byte-identical, op:'move' never POSTs) AND fix works (editedRingSnapshot differs after drag). Colored BaronyLayer convergence via /render intentionally out of scope. Playwright 2/2 green.
severity: high
source: 2026-05-29 human browser UAT
expected: Dragging an anchor/handle and releasing visibly updates the rendered barony on the map (and persists).
actual: "Edit das curvas não persistiu e nada mudou depois de eu alterar as béziers." Two compounding causes, second NOT independently verified: (1) G5 — targets too small to actually grab, so most "edits" never registered a real drag. (2) The visible colored barony comes from `effectiveBaronies = baroniesQ.data` (CanvasViewer.tsx:365) — a TanStack Query of the BACKEND-rendered barony GeoJSON — NOT from `useEditorStore.vertices`. `BezierEditLayer.handleDragEnd` commits to `store.vertices` via `setVerticesAndLog` (line 538) and refits only its own green overlay (line 561); the `BaronyLayer` the user reads as "the map" is never recomputed from `vertices`, so even a successful drag would show no change on the colored polygon. No Apply→backend-re-render path was observed for barony Bézier edits.
fix_hint: Planner MUST first reproduce with real mouse at usable zoom (after G5) to separate "couldn't grab" from "grabbed but no feedback". Then decide the render-feedback contract: either (a) render the active barony's edited contour live from `store.vertices` as an overlay so the change is immediately visible, and/or (b) wire an Apply that POSTs edited barony vertices to the backend and refetches `baroniesQ` (mirror the VertexEditLayer/landmask Apply flow). Confirm how Phase 08 raw-vertex barony edits surface visually and match that contract.

### G7. Cannot add control points with a real double-click
status: resolved
closure: Plan 08.1-07 (commit a928d0f, adefb6d) — onDblClick production bug fixed (getRelativePointerPosition map-space, was getPointerPosition screen-space), hit-path widened 12→24 on-screen px, midCurvePoint mid-segment target avoids the anchor-Rect z-order trap. Playwright G7 uses REAL page.mouse.dblclick (no __forgeBezierTriggerInsert) — 1/1 green. AWAITING human browser re-verify (real dbl-click between anchors inserts point).
severity: medium
source: 2026-05-29 human browser UAT
expected: A real double-click on the curve inserts a new anchor (G3 feature) in the running app.
actual: "Não consegui adicionar novos pontos de controle." The `onDblClick` handler exists on `bezier-hit-path` (BezierEditLayer.tsx:729-736) and the insert logic is unit/Playwright-green — but the Playwright spec drives `__forgeBezierTriggerInsert(segmentIdx)` (index-based DEV hook, lines 669-681) which bypasses real pointer hit-testing. With the hit-path only ~3px wide on screen (G5), a real dbl-click misses the stroke entirely. False-green: the automated test never exercised the real Konva dbl-click event path.
fix_hint: Fix G5 (screen-space hit-path width, e.g. `24 / currentScale`), then add a Playwright UAT that performs a REAL `page.mouse` double-click on the curve at a usable zoom and asserts anchorCount increments — retire reliance on `__forgeBezierTriggerInsert` for the reachability claim.

### G8. Bézier (vertex `move`) edits never reach the rendered map or export — backend replay gap
status: deferred
deferred_to: phase 08.2 (backend move/add/delete replay + render cascade) — user decision 2026-05-29. Out of 08.1's UI-only charter.
severity: high
source: 2026-05-29 human re-verify (root cause traced)
scope: BACKEND pipeline — beyond 08.1's declared UI-only charter.
expected: After editing a barony contour with the Bézier tool and triggering a render, the colored barony boundary on the map AND the 12-file Unity export reflect the edited geometry.
actual: VERIFIED ROOT CAUSE — Bézier drag commits `op:'move'` and the edit IS persisted to the branch edit-log (POST `/edit-events`, EditorSyncBridge → useAppendEditEvent). BUT the render replay stage `manual_edit.compute()` only replays `split`/`merge`/`translate`: `backend/medieval_forge/services/pipeline/manual_edit.py:128` — "vertex-level ops (move/add/delete) handled in follow-up plans". `op:'move'` is silently dropped. `render.py:148-167` only feeds `landmask_replace` edit-events into render (`cfg.landmask_override`); there is no barony-vertex override path. Net: even after a manual re-render, the colored barony boundary will NEVER change for a Bézier edit. The `move`/`add`/`delete` replay was deferred in Phase 08 and never implemented. Confirmed by human: "movi bezier etc e nada mudou nos limites das baronias."
fix_hint: Implement `move` (and `add`/`delete`) vertex-op replay in `manual_edit.py` so flattened barony vertices from the branch edit-log are applied during render, AND wire a render trigger (or explicit Apply) after Bézier commit so the colored BaronyLayer + export converge (mirror the landmask_replace → cfg.landmask_override → /render cascade for the barony raster). This is backend + frontend work, likely a NEW phase (e.g. 08.2) rather than a UI-layer gap-closure on 08.1.
