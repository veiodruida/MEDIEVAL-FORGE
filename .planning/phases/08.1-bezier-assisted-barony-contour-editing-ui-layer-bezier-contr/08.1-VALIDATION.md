---
phase: 08.1
slug: bezier-assisted-barony-contour-editing-ui-layer-bezier-contr
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-28
---

# Phase 08.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (frontend unit) + Playwright (UAT) |
| **Config file** | `frontend/vitest.config.ts`, `frontend/playwright.config.ts` |
| **Quick run command** | `cd frontend && npx vitest run src/components/canvas src/stores` |
| **Full suite command** | `cd frontend && npx vitest run && npx playwright test` |
| **Estimated runtime** | ~30 seconds (vitest) / ~60s (Playwright UAT) |

---

## Sampling Rate

- **After every task commit:** Run quick run command
- **After every plan wave:** Run full suite command
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 08.1-01-01 | 01 | 0 | BEZ-IDENTITY-01 | — | enter+exit edit mode, zero drags → `useEditorStore.vertices` byte-identical AND `editLog.length` unchanged | unit | `npx vitest run src/stores/bezierIdentity.test.ts` | ❌ W0 | ⬜ pending |
| 08.1-01-02 | 01 | 0 | flatten-resolution calibration | — | fit→flatten a real Iberia barony ring; flattened vertex count + bbox within tolerance of original | unit | `npx vitest run src/components/canvas/bezierFlatten.test.ts` | ❌ W0 | ⬜ pending |
| 08.1-02-01 | 02 | 1 | buildPolyIndexMap | — | each cubic maps to a contiguous original polygon vertex range covering all N points with no gaps/overlap | unit | `npx vitest run src/components/canvas/buildPolyIndexMap.test.ts` | ❌ W0 | ⬜ pending |
| 08.1-02-02 | 02 | 1 | dirty-segment flatten | — | dragging anchor `i` marks segments `i-1` AND `i` dirty; only those segments' vertices change, all others verbatim | unit | `npx vitest run src/components/canvas/dirtyFlatten.test.ts` | ❌ W0 | ⬜ pending |
| 08.1-03-01 | 03 | 2 | render anchors+handles | — | react-konva renders ~10-20 anchors (not hundreds of raw vertices) + control handles for active anchor only | uat | `npx playwright test bezier-editor.spec.ts` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `frontend/src/stores/bezierIdentity.test.ts` — BEZ-IDENTITY-01 empty-log byte-identity guard (write FIRST, before any drag code)
- [ ] `frontend/src/components/canvas/__fixtures__/iberiaBaronyRing.ts` — real σ=3.0 smoothed Voronoi barony ring as explicit numeric fixture for flatten calibration
- [ ] `frontend/package.json` — add `fit-curve@0.2.0` dependency
- [ ] `frontend/vitest.config.ts` — add `deps.inline: ['fit-curve']` only if CJS transform fails in tests

*Calibration: the fit-curve error-tolerance value (30px hypothesis) must be empirically tuned in Wave 0 against the real barony fixture.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Curve handles feel usable (vs hundreds of raw dots) | UX goal driving the phase | Subjective usability — the entire reason Phase 08 raw handles were rejected | In browser: select a barony, enter edit mode, confirm a manageable count of anchors appears; drag an anchor and confirm the contour follows smoothly |
| Re-flattened contour does not visibly coarsen the map | flatten-resolution | Visual raster fidelity at σ=3.0 density | After editing + apply, compare rendered raster to pre-edit; no visible coarsening at edited segments |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
