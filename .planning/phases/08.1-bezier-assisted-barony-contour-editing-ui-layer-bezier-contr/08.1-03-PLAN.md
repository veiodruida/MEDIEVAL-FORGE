---
phase: 08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - frontend/src/components/canvas/BezierEditLayer.tsx
  - frontend/src/components/canvas/__tests__/BezierEditLayer.drag.test.tsx
autonomous: true
requirements: [BEZ-DRAG-01]
must_haves:
  truths:
    - "Dragging anchor i commits via setVerticesAndLog with op:'move' and marks BOTH segment i-1 and i dirty (boundary guards; closing segment non-editable)"
    - "Only dirty segments flatten; non-dirty index ranges in nextVertices are copied verbatim from store.vertices (no float recompute)"
    - "Anchor drags snap via snapToNeighbour (anchors only); control-handle drags never snap"
    - "Shared-vertex coupling (getCoupledVertices) applied to changed vertices before commit — coupled partners in neighbour baronies move too"
    - "BEZ-IDENTITY-01 (Plan 02) stays GREEN — entering+exiting with zero drags still leaves vertices byte-identical"
  artifacts:
    - path: "frontend/src/components/canvas/BezierEditLayer.tsx"
      provides: "anchor drag + control-handle drag → dirty-segment flatten → setVerticesAndLog(op:'move') with snap + shared-vertex coupling"
  key_links:
    - from: "frontend/src/components/canvas/BezierEditLayer.tsx"
      to: "useEditorStore.setVerticesAndLog"
      via: "onDragEnd flatten commit with op:'move'"
      pattern: "setVerticesAndLog\\("
    - from: "frontend/src/components/canvas/BezierEditLayer.tsx"
      to: "frontend/src/lib/bezierFlatten.ts"
      via: "flattenSegment per dirty segment"
      pattern: "flattenSegment\\("
    - from: "frontend/src/components/canvas/BezierEditLayer.tsx"
      to: "frontend/src/lib/snap.ts + frontend/src/lib/sharedVertex.ts"
      via: "snapToNeighbour (anchor drag) + getCoupledVertices (commit)"
      pattern: "snapToNeighbour\\(|getCoupledVertices\\("
---

<objective>
Add drag-commit behavior to `BezierEditLayer.tsx`: dragging an anchor (or a control handle of the active anchor) updates a live preview, marks the two adjacent segments dirty, flattens ONLY the dirty segments back to polygon vertices, copies non-dirty ranges verbatim from the store, applies snap (anchors only) and shared-vertex coupling, and commits via `setVerticesAndLog(nextVertices, { op: 'move' })`. The empty-log identity contract from Plan 02 must remain green.

Purpose: This is the actual editing capability. The dirty-only flatten is the mechanism that enforces identity — unedited segments keep their original vertex values byte-verbatim, so a zero-drag session never touches the store (RESEARCH §Parity-Safe Architecture, §Pitfall 2). Reuse Phase 08's proven snap + coupling functions verbatim — do not reinvent them (CLAUDE.md: snapToNeighbour, getCoupledVertices reused).
Output: drag handlers on BezierEditLayer + BEZ-DRAG-01 test, with the Plan 02 identity test still passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-RESEARCH.md
@.planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-UI-SPEC.md

<interfaces>
<!-- Reused Phase 08 helpers — call verbatim, do not reimplement. Sources below. -->
From frontend/src/lib/snap.ts:
```typescript
export function snapToNeighbour(
  cursorWorld: { lat: number; lon: number },
  candidates: { id: string; lat: number; lon: number }[],
  stageScale: number,
  altHeld: boolean,
): { id: string; lat: number; lon: number } | null;
```

From frontend/src/lib/sharedVertex.ts:
```typescript
export type SharedVertexIndex = Map<string, string[]>;
export function buildSharedVertexIndex(vertices: { vertexId: string; baronyId: string; lat: number; lon: number }[], tolerance?: number): SharedVertexIndex;
export function getCoupledVertices(index: SharedVertexIndex, vertexId: string): string[]; // returns [vertexId] if uncoupled
```

From frontend/src/lib/bezierFlatten.ts (Plan 01):
```typescript
export function flattenSegment(cubic: BezierCubic, targetCount: number, projection: ProjectionConfig): Array<[number, number]>;
```

From frontend/src/stores/useEditorStore.ts:
```typescript
// CHOKEPOINT — the ONLY undoable mutation entry point. op MUST be 'move' (never 'bezier_drag').
setVerticesAndLog(next: Record<string, { lat: number; lon: number }>, op: EditOp): void;
// EditOp.op union does NOT include 'bezier_drag' — do not add it (RESEARCH anti-pattern).
```

<!-- The dirty-segment rule (RESEARCH §During drag, UI-SPEC §Anchor interaction onDragEnd) -->
Dragging anchor i → dirtySegments.add(i-1); dirtySegments.add(i)
  boundary: i=0 → only add segment 0 (segment -1 doesn't exist; closing segment non-editable V1)
  boundary: i=N-1 → only add segment N-2 (segment N-1 is the non-editable closing segment)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Anchor + control-handle drag → dirty-segment flatten → setVerticesAndLog(op:'move') with snap + coupling</name>
  <files>frontend/src/components/canvas/BezierEditLayer.tsx, frontend/src/components/canvas/__tests__/BezierEditLayer.drag.test.tsx</files>
  <read_first>
    - frontend/src/components/canvas/BezierEditLayer.tsx (the render+activate component from Plan 02 — extend it; do NOT rewrite the render path)
    - frontend/src/components/canvas/VertexEditLayer.tsx lines 497-585 (handleDragEnd: the EXACT snap + getCoupledVertices + setVerticesAndLog commit pattern to mirror; previewRef pattern; snapCandidatesRef; sharedIndexRef + rebuildSharedIndex)
    - frontend/src/lib/snap.ts, frontend/src/lib/sharedVertex.ts (signatures — call verbatim)
    - frontend/src/lib/bezierFlatten.ts, frontend/src/lib/bezierFit.ts (Plan 01)
    - .planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-RESEARCH.md §During drag / §On drag commit, §Pitfall 2 (only dirty flatten — verbatim non-dirty copy), §Critical Implicit Constraints 1 (coupling) + 2 (snap anchors only, never handles) + 6 (error calibration)
    - .planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-UI-SPEC.md §Anchor interaction (Drag anchor steps 1-4), §Control-handle interaction
  </read_first>
  <behavior>
    - Test BEZ-DRAG-01: simulate dragging anchor `i` (1 ≤ i ≤ N-2, a non-boundary anchor) to a new px position and firing onDragEnd. Assert setVerticesAndLog was called exactly once with op:'move'. Assert the committed nextVertices differs from the store ONLY at indices within segment i-1's and segment i's ranges; every other vertex key has the byte-identical original value (verbatim copy, no float drift).
    - Test (anchor drag carries handles): dragging anchor `i` by delta (dx,dy) moves the anchor's cp1Px and cp2Px by the same (dx,dy) — the rebuilt cubics for segments i-1 and i use the translated handle positions (local curve shape preserved, no cusp).
    - Test (boundary i=0): dragging anchor 0 marks only segment 0 dirty (no segment -1); commit succeeds.
    - Test (boundary i=N-1): dragging the last anchor marks only segment N-2 dirty (closing segment N-1 stays non-editable).
    - Test (coupling): when a changed vertex id is coupled (shared with a neighbour barony per a seeded sharedIndex), the commit's nextVertices includes the coupled partner moved to the same position.
    - Test (identity still green): re-run the Plan 02 BEZ-IDENTITY-01 — mount/unmount with zero drags still leaves vertices byte-identical (no regression from adding drag handlers).
  </behavior>
  <action>
    Extend `BezierEditLayer.tsx` (do not rewrite Plan 02's render). Add:
    - `previewRef` for in-flight anchor/handle px positions (mirror VertexEditLayer.tsx previewRef pattern). `onDragMove` updates previewRef + repaints the dragged segment as a live `Konva.Shape sceneFunc ctx.bezierCurveTo` (UI-SPEC §Color "Dragged segment live preview", Note #9) — no store write during move.
    - `dirtySegments` as a local `Set<number>` (RESEARCH §During drag). On anchor `i` drag start (or in onDragEnd before flatten): `if (i-1 >= 0) dirty.add(i-1); if (i < anchors.length-1) dirty.add(i);` — the i<length-1 guard keeps the closing segment non-editable (UI-SPEC §Closing segment). Control-handle drag of the active anchor marks the same adjacent segment(s) dirty.
    - `onDragEnd` (anchor): compute final px = drag position; **on anchor drag, translate BOTH control handles (cp1Px, cp2Px) of that anchor by the same drag delta as the anchor, preserving local curve shape (handles move rigidly with the anchor — no cusp). This is the better-UX default.** Convert the anchor px to geo via canvasToGeo; for ANCHOR drags only, run `snapToNeighbour({lat,lon}, candidates, stageScale, altHeld)` (candidates = neighbour barony vertices, same source as VertexEditLayer snapCandidatesRef) and snap if hit (when snapped, re-derive the handle translation from the snapped anchor position so the handles stay rigid relative to the final anchor). Control-handle drags SKIP snap entirely (RESEARCH constraint 2) and move only the dragged handle (the opposite handle is NOT mirrored in V1).
    - Flatten: for each dirty segment `s`, `flattenSegment(cubicForSegmentS_withDraggedEndpoints, anchors[s].polyRangeEnd - anchors[s].polyRangeStart + 1, projection)` → [lon,lat][]. The cubic must reflect the dragged anchor/handle positions (rebuild the segment's p0/c1/c2/p3 from the updated anchor + neighbour anchor, using the translated handle positions from the step above).
    - Build `nextVertices`: start from `{ ...useEditorStore.getState().vertices }`. For each dirty segment, overwrite the keys in its polyRange (keyed by the same `#N` scheme) with the flattened coords. Non-dirty ranges are left as the verbatim store values (NEVER recomputed — RESEARCH Pitfall 2). This verbatim-copy is the identity mechanism.
    - Coupling: collect changed vertex ids; for each, `getCoupledVertices(sharedIndexRef.current, id)`; if coupled, set partners to the same coord in nextVertices (mirror VertexEditLayer.tsx:560-579). Build/refresh sharedIndexRef via buildSharedVertexIndex over the editable vertex set, same as VertexEditLayer's rebuildSharedIndex.
    - Commit: `useEditorStore.getState().setVerticesAndLog(nextVertices, { op: 'move', ts: Date.now(), vertexIds: changedIds })`. op MUST be 'move' (never 'bezier_drag').
    - After commit: clear dirtySegments + previewRef; recompute BezierDisplayState from updated store vertices (UI-SPEC §Anchor interaction step 4). Update `__forgeBezierState` to report live `dirtySegmentCount`.
    Create `frontend/src/components/canvas/__tests__/BezierEditLayer.drag.test.tsx` covering the behaviors above. Use the react-konva Stage harness + IBERIA_BARONY_RING fixture + inline ProjectionConfig. Simulate drag by invoking the anchor's onDragEnd with a synthetic Konva event whose target reports the new x/y (mirror how VertexEditLayer drag tests fire events — check existing Phase 08 drag test for the event-mock shape).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/canvas/__tests__/BezierEditLayer.drag.test.tsx src/components/canvas/__tests__/BezierEditLayer.test.tsx src/stores/__tests__/useEditorStore.bezierIdentity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - BEZ-DRAG-01: anchor drag commits once with op:'move'; both adjacent segments flatten; non-dirty ranges verbatim.
    - Anchor drag translates both control handles by the same delta (local curve shape preserved); boundary tests (i=0, i=N-1) and coupling test pass.
    - BEZ-IDENTITY-01 (Plan 02) still passes — zero-drag mount/unmount unchanged.
    - Grep gate: `grep -n "bezier_drag" frontend/src/components/canvas/BezierEditLayer.tsx` returns nothing; the only setVerticesAndLog call uses op:'move'.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anchor drag (canvas px) → store.vertices | User drag positions cross from canvas px into geo coords written to the store; float overflow or out-of-range coords could corrupt geometry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-03-01 | Tampering | flatten commit → setVerticesAndLog | mitigate | lat/lon are clamped to valid range by the existing `canvasToGeo` projection math (RESEARCH §Security: "already done by projection"); only DIRTY segments are written, non-dirty ranges copied verbatim — bounding the blast radius of any drag. |
| T-08.1-03-02 | Tampering | non-dirty range recompute | mitigate | Non-dirty ranges MUST be copied verbatim from `store.vertices` (no flatten recompute) — prevents float-drift corruption of un-edited geometry and preserves the identity contract. Enforced by the BEZ-DRAG-01 "verbatim copy" assertion. |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/canvas/__tests__/BezierEditLayer.drag.test.tsx src/components/canvas/__tests__/BezierEditLayer.test.tsx src/stores/__tests__/useEditorStore.bezierIdentity.test.ts` exits 0.
- `cd frontend && npx vitest run` (full suite) green.
- Grep gate: `grep -n "snapToNeighbour\|getCoupledVertices" frontend/src/components/canvas/BezierEditLayer.tsx` shows both reused (not reimplemented).
- Grep gate: the sole `setVerticesAndLog` call in BezierEditLayer uses `op: 'move'`; no `'bezier_drag'` token anywhere.
- Identity regression: BEZ-IDENTITY-01 remains green after drag code added.
</verification>

<success_criteria>
- BEZ-DRAG-01 passing: anchor drag → both adjacent segments dirty → flatten → op:'move' commit, with snap (anchors) + coupling.
- Anchor drag carries both control handles rigidly (preserves local curve shape).
- Non-dirty ranges verbatim — identity contract preserved (Plan 02 test still green).
- Phase 08 snap + shared-vertex helpers reused verbatim.
- Full vitest suite green; zero backend changes.
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-03-SUMMARY.md`. Confirm BEZ-IDENTITY-01 stayed green after drag code landed and note any deviation in the dirty-segment boundary handling.
</output>
</output>
