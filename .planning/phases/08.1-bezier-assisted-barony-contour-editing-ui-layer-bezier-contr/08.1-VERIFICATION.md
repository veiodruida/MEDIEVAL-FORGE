---
phase: 08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr
verified: 2026-05-29T15:45:00Z
status: human_needed
score: 17/17 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 14/14
  gaps_closed:
    - "G5 — anchors/handles/hit-path too small to grab at fit-to-view: screen-space sizing (BASE/currentScale) implemented; MAX_SCALE_MULTIPLIER raised 4→16; three half-size desync sites (Rect offset, draggedCenterPx, DEV drag hook) all use live anchorHalfRef.current"
    - "G7 — cannot add control points with real double-click: onDblClick now uses getRelativePointerPosition() (map-space fix); hit-path widened from 12→24 on-screen px; dblclick targets mid-segment on-curve point (z-order trap documented); real Playwright dblclick inserts anchor"
    - "G6 — edits not visibly reflected on map: live amber-dashed edited-contour overlay reads store.vertices via useMemo, updates immediately on commit via editedRingPtsRef; Playwright geometry-change assertion via editedRingSnapshot diff (not just count) green"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "G5: Anchor usability — grab, drag, handle feedback in real browser at zoomed-in scale"
    expected: "After zooming into a single barony (wheel up to 6-16x), anchor squares are visibly sized (~10px), can be grabbed and dragged with a real mouse; the Bézier curve follows the drag live; on release the contour updates"
    why_human: "Real-browser tactile usability is subjective. Playwright proved real page.mouse drag commits at 6x zoom under Chromium headless, but the G5 complaint (anchors too small to see/grab) requires hands-on confirmation that the on-screen size is comfortable to a real designer"
  - test: "G6: Edited contour overlay is visible and informative — does the amber overlay satisfy 'edits visibly change the map'?"
    expected: "After dragging an anchor and releasing, a dashed amber outline appears tracking the new contour shape, distinct from the unchanged colored BaronyLayer. The user reads this as confirmation that the shape changed. NOTE: the colored barony polygon (BaronyLayer) does NOT update — that requires /render (out of scope). Judge whether the overlay is sufficient or whether this re-opens as a future phase"
    why_human: "The phase goal says edits should 'visibly change the map'. The fix chose option (a): an overlay, not a backend re-render. Only a human can judge whether the amber overlay satisfies the original complaint or whether the colored raster still being stale makes G6 feel unsolved. This decision cannot be automated"
  - test: "G7: Real double-click between anchors inserts a new control point"
    expected: "Double-clicking on the green curve outline BETWEEN two anchor squares inserts a new anchor (visible square appears at the click point). NOTE: do NOT double-click on or near an anchor square — z-order means the Rect intercepts the click and the hit-path never fires. Target the space between anchors"
    why_human: "Playwright proved real dblclick on midCurvePoint inserts under Chromium headless (anchorCount incremented, editLog unchanged). Real-browser confirmation needed: the designer should try at a comfortable zoom level on the live app"
  - test: "G1+G4: Closed ring + distinct palette visible in real browser"
    expected: "Bézier outline forms a visibly closed loop; curve (green), inactive anchors (blue), active anchor (amber), handles (magenta), tethers (slate-gray) are all distinguishable; closed ring visible with no open gap"
    why_human: "Visual rendering quality and color readability require browser confirmation. Code proves N C commands and pairwise hex inequality; actual screen rendering quality cannot be asserted programmatically"
  - test: "Playwright UAT: all real-event specs pass in the running app (08.1-bezier-usable-targets.spec.ts 3/3 and 08.1-bezier-render-feedback.spec.ts 2/2)"
    expected: "With backend and Vite running (globalSetup seeds Iberia 868): cd frontend && npx playwright test tests/uat/playwright/08.1-bezier-usable-targets.spec.ts tests/uat/playwright/08.1-bezier-render-feedback.spec.ts — all 5 tests green"
    why_human: "The orchestrator reports these green against freshly-built code. Code-level spot verification confirms real events, no __forgeBezierTrigger* for claims, and correct wiring. However the verification session did not independently re-run Playwright — this confirms the orchestrator's report in the running app"
---

# Phase 08.1: Bézier-Assisted Barony Contour Editing — Verification Report (Re-verification: Plans 07+08)

**Phase Goal:** Bezier-assisted barony contour editing — a UI-layer Bézier control-point editor over the existing polygon model, parity-safe (store stays polygon; curve-fit derived for display; only edited segments flatten back). The editor must be USABLE in a real browser (anchors grabbable, edits visibly change the map, points addable via real double-click) — gap-closure plans 07+08 specifically close G5, G6, and G7 found in HUMAN browser UAT.
**Verified:** 2026-05-29T15:45:00Z
**Status:** human_needed
**Re-verification:** Yes — gap closure after plans 07+08 (G5: screen-space sizing + zoom cap, G6: live overlay, G7: real dblclick wiring)

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Anchors, handles, and hit-path render at a constant on-screen size regardless of stage zoom (size = BASE / currentScale) — G5 code fix | VERIFIED | `safeScale`, `anchorSize = ANCHOR_BASE_PX / safeScale`, `handleRadius = (HANDLE_BASE_PX/2)/safeScale`, `hitPathWidth = HITPATH_BASE_PX/safeScale` at BezierEditLayer.tsx lines 235-241. Old map-space literals `width={10}`, `radius={4}`, `strokeWidth={12}` are absent (grep returns zero hits). Render lines 947-948, 875, 901, 989, 1000 use the derived variables |
| 2 | MAX_SCALE_MULTIPLIER raised from 4 to 16 so the designer can zoom into a single barony — G5 | VERIFIED | useZoomPan.ts line 5: `export const MAX_SCALE_MULTIPLIER = 16`. Old value 4 absent. Comment documents reason |
| 3 | The three half-size desync sites (Rect offset, draggedCenterPx, DEV drag hook) all use the SAME live anchorHalfRef.current — G5 correctness trap | VERIFIED | anchorHalfRef.current = anchorHalf synced every render at line 281. draggedCenterPx signature accepts anchorHalf param (line 188); both call sites use anchorHalfRef.current (lines 453, 473). DEV drag hook uses anchorHalfRef.current (lines 717-718). Old literal `- 5 + dx` absent (grep zero hits) |
| 4 | currentScale prop is threaded live from CanvasViewer into BezierEditLayer so sizing updates on every wheel tick | VERIFIED | CanvasViewer.tsx line 772: `<BezierEditLayer projection={projection} currentScale={currentScale} />`. Same currentScale state already fed to DecorationsLayer (line 754). BezierEditLayerProps declares `currentScale: number` at line 80 |
| 5 | The onDblClick handler uses getRelativePointerPosition() (map-space) not getPointerPosition() (screen-space) — G7 coordinate-space fix | VERIFIED | BezierEditLayer.tsx line 916: `const pos = stage.getRelativePointerPosition()`. Comment documents the screen-space vs map-space distinction and the production bug fixed in Plan 07 |
| 6 | The dedicated hit-path is 24px wide (on-screen) so a real dblclick at usable zoom reaches it — G7 hit-area widening | VERIFIED | HITPATH_BASE_PX = 24 (line 232). hitPathWidth = HITPATH_BASE_PX / safeScale. strokeWidth and hitStrokeWidth both set to hitPathWidth at lines 901-902 |
| 7 | A real Playwright page.mouse drag on the anchor at 6x zoom commits op:'move' (editLogLength grows) — G5 real-event Playwright | VERIFIED | 08.1-bezier-usable-targets.spec.ts: Test 1 uses page.mouse.move/down/move/up (lines 224-227); polls editLogLength (line 231). No __forgeBezierTrigger* in the file for the claim. Anti-pattern guard header present. Orchestrator reports 3/3 green |
| 8 | A real Playwright page.mouse.dblclick on midCurvePoint (mid-segment, NOT on an anchor) inserts an anchor (anchorCount +1, editLog unchanged) — G7 real-event Playwright | VERIFIED | 08.1-bezier-usable-targets.spec.ts: Test 2 reads midCurvePoint, targets it with page.mouse.dblclick(mid.x, mid.y) (line 316); z-order trap documented in comments; asserts anchorCount+1 and editLog unchanged. midCurvePoint used as dblclick coordinate, not firstAnchor. Orchestrator reports 3/3 green |
| 9 | G6 reproduction: real drag commits (editLog grows) AND the backend-served colored barony ring is byte-identical — G6 confirmed independent of G5 | VERIFIED | 08.1-bezier-render-feedback.spec.ts Describe 1: real drag (page.mouse.down/move/up lines 241-245), polls editLogLength growth, refetches baronies.geojson, asserts byte-identical. No __forgeBezierTrigger*. Orchestrator reports 2/2 green. Note: snapshot predicate matches first non-null feature, not the specific active barony (REVIEW.md WR-01 — misleading scope but conclusion sound since op:move never POSTs to any feature) |
| 10 | Live amber-dashed edited-contour overlay reads store.vertices via useMemo, updates immediately after commit — G6 fix option (a) | VERIFIED | BezierEditLayer.tsx: editedRingPts useMemo at lines 826-838 derives flat [x,y,...] from subscribed vertices via geoToCanvas. editedRingPtsRef.current synced every render at line 841 (stale-closure fix). bezier-edited-contour Line rendered at lines 870-880. Zero new setVerticesAndLog call sites (count = 1 real at line 567) |
| 11 | Playwright proves overlay geometry CHANGES after a real drag (editedRingSnapshot diff) — G6 fix verification | VERIFIED | 08.1-bezier-render-feedback.spec.ts Describe 2: captures editedRingSnapshot before, drives real drag, asserts snapshotAfter !== snapshotBefore (line 335). Count invariance documented (reshape not resize). Orchestrator reports 2/2 green |
| 12 | BEZ-IDENTITY-01 holds through G5/G7/G6 changes: enter+exit with zero drags leaves store byte-identical | VERIFIED | useEditorStore.bezierIdentity.test.ts 2/2 passed (independently re-run this session: 24ms). editedRingPts is read-only useMemo (zero store write). G5 sizing/zoom changes touch only Konva prop values. G6 overlay adds no setVerticesAndLog |
| 13 | The Bézier outline forms a CLOSED loop (G1) | VERIFIED | Carried from previous verification (Plans 05+06). buildPathData emits N cubic C commands. All tests green in BezierEditLayer.test.tsx |
| 14 | Every ring segment including the closing segment is editable (G2) | VERIFIED | Carried. addSeg guard s<N; incomingSeg=(idx-1+N)%N. Tests green |
| 15 | Double-click inserts anchor at nearest curve param, identity-safe NO-OP (G3) | VERIFIED | Carried. handleInsertAtPx uses getRelativePointerPosition() (Plan 07 map-space fix). addpoint.test.tsx 8/8 green |
| 16 | Six visually distinct color constants (G4) | VERIFIED | Carried. Six pairwise-distinct hex literals at lines 54-59 |
| 17 | All four DEV hooks are DEV-gated and torn down on unmount | VERIFIED | Carried. Single import.meta.env.DEV-gated useEffect; cleanup deletes __forgeBezierState, __forgeBezierTriggerDrag, __forgeBezierTriggerHandleDrag, __forgeBezierTriggerInsert, __forgePanToFirstAnchor |

**Score:** 17/17 truths verified (code-level). 5 items require human verification for real-browser usability judgment (G5 feel, G6 overlay adequacy, G7 real-browser dblclick, G1+G4 visual, Playwright re-run).

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/src/hooks/useZoomPan.ts` | MAX_SCALE_MULTIPLIER raised 4→16 | VERIFIED | Line 5: `export const MAX_SCALE_MULTIPLIER = 16` with explanatory comment |
| `frontend/src/components/canvas/BezierEditLayer.tsx` | currentScale prop + screen-space sizing (anchor, handle, hit-path, tether, curve) + three half-size sites using anchorHalfRef + midCurvePoint read-only field + live edited-contour overlay + editedRingPtsRef stale-closure fix | VERIFIED | All present. Props line 80. Size vars lines 235-241. anchorHalfRef lines 280-281 synced every render. anchorHalfRef.current at lines 453, 473, 717-718. onDblClick getRelativePointerPosition() line 916. editedRingPts useMemo lines 826-838. editedRingPtsRef.current synced line 841. bezier-edited-contour Line lines 870-880. Additive DEV-hook fields editedContourPointCount+editedRingSnapshot lines 695-698 |
| `frontend/src/components/canvas/CanvasViewer.tsx` | currentScale={currentScale} threaded into BezierEditLayer | VERIFIED | Line 772. Same value fed to DecorationsLayer at line 754 |
| `frontend/tests/uat/playwright/08.1-bezier-usable-targets.spec.ts` | REAL page.mouse drag + REAL dblclick on mid-segment at usable zoom; no __forgeBezierTrigger* for claims | VERIFIED | File exists. Anti-pattern guard header present. page.mouse.down/up/wheel/dblclick all present. midCurvePoint used as dblclick target (line 316). __forgeBezierTrigger* appear only in comment text, never as calls |
| `frontend/tests/uat/playwright/08.1-bezier-render-feedback.spec.ts` | G6 reproduction discriminator + live-overlay-reflects-edit assertion via real drag | VERIFIED | File exists. Describe 1: reproduction (editLog grows + backend ring byte-identical). Describe 2: overlay geometry changes (editedRingSnapshot diff). No __forgeBezierTrigger* calls |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| BezierEditLayer anchor Rect / Circle / hit-path | currentScale prop | size = BASE / currentScale, anchorHalfRef.current for offsets/draggedCenterPx/DEV hook | WIRED | Lines 235-241 size derivation; lines 280-281 ref sync; lines 453, 473, 717-718 ref usage; lines 947-948, 989, 1000, 901-902 render |
| CanvasViewer BezierEditLayer mount | currentScale state | currentScale={currentScale} prop | WIRED | Line 772 |
| onDblClick handler | handleInsertAtPx (map-space coords) | stage.getRelativePointerPosition() inverts stage transform | WIRED | Line 916; comment documents map-space vs screen-space distinction |
| editedRingPts useMemo | store.vertices (subscribed) | geoToCanvas per vertex key filtered to activeTerritoryId | WIRED | Lines 826-838; editedRingPtsRef synced line 841; bezier-edited-contour Line reads editedRingPts line 872 |
| 08.1-bezier-usable-targets.spec.ts drag | editLogLength growth | real page.mouse.down/move/up on firstAnchor → Konva Rect hit-test → handleDragEnd → setVerticesAndLog | WIRED | Lines 224-232; no DEV trigger for claim |
| 08.1-bezier-usable-targets.spec.ts dblclick | anchorCount increment | real page.mouse.dblclick on midCurvePoint → Konva hit-path onDblClick → handleInsertAtPx | WIRED | Lines 311-321; midCurvePoint not firstAnchor (z-order trap avoided) |
| 08.1-bezier-render-feedback.spec.ts drag | editedRingSnapshot change | real drag → setVerticesAndLog → store.vertices update → useMemo recomputes editedRingPts → editedRingPtsRef.current updated | WIRED | Lines 311-335; snapshot-diff assertion |

---

### Data-Flow Trace (Level 4)

This phase is a UI-only layer. All data flows are component-local or store-local.

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| BezierEditLayer anchors | anchors (component state) | store.vertices → fitPolygonToBezier → buildPolyIndexMap → deriveAnchors | Yes — live geometry | FLOWING |
| BezierEditLayer edited-contour overlay | editedRingPts | store.vertices (subscribed) → sortRingKeys + geoToCanvas | Yes — re-derived on every vertices change via useMemo | FLOWING |
| Drag commit | nextVertices | anchor state → flattenSegment → setVerticesAndLog(op:'move') | Yes — single real call site at line 567 | FLOWING |
| Insert (dblclick) | anchor state only | handleInsertAtPx → splitPolyRange → setAnchors (component-local) | Yes — zero store write (identity-safe NO-OP) | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command/Check | Result | Status |
|----------|--------------|--------|--------|
| BEZ-IDENTITY-01 (enter+exit zero drags) | `npx vitest run src/stores/__tests__/useEditorStore.bezierIdentity.test.ts` | 2/2 passed (run this session, 24ms) | PASS |
| Screen-space sizing: old map-space literals absent | grep `width={10}\|radius={4}\|strokeWidth={12}` in BezierEditLayer.tsx | Zero hits | PASS |
| Three half-size sites converted: old literal-5 absent | grep `- 5 + dx\|x + 5\|anchorPx\[0\] - 5` in BezierEditLayer.tsx | Zero hits | PASS |
| MAX_SCALE_MULTIPLIER = 16 | grep in useZoomPan.ts | One hit at line 5 | PASS |
| currentScale threaded | grep `currentScale={currentScale}` in CanvasViewer.tsx | Hit at line 772 (BezierEditLayer) + line 754 (DecorationsLayer) | PASS |
| bezier-edited-contour overlay present | grep in BezierEditLayer.tsx | One hit at line 879 | PASS |
| setVerticesAndLog call count unchanged at 1 (real) + 2 (comments) | grep + count in BezierEditLayer.tsx | 3 total; 1 at line 567 (real), lines 704-705 (comments) | PASS |
| onDblClick uses getRelativePointerPosition | grep in BezierEditLayer.tsx | Line 916 | PASS |
| No __forgeBezierTrigger* in real-event specs | grep in both new spec files | Zero call-site hits (comments only in usable-targets) | PASS |
| G5+G7 Playwright (real events) | Orchestrator-reported result | 3/3 green (08.1-bezier-usable-targets.spec.ts) | PASS (not re-run) |
| G6 Playwright (real events) | Orchestrator-reported result | 2/2 green (08.1-bezier-render-feedback.spec.ts) | PASS (not re-run) |

---

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| BEZ-IDENTITY-01 | 02, 05, 06, 07, 08 | Enter+exit+insert with zero drags → store byte-identical | SATISFIED | bezierIdentity.test.ts 2/2 (re-run this session); overlay is read-only useMemo; G5/G7 changes are display-only |
| BEZ-FIT-01 | 01 | Calibrated fit error (3..40 cubics, BEZ_FIT_ERROR=30 stable) | SATISFIED (carried) | bezierFit.test.ts; fit-count band tests green; BEZ_FIT_ERROR=30 locked |
| BEZ-INDEX-01 | 01, 06 | buildPolyIndexMap ranges contiguous, full coverage, closing-duplicate covered; splitPolyRange disjoint contiguous | SATISFIED (carried) | bezierFit.test.ts: 5 BEZ-INDEX-01 tests + splitPolyRange contiguity tests |
| BEZ-FLATTEN-01 | 01 | flattenSegment even-spacing for targetCount >= 2 | SATISFIED (carried) | bezierFlatten.test.ts: existing even-spacing tests green |
| BEZ-FLATTEN-02 | 01 | flattenSegment emits both endpoints for targetCount < 2 (WR-03) | SATISFIED (carried) | bezierFlatten.test.ts: degenerate-guard test |
| BEZ-RENDER-01 | 02, 05, 07 | Anchor count in 4..40 range; visible curve outline renders; screen-space sizing | SATISFIED | BezierEditLayer.test.tsx BEZ-RENDER-01 green; screen-space sizing verified (grep gates) |
| BEZ-RENDER-02 | 02, 07 | Nothing renders when activeTerritoryId is null | SATISFIED | BezierEditLayer.test.tsx BEZ-RENDER-02 green |
| BEZ-DRAG-01 | 03, 05, 07, 08 | Drag commits op:'move'; dirty-only flatten; verbatim copy for non-dirty; commit path survives screen-space sizing | SATISFIED | BezierEditLayer.drag.test.tsx 9 tests green; real Playwright drag at 6x zoom commits; setVerticesAndLog count unchanged after G6 overlay |
| BEZ-UAT-01 | 04, 07, 08 | Playwright: select barony → anchors → real drag commits; real dblclick inserts | NEEDS HUMAN (real-browser confirm) | Wiring verified at code level; real-event Playwright reported green by orchestrator; human confirmation needed for real-browser usability (see human section) |
| BEZ-UAT-02 | 04 | Playwright: zero-drag round trip → lookup PNG byte-identical | SATISFIED (carried) | SUMMARY-04 reports green; BEZ-IDENTITY-01 confirmed to hold through G5/G6/G7 changes |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| 08.1-bezier-render-feedback.spec.ts | 223-272 | G6 reproduction asserts first non-null feature, not the selected barony; `baronyId` is dead (REVIEW.md WR-01) | Warning | Misleading test scope but conclusion is sound (op:'move' never POSTs to any backend feature). Not a false-green |
| BezierEditLayer.tsx | 826-838 | editedRingPts overlay useMemo has no all-vertices fallback (REVIEW.md IN-01) | Info | Latent: if key scheme ever mismatches activeTerritoryId, overlay empty but anchors still render |
| BezierEditLayer.tsx | 875 | Overlay stroke uses inline `2.5` instead of named OVERLAY_BASE_PX constant (REVIEW.md IN-02) | Info | Cosmetic inconsistency; all other screen-space sizes use *_BASE_PX |
| BezierEditLayer.tsx | 29 | Header comment says "5 visually distinct colors" but lists 6 (cosmetic, IN-03 from prior review) | Info | No behavioral impact |

No blockers found. One warning (misleading test scope, non-false-green) and three info items.

---

### Human Verification Required

#### 1. G5: Anchor usability — grab and drag feel in a real browser at zoomed-in scale

**Test:** Open the running app (backend + Vite both running), open an Iberia 868 project, click the V edit tool, select a barony. Zoom in with the mouse wheel to 6-16x until the barony fills much of the viewport. Observe the anchor squares and control-handle circles. Try clicking and dragging an anchor square.
**Expected:** Anchor squares are visibly sized (~10px on screen) at any zoom level. The cursor can grab an anchor and drag it; the green Bézier curve follows the drag live; on release the edit commits (the amber overlay also shifts).
**Why human:** Real-browser tactile usability is subjective. Playwright proved real page.mouse drag commits at 6x zoom under headless Chromium. The original G5 complaint was "too small to see/grab" — only a designer using a real mouse can confirm the sizing feels comfortable.

#### 2. G6: Live overlay adequacy — does the amber dashed outline satisfy "edits visibly change the map"?

**Test:** After confirming a drag commits (G5), observe what changes on screen. The colored BaronyLayer polygon will NOT change (it reads backend GeoJSON; update requires /render which is out of scope). Instead a dashed amber outlined region should appear tracking the new contour shape.
**Expected:** A dashed amber outline (the live edited-contour overlay) updates immediately on the edited barony, showing the new shape. The colored raster remains as-is (this is by design — the phase boundary forbids backend seeing Bézier).
**Why human:** The phase goal says "edits visibly change the map." The fix chose option (a): a UI-layer overlay, not a backend re-render. A human must judge whether the amber overlay satisfies the original complaint or whether the stale colored raster still makes G6 feel unresolved (which would open a new phase, not a gap here).

#### 3. G7: Real double-click between anchors inserts a new control point

**Test:** Select a barony, zoom in to ~6-8x. With no anchor selected (so no handles are visible), find a stretch of the green curve outline BETWEEN two anchor squares. Double-click on the curve at that point (not on or near an existing anchor).
**Expected:** A new anchor square appears at the double-click location, splitting the segment. The total anchor count increases by 1.
**Why human:** Playwright proved real dblclick on midCurvePoint (t=0.5 between anchor[0] and anchor[1]) inserts under headless Chromium. Real-browser confirmation needed. IMPORTANT: do NOT double-click on or near an anchor square — the z-order means the Rect intercepts the click and the hit-path onDblClick never fires. The target must be the curve between anchors.

#### 4. G1+G4: Closed ring + distinct palette visible in real browser

**Test:** Select a barony and observe the Bézier overlay at comfortable zoom.
**Expected:** The green curve outline forms a completely closed loop (no open gap at any segment). At least 5 visually distinct colors are present: green (curve outline), blue (inactive anchors), amber (active anchor, visible when one is clicked), magenta (control handles, visible when an anchor is active), slate-gray (tether lines).
**Why human:** Code proves N C commands and pairwise hex inequality. Screen rendering quality, actual color appearance under the browser's color profile, and ring closure readability require visual inspection.

#### 5. Playwright UAT re-run confirmation (independent of orchestrator report)

**Test:** With backend and Vite running (globalSetup seeds Iberia 868 project):
```
cd frontend && npx playwright test tests/uat/playwright/08.1-bezier-usable-targets.spec.ts tests/uat/playwright/08.1-bezier-render-feedback.spec.ts
```
**Expected:** 5/5 tests green: G5 real drag commits, G7 real dblclick inserts, BEZ-IDENTITY-01 re-assert, G6 reproduction discriminator, G6 fix overlay geometry change.
**Why human:** The orchestrator reported these green against freshly-built code. Code-level spot verification confirmed real events, no __forgeBezierTrigger* for claims, and correct event/store wiring. The verification session did not independently re-execute Playwright. This step confirms the orchestrator's report in the target environment.

---

### Gaps Summary

No gaps. All three human-origin gaps from the previous verification have been closed in the codebase:

- **G5** (targets too small): screen-space sizing (BASE/currentScale) and MAX_SCALE_MULTIPLIER=16 implemented and verified by code inspection and real-event Playwright
- **G6** (edits not visible): live amber-dashed overlay reads store.vertices read-only; geometry-change Playwright assertion (snapshot diff) green
- **G7** (dblclick misses): onDblClick uses getRelativePointerPosition() (map-space fix); hit-path 24px wide; dblclick targets mid-segment on-curve point (z-order trap documented); real-event Playwright green

Status is `human_needed` because:
1. Real-browser tactile usability (G5) cannot be asserted programmatically
2. The G6 overlay-vs-colored-raster adequacy question requires a human judgment call (see Human Verification item 2)
3. G7 dblclick between-anchors location requires hands-on confirmation
4. G1+G4 visual ring closure and palette readability require browser observation
5. Playwright re-run not independently executed in this verification session

These are usability/visual/integration confirmation items — not code gaps. The codebase correctly implements all phase deliverables.

---

_Verified: 2026-05-29T15:45:00Z_
_Verifier: Claude (gsd-verifier)_
_Re-verification: Plans 07 (G5+G7) and 08 (G6) gap closure_
