---
phase: 08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr
verified: 2026-05-29T11:30:00Z
status: human_needed
score: 14/14 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 10/10
  gaps_closed:
    - "G1 — Bézier outline forms a closed loop (buildPathData emits N cubic C commands including wrap-around closing segment)"
    - "G2 — closing-segment handle drags commit via op:'move'; WR-02 activeAnchorIdx clamp; WR-03 flattenSegment emits both endpoints"
    - "G3 — double-click on dedicated hit-Path inserts an anchor (identity-safe NO-OP; first commit on subsequent drag)"
    - "G4 — five distinct hex constants; CURVE_STROKE (#22c55e) no longer collides with ANCHOR_INACTIVE_FILL (#4a9eff)"
    - "BEZ-IDENTITY-01 extended to cover insert-without-drag byte-identity case"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Re-flattened contour does not visibly coarsen/thicken the map after edit+apply"
    expected: "After editing a segment and committing, the rendered raster shows no visible coarsening at edited segments vs pre-edit state"
    why_human: "Visual raster fidelity — SSIM/eye-judgement, not a unit assertion. Previously blocked by G2 (silent drop); G2 is now closed so this is now runnable."
  - test: "Curve handles feel usable in the running app (subjective usability)"
    expected: "Selecting a barony in V-tool on the baronies layer shows ~4-20 manageable Bézier anchors; dragging makes the contour follow smoothly with closed ring visible; double-click inserts a new anchor"
    why_human: "Subjective usability + visual ring closure + G4 palette readability in the actual browser. The closed-ring shape (G1) and distinct palette (G4) can only be confirmed visually."
  - test: "Playwright UAT: G1+G2 closing-segment handle drag commits in the running app"
    expected: "Running 'npx playwright test 08.1-bezier-edit.spec.ts' (3 tests) exits 0: BEZ-UAT-01 (barony select + drag commits), G1+G2 re-verify (cp1 on anchor 0 grows editLog), G3 re-verify (bare insert keeps editLog, follow-up drag grows it)"
    why_human: "The Playwright spec and hook wiring are verified by grep (unit tests prove the handler path against mocked Konva). The three scenarios exercising real Konva + real pointer-to-stage coords in a browser are reported green in SUMMARY-06 but not independently confirmed this run. Confirm with: cd frontend && npx playwright test 08.1-bezier-edit.spec.ts"
---

# Phase 08.1: Bézier-Assisted Barony Contour Editing — Verification Report (Re-verification: Plans 05+06)

**Phase Goal:** Close the 4 human-UAT gaps (G1 closed Bézier ring, G2 closing-segment edits persist, G3 add control points via double-click insert, G4 distinct color palette) in the Bézier-assisted barony contour editor, while preserving the BEZ-IDENTITY-01 contract (pure entry/exit/insert must not mutate the store editLog).
**Verified:** 2026-05-29T11:30:00Z
**Status:** human_needed
**Re-verification:** Yes — gap closure after previous human_needed result (Plans 05 and 06)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The Bézier outline forms a CLOSED loop: the segment from the last anchor back to anchor 0 is drawn (G1) | ✓ VERIFIED | `buildPathData` (lines 101-116) emits exactly N cubic `C` commands (loop i=0..N-2 + explicit closing segment after loop). Tests: `BezierEditLayer.test.tsx` asserts C-command count === anchors.length for N=3 and N=4; last C targets anchor[0] anchorPx. |
| 2 | Every ring segment including the closing segment is editable: dragging anchor 0's cp1 or the last anchor's cp2 commits a flatten (G2) | ✓ VERIFIED | `addSeg` guard (line 474): `s < N` (not `s < N-1`). Dirty routing: `incomingSeg = (idx-1+N)%N` (line 479) so cp1-on-anchor-0 → incomingSeg=N-1. Drag tests: "cp1 drag on anchor 0 produces a non-empty changedIds" ✓; "cp2 drag on last anchor produces non-empty changedIds" ✓. Comment "non-editable closing segment" removed (grep confirms 0 hits). |
| 3 | Dragging any anchor or handle and releasing persists to the store via setVerticesAndLog(op:'move') — no silent drop (G2) | ✓ VERIFIED | Single real call site at BezierEditLayer.tsx:538 (`useEditorStore.getState().setVerticesAndLog(nextVertices, { op: 'move', ... })`). 3 other occurrences are comments. `op: 'bezier_drag'` token absent in non-comment code. |
| 4 | After a commit refit, activeAnchorIdx is clamped so the active anchor never silently disappears (WR-02) | ✓ VERIFIED | Line 565: `setActiveAnchorIdx((cur) => (cur === null ? null : Math.min(cur, refit.length - 1)))`. Grep confirms hit. WR-02 drag test asserts clamping behavior. |
| 5 | flattenSegment emits both endpoints even when targetCount < 2 (WR-03) | ✓ VERIFIED | `bezierFlatten.ts` lines 49-52: guard `if (targetCount < 2) return [canvasToGeo(p0), canvasToGeo(p3)]`. Flatten test asserts `flattenSegment(cubic, 1, proj).length >= 2` with endpoints p0/p3. |
| 6 | Curve outline, inactive anchor, active anchor, handle, and tether each use a visually distinct hex (G4) | ✓ VERIFIED | Six exported constants at lines 54-59 are pairwise distinct: CURVE_STROKE=#22c55e (green), ANCHOR_INACTIVE_FILL=#4a9eff (blue), ANCHOR_ACTIVE_FILL=#f0c040 (amber), ANCHOR_HOVER_FILL=#ffffff (white), HANDLE_FILL=#e879f9 (magenta), TETHER_STROKE=#94a3b8 (slate-gray). Prior collision CURVE_STROKE=ANCHOR_INACTIVE_FILL resolved. |
| 7 | BEZ-IDENTITY-01 stays green: enter+exit closed-ring edit mode with zero drags → vertices byte-identical, editLog unchanged | ✓ VERIFIED | `npx vitest run src/stores/__tests__/useEditorStore.bezierIdentity.test.ts` — 2/2 passed. Mount+unmount real BezierEditLayer against real store; no store write on entry fit. |
| 8 | BEZ-IDENTITY-01 extended: insert-without-drag leaves vertices byte-identical AND editLog unchanged (G3 identity contract) | ✓ VERIFIED | `BezierEditLayer.addpoint.test.tsx`: "insert-without-drag: store.vertices byte-identical" ✓; "editLog length unchanged" ✓; "BEZ-IDENTITY-01 extended" ✓. `handleInsertAtPx` calls only `setAnchors`/`setActiveAnchorIdx` — zero `setVerticesAndLog` calls. |
| 9 | Double-clicking the curve outline inserts a new anchor at the nearest curve parameter, splitting the segment's poly range into two disjoint contiguous ranges (G3) | ✓ VERIFIED | `handleInsertAtPx` (lines 357-418): calls `findNearestSegmentAndVertex`, snaps to `pxPts[splitVertexIndex]`, calls `splitPolyRange`, inserts anchor. Dedicated hit-Path (`bezier-hit-path`, listening=true, strokeWidth=12, onDblClick) is separate from visible outline (listening=false). Addpoint test: "after insert, anchorCount increased by 1" ✓; "bezier-hit-path is DISTINCT element from bezier-curve-outline" ✓. |
| 10 | An inserted-but-undragged anchor is a pure component-local NO-OP (identity-safe insert) | ✓ VERIFIED | See truth 8. No `setVerticesAndLog` in `handleInsertAtPx`. First commit on drag via existing op:'move' path (addpoint test: "drag after insert: editLog grows" ✓; `lastEntry.op === 'move'` ✓). |
| 11 | After insert, two sub-segments map to disjoint contiguous original-vertex index ranges (no gap, no overlap) | ✓ VERIFIED | `splitPolyRange` (bezierFit.ts:126-139): shared-endpoint convention, `left.rangeEnd === right.rangeStart`. 6 tests in bezierFit.test.ts: exact numeric fixture `{4,9}→[{4,6},{6,9}]`, clamp guards, coverage preservation. All green. |
| 12 | The visible curve outline Path stays listening={false}; double-click is handled by a SEPARATE dedicated transparent hit-Path | ✓ VERIFIED | BezierEditLayer.tsx line 715: `listening={false}` on `bezier-curve-outline`. Line 728-736: `bezier-hit-path` has `listening={true}` and `onDblClick`. Grep: `onDblClick` appears only on bezier-hit-path, not on bezier-curve-outline. |
| 13 | splitPolyRange and findNearestSegmentAndVertex exist as exported pure functions | ✓ VERIFIED | `bezierFit.ts` lines 126 and 154. 10 dedicated tests covering both: splitPolyRange contiguity/clamp, findNearestSegmentAndVertex hit-test (segment 0, closing segment N-1, null far from curve). |
| 14 | All four DEV hooks are DEV-gated and torn down on unmount | ✓ VERIFIED | Single `import.meta.env.DEV`-gated useEffect (line 577). Hooks: `__forgeBezierState`, `__forgeBezierTriggerDrag`, `__forgeBezierTriggerHandleDrag`, `__forgeBezierTriggerInsert`. All four deleted in the return/cleanup (lines 683-687). `handleInsertAtPx` added to dep array (line 688). |

**Score:** 14/14 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/src/lib/bezierFit.ts` | buildPolyIndexMap range tiling + splitPolyRange + findNearestSegmentAndVertex | ✓ VERIFIED | All three exported (lines 74, 126, 154). buildPolyIndexMap forces last range to M-1 (closing-duplicate vertex coverage). |
| `frontend/src/components/canvas/BezierEditLayer.tsx` | Closed-ring buildPathData + editable closing segment + clamped refit + distinct palette + 3 DEV hooks + dedicated hit-Path | ✓ VERIFIED | All present. buildPathData: N C commands. addSeg: s<N. WR-02 clamp: line 565. G4 palette: 6 distinct hexes. DEV hooks: 4 registered, DEV-gated, unmount-cleaned. hit-Path: bezier-hit-path with onDblClick. |
| `frontend/src/lib/bezierFlatten.ts` | flattenSegment hardened for targetCount < 2 (WR-03) | ✓ VERIFIED | Lines 49-52: degenerate guard always emits [p0, p3]. |
| `frontend/src/components/canvas/__tests__/BezierEditLayer.addpoint.test.tsx` | G3 insert identity-safe + hit-path + drag-after-insert | ✓ VERIFIED | Created in Plan 06. 8 tests, all green. |
| `frontend/tests/uat/playwright/08.1-bezier-edit.spec.ts` | BEZ-UAT closing-segment-handle-drag + G3 insert/drag integration | ✓ VERIFIED (wiring) | Hook calls present at lines 138-170 (G2 re-verify) and 186-235 (G3 re-verify). No direct store-action calls (grep confirms). Running-app confirmation is in human section. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| BezierEditLayer.buildPathData | closing cubic anchor[N-1] -> anchor[0] | explicit wrap-around C command after loop | ✓ WIRED | Lines 112-114; tests assert N C commands and last target = anchor[0].anchorPx |
| BezierEditLayer.handleDragEnd | useEditorStore.setVerticesAndLog | closing segment now in dirty set, op:'move' | ✓ WIRED | incomingSeg=(idx-1+N)%N; addSeg s<N; single call at line 538 |
| 08.1-bezier-edit.spec.ts cp1-on-anchor-0 drag | useEditorStore.editLog | __forgeBezierTriggerHandleDrag → handleDragMove/handleDragEnd → setVerticesAndLog | ✓ WIRED (proxy) | Hook defined at lines 646-662; spec drives it at lines 170-177. Real-app confirmation: human section. |
| BezierEditLayer dedicated hit-Path onDblClick | insert anchor + splitPolyRange | nearest-curve-parameter hit-test, component-local setAnchors | ✓ WIRED | Lines 728-736 (hit-Path onDblClick → handleInsertAtPx); handleInsertAtPx calls findNearestSegmentAndVertex + splitPolyRange. |
| 08.1-bezier-edit.spec.ts __forgeBezierTriggerInsert | useEditorStore.editLog (unchanged on insert) + (grows on follow-up drag) | DEV hook invokes real insert handler; drag via __forgeBezierTriggerDrag | ✓ WIRED (proxy) | Hook defined at lines 669-681; spec drives it at lines 222-240. Real-app confirmation: human section. |

### Data-Flow Trace (Level 4)

This phase is a UI-only layer (no API/DB calls). Data flow is component-local:
- Entry: store.vertices → geoToCanvas → fitPolygonToBezier → buildPolyIndexMap → deriveAnchors → component state (no store write)
- Drag commit: component anchor state → flattenSegment → setVerticesAndLog (single chokepoint) → store.vertices updated
- Insert: component anchor state → splitPolyRange → setAnchors only (zero store write, identity-safe)

All data flows verified through unit tests and code inspection. No static/hollow data flow patterns detected.

### Behavioral Spot-Checks

| Behavior | Check | Result | Status |
|----------|-------|--------|--------|
| BEZ-IDENTITY-01 (enter+exit zero drags) | `npx vitest run src/stores/__tests__/useEditorStore.bezierIdentity.test.ts` | 2/2 passed | ✓ PASS |
| G3 insert identity (insert-without-drag) | `npx vitest run src/components/canvas/__tests__/BezierEditLayer.addpoint.test.tsx` | 8/8 passed | ✓ PASS |
| G2 cp1-anchor-0 drag (no silent drop) | BezierEditLayer.drag.test.tsx: "cp1 drag on anchor 0 produces non-empty changedIds" | ✓ green | ✓ PASS |
| G1 closed ring (N C commands) | BezierEditLayer.test.tsx: "buildPathData with N anchors emits exactly N C commands" | ✓ green | ✓ PASS |
| splitPolyRange contiguity | bezierFit.test.ts: 6 splitPolyRange tests | 6/6 passed | ✓ PASS |
| findNearestSegmentAndVertex closing segment | bezierFit.test.ts: "closing segment (segmentIdx===N-1) is reachable" | ✓ green | ✓ PASS |
| Full Playwright G1+G2+G3 UAT in browser | `npx playwright test 08.1-bezier-edit.spec.ts` | SUMMARY-06 self-reports 3/3 green; not re-run | ? SKIP (human section) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| BEZ-IDENTITY-01 | 05, 06 | Enter+exit+insert with zero drags → store byte-identical | ✓ SATISFIED | bezierIdentity.test.ts (2 tests) + addpoint.test.tsx (3 identity tests) |
| BEZ-FIT-01 | 05 | Calibrated fit error (3..40 cubics, BEZ_FIT_ERROR=30 stable) | ✓ SATISFIED | bezierFit.test.ts: fit count 4..30 band + BEZ_FIT_ERROR===30 locked |
| BEZ-INDEX-01 | 05, 06 | buildPolyIndexMap ranges contiguous, full coverage, closing-duplicate covered | ✓ SATISFIED | bezierFit.test.ts: 5 BEZ-INDEX-01 tests + 3 closing-segment coverage tests |
| BEZ-FLATTEN-01 | 05 | flattenSegment even-spacing for targetCount >= 2 | ✓ SATISFIED | bezierFlatten.test.ts: existing even-spacing tests green |
| BEZ-FLATTEN-02 | 05 | flattenSegment emits both endpoints for targetCount < 2 (WR-03) | ✓ SATISFIED | bezierFlatten.test.ts: "flattenSegment(cubic, 1, ...).length >= 2" + endpoints p0/p3 |
| BEZ-RENDER-01 | 05, 06 | Anchor count in 4..40 range, visible curve outline renders | ✓ SATISFIED | BezierEditLayer.test.tsx: BEZ-RENDER-01 green; bezier-curve-outline present with listening=false |
| BEZ-RENDER-02 | 05, 06 | Nothing renders when activeTerritoryId is null | ✓ SATISFIED | BezierEditLayer.test.tsx: BEZ-RENDER-02 green |
| BEZ-DRAG-01 | 05 | Drag commits op:'move'; dirty-only flatten; verbatim copy for non-dirty | ✓ SATISFIED | BezierEditLayer.drag.test.tsx: 9 tests green including cp1-anchor-0 and cp2-last-anchor |
| BEZ-UAT-01 | 05 | Playwright: select barony → anchors → drag commits in running app | ? NEEDS HUMAN | Wiring verified; running-app execution in human section |
| BEZ-UAT-02 | (prior plans) | Playwright: zero-drag round trip → lookup PNG byte-identical | ✓ SATISFIED | SUMMARY-04 reports green; identity contract preserved by design (IDENTITY-01 holds, no new store writes on entry or insert) |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| BezierEditLayer.tsx | 29 | Comment says "5 visually distinct colors" but lists 6 | ℹ️ Info | Cosmetic mismatch in a header comment; no behavioral impact (IN-03 from code review) |
| BezierEditLayer.tsx | 428 | `setDirtyCount(idx === 0 \|\| kind === 'cp2' ? 1 : 2)` is misaligned with the real commit dirty-set (IN-01 from code review) | ℹ️ Info | `dirtyCount` is transient display state surfaced only in `__forgeBezierState().dirtySegmentCount`; resets to 0 on dragEnd; no commit-path or store impact. UAT spec asserts on `editLogLength`, not `dirtySegmentCount`. |

No blockers or warnings found. Two info-level items from the code review (IN-01, IN-03) are cosmetic; the commit path and identity contract are unaffected.

### Human Verification Required

#### 1. Re-flattened contour does not visibly coarsen the map

**Test:** In the running app (backend + Vite both running), open an Iberia 868 project, select a barony in the V-tool Bézier mode, drag an anchor handle to edit a segment, confirm the change, then visually inspect the rendered raster.
**Expected:** The barony contour at the edited segment shows no visible thickening or coarsening vs the pre-edit state. The closing segment of the ring should be visible (closed ring, G1 verified).
**Why human:** Visual raster fidelity at σ=3.0 smoothing density — SSIM / eye-judgement, not a unit assertion. This test was previously blocked by G2 (silent drop); G2 is now closed so the test is now runnable.

#### 2. Curve handles feel usable in the running browser (G1 ring + G4 palette visible)

**Test:** Select a barony in V-tool mode. Observe that the Bézier outline forms a completely closed ring around the barony. Observe that the contour (green), inactive anchors (blue), active anchor (amber), handles (magenta), and tethers (slate-gray) are visually distinguishable. Try double-clicking the outline to insert a new anchor.
**Expected:** Closed ring visible; 5+ visually distinct colors; double-click inserts a new square anchor that can then be dragged.
**Why human:** G1 closed-ring rendering and G4 palette readability are visual properties — code verifies the N C commands and pairwise hex inequality but cannot assess actual screen rendering quality or user perception.

#### 3. Playwright UAT: G1+G2+G3 closing-segment and insert scenarios pass in the running app

**Test:** With backend and Vite running (globalSetup seeds Iberia 868 project): `cd frontend && npx playwright test 08.1-bezier-edit.spec.ts`
**Expected:** 3/3 tests pass: BEZ-UAT-01 (barony → anchors → drag commits op:'move'), G1+G2 re-verify (cp1 drag on anchor 0 via `__forgeBezierTriggerHandleDrag('cp1', 0, 25, 25)` grows editLog), G3 re-verify (bare insert via `__forgeBezierTriggerInsert(0)` keeps editLog, follow-up drag grows it).
**Why human:** The hook wiring is verified by grep and unit tests run against a mocked Konva. The full integration path through real Konva + browser pointer-to-stage coordinate resolution is reported green in SUMMARY-06 but was not independently re-run in this verification pass.

### Gaps Summary

No gaps. All 4 human-UAT gaps (G1, G2, G3, G4) are closed in the codebase and verified by automated tests. The BEZ-IDENTITY-01 contract holds through both the drag-commit and insert-without-drag paths.

Status is `human_needed` because:
1. Visual raster coarsening test (previously blocked by G2) is now runnable but requires human judgment
2. Visual ring closure (G1) and palette readability (G4) need browser confirmation
3. The Playwright G1+G2+G3 integration scenarios against real Konva in a browser were self-reported in SUMMARY-06 but not independently executed this verification pass

These are not regressions or new gaps — they are the same class of human-only verification items present in the prior verification, with one item (raster coarsening) now unblocked and one new item (Playwright re-run confirmation) added.

---

_Verified: 2026-05-29T11:30:00Z_
_Verifier: Claude (gsd-verifier)_
_Re-verification: Plans 05 (G1/G2/G4) and 06 (G3) gap closure_
