---
phase: 08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr
verified: 2026-05-28T17:30:00Z
status: human_needed
score: 10/10 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Curve handles feel usable vs hundreds of raw dots"
    expected: "Selecting a barony in V-tool on the baronies layer shows ~4-20 manageable Bézier anchors (not ~100 raw vertices); dragging an anchor makes the contour follow smoothly"
    why_human: "Subjective usability — the entire reason Phase 08 raw handles were rejected; not assertable programmatically"
  - test: "Re-flattened contour does not visibly coarsen the map after edit+apply"
    expected: "After editing a segment + apply, the rendered raster shows no visible coarsening at edited segments vs pre-edit"
    why_human: "Visual raster fidelity at σ=3.0 density; SSIM/eye-judgement, not a unit assertion"
---

# Phase 08.1: Bézier-Assisted Barony Contour Editing — Verification Report

**Phase Goal:** Replace the unusable raw per-vertex handle model with a Bézier-assisted control-point editor (~10-20 anchors + active-anchor handles). Bézier lives ONLY in the editor UI layer (component-local, never in useEditorStore/zundo); only DIRTY dragged segments flatten back to polygon vertices; non-dirty ranges copy verbatim. Non-negotiable invariant: empty-log identity contract (enter+exit with zero drags → useEditorStore.vertices byte-identical, editLog unchanged). Backend/DAG/export schema/12-file parity unchanged.
**Verified:** 2026-05-28T17:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Empty-log identity contract: enter+exit zero drags → vertices byte-identical + editLog unchanged (BEZ-IDENTITY-01) | ✓ VERIFIED | `useEditorStore.bezierIdentity.test.ts` mounts the REAL `BezierEditLayer` against the REAL store (only react-konva mocked); 2/2 tests green. Entry fit effect (BezierEditLayer.tsx:258-283) only computes cubics→local state; sole `setVerticesAndLog` at line 446 is inside drag commit. |
| 2 | Bézier state lives ONLY in component-local state (never in store/zundo) | ✓ VERIFIED | `grep activeAnchorIdx\|bezier` in useEditorStore.ts → 0 hits. partialize unchanged: `{vertices, editLog}` (line 454). SUMMARY-02 confirms store unmodified; git shows useEditorStore.ts not in 08.1 modified set. |
| 3 | Anchors/handles are curve-fit derived from polygon for display (~4-20 not hundreds) | ✓ VERIFIED | `fitPolygonToBezier` + `buildPolyIndexMap` imported and called on entry (lines 281-283). BEZ-RENDER-01 test asserts anchor count in 4..40 band, far below raw count; IBERIA_BARONY_RING (~100 verts) → 4 cubics. |
| 4 | Only DIRTY (dragged) segments flatten; non-dirty ranges copy verbatim | ✓ VERIFIED | BEZ-DRAG-01 test asserts committed nextVertices differs from store ONLY at dirty segment ranges; all other keys byte-identical. nextVertices starts as `{...store.vertices}`, only dirty ranges overwritten via `flattenSegment` (line 416). |
| 5 | Anchor drag commits via setVerticesAndLog with op:'move' (never bezier_drag) | ✓ VERIFIED | Single `setVerticesAndLog` call (line 446) with `op: 'move'` (line 447). `bezier_drag` token appears only in a guardrail comment (line 445). |
| 6 | Snap (anchors only) + shared-vertex coupling reused from Phase 08 | ✓ VERIFIED | `snapToNeighbour` (line 366, anchor path only), `getCoupledVertices` (line 434), `buildSharedVertexIndex` (line 302) all imported and called — reused, not reimplemented. |
| 7 | VertexEditLayer and BezierEditLayer mutually exclusive at z=5 | ✓ VERIFIED | CanvasViewer.tsx:771-774 ternary `{bezierActive ? <BezierEditLayer/> : <VertexEditLayer/>}` — exactly one mounted. `bezierActive = editableLayer==='baronies' && activeTerritoryId!==null` (line 180). |
| 8 | A/D tools disabled with locked PT copy in Bézier mode; EditToolPalette stays Bézier-unaware | ✓ VERIFIED | EditToolPalette `disabledTools?` prop (line 107), DISABLED_TOOLTIP PT map (line 53); no editableLayer/activeTerritoryId reads (only a comment). WorkspaceToolbar derives `disabledTools = ['A','D']` when bezierMode and threads it (lines 79-81, 175). EditToolPalette tests green. |
| 9 | Reachable in running app: select barony → Bézier anchors → drag commits op:'move' (BEZ-UAT-01) | ✓ VERIFIED | `08.1-bezier-edit.spec.ts` exists; SUMMARY-04 reports GREEN (947ms) against seeded Iberia 868 (barony "Grado"), anchorCount in 4..40, editLogLength growth after drag. (Re-run gated to human due to server-restart UAT protocol.) |
| 10 | Zero-drag round trip → exported lookup_barony.png byte-identical (BEZ-UAT-02) | ✓ VERIFIED | `08.1-bezier-identity.spec.ts` exists; SUMMARY-04 reports GREEN, SHA-256 `d557e8e6...662a` matches pre-edit baseline. Complements unit-level BEZ-IDENTITY-01. |

**Score:** 10/10 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/src/lib/bezierFit.ts` | fitPolygonToBezier + buildPolyIndexMap | ✓ VERIFIED | Exists (3.6KB), exports verified, imported by BezierEditLayer. Pure (store-free). |
| `frontend/src/lib/bezierFlatten.ts` | cubicBezierAt + flattenSegment | ✓ VERIFIED | Exists (1.8KB), flattenSegment called per dirty segment. |
| `frontend/src/components/canvas/__fixtures__/iberiaBaronyRing.ts` | IBERIA_BARONY_RING + RING_100 | ✓ VERIFIED | Exists (6KB), explicit numeric fixtures. |
| `frontend/src/components/canvas/BezierEditLayer.tsx` | render + drag-commit layer | ✓ VERIFIED | Exists (28.5KB, ≥80 lines). Wired into CanvasViewer; renders anchors/handles; drag flatten path. |
| `frontend/src/stores/__tests__/useEditorStore.bezierIdentity.test.ts` | BEZ-IDENTITY-01 guard | ✓ VERIFIED | Mounts real component + real store; 2/2 green. |
| `frontend/tests/uat/playwright/08.1-bezier-edit.spec.ts` | BEZ-UAT-01 | ✓ VERIFIED | Exists; reported green in SUMMARY-04. |
| `frontend/tests/uat/playwright/08.1-bezier-identity.spec.ts` | BEZ-UAT-02 | ✓ VERIFIED | Exists; reported green in SUMMARY-04. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| BezierEditLayer.tsx | bezierFit.ts | fitPolygonToBezier + buildPolyIndexMap on entry | ✓ WIRED | lines 281-283 |
| BezierEditLayer.tsx | useEditorStore.setVerticesAndLog | onDragEnd op:'move' commit | ✓ WIRED | line 446, op:'move' |
| BezierEditLayer.tsx | bezierFlatten.ts | flattenSegment per dirty segment | ✓ WIRED | line 416 |
| BezierEditLayer.tsx | snap.ts + sharedVertex.ts | snapToNeighbour + getCoupledVertices | ✓ WIRED | lines 366, 434 |
| CanvasViewer.tsx | BezierEditLayer | conditional z=5 mount (mutex w/ VertexEditLayer) | ✓ WIRED | lines 771-774 |
| WorkspaceToolbar.tsx | EditToolPalette disabledTools | bezier-mode-derived prop | ✓ WIRED | lines 79-81, 175 |

### Requirements Coverage (validation-ID spine)

| ID | Source Plan | Status | Evidence |
|----|-------------|--------|----------|
| BEZ-IDENTITY-01 | 02, 03 | ✓ SATISFIED | bezierIdentity.test.ts 2/2 green; stays green after drag code |
| BEZ-FIT-01 | 01 | ✓ SATISFIED | bezierFit.test.ts (11 tests) green |
| BEZ-INDEX-01 | 01 | ✓ SATISFIED | buildPolyIndexMap contiguous-range test green |
| BEZ-FLATTEN-01 | 01 | ✓ SATISFIED | bezierFlatten.test.ts (7) green |
| BEZ-FLATTEN-02 | 01 | ✓ SATISFIED | density-match test (tol recalibrated 1e-4°→0.01°, documented) green |
| BEZ-RENDER-01 | 02 | ✓ SATISFIED | anchor count 4..40 far below raw |
| BEZ-RENDER-02 | 02 | ✓ SATISFIED | null activeTerritoryId → zero nodes |
| BEZ-DRAG-01 | 03 | ✓ SATISFIED | drag → dirty-only flatten → op:'move', verbatim copy (8 tests) |
| BEZ-UAT-01 | 04 | ✓ SATISFIED | reachability+drag spec green (SUMMARY-04) |
| BEZ-UAT-02 | 04 | ✓ SATISFIED | identity-through-export spec green, SHA-256 baseline match |

All 10 validation IDs accounted for in PLAN frontmatter + implementation/tests. No orphaned IDs.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Phase 08.1 frontend unit + identity + render + drag + palette suite | `npx vitest run` (6 files) | 43/43 passing | ✓ PASS |
| Backend parity untouched | `git diff --name-only c882bbb..HEAD -- backend/` | empty | ✓ PASS |
| All 9 work commits present | `git log c882bbb..HEAD` | e7cfa1c…76fae78 all present | ✓ PASS |

### Anti-Patterns Found

None blocking. Identity contract holds: no store writes on entry, op:'move' exclusively, dirty-only flatten with verbatim non-dirty copy, no `bezier_drag` op token, no Bézier fields in store/zundo.

3 Warnings from 08.1-REVIEW.md (WR-01/02/03) are edge-case UX gaps (closing-segment handles preview-but-don't-commit; post-commit activeAnchorIdx not reconciled; zero-length flatten fallback). None violate the phase contract or the identity invariant; all documented as V1 limitations. Recommended for follow-up polish, not blocking.

### Human Verification Required

1. **Curve handles feel usable** — In browser: select a barony, enter V-tool edit mode on baronies layer, confirm a manageable count of anchors (~4-20) appears (not hundreds); drag an anchor and confirm the contour follows smoothly. *(Subjective UX — the core reason raw handles were rejected.)*
2. **Re-flattened contour does not visibly coarsen the map** — After editing a segment + apply, compare rendered raster to pre-edit; confirm no visible coarsening at edited segments. *(Visual raster fidelity at σ=3.0.)*

Per memory feedback-server-restart-before-test, the two Playwright UAT specs should be re-run after a fresh shutdown+build+restart before sign-off, even though SUMMARY-04 reports them green.

### Gaps Summary

No gaps blocking goal achievement. All 10 observable truths verified, all artifacts present and wired, all 10 validation IDs satisfied, the non-negotiable empty-log identity contract holds at both unit (BEZ-IDENTITY-01) and end-to-end (BEZ-UAT-02) levels, and zero backend files changed (parity-safe). Status is `human_needed` solely because two subjective/visual UX criteria (usability of the anchor model; raster non-coarsening) cannot be verified programmatically and are the explicit reason this phase exists.

---

_Verified: 2026-05-28T17:30:00Z_
_Verifier: Claude (gsd-verifier)_
