---
phase: 08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - frontend/src/components/canvas/CanvasViewer.tsx
  - frontend/src/components/editor/EditToolPalette.tsx
  - frontend/src/components/workspace/WorkspaceToolbar.tsx
  - frontend/src/components/editor/__tests__/EditToolPalette.test.tsx
  - frontend/tests/uat/playwright/08.1-bezier-edit.spec.ts
  - frontend/tests/uat/playwright/08.1-bezier-identity.spec.ts
autonomous: true
requirements: [BEZ-UAT-01, BEZ-UAT-02]
must_haves:
  truths:
    - "In the running app: select a barony with V-tool on baronies layer → Bézier anchors visible on canvas (NOT hundreds of raw dots); BezierEditLayer mounted, VertexEditLayer barony-handle section not active"
    - "VertexEditLayer (z=5) and BezierEditLayer (z=5) are mutually exclusive — never both mounted/active at once"
    - "A and D IconButtons render with Radix disabled prop + the locked PT tooltip copy when Bézier mode is active; EditToolPalette stays Bézier-unaware (receives disabledTools as a prop from WorkspaceToolbar)"
    - "Playwright: select barony → drag a Bézier anchor → canvas curve updates and store commits op:'move'"
    - "Playwright: select barony → enter+exit edit mode with zero drags → export → lookup_barony.png SHA-256 matches pre-edit baseline"
  artifacts:
    - path: "frontend/tests/uat/playwright/08.1-bezier-edit.spec.ts"
      provides: "BEZ-UAT-01 reachability + drag integration spec (the Phase 08 isolation-vs-integration lesson)"
    - path: "frontend/tests/uat/playwright/08.1-bezier-identity.spec.ts"
      provides: "BEZ-UAT-02 identity-through-export spec (slow, phase-gate only)"
  key_links:
    - from: "frontend/src/components/canvas/CanvasViewer.tsx"
      to: "BezierEditLayer"
      via: "conditional z=5 mount, mutually exclusive with VertexEditLayer barony handles"
      pattern: "BezierEditLayer"
    - from: "frontend/src/components/workspace/WorkspaceToolbar.tsx"
      to: "EditToolPalette disabledTools prop"
      via: "Bézier-mode-derived disabled set threaded as prop"
      pattern: "disabledTools"
---

<objective>
Wire `BezierEditLayer` into the running app and prove it is reachable — the Phase 08 isolation-vs-integration lesson (RESEARCH Pitfall 5: unit tests passed but the editor was non-functional because integration wiring was missing). Add the conditional z=5 mount in `CanvasViewer` (mutually exclusive with `VertexEditLayer`'s barony-handle section), thread `disabledTools` into `EditToolPalette` from `WorkspaceToolbar` to disable A/D in Bézier mode, and deliver the two Playwright UAT specs (BEZ-UAT-01 reachability+drag, BEZ-UAT-02 identity-through-export).

Purpose: A passing unit suite is not proof of a working feature. This plan is the integration gate — it makes Bézier editing actually appear when a designer selects a barony, disables the inapplicable tools with the locked PT copy, and drives the whole flow end-to-end in a real browser including the byte-identity export guarantee.
Output: integrated mount + disabled-tools threading + 2 Playwright specs. Phase gate: full vitest + BEZ-UAT-01 green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-RESEARCH.md
@.planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-UI-SPEC.md

<interfaces>
<!-- CanvasViewer current z=5 mount (the conditional goes here). Source: CanvasViewer.tsx:750-764 -->
```tsx
const editableLayer = useEditorStore((s) => s.editableLayer)        // 'baronies' | 'landmask'
const activeTerritoryId = useEditorStore((s) => s.activeTerritoryId)
// existing z=5:
<VertexEditLayer stageRef={stageRef} viewport={vertexViewport} tier="barony"
  projectId={projectId} branchId={activeBranchId ?? undefined}
  editableLayer={editableLayer} landmaskCoords={landmaskRing}
  onLandmaskCoordsChange={handleLandmaskCoordsChange} />
```

<!-- EditToolPalette current (no props). Source: EditToolPalette.tsx:91. TOOL_DESCRIPTORS has keys V,A,D,S,M,ESC -->
```tsx
export function EditToolPalette() { /* maps TOOL_DESCRIPTORS → IconButton wrapped in Tooltip */ }
// Radix IconButton supports `disabled` prop; Tooltip `content` carries the copy.
```

<!-- WorkspaceToolbar mounts <EditToolPalette /> with no props (line 162). It already reads useEditorStore for undo/redo. -->

<!-- DEV escape hatches available for Playwright: -->
// window.__forgeSelectBarony(name)  (BaronyLayer.tsx) — selects a barony by name in the seeded Iberia 868 fixture
// window.__forgeBezierState()       (BezierEditLayer.tsx, Plan 02) → { anchorCount, activeAnchorIdx, dirtySegmentCount }
// window.__forgeStageScale          (CanvasViewer.tsx)

<!-- UI-SPEC locked disabled-tool tooltip copy (PT, verbatim) -->
A disabled: "Adicionar vértice indisponível em modo Bézier — alterne para modo raw para adicionar vértices"
D disabled: "Excluir vértice indisponível em modo Bézier — alterne para modo raw para excluir vértices"

<!-- UI-SPEC Konva Layer Stack mount rule (z=5 mutual exclusion):
  editableLayer==='landmask'                          → VertexEditLayer active, BezierEditLayer unmounted
  editableLayer==='baronies' && activeTerritoryId!==null → BezierEditLayer mounted; VertexEditLayer barony-handle section inactive -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CanvasViewer conditional mount + EditToolPalette disabledTools prop + WorkspaceToolbar threading</name>
  <files>frontend/src/components/canvas/CanvasViewer.tsx, frontend/src/components/editor/EditToolPalette.tsx, frontend/src/components/workspace/WorkspaceToolbar.tsx, frontend/src/components/editor/__tests__/EditToolPalette.test.tsx</files>
  <read_first>
    - frontend/src/components/canvas/CanvasViewer.tsx lines 160-231 (editableLayer/activeTerritoryId selectors, clearCache effect), 748-790 (z=5 mount block + how projection is obtained for the layer)
    - frontend/src/components/canvas/BezierEditLayer.tsx (Plan 02/03 — props it expects, e.g. projection)
    - frontend/src/components/editor/EditToolPalette.tsx (TOOL_DESCRIPTORS, the IconButton/Tooltip render loop — add disabled handling without making the component Bézier-aware)
    - frontend/src/components/workspace/WorkspaceToolbar.tsx line 162 (the <EditToolPalette /> mount; it already imports useEditorStore patterns for undo/redo)
    - frontend/src/components/editor/__tests__/EditToolPalette.test.tsx (existing test harness/wrap util to extend for disabledTools cases)
    - .planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-UI-SPEC.md §A and D tools in Bézier mode (Note #7 — EditToolPalette stays unaware; signal via prop), §Konva Layer Stack (mount rule), §Disabled-tool states (copy)
  </read_first>
  <action>
    1. `EditToolPalette.tsx`: add optional prop `disabledTools?: EditTool[]` (default `[]`). In the TOOL_DESCRIPTORS render loop, compute `const isDisabled = disabledTools.includes(key as EditTool)`. Pass `disabled={isDisabled}` to the Radix `IconButton`. When disabled, override the Tooltip `content` with the locked PT copy: A → "Adicionar vértice indisponível em modo Bézier — alterne para modo raw para adicionar vértices"; D → "Excluir vértice indisponível em modo Bézier — alterne para modo raw para excluir vértices" (define a `DISABLED_TOOLTIP: Partial<Record<EditTool, string>>` map). EditToolPalette must NOT read editableLayer/activeTerritoryId — it stays Bézier-unaware (Note #7); it only honors the prop.
    2. `WorkspaceToolbar.tsx`: read `editableLayer`, `activeTerritoryId`, `activeTool` from useEditorStore. Compute `const bezierMode = editableLayer === 'baronies' && activeTerritoryId !== null && activeTool === 'V';` and `const disabledTools: EditTool[] = bezierMode ? ['A', 'D'] : [];`. Pass `<EditToolPalette disabledTools={disabledTools} />`.
    3. `CanvasViewer.tsx`: at z=5, make VertexEditLayer and BezierEditLayer mutually exclusive (UI-SPEC §Konva Layer Stack). Compute `const bezierActive = editableLayer === 'baronies' && activeTerritoryId !== null;`. Render `<VertexEditLayer .../>` ONLY when `!bezierActive` (i.e. landmask mode, or no barony selected). Render `<BezierEditLayer projection={projection} .../>` when `bezierActive`. Note: VertexEditLayer's barony-handle section already requires editableLayer guards; the cleanest mutual-exclusion is to swap which layer mounts at z=5 based on `bezierActive`. Keep the existing clearCache-on-activeTerritoryId effect intact. Obtain `projection` the same way VertexEditLayer does (useProjection / the projection already in scope).
    4. Extend `EditToolPalette.test.tsx`: add cases — (a) `disabledTools={['A','D']}` renders the A and D IconButtons with `disabled` and the locked PT tooltip copy; (b) default (no prop) renders all tools enabled (regression — Phase 08 behavior unchanged). Use descriptive test names.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/editor/__tests__/EditToolPalette.test.tsx && npx tsc --noEmit -p frontend/tsconfig.json</automated>
  </verify>
  <done>
    - EditToolPalette accepts `disabledTools` and renders disabled A/D + locked PT tooltips; stays Bézier-unaware (no store reads for mode).
    - WorkspaceToolbar threads the Bézier-mode-derived disabled set.
    - CanvasViewer mounts BezierEditLayer when bezierActive, VertexEditLayer otherwise — never both at z=5.
    - `tsc --noEmit` clean; EditToolPalette tests (new + existing) pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Playwright BEZ-UAT-01 (reachability + drag) and BEZ-UAT-02 (identity through export)</name>
  <files>frontend/tests/uat/playwright/08.1-bezier-edit.spec.ts, frontend/tests/uat/playwright/08.1-bezier-identity.spec.ts</files>
  <read_first>
    - frontend/src/components/canvas/BaronyLayer.tsx lines 60-73 (__forgeSelectBarony escape hatch — how to select a barony in a spec)
    - frontend/src/components/canvas/BezierEditLayer.tsx (Plan 02 — window.__forgeBezierState() shape)
    - frontend/tests/uat/playwright/ (existing specs e.g. 08-editor-reachable.spec.ts from Plan 08-15 — copy its app-boot + project-open + editor-entry scaffolding; reuse its selectors and runner config)
    - .planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-VALIDATION.md (BEZ-UAT-01/02 commands), 08.1-RESEARCH.md §Pitfall 5 (integration vs isolation)
    - frontend/playwright.config.ts (test dir, projects)
  </read_first>
  <action>
    Create `frontend/tests/uat/playwright/08.1-bezier-edit.spec.ts` (BEZ-UAT-01): boot the app, open the seeded Iberia 868 project, ensure editableLayer='baronies' + V-tool active, call `window.__forgeSelectBarony('<a known barony name from the seeded fixture>')`, then assert via `await page.evaluate(() => window.__forgeBezierState())` that `anchorCount` is in the 4..40 range (NOT hundreds — the whole point of the phase). Then simulate dragging an anchor: locate the active anchor Konva node (or use a DEV hook to trigger a drag) and assert the curve/store updated — assert `__forgeBezierState().dirtySegmentCount` was >0 during/after a drag OR that the store editLog grew by one op:'move'. Reuse the editor-entry scaffolding from the Phase 08 08-editor-reachable spec. Mirror the 08.1-CONTEXT memory rule: shutdown+build+restart servers before the spec runs (use the existing UAT runner harness).
    Create `frontend/tests/uat/playwright/08.1-bezier-identity.spec.ts` (BEZ-UAT-02, slow — phase-gate only, mark with the project's slow/uat tag): open project, capture pre-edit `lookup_barony.png` SHA-256 (via the export endpoint or artifact fetch used in Phase 06/08 export specs), enter Bézier edit mode (select barony, V-tool), exit WITHOUT dragging, trigger export, fetch `lookup_barony.png`, assert SHA-256 matches the pre-edit baseline byte-for-byte. This is the end-to-end port of the Phase 08 `test_empty_edit_log_preserves_lookup_barony_png` parity test.
    If a stable selector for entering the editor is not reachable (Phase 07.1 harness-fallback lesson), add a minimal DEV-gated test route or reuse the existing one — do not block on a brittle DOM path.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test tests/uat/playwright/08.1-bezier-edit.spec.ts</automated>
  </verify>
  <done>
    - 08.1-bezier-edit.spec.ts (BEZ-UAT-01) green: barony select → anchors in 4..40 range visible → anchor drag updates canvas/store.
    - 08.1-bezier-identity.spec.ts (BEZ-UAT-02) authored; enter+exit-no-drag → export → lookup_barony.png SHA-256 matches baseline.
    - Both specs use the DEV escape hatches (__forgeSelectBarony, __forgeBezierState), not brittle Konva-canvas DOM scanning.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editableLayer/activeTool state → which z=5 layer mounts | Mode state decides whether raw-vertex or Bézier editing is reachable; a wiring error could mount both (double handlers) or neither (feature unreachable) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-04-01 | Denial of Service (feature unreachable / double-mount) | CanvasViewer z=5 mount | mitigate | Mutual-exclusion condition `bezierActive` gates exactly one of VertexEditLayer / BezierEditLayer at z=5; BEZ-UAT-01 proves reachability in the running app (RESEARCH Pitfall 5). |
| T-08.1-04-02 | Tampering (silent geometry rewrite reaching export) | enter+exit → export path | mitigate | BEZ-UAT-02 asserts `lookup_barony.png` SHA-256 byte-identity after a zero-drag edit-mode round trip — the end-to-end identity guarantee, complementing the Plan 02 unit-level BEZ-IDENTITY-01. |
| T-08.1-04-03 | Elevation/misuse (editing tools that corrupt geometry in Bézier mode) | A/D tools | mitigate | A and D rendered Radix-`disabled` in Bézier mode (no silent no-op) with explanatory PT copy; verified by EditToolPalette test. |
</threat_model>

<verification>
- `cd frontend && npx vitest run` (full suite) green — phase gate.
- `cd frontend && npx playwright test tests/uat/playwright/08.1-bezier-edit.spec.ts` green — BEZ-UAT-01 reachability+drag, phase gate.
- `cd frontend && npx playwright test tests/uat/playwright/08.1-bezier-identity.spec.ts` — BEZ-UAT-02 identity-through-export (slow; phase-gate run).
- `pytest tests/parity/` green (UI-SPEC Note #10 — zero backend commits this phase; parity must be untouched).
- Grep gate: `grep -rn "editableLayer\|activeTerritoryId" frontend/src/components/editor/EditToolPalette.tsx` returns nothing (EditToolPalette stays Bézier-unaware — Note #7).
- `npx tsc --noEmit` clean across frontend.
</verification>

<success_criteria>
- BEZ-UAT-01 + BEZ-UAT-02 specs authored and BEZ-UAT-01 green in a real browser.
- BezierEditLayer reachable in the running app; mutually exclusive with VertexEditLayer at z=5.
- A/D disabled with locked PT copy in Bézier mode; EditToolPalette stays Bézier-unaware.
- Full vitest suite + backend parity green; zero backend commits.
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-04-SUMMARY.md`. Record the barony name used in the UAT, the anchor count it produced live, and the BEZ-UAT-02 baseline SHA-256 confirmation. Note any DEV test-route fallback used for editor entry.
</output>
