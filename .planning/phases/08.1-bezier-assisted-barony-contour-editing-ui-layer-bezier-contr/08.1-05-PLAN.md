---
phase: 08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/bezierFit.ts
  - frontend/src/components/canvas/BezierEditLayer.tsx
  - frontend/src/components/canvas/__tests__/BezierEditLayer.test.tsx
  - frontend/src/components/canvas/__tests__/BezierEditLayer.drag.test.tsx
  - frontend/src/lib/__tests__/bezierFit.test.ts
  - frontend/src/lib/__tests__/bezierFlatten.test.ts
  - frontend/src/lib/bezierFlatten.ts
  - frontend/tests/uat/playwright/08.1-bezier-edit.spec.ts
autonomous: true
gap_closure: true
requirements: [BEZ-IDENTITY-01, BEZ-FIT-01, BEZ-INDEX-01, BEZ-FLATTEN-01, BEZ-FLATTEN-02, BEZ-RENDER-01, BEZ-RENDER-02, BEZ-DRAG-01]

user_setup: []

must_haves:
  truths:
    - "The Bézier outline forms a CLOSED loop: the segment from the last anchor back to anchor 0 is drawn (G1)"
    - "Every ring segment, including the closing segment, is editable: dragging anchor 0's cp1 or the last anchor's cp2 commits a flatten (G2/WR-01 superseded)"
    - "Dragging any anchor or handle and releasing persists to the store via setVerticesAndLog(op:'move') — no silent drop (G2)"
    - "After a commit refit, activeAnchorIdx is clamped so the active anchor never silently disappears (WR-02)"
    - "flattenSegment emits both endpoints even when targetCount < 2 (WR-03)"
    - "Curve outline, inactive anchor, active anchor, handle, and tether each use a visually distinct hex (G4)"
    - "BEZ-IDENTITY-01 stays green: enter+exit closed-ring edit mode with zero drags → vertices byte-identical, editLog unchanged"
    - "Playwright UAT re-verifies G1+G2 in the running app: a cp1 drag on anchor 0 (the previously-dead closing-segment handle) commits op:'move' end-to-end (editLog grows), not just in the unit harness"
  artifacts:
    - path: "frontend/src/lib/bezierFit.ts"
      provides: "buildPolyIndexMap range tiling that covers the wrap-around closing segment"
    - path: "frontend/src/components/canvas/BezierEditLayer.tsx"
      provides: "Closed-ring buildPathData + editable closing segment + clamped refit + distinct palette + __forgeBezierTriggerHandleDrag DEV hook"
    - path: "frontend/src/lib/bezierFlatten.ts"
      provides: "flattenSegment hardened for targetCount < 2"
    - path: "frontend/tests/uat/playwright/08.1-bezier-edit.spec.ts"
      provides: "BEZ-UAT closing-segment-handle-drag integration assertion"
  key_links:
    - from: "BezierEditLayer.buildPathData"
      to: "closing cubic anchor[N-1] -> anchor[0]"
      via: "explicit wrap-around C command"
      pattern: "buildPathData"
    - from: "BezierEditLayer.handleDragEnd"
      to: "useEditorStore.setVerticesAndLog"
      via: "closing segment now in dirty set, op:'move'"
      pattern: "setVerticesAndLog"
    - from: "08.1-bezier-edit.spec.ts cp1-on-anchor-0 drag"
      to: "useEditorStore.editLog"
      via: "__forgeBezierTriggerHandleDrag → real handleDragMove/handleDragEnd → setVerticesAndLog"
      pattern: "__forgeBezierTriggerHandleDrag"
---

<objective>
Fechar G1 (anel Bézier não fechado), G2 (curva editada descartada silenciosamente, incluindo WR-02 e WR-03) e G4 (paleta de cores sem diferenciação) — os três gaps que tocam `BezierEditLayer.tsx`. São agrupados num único plano porque editam o MESMO arquivo (não podem rodar em paralelo na mesma wave) e G2 depende diretamente do modelo de anel fechado de G1.

Purpose: tornar o editor Bézier realmente usável — contorno fechado, todo segmento editável e commitável, e elementos visualmente distinguíveis — sem violar o contrato de identidade BEZ-IDENTITY-01.
Output: anel fechado e totalmente editável, commit confiável em qualquer handle, refit com índice ativo preservado, paleta distinta, e uma UAT Playwright que re-verifica G1+G2 no app rodando.

**REVERSÃO DE DECISÃO (registrar):** RESEARCH §Pattern 1 documentou o segmento de fechamento como NÃO-editável (limitação deliberada V1). O UAT humano + confirmação do usuário REVERTEM essa decisão. Não "re-conserte" para o comportamento V1 não-editável. Da mesma forma, o fix sugerido por 08.1-REVIEW WR-01 ("esconder / tornar não-arrastável o cp1 da âncora 0 e o cp2 da última âncora") é SUPERSEDED por este plano: esses handles agora moldam o segmento de fechamento editável e DEVEM permanecer arrastáveis e committáveis. WR-02 e WR-03 continuam válidos e são dobrados aqui.

**RE-VERIFICAÇÃO UAT (registrar):** os gaps G1–G4 foram achados por UAT HUMANO numa superfície de UI — exatamente o modo "passa isolado / falha integrado" (RESEARCH Pitfall 5), e CLAUDE.md exige uma UAT Playwright para qualquer superfície de UI. Por isso o Task 2 estende a spec existente `08.1-bezier-edit.spec.ts` com uma asserção que prova G1+G2 no app rodando, não só no harness unitário. Como o canvas Konva é opaco ao DOM e o handle tem ~4px (arraste real de mouse é não-determinístico), a spec dirige um DEV-hook que invoca os MESMOS handlers reais (`handleDragMove`/`handleDragEnd`) que os props `onDrag*` do Konva chamam — NÃO um write direto no store.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-HUMAN-UAT.md
@.planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-REVIEW.md
@.planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-UI-SPEC.md
@frontend/tests/uat/playwright/08.1-bezier-edit.spec.ts

<interfaces>
<!-- Contratos-chave já existentes — usar diretamente, sem explorar o codebase. -->

From frontend/src/lib/bezierFit.ts:
```typescript
export type BezierCubic = [[number, number], [number, number], [number, number], [number, number]]
export interface PolyRange { rangeStart: number; rangeEnd: number }
export const BEZ_FIT_ERROR = 30
export function fitPolygonToBezier(coords, projection, error?): BezierCubic[]
export function buildPolyIndexMap(inputPxPts, cubics): PolyRange[]
```

From frontend/src/lib/bezierFlatten.ts:
```typescript
export function cubicBezierAt(p0, c1, c2, p3, t): [number, number]
export function flattenSegment(cubic: BezierCubic, targetCount: number, projection): Array<[number, number]>
```

From frontend/src/components/canvas/BezierEditLayer.tsx (current — to be modified):
```typescript
export interface BezierAnchor { idx; anchorPx; cp1Px; cp2Px; polyRangeStart; polyRangeEnd }
export function buildPathData(anchors: BezierAnchor[]): string          // loops i=0..N-2 (OPEN — the G1 bug)
export function deriveAnchors(cubics, ranges): BezierAnchor[]           // anchor j = cubic j's p0; last fit point gets NO anchor
export const ANCHOR_INACTIVE_FILL = '#4a9eff'  // SAME as CURVE_STROKE — the G4 collision
export const CURVE_STROKE = '#4a9eff'
```

NOTE on current data model: `deriveAnchors` makes one anchor per cubic (anchor j == cubic j's p0). The FINAL fit point `cubics[N-1][3]` (p3 of the last cubic) currently gets NO anchor, and `buildPathData` (i=0..N-2) draws only cubics 0..N-2 — so both the last cubic AND the wrap segment are unrendered. The closing-segment guard `addSeg` uses `s < N - 1` (line 387), excluding segment N-1.

EXISTING DEV-hook pattern to MIRROR (BezierEditLayer.tsx lines 521-537): `__forgeBezierTriggerDrag(idx, dx, dy)` reads `anchorsRef.current[idx]`, synthesizes a fake Konva event `{ target: { x, y, getStage } }`, and calls the REAL `handleDragMove('anchor', idx, evt)` then `handleDragEnd('anchor', idx, evt)`. It is registered DEV-only (`import.meta.env.DEV`) and torn down in the unmount cleanup (lines 538-541). The anchor hook computes `targetX = anchorPx[0] - 5 + dx` because the anchor Rect is TOP-LEFT-anchored (`x = anchorPx - 5`) and `draggedCenterPx('anchor', x, y)` adds +5 back. **CRITICAL for the new handle hook:** control handles are Circles (CENTER-anchored), so `draggedCenterPx('cp1'|'cp2', x, y)` returns `[x, y]` with NO +5 offset — the new hook must use `cp1Px[0] + dx` / `cp2Px[1] + dy` directly, WITHOUT the `-5`.

EXISTING Playwright helpers in 08.1-bezier-edit.spec.ts to REUSE (do not re-implement): `loadProjectInfo()`, `discoverBaronyId(page, projectId)`, `navigateToWorkspace(page, projectId)`, `readBezierState(page)` (returns `{ anchorCount, activeAnchorIdx, dirtySegmentCount, editLogLength, firstAnchor }`), the `edit-tool-v` palette click, and the `__forgeSelectBarony` hatch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close the ring — render + index-map the wrap-around segment (G1)</name>
  <files>frontend/src/lib/bezierFit.ts, frontend/src/components/canvas/BezierEditLayer.tsx, frontend/src/lib/__tests__/bezierFit.test.ts, frontend/src/components/canvas/__tests__/BezierEditLayer.test.tsx</files>
  <read_first>
    - frontend/src/components/canvas/BezierEditLayer.tsx (buildPathData line 93-102; deriveAnchors line 109-132; entry fit effect line 258-291; dirty/addSeg line 384-396; flatten loop line 403-424)
    - frontend/src/lib/bezierFit.ts (buildPolyIndexMap line 73-107 — range tiling logic + the forced last-index at line 85-87)
    - frontend/src/lib/bezierFlatten.ts (flattenSegment line 38-51)
    - frontend/src/components/canvas/__tests__/BezierEditLayer.test.tsx (BEZ-RENDER-01/02 expectations)
    - frontend/src/lib/__tests__/bezierFit.test.ts (BEZ-INDEX-01 range expectations)
  </read_first>
  <behavior>
    - Test (buildPathData): given anchors with N>=3, the returned path string contains N cubic `C` commands (one per ring segment INCLUDING the wrap anchor[N-1]->anchor[0]), and ends by returning to anchor[0]'s coords. Use a small explicit fixture (e.g. 3 anchors) and assert the `C` command count === anchors.length, and that the last `C` targets anchorPx of anchor 0.
    - Test (buildPolyIndexMap closing range): for a closed ring with N cubics and M original px points, the ranges tile `0..M-1` with NO gap and NO overlap between consecutive non-closing ranges, and the wrap/closing segment's range covers the final index back to 0 (rangeStart = last interior endpoint, rangeEnd maps the wrap). Assert ranges.length === cubics.length and that consecutive ranges are contiguous (range[j].rangeEnd === range[j+1].rangeStart for interior j).
    - Test (BEZ-FIT-01 / calibration): existing fit-count band tests (3..40, IBERIA 4..30) stay green — DO NOT regress the fit error constant.
  </behavior>
  <action>
    DECISÃO DE MODELO DE ANEL (escolher e implementar UMA, documentar no SUMMARY): o ponto final de fit `cubics[N-1][3]` atualmente não vira âncora. Para um anel fechado correto, promova-o OU faça a última âncora dar wrap. **Recomendado:** manter anchor count == cubic count e tratar o segmento de fechamento como o segmento que vai da ÚLTIMA âncora (idx N-1) de volta à âncora 0, usando o último cubic (`cubics[N-1]`) como a geometria desse segmento de fechamento. Isso evita criar uma âncora órfã para o ponto final duplicado e mantém `deriveAnchors` estável.

    1. **buildPathData (line 93-102):** após o loop `i=0..N-2`, emita o cubic de FECHAMENTO `anchor[N-1] -> anchor[0]`. Concretamente, depois do loop, adicione:
       ```
       const lastA = anchors[anchors.length - 1]
       const firstA = anchors[0]
       d += ` C ${lastA.cp2Px[0]} ${lastA.cp2Px[1]} ${firstA.cp1Px[0]} ${firstA.cp1Px[1]} ${firstA.anchorPx[0]} ${firstA.anchorPx[1]}`
       ```
       Resultado: N comandos `C` (não N-1), terminando de volta em anchor 0.

    2. **deriveAnchors (line 109-132, line 120):** o cp1 (handle de entrada) da âncora 0 atualmente cai no fallback coincidente `[c1[0], c1[1]]` (= cp2, ver IN-01). Para um segmento de fechamento editável, o cp1 da âncora 0 deve vir do handle de SAÍDA do segmento de fechamento (o c2 do último cubic, próximo de p3 = âncora 0). Substitua o fallback `j > 0 ? prev.c2 : [c1...]` por: para j===0, use `cubics[cubics.length-1][2]` (c2 do último cubic) como cp1Px da âncora 0. Documente que isso conecta o handle de entrada da âncora 0 ao segmento de fechamento.

    3. **buildPolyIndexMap (bezierFit.ts line 73-107):** MANTENHA a coerção do último range para `k = inputPxPts.length - 1` (line 85-87). NÃO substitua por forward-scan. Antes, IN-02 dizia que esse índice forçado era inofensivo porque o segmento de fechamento era NÃO-editável; agora que ele É editável, esse índice forçado é NECESSÁRIO: `pxPts` inclui a duplicata de fechamento (`pxPts[M-1] === pxPts[0]`), `fitCurve` dropa a duplicata, então `cubics[N-1].p3 = P_{M-2}`. Um forward-scan acharia `P_{M-2}` em M-2, deixando o vértice de fechamento M-1 SEM range — e arrastar a âncora 0 moveria o índice 0 mas não o M-1, divergindo os dois e REABRINDO o anel (sintoma G1 original). Com k=M-1, o segmento de fechamento (via flatten `(s+1) % N` da Task 2) escreve `orderedKeys[q..M-1]`, fixando a duplicata M-1 na posição da âncora 0 e mantendo o anel fechado. AÇÃO: troque APENAS o comentário em line 85-87 para explicar que o índice forçado pertence ao segmento de fechamento editável e cobre a duplicata. Garanta `ranges.length === cubics.length` e que os ranges interiores são contíguos (`range[j].rangeEnd === range[j+1].rangeStart`).

    4. **addSeg guard (line 384-396):** relaxe `s < N - 1` para `s < N` (ou trate `s === N-1` como o segmento de fechamento válido). O segmento de fechamento agora é editável. AÇÃO ADICIONAL: o comentário inline em BezierEditLayer.tsx:387 (`// segment N-1 is the non-editable closing segment`) fica ATIVAMENTE FALSO depois desse relax — atualize/substitua-o para refletir que o segmento de fechamento AGORA é editável (ex.: `// all N segments editable incl. closing segment N-1 (G1)`). A frase obsoleta "non-editable closing segment" NÃO pode mais aparecer no arquivo.

    5. **flatten loop (line 403-424):** `endAnchor = working[s + 1]` é `undefined` quando `s === N-1` (segmento de fechamento). Troque por `working[(s + 1) % N]` para que o segmento de fechamento use a âncora 0 como endpoint. Confirme que o `cubic` montado para o segmento de fechamento usa `startAnchor.cp2Px` e `endAnchor(=anchor0).cp1Px` como handles.

    Mantenha tudo em px-space; geo só materializa no flatten. NÃO escreva no store em nenhuma parte do caminho de entrada/render (BEZ-IDENTITY-01).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/__tests__/bezierFit.test.ts src/components/canvas/__tests__/BezierEditLayer.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/lib/__tests__/bezierFit.test.ts src/components/canvas/__tests__/BezierEditLayer.test.tsx` exits 0
    - Grep: `grep -n "% N" frontend/src/components/canvas/BezierEditLayer.tsx` returns at least one hit (wrap-around endpoint in flatten loop)
    - Grep: `grep -c "C " ` on a buildPathData output fixture in the test asserts N cubic commands (N === anchors.length), not N-1 — encoded as an explicit numeric assertion in BezierEditLayer.test.tsx
    - Grep: `grep -n "s < N - 1" frontend/src/components/canvas/BezierEditLayer.tsx` returns NO hits (the closing-segment exclusion guard is gone)
    - Grep: `grep -n "non-editable closing segment" frontend/src/components/canvas/BezierEditLayer.tsx` returns NO hits (the stale comment is gone — closing segment is now editable)
    - A new test in bezierFit.test.ts asserts `ranges.length === cubics.length` and contiguity of interior ranges
    - DISCRIMINATING: a new test asserts `ranges[ranges.length - 1].rangeEnd === inputPxPts.length - 1` (closing segment owns the closing-duplicate vertex), AND a drag test asserts dragging anchor 0 updates BOTH orderedKeys[0] and orderedKeys[M-1] to the same coord (ring stays closed). This is the check that catches a forward-scan regression — the contiguity/length checks alone do NOT.
  </acceptance_criteria>
  <done>buildPathData draws N cubics ending back at anchor 0; closing segment is index-mapped and editable; fit-count calibration tests unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commit closing-segment drags + clamp refit (WR-02) + harden flatten (WR-03) + Playwright re-verify (G2)</name>
  <files>frontend/src/components/canvas/BezierEditLayer.tsx, frontend/src/lib/bezierFlatten.ts, frontend/src/components/canvas/__tests__/BezierEditLayer.drag.test.tsx, frontend/src/lib/__tests__/bezierFlatten.test.ts, frontend/tests/uat/playwright/08.1-bezier-edit.spec.ts</files>
  <read_first>
    - frontend/src/components/canvas/BezierEditLayer.tsx (handleDragMove line 336-345; handleDragEnd line 347-475 — dirty set line 384-396, changedIds early-return line 426-430, refit line 452-472; existing __forgeBezierTriggerDrag DEV hook line 521-542)
    - frontend/src/lib/bezierFlatten.ts (flattenSegment line 38-51 — the targetCount<2 path)
    - frontend/src/components/canvas/__tests__/BezierEditLayer.drag.test.tsx (BEZ-DRAG-01 dirty-range expectations)
    - frontend/src/lib/__tests__/bezierFlatten.test.ts (BEZ-FLATTEN-01/02 expectations)
    - frontend/tests/uat/playwright/08.1-bezier-edit.spec.ts (BEZ-UAT-01 flow + helpers to reuse: loadProjectInfo, discoverBaronyId, navigateToWorkspace, readBezierState, edit-tool-v click, __forgeSelectBarony)
    - 08.1-REVIEW.md (WR-01 — note it is SUPERSEDED here; WR-02 + WR-03 — apply)
  </read_first>
  <behavior>
    - Test (closing-segment commit): with the ring closed (Task 1), dragging anchor 0's cp1 OR the last anchor's cp2 now produces a non-empty `dirty` set (the closing segment N-1) → `changedIds.length > 0` → `setVerticesAndLog` is called with `op:'move'`. Assert the store mutated (changedIds non-empty) and op === 'move'. Previously these drags hit the empty-dirty early-return (WR-01) and silently dropped.
    - Test (WR-02 clamp): after a commit whose refit yields FEWER cubics than before, `activeAnchorIdx` (if it was set past the new length) is clamped via Math.min to a valid index (or cleared to null), so `displayAnchors[activeAnchorIdx]` never silently goes undefined. Use a fixture/mock where deriveAnchorsFromStore returns a shorter array; assert activeAnchorIdx <= refit.length - 1 (or null).
    - Test (WR-03 endpoints): `flattenSegment(cubic, 1, proj)` and `flattenSegment(cubic, 0, proj)` still emit BOTH p0 and p3 (length >= 2 when targetCount < 2 with the guard) rather than collapsing to a single p0 sample. Assert the first emitted geo === canvasToGeo(p0) and the last === canvasToGeo(p3).
    - Playwright (G1+G2 integration, BEZ-UAT closing-segment-handle-drag): in the running app, select a real barony via the existing hatch, read `editLogLength` baseline, trigger a cp1 drag on ANCHOR 0 (the closing-segment handle that was previously a silent-drop dead zone) via the new `__forgeBezierTriggerHandleDrag('cp1', 0, dx, dy)` hook, then poll until `editLogLength` grew. This passes ONLY if Task 2's `idx===0 cp1 → addSeg(N-1)` fix routes the closing segment into the dirty set and commits op:'move' — a true re-verification of this plan's own change against the live UI, not the unit harness.
  </behavior>
  <action>
    1. **Closing-segment commit:** with Task 1's addSeg relax + wrap-around endpoint, a cp1-on-anchor-0 drag adds segment N-1 to dirty (via `addSeg(idx-1)` where idx=0 → `addSeg(-1)` must now resolve to the closing segment) and a cp2-on-last-anchor drag adds segment N-1 (via `addSeg(idx)` where idx=N-1). Update the dirty-marking so anchor 0's INCOMING segment is the closing segment: in the `cp1` branch, when `idx === 0`, addSeg the closing segment index (N-1) instead of -1. Symmetrically the `anchor` branch for idx 0 must mark both the closing segment (N-1) and segment 0. Confirm the early-return at line 426-430 is now only reachable in genuinely degenerate cases (document that closing the ring removes the dead zone).

    2. **WR-02 — clamp activeAnchorIdx after refit:** at line 469 (`setAnchors(refit)`), immediately follow with:
       ```
       setActiveAnchorIdx((cur) => (cur === null ? null : Math.min(cur, refit.length - 1)))
       ```
       so the active anchor never points past the new array.

    3. **WR-03 — harden flattenSegment (bezierFlatten.ts line 38-51):** when `targetCount < 2`, still emit both endpoints. Change the loop guard so it always returns at least `[canvasToGeo(p0), canvasToGeo(p3)]` (length 2) rather than a single t=0 sample. Keep the even-spacing for targetCount >= 2 unchanged. Add the descriptive-named tests with explicit numeric fixtures per CLAUDE.md convention.

    4. **NEW DEV hook `__forgeBezierTriggerHandleDrag` (BezierEditLayer.tsx):** add a SECOND DEV hook alongside the existing `__forgeBezierTriggerDrag` (line 521-537), mirroring its pattern EXACTLY — register it inside the same DEV-gated `useEffect` (`if (!import.meta.env.DEV) return`) and add its `delete` to the existing unmount cleanup (line 538-541). Signature:
       ```
       __forgeBezierTriggerHandleDrag(kind: 'cp1' | 'cp2', idx: number, dx?: number, dy?: number): boolean
       ```
       Implementation rules (DO NOT deviate — integration fidelity is the whole point of the warning fix):
       - Read `const a = anchorsRef.current[idx]; if (!a) return false`. Read the handle px directly from `a.cp1Px` / `a.cp2Px` — these exist on every anchor even though handles only RENDER for the active anchor, so the hook works regardless of activeAnchorIdx.
       - **Center-anchored offset (CRITICAL):** control handles are Konva Circles (center-anchored), so `draggedCenterPx(kind, x, y)` returns `[x, y]` with NO +5. Therefore set `targetX = (kind === 'cp1' ? a.cp1Px[0] : a.cp2Px[0]) + dx` and `targetY = (kind === 'cp1' ? a.cp1Px[1] : a.cp2Px[1]) + dy`. Do NOT subtract 5 (that offset is anchor-Rect-specific and is wrong for handles).
       - Synthesize the same fake event shape used by `__forgeBezierTriggerDrag`: `{ target: { x: () => targetX, y: () => targetY, getStage: () => stage } }`.
       - Call the REAL handlers: `handleDragMove(kind, idx, evt)` then `handleDragEnd(kind, idx, evt)`. Return true. NEVER call `setVerticesAndLog`/`setAnchors` directly — the commit must flow through the same path as a real Konva handle gesture.
       - Add `handleDragMove`/`handleDragEnd` to the effect dep array if not already covered (they are already deps for the existing hook).

    5. **Playwright re-verify (extend 08.1-bezier-edit.spec.ts):** add a NEW `test(...)` inside the existing `test.describe('Phase 08.1 — ...')` block (or a new sibling describe). REUSE the existing helpers — do not duplicate them. Flow:
       - `const info = loadProjectInfo(); await navigateToWorkspace(page, info.project_id)`
       - `await expect(page.getByTestId('edit-tool-palette')).toBeVisible(); await page.getByTestId('edit-tool-v').click()`
       - `const baronyId = await discoverBaronyId(page, info.project_id)` then select via `__forgeSelectBarony`
       - Poll `readBezierState(page)` until `anchorCount >= 4` (ring fitted).
       - `const editLog0 = (await readBezierState(page))!.editLogLength`
       - Trigger the closing-segment handle drag through the new hook:
         ```
         const dragged = await page.evaluate(() => {
           const fn = (window as any).__forgeBezierTriggerHandleDrag
           return fn ? fn('cp1', 0, 25, 25) : false
         })
         expect(dragged, 'cp1 handle-drag DEV hook must be present and find anchor 0').toBe(true)
         ```
       - Assert the commit landed: `await expect.poll(async () => (await readBezierState(page))?.editLogLength ?? editLog0, { timeout: 8_000 }).toBeGreaterThan(editLog0)`.
       - Add a doc comment in the spec naming this as the G1+G2 closing-segment re-verification and the RESEARCH Pitfall 5 / CLAUDE.md UI-UAT mandate. Keep the existing ANTI-PATTERN GUARD note: no direct store-action calls; the commit flows through real handlers only.

    6. Keep the single commit chokepoint: exactly one `setVerticesAndLog(..., {op:'move'})`. Do NOT introduce any new op token. Do NOT hide/disable any handle (WR-01 superseded — handles stay draggable and functional).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/canvas/__tests__/BezierEditLayer.drag.test.tsx src/lib/__tests__/bezierFlatten.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/canvas/__tests__/BezierEditLayer.drag.test.tsx src/lib/__tests__/bezierFlatten.test.ts` exits 0
    - Playwright UAT green: `cd frontend && npx playwright test 08.1-bezier-edit.spec.ts` exits 0 (runs the existing BEZ-UAT-01 spec PLUS the new closing-segment-handle-drag test against the globalSetup-seeded Iberia 868 project)
    - Grep: `grep -n "Math.min(cur, refit.length" frontend/src/components/canvas/BezierEditLayer.tsx` returns a hit (WR-02 clamp)
    - Grep: `grep -c "setVerticesAndLog" frontend/src/components/canvas/BezierEditLayer.tsx` === 1 (single chokepoint preserved — the new DEV hook adds NO store-write site)
    - Grep: `grep -n "op: 'move'" frontend/src/components/canvas/BezierEditLayer.tsx` returns a hit; `grep -n "bezier_drag" frontend/src/components/canvas/BezierEditLayer.tsx` returns NO hit outside comments
    - Grep: `grep -n "__forgeBezierTriggerHandleDrag" frontend/src/components/canvas/BezierEditLayer.tsx` returns a hit, gated by `import.meta.env.DEV` and torn down in the unmount cleanup
    - Grep: `grep -n "__forgeBezierTriggerHandleDrag" frontend/tests/uat/playwright/08.1-bezier-edit.spec.ts` returns a hit (the spec drives the new hook)
    - DISCRIMINATING: the new handle hook uses the center-anchored Circle offset (NO `- 5`), unlike the existing anchor hook's `anchorPx[0] - 5 + dx`. Pin with an exact-string grep that ONLY the new hook contains: `grep -n "cp1Px\[0\] \+ dx" frontend/src/components/canvas/BezierEditLayer.tsx` returns a hit AND `grep -n "cp2Px\[0\] \+ dx" frontend/src/components/canvas/BezierEditLayer.tsx` returns a hit (neither line carries a `- 5`)
    - A new drag test asserts a cp1-on-anchor-0 drag produces changedIds.length > 0 (no silent drop)
    - A new flatten test asserts `flattenSegment(cubic, 1, proj).length >= 2` and endpoints are p0/p3
  </acceptance_criteria>
  <done>Closing-segment handle drags commit via op:'move'; refit clamps activeAnchorIdx; flatten always emits both endpoints; single chokepoint intact; new DEV handle-drag hook mirrors the existing pattern; Playwright spec re-verifies G1+G2 (cp1-on-anchor-0 → editLog grows) in the running app.</done>
</task>

<task type="auto">
  <name>Task 3: Distinct color palette for contour / anchors / handles / tethers (G4)</name>
  <files>frontend/src/components/canvas/BezierEditLayer.tsx, frontend/src/components/canvas/__tests__/BezierEditLayer.test.tsx</files>
  <read_first>
    - frontend/src/components/canvas/BezierEditLayer.tsx (color literals line 48-54; usage line 565, 576-580, 613-645)
    - .planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-UI-SPEC.md (Konva color table reference)
    - frontend/src/components/canvas/__tests__/BezierEditLayer.test.tsx (any test asserting exact hex values)
  </read_first>
  <action>
    O contorno `CURVE_STROKE` (#4a9eff) é IDÊNTICO às âncoras inativas `ANCHOR_INACTIVE_FILL` (#4a9eff); handles + tethers compartilham #94a3b8. Atribua uma paleta com 5 valores visualmente separáveis e atualize os literais hex em BezierEditLayer.tsx:49-54. Use exatamente:
      - `CURVE_STROKE`        = `#22c55e`  (contorno — verde)
      - `ANCHOR_INACTIVE_FILL`= `#4a9eff`  (âncora inativa — azul, mantida)
      - `ANCHOR_ACTIVE_FILL`  = `#f0c040`  (âncora ativa — âmbar, mantida)
      - `ANCHOR_HOVER_FILL`   = `#ffffff`  (hover — branco, mantida)
      - `HANDLE_FILL`         = `#e879f9`  (handle — magenta)
      - `TETHER_STROKE`       = `#94a3b8`  (tether — cinza, mantida; agora distinto do handle)
    Atualize a tabela de cores no comentário UI-SPEC do topo do arquivo (line 22-27) e em 08.1-UI-SPEC.md se houver uma tabela de cores Konva ali. Se algum teste asserir os hex exatos (ex.: CURVE_STROKE === '#4a9eff'), atualize esses literais de teste para os novos valores. NÃO altere geometria nem comportamento — apenas constantes de cor + comentário.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/canvas/__tests__/BezierEditLayer.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/canvas/__tests__/BezierEditLayer.test.tsx` exits 0
    - Grep: `grep -n "CURVE_STROKE = '#22c55e'" frontend/src/components/canvas/BezierEditLayer.tsx` returns a hit
    - Grep: `grep -n "HANDLE_FILL = '#e879f9'" frontend/src/components/canvas/BezierEditLayer.tsx` returns a hit
    - CURVE_STROKE and ANCHOR_INACTIVE_FILL are no longer the same value: `grep -c "'#4a9eff'" frontend/src/components/canvas/BezierEditLayer.tsx` shows the duplicate collision is resolved (CURVE_STROKE no longer #4a9eff)
    - No hex literal is shared between CURVE_STROKE, HANDLE_FILL, TETHER_STROKE, ANCHOR_INACTIVE_FILL, ANCHOR_ACTIVE_FILL
  </acceptance_criteria>
  <done>Five distinct hexes; contour no longer collides with inactive anchor; handle distinct from tether; UI-SPEC comment updated; tests green.</done>
</task>

</tasks>

<verification>
- BEZ-IDENTITY-01 stays green: `cd frontend && npx vitest run src/stores/__tests__/useEditorStore.bezierIdentity.test.ts` exits 0 (enter+exit closed-ring with zero drags → vertices byte-identical, editLog unchanged). This is the NON-NEGOTIABLE invariant — it MUST pass unchanged after closing the ring.
- Playwright UAT (G1+G2 integration) green: `cd frontend && npx playwright test 08.1-bezier-edit.spec.ts` exits 0 — the new closing-segment cp1-on-anchor-0 drag commits op:'move' end-to-end (editLog grows) against the seeded Iberia 868 project. globalSetup provides UAT_PROJECT_INFO_PATH; webServer boots backend+Vite per playwright.config.ts.
- Full Phase 08.1 frontend suite green: `cd frontend && npx vitest run` exits 0.
- Zero backend files touched: `git diff --name-only HEAD -- backend/` is empty.
- tsc clean: `cd frontend && npx tsc --noEmit` exits 0.
</verification>

<success_criteria>
- G1 closed: outline forms a closed loop (N cubic commands); closing segment index-mapped.
- G2 closed: closing-segment handle drags commit via op:'move'; WR-02 clamp + WR-03 endpoints in place; no silent drop.
- G1+G2 re-verified in the running app: Playwright cp1-on-anchor-0 drag grows editLog via the real handler path (not the unit harness).
- G4 closed: 5 distinct hexes; contour/anchor collision resolved.
- BEZ-IDENTITY-01 green; single setVerticesAndLog chokepoint intact; zero backend changes.
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-05-SUMMARY.md`. Document the ring-model decision taken in Task 1 (promote final point vs last-anchor wrap), the RESEARCH §Pattern 1 / WR-01 reversal, and the new `__forgeBezierTriggerHandleDrag` DEV hook + Playwright re-verification of G1+G2.
</output>
