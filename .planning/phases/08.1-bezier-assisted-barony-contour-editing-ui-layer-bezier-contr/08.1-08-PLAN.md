---
phase: 08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr
plan: 08
type: execute
wave: 2
depends_on: [07]
files_modified:
  - frontend/src/components/canvas/BezierEditLayer.tsx
  - frontend/src/components/canvas/__tests__/BezierEditLayer.test.tsx
  - frontend/tests/uat/playwright/08.1-bezier-render-feedback.spec.ts
autonomous: true
gap_closure: true
requirements: [BEZ-DRAG-01, BEZ-RENDER-01, BEZ-UAT-01, BEZ-IDENTITY-01]

user_setup: []

must_haves:
  truths:
    - "REPRODUCTION FIRST: at usable zoom (post-07), a real drag commits op:'move' (editLogLength grows) AND the colored BaronyLayer geometry is unchanged — this discriminator proves G6 is real and INDEPENDENT of G5 (grab works, but the colored map does not reflect the edit)"
    - "There is NO Phase 08 precedent for op:'move' surfacing on the colored map: only split/merge/translate POST /editor/apply, and those converge only via /render — op:'move' never POSTs. So G6 needs a fresh contract, not a phantom pattern"
    - "DECISION (locked, option a): the active barony's edited contour renders LIVE from useEditorStore.vertices as a distinct overlay (an unsaved-edit outline), so the shape change is immediately visible on the map; the colored backend BaronyLayer is intentionally left for /render to converge later"
    - "The live overlay only READS store.vertices (and the component-local anchors already derived from it) — it adds ZERO store writes, so BEZ-IDENTITY-01 holds by construction"
    - "After a drag commit, the live overlay updates because the refit reads the updated store.vertices (the existing handleDragEnd refit path); the overlay tracks the committed geometry, visually distinct from the colored raster baronies"
    - "A real-mouse Playwright UAT proves: at usable zoom, a real drag changes the live edited-contour overlay on the map (not just the green Bézier handles) — driven by real events, NOT __forgeBezierTrigger*"
  artifacts:
    - path: "frontend/src/components/canvas/BezierEditLayer.tsx"
      provides: "Live edited-contour overlay: a Konva closed Line rendering the flattened polygon vertices of the active barony from store.vertices, visually distinct (dashed amber), read-only — zero store writes"
      contains: "bezier-edited-contour"
    - path: "frontend/tests/uat/playwright/08.1-bezier-render-feedback.spec.ts"
      provides: "REPRODUCTION discriminator (drag commits + colored barony stale) + live-overlay-reflects-edit assertion via real page.mouse drag at zoom"
  key_links:
    - from: "BezierEditLayer live edited-contour overlay Konva Line"
      to: "useEditorStore.vertices (active barony ring)"
      via: "read-only: build flat [x,y,...] from store vertices sorted by #N → geoToCanvas; closed Line, listening=false"
      pattern: "edited-contour|editedRingPts|geoToCanvas"
    - from: "08.1-bezier-render-feedback.spec.ts real drag"
      to: "live overlay geometry change on the map"
      via: "page.mouse.down/move/up at usable zoom; assert editedContourPointCount present AND editLogLength grew"
      pattern: "page.mouse|edited"
---

<objective>
Fechar G6 (edições não mudam visivelmente o mapa; caminho de persistência não verificado de forma independente). A baronia colorida visível vem de `effectiveBaronies = baroniesQ.data` (CanvasViewer.tsx:365) — um GeoJSON renderizado pelo BACKEND — NÃO de `useEditorStore.vertices`. `BezierEditLayer.handleDragEnd` commita em `store.vertices` e refaz só o seu próprio overlay verde de curva; o `BaronyLayer` que o usuário lê como "o mapa" nunca é recomputado a partir de `vertices`, então mesmo um drag bem-sucedido não muda o polígono colorido.

Purpose: dar feedback visual imediato no mapa quando o designer edita um contorno Bézier, sem violar a fronteira de fase (backend/DAG/`/editor/apply`/export NUNCA veem Bézier).
Output: (1) uma tarefa de REPRODUÇÃO com mouse real que separa "não consegui agarrar" (G5, já fechado em 07) de "agarrei mas o mapa não muda" (G6); (2) um overlay VIVO do contorno editado renderizado a partir de `store.vertices`; (3) uma UAT Playwright com eventos reais provando que o overlay reflete a edição.

**DEPENDÊNCIA (depends_on: [07]):** a reprodução com mouse real SÓ é possível depois que 07 tornou os alvos utilizáveis (screen-space sizing + MAX_SCALE_MULTIPLIER=16). Sem 07, um drag real falharia por causa do G5 e não conseguiríamos isolar o G6. Por isso este plano é Wave 2.

**SEM PRECEDENTE NO PHASE 08 (registrar — não perseguir um padrão fantasma):** a orientação do gap diz "case com a forma como as edições raw-vertex do Phase 08 aparecem". Investigação do código prova que NÃO HÁ tal precedente para `op:'move'`: `setVerticesAndLog`/`moveVertex` apenas escrevem no store; APENAS `splitPolygon`/`mergePolygons`/`translatePolygon` fazem `POST /editor/apply`, e essas só convergem no mapa via `/render` (BLOCKER-1 / D-17). Um `op:'move'` de vértice nunca faz POST e nunca aparece na baronia colorida — nem no Phase 08. Portanto G6 precisa de um contrato NOVO. Escrever esse fato no SUMMARY.

**DECISÃO DE CONTRATO (LOCKED — opção a, overlay vivo):** renderizar o contorno editado da baronia ativa AO VIVO a partir de `useEditorStore.vertices` como um overlay DISTINTO (um contorno de "edição não salva"), para que a mudança de forma seja imediatamente visível no mapa. NÃO escolher a opção (b) Apply→`/render`: ela contradiz a fronteira de fase travada na CONTEXT.md ("Backend, DAG, /editor/apply… NONE of them ever see Bézier") e é pesada. Se algum dia a opção (a) se mostrar insuficiente para "o usuário vê a mudança", isso é uma fase NOVA, não este gap. O overlay APENAS LÊ `store.vertices` (e os anchors component-local já derivados dela) — zero writes no store, então BEZ-IDENTITY-01 é seguro por construção.

**NUANCE (escrever no `<action>`):** a curva Bézier VERDE já segue a edição (previewAnchors durante o drag, refit depois do commit). O que fica obsoleto é a baronia COLORIDA — que o usuário lê como "o mapa". O overlay novo deve mostrar a mudança de FORMA numa representação que o usuário leia como o mapa (um contorno fechado do polígono editado), visualmente distinto (é uma edição não salva, não um raster re-renderizado).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-CONTEXT.md
@.planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-HUMAN-UAT.md

<interfaces>
<!-- Extracted from the codebase. Use directly — no exploration needed. -->

From frontend/src/components/canvas/CanvasViewer.tsx — why the colored map is stale:
```tsx
// effectiveBaronies is the BACKEND GeoJSON (TanStack query), NOT store.vertices:
const effectiveBaronies = baroniesQ.data ?? lastGoodBaroniesRef.current   // line 365
<BaronyLayer baronies={effectiveBaronies} baronyColors={effectiveBaronyColors} visible={...} />  // line 742
// BezierEditLayer commits to store.vertices via setVerticesAndLog; BaronyLayer never reads store.vertices.
```

From frontend/src/components/canvas/BezierEditLayer.tsx — store-vertices read + refit already present:
```typescript
const vertices = useEditorStore((s) => s.vertices)   // line 225 — subscribed
// sortRingKeys(keys) sorts 'b1#0'..'b1#10' by numeric #N suffix (line 84)
// keyBarony(key) extracts baronyId from 'baronyId#n' (line 93)
// After commit, handleDragEnd refits originalRef + setAnchors from useEditorStore.getState().vertices (lines 545-561)
// geoToCanvas(lon, lat, projection) → [x, y] canvas px (imported, line 43)
// currentScale prop is threaded by plan 07 — reuse it for the screen-space overlay stroke.
```

From frontend/src/components/canvas/BaronyLayer.tsx — how a barony polygon is drawn (the pattern to mirror for the overlay):
```tsx
<Line points={b.points} closed fill={...} stroke={...} strokeWidth={...} listening={visible} />
// b.points is a flat [x0,y0,x1,y1,...] number[] in canvas px.
```

From frontend/src/lib/projection.ts:
```typescript
export function geoToCanvas(lon: number, lat: number, projection: ProjectionConfig): [number, number]
```

From the existing 08.1-bezier-edit.spec.ts: loadProjectInfo, discoverBaronyId, navigateToWorkspace, readBezierState (BezierState has anchorCount, activeAnchorIdx, dirtySegmentCount, editLogLength, firstAnchor) — reuse the helper shapes. The __forgeBezierState DEV hook returns that object (BezierEditLayer.tsx lines 578-614).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REPRODUCTION — real-mouse drag at usable zoom, prove G6 is real and independent of G5</name>
  <files>frontend/tests/uat/playwright/08.1-bezier-render-feedback.spec.ts</files>
  <read_first>
    - frontend/tests/uat/playwright/08.1-bezier-usable-targets.spec.ts (from plan 07 — the real-event drag + zoom pattern to reuse; copy loadProjectInfo/discoverBaronyId/navigateToWorkspace/readBezierState helpers)
    - frontend/src/components/canvas/CanvasViewer.tsx (line 365 effectiveBaronies; 490-498 __forgeStageScale; 742 BaronyLayer mount)
    - frontend/src/components/canvas/BaronyLayer.tsx (the colored Line the user reads as "the map")
  </read_first>
  <action>
    Create `frontend/tests/uat/playwright/08.1-bezier-render-feedback.spec.ts` with a FIRST describe block named "Phase 08.1 — G6 reproduction (drag commits, colored map stale)". This task's job is to AUTOMATE the reproduction the gap demanded, so the fix decision is grounded in evidence, not assumption. Because the plan is autonomous, this runs as an assertion (no human checkpoint).

    Setup (reuse plan-07 helpers — copy them into this file, do not import across specs): navigate to seeded Iberia 868, click `edit-tool-v`, `__forgeSelectBarony(discoveredId)`, poll `readBezierState().anchorCount >= 4`. Zoom in with REAL wheel events to a usable scale exactly as plan-07's spec does (`page.mouse.move(cx, cy)` + `page.mouse.wheel(0, -100)` loop until `__forgeStageScale` >= ~6× initial).

    The DISCRIMINATOR test "real drag commits op:move BUT the colored barony geometry is unchanged (G6 confirmed, distinct from G5)":
    - Capture the colored barony's current ring BEFORE the drag. Simplest stable source: fetch the backend GeoJSON the colored layer renders — `await page.evaluate(pid => fetch('/api/v3/projects/'+pid+'/artifacts/baronies.geojson').then(r=>r.json()), projectId)` — and snapshot the active barony feature's coordinate ring (JSON.stringify).
    - Capture `editLog0 = readBezierState().editLogLength`.
    - Drive a REAL drag on `firstAnchor` (read post-zoom from readBezierState): `page.mouse.move(x,y); page.mouse.down(); page.mouse.move(x+30, y+30, {steps: 8}); page.mouse.up()`.
    - Poll `readBezierState().editLogLength > editLog0` (timeout 8000) → GRAB+COMMIT works (G5 is fixed; this is NOT a grab failure).
    - Re-fetch the backend baronies.geojson and assert the active barony's coordinate ring is BYTE-IDENTICAL to the pre-drag snapshot → the colored map did NOT change. This is the G6 confirmation: the edit commits to the store but the colored representation the user reads is stale.
    - Add a comment block recording the conclusion: "editLog grew (commit OK, G5 fixed) + colored ring unchanged → G6 is real and independent. Fix = Task 2 live overlay from store.vertices (option a). No /editor/apply, no /render — phase boundary forbids backend seeing Bézier."

    ANTI-PATTERN GUARD header: "MUST NOT call __forgeBezierTrigger* for the claim; drag flows through real page.mouse only." This reproduction test is the evidence that justifies Task 2's contract — it stays in the file as a permanent regression-of-understanding guard.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test tests/uat/playwright/08.1-bezier-render-feedback.spec.ts -g "reproduction"</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/tests/uat/playwright/08.1-bezier-render-feedback.spec.ts` exists with a reproduction describe block
    - The reproduction test passes: editLogLength grows after a REAL drag AND the backend-fetched colored barony ring is unchanged
    - `rg -n "__forgeBezierTrigger" frontend/tests/uat/playwright/08.1-bezier-render-feedback.spec.ts` returns ZERO hits
    - `rg -n "page.mouse.down|page.mouse.up|page.mouse.wheel" frontend/tests/uat/playwright/08.1-bezier-render-feedback.spec.ts` returns hits
  </acceptance_criteria>
  <done>An automated real-mouse reproduction proves G6: a usable-zoom drag commits (editLog grows) yet the colored barony stays stale — confirming the fix must surface store.vertices on the map (option a), not re-grab (G5).</done>
</task>

<task type="auto">
  <name>Task 2: Live edited-contour overlay from store.vertices (option a) — read-only, zero store writes</name>
  <files>frontend/src/components/canvas/BezierEditLayer.tsx</files>
  <read_first>
    - frontend/src/components/canvas/BezierEditLayer.tsx (FULL — line 225 vertices subscription; 84-94 sortRingKeys/keyBarony; 221-235 isActive gate; 290-308 ring filter on entry; 545-561 post-commit refit; 578-614 __forgeBezierState; 701-816 render/Layer)
    - frontend/src/components/canvas/BaronyLayer.tsx (the closed Line pattern to mirror)
    - frontend/src/components/canvas/CanvasViewer.tsx (currentScale prop threaded by plan 07 — reuse for the overlay stroke width)
  </read_first>
  <action>
    Add a LIVE edited-contour overlay so a Bézier edit is immediately visible on the map (option a). The overlay renders the active barony's CURRENT polygon ring straight from `useEditorStore.vertices` — the same source of truth the commit writes — so it always reflects the latest committed geometry, while the colored backend BaronyLayer stays as-is until a future `/render` (out of scope).

    1) In BezierEditLayer, derive the edited ring points in canvas px from the SUBSCRIBED `vertices` (already read at line 225), filtered to the active barony and sorted by #N (reuse `sortRingKeys` + `keyBarony`). Build a flat `number[]` `[x0,y0,x1,y1,...]` via `geoToCanvas`:
    ```typescript
    const editedRingPts = useMemo<number[]>(() => {
      if (!isActive || activeTerritoryId === null) return []
      const ringKeys = sortRingKeys(
        Object.keys(vertices).filter((k) => keyBarony(k) === activeTerritoryId),
      )
      const flat: number[] = []
      for (const k of ringKeys) {
        const v = vertices[k]
        const [x, y] = geoToCanvas(v.lon, v.lat, projection)
        flat.push(x, y)
      }
      return flat
    }, [vertices, isActive, activeTerritoryId, projection])
    ```
    This recomputes whenever `vertices` changes — i.e. immediately after `setVerticesAndLog` runs in handleDragEnd (the store subscription triggers a re-render). No new effect, no new state, NO store write.

    2) Render it as the FIRST child inside the existing `<Layer>` (below the curve outline so anchors/handles stay on top), a CLOSED Konva Line, visually distinct from both the colored raster baronies and the green Bézier curve — it is an UNSAVED edit. Reuse `currentScale` (threaded by plan 07) for the screen-space stroke:
    ```tsx
    {editedRingPts.length >= 6 && (
      <Line
        points={editedRingPts}
        closed
        stroke="#f0c040"            // amber — "unsaved edit" accent, distinct from green curve outline
        strokeWidth={2.5 / (currentScale > 0 ? currentScale : 1)}   // screen-space (plan 07 pattern)
        dash={[6, 4]}               // dashed = unsaved / live-edit affordance
        fill="rgba(240, 192, 64, 0.12)"   // faint amber wash so the shape change reads as a filled region
        listening={false}
        data-testid="bezier-edited-contour"
      />
    )}
    ```
    Do NOT re-add the `currentScale` prop — plan 07 already added it to BezierEditLayerProps and the component signature.

    3) Add an additive read-only field to the existing `__forgeBezierState` DEV hook return object (lines 607-613) so Task 3's Playwright can inspect the overlay without DOM access: add `editedContourPointCount: editedRingPts.length`. This is read-only state inspection — NOT a trigger hook. The field is OPTIONAL/additive, so 08.1-bezier-edit.spec.ts and 08.1-bezier-usable-targets.spec.ts (which omit it from their BezierState interface) keep compiling.

    4) Do NOT modify handleDragEnd's commit logic, the colored BaronyLayer, effectiveBaronies, or any backend call. The overlay is purely a read-derived render. Confirm no `setVerticesAndLog` is added. Surgical: the only new code is the `editedRingPts` memo, the one `<Line>`, and the one additive DEV-hook field.

    Rationale to embed as a code comment near the overlay: "G6 fix (option a, locked): the colored BaronyLayer is backend GeoJSON and only converges via /render; the phase boundary forbids backend seeing Bézier, so we surface the live edited contour from store.vertices as an unsaved-edit overlay. Read-only → BEZ-IDENTITY-01 safe."
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/canvas/__tests__/BezierEditLayer.test.tsx src/stores/__tests__/useEditorStore.bezierIdentity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "bezier-edited-contour" frontend/src/components/canvas/BezierEditLayer.tsx` returns one hit (the overlay Line)
    - `rg -n "editedRingPts" frontend/src/components/canvas/BezierEditLayer.tsx` shows the memo derived from store vertices
    - `rg -n "editedContourPointCount" frontend/src/components/canvas/BezierEditLayer.tsx` returns one hit (additive read-only DEV-hook field)
    - The COUNT of `setVerticesAndLog(` (non-comment) call sites in BezierEditLayer.tsx is UNCHANGED vs pre-task (single real commit at handleDragEnd; the overlay adds none)
    - useEditorStore.bezierIdentity.test.ts passes (overlay is read-only — entry/exit adds no store write)
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>A live amber dashed edited-contour overlay renders from store.vertices on the map; it tracks committed Bézier edits immediately; read-only so identity holds; tsc + unit tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Unit test the live overlay reflects store.vertices + Playwright proves it changes on a real drag</name>
  <files>frontend/src/components/canvas/__tests__/BezierEditLayer.test.tsx, frontend/tests/uat/playwright/08.1-bezier-render-feedback.spec.ts</files>
  <read_first>
    - frontend/src/components/canvas/__tests__/BezierEditLayer.test.tsx (existing render tests + the screen-space test added by plan 07; match its mock/query style)
    - frontend/tests/uat/playwright/08.1-bezier-render-feedback.spec.ts (Task 1's reproduction block — add the fix-verification block here)
  </read_first>
  <action>
    Two layers of proof for the overlay.

    1) UNIT (BezierEditLayer.test.tsx): add a descriptive test `it('renders a live edited-contour overlay (bezier-edited-contour) whose points derive from store.vertices', ...)`. Load a known barony ring into the store via `useEditorStore.setState({ vertices: { 'b1#0': {lat: 40.0, lon: -8.0}, 'b1#1': {lat: 40.5, lon: -8.0}, 'b1#2': {lat: 40.5, lon: -7.5}, 'b1#3': {lat: 40.0, lon: -7.5} } })` with `activeTerritoryId='b1'`, V tool, editableLayer='baronies' (mirror the existing render-test setup in this file). Render BezierEditLayer with `currentScale={1}` and the test projection already used in this file. Assert a node with `data-testid="bezier-edited-contour"` exists and its `points` array has length `2 × vertexCount = 8` (explicit numeric fixture: 4 vertices → 8 numbers). Use the SAME query mechanism the file already uses to inspect rendered Konva nodes — do not invent one.

    2) PLAYWRIGHT (08.1-bezier-render-feedback.spec.ts, NEW describe block "G6 fix — live overlay reflects the edit"): reuse the same setup + real zoom as Task 1. Extend the BezierState interface in THIS spec with `editedContourPointCount?: number` (the additive field Task 2 added to __forgeBezierState). After zooming:
    - Capture `editLog0 = readBezierState().editLogLength` and `before = readBezierState().editedContourPointCount`.
    - Drive a REAL drag on firstAnchor (page.mouse.down/move/up at zoom).
    - Poll until `editLogLength` grows (commit happened) AND assert `readBezierState().editedContourPointCount` is present and `> 0` — proving the live overlay is mounted and tracking the edited store geometry on the map (not just the green handles).
    - Header ANTI-PATTERN GUARD: "the DRAG is a real page.mouse event; __forgeBezierState is read-only state inspection, never a trigger. No __forgeBezierTrigger* drives the action."

    The `editedContourPointCount` field is OPTIONAL on the BezierState interface so it stays additive and never breaks the sibling specs that omit it.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/canvas/__tests__/BezierEditLayer.test.tsx && npx playwright test tests/uat/playwright/08.1-bezier-render-feedback.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The unit test asserting `bezier-edited-contour` points length === 8 (4 vertices) is present and passes
    - The Playwright fix-verification test passes: after a REAL drag at usable zoom, editLogLength grew AND editedContourPointCount > 0 (overlay tracks the edited geometry)
    - `rg -n "__forgeBezierTrigger" frontend/tests/uat/playwright/08.1-bezier-render-feedback.spec.ts` returns ZERO hits (action driven by real events; only read-only __forgeBezierState used for inspection)
    - `rg -n "setVerticesAndLog|moveVertex" frontend/tests/uat/playwright/08.1-bezier-render-feedback.spec.ts` returns ZERO hits
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Unit test pins the overlay-from-store math (8 points for 4 vertices); Playwright proves a real drag at usable zoom updates the live edited-contour overlay on the map; the action is real-event-driven, identity-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| useEditorStore.vertices → BezierEditLayer overlay render | Read-only derivation; the overlay never writes back |
| BezierEditLayer → backend | Unchanged — the overlay makes NO /editor/apply or /render call; the phase boundary (backend never sees Bézier) is preserved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-08-01 | Tampering | overlay accidentally writes store on render | mitigate | Overlay is a pure useMemo + read-only Konva Line; acceptance asserts setVerticesAndLog call-site count unchanged; bezierIdentity test stays green |
| T-08.1-08-02 | Information disclosure | user mistakes the unsaved-edit overlay for a saved/rendered raster | accept | Visually distinct (amber dashed + faint wash) signals "unsaved live edit"; the colored raster converging via /render is a separate, out-of-scope concern |
| T-08.1-08-03 | Denial of Service | malformed ring (<3 verts) → overlay Line throws | mitigate | `editedRingPts.length >= 6` guard (≥3 points) before rendering the Line; mirrors the isActive vertexCount>=3 gate |
</threat_model>

<verification>
- `cd frontend && npx vitest run` — full suite green (overlay memo + unit test add no regression)
- `cd frontend && npx vitest run src/stores/__tests__/useEditorStore.bezierIdentity.test.ts` — identity contract green (overlay is read-only)
- `cd frontend && npx playwright test tests/uat/playwright/08.1-bezier-render-feedback.spec.ts` — reproduction + fix-verification tests pass
- `cd frontend && npx tsc --noEmit` — clean
- Grep gates: `bezier-edited-contour` present in BezierEditLayer.tsx; zero `__forgeBezierTrigger` in the new spec; setVerticesAndLog call-site count unchanged in BezierEditLayer.tsx
</verification>

<success_criteria>
- Reproduction proves G6 is real and independent of G5: a real drag commits (editLog grows) while the colored backend barony stays stale
- The fix records the no-precedent fact (op:'move' never POSTs; only split/merge/translate → /render) and chooses option (a) live overlay, consistent with the locked phase boundary
- A live amber-dashed edited-contour overlay renders the active barony from store.vertices and updates immediately on commit, visible on the map
- The overlay is read-only — BEZ-IDENTITY-01 stays green; no /editor/apply, no /render, backend never sees Bézier
- A real-mouse Playwright UAT proves the overlay reflects the edit at usable zoom, driven by real events (no __forgeBezierTrigger*)
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-bezier-assisted-barony-contour-editing-ui-layer-bezier-contr/08.1-08-SUMMARY.md`
</output>
