---
phase: "04"
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/unit/test_cleanup_split.py
  - backend/tests/unit/test_dag_tokens.py
  - backend/tests/unit/test_stage_cache.py
  - backend/tests/integration/test_render_endpoint.py
  - backend/tests/integration/test_render_cancel.py
  - backend/tests/parity/test_iberia_868_render_default.py
  - frontend/src/components/canvas/__tests__/ParameterSidebar.test.tsx
  - frontend/src/components/canvas/__tests__/SliderCard.test.tsx
  - frontend/src/components/canvas/__tests__/StageViewToggle.test.tsx
  - frontend/src/components/canvas/__tests__/CanvasViewer.clearCache.test.tsx
  - frontend/src/components/canvas/__tests__/BaronyLabels.test.tsx
  - frontend/src/api/__tests__/useRenderStream.test.ts
  - frontend/src/stores/__tests__/usePipelineParams.test.ts
  - frontend/src/components/workspace/__tests__/WorkspaceToolbar.cancel.test.tsx
  - frontend/tests/e2e/parameter-studio-sc3.spec.ts
  - frontend/tests/e2e/parameter-studio-cancel.spec.ts
autonomous: true
requirements: ["SC-1", "SC-2", "SC-3", "SC-4"]
requirements_addressed:
  - "VALIDATION.md Wave 0 list (14 stub files)"
  - "Anti-symmetry samples 1-12 mapped to skeleton tests"

must_haves:
  truths:
    - "All 14 Wave 0 stub files exist on disk before any implementation plan starts"
    - "Each stub uses pytest.skip / it.skip / test.skip so the suite stays green"
    - "Stub names match the per-task verify map in 04-VALIDATION.md exactly"
  artifacts:
    - path: "backend/tests/unit/test_cleanup_split.py"
      provides: "stub for D-01 split-function unit tests"
    - path: "backend/tests/unit/test_dag_tokens.py"
      provides: "stub for D-02 token determinism + isolation tests"
    - path: "backend/tests/unit/test_stage_cache.py"
      provides: "stub for D-03 cache lifecycle tests"
    - path: "backend/tests/integration/test_render_endpoint.py"
      provides: "stub for D-04 endpoint + 409 single-flight tests"
    - path: "backend/tests/integration/test_render_cancel.py"
      provides: "stub for D-13/D-14 cancel mechanics tests"
    - path: "backend/tests/parity/test_iberia_868_render_default.py"
      provides: "stub for D-17 incremental-vs-full byte-equal parity"
    - path: "frontend/src/components/canvas/__tests__/ParameterSidebar.test.tsx"
      provides: "stub for D-05/D-06 sidebar layout"
    - path: "frontend/src/components/canvas/__tests__/SliderCard.test.tsx"
      provides: "stub for D-08 slider+input+reset"
    - path: "frontend/src/components/canvas/__tests__/StageViewToggle.test.tsx"
      provides: "stub for D-10/D-11 radio group"
    - path: "frontend/src/components/canvas/__tests__/CanvasViewer.clearCache.test.tsx"
      provides: "stub for clearCache discipline"
    - path: "frontend/src/components/canvas/__tests__/BaronyLabels.test.tsx"
      provides: "stub for D-12 barony name labels"
    - path: "frontend/src/api/__tests__/useRenderStream.test.ts"
      provides: "stub for SSE consumer + stage_cancel handling"
    - path: "frontend/src/stores/__tests__/usePipelineParams.test.ts"
      provides: "stub for slider state + debounce + latest-wins"
    - path: "frontend/src/components/workspace/__tests__/WorkspaceToolbar.cancel.test.tsx"
      provides: "stub for status-badge → cancel-button switch"
    - path: "frontend/tests/e2e/parameter-studio-sc3.spec.ts"
      provides: "Playwright stub for σ 3.0→4.5 timing"
    - path: "frontend/tests/e2e/parameter-studio-cancel.spec.ts"
      provides: "Playwright stub for cancel UI flow"
  key_links:
    - from: "Plan 04-01..04-06 <verify> blocks"
      to: "this plan's stub files"
      via: "pytest path / vitest path / playwright path string match"
      pattern: "tests/unit/test_cleanup_split|tests/unit/test_dag_tokens|tests/unit/test_stage_cache|tests/integration/test_render_endpoint|tests/integration/test_render_cancel|tests/parity/test_iberia_868_render_default|ParameterSidebar.test|SliderCard.test|StageViewToggle.test|CanvasViewer.clearCache.test|BaronyLabels.test|useRenderStream.test|usePipelineParams.test|WorkspaceToolbar.cancel.test|parameter-studio-sc3.spec|parameter-studio-cancel.spec"
---

<objective>
Wave 0 stub bootstrap. Create all 14 test files referenced by 04-VALIDATION.md so that
implementation plans (04-01..04-06) can cite real `<verify>` commands instead of
`MISSING — Wave 0 must create...`. Stubs use pytest.skip / it.skip / test.skip so the
existing test suites stay green; implementation tasks remove the skip and fill the body.

Purpose: Continuous-sampling discipline. Per VALIDATION.md "Sampling continuity: no 3
consecutive tasks without automated verify".
Output: 14 committed stub files, full backend + frontend suite still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-parameter-studio-live-re-render/04-VALIDATION.md
@.planning/phases/04-parameter-studio-live-re-render/04-CONTEXT.md
@backend/tests/unit/test_v3_generate.py
@frontend/src/api/useGenerateStream.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create 6 backend stub test files</name>
  <files>
    backend/tests/unit/test_cleanup_split.py,
    backend/tests/unit/test_dag_tokens.py,
    backend/tests/unit/test_stage_cache.py,
    backend/tests/integration/test_render_endpoint.py,
    backend/tests/integration/test_render_cancel.py,
    backend/tests/parity/test_iberia_868_render_default.py
  </files>
  <read_first>
    .planning/phases/04-parameter-studio-live-re-render/04-VALIDATION.md
    backend/tests/unit/test_v3_generate.py
    backend/tests/parity/test_iberia_868.py
    backend/tests/integration/test_app_boot.py
  </read_first>
  <action>
Create 6 pytest stub files. Each file:

1. Imports `pytest` only (no production imports — modules don't exist yet).
2. Uses module-level `pytestmark` for the correct marker:
   - `test_cleanup_split.py` → `pytestmark = pytest.mark.unit`
   - `test_dag_tokens.py` → `pytestmark = pytest.mark.unit`
   - `test_stage_cache.py` → `pytestmark = pytest.mark.unit`
   - `test_render_endpoint.py` → `pytestmark = pytest.mark.integration`
   - `test_render_cancel.py` → `pytestmark = pytest.mark.integration`
   - `test_iberia_868_render_default.py` → `pytestmark = pytest.mark.parity`
3. One function per anti-symmetry sample from VALIDATION.md §Nyquist Anti-Symmetry Samples.
4. Each function body = `pytest.skip("Wave 0 stub — Plan 04-NN implements")`.

Test names (verbatim from VALIDATION.md §Nyquist samples + §Per-Task Verification Map):

`test_cleanup_split.py`:
- `test_apply_median_does_not_mutate_input` (sample #2)
- `test_apply_median_default_cfg_matches_monolith` (D-17 default-cfg parity)
- `test_remove_fragments_does_not_mutate_input`
- `test_smooth_per_territory_returns_new_array`
- `test_merge_small_blobs_does_not_mutate_input`
- `test_split_chain_default_cfg_matches_cleanup_and_smooth` (D-01 contract: monolith == split chain at default cfg)

`test_dag_tokens.py`:
- `test_token_stable_across_runs` (sample #8)
- `test_sigma_change_does_not_invalidate_median` (sample #1, D-02)
- `test_median_passes_change_invalidates_median` (D-02 reads scoping)
- `test_upstream_token_change_cascades` (D-02 transitive invalidation)

`test_stage_cache.py`:
- `test_cache_put_then_get_returns_entry`
- `test_second_put_promotes_prior` (D-03 latest+prior)
- `test_clear_project_removes_all_entries` (D-03 lifetime)
- `test_concurrent_put_get_thread_safe` (RLock — research Pitfall 5)

`test_render_endpoint.py`:
- `test_render_returns_202_with_run_id` (D-04 happy path)
- `test_render_409_when_render_alive` (D-04 single-flight, sample #4)
- `test_render_409_when_generate_alive` (D-04 cross-router single-flight)
- `test_generate_clears_cache` (sample #3)
- `test_render_does_not_mutate_persisted_cfg` (sample #12, D-18)
- `test_stage_view_change_does_not_recompute` (sample #10, D-09)
- `test_render_clamps_sigma_to_3_0_4_5_bounds` (D-05 + CLAUDE.md rule #2 + ASVS V5)
- `test_render_rejects_unknown_cfg_field` (ASVS V5)

`test_render_cancel.py`:
- `test_cancel_during_median_returns_within_2s` (sample #5, D-14)
- `test_cancel_emits_stage_cancel_per_affected_stage` (D-13)
- `test_cancel_on_first_render_falls_back_to_generate_baseline` (sample #6)
- `test_cancel_does_not_update_cache` (D-13 atomicity invariant)

`test_iberia_868_render_default.py`:
- `test_render_at_default_cfg_matches_generate_byte_equal` (sample #11, D-17)

Standard stub body shape:

```python
"""Wave 0 stub for Plan 04-XX — DO NOT remove imports.

Each test is `pytest.skip`-marked; Plan 04-XX implementation removes the skip
and fills the body. Keeping the skipped functions present lets `pytest --collect-only`
catch typos in test names referenced by 04-VALIDATION.md and lets <verify>
commands in implementation plans return exit 0 (skipped, not errored).
"""
from __future__ import annotations

import pytest

pytestmark = pytest.mark.unit  # change marker per file


def test_apply_median_does_not_mutate_input() -> None:
    pytest.skip("Wave 0 stub — Plan 04-01 implements")


# ... one function per name listed above
```

For `test_iberia_868_render_default.py`, also add `pytestmark = pytest.mark.parity` and
add a `# This test runs only when --run-parity is implicitly enabled by the marker config
# in pyproject.toml (consistent with test_iberia_868.py)` comment.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -m pytest tests/unit/test_cleanup_split.py tests/unit/test_dag_tokens.py tests/unit/test_stage_cache.py tests/integration/test_render_endpoint.py tests/integration/test_render_cancel.py tests/parity/test_iberia_868_render_default.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only -q` for the 6 files reports the exact function names listed in `<action>` (no extras, no typos)
    - All 6 files have `pytestmark = pytest.mark.{unit|integration|parity}` matching VALIDATION.md
    - `python -m pytest backend/tests/unit/test_cleanup_split.py -q` exits 0 with all functions reported as `skipped`
    - `grep -r "from medieval_forge" backend/tests/unit/test_cleanup_split.py` returns zero (no production imports yet)
    - Existing parity test `tests/parity/test_iberia_868.py` continues to pass (no regression): `python -m pytest tests/parity/test_iberia_868.py -x` exits 0
  </acceptance_criteria>
  <done>
    All 6 backend stub files exist; pytest collects exactly the listed test names; existing parity stays green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create 8 frontend stub test files</name>
  <files>
    frontend/src/components/canvas/__tests__/ParameterSidebar.test.tsx,
    frontend/src/components/canvas/__tests__/SliderCard.test.tsx,
    frontend/src/components/canvas/__tests__/StageViewToggle.test.tsx,
    frontend/src/components/canvas/__tests__/CanvasViewer.clearCache.test.tsx,
    frontend/src/components/canvas/__tests__/BaronyLabels.test.tsx,
    frontend/src/api/__tests__/useRenderStream.test.ts,
    frontend/src/stores/__tests__/usePipelineParams.test.ts,
    frontend/src/components/workspace/__tests__/WorkspaceToolbar.cancel.test.tsx
  </files>
  <read_first>
    .planning/phases/04-parameter-studio-live-re-render/04-VALIDATION.md
    frontend/src/components/canvas/__tests__/InspectorSidebar.test.tsx
    frontend/src/stores/__tests__/uiStore.test.ts
    frontend/vitest.config.ts
  </read_first>
  <action>
Create 8 vitest stub files. Each file imports `describe`, `it` from `vitest` only
(no production imports — components don't exist yet). Each `it` body uses `it.skip(name, () => {})`.

Test names (verbatim from VALIDATION.md):

`ParameterSidebar.test.tsx` (D-05/D-06):
- `it.skip('renders 320px collapsible left sidebar')`
- `it.skip('renders StageViewToggle at top + 4 SliderCards stacked')`
- `it.skip('collapses to width 0 when toggle closed')`
- `it.skip('mirrors InspectorSidebar borderRight: 1px solid var(--gray-6)')`

`SliderCard.test.tsx` (D-08):
- `it.skip('renders Radix Slider.Root with min, max, step from prop')`
- `it.skip('renders adjacent numeric input with type=number')`
- `it.skip('clamps numeric input out-of-bounds and flashes red 600ms')`
- `it.skip('reset IconButton reverts to default and fires onCommit immediately')`
- `it.skip('default-tick mark renders at percentage matching default value')`

`StageViewToggle.test.tsx` (D-10/D-11):
- `it.skip('renders 5 RadioGroup items with PT-BR labels')`
- `it.skip('default selection is render-final')`
- `it.skip('onChange fires with new stage_view value')`
- `it.skip('section heading reads "Vista do estágio"')`

`CanvasViewer.clearCache.test.tsx` (CLAUDE.md rule + SC-4):
- `it.skip('test_clearCache_called_on_token_change')` (sample #7)
- `it.skip('test_clearCache_called_on_stage_view_change')`
- `it.skip('test_clearCache_NOT_called_when_token_unchanged')`

`BaronyLabels.test.tsx` (D-12):
- `it.skip('renders Konva Text per barony when layerVisibility.baronies is true')`
- `it.skip('hides labels when layerVisibility.baronies is false')`
- `it.skip('truncates label at 12 chars with ellipsis')`
- `it.skip('positions text at barony centroid pixel coordinate')`
- `it.skip('font size is 10px, fill is #FFFFFF, shadow blur is 1px black')`

`useRenderStream.test.ts` (D-04 + D-13):
- `it.skip('subscribes to /api/v3/projects/{id}/render/stream')`
- `it.skip('translates stage_start envelope to runStore.startStage')`
- `it.skip('translates stage_done envelope to runStore.finishStage')`
- `it.skip('translates stage_cancel envelope to runStore.revertStage')`
- `it.skip('handles done envelope by closing EventSource and finish(generated)')`

`usePipelineParams.test.ts` (D-07):
- `it.skip('test_slider_change_debounces_250ms')` (sample #9)
- `it.skip('latest-wins: new debounce cancels in-flight render via cancel endpoint')`
- `it.skip('reset action skips debounce and fires immediately')`
- `it.skip('exposes stage_view as client-only state (not in cfg overrides on /generate)')`

`WorkspaceToolbar.cancel.test.tsx` (D-16):
- `it.skip('renders status badge when state in {idle, generated}')`
- `it.skip('replaces status badge with red Cancelar button when state in {generating, rendering}')`
- `it.skip('Cancelar onClick fires postRenderCancel(projectId)')`
- `it.skip('badge restores when state flips back to generated')`

Standard stub body shape:

```typescript
/**
 * Wave 0 stub for Plan 04-XX. DO NOT add production imports until 04-XX
 * implementation. Keeping `it.skip` lets vitest run green and lets <verify>
 * commands in implementation plans return exit 0 with `skipped` reports.
 */
import { describe, it } from 'vitest'

describe('ParameterSidebar', () => {
  it.skip('renders 320px collapsible left sidebar', () => {})
  // ... one it.skip per name listed above
})
```

Note: `frontend/src/api/__tests__/` and `frontend/src/stores/__tests__/` and
`frontend/src/components/workspace/__tests__/` directories may not exist —
create them.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx vitest run src/components/canvas/__tests__/ParameterSidebar.test.tsx src/components/canvas/__tests__/SliderCard.test.tsx src/components/canvas/__tests__/StageViewToggle.test.tsx src/components/canvas/__tests__/CanvasViewer.clearCache.test.tsx src/components/canvas/__tests__/BaronyLabels.test.tsx src/api/__tests__/useRenderStream.test.ts src/stores/__tests__/usePipelineParams.test.ts src/components/workspace/__tests__/WorkspaceToolbar.cancel.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - All 8 files exist on disk
    - vitest run reports total skipped tests = 33 (sum of `it.skip` counts above: 4+5+4+3+5+5+4+4 actually = 34; correct count must match the listed names)
    - vitest exits 0 (no failures)
    - `grep -r "from '\.\./" frontend/src/components/canvas/__tests__/ParameterSidebar.test.tsx` returns zero (no production imports yet)
    - Existing test suite continues green: `cd frontend && npx vitest run` exits 0
  </acceptance_criteria>
  <done>All 8 frontend stub files exist; vitest reports skipped (not failed); existing suite green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create 2 Playwright stub specs</name>
  <files>
    frontend/tests/e2e/parameter-studio-sc3.spec.ts,
    frontend/tests/e2e/parameter-studio-cancel.spec.ts
  </files>
  <read_first>
    .planning/phases/04-parameter-studio-live-re-render/04-VALIDATION.md
    frontend/playwright.config.ts
    frontend/tests/e2e
  </read_first>
  <action>
Create 2 Playwright stub specs. Each:

1. Imports `test, expect` from `@playwright/test`
2. Uses `test.skip(true, "Wave 0 stub — Plan 04-06 implements")` at the top of every test body so they skip cleanly without `test.fixme`-style noise

`parameter-studio-sc3.spec.ts` (SC-3):
```typescript
import { test, expect } from '@playwright/test'

test.describe('Parameter Studio - SC-3 timing', () => {
  test('sigma 3.0 to 4.5 produces visible canvas pixel diff in <500ms', async ({ page }) => {
    test.skip(true, 'Wave 0 stub — Plan 04-06 implements')
    // Implementation will: open project workspace, capture canvas pixel, drag
    // smooth_sigma slider 3.0 -> 4.5, wait for stage_done SSE, capture pixel
    // again, assert different + assert elapsed < 500ms.
  })
})
```

`parameter-studio-cancel.spec.ts` (SC-4):
```typescript
import { test, expect } from '@playwright/test'

test.describe('Parameter Studio - SC-4 cancel', () => {
  test('cancel restores prior cfg + canvas swap <50ms', async ({ page }) => {
    test.skip(true, 'Wave 0 stub — Plan 04-06 implements')
    // Implementation will: open workspace post-generate, drag slider mid-render,
    // click Cancelar, assert canvas pixel matches pre-drag baseline AND slider
    // value matches pre-drag value AND elapsed < 50ms after click.
  })
})
```

If `frontend/tests/e2e/` does not exist, create it.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx playwright test tests/e2e/parameter-studio-sc3.spec.ts tests/e2e/parameter-studio-cancel.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - Both spec files exist
    - Playwright reports both tests as `skipped` (not `passed`, not `failed`)
    - Playwright exit code is 0
    - Each file has exactly 1 test inside its describe block
  </acceptance_criteria>
  <done>Both Playwright stubs commit; tests report skipped; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Wave 0 only adds test files; no new HTTP endpoints, no new auth surfaces, no user-input parsing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00-01 | Tampering | test stubs | accept | Stubs use `pytest.skip` / `it.skip` / `test.skip` — production code is not executed; no runtime surface introduced |

Wave 0 is purely scaffolding. Real ASVS L1 controls land in Plan 04-02 (`/render`
endpoint input validation + cross-router single-flight gate).
</threat_model>

<verification>
After all 3 tasks:
- `cd backend && python -m pytest --collect-only -q tests/unit/test_cleanup_split.py tests/unit/test_dag_tokens.py tests/unit/test_stage_cache.py tests/integration/test_render_endpoint.py tests/integration/test_render_cancel.py tests/parity/test_iberia_868_render_default.py` lists all expected test names
- `cd backend && python -m pytest -m "unit or integration or parity" -q` exits 0 (full suite still green; new stubs report skipped)
- `cd frontend && npx vitest run` exits 0 (full suite still green)
- `cd frontend && npx playwright test --list` includes the two new specs
- `git status` shows exactly 16 new files (14 stubs + 2 e2e specs); no production-code modifications
</verification>

<success_criteria>
- 14 stub files exist with names verbatim from 04-VALIDATION.md
- Backend pytest, frontend vitest, Playwright runs all exit 0
- No production-code imports (purely structural scaffolding)
- VALIDATION.md `Wave 0 Requirements` checklist becomes mostly checkable (impl plans tick the boxes when they remove skips)
</success_criteria>

<output>
After completion, create `.planning/phases/04-parameter-studio-live-re-render/04-00-SUMMARY.md`
listing each created stub path + the count of test names per file.
</output>
