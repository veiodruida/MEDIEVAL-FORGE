---
phase: 4
slug: parameter-studio-live-re-render
status: approved
shadcn_initialized: false
preset: none
created: 2026-05-10
reviewed_at: 2026-05-10
---

# Phase 04 — UI Design Contract

> Visual and interaction contract for the parameter studio. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Language convention (established, Phase 03):** PT-BR for user-facing chrome; English for technical inspector field labels. All new Phase 04 chrome (sliders, stage-view radios, Cancel button, reset buttons) follows PT-BR.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | @radix-ui/themes 3.3.0 |
| Icon library | @radix-ui/react-icons 1.3.2 |
| Font | Radix Themes default system stack |
| Styling | Tailwind CSS v4 via @tailwindcss/vite — utility classes + Radix CSS variables |

**shadcn gate:** Not applicable. `components.json` is absent by design — the project committed to Radix Themes (see `frontend/src/index.css` Pitfall-7 import order). Initializing shadcn would conflict with the Radix Themes CSS layer. No shadcn registry used.

---

## Spacing Scale

All values are Radix Themes prop names (`gap`, `px`, `p`) plus px equivalents. Phase 04 components extend existing Phase 03 patterns — do not invent a parallel scale.

| Token | Radix Prop | px Equivalent | Usage |
|-------|-----------|---------------|-------|
| xs | `gap="1"` | 4px | Icon gaps, reset button icon spacing |
| sm | `gap="2"` | 8px | Slider label ↔ numeric input gap |
| md | `gap="3"` | 12px | Items within a SliderCard |
| lg | `gap="4"` / `px="4"` | 16px | ParameterSidebar internal padding; toolbar zone px |
| xl | `gap="5"` | 20px | Toolbar flex gap (existing, WorkspaceToolbar) |
| 2xl | — | 48px | Toolbar fixed height (existing contract) |
| sidebar | — | 320px | ParameterSidebar fixed width (from CONTEXT D-06) |

Exceptions:
- Toolbar height is 48px (fixed, existing Phase 03 contract — not changed).
- ParameterSidebar width is 320px (from CONTEXT D-06 "~320 px" spec).
- Slider touch targets: Radix `Slider.Root` thumb is 20px diameter by default; no exception needed (mouse-first tool, not touch).

---

## Typography

Radix Themes `size` prop maps to: size="1" = 12px, size="2" = 14px, size="3" = 16px. Weights: `weight="regular"` = 400, `weight="bold"` = 700 (Radix maps to semibold visually at ~600 rendered weight in the system stack).

| Role | Radix size | px | Weight | Line Height | Usage |
|------|-----------|-----|--------|-------------|-------|
| Body | `size="2"` | 14px | regular (400) | 1.5 | Slider value labels, stage-view radio labels, help hints |
| Label | `size="1"` | 12px | regular (400) | 1.4 | Slider bounds annotations, default-tick label, hint suffixes |
| Heading | `size="2"` | 14px | bold (~600) | 1.2 | SliderCard title ("Suavização σ"), section headings in sidebar |
| Display | `size="3"` | 16px | bold (~600) | 1.2 | ParameterSidebar section header ("Parâmetros") |

Note: The existing InspectorSidebar uses `size="3"` for territory names (Heading). ParameterSidebar uses the same heading slot but at `size="3"` only for the top-level section label, matching Phase 03 visual weight.

---

## Color

The Phase 03 color contract is inherited unchanged. Phase 04 only adds the Cancel/destructive surface.

| Role | Radix color token | Usage |
|------|------------------|-------|
| Dominant (60%) | `var(--color-panel-solid)` | Canvas background, ParameterSidebar panel surface |
| Secondary (30%) | `var(--gray-2)` / Radix `Card variant="surface"` | SliderCard backgrounds, sidebar card shells, LayerTogglePanel |
| Accent (10%) | Radix `color="blue"` | Primary CTAs only: "Gerar Mapa", "Regenerar" buttons |
| Destructive | Radix `color="red"` | Cancel button (WorkspaceToolbar badge → "Cancelar" red, active only during `state ∈ { generating, rendering }`) |

**Tier badge colors (locked from Phase 03 — do not change):**
- amber = kingdom tier
- blue = duchy tier + "Ingerindo…" state badge
- grass = condado tier + "Mapa gerado" success badge
- gray = barony tier + "Pronto" idle badge
- red = error state + Cancel affordance (Phase 04 addition)

Accent reserved for: "Gerar Mapa" / "Regenerar" button only. Stage-view radios use Radix `RadioGroup` with default gray/blue selection highlight. Slider thumb uses Radix default (blue). Reset icon button uses `color="gray"` `variant="ghost"`.

---

## Component Inventory

New components Phase 04 introduces (file names per CONTEXT code_context):

### ParameterSidebar (`components/canvas/ParameterSidebar.tsx`)

- Fixed width: 320px
- Position: left side, below WorkspaceToolbar; mirrors right-side InspectorSidebar
- Structure: Radix `ScrollArea` wrapping two sections — (1) StageViewToggle at top, (2) four SliderCards stacked vertically
- Collapse toggle: button in WorkspaceToolbar (new icon button, left zone) opens/closes the sidebar; closed state collapses to 0px (not an icon rail)
- Header: `Text size="3" weight="bold"` → "Parâmetros" (PT-BR) [default]
- Border: `borderRight: '1px solid var(--gray-6)'` — matches InspectorSidebar `borderLeft` convention

### SliderCard (`components/canvas/SliderCard.tsx`)

Each of the 4 sliders gets a Radix `Card variant="surface"` with internal layout:

```
[Title: "Suavização (σ)"]             [↺ Redefinir]
[────────●──────────] (Radix Slider.Root)
[3.0]                          [4.5]   <- bounds labels, size="1" gray
[Numeric input: 3.0]  default: 3.0 ▼  <- default-tick label
```

- Title: `Text size="2" weight="bold"` (PT-BR label)
- Slider: Radix `Slider.Root` with `min`, `max`, `step` per CONTEXT D-05
- Numeric input: `<input type="number">` adjacent to slider; commits on blur or Enter; out-of-bounds values clamp + flash `color="red"` for 600ms then revert to last valid
- Reset button: `IconButton size="1" variant="ghost" color="gray"` with `UpdateIcon` (or `ResetIcon` from @radix-ui/react-icons) + `Tooltip content="Redefinir para padrão"`
- Default-tick: a small visual marker at the default position on the Radix Slider track; implemented as an absolutely-positioned `Box` at the percentage that maps to the default value

**Four slider specs (bounds from CONTEXT D-05, CLAUDE.md Rule #2):**

| Slider | PT-BR Label | min | max | step | default |
|--------|------------|-----|-----|------|---------|
| `smooth_sigma` | "Suavização (σ)" | 3.0 | 4.5 | 0.1 | 3.0 |
| `median_passes` | "Passes Mediana" | 1 | 12 | 1 | 8 |
| `fragment_min_px` | "Fragmento mín. (px)" | 0 | 2000 | 50 | 600 |
| `blob_merge_px` | "Fusão de blobs (px)" | 0 | 500 | 25 | 200 |

PT-BR labels are defaults from the researcher. Executor may refine phrasing; the field names and bounds are locked per CONTEXT D-05.

### StageViewToggle (`components/canvas/StageViewToggle.tsx`)

- Sits at the top of ParameterSidebar, above SliderCards
- Component: Radix `RadioGroup.Root` + `RadioGroup.Item` × 5
- Orientation: vertical stack (consistent with sidebar layout)
- Labels (PT-BR defaults — executor may refine):
  - `landmask` → "Máscara terrestre"
  - `voronoi-raw` → "Voronoi bruto"
  - `cleanup` → "Limpeza"
  - `smooth` → "Suavização"
  - `render-final` → "Mapa final" (default selected)
- When `stage_view ≠ render-final`: BaronyLayer, DecorationsLayer, and RiversOverlay hide. Only the base raster for the selected stage renders. LayerTogglePanel checkboxes for non-applicable layers are visually disabled (`opacity: 0.4`, still toggleable but visually inert).
- Section heading: `Text size="1" color="gray"` → "Vista do estágio" above the radio group

### WorkspaceToolbar Extensions (modifications to existing)

1. **Left zone addition:** `IconButton` to toggle ParameterSidebar (e.g. `MixerHorizontalIcon`). Sits left of the "← Projetos" back link.
2. **Cancel state:** When `runState ∈ { generating, rendering }`, `GenerateStatusBadge` re-purposes to a Radix `Button color="red" variant="solid"` labeled "Cancelar". On click, fires the cancel endpoint (planner picks `POST /render/cancel` vs `DELETE /render`). When state returns to idle/generated, badge restores to its normal shape per Phase 03 contract.

### BaronyLabel (visual layer inside `components/canvas/BaronyLayer.tsx` or `DecorationsLayer.tsx`)

Renders the name of each barony centered on its centroid. Added per CONTEXT D-12 ("when Baronies toggle is on").

**Trigger condition:** Visible when `layerVisibility.baronies === true` (the existing LayerTogglePanel "Baronias" checkbox). NOT zoom-gated independently — follows the Baronies layer toggle, not the "Nomes" (condado labels) toggle which gates at zoom ≥ 1.5×. Executor confirms via `uiStore.layerVisibility` read in `BaronyLayer`.

**Implementation options** (planner picks one per CONTEXT D-12):
- Konva `Text` node inside `BaronyLayer` positioned at barony centroid `cfg.condados[i].baronies[j]`
- DOM overlay using the same pattern as `HoverTooltip` (DOM precedent in the codebase)

**Visual contract (applies regardless of implementation):**

| Property | Value | Source |
|----------|-------|--------|
| Font size | 10px (Konva px) or `size="1"` equivalent | Below hierarchy label size to avoid competing with condado names |
| Color | white (`#FFFFFF`) | Readable on colored territory fills |
| Halo / stroke | black shadow, 1px spread | Matches existing label halo convention seen in Phase 03 visual sign-off (`phase03-final.png`) |
| Font weight | regular (400) | Labels, not headings |
| Text anchor | center-center on barony centroid | Standard centroid positioning |
| Truncation | Truncate at 12 characters with "…" if label overflows barony bounding box | Prevents label overlap on small baronies |
| Zoom gate | None — visible whenever Baronies toggle is on, at any zoom level | Per CONTEXT D-12 "Baronies toggle is on" |
| Update on re-render | Re-hydrate on every `cacheVersion` change that affects the barony tier (i.e., `smooth` or `merge` slider changes) | Centroids may shift when merge reshapes baronies |

---

## Interaction Contract

### Slider → Re-render Flow

1. User drags a slider or edits the numeric input.
2. `onChange` fires; local state updates immediately (the slider visually tracks).
3. After 250ms debounce expiry with no further change (`useDebouncedCallback`), the frontend:
   a. Closes active SSE EventSource (if open).
   b. POSTs `/api/v3/projects/{id}/render` with changed cfg overrides.
   c. `runState` transitions to `rendering` (new state, distinct from `generating`).
4. SSE events arrive: `stage_start` / `stage_done` per affected stage only.
5. On pipeline completion: canvas hydrates with new artifacts, `Konva.clearCache()` fires per layer, `runState` → `generated`.
6. Latest-wins: if another drag fires while a render is in flight, step 3a cancels the in-flight request before posting a new one.

### Cancel Flow

1. User clicks "Cancelar" in the toolbar.
2. Cancel endpoint fires immediately (planner picks `POST /render/cancel` vs `DELETE /render`).
3. Backend sets `stop_event`, emits `stage_cancel` SSE event per affected stage carrying prior tokens.
4. Frontend: slider values revert to the values that produced `prior_token`; `Konva.clearCache()` per layer; `runState` → `generated` (reverted state).
5. Latency target: <50ms end-to-end (O(1) cache swap per CONTEXT D-13).

### Stage View Switch

1. User selects a radio option in StageViewToggle.
2. `stage_view` local state updates.
3. `useCanvasArtifacts` re-keys its TanStack Query on `(stage_view, token)` to re-fetch the raster for the selected stage. The planner picks the endpoint shape (new `GET /stage/{name}.png` endpoint, or expanded `/artifacts/*` semantics — CONTEXT Claude's Discretion).
4. BaronyLayer / DecorationsLayer / RiversOverlay visibility set by `stage_view === 'render-final'`.
5. No pipeline re-run triggered by a stage-view switch alone.

### Slider Reset

1. User clicks the reset `IconButton` on a SliderCard.
2. That slider reverts to its `RegionConfig` default (per table above).
3. The standard re-render debounce fires immediately (not debounced — reset is intentional, not a drag gesture).

### Barony Label Display

1. When `layerVisibility.baronies` flips to `true`, BaronyLabels render on top of BaronyLayer polygons.
2. Labels re-position on every `cacheVersion` change (merge or smooth slider updates may shift barony centroids).
3. `Konva.clearCache()` is called on the label layer alongside all other layers on hydration.

---

## Copywriting Contract

Language: PT-BR for chrome labels, English for technical/inspector fields (established Phase 03 pattern).

| Element | Copy | Language |
|---------|------|----------|
| Primary CTA (pre-generate) | "Gerar Mapa" | PT-BR |
| Primary CTA (post-generate) | "Regenerar" | PT-BR |
| Cancel button (active during render) | "Cancelar" | PT-BR |
| ParameterSidebar header | "Parâmetros" | PT-BR |
| StageViewToggle section label | "Vista do estágio" | PT-BR |
| Stage view: render-final | "Mapa final" | PT-BR |
| Stage view: smooth | "Suavização" | PT-BR |
| Stage view: cleanup | "Limpeza" | PT-BR |
| Stage view: voronoi-raw | "Voronoi bruto" | PT-BR |
| Stage view: landmask | "Máscara terrestre" | PT-BR |
| Slider reset tooltip | "Redefinir para padrão" | PT-BR |
| Slider bounds out-of-range flash | (no copy — visual red flash + clamp, no modal) | — |
| Empty state for ParameterSidebar | not applicable — sliders are always default-populated from RegionConfig on mount | — |
| Error state (render fails mid-stream) | "Erro no estágio {stage_name}. Tente novamente." | PT-BR |
| Cancel restores prior state | No copy — canvas reverts silently; status badge returns to "Mapa gerado" | — |
| Destructive confirmation for Cancel | None — cancel is reversible (prior_token swap is lossless), so no modal required | — |

**Destructive action policy:** Cancel is the only action in Phase 04 that interrupts an in-flight operation. It is NOT destructive in the data-loss sense (prior state is cached O(1) swap). Therefore: no confirmation dialog. Single-click "Cancelar" button executes immediately.

**No new destructive actions** (delete, overwrite, etc.) are introduced in Phase 04.

---

## State Machine Extension

Phase 04 extends `useRunStore` with a `rendering` state:

```
idle → ingesting → generating → generated | error
idle → generated → rendering → generated | error
                             ↑ (Cancel) → generated (reverted to prior)
```

| State | GenerateStatusBadge color | Copy | Cancel visible |
|-------|--------------------------|------|---------------|
| idle | gray | "Pronto" | no |
| ingesting | blue | "Ingerindo…" | no |
| generating | amber | "Gerando: {stage}" | no (full re-run) |
| rendering | amber | "Renderizando: {stage}" | yes (red "Cancelar" button) |
| generated | grass | "Mapa gerado" | no |
| error | red | "Erro" | no |

---

## Layout Contract

```
┌─────────────────────────────────────────────── WorkspaceToolbar (48px) ────────────┐
│ [⬡ Params] ← Projetos | Project Name         [Status Badge / Cancelar]   [CTAs]  │
└─────────────────────────────────────────────────────────────────────────────────────┘
┌─ ParameterSidebar (320px) ─┐ ┌─── CanvasViewer (flex-grow) ─────────────────────────┐
│ Vista do estágio:          │ │                                                       │
│ ○ Mapa final (default)    │ │         [Konva Stage — map canvas]                    │
│ ○ Suavização               │ │                                                       │
│ ○ Limpeza                  │ │   [LayerTogglePanel — top-left overlay, z=10]         │
│ ○ Voronoi bruto            │ │   [HoverTooltip — DOM overlay, z=30]                  │
│ ○ Máscara terrestre        │ │   [FitToViewButton — bottom-right, z=10]              │
│                            │ │                                                       │
│ ─────────────────────────  │ └───────────────────────────────────────────────────────┘
│ [SliderCard: Suavização σ] │ ┌─ InspectorSidebar (~320px) ────────────────────────────┐
│ [SliderCard: Passes Med.]  │ │ [Territory inspector — unchanged from Phase 03]        │
│ [SliderCard: Frag. mín.]   │ │                                                        │
│ [SliderCard: Fusão blobs]  │ └────────────────────────────────────────────────────────┘
└────────────────────────────┘
```

- ParameterSidebar is collapsible. Collapsed state: width 0, content hidden; toggle button remains in toolbar.
- When collapsed, CanvasViewer takes full width minus InspectorSidebar width.
- InspectorSidebar width is unchanged (~320px, existing Phase 03 contract).
- LayerTogglePanel position is `absolute top: 12px left: 12px` relative to the CanvasViewer container — unchanged from Phase 03. When ParameterSidebar is open, the LayerTogglePanel remains at its absolute position within CanvasViewer, not overlapping the sidebar.

---

## Accessibility

- All Radix Themes interactive components (Slider.Root, RadioGroup, Checkbox, Button) are keyboard-navigable by default. No additions required.
- Slider numeric inputs use `aria-label` matching the PT-BR slider label ("Suavização σ value", etc.).
- Cancel button is a `<button>` (Radix Button) — native keyboard focus and Enter/Space activation.
- Stage-view radios: `RadioGroup.Root` has `aria-label="Vista do estágio"`.
- Default-tick markers are `aria-hidden="true"` (decorative).
- Color-only status differentiation: the Cancel button is also labeled "Cancelar" (not just red) — no color-only communication.
- Barony labels: Konva `Text` nodes are canvas-rendered (not in the DOM accessibility tree). This is acceptable — barony names are already surfaced in the Inspector when the user clicks a territory.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| third-party | none | not applicable |

No third-party component registries. All UI is built from @radix-ui/themes 3.3.0 + @radix-ui/react-icons 1.3.2 (both already installed in package.json).

---

## Pre-Population Sources

| Source | Decisions Used |
|--------|---------------|
| CONTEXT.md (04-CONTEXT.md) | 19 — slider bounds (D-05), sidebar slot (D-06), debounce 250ms (D-07), slider component shape (D-08), stage-view semantics (D-09, D-10, D-11), cancel mechanic (D-13, D-14, D-16), undo deferral (D-15), barony labels (D-12) including visual contract, rendering state extension |
| ROADMAP.md | 4 — Phase 04 success criteria SC-1..SC-4 drive state machine and cancel copy |
| STATE.md | 1 — Phase 03 complete; canvas + sidecars infrastructure confirmed |
| REQUIREMENTS.md | 0 — file not found |
| Codebase scan | 12 — Radix Themes color tokens (amber/blue/grass/gray/red), toolbar 48px, InspectorSidebar patterns, WorkspaceToolbar structure, GenerateStatusBadge state map, LayerTogglePanel absolute positioning, PT-BR chrome convention, PIPELINE_STAGES, useRunStore 5-state machine |
| Researcher defaults | 6 — PT-BR slider labels, PT-BR stage-view labels, stage-view layer hide behavior, error copy, default-tick affordance, barony label font/halo spec |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
