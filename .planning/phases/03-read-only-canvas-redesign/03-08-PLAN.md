---
phase: 03
plan: 08
type: execute
wave: 4
depends_on: [04, 05, 06, 07]
files_modified:
  - frontend/tests/uat/playwright/03-canvas-workspace.spec.ts
  - backend/tests/fixtures/seed_phase01_artifacts.py
autonomous: false
requirements: [SC-1, SC-2, SC-3, SC-4]
must_haves:
  truths:
    - "Playwright UAT scenario seeds a project with Phase 01 artifacts (10 Unity files + 4 sidecars), opens /projects/<id>, asserts canvas hydrates, clicks a territory, asserts InspectorSidebar populates, toggles a layer, shift+clicks a second territory, asserts MultiSelectInspector renders aggregate count"
    - "UAT spec asserts ZERO console errors throughout the flow (SC-3)"
    - "UAT spec asserts no requests to /preview/* fired (SC-4 hint — only /api/v3/projects/{id}/artifacts/* observed)"
    - "Phase 01 parity test still 10/10 green at end of Wave 4"
    - "Final dangling-import grep across BOTH frontend/src/ AND backend/medieval_forge/ returns zero"
  artifacts:
    - path: "frontend/tests/uat/playwright/03-canvas-workspace.spec.ts"
      provides: "Single happy-path UAT covering SC-1..SC-4"
      min_lines: 80
    - path: "backend/tests/fixtures/seed_phase01_artifacts.py"
      provides: "Helper to seed projects/<uuid>/output/ with Phase 01 artifact set for UAT runs"
  key_links:
    - from: "Playwright spec"
      to: "Phase 01 deployed artifacts copied into projects/<seeded-uuid>/output/"
      via: "fixture script"
      pattern: "seed_phase01_artifacts"
---

<objective>
Wave 4 final acceptance gate: Playwright UAT scenario covering all four
ROADMAP success criteria + a final cross-surface dangling-import grep
+ Phase 01 parity re-run. The UAT is the single happy-path described in
ROADMAP Phase 03 success criteria; richer scenarios are Phase 04
territory.

Has a checkpoint at the end for human-eyeballing the workspace shell
visual against UI-SPEC §Layout Contract (the manual-only verification
documented in 03-VALIDATION.md §Manual-Only Verifications).

Output: 1 Playwright spec + 1 backend fixture helper + a final
verification commit that closes Phase 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-read-only-canvas-redesign/03-CONTEXT.md
@.planning/phases/03-read-only-canvas-redesign/03-UI-SPEC.md
@.planning/phases/03-read-only-canvas-redesign/03-VALIDATION.md
@frontend/playwright.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend fixture helper to seed Phase 01 artifacts into a project dir</name>
  <files>backend/tests/fixtures/seed_phase01_artifacts.py, backend/tests/unit/test_seed_phase01_artifacts.py</files>
  <read_first>
    - backend/tests/parity/test_iberia_868.py (where the iberia_868 fixture output lives)
    - backend/medieval_forge/services/paths.py (project_dir helper + PROJECTS_ROOT layout)
  </read_first>
  <behavior>
    - Test: `seed_phase01_artifacts(project_id, source_dir)` creates `projects/<project_id>/output/` and copies all 14 expected files (10 Unity + 4 sidecars)
    - Test: function is idempotent (running twice yields identical state)
    - Test: function rejects an invalid project_id via ValueError (delegates to paths.is_valid_uuid)
  </behavior>
  <action>
    1. Create `backend/tests/fixtures/seed_phase01_artifacts.py` exporting `seed_phase01_artifacts(project_id: str, source_dir: Path) -> Path` that copies each of the 14 ARTIFACT_FILES (import the frozenset from `api/v3/artifacts.py`) from `source_dir` into `project_dir(project_id) / "output" / file`. Use `shutil.copy2` for byte-exactness.
    2. Create `backend/tests/unit/test_seed_phase01_artifacts.py` with the 3 behavior tests above.
    3. The `source_dir` for UAT runs will be the iberia_868 parity output directory; the Playwright spec calls this fixture before launching the dev server.
  </action>
  <verify>
    <automated>cd backend && pytest tests/unit/test_seed_phase01_artifacts.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_seed_phase01_artifacts.py -x` exits 0 (3 tests green)
    - `grep -n "ARTIFACT_FILES" backend/tests/fixtures/seed_phase01_artifacts.py` shows the import (single source of truth)
    - The function copies all 14 files (10 Unity + 4 sidecars), not just 10
  </acceptance_criteria>
  <done>Fixture helper green; UAT can seed a deterministic project state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Playwright UAT scenario for SC-1..SC-4</name>
  <files>frontend/tests/uat/playwright/03-canvas-workspace.spec.ts</files>
  <read_first>
    - frontend/playwright.config.ts (existing config — base URL, projects, etc.)
    - .planning/phases/03-read-only-canvas-redesign/03-UI-SPEC.md (§Layout Contract — assertions on toolbar height, inspector width, etc.)
    - .planning/phases/03-read-only-canvas-redesign/03-CONTEXT.md (D-15..D-17 interaction contract)
  </read_first>
  <behavior>
    - Spec scenario "canvas workspace happy path":
      (1) Setup: create a project via POST /api/projects (or seed DB directly via a Python helper invoked from a shell script in `globalSetup`); seed its output dir using `seed_phase01_artifacts`; project.status = 'generated'; project.updated_at set.
      (2) Navigate to `/projects/{seeded-id}`.
      (3) Assert WorkspaceToolbar visible with project name + "Regenerar" button (status badge color = grass, copy = "Mapa gerado").
      (4) Assert CanvasViewer renders (Konva stage present); wait for `[data-testid="territory-layer-ready"]` or analogous.
      (5) Click on a territory polygon (use a known coordinate from the seeded territory_metadata.json); assert InspectorSidebar shows the condado's name + at least one neighbor.
      (6) Toggle off the BackgroundLayer via LayerTogglePanel; assert the layer's `visibility` Konva attr changes (or its data-testid disappears).
      (7) Shift+click on a second territory; assert MultiSelectInspector shows "2 condados selecionados".
      (8) Hover a third territory; assert HoverTooltip div appears with the condado name.
      (9) Click on empty stage area (water); assert InspectorSidebar shows the PT-BR placeholder "Clique num território para ver detalhes" (D-16).
      (10) Throughout: `page.on('console', msg => { if (msg.type()==='error') errors.push(msg.text()) })`. Final assert: `errors.length === 0` (SC-3).
      (11) Network log: `page.on('request', req => { if (req.url().includes('/preview/')) leaks.push(req.url()) })`. Final assert: `leaks.length === 0` (SC-4 hint — no v1 URL still hit).
  </behavior>
  <action>
    1. Create `frontend/tests/uat/playwright/03-canvas-workspace.spec.ts` per the scenario above. Use `test.describe("Phase 03 canvas workspace")` + `test.beforeEach` to seed via a Python subprocess that invokes the Plan 08 Task 1 fixture, OR via a small Python CLI helper invoked from `globalSetup` in `playwright.config.ts`. Seed function args: project_id (uuid4), source_dir (env var pointing to Phase 01 parity output).
    2. Add data-testids to the components if missing (1-line patches into Plan 04/05 files allowed):
       - `WorkspaceToolbar`: `data-testid="workspace-toolbar"`, project name `data-testid="project-name"`, status badge `data-testid="status-badge"`
       - `CanvasViewer`: stage container `data-testid="canvas-stage"`, "ready" sentinel after artifacts loaded `data-testid="territory-layer-ready"`
       - `LayerTogglePanel`: each row `data-testid="layer-toggle-{name}"`
       - `InspectorSidebar`: `data-testid="inspector-sidebar"`, single-select view `data-testid="inspector-single"`, MultiSelect view `data-testid="inspector-multi"`, placeholder `data-testid="inspector-placeholder"`
       - `HoverTooltip`: `data-testid="hover-tooltip"`
       (These touch files modified in earlier plans; flag in SUMMARY as pre-Wave-4 micro-edits.)
    3. Cross-surface dangling-import grep gate (final):
       ```bash
       grep -rn "components/pipeline\|usePipelineStore\|useResearchStore\|useEditorStore\|useValidationStore\|useProjectStore\|useResearchStream\|useCodexStream\|useRubberBandSelection\|useUndoShortcut\|useEditKeyboardMap\|useBeforeUnloadGuard\|api/edit\|api/research\|api/codex\|EditApiError\|TerritoryEditor\|EditToolbar\|SplitTool\|VertexHandlesLayer\|SelectionFloatingToolbar\|ValidationBadgesLayer\|TerrainBadgesLayer\|SaveStatusIndicator\|persistence\|services/validation\|TerrainDataSection\|AssignmentEditor\|ResearchDialog\|ProviderEffortPicker\|BaronyGranularitySlider\|CodexViewer\|AuthSetupSheet\|ProviderSelector\|Stepper\|StepCard" frontend/src/ && \
       grep -rn "ingest_runner\|ingest_wikidata\|research_runner\|research_cache\|codex_runner\|codex_cache\|services/llm\|api/research\|api/codex\|api/llm\|api/auth\|credential_store\|LLMCredential\|ResearchCache\|CodexCache\|app.state.credentials\|app.state.oauth_states\|class LLMCredential" backend/medieval_forge/
       ```
       Both must return zero results.
    4. Phase 01 parity re-run: `cd backend && pytest tests/parity/test_iberia_868.py -x` → 10/10 + sidecar assertion green.
    5. Run the Playwright spec: `cd frontend && npx playwright test 03-canvas-workspace.spec.ts`.
  </action>
  <verify>
    <automated>cd backend && pytest tests/parity/test_iberia_868.py tests/unit/test_seed_phase01_artifacts.py -x && cd ../frontend && npx playwright test 03-canvas-workspace.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test 03-canvas-workspace.spec.ts` exits 0 (1 scenario, ~10 assertions green)
    - The dual cross-surface grep returns zero matches across both frontend/src/ and backend/medieval_forge/
    - `pytest tests/parity/test_iberia_868.py -x` exits 0
    - Console-error count assertion green inside the spec (SC-3)
    - Preview-URL leak assertion green inside the spec (SC-4)
  </acceptance_criteria>
  <done>UAT green; cross-surface grep clean; Phase 01 parity green; SC-1..SC-4 verifiable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual sign-off — workspace shell vs UI-SPEC.md mockup</name>
  <what-built>
    Mapbox-like full-bleed workspace at /projects/<id>: 48px sticky toolbar with back link + project name + status badge + Gerar Mapa + Exportar ZIP; right-docked 320px collapsible inspector; LayerTogglePanel + LegendCard upper-left; FitToView bottom-right; canvas occupies the rest. PT-BR strings per UI-SPEC §Copywriting Contract.
  </what-built>
  <how-to-verify>
    1. Start backend: `cd backend && uvicorn medieval_forge.main:app --reload`
    2. Start frontend: `cd frontend && npm run dev`
    3. Seed a project with Phase 01 artifacts (`python -m medieval_forge.tests.fixtures.seed_phase01_artifacts <new-uuid> path/to/iberia_868/output`).
    4. Open `http://localhost:5173/projects/<seeded-uuid>`.
    5. Compare against `.planning/phases/03-read-only-canvas-redesign/03-UI-SPEC.md` §Layout Contract:
       - Toolbar height 48px, sticky top, gray-2 background, gray-6 bottom border
       - Inspector docked right at 320px width, collapsible via ChevronRight icon
       - LayerTogglePanel floating top-left at 12px/12px
       - LegendCard floating bottom-left at 12px/12px
       - FitToViewButton floating bottom-right at 12px/12px
       - All PT-BR text strings present per §Copywriting Contract (← Projetos, Regenerar/Gerar Mapa, Exportar ZIP, Mapa gerado, etc.)
    6. Click "Regenerar" → verify status badge transitions through ingesting/generating with SSE log panel showing 11 ✓ marks in DAG order (D-03).
    7. Click a territory → InspectorSidebar populates with name + neighbors (D-14). Shift+click another → "2 condados selecionados" view (D-17). Hover a third → tooltip shows name (D-15). Click on water → placeholder restores (D-16).
  </how-to-verify>
  <resume-signal>Type "approved" if visual matches UI-SPEC; otherwise describe deviations and which plan to revise.</resume-signal>
  <files>n/a — manual verification of running dev servers; no file modifications</files>
  <action>Human visual sign-off task. The instructions are in <how-to-verify> above; no automated implementation. Executor pauses, user follows the steps, then types the resume signal.</action>
  <verify><automated>SKIP — manual checkpoint by design (visual UI-SPEC §Layout Contract comparison + SSE log panel observation cannot be unit-tested at the resolution this checkpoint asks for)</automated></verify>
  <done>User has typed "approved" in the resume signal, OR has described deviations + which plan to revise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new — UAT exercises existing boundaries already mitigated by Plans 02/06/07) | n/a |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-UAT-01 | Tampering | Playwright fixture could leak deployed Reconquista artifacts into a wrong path | mitigate | seed_phase01_artifacts uses paths.project_dir (UUID-validated) — same containment guarantee as artifacts.py |
</threat_model>

<verification>
- Playwright spec green
- Final cross-surface dangling-import grep returns zero across frontend + backend
- Phase 01 parity 10/10 + sidecar assertion green
- All Plan 02 + Plan 03 + Plan 04 + Plan 05 vitest/pytest suites still green
- Human visual sign-off captured
</verification>

<success_criteria>
1. SC-1 verified: pan/zoom/click works against Phase 01 project (Playwright)
2. SC-2 verified: Inspector populates + layer toggles work (Playwright)
3. SC-3 verified: zero console errors (Playwright assertion) + zero dangling imports (grep)
4. SC-4 verified: artifacts come from Phase 01 output directly (Playwright network observer + parity test)
5. Human sign-off recorded in SUMMARY
</success_criteria>

<output>
After completion, create `.planning/phases/03-read-only-canvas-redesign/03-08-SUMMARY.md` summarising all 8 plans and capturing the human visual sign-off note.
</output>
