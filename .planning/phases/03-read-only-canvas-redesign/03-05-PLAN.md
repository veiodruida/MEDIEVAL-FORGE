---
phase: 03
plan: 05
type: execute
wave: 2
depends_on: [03]
files_modified:
  - frontend/src/components/canvas/CanvasViewer.tsx
  - frontend/src/components/canvas/TerritoryLayer.tsx
  - frontend/src/components/canvas/InteractionLayer.tsx
  - frontend/src/components/canvas/LayerTogglePanel.tsx
  - frontend/src/components/canvas/HoverTooltip.tsx
  - frontend/src/components/canvas/MultiSelectInspector.tsx
  - frontend/src/components/canvas/InspectorSidebar.tsx
  - frontend/src/components/canvas/__tests__/InteractionLayer.multiSelect.test.tsx
  - frontend/src/components/canvas/__tests__/HoverTooltip.test.tsx
  - frontend/src/components/canvas/__tests__/MultiSelectInspector.test.tsx
  - frontend/src/components/canvas/__tests__/TerritoryLayer.shiftClick.test.tsx
autonomous: true
requirements: [SC-1, SC-2]
must_haves:
  truths:
    - "CanvasViewer.tsx imports zero deleted v1 modules (no useEditorStore, useProjectStore, useResearchStore, useValidationStore, EditToolbar, SplitTool, VertexHandlesLayer, SelectionFloatingToolbar, ValidationBadgesLayer, TerrainBadgesLayer, SaveStatusIndicator, EditApiError)"
    - "InteractionLayer renders gold #f0c040 3px outlines for ALL ids in selectedTerritoryIds (multi-select D-17)"
    - "TerritoryLayer click handler: plain click → selectIds([id]); shift+click → toggle id in/out of set"
    - "Empty-stage click clears selection (D-16) using e.target === e.target.getStage() pattern (Pitfall 5)"
    - "HoverTooltip overlay renders condado name on mouseover; uses Stage.getPointerPosition() positioning (D-15)"
    - "MultiSelectInspector renders count + total area + union kingdoms/duchies + condado name list when selectedTerritoryIds.length >= 2"
    - "InspectorSidebar dispatches single-select view (D-14, unchanged) when length === 1, MultiSelectInspector when >= 2, placeholder 'Clique num território para ver detalhes' when 0 (D-16)"
    - "LayerTogglePanel no longer offers 'terrain' row"
    - "ResizeObserver callback-ref + Konva clearCache patterns from v1-archive preserved verbatim (Pitfalls 4 + 5)"
  artifacts:
    - path: "frontend/src/components/canvas/CanvasViewer.tsx"
      provides: "Stripped read-only canvas viewer"
      max_lines: 420
    - path: "frontend/src/components/canvas/HoverTooltip.tsx"
      provides: "DOM tooltip overlay anchored via Stage.getPointerPosition()"
    - path: "frontend/src/components/canvas/MultiSelectInspector.tsx"
      provides: "Aggregate inspector for D-17 multi-select"
  key_links:
    - from: "TerritoryLayer click handler"
      to: "uiStore.selectIds()"
      via: "plain vs shift+click branch"
      pattern: "selectIds"
    - from: "InteractionLayer"
      to: "uiStore.selectedTerritoryIds"
      via: "set membership filter"
      pattern: "selectedTerritoryIds"
    - from: "HoverTooltip"
      to: "Stage.getPointerPosition()"
      via: "absolute-positioned div sibling to Stage"
      pattern: "getPointerPosition"
---

<objective>
Refit the canvas surface for read-only Phase 03 use. Strip
`CanvasViewer.tsx` of every edit-mode dependency (`EditToolbar`,
`SplitTool`, `VertexHandlesLayer`, `SelectionFloatingToolbar`,
`ValidationBadgesLayer`, `TerrainBadgesLayer`, `SaveStatusIndicator`,
`useEditorStore`, `useProjectStore`, `useResearchStore`,
`useValidationStore`, `useRubberBandSelection`, `useUndoShortcut`,
`useEditKeyboardMap`, `useBeforeUnloadGuard`, `EditApiError`,
`api/edit.ts`, `services/persistence.ts`, `services/validation.ts`).
Promote `InteractionLayer` to multi-id rendering. Rewrite
`TerritoryLayer` click handler to dispatch shift+click multi-select via
`uiStore.selectIds`. Add `HoverTooltip` (D-15) + `MultiSelectInspector`
(D-17). Wire `InspectorSidebar` to dispatch the 3-mode view (none / one
/ many). Trim `LayerTogglePanel` to 4 layers (drop 'terrain').

Purpose: SC-1 (canvas usable end-to-end) + SC-2 (inspector populates +
toggles work) + D-14/15/16/17 interaction contract.

Output: 1 stripped file (CanvasViewer ~700→~400 LOC) + 1 modified file
(TerritoryLayer click handler) + 1 extended file (InteractionLayer
props) + 1 trimmed file (LayerTogglePanel) + 2 new components
(HoverTooltip, MultiSelectInspector) + 1 dispatcher update
(InspectorSidebar) + 4 vitest files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-read-only-canvas-redesign/03-CONTEXT.md
@.planning/phases/03-read-only-canvas-redesign/03-RESEARCH.md
@.planning/phases/03-read-only-canvas-redesign/03-UI-SPEC.md
@frontend/src/components/canvas/CanvasViewer.tsx
@frontend/src/components/canvas/TerritoryLayer.tsx
@frontend/src/components/canvas/InteractionLayer.tsx
@frontend/src/components/canvas/InspectorSidebar.tsx
@frontend/src/components/canvas/LayerTogglePanel.tsx
@frontend/src/stores/uiStore.ts

<interfaces>
uiStore (from Plan 03):
  selectedTerritoryIds: string[]
  selectIds(ids: string[]): void
  selectSelectedTerritoryId selector: (s) => s.selectedTerritoryIds[0] ?? null

InteractionLayer (NEW prop):
  Props: { territories: TerritoryRender[] }   // reads selectedTerritoryIds from store internally
  Renders: <Line stroke="#f0c040" strokeWidth={3} listening={false}> for every territory whose id is in selectedTerritoryIds set

HoverTooltip (NEW):
  Props: { name: string | null, x: number, y: number }
  Renders: position:absolute; pointer-events:none; z-index:50 div with Card variant=surface, padding 4px 8px, Text size=1
  Hidden when name === null

MultiSelectInspector (NEW):
  Props: { selectedIds: string[], metadata: TerritoryMetadata, mapSize: {w:number,h:number} }
  Renders: count, "X condados selecionados"; total area km² (sum pixel_count via pixelsToKm2 from InspectorSidebar);
           Flex<Badge color="amber"> per unique kingdom; Flex<Badge color="blue"> per unique duchy;
           ScrollArea(maxHeight=200) listing condado names

InspectorSidebar (MODIFIED dispatcher logic):
  - if selectedTerritoryIds.length === 0 → render placeholder "Clique num território para ver detalhes"
  - if length === 1 → existing single-select view (D-14, current behavior unchanged; English COPY preserved)
  - if length >= 2 → MultiSelectInspector
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strip CanvasViewer + extend InteractionLayer + update TerritoryLayer click handler</name>
  <files>frontend/src/components/canvas/CanvasViewer.tsx, frontend/src/components/canvas/InteractionLayer.tsx, frontend/src/components/canvas/TerritoryLayer.tsx, frontend/src/components/canvas/LayerTogglePanel.tsx, frontend/src/components/canvas/__tests__/InteractionLayer.multiSelect.test.tsx, frontend/src/components/canvas/__tests__/TerritoryLayer.shiftClick.test.tsx</files>
  <read_first>
    - frontend/src/components/canvas/CanvasViewer.tsx (the 697-line file — identify deletion targets)
    - frontend/src/components/canvas/InteractionLayer.tsx (line 23 — current selection filter logic)
    - frontend/src/components/canvas/TerritoryLayer.tsx (current click handler — uses deleted stores)
    - frontend/src/components/canvas/LayerTogglePanel.tsx
    - frontend/src/stores/uiStore.ts (Plan 03 outputs)
    - .planning/phases/03-read-only-canvas-redesign/03-RESEARCH.md (Pitfalls 4, 5, 8 + Pattern 3)
    - frontend/src/components/canvas/__tests__/CanvasViewer.test.tsx (existing tests must still pass — don't break the read-only happy path)
  </read_first>
  <behavior>
    - InteractionLayer test: given territories=[{id:'a'},{id:'b'},{id:'c'}], uiStore.selectedTerritoryIds=['a','c'] → renders 2 <Line> elements with stroke #f0c040, strokeWidth 3
    - InteractionLayer test: given selectedTerritoryIds=[] → renders Layer with no children
    - TerritoryLayer click test: plain click on territory 'x' → uiStore.selectedTerritoryIds === ['x']
    - TerritoryLayer click test: shift+click on territory 'x' (where ids was ['a']) → ids === ['a','x']
    - TerritoryLayer click test: shift+click on territory 'a' (where ids was ['a','b']) → ids === ['b'] (toggle off)
    - TerritoryLayer click test: shift+click on a single id → ids contains that id
    - LayerTogglePanel test: rendered options do NOT include 'terrain' row (assert by querying for the row's testid or label)
    - CanvasViewer test (existing CanvasViewer.test.tsx + hydrate + resize + panOnSelect must continue green) — regression safety net
  </behavior>
  <action>
    1. **CanvasViewer.tsx surgical strip** — remove these imports + their usages (every line that references them):
       - `useEditorStore`, `useProjectStore`, `useResearchStore`, `useValidationStore`
       - `EditToolbar`, `SplitTool`, `VertexHandlesLayer`, `SelectionFloatingToolbar`, `ValidationBadgesLayer`, `TerrainBadgesLayer`, `SaveStatusIndicator`, `SettingsPanel`
       - `useRubberBandSelection`, `useUndoShortcut`, `useEditKeyboardMap`, `useBeforeUnloadGuard`
       - `services/persistence`, `services/validation`
       - `api/edit`, `EditApiError`
       - any `'terrain'` LayerName branch (e.g., `if (visibleLayers.terrain) ...`)
       PRESERVE verbatim: ResizeObserver callback-ref pattern (Pitfall 4 — copy lines 96-145 of current file behavior); empty-stage deselect using `e.target === e.target.getStage()` (Pitfall 5); the existing `Konva.clearCache()` IF present in read-only paths (drop only the post-mutation cleanup paths).
       Replace `selectedTerritoryId` reads with `selectSelectedTerritoryId(useUIStore.getState())` selector.
       Result file ≤ 420 LOC. If after the strip the file is still huge (~600+), the file likely had pre-existing concerns (Konva imperative draw helpers); leave them.
    2. **InteractionLayer.tsx** — replace single-id filter with multi-id Set membership (RESEARCH §Code Examples §InteractionLayer multi-select):
       ```tsx
       const selectedIds = useUIStore((s) => s.selectedTerritoryIds)
       const idsSet = new Set(selectedIds)
       const selectedPolygons = territories.filter((t) => idsSet.has(t.id))
       ```
       Render `<Line key={`${t.id}-${i}`} ...>` per polygon (handles MultiPolygon case via index).
    3. **TerritoryLayer.tsx click handler** — rewrite per RESEARCH Pitfall 8:
       ```tsx
       const handleClick = useCallback((id: string, shift: boolean) => {
         const ui = useUIStore.getState()
         const current = ui.selectedTerritoryIds
         if (shift) {
           ui.selectIds(current.includes(id) ? current.filter(x => x !== id) : [...current, id])
         } else {
           ui.selectIds([id])
         }
       }, [])
       ```
       Wire `shift` from Konva event: `evt.evt.shiftKey` (the synthetic Konva event wraps the native one).
       REMOVE all `useEditorStore.getState()` references; REMOVE the terrain-color rendering branch (read color exclusively from condado_colors.json fixture per existing data flow).
    4. **LayerTogglePanel.tsx** — remove the `'terrain'` row + any toggle handler that references it. The 4 layers exposed: background, territory, barony, decorations.
    5. **Empty-stage deselect** — in CanvasViewer's stage-level click handler:
       ```tsx
       onClick={(e) => {
         if (e.target === e.target.getStage()) useUIStore.getState().selectIds([])
       }}
       ```
       Verbatim per Pitfall 5.
    6. Create the 2 new test files for InteractionLayer multi-select + TerritoryLayer shift+click. Reuse vitest + RTL pattern from existing canvas tests; LayerTogglePanel changes covered by extending its existing `__tests__` (or asserting via grep — see acceptance criteria).
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/components/canvas/__tests__/InteractionLayer.multiSelect.test.tsx src/components/canvas/__tests__/TerritoryLayer.shiftClick.test.tsx src/components/canvas/__tests__/CanvasViewer.test.tsx src/components/canvas/__tests__/CanvasViewer.hydrate.test.tsx src/components/canvas/__tests__/CanvasViewer.resize.test.tsx src/components/canvas/__tests__/CanvasViewer.panOnSelect.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 6 vitest files green
    - `grep -nE "useEditorStore|useProjectStore|useResearchStore|useValidationStore|useRubberBandSelection|useUndoShortcut|useEditKeyboardMap|useBeforeUnloadGuard|EditToolbar|SplitTool|VertexHandlesLayer|SelectionFloatingToolbar|ValidationBadgesLayer|TerrainBadgesLayer|SaveStatusIndicator|SettingsPanel|services/persistence|services/validation|api/edit|EditApiError" frontend/src/components/canvas/CanvasViewer.tsx` returns zero
    - `grep -nE "useEditorStore|useProjectStore|'terrain'" frontend/src/components/canvas/TerritoryLayer.tsx` returns zero
    - `grep -n "'terrain'" frontend/src/components/canvas/LayerTogglePanel.tsx` returns zero
    - `wc -l frontend/src/components/canvas/CanvasViewer.tsx` ≤ 420
    - `grep -nE "e\.target === e\.target\.getStage\(\)" frontend/src/components/canvas/CanvasViewer.tsx` shows the canonical empty-stage deselect (Pitfall 5)
  </acceptance_criteria>
  <done>CanvasViewer stripped + 4 tests green; InteractionLayer multi-id; TerritoryLayer shift+click; LayerTogglePanel trimmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add HoverTooltip + MultiSelectInspector + dispatch in InspectorSidebar</name>
  <files>frontend/src/components/canvas/HoverTooltip.tsx, frontend/src/components/canvas/MultiSelectInspector.tsx, frontend/src/components/canvas/InspectorSidebar.tsx, frontend/src/components/canvas/CanvasViewer.tsx, frontend/src/components/canvas/__tests__/HoverTooltip.test.tsx, frontend/src/components/canvas/__tests__/MultiSelectInspector.test.tsx</files>
  <read_first>
    - frontend/src/components/canvas/InspectorSidebar.tsx (D-14 single-select view — DO NOT translate the locked English COPY const)
    - .planning/phases/03-read-only-canvas-redesign/03-UI-SPEC.md (§Hover, §Multi-select Inspector aggregate, §Copywriting Contract — locked English COPY rule)
    - .planning/phases/03-read-only-canvas-redesign/03-RESEARCH.md (Pattern 6 hover tooltip + Pitfall 7 pixel→km² needs metadata.bounds)
  </read_first>
  <behavior>
    - HoverTooltip test: name='Coimbra', x=100, y=200 → div with text 'Coimbra' and style top:200px left:100px
    - HoverTooltip test: name=null → component renders nothing (or returns null)
    - MultiSelectInspector test: selectedIds=['a','b'], metadata with kingdoms/duchies map → renders heading "2 condados selecionados"; sums pixel_count for the 2 condados; renders unique kingdom badges (amber) + duchy badges (blue); ScrollArea lists 2 names
    - MultiSelectInspector test: pixelsToKm2 conversion uses metadata.bounds — feeding bounds=null still renders pixel count (graceful fallback) — OR document that bounds are required (planner pick: keep test asserting bounds=null is gracefully handled)
    - InspectorSidebar test: selectedTerritoryIds=[] → renders "Clique num território para ver detalhes" (PT-BR placeholder, NEW)
    - InspectorSidebar test: selectedTerritoryIds=['a'] → renders existing single-select view with English COPY (PROJECT_OVERVIEW, etc.) UNCHANGED — verbatim assertion
    - InspectorSidebar test: selectedTerritoryIds=['a','b'] → renders MultiSelectInspector
    - CanvasViewer integration: hover layer reports tooltip name to parent state (via callback prop or via a new `useHoverStore`); planner picks — see action item #4
  </behavior>
  <action>
    1. Create `frontend/src/components/canvas/HoverTooltip.tsx`:
       ```tsx
       export function HoverTooltip({ name, x, y }: { name: string | null, x: number, y: number }) {
         if (!name) return null
         return (
           <div style={{ position: 'absolute', left: x + 8, top: y + 8, pointerEvents: 'none', zIndex: 50 }}>
             <Card variant="surface" style={{ padding: '4px 8px' }}>
               <Text size="1">{name}</Text>
             </Card>
           </div>
         )
       }
       ```
    2. Create `frontend/src/components/canvas/MultiSelectInspector.tsx` per UI-SPEC §Multi-select Inspector aggregate:
       - Heading size=3: `${selectedIds.length} condados selecionados`
       - "Área total" Label + pixelsToKm2 sum via existing helper from InspectorSidebar (Pitfall 7: extract `pixelsToKm2(pixels, bounds, mapSize)` to a shared util `frontend/src/lib/pixelsToKm2.ts` if needed; otherwise import from InspectorSidebar's module). Document: extract is preferred → 1 small util file.
       - "Reinos" + Flex with Badge color="amber" variant="soft" per unique kingdom name
       - "Ducados" + Flex with Badge color="blue" variant="soft" per unique duchy name
       - "Condados" + ScrollArea maxHeight=200px with Text size=2 per condado name
    3. Update `frontend/src/components/canvas/InspectorSidebar.tsx`:
       - Replace top-of-render dispatcher: read `useUIStore((s) => s.selectedTerritoryIds)`. Branch on `length`: 0 → placeholder, 1 → existing single-select code (UNCHANGED — locked COPY), ≥2 → MultiSelectInspector. Add the PT-BR placeholder string as a NEW const `PLACEHOLDER_PT = "Clique num território para ver detalhes"` — DO NOT translate the existing English COPY const.
    4. **Hover wiring in CanvasViewer**: add a small piece of local state in `CanvasViewer.tsx`:
       ```tsx
       const [hover, setHover] = useState<{ name: string|null, x: number, y: number }>({ name: null, x: 0, y: 0 })
       ```
       Pass `onHoverEnter(id)` / `onHoverLeave()` callbacks down to `TerritoryLayer`. In `TerritoryLayer`, on `mouseover` of a territory polygon, call `onHoverEnter(territory.id)` + on `mouseout`, call `onHoverLeave()`. The CanvasViewer translates id → name via `metadata.condados[id].name` and reads `stage.getPointerPosition()` (verify ref availability) to derive x/y. Render `<HoverTooltip name={hover.name} x={hover.x} y={hover.y} />` as a sibling of `<Stage>` in the workspace container.
    5. Create the 2 new test files (HoverTooltip + MultiSelectInspector). Extend the existing `InspectorSidebar.test.tsx` (or add a sibling `InspectorSidebar.dispatcher.test.tsx`) with the 3 dispatcher tests.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/components/canvas/__tests__/HoverTooltip.test.tsx src/components/canvas/__tests__/MultiSelectInspector.test.tsx src/components/canvas/__tests__/InspectorSidebar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 3 vitest files green (HoverTooltip + MultiSelectInspector + InspectorSidebar dispatcher)
    - `grep -n "Clique num território para ver detalhes" frontend/src/components/canvas/InspectorSidebar.tsx` shows the PT-BR placeholder
    - `grep -nE "PROJECT_OVERVIEW|PATH_LABEL|CENTROID_LABEL|CAPITAL_LABEL|ADJACENT_LABEL|NO_CAPITAL|NO_NEIGHBORS" frontend/src/components/canvas/InspectorSidebar.tsx` returns the SAME line count as before (English COPY constants untouched)
    - `grep -n "getPointerPosition" frontend/src/components/canvas/CanvasViewer.tsx` shows the hover wiring
    - `grep -n "condados selecionados" frontend/src/components/canvas/MultiSelectInspector.tsx` shows the PT-BR aggregate heading
  </acceptance_criteria>
  <done>HoverTooltip + MultiSelectInspector built; InspectorSidebar dispatches 3 modes; locked English COPY untouched; PT-BR additions verbatim per UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — pure frontend rendering, no network surface) | n/a |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-FE-CANVAS-01 | Tampering | territory id collision could cross-contaminate selection | accept | ids come from territory_metadata.json which is server-emitted (Plan 01); duplicates would already break Phase 01 parity — defense by upstream invariant |
| T-03-FE-CANVAS-02 | DoS | mousemove fires hundreds/sec, hover tooltip re-renders | mitigate | per RESEARCH Pattern 6: throttle is unneeded; React 19 auto-batches; hover layer set listening={false}; only ONE div re-renders per move |
</threat_model>

<verification>
- 8 vitest files green (4 from Task 1 + 3 from Task 2 + InteractionLayer/TerritoryLayer canvas tests)
- CanvasViewer ≤ 420 LOC with zero deleted-module imports
- Empty-stage deselect uses canonical Pitfall 5 pattern
- Hover wiring + multi-select inspector functional
</verification>

<success_criteria>
1. CanvasViewer no longer references any v1 deleted module — SC-3 prerequisite for canvas surface
2. Multi-select (D-17) end-to-end: shift+click → uiStore → InteractionLayer renders multiple gold outlines → MultiSelectInspector renders aggregate
3. Hover (D-15) end-to-end: tooltip appears on mouseover with condado name
4. Empty-stage click clears selection (D-16); InspectorSidebar shows PT-BR placeholder
5. English COPY constants in InspectorSidebar UNCHANGED (test-asserted; PT-BR translation is OUT OF SCOPE here)
</success_criteria>

<output>
After completion, create `.planning/phases/03-read-only-canvas-redesign/03-05-SUMMARY.md`
</output>
