---
phase: 03
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - backend/medieval_forge/api/v3/generate.py
  - backend/medieval_forge/api/v3/status.py
  - backend/medieval_forge/api/v3/artifacts.py
  - backend/medieval_forge/api/v3/__init__.py
  - backend/medieval_forge/main.py
  - backend/tests/unit/test_v3_generate.py
  - backend/tests/unit/test_v3_status.py
  - backend/tests/unit/test_v3_artifacts.py
autonomous: true
requirements: [SC-1, SC-4]
must_haves:
  truths:
    - "POST /api/v3/projects/{id}/generate returns 202 + run_id; sets project.status='generating'; schedules run_pipeline in a worker thread"
    - "GET /api/v3/projects/{id}/generate/stream emits 22 stage events as structured SSE JSON, terminal `done` event, and 200 OK content-type text/event-stream"
    - "GET /api/v3/projects/{id}/status returns {status, has_artifacts, last_generated_at} with has_artifacts boolean per file across the 14-file allowlist (10 Unity + 4 sidecars)"
    - "GET /api/v3/projects/{id}/artifacts/{file_name} serves files from projects/<uuid>/output/ with Cache-Control: immutable; rejects 400 on bad UUID; rejects 404 on file not in allowlist; rejects 404 on path-traversal attempts"
    - "v3 router registered in main.py; trigger_generate updates project.updated_at on success so D-19 cache-bust fires"
  artifacts:
    - path: "backend/medieval_forge/api/v3/generate.py"
      provides: "POST /generate + GET /generate/stream pair (D-22)"
      exports: ["router"]
    - path: "backend/medieval_forge/api/v3/status.py"
      provides: "GET /status manifest endpoint (D-21)"
      exports: ["router"]
    - path: "backend/medieval_forge/api/v3/artifacts.py"
      provides: "GET /artifacts/{file_name} FileResponse + UUID + allowlist + traversal guard (D-18, T-03-01, T-03-05)"
      exports: ["router"]
  key_links:
    - from: "api/v3/generate.py"
      to: "services/pipeline.run_pipeline + cfg.on_stage"
      via: "asyncio.to_thread + _make_on_stage callback bridge"
      pattern: "asyncio\\.to_thread\\(run_pipeline"
    - from: "api/v3/artifacts.py"
      to: "services/paths.is_valid_uuid + project_dir"
      via: "uuid validation + path containment check"
      pattern: "is_valid_uuid|project_dir"
    - from: "main.py"
      to: "api/v3/{generate,status,artifacts}.router"
      via: "include_router calls"
      pattern: "include_router"
---

<objective>
Build the three new v3 endpoints + register them in `main.py`. This plan
mirrors the Phase 02 `api/v3/ingest.py` SSE template, uses `FileResponse`
(not `StaticFiles`) for the artifacts route per RESEARCH §Pattern 2 +
Pitfall 3, and consumes the `cfg.on_stage` callback Plan 01 wired into
`run_pipeline`. Single-flight enforcement via per-project queue map +
409 on overlap.

Purpose: backend half of SC-1 (canvas hydrates from `/artifacts/*`),
SC-4 (artifacts come from `run_pipeline`), and the live progress feed
that Plan 04 consumes for the SSE log panel.

Output: 3 new router files + main.py wiring + 3 backend test modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-read-only-canvas-redesign/03-CONTEXT.md
@.planning/phases/03-read-only-canvas-redesign/03-RESEARCH.md
@CLAUDE.md
@backend/medieval_forge/api/v3/ingest.py
@backend/medieval_forge/api/v3/__init__.py
@backend/medieval_forge/main.py
@backend/medieval_forge/services/paths.py
@backend/medieval_forge/services/pipeline/__init__.py
@backend/medieval_forge/services/pipeline/contracts.py

<interfaces>
SSE envelope (consumed by frontend useRunStore):
  data: {"event_type":"started","stage":null,"message":"...","progress":0.0}
  data: {"event_type":"stage_start","stage":"voronoi","message":"OK","progress":null}
  data: {"event_type":"stage_done","stage":"voronoi","message":"OK","progress":null}
  data: {"event_type":"error","stage":"<stage_or_null>","message":"<exc class>","progress":null}
  data: {"event_type":"done","stage":null,"message":"OK","progress":1.0}

Allowlist (single source of truth — define in api/v3/artifacts.py and import from status.py):
  ARTIFACT_FILES = frozenset({
    "lookup_barony.png", "lookup_condado.png",
    "lookup_barony_colors.json", "lookup_condado_colors.json",
    "territory_metadata.json",
    "visual_condado.png", "visual_barony.png",
    "mountains_mask.png", "rivers_overlay.png",
    "mountain_river_data.json",
    "territories.geojson", "baronies.geojson",
    "condado_colors.json", "barony_colors.json",
  })

Helpers from Plan 01 + existing:
  services.paths.is_valid_uuid(s) -> bool
  services.paths.project_dir(project_id) -> Path  (raises ValueError if escape)
  services.pipeline.run_pipeline(cfg)
  services.pipeline.contracts.RegionConfig (with new on_stage field)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement /artifacts route + /status manifest</name>
  <files>backend/medieval_forge/api/v3/artifacts.py, backend/medieval_forge/api/v3/status.py, backend/tests/unit/test_v3_artifacts.py, backend/tests/unit/test_v3_status.py</files>
  <read_first>
    - backend/medieval_forge/api/v3/ingest.py (router pattern, prefix conventions)
    - backend/medieval_forge/services/paths.py (is_valid_uuid, project_dir signatures + behavior)
    - .planning/phases/03-read-only-canvas-redesign/03-RESEARCH.md (§Code Examples §status.py + §artifacts.py — copy verbatim)
    - .planning/phases/03-read-only-canvas-redesign/03-RESEARCH.md (Pitfall 3 — why FileResponse not StaticFiles)
  </read_first>
  <behavior>
    - Test: GET /api/v3/projects/<bad-id>/artifacts/territory_metadata.json → 400 "must be a valid UUID"
    - Test: GET /api/v3/projects/<valid-uuid>/artifacts/passwd → 404 "not a serveable artifact" (not in allowlist)
    - Test: GET /api/v3/projects/<valid-uuid>/artifacts/../../etc/passwd → 400 or 404 (path traversal blocked)
    - Test: GET /api/v3/projects/<valid-uuid>/artifacts/territory_metadata.json when file absent → 404 "not generated yet"
    - Test: GET /api/v3/projects/<valid-uuid>/artifacts/territory_metadata.json when file present → 200 + JSON body + Cache-Control header contains "immutable"
    - Test: GET /api/v3/projects/<valid-uuid>/status when project absent → 404
    - Test: GET /api/v3/projects/<valid-uuid>/status when project present + no artifacts → 200 + has_artifacts all False + last_generated_at None
    - Test: GET /api/v3/projects/<valid-uuid>/status when project present + 14 artifacts present → has_artifacts all True + last_generated_at == project.updated_at.isoformat()
  </behavior>
  <action>
    1. Create `backend/medieval_forge/api/v3/artifacts.py` per RESEARCH §Code Examples §artifacts.py. Define `ARTIFACT_FILES` as a `frozenset` containing the 14 filenames listed in `<interfaces>` above. Use `is_valid_uuid` first, then membership check (return 404 if not allowlisted), then `project_dir(project_id)` (raises if escape — wrap in try/except → 404), then `(root / "output" / file_name).resolve()` and assert containment via `str(target).startswith(str((root/"output").resolve()))`. Return `FileResponse(target, headers={"Cache-Control": "public, max-age=31536000, immutable"})`.
    2. Create `backend/medieval_forge/api/v3/status.py` per RESEARCH §Code Examples §status.py. Import `ARTIFACT_FILES` from `.artifacts` (single source of truth). Define `StatusResponse(BaseModel)` with `status: str`, `has_artifacts: dict[str, bool]`, `last_generated_at: str | None`. The `has_artifacts` dict iterates `ARTIFACT_FILES` and probes `(out / f).is_file()`. `last_generated_at` is `project.updated_at.isoformat()` when `project.status == "generated"`, else None.
    3. Create `backend/tests/unit/test_v3_artifacts.py` with all 5 artifact tests above + the path-traversal test using `urllib.parse.quote` to send the dotted segment.
    4. Create `backend/tests/unit/test_v3_status.py` with all 3 status tests above.
    5. Use the same `monkeypatch` test seam as `test_v3_ingest.py` for `PROJECTS_ROOT` / DB session factory.
  </action>
  <verify>
    <automated>cd backend && pytest tests/unit/test_v3_artifacts.py tests/unit/test_v3_status.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_v3_artifacts.py -x` exits 0 (≥5 tests green)
    - `pytest tests/unit/test_v3_status.py -x` exits 0 (≥3 tests green)
    - `grep -n "FileResponse" backend/medieval_forge/api/v3/artifacts.py` shows the import + return
    - `grep -n "is_valid_uuid" backend/medieval_forge/api/v3/artifacts.py` shows the validation
    - `grep -n "ARTIFACT_FILES" backend/medieval_forge/api/v3/status.py` shows it imports from .artifacts (no duplicate definition)
  </acceptance_criteria>
  <done>2 endpoints implemented + 8+ unit tests green; allowlist single-sourced; T-03-01 + T-03-05 mitigations present and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement POST /generate + GET /generate/stream pair</name>
  <files>backend/medieval_forge/api/v3/generate.py, backend/tests/unit/test_v3_generate.py</files>
  <read_first>
    - backend/medieval_forge/api/v3/ingest.py (template — _v3_sse_generator + _adapter_producer + _STOP_EVENTS shape)
    - backend/medieval_forge/services/pipeline/__init__.py (run_pipeline signature)
    - backend/medieval_forge/services/pipeline/contracts.py (RegionConfig + on_stage field from Plan 01)
    - .planning/phases/03-read-only-canvas-redesign/03-RESEARCH.md (§Code Examples §generate.py — adapt verbatim)
    - .planning/phases/03-read-only-canvas-redesign/03-RESEARCH.md (Pitfall 9 — refresh-mid-run is documented limitation)
  </read_first>
  <behavior>
    - Test: POST /generate with bad UUID → 400
    - Test: POST /generate when project missing → 404
    - Test: POST /generate when project.status == "generating" AND task is alive → 409 "already generating"
    - Test: POST /generate happy path → 202 + JSON body `{"run_id": "<uuid>", "status": "scheduled"}`; project.status committed to "generating" before response returns
    - Test: GET /generate/stream when no queue → 404 "no active generate run"
    - Test: GET /generate/stream after POST + a stub run_pipeline that calls cfg.on_stage(...) twice → SSE body contains `"event_type": "started"`, `"event_type": "stage_start"` for at least one stage, `"event_type": "done"`, terminator
    - Test: SSE error path → stub run_pipeline raises RuntimeError("boom") → stream emits `"event_type": "error"` + project.status committed to "error_generating"
    - Test: After successful generate, project.updated_at is bumped (D-19 cache-bust precondition)
  </behavior>
  <action>
    1. Create `backend/medieval_forge/api/v3/generate.py`. Copy the structure from RESEARCH §Code Examples §generate.py (`_RUN_QUEUES`, `_RUN_TASKS`, `_STOP_EVENTS`, `_emit`, `_set_status`, `_make_on_stage`, `_generate_producer`, `trigger_generate`, `stream_generate`).
    2. The `_make_on_stage(queue, loop)` factory bridges the sync callback (called from the worker thread) to the asyncio queue using `loop.call_soon_threadsafe(queue.put_nowait, payload_json)`. Map `evt` to `event_type`: `"start"` → `"stage_start"`, `"done"` → `"stage_done"`.
    3. In `_generate_producer`, build `cfg` via `iberia_config()` (or whatever helper Phase 01 exposed; verify by reading `services/pipeline/regions.py` or equivalent). Override `cfg.output_dir = str(project_dir(project_id) / "output")` and `cfg.on_stage = _make_on_stage(queue, asyncio.get_running_loop())`. Call `await asyncio.to_thread(run_pipeline, cfg)`. On success: `_set_status(project_id, "generated", sf)` AND bump `project.updated_at = datetime.utcnow()` in same session (D-19 cache-bust). On exception: emit `error` event with `exc.__class__.__name__` + set status to `error_generating`. Always `queue.put(None)` in finally.
    4. POST handler: `is_valid_uuid` → 400; project lookup → 404; check `_RUN_TASKS.get(project_id)` is alive AND `project.status=="generating"` → 409; otherwise commit `status=generating`, create queue, schedule `_generate_producer` task, return `{"run_id": str(uuid4()), "status": "scheduled"}`.
    5. GET stream handler: `is_valid_uuid` → 400; lookup `_RUN_QUEUES.get(project_id)` → 404 if None; otherwise yield from queue until None sentinel. Headers: `Cache-Control: no-cache`, `X-Accel-Buffering: no`, `Connection: keep-alive`.
    6. Create `backend/tests/unit/test_v3_generate.py`. Use `monkeypatch.setattr` on the `run_pipeline` symbol imported into `api.v3.generate` to stub it with a fake that calls `cfg.on_stage("voronoi","start"); cfg.on_stage("voronoi","done")`. Use `httpx.AsyncClient` against the FastAPI app; consume the SSE body with a streaming reader (mirror the pattern in `test_v3_ingest.py`).
  </action>
  <verify>
    <automated>cd backend && pytest tests/unit/test_v3_generate.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_v3_generate.py -x` exits 0 (≥7 tests green covering bad-uuid, 404, 409, happy path, stream-without-run, stream-happy, stream-error, updated_at-bumped)
    - `grep -n "asyncio.to_thread(run_pipeline" backend/medieval_forge/api/v3/generate.py` shows the worker dispatch
    - `grep -n "call_soon_threadsafe" backend/medieval_forge/api/v3/generate.py` shows the threadsafe bridge
    - `grep -n "updated_at" backend/medieval_forge/api/v3/generate.py` shows the timestamp bump (D-19)
    - 409 path test asserts the error response body includes "already generating"
  </acceptance_criteria>
  <done>POST + GET-SSE pair functioning end-to-end against stubbed run_pipeline; T-03-02 (per-project queue keying) verified; updated_at bumped for D-19.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register all 3 routers in main.py + smoke test</name>
  <files>backend/medieval_forge/api/v3/__init__.py, backend/medieval_forge/main.py, backend/tests/unit/test_v3_routers_registered.py</files>
  <read_first>
    - backend/medieval_forge/main.py (current router registrations + lifespan body)
    - backend/medieval_forge/api/v3/__init__.py (export pattern)
    - backend/medieval_forge/api/v3/ingest.py (existing v3 router as reference)
  </read_first>
  <behavior>
    - Test: `app.routes` contains a route matching `/api/v3/projects/{project_id}/generate` (POST), `/api/v3/projects/{project_id}/generate/stream` (GET), `/api/v3/projects/{project_id}/status` (GET), `/api/v3/projects/{project_id}/artifacts/{file_name}` (GET)
    - Test: `app.routes` still contains the Phase 02 `/api/v3/projects/{project_id}/ingest` route (regression safety)
    - Test: import-time side effects don't break — `from medieval_forge.main import app` succeeds without LLM modules being importable (this fails today and is the gate that Plan 07's purge eventually clears; for THIS plan, we keep current main.py imports and only ADD the 3 new include_router calls)
  </behavior>
  <action>
    1. In `backend/medieval_forge/api/v3/__init__.py`, re-export the 3 new routers alongside `ingest`:
       ```python
       from .ingest import router as ingest_router
       from .generate import router as generate_router
       from .status import router as status_router
       from .artifacts import router as artifacts_router
       __all__ = ["ingest_router", "generate_router", "status_router", "artifacts_router"]
       ```
    2. In `backend/medieval_forge/main.py`, add three new `app.include_router(...)` lines next to the existing v3 ingest registration. Each router already declares `prefix="/v3/projects"` so the global API prefix `/api` (verify the existing convention) yields the correct paths. Do NOT touch the lifespan body or v1 router registrations in this plan — Plan 07 owns the lifespan shrink + v1 deletes.
    3. Create `backend/tests/unit/test_v3_routers_registered.py` with the 3 route-presence assertions above. Use `app.routes` introspection (each `Route.path` attr).
  </action>
  <verify>
    <automated>cd backend && pytest tests/unit/test_v3_routers_registered.py tests/unit/test_v3_artifacts.py tests/unit/test_v3_status.py tests/unit/test_v3_generate.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_v3_routers_registered.py -x` exits 0
    - `curl -s http://localhost:8000/openapi.json | grep -c "/api/v3/projects"` ≥ 4 (after dev server boot — manual smoke during integration)
    - All Plan 02 unit tests green collectively
    - Phase 01 parity test still 10/10 green
  </acceptance_criteria>
  <done>3 routers registered in main.py; smoke test confirms route presence; existing routes untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/v3/projects/{id}/artifacts/* | path traversal + arbitrary-file disclosure surface |
| browser → /api/v3/projects/{id}/generate | DoS / overlap surface + state-mutation |
| browser → /api/v3/projects/{id}/generate/stream | cross-project event leak surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering / Information Disclosure | api/v3/artifacts.py path handling | mitigate | is_valid_uuid first → 400; allowlist membership → 404; project_dir() containment check (raises on escape); .resolve() + startswith(output_dir.resolve()) defense-in-depth. Tested explicitly in test_v3_artifacts.py. |
| T-03-02 | Information Disclosure | api/v3/generate.py SSE stream | mitigate | _RUN_QUEUES keyed by project_id; per-project task tracking; no broadcast across projects. Local-only posture (D-20) reduces residual risk. |
| T-03-03 | DoS | api/v3/generate.py | accept | 409 single-flight gate prevents trivial overlap; run_pipeline ~10s, no rate limiter needed for local-only deployment per D-20. |
| T-03-05 | Information Disclosure | api/v3/artifacts.py allowlist | mitigate | ARTIFACT_FILES frozenset enumerates the 14 serveable files; any other path returns 404. Single source of truth in artifacts.py; status.py imports it. |
</threat_model>

<verification>
- `pytest tests/unit/test_v3_artifacts.py tests/unit/test_v3_status.py tests/unit/test_v3_generate.py tests/unit/test_v3_routers_registered.py -x` all green
- Phase 01 parity 10/10 still green
- Manual smoke (deferred to Plan 08 UAT): `curl -X POST http://localhost:8000/api/v3/projects/<seeded-uuid>/generate` returns 202; `curl http://localhost:8000/api/v3/projects/<seeded-uuid>/generate/stream` streams events
</verification>

<success_criteria>
1. 3 new endpoints registered + tested
2. T-03-01, T-03-02, T-03-05 mitigations present + asserted in tests
3. cfg.on_stage callback bridge produces SSE events Plan 04 frontend will consume
4. project.updated_at bumped on success (D-19 cache-bust precondition met)
5. No regression to Phase 02 ingest or Phase 01 parity
</success_criteria>

<output>
After completion, create `.planning/phases/03-read-only-canvas-redesign/03-02-SUMMARY.md`
</output>
