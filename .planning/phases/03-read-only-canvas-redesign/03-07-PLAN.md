---
phase: 03
plan: 07
type: execute
wave: 3
depends_on: [02, 06]
files_modified:
  - backend/medieval_forge/api/ingest.py
  - backend/medieval_forge/api/auth.py
  - backend/medieval_forge/api/research.py
  - backend/medieval_forge/api/codex.py
  - backend/medieval_forge/api/llm.py
  - backend/medieval_forge/services/ingest_runner.py
  - backend/medieval_forge/services/ingest_wikidata.py
  - backend/medieval_forge/services/research_runner.py
  - backend/medieval_forge/services/research_cache.py
  - backend/medieval_forge/services/codex_runner.py
  - backend/medieval_forge/services/codex_cache.py
  - backend/medieval_forge/services/credential_store.py
  - backend/medieval_forge/services/llm/
  - backend/medieval_forge/services/territory_builder.py
  - backend/medieval_forge/models.py
  - backend/medieval_forge/main.py
autonomous: true
requirements: [SC-3, SC-4]
must_haves:
  truths:
    - "v1 ingest endpoint /api/projects/{id}/ingest is removed; only /api/v3/projects/{id}/ingest survives"
    - "All LLM-related backend modules listed in D-13 deleted: api/auth.py, api/research.py, api/codex.py, api/llm.py, services/research_runner.py, services/research_cache.py, services/codex_runner.py, services/codex_cache.py, services/credential_store.py, services/llm/ subdir, services/territory_builder.py"
    - "models.LLMCredential, models.ResearchCache, models.CodexCache deleted from models.py"
    - "main.py lifespan body shrinks: drops app.state.credentials preload + app.state.oauth_states; keeps Base.metadata.create_all for the surviving Project table"
    - "main.py removes include_router calls for: ingest_router, auth_router, research_router, codex_router, llm_router, edit_api"
    - "Phase 02 v3 ingest still green (depends_on:[02] proven by retest)"
    - "Phase 01 parity test still 10/10 green"
  artifacts:
    - path: "backend/medieval_forge/api/auth.py"
      provides: "DELETED (D-13 audit conclusion — no surviving consumer)"
    - path: "backend/medieval_forge/services/credential_store.py"
      provides: "DELETED (D-13 audit conclusion)"
    - path: "backend/medieval_forge/models.py"
      provides: "Shrunk: Project survives; LLMCredential / ResearchCache / CodexCache deleted"
  key_links:
    - from: "main.py lifespan"
      to: "Base.metadata.create_all (Project only)"
      via: "shrunk SQLAlchemy bootstrap"
      pattern: "Base.metadata.create_all"
---

<objective>
Backend deletion sweep: closes D-12 (v1 ingest endpoints + ingest_runner
+ ingest_wikidata) and D-13 (full LLM stack including
auth.py + credential_store.py + LLMCredential + ResearchCache +
CodexCache models per RESEARCH §LLM consumer graph audit conclusion).
Shrinks `main.py` lifespan to drop `app.state.credentials` /
`app.state.oauth_states` preload and the now-orphan router registrations.

Two tasks: (1) delete v1 ingest backend (D-12) — gated on Plan 01's
`_write_geojson_atomic` lift; (2) delete LLM backend stack (D-13) —
gated on Plan 06 frontend purge so no surviving frontend caller exists.

Purpose: SC-3 (no console errors at any layer; LLM dependencies untangle
cleanly) + SC-4 (Phase 01 parity stays green).

Output: ~14 deleted backend files + models.py shrunk + main.py shrunk;
Phase 01 + Phase 02 v3 tests still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-read-only-canvas-redesign/03-CONTEXT.md
@.planning/phases/03-read-only-canvas-redesign/03-RESEARCH.md
@CLAUDE.md
@backend/medieval_forge/main.py
@backend/medieval_forge/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete v1 ingest backend (D-12)</name>
  <files>backend/medieval_forge/api/ingest.py, backend/medieval_forge/services/ingest_runner.py, backend/medieval_forge/services/ingest_wikidata.py, backend/medieval_forge/main.py</files>
  <read_first>
    - backend/medieval_forge/main.py (find ingest_router include + import; remove BOTH)
    - backend/medieval_forge/api/ingest.py (legacy v1 endpoint — delete entire file)
    - backend/medieval_forge/services/ingest_runner.py (Plan 01 already lifted _write_geojson_atomic; verify the re-export stub is the only thing here OR delete cleanly if Plan 01 left only the re-export)
    - backend/medieval_forge/services/pipeline/adapters/base.py (verify it imports _write_geojson_atomic from ..paths now, NOT from ..ingest_runner)
    - backend/medieval_forge/services/ingest_terrain/runner.py (verify same)
    - .planning/phases/03-read-only-canvas-redesign/03-RESEARCH.md (Pitfall 2 — gating dependency)
  </read_first>
  <action>
    1. Verify Plan 01 succeeded: `grep -rn "from .ingest_runner import \|from ..ingest_runner import " backend/medieval_forge/` should return zero (or only the re-export stub inside `ingest_runner.py` itself).
    2. Delete `backend/medieval_forge/api/ingest.py` and its test `backend/tests/unit/test_v1_ingest_*.py` (or `test_ingest.py` — find via `find backend/tests -name "*ingest*"` and prune the v1-only files; preserve any `test_v3_ingest*.py`).
    3. Delete `backend/medieval_forge/services/ingest_runner.py` (the re-export stub from Plan 01 dies here) and its test `backend/tests/unit/test_ingest_runner.py` (or analogous).
    4. Delete `backend/medieval_forge/services/ingest_wikidata.py` and its test (`test_ingest_wikidata.py` etc.).
    5. In `backend/medieval_forge/main.py`:
       - Remove `from .api.ingest import router as ingest_router`
       - Remove `app.include_router(ingest_router, prefix="/api")` (or whatever shape)
       - Keep the v3 ingest router (Phase 02) untouched.
    6. Run `pytest -m "not parity" -x` → expect green (no broken imports).
    7. Run `pytest tests/parity/test_iberia_868.py -x` + `pytest tests/unit/test_v3_ingest.py tests/unit/test_v3_generate.py tests/unit/test_v3_status.py tests/unit/test_v3_artifacts.py -x` → all green.
  </action>
  <acceptance_criteria>
    - `find backend/medieval_forge/api -name "ingest.py" 2>&1 | grep -v "No such"` returns nothing (file deleted)
    - `find backend/medieval_forge/services -name "ingest_runner.py" -o -name "ingest_wikidata.py" 2>&1 | grep -v "No such"` returns nothing
    - `grep -rn "from .ingest_runner\|from .ingest_wikidata\|from .api.ingest import" backend/medieval_forge/` returns zero
    - `cd backend && pytest tests/parity/test_iberia_868.py -x` exits 0 (10/10 + sidecar assertion)
    - `cd backend && pytest -m "not parity" -x` exits 0
    - `grep -n "ingest_router" backend/medieval_forge/main.py` returns zero (only v3 ingest router survives, named differently)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && pytest tests/parity/test_iberia_868.py tests/unit/test_v3_ingest.py tests/unit/test_v3_generate.py tests/unit/test_v3_status.py tests/unit/test_v3_artifacts.py -x</automated>
  </verify>
  <done>D-12 deletes complete; Phase 01 parity + Phase 02 v3 ingest + Plan 02 v3 generate/status/artifacts all green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete LLM backend stack (D-13) + shrink main.py lifespan + shrink models.py</name>
  <files>backend/medieval_forge/api/auth.py, backend/medieval_forge/api/research.py, backend/medieval_forge/api/codex.py, backend/medieval_forge/api/llm.py, backend/medieval_forge/services/research_runner.py, backend/medieval_forge/services/research_cache.py, backend/medieval_forge/services/codex_runner.py, backend/medieval_forge/services/codex_cache.py, backend/medieval_forge/services/credential_store.py, backend/medieval_forge/services/llm/, backend/medieval_forge/services/territory_builder.py, backend/medieval_forge/models.py, backend/medieval_forge/main.py</files>
  <read_first>
    - backend/medieval_forge/main.py (lifespan body lines 30-37 per RESEARCH; identify the credentials preload + oauth_states init)
    - backend/medieval_forge/models.py (LLMCredential lines 44-66, ResearchCache lines 69-80, CodexCache lines 83-100 per RESEARCH)
    - backend/medieval_forge/services/credential_store.py (verify NO surviving consumer outside D-13 set via grep)
    - .planning/phases/03-read-only-canvas-redesign/03-RESEARCH.md (§LLM consumer graph — D-13 audit conclusion: DELETE EVERYTHING)
  </read_first>
  <action>
    1. Pre-flight grep audit (must pass before any delete):
       ```bash
       grep -rn "credential_store\|LLMCredential\|ResearchCache\|CodexCache\|services/llm\|services/research_runner\|services/research_cache\|services/codex_runner\|services/codex_cache\|services/territory_builder\|api/auth\|api/research\|api/codex\|api/llm\|app.state.credentials\|app.state.oauth_states" backend/medieval_forge/ --include="*.py"
       ```
       Every match must be in one of the to-be-deleted files OR in `main.py` (lifespan body — to be edited). If matches in any other file, STOP and resolve (likely an audit miss).
    2. Delete files in this order (avoid intermittent ImportErrors):
       (a) Delete `services/llm/` subdir entirely (`rm -rf backend/medieval_forge/services/llm/`)
       (b) Delete `api/auth.py`, `api/research.py`, `api/codex.py`, `api/llm.py`
       (c) Delete `services/research_runner.py`, `services/research_cache.py`, `services/codex_runner.py`, `services/codex_cache.py`, `services/credential_store.py`, `services/territory_builder.py`
       (d) Delete all sibling `__tests__/` or `tests/unit/test_<name>*.py` matching the deleted modules. The deletion ledger from RESEARCH.md §LLM consumer graph lists them: test_auth_session, test_oauth_flow, test_research_cache, test_research_runner_map_path, test_codex_*, test_llm_*, test_assignment_edit, test_research_sse, test_codex_endpoints, test_providers_endpoint, test_territory_builder*, test_condado_assignment, test_cli_piggyback, test_llm_routing, test_codex_runner, test_codex_schema, test_codex_prompt, test_llamacpp_provider, test_map_research_prompt, test_llm_schemas, test_llm_retry, test_llm_registry, test_llm_providers, test_barony_assignments_validation. Find via `find backend/tests -name "test_*.py"` + match the prefixes; delete confirmed v1-LLM tests.
    3. Shrink `models.py`: delete `class LLMCredential`, `class ResearchCache`, `class CodexCache`. Keep `class Project` and any other non-LLM models. Keep `Base = declarative_base()` declaration.
    4. Shrink `main.py`:
       - Remove imports: `from .api.auth import router as auth_router`, `.api.research`, `.api.codex`, `.api.llm`, and any `from .services import credential_store` / `from .services.llm import ...`.
       - Remove `app.include_router(auth_router, ...)` and analogous for research/codex/llm.
       - In the lifespan body, REMOVE: `app.state.credentials = credential_store.preload(...)` and `app.state.oauth_states = {}`. Keep `Base.metadata.create_all(...)`. Confirm the lifespan still creates the surviving Project table.
       - Audit `from .api import edit as edit_api` and `app.include_router(edit_api.router, ...)` — both should be gone (api/edit.ts was a frontend-only file; backend `api/edit.py` was deleted as part of D-13 if it existed, or was never present — verify with `ls backend/medieval_forge/api/`). If a backend `api/edit.py` exists, add it to deletion list.
    5. AUDIT `api/export.py` — Plan 04 wires "Exportar ZIP" to `/api/projects/{id}/export`. Verify `api/export.py` (a) exists, (b) reads from `projects/{id}/output/` (the v3 layout), (c) does NOT import any LLM module. If it does any LLM imports, refactor minimally OR document a follow-up task. DO NOT delete `api/export.py` — Plan 04's button depends on it. If it's broken against new layout, add a small fix here (separate commit).
    6. Run final backend test sweep:
       ```bash
       cd backend && pytest tests/parity/test_iberia_868.py tests/unit/ -x
       ```
       Expected: all green. Phase 01 parity 10/10 + Phase 02 v3 ingest + Plan 02 v3 endpoints.
    7. Smoke test main.py importability: `python -c "from medieval_forge.main import app; print(len(app.routes))"` must succeed without ImportError.
  </action>
  <acceptance_criteria>
    - `grep -rn "credential_store\|LLMCredential\|ResearchCache\|CodexCache\|app.state.credentials\|app.state.oauth_states" backend/medieval_forge/ --include="*.py"` returns zero
    - `find backend/medieval_forge/services/llm 2>&1 | grep -v "No such"` returns nothing
    - `find backend/medieval_forge/api -name "auth.py" -o -name "research.py" -o -name "codex.py" -o -name "llm.py" 2>&1 | grep -v "No such"` returns nothing
    - `find backend/medieval_forge/services -name "research_runner.py" -o -name "research_cache.py" -o -name "codex_runner.py" -o -name "codex_cache.py" -o -name "credential_store.py" -o -name "territory_builder.py" 2>&1 | grep -v "No such"` returns nothing
    - `grep -n "class LLMCredential\|class ResearchCache\|class CodexCache" backend/medieval_forge/models.py` returns zero
    - `cd backend && pytest tests/parity/test_iberia_868.py -x` exits 0 (10/10 + sidecar assertion)
    - `cd backend && pytest -m "not parity" -x` exits 0
    - `python -c "from medieval_forge.main import app"` succeeds
    - `api/export.py` survives + still routes correctly (audit documented in SUMMARY)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && pytest tests/parity/test_iberia_868.py tests/unit/ -x</automated>
  </verify>
  <done>D-13 deletes complete; main.py + models.py shrunk; Phase 01 parity + Plan 02 endpoints green; export.py audited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none added — all deletion; existing v3 endpoint boundaries unchanged) | n/a |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure / Tampering | stale credential code path could surface old API keys if any consumer survives | mitigate | full grep audit before delete; main.py lifespan loses app.state.credentials; LLMCredential model deleted; orphan SQLite table left in user DB is harmless per RESEARCH §Runtime State Inventory |
| T-03-BE-PURGE-01 | DoS | broken main.py importability post-delete | mitigate | Task 2 acceptance: `python -c "from medieval_forge.main import app"` must succeed |
| T-03-BE-PURGE-02 | Tampering | parity regression from accidental pipeline-related deletion | mitigate | Phase 01 parity test gate at end of each task |
</threat_model>

<verification>
- All grep audits return zero for deletion targets
- pytest parity + unit suites green
- main.py importable
</verification>

<success_criteria>
1. SC-3 prerequisite for backend: zero references to deleted v1/LLM modules
2. SC-4 preserved: Phase 01 parity 10/10 + Plan 02 endpoints green
3. T-03-04 mitigated: no stale credential code path
4. main.py importable; lifespan shrunk; v3 routers survive
5. export.py audited (kept + verified, OR a follow-up task documented)
</success_criteria>

<output>
After completion, create `.planning/phases/03-read-only-canvas-redesign/03-07-SUMMARY.md`
</output>
