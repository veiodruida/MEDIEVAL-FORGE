---
phase: 03
plan: 06
type: execute
wave: 3
depends_on: [04, 05]
files_modified:
  - frontend/src/pages/TerritoryEditor.tsx
  - frontend/src/components/pipeline/
  - frontend/src/components/research/
  - frontend/src/components/codex/
  - frontend/src/components/ingest/BaronyGranularitySlider.tsx
  - frontend/src/components/canvas/EditToolbar.tsx
  - frontend/src/components/canvas/SplitTool.tsx
  - frontend/src/components/canvas/VertexHandlesLayer.tsx
  - frontend/src/components/canvas/SelectionFloatingToolbar.tsx
  - frontend/src/components/canvas/ValidationBadgesLayer.tsx
  - frontend/src/components/canvas/TerrainBadgesLayer.tsx
  - frontend/src/components/canvas/SaveStatusIndicator.tsx
  - frontend/src/components/canvas/SettingsPanel.tsx
  - frontend/src/stores/usePipelineStore.ts
  - frontend/src/stores/useResearchStore.ts
  - frontend/src/stores/useEditorStore.ts
  - frontend/src/stores/useValidationStore.ts
  - frontend/src/stores/useProjectStore.ts
  - frontend/src/hooks/useResearchStream.ts
  - frontend/src/hooks/useCodexStream.ts
  - frontend/src/hooks/useUndoShortcut.ts
  - frontend/src/hooks/useBeforeUnloadGuard.ts
  - frontend/src/hooks/useEditKeyboardMap.ts
  - frontend/src/hooks/useRubberBandSelection.ts
  - frontend/src/api/edit.ts
  - frontend/src/api/research.ts
  - frontend/src/api/codex.ts
  - frontend/src/api/useTerrainStepStream.ts
  - frontend/src/api/client.ts
  - frontend/src/services/persistence.ts
  - frontend/src/services/validation.ts
  - frontend/src/App.tsx
autonomous: true
requirements: [SC-3]
must_haves:
  truths:
    - "All v1 frontend modules listed in CONTEXT D-10/D-11/D-13 are deleted; their __tests__ deleted alongside"
    - "Dangling-import grep returns ZERO results for the deletion set across frontend/src/"
    - "App.tsx route registrations no longer reference deleted pages (e.g., /projects/:id/edit removed)"
    - "api/client.ts loses v1-only hooks (useIngestStream, useGenerate, useExport, useTerritoryTemplate, useRenderModern, useIngestStatus)"
    - "npm run build completes with no errors; npm run test completes green"
    - "vite + tsc no warnings about missing modules"
  artifacts:
    - path: "frontend/src/components/pipeline/"
      provides: "DELETED (D-11)"
    - path: "frontend/src/components/research/"
      provides: "DELETED (D-13)"
    - path: "frontend/src/components/codex/"
      provides: "DELETED (D-13)"
    - path: "frontend/src/pages/TerritoryEditor.tsx"
      provides: "DELETED (D-10)"
  key_links:
    - from: "App.tsx"
      to: "pages/ProjectDetail.tsx (Plan 04 rewrite)"
      via: "<Route path=\"/projects/:id\">"
      pattern: "ProjectDetail"
---

<objective>
Mechanical deletion sweep. Closes D-10 (edit-only components), D-11 (v1
stepper UI purge), D-13 (frontend LLM purge). Includes a dangling-import
grep gate that BLOCKS the plan from being marked complete unless every
deletion target is also gone from every other file's import graph
(SC-3 prerequisite per Pitfall 1).

Two tasks: (1) delete edit-only graph (D-10) and run grep; (2) delete
v1 stepper + LLM frontend (D-11+D-13) and run final grep + npm build.

Purpose: SC-3 — old stepper invisible, no console errors, no broken
imports.

Output: ~30 deleted files; tests deleted alongside; api/client.ts
shrunk; App.tsx route updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-read-only-canvas-redesign/03-CONTEXT.md
@.planning/phases/03-read-only-canvas-redesign/03-RESEARCH.md
@CLAUDE.md
@frontend/src/App.tsx
@frontend/src/api/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete edit-only graph (D-10) + grep gate</name>
  <files>frontend/src/pages/TerritoryEditor.tsx, frontend/src/components/canvas/EditToolbar.tsx, frontend/src/components/canvas/SplitTool.tsx, frontend/src/components/canvas/VertexHandlesLayer.tsx, frontend/src/components/canvas/SelectionFloatingToolbar.tsx, frontend/src/components/canvas/ValidationBadgesLayer.tsx, frontend/src/components/canvas/TerrainBadgesLayer.tsx, frontend/src/components/canvas/SaveStatusIndicator.tsx, frontend/src/components/canvas/SettingsPanel.tsx, frontend/src/stores/useEditorStore.ts, frontend/src/stores/useValidationStore.ts, frontend/src/stores/useProjectStore.ts, frontend/src/hooks/useUndoShortcut.ts, frontend/src/hooks/useBeforeUnloadGuard.ts, frontend/src/hooks/useEditKeyboardMap.ts, frontend/src/hooks/useRubberBandSelection.ts, frontend/src/api/edit.ts, frontend/src/services/persistence.ts, frontend/src/services/validation.ts, frontend/src/components/research/AssignmentEditor.tsx, frontend/src/App.tsx</files>
  <read_first>
    - .planning/phases/03-read-only-canvas-redesign/03-CONTEXT.md (D-10 list)
    - .planning/phases/03-read-only-canvas-redesign/03-RESEARCH.md (Pitfall 1 grep pattern; §Frontend consumer graph audit)
    - frontend/src/App.tsx (find /projects/:id/edit route to remove)
  </read_first>
  <action>
    1. Delete the files listed in `<files>` for this task. For each component file (EditToolbar.tsx etc.), also delete its `__tests__/EditToolbar.test.tsx` sibling (and analogous patterns: split, vertex, save-status, settings, rubberband, undo, beforeUnload, editKeyboardMap, validation, persistence, etc.). Delete `frontend/src/components/research/AssignmentEditor.tsx` + its test as part of D-10 (research subdir continues in Task 2).
    2. Update `App.tsx`: remove the `/projects/:id/edit` route + its lazy import of `TerritoryEditor`. Verify `<Route path="/projects/:id">` still points at the rewritten `ProjectDetail` from Plan 04.
    3. Run the dangling-import grep (Pitfall 1, partial — D-10 subset):
       ```bash
       grep -rn "useEditorStore\|useValidationStore\|useProjectStore\|useUndoShortcut\|useBeforeUnloadGuard\|useEditKeyboardMap\|useRubberBandSelection\|EditToolbar\|SplitTool\|VertexHandlesLayer\|SelectionFloatingToolbar\|ValidationBadgesLayer\|TerrainBadgesLayer\|SaveStatusIndicator\|SettingsPanel\|TerritoryEditor\|api/edit\|EditApiError\|services/persistence\|services/validation\|AssignmentEditor" frontend/src/
       ```
       Expected: ZERO matches. If matches found, fix the offending file (most likely Plan 05's CanvasViewer strip missed something).
    4. Run `npm run build` — expect no errors mentioning these symbols.
  </action>
  <acceptance_criteria>
    - The Pitfall-1 grep above returns zero matches
    - `cd frontend && npm run build` exits 0
    - `cd frontend && npm run test -- --run` exits 0 (existing test suite green; deleted tests gone)
    - `find frontend/src/pages -name "TerritoryEditor.tsx"` returns nothing (file deleted)
    - `grep -n "TerritoryEditor\|/projects/:id/edit" frontend/src/App.tsx` returns zero
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -30 && npm run test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>D-10 deletions complete; grep clean; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete v1 stepper + LLM frontend (D-11 + D-13) + final grep + npm build</name>
  <files>frontend/src/components/pipeline/, frontend/src/components/research/, frontend/src/components/codex/, frontend/src/components/ingest/BaronyGranularitySlider.tsx, frontend/src/stores/usePipelineStore.ts, frontend/src/stores/useResearchStore.ts, frontend/src/hooks/useResearchStream.ts, frontend/src/hooks/useCodexStream.ts, frontend/src/api/research.ts, frontend/src/api/codex.ts, frontend/src/api/useTerrainStepStream.ts, frontend/src/api/client.ts</files>
  <read_first>
    - .planning/phases/03-read-only-canvas-redesign/03-CONTEXT.md (D-11 + D-13 lists)
    - .planning/phases/03-read-only-canvas-redesign/03-RESEARCH.md (Pitfall 1 full grep pattern)
    - frontend/src/api/client.ts (identify v1-only hooks to drop: useIngestStream, useGenerate, useExport, useTerritoryTemplate, useRenderModern, useIngestStatus — verify each via grep before delete)
  </read_first>
  <action>
    1. Recursively delete the entire subdirectories: `frontend/src/components/pipeline/`, `frontend/src/components/research/`, `frontend/src/components/codex/`. Delete `frontend/src/components/ingest/BaronyGranularitySlider.tsx` + its test.
    2. Delete the listed stores, hooks, and api files in `<files>`. Delete sibling `__tests__/*.test.ts(x)` for each.
    3. Surgically shrink `frontend/src/api/client.ts`: remove these hooks (keep `useProject`, `useProjects`, `useStatusManifest` from Plan 04, and any other surviving generic hooks):
       - `useIngestStream`, `useGenerate` (v1 generate), `useExport` (audit: keep ONLY if Plan 04's "Exportar ZIP" wires through it AND it still works against `projects/{id}/output/`; otherwise delete and update Plan 04's button to call the URL directly), `useTerritoryTemplate`, `useRenderModern`, `useIngestStatus`.
       For each, grep `frontend/src/` for usages — expect zero before deletion.
    4. Run the FULL Pitfall-1 grep (Pitfall 1 in RESEARCH.md):
       ```bash
       grep -rn "components/pipeline\|usePipelineStore\|useResearchStore\|useEditorStore\|useValidationStore\|useProjectStore\|useResearchStream\|useCodexStream\|useRubberBandSelection\|useUndoShortcut\|useEditKeyboardMap\|useBeforeUnloadGuard\|api/edit\|api/research\|api/codex\|EditApiError\|TerritoryEditor\|EditToolbar\|SplitTool\|VertexHandlesLayer\|SelectionFloatingToolbar\|ValidationBadgesLayer\|TerrainBadgesLayer\|SaveStatusIndicator\|persistence\|services/validation\|TerrainDataSection\|AssignmentEditor\|ResearchDialog\|ProviderEffortPicker\|BaronyGranularitySlider\|CodexViewer\|AuthSetupSheet\|ProviderSelector\|Stepper\|StepCard" frontend/src/
       ```
       Expected: ZERO. If matches found, fix or delete the offending file.
    5. `npm run build` + `npm run test` + a fresh `tsc --noEmit` MUST all be green.
  </action>
  <acceptance_criteria>
    - The full Pitfall-1 grep returns zero matches across `frontend/src/`
    - `cd frontend && npm run build 2>&1 | tail -30` shows successful Vite build, no "Module not found" warnings
    - `cd frontend && npm run test -- --run 2>&1 | tail -20` exits 0
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | tail -10` shows zero errors
    - `find frontend/src/components/pipeline frontend/src/components/research frontend/src/components/codex 2>&1 | grep -v "No such file"` returns nothing (dirs gone)
    - `grep -nE "useIngestStream|useGenerate|useExport|useTerritoryTemplate|useRenderModern|useIngestStatus" frontend/src/api/client.ts` returns zero
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -10 && npm run test -- --run 2>&1 | tail -20 && npx tsc --noEmit -p tsconfig.app.json 2>&1 | tail -5</automated>
  </verify>
  <done>D-11 + D-13 frontend deletions complete; full Pitfall-1 grep returns zero; vite build + vitest + tsc all green; SC-3 prerequisite met for frontend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — pure deletion plan; no new attack surface) | n/a |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-FE-PURGE-01 | Tampering | dangling import causes runtime crash post-deploy | mitigate | full Pitfall-1 grep gate + npm build + tsc --noEmit + vitest all required green before plan completes |
| T-03-FE-PURGE-02 | Information Disclosure | deleted code paths could leave stale browser cache | accept | TanStack Query keys distinct (`/preview/*` cache evicted naturally on tab close per Runtime State Inventory); local-only posture (D-20) |
</threat_model>

<verification>
- All Pitfall-1 grep results zero
- Vite build + vitest + tsc green
- ~30 files deleted; deletion ledger documented in SUMMARY
</verification>

<success_criteria>
1. SC-3 prerequisite for FE: zero references to deleted v1 modules
2. npm run build + npm run test + tsc all green
3. App.tsx routes only to surviving pages
</success_criteria>

<output>
After completion, create `.planning/phases/03-read-only-canvas-redesign/03-06-SUMMARY.md`
</output>
