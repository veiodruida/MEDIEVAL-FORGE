---
phase: 03
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/medieval_forge/services/paths.py
  - backend/medieval_forge/services/ingest_runner.py
  - backend/medieval_forge/services/pipeline/adapters/base.py
  - backend/medieval_forge/services/ingest_terrain/runner.py
  - backend/medieval_forge/services/pipeline/contracts.py
  - backend/medieval_forge/services/pipeline/__init__.py
  - backend/medieval_forge/services/territories_geojson.py
  - backend/medieval_forge/services/baronies_builder.py
  - backend/tests/parity/test_iberia_868.py
  - backend/tests/unit/test_paths_write_geojson_atomic.py
autonomous: true
requirements: [SC-1, SC-4]
must_haves:
  truths:
    - "run_pipeline emits territories.geojson + baronies.geojson + condado_colors.json + barony_colors.json into cfg.output_dir alongside the 10 Unity contract files"
    - "_write_geojson_atomic exists in services/paths.py; old import sites resolve from there"
    - "RegionConfig accepts an optional cfg.on_stage callable; default None preserves Phase 01 parity"
  artifacts:
    - path: "backend/medieval_forge/services/paths.py"
      provides: "_write_geojson_atomic helper"
      contains: "def _write_geojson_atomic"
    - path: "backend/medieval_forge/services/pipeline/contracts.py"
      provides: "cfg.on_stage field on RegionConfig"
      contains: "on_stage"
    - path: "backend/medieval_forge/services/pipeline/__init__.py"
      provides: "11-stage on_stage hooks + canvas-sidecar emission step"
      contains: "on_stage"
    - path: "backend/tests/parity/test_iberia_868.py"
      provides: "test_canvas_sidecars_exist assertion (4 sidecar files)"
      contains: "test_canvas_sidecars_exist"
  key_links:
    - from: "services/pipeline/adapters/base.py"
      to: "services/paths.py"
      via: "from ..paths import _write_geojson_atomic"
      pattern: "from \\.\\.paths import _write_geojson_atomic"
    - from: "services/ingest_terrain/runner.py"
      to: "services/paths.py"
      via: "from ..paths import _write_geojson_atomic"
      pattern: "from \\.\\.paths import _write_geojson_atomic"
    - from: "services/pipeline/__init__.py"
      to: "services/territories_geojson.py + services/baronies_builder.py"
      via: "final-step call after rivers stage"
      pattern: "build_territories_geojson|build_baronies_geojson"
---

<objective>
Wave 0 BLOCKER: extend `run_pipeline` to emit the four canvas-sidecar files
(`territories.geojson`, `baronies.geojson`, `condado_colors.json`,
`barony_colors.json`) so that the new `/artifacts/*` route can serve them
to `useCanvasArtifacts`. Without this, SC-1 fails on first canvas hydration
(404 on every sidecar). In the same plan, lift `_write_geojson_atomic`
from the to-be-deleted `services/ingest_runner.py` into `services/paths.py`
so Plan 07 can delete `ingest_runner.py` cleanly. Add an optional
`cfg.on_stage: Callable[[str, str], None] | None = None` callback slot
on `RegionConfig` and sprinkle 11 hook points across `run_pipeline`'s
numbered stages — default `None` keeps Phase 01 parity 10/10 untouched.

Purpose: closes Pitfall 10 (canvas-sidecar gap) + Pitfall 2 (lift
`_write_geojson_atomic`) + sets up the SSE per-stage envelope that
Plan 02 will consume.

Output: 4 sidecar files in `cfg.output_dir`; helper in `paths.py`;
`cfg.on_stage` slot wired across 11 stages; parity test still 10/10
plus a new `test_canvas_sidecars_exist` assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-read-only-canvas-redesign/03-CONTEXT.md
@.planning/phases/03-read-only-canvas-redesign/03-RESEARCH.md
@CLAUDE.md
@backend/medieval_forge/services/pipeline/__init__.py
@backend/medieval_forge/services/pipeline/contracts.py
@backend/medieval_forge/services/paths.py
@backend/medieval_forge/services/ingest_runner.py
@backend/medieval_forge/services/territories_geojson.py
@backend/medieval_forge/services/baronies_builder.py
@backend/medieval_forge/services/pipeline/adapters/base.py
@backend/medieval_forge/services/ingest_terrain/runner.py

<interfaces>
RegionConfig (services/pipeline/contracts.py) — add field:
  on_stage: Optional[Callable[[str, str], None]] = field(default=None)
  # called as cfg.on_stage("voronoi", "start") / ("voronoi", "done")

run_pipeline stages (in order, must match SSE envelope on frontend):
  landmask, border, voronoi, cleanup, smooth, merge, hierarchy, render,
  lookup, metadata, export

Canvas sidecar contract (consumed by frontend useCanvasArtifacts.ts):
  territories.geojson  — FeatureCollection of condado polygons (id,name,
                          neighbors,centroid,kingdom_id,duchy_id)
  baronies.geojson     — FeatureCollection of barony polygons (id,name,
                          condado_id,centroid,pixel_count)
  condado_colors.json  — { "<id>": "#RRGGBB", ... }  (matches lookup_condado.png)
  barony_colors.json   — { "<id>": "#RRGGBB", ... }  (matches lookup_barony.png)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lift _write_geojson_atomic to services/paths.py</name>
  <files>backend/medieval_forge/services/paths.py, backend/medieval_forge/services/ingest_runner.py, backend/medieval_forge/services/pipeline/adapters/base.py, backend/medieval_forge/services/ingest_terrain/runner.py, backend/tests/unit/test_paths_write_geojson_atomic.py</files>
  <read_first>
    - backend/medieval_forge/services/ingest_runner.py (find existing `_write_geojson_atomic` definition — copy verbatim)
    - backend/medieval_forge/services/paths.py (target file)
    - backend/medieval_forge/services/pipeline/adapters/base.py (callsite — line ~6)
    - backend/medieval_forge/services/ingest_terrain/runner.py (3 callsites at lines ~20, 143, 222, 289)
    - .planning/phases/03-read-only-canvas-redesign/03-RESEARCH.md (Pitfall 2)
  </read_first>
  <behavior>
    - Test: importing `_write_geojson_atomic` from `medieval_forge.services.paths` works
    - Test: `_write_geojson_atomic(tmp_path / "out.geojson", {"type":"FeatureCollection","features":[]})` writes valid JSON atomically (file exists, content roundtrips)
    - Test: write to a non-existent parent directory raises (no auto-mkdir behavior change vs original)
    - Test: legacy import path `from medieval_forge.services.ingest_runner import _write_geojson_atomic` continues to work via re-export (keeps Phase 02 callsites green until Plan 07 deletes `ingest_runner.py`)
  </behavior>
  <action>
    1. Copy `_write_geojson_atomic(path, payload)` definition VERBATIM from `services/ingest_runner.py` into `services/paths.py`. Preserve any json dumps kwargs and tempfile rename semantics.
    2. In `services/ingest_runner.py`, replace the original definition with `from .paths import _write_geojson_atomic  # re-export — survives until Plan 07 deletes this file` so existing callsites stay green during Wave 1.
    3. Update `services/pipeline/adapters/base.py` import to `from ..paths import _write_geojson_atomic` (was `from ..ingest_runner import ...`).
    4. Update `services/ingest_terrain/runner.py` import to `from ..paths import _write_geojson_atomic` (single import, 3 callsites in the file are unchanged).
    5. Add `__all__` entry in `paths.py` if a `__all__` list exists.
    6. Create `backend/tests/unit/test_paths_write_geojson_atomic.py` with the 4 tests above.
  </action>
  <verify>
    <automated>cd backend && pytest tests/unit/test_paths_write_geojson_atomic.py tests/parity/test_iberia_868.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && pytest tests/unit/test_paths_write_geojson_atomic.py -x` exits 0
    - `cd backend && pytest tests/parity/test_iberia_868.py -x` still 10/10 green (Phase 01 parity untouched)
    - `grep -rn "from .ingest_runner import _write_geojson_atomic\|from ..ingest_runner import _write_geojson_atomic" backend/medieval_forge/` returns zero (only the re-export stub in `ingest_runner.py` itself remains, with `from .paths import` shape)
  </acceptance_criteria>
  <done>Helper lives in `paths.py`; all production callsites import from `paths.py`; legacy import path re-exports for tests; parity 10/10.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cfg.on_stage callback slot to RegionConfig + 11 hook points in run_pipeline</name>
  <files>backend/medieval_forge/services/pipeline/contracts.py, backend/medieval_forge/services/pipeline/__init__.py, backend/tests/unit/test_run_pipeline_on_stage.py</files>
  <read_first>
    - backend/medieval_forge/services/pipeline/contracts.py (RegionConfig dataclass)
    - backend/medieval_forge/services/pipeline/__init__.py (numbered stages 1..14)
    - .planning/phases/03-read-only-canvas-redesign/03-RESEARCH.md (Pattern 5 SSE envelope, lighter implementation)
  </read_first>
  <behavior>
    - Test: `RegionConfig()` instantiates with `on_stage` defaulting to None — Phase 01 parity unchanged
    - Test: `run_pipeline(cfg_with_on_stage_collector)` invokes the callback with stages in EXACT order: ["landmask","border","voronoi","cleanup","smooth","merge","hierarchy","render","lookup","metadata","export"], each with both ("start", "done") events — 22 calls total
    - Test: callback receives positional args `(stage: str, evt: str)` where `evt in {"start","done"}`
    - Test: if `on_stage` raises, `run_pipeline` does NOT swallow — exception propagates (the SSE producer wraps and reports)
  </behavior>
  <action>
    1. In `services/pipeline/contracts.py`, add to `RegionConfig`:
       ```python
       from typing import Callable
       # ...
       on_stage: Optional[Callable[[str, str], None]] = field(default=None)
       ```
       Add `Callable` to the typing import. Keep the field at the end of the dataclass.
    2. In `services/pipeline/__init__.py`, define a small private helper `_emit(cfg, stage, evt)` at module top:
       ```python
       def _emit(cfg, stage: str, evt: str) -> None:
           if cfg.on_stage is not None:
               cfg.on_stage(stage, evt)
       ```
    3. Add `_emit(cfg, "<stage>", "start")` at the entry and `_emit(cfg, "<stage>", "done")` at the exit of the 11 stages. Map existing print-numbered stages to the canonical 11 names:
       - Step [3]+[4] (build land mask 1x+2x) → `"landmask"` (start before [3], done after [4])
       - Step [5] (build border mask) → `"border"`
       - Step [6]+[7] (setup baronies + rasterize) → `"voronoi"`
       - Step [8] (cleanup_and_smooth) — note `cleanup_and_smooth` covers cleanup+smooth+merge; emit `"cleanup"` start at function entry, `"cleanup"` done after median passes (use a SECOND optional callback inside `cleanup.py` if structurally clean — OR emit `"cleanup"`/`"smooth"`/`"merge"` as three rapid markers around the single function call. Pick the rapid-marker approach: `_emit(cfg,"cleanup","start"); _emit(cfg,"cleanup","done"); _emit(cfg,"smooth","start"); ... _emit(cfg,"merge","done")` immediately around the existing `result = cleanup_and_smooth(...)` line. Document in code comment that finer-grained timing is Phase 04 territory.)
       - Step [9] (build_hierarchy_maps) → `"hierarchy"`
       - Step [10] (render visual maps) → `"render"`
       - Step [11] (generate_lookup_map loop) → `"lookup"`
       - Step [12] (export_metadata) → `"metadata"`
       - Steps [13]+[14] (mountains + rivers + final copy) → `"export"`
    4. Create `backend/tests/unit/test_run_pipeline_on_stage.py`:
       - Build a minimal `iberia_868` cfg via existing fixture loader (mirror `test_iberia_868.py` setup), inject `cfg.on_stage = events.append` lambda, run pipeline, assert the 22-event sequence in correct order.
       - Second test: cfg without on_stage — assert pipeline produces the same 10 contract files (parity-safe regression check).
  </action>
  <verify>
    <automated>cd backend && pytest tests/unit/test_run_pipeline_on_stage.py tests/parity/test_iberia_868.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_run_pipeline_on_stage.py -x` exits 0
    - `pytest tests/parity/test_iberia_868.py -x` exits 0 (10/10 — parity unchanged)
    - `grep -c "_emit(cfg," backend/medieval_forge/services/pipeline/__init__.py` ≥ 22
    - `grep -n "on_stage" backend/medieval_forge/services/pipeline/contracts.py` shows the new field
  </acceptance_criteria>
  <done>RegionConfig has `on_stage` slot; default None preserves parity; tests confirm 22 events in canonical order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Emit 4 canvas-sidecar files at end of run_pipeline + parity test assertion</name>
  <files>backend/medieval_forge/services/pipeline/__init__.py, backend/medieval_forge/services/territories_geojson.py, backend/medieval_forge/services/baronies_builder.py, backend/tests/parity/test_iberia_868.py</files>
  <read_first>
    - backend/medieval_forge/services/pipeline/__init__.py (after step [14] is the insertion point)
    - backend/medieval_forge/services/territories_geojson.py (existing emitter; verify signature + dependencies)
    - backend/medieval_forge/services/baronies_builder.py (existing emitter; verify signature)
    - backend/tests/parity/test_iberia_868.py (current 10-file assertion)
    - .planning/phases/03-read-only-canvas-redesign/03-RESEARCH.md (Pitfall 10, Don't Hand-Roll table)
  </read_first>
  <behavior>
    - Test: after `run_pipeline(cfg)` with iberia_868 fixture, `cfg.output_dir` contains `territories.geojson`, `baronies.geojson`, `condado_colors.json`, `barony_colors.json`
    - Test: each file is non-empty valid JSON
    - Test: `territories.geojson` is a FeatureCollection with `len(features) == nc_active` (matches metadata.json condado count)
    - Test: `condado_colors.json` keys are condado ids that intersect those in `lookup_condado_colors.json` (every visible condado has a color)
    - Test: 10 Unity contract files still exist + still pass byte/SSIM parity (regression safety)
  </behavior>
  <action>
    1. Audit `services/territories_geojson.py` and `services/baronies_builder.py`. Confirm they:
       (a) accept the data structures `run_pipeline` already has at end of Step [14] (`result`, `pc`, `condados`, `duchies`, `kingdoms`, `bars`, `nb`, `nc`, `cfg`);
       (b) have NO LLM dependency (they should be pure geometry — verify with `grep -E "research|llm|openai|anthropic" services/territories_geojson.py services/baronies_builder.py` returns zero).
       If they import a deleted module or expect v1 paths, refactor minimally to accept `cfg.output_dir` + the in-memory arrays. Document any signature change in the code comment.
    2. In `services/pipeline/__init__.py` after the river compositing block (after step [14], before the `print("DONE")` line), add a new step [15] block:
       ```python
       # 15. Canvas sidecars (Phase 03 BLOCKER fix — emit-only, parity-safe)
       _emit(cfg, "export", "start")  # if export start emitted earlier, replace; otherwise this is part of "export"
       print("[15] Emitting canvas sidecars...")
       from ..territories_geojson import build_territories_geojson_with_colors_sidecar
       from ..baronies_builder import build_baronies_geojson_with_colors_sidecar
       build_territories_geojson_with_colors_sidecar(
           result=result, pc=pc, condados=condados, duchies=duchies,
           kingdoms=kingdoms, cfg=cfg,
           out_geojson=os.path.join(cfg.output_dir, "territories.geojson"),
           out_colors=os.path.join(cfg.output_dir, "condado_colors.json"),
       )
       build_baronies_geojson_with_colors_sidecar(
           result=result, bars=bars, nb=nb, cfg=cfg,
           out_geojson=os.path.join(cfg.output_dir, "baronies.geojson"),
           out_colors=os.path.join(cfg.output_dir, "barony_colors.json"),
       )
       _emit(cfg, "export", "done")
       ```
       (Adjust function names/signatures to match the audited reality — names above are placeholders. Do NOT invent kwargs not present in the existing functions; refactor the existing function to accept these explicit args if it currently reads from globals.)
    3. Add `test_canvas_sidecars_exist` to `backend/tests/parity/test_iberia_868.py`:
       ```python
       @pytest.mark.parity
       def test_canvas_sidecars_exist(iberia_output_dir):
           for name in ("territories.geojson", "baronies.geojson",
                       "condado_colors.json", "barony_colors.json"):
               p = iberia_output_dir / name
               assert p.is_file(), f"missing canvas sidecar: {name}"
               data = json.loads(p.read_text(encoding="utf-8"))
               assert data, f"empty canvas sidecar: {name}"
           # Spot-check: territories.geojson has features
           tj = json.loads((iberia_output_dir / "territories.geojson").read_text(encoding="utf-8"))
           assert tj["type"] == "FeatureCollection"
           assert len(tj["features"]) > 0
       ```
       This test does NOT byte-assert against deployed Reconquista (sidecars are v3-only). Existing 10-file parity assertions stay untouched.
  </action>
  <verify>
    <automated>cd backend && pytest tests/parity/test_iberia_868.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/parity/test_iberia_868.py::test_canvas_sidecars_exist -x` exits 0
    - All previous 10-file parity assertions in `test_iberia_868.py` still pass
    - After running pipeline against iberia_868 fixture, `ls cfg.output_dir | grep -E "territories.geojson|baronies.geojson|condado_colors.json|barony_colors.json" | wc -l` equals 4
    - `grep -E "research|llm|openai|anthropic" backend/medieval_forge/services/territories_geojson.py backend/medieval_forge/services/baronies_builder.py` returns zero (these emitters are LLM-free, per D-13)
  </acceptance_criteria>
  <done>4 sidecars land in `cfg.output_dir`; new parity assertion green; 10-file parity preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → filesystem | run_pipeline writes to cfg.output_dir; this plan adds 4 new file writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-LOCAL-PIPE-01 | Tampering | Sidecar emission overwrites unrelated dir | accept | cfg.output_dir comes from project_dir(project_id) (validated UUID) — handled in Plan 02; this plan operates on the already-validated path |
| T-03-LOCAL-PIPE-02 | DoS | on_stage callback could block pipeline | accept | callback is documented as fire-and-forget; SSE producer in Plan 02 uses loop.call_soon_threadsafe so the callback is non-blocking by construction |
</threat_model>

<verification>
- `pytest tests/unit/test_paths_write_geojson_atomic.py -x` exits 0
- `pytest tests/unit/test_run_pipeline_on_stage.py -x` exits 0
- `pytest tests/parity/test_iberia_868.py -x` exits 0 (11 assertions: 10 original + 1 new sidecar assertion)
- `grep -c "_emit(cfg," backend/medieval_forge/services/pipeline/__init__.py` ≥ 22 (11 stages × 2 events)
- `ls projects/<test-uuid>/output/ | wc -l` ≥ 14 after a generate run (10 Unity + 4 sidecars)
</verification>

<success_criteria>
1. Phase 01 parity stays 10/10 green (regression-safe)
2. 4 new canvas-sidecar files land in `cfg.output_dir` for every `run_pipeline(cfg)` invocation
3. `cfg.on_stage` callback fires with 22 events (11 stages × start+done) in canonical order
4. `_write_geojson_atomic` lives in `services/paths.py`; `ingest_runner.py` re-exports for legacy callsites
5. Plan 02 can wire the SSE producer against `cfg.on_stage` without further pipeline changes
</success_criteria>

<output>
After completion, create `.planning/phases/03-read-only-canvas-redesign/03-01-SUMMARY.md`
</output>
