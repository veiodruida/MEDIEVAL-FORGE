---
phase: 05
plan: 05
type: execute
wave: 5
depends_on: [05-04]
files_modified:
  - backend/medieval_forge/services/pipeline/regions.py
  - backend/medieval_forge/data/regions/iberia_868/territory_data.py
  - backend/tests/parity/test_iberia_868.py
  - backend/tests/parity/test_iberia_868_live.py
  - backend/tests/parity/test_iberia_868_render_default.py
  - backend/tests/parity/conftest.py
  - backend/tests/integration/test_render_endpoint.py
  - backend/tests/fixtures/uat_setup.py
autonomous: true
requirements: [SC-1]
must_haves:
  truths:
    - "`backend/medieval_forge/services/pipeline/regions.py` no longer exists"
    - "`backend/medieval_forge/data/regions/iberia_868/territory_data.py` no longer exists"
    - "Legacy parity test `test_iberia_868.py` is retired (file deleted; YAML parity test in Plan 05-03 is canonical)"
    - "ALL remaining tests/fixtures that previously imported `iberia_config` / `REGIONS` are migrated to `load_region('iberia_868')` (wrapped in `dataclasses.replace` when mutated)"
    - "No production code, no test code, and no fixture imports `iberia_config` or `REGIONS` from anywhere — repo-wide grep is empty"
    - "Pytest collection clean — no import errors from deleted symbols"
  artifacts: []
  key_links: []
---

<objective>
Delete `regions.py` (iberia_config + REGIONS dict) and `territory_data.py` per D-V3-04 (delete v1 dead code, never namespace). Retire `tests/parity/test_iberia_868.py` (D-14 YAML test in Plan 05-03 is the canonical replacement). MIGRATE — not delete — the 5 other tests/fixtures that import the doomed symbols.

Purpose: Locks D-13 + D-17 step 5. Only runs after Plan 05-04 swaps every production callsite to `load_region`.
Output: 3 deletions + 5 migrations; pytest stays green.

**Scope clarification (revision):** RESEARCH line 642 named only 2 fixture files, but a full repo grep finds **5 fixtures/tests** that reference the doomed symbols. All 5 must migrate; only `test_iberia_868.py` (replaced by the YAML parity test) gets deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-region-generalization/05-CONTEXT.md (D-13)
@.planning/phases/05-region-generalization/05-RESEARCH.md (line 642 — fixture inventory)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deletion audit — enumerate every remaining import</name>
  <files>(read-only audit, no writes)</files>
  <read_first>
    backend/medieval_forge/api/v3/generate.py,
    backend/medieval_forge/api/v3/render.py
  </read_first>
  <action>
    Run these greps against the whole repo (backend + tests + scripts) to enumerate every remaining import of the symbols about to disappear. If ANY production-code match is found (under `backend/medieval_forge/` excluding `regions.py` itself), STOP and fix the callsite before Task 2.

    Required commands (record all outputs):
    - `grep -rn "from medieval_forge.services.pipeline.regions" backend/ tests/ 2>&1`
    - `grep -rn "from .regions\\|from ..regions\\|from ...regions" backend/medieval_forge/services/ 2>&1`
    - `grep -rn "iberia_config\\b" backend/ tests/ 2>&1`
    - `grep -rn "\\bREGIONS\\b" backend/ tests/ 2>&1`
    - `grep -rn "import territory_data\\|from .territory_data\\|from .*territory_data" backend/ tests/ 2>&1`

    EXPECTED matches (allowed — Task 2 will migrate or delete):
    - `scripts/migrate_iberia_to_yaml.py` (one-shot script, imports both — kept for reproducibility)
    - `backend/medieval_forge/services/pipeline/regions.py` itself (about to be deleted)
    - `backend/medieval_forge/data/regions/iberia_868/territory_data.py` itself (about to be deleted)
    - `backend/tests/parity/test_iberia_868.py` (about to be deleted in Task 3)
    - `backend/tests/parity/test_iberia_868_live.py` (about to be MIGRATED in Task 2)
    - `backend/tests/parity/test_iberia_868_render_default.py` (about to be MIGRATED in Task 2)
    - `backend/tests/parity/conftest.py` (about to be MIGRATED in Task 2)
    - `backend/tests/integration/test_render_endpoint.py` (about to be MIGRATED in Task 2)
    - `backend/tests/fixtures/uat_setup.py` (about to be MIGRATED in Task 2)

    UNEXPECTED matches → STOP. Re-open Plan 05-04 to add the missing swap. Any match in `backend/medieval_forge/` outside `services/pipeline/regions.py` is a Plan 05-04 escape.
  </action>
  <verify>
    <automated>cd C:/Users/veio_/Documents/Unity_Projects/MEDIEVAL-FORGE && (grep -rn "iberia_config\\b\\|\\bREGIONS\\b" backend/medieval_forge/ | grep -v "regions.py$" || echo "OK_NO_PROD_CALLSITES")</automated>
  </verify>
  <acceptance_criteria>
    - No production-code (under `backend/medieval_forge/` excluding `regions.py` itself) match for `iberia_config` or `REGIONS`.
    - The 5 test/fixture files listed above are the ONLY non-production remaining matches.
    - Migration script and the about-to-be-deleted files are the only additional allowed matches.
  </acceptance_criteria>
  <done>Audit confirms zero production callsites remain; deletion + migration list is complete and reconciles with reality.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the 5 test/fixture files to load_region('iberia_868')</name>
  <files>
    backend/tests/parity/test_iberia_868_live.py,
    backend/tests/parity/test_iberia_868_render_default.py,
    backend/tests/parity/conftest.py,
    backend/tests/integration/test_render_endpoint.py,
    backend/tests/fixtures/uat_setup.py
  </files>
  <read_first>
    backend/tests/parity/test_iberia_868_live.py (line 47, 80 — `iberia_config()` callsite),
    backend/tests/parity/test_iberia_868_render_default.py (lines 50, 58, 73 — three `iberia_config()` callsites),
    backend/tests/parity/conftest.py (line ~58 — `from medieval_forge.services.pipeline.regions import REGIONS`),
    backend/tests/integration/test_render_endpoint.py (lines 245, 250, 273 — `iberia_config()` callsites in D-18 invariant test),
    backend/tests/fixtures/uat_setup.py (line 40 — `from medieval_forge.services.pipeline.regions import REGIONS`),
    backend/medieval_forge/services/pipeline/region_loader.py (load_region signature + clear_region_cache)
  </read_first>
  <action>
    **Migration pattern (apply uniformly):**

    For `iberia_config()` calls that ARE mutated afterwards (e.g. `cfg.output_dir = ...`):
    ```python
    # BEFORE
    from medieval_forge.services.pipeline.regions import iberia_config
    cfg = iberia_config()
    cfg.output_dir = str(tmp_path)

    # AFTER
    from dataclasses import replace
    from medieval_forge.services.pipeline.region_loader import load_region
    cfg = replace(load_region("iberia_868"), output_dir=str(tmp_path))
    ```

    For `iberia_config()` calls used read-only (e.g. capturing baseline for D-18 non-mutation assertion):
    ```python
    # BEFORE
    baseline = iberia_config()
    assert baseline.smooth_sigma == ...

    # AFTER
    baseline = load_region("iberia_868")
    assert baseline.smooth_sigma == ...
    ```

    For `REGIONS` dict lookups:
    ```python
    # BEFORE
    from medieval_forge.services.pipeline.regions import REGIONS
    cfg = REGIONS["iberia_868"]()

    # AFTER
    from medieval_forge.services.pipeline.region_loader import load_region
    cfg = load_region("iberia_868")
    ```

    **Per-file migration:**

    1) `backend/tests/parity/test_iberia_868_live.py` — Replace line 47 import + line 80 callsite. This test mutates `cfg` afterwards, so use `replace()`. XFAIL marker stays untouched.

    2) `backend/tests/parity/test_iberia_868_render_default.py` — Replace line 50 import + lines 58, 73 callsites (`cfg_gen` and `cfg_render`). Both are mutated downstream (output_dir override) → use `replace()`.

    3) `backend/tests/parity/conftest.py` — Replace line ~58 `from medieval_forge.services.pipeline.regions import REGIONS` with `from medieval_forge.services.pipeline.region_loader import load_region`. Replace any `REGIONS[key]()` calls with `load_region(key)`.

    4) `backend/tests/integration/test_render_endpoint.py` — Replace line 245 import + lines 250, 273 callsites. The D-18 invariant ("fresh iberia_config() returns original sigma") becomes "fresh load_region('iberia_868') returns original sigma" — semantically identical because load_region is cached and Plan 05-04 Task 3 guarantees no mutation. Both calls are read-only assertion baselines → no `replace()` needed.

    5) `backend/tests/fixtures/uat_setup.py` — Replace line 40 `from medieval_forge.services.pipeline.regions import REGIONS` with `from medieval_forge.services.pipeline.region_loader import load_region`. Update any `REGIONS["iberia_868"]()` call to `load_region("iberia_868")` (wrap in `replace()` if the result is then mutated for UAT cache dir).

    6) After all 5 files edited, run pytest collection to confirm zero import errors:
       `cd backend && pytest --collect-only -q 2>&1 | tail -5`
  </action>
  <verify>
    <automated>cd backend && pytest --collect-only -q 2>&1 | tail -5 && pytest tests/parity/test_iberia_868_yaml.py tests/parity/test_iberia_868_render_default.py tests/integration/test_render_endpoint.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "iberia_config" backend/tests/parity/test_iberia_868_live.py` returns 0 matches in import/code (docstring mentions are OK if any).
    - `grep -n "iberia_config" backend/tests/parity/test_iberia_868_render_default.py` returns 0 matches.
    - `grep -n "iberia_config" backend/tests/integration/test_render_endpoint.py` returns 0 matches.
    - `grep -nE "\\bREGIONS\\b" backend/tests/parity/conftest.py` returns 0 matches.
    - `grep -nE "\\bREGIONS\\b" backend/tests/fixtures/uat_setup.py` returns 0 matches.
    - `grep -rn "from medieval_forge.services.pipeline.regions" backend/tests/` returns 0 matches.
    - `grep -n "load_region" backend/tests/parity/conftest.py backend/tests/fixtures/uat_setup.py backend/tests/parity/test_iberia_868_live.py backend/tests/parity/test_iberia_868_render_default.py backend/tests/integration/test_render_endpoint.py` matches ≥1 each.
    - `pytest --collect-only -q` reports 0 errors.
    - `test_iberia_868_render_default.py` + `test_render_endpoint.py` still pass (D-17 and D-18 invariants preserved).
  </acceptance_criteria>
  <done>5 fixture/test files migrated to load_region; pytest collection clean; D-17 + D-18 invariants preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Delete regions.py + territory_data.py + retire legacy parity test</name>
  <files>
    backend/medieval_forge/services/pipeline/regions.py,
    backend/medieval_forge/data/regions/iberia_868/territory_data.py,
    backend/tests/parity/test_iberia_868.py
  </files>
  <read_first>
    backend/medieval_forge/services/pipeline/regions.py,
    backend/tests/parity/test_iberia_868.py
  </read_first>
  <action>
    Only run this task AFTER Task 2 is green. Re-run the audit grep one last time before deletion to catch any new callsite slipped in by parallel work.

    1) `git rm backend/medieval_forge/services/pipeline/regions.py`
    2) `git rm backend/medieval_forge/data/regions/iberia_868/territory_data.py`
    3) **R-09 (review) — unconditional retirement.** `git rm backend/tests/parity/test_iberia_868.py` in this task. No "wait for one CI cycle" hedge: D-14's `test_iberia_868_yaml.py` (Plan 05-03) is canonical per CONTEXT.md and covers byte-equality from the same Reconquista goldens. The legacy test imports the deleted `iberia_config` symbol and would error on collection after Task 3, so retiring it in the SAME task as the symbol deletion is mandatory.
    4) Verify `backend/medieval_forge/data/regions/iberia_868/__init__.py` still exists (or remove if it becomes an empty stub — if it only re-exported territory_data symbols, edit it to an empty file with a module docstring noting the migration).
    5) Run full pytest collection: `cd backend && pytest --collect-only -q 2>&1 | grep -E "(error|Error)" | head -20`. There should be ZERO errors.
    6) Run YAML parity gate one more time to confirm zero regression.
  </action>
  <verify>
    <automated>cd backend && pytest --collect-only -q 2>&1 | tail -3 && pytest tests/parity/test_iberia_868_yaml.py -q && pytest tests/unit/test_region_loader.py tests/integration/test_generate_render_load_region.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f backend/medieval_forge/services/pipeline/regions.py && echo EXISTS || echo OK_DELETED` prints `OK_DELETED`.
    - `test -f backend/medieval_forge/data/regions/iberia_868/territory_data.py && echo EXISTS || echo OK_DELETED` prints `OK_DELETED`.
    - **R-09**: `test -f backend/tests/parity/test_iberia_868.py && echo EXISTS || echo OK_DELETED` prints `OK_DELETED` (unconditional retirement — no hedge, no CI-cycle wait).
    - **R-12 (CI grep gate)**: `grep -rn "iberia_config\\b" backend/medieval_forge/ --include="*.py"` returns 0 matches (prevents accidental re-introduction in any future plan/task).
    - **Repo-wide grep zero-match guard:** `grep -rn "iberia_config\\b" backend/medieval_forge/ backend/tests/` returns 0 matches.
    - **Repo-wide grep zero-match guard:** `grep -rn "from medieval_forge.services.pipeline.regions\\b" backend/medieval_forge/ backend/tests/` returns 0 matches.
    - `grep -rn "\\bREGIONS\\b" backend/medieval_forge/ backend/tests/` returns 0 matches (other than incidental string literals — verify each manually if any match).
    - `pytest --collect-only -q` reports 0 errors.
    - `pytest tests/parity/test_iberia_868_yaml.py -q` exits 0 (the canonical parity gate stays green after the deletion).
    - All Plan 05-01..05-04 tests still green.
  </acceptance_criteria>
  <done>3 files deleted; 5 files migrated; repo-wide grep for `iberia_config` and `REGIONS` returns zero; pytest collection clean; YAML parity gate canonical.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | — | deletion only | accept | No new attack surface introduced; pre-deletion audit (Task 1) + explicit migration (Task 2) prevent breakage from missed callsites. |
</threat_model>

<verification>
- 3 files deleted, 5 files migrated.
- Zero remaining production imports of deleted symbols.
- Zero remaining test imports of deleted symbols.
- YAML parity gate green; D-17 + D-18 invariant tests green.
</verification>

<success_criteria>
- regions.py, territory_data.py, test_iberia_868.py removed.
- 5 test/fixture files migrated to load_region.
- Pytest collection clean; all prior tests green.
- Repo-wide grep for `iberia_config` and `REGIONS` (as a symbol) returns zero.
</success_criteria>

<output>
After completion, create `.planning/phases/05-region-generalization/05-05-SUMMARY.md`
</output>
