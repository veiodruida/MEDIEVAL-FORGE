---
phase: 05
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - backend/tests/parity/test_iberia_868_yaml.py
autonomous: true
requirements: [SC-1]
must_haves:
  truths:
    - "`pytest tests/parity/test_iberia_868_yaml.py` passes (pixel-perfect lookup PNGs + SSIM ≥ 0.98 visual PNGs + structural-equal JSON)"
    - "Test is non-skippable (marked `@pytest.mark.parity`)"
    - "Test cannot be merged or skipped before deletion of `iberia_config()` in Plan 05-05"
  artifacts:
    - path: "backend/tests/parity/test_iberia_868_yaml.py"
      provides: "Hard parity gate proving YAML-loaded cfg equivalent to legacy"
      contains: "load_region('iberia_868')"
  key_links:
    - from: "backend/tests/parity/test_iberia_868_yaml.py"
      to: "backend/medieval_forge/services/pipeline/region_loader.py"
      via: "load_region call"
      pattern: "load_region\\("
---

<objective>
Implement the hard parity gate `tests/parity/test_iberia_868_yaml.py` (D-14). It loads via `load_region('iberia_868')`, runs the full pipeline, asserts byte-equal lookup PNGs + SSIM ≥ 0.98 visual PNGs + structural-equal JSON against the same Phase 01 golden set used by `test_iberia_868.py`.

Purpose: Proves YAML-loaded cfg produces byte-equal artifacts. Locks D-14 + D-17 step 3 (gate exists BEFORE Plan 05-05 deletes `iberia_config()`).
Output: One non-skippable parity test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-region-generalization/05-CONTEXT.md
@backend/tests/parity/test_iberia_868.py
@backend/medieval_forge/services/pipeline/region_loader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement test_iberia_868_yaml.py as parity copy using load_region</name>
  <files>backend/tests/parity/test_iberia_868_yaml.py</files>
  <read_first>
    backend/tests/parity/test_iberia_868.py (golden comparison patterns),
    backend/tests/conftest.py (golden fixture path setup),
    backend/medieval_forge/services/pipeline/region_loader.py
  </read_first>
  <action>
    Copy the structure of `tests/parity/test_iberia_868.py` into `tests/parity/test_iberia_868_yaml.py`. The ONLY differences:
    - Replace `from medieval_forge.services.pipeline.regions import iberia_config` with `from medieval_forge.services.pipeline.region_loader import load_region, clear_region_cache`.
    - Replace `cfg = iberia_config()` with `clear_region_cache(); cfg = load_region('iberia_868')`.
    - Each test function gets `@pytest.mark.parity` marker (the legacy file already has this — preserve).
    - Override `cfg.output_dir` to point at the test's `tmp_path` (same trick the legacy test uses — `tests/conftest.py` already handles via fixture if present; otherwise inline a `cfg = replace(cfg, output_dir=str(tmp_path))` using `dataclasses.replace`).
    - Comparison logic (golden file paths, SSIM threshold ≥ 0.98, byte-equal for lookup PNGs, structural JSON deep-equal) is identical to legacy.

    Do NOT add `@pytest.mark.skip` or `xfail`. Do NOT use mocks. Run the actual pipeline.
  </action>
  <verify>
    <automated>cd backend && pytest tests/parity/test_iberia_868_yaml.py -q</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `grep -n "load_region" backend/tests/parity/test_iberia_868_yaml.py` >=1 match.
    - `grep -nE "(skip|xfail)" backend/tests/parity/test_iberia_868_yaml.py` returns 0 (no skip/xfail markers; non-skippable).
    - `grep -n "pytest.mark.parity" backend/tests/parity/test_iberia_868_yaml.py` >=1 match.
    - `pytest tests/parity/test_iberia_868_yaml.py -q` exits 0 with N tests passing where N matches the number of parity assertions in the legacy file (every lookup PNG byte-equal, every visual PNG SSIM ≥ 0.98, every JSON deep-equal).
    - Legacy `pytest tests/parity/test_iberia_868.py -q` ALSO still exits 0 (both green simultaneously, per D-17 invariant).
  </acceptance_criteria>
  <done>YAML parity test exists, non-skippable, green; legacy parity test untouched and green.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | — | parity test | accept | Test-only code; no production attack surface. |
</threat_model>

<verification>
- New YAML parity test green.
- Legacy parity test still green (both gates pass simultaneously).
</verification>

<success_criteria>
- Non-skippable parity gate proves YAML-loaded cfg produces byte-equal Iberia output.
</success_criteria>

<output>
After completion, create `.planning/phases/05-region-generalization/05-03-SUMMARY.md`
</output>
