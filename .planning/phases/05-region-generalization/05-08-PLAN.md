---
phase: 05
plan: 08
type: execute
wave: 5
depends_on: [05-04, 05-07]
files_modified:
  - frontend/src/api/useRegions.ts
  - frontend/src/types/region.ts
  - frontend/src/api/client.ts
  - frontend/src/components/projects/NewProjectModal.tsx
  - frontend/src/components/projects/__tests__/NewProjectModal.test.tsx
  - frontend/src/pages/ProjectList.tsx
autonomous: true
requirements: [SC-2]
must_haves:
  truths:
    - "Clicking the existing `Novo projeto` button on `ProjectList` opens `NewProjectModal` (not the legacy `/projects/new` page)"
    - "Modal renders `Dialog.Title` `Novo projeto`, project-name `TextField`, and `Select.Root` populated from `GET /api/v3/regions`"
    - "Default selected region is `iberia_868`"
    - "Regions with `has_dataset: false` render as disabled `Select.Item` with suffix `(sem dataset)`"
    - "Submit calls `POST /api/v3/projects` with `{name, region_key}` and navigates to `/projects/{id}` on success"
    - "All copy in PT-BR per UI-SPEC"
  artifacts:
    - path: "frontend/src/components/projects/NewProjectModal.tsx"
      provides: "Radix Dialog + Select modal for region-aware project creation"
      min_lines: 120
    - path: "frontend/src/api/useRegions.ts"
      provides: "TanStack Query hook for /api/v3/regions"
      contains: "useQuery"
  key_links:
    - from: "frontend/src/components/projects/NewProjectModal.tsx"
      to: "frontend/src/api/useRegions.ts"
      via: "useRegions hook"
      pattern: "useRegions\\("
    - from: "frontend/src/components/projects/NewProjectModal.tsx"
      to: "/api/v3/projects"
      via: "useCreateV3Project mutation"
      pattern: "/api/v3/projects"
---

<objective>
Ship the frontend create-project modal per `05-UI-SPEC.md` (approved 6/6 dimensions). New `NewProjectModal.tsx` (Radix `Dialog.Root` + `TextField` + `Select.Root`) replaces the click-target of the existing `Novo projeto` button. Legacy `ProjectNew.tsx` page stays mounted for Wikidata flows (out of scope).

Purpose: SC-2 user-facing wire. Locks D-07 + D-17 step 8.
Output: Modal + hook + types + mutation + ProjectList wiring + vitest coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-region-generalization/05-UI-SPEC.md
@.planning/phases/05-region-generalization/05-CONTEXT.md (D-07)
@frontend/src/api/client.ts
@frontend/src/pages/ProjectList.tsx
@frontend/src/main.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useRegions hook + RegionInfo type + useCreateV3Project mutation</name>
  <files>
    frontend/src/types/region.ts,
    frontend/src/api/useRegions.ts,
    frontend/src/api/client.ts
  </files>
  <read_first>
    frontend/src/api/client.ts (existing `useCreateProject` + hook patterns),
    .planning/phases/05-region-generalization/05-UI-SPEC.md (RegionInfo shape)
  </read_first>
  <behavior>
    - `useRegions()` returns `{ data: RegionInfo[] | undefined, isLoading: boolean, isError: boolean, refetch: () => void }` from TanStack Query.
    - Query key: `['v3', 'regions']`. URL: `/api/v3/regions`.
    - `useCreateV3Project()` returns a TanStack mutation posting to `/api/v3/projects` with body `{name: string, region_key: string}`, returning `{id, name, region_key}`.
    - On mutation success, invalidates `['projects']` query so `ProjectList` refreshes.
  </behavior>
  <action>
    1) `frontend/src/types/region.ts`:
    ```ts
    export interface RegionBounds {
      lon_min: number; lon_max: number; lat_min: number; lat_max: number;
    }
    export interface RegionInfo {
      key: string;
      display_name: string;
      bounds: RegionBounds;
      has_dataset: boolean;
    }
    ```

    2) `frontend/src/api/useRegions.ts`:
    ```ts
    import { useQuery } from '@tanstack/react-query';
    import type { RegionInfo } from '../types/region';

    export function useRegions() {
      return useQuery<RegionInfo[]>({
        queryKey: ['v3', 'regions'],
        queryFn: async () => {
          const res = await fetch('/api/v3/regions');
          if (!res.ok) throw new Error(`failed to load regions (${res.status})`);
          return res.json();
        },
      });
    }
    ```

    3) Extend `frontend/src/api/client.ts` with `useCreateV3Project` mutation (do NOT modify the legacy `useCreateProject` — keep ProjectNew.tsx working). Use `useMutation` + `queryClient.invalidateQueries({queryKey:[...]})` on success.

    **R-08 (review) — confirm the verbatim `useProjects` query key BEFORE coding the invalidation.** TanStack invalidation must match the *exact* key array used by the consumer or the list view will not refresh. Steps:
       a. Locate the existing projects query: `grep -rnE "queryKey:\\s*\\[" frontend/src/api/ frontend/src/hooks/ | grep -i project` — record the literal array (it may be `['projects']`, `['v3','projects']`, or include filters).
       b. Use that EXACT array in `useCreateV3Project.onSuccess: () => queryClient.invalidateQueries({queryKey: <verbatim array>})`.
       c. Acceptance below adds a grep that the two literals match byte-for-byte.
  </action>
  <verify>
    <automated>cd frontend && vitest run --no-coverage src/api 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/types/region.ts` exports `RegionInfo` and `RegionBounds`.
    - `useRegions` hook exported from `frontend/src/api/useRegions.ts`; query key is `['v3', 'regions']`.
    - `useCreateV3Project` exported from `frontend/src/api/client.ts`; targets `/api/v3/projects`.
    - **R-08 grep (verbatim query-key match)**: the query-key array passed to `queryClient.invalidateQueries` inside `useCreateV3Project.onSuccess` is byte-equal to the `queryKey` array declared in the `useProjects` hook. Verify with: extract both literals via `grep -nE "queryKey:\\s*\\[[^\\]]+\\]" frontend/src/api/client.ts frontend/src/api/useProjects.ts frontend/src/hooks/useProjects.ts 2>/dev/null` — the two literals must be identical strings.
    - `grep -n "useCreateProject\\b" frontend/src/pages/ProjectNew.tsx` STILL matches (legacy untouched).
    - `cd frontend && tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Types + hook + mutation in place; legacy useCreateProject untouched; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NewProjectModal.tsx + vitest + ProjectList trigger swap</name>
  <files>
    frontend/src/components/projects/NewProjectModal.tsx,
    frontend/src/components/projects/__tests__/NewProjectModal.test.tsx,
    frontend/src/pages/ProjectList.tsx
  </files>
  <read_first>
    .planning/phases/05-region-generalization/05-UI-SPEC.md (full doc — copy table, interaction table, spacing tokens),
    frontend/src/pages/ProjectList.tsx (existing `Novo projeto` button),
    frontend/src/main.tsx (Radix Theme + Toast Provider verification)
  </read_first>
  <behavior>
    - Open: clicking `Novo projeto` on `ProjectList` opens dialog; project-name `TextField` autofocused.
    - Loading: while `useRegions.isLoading`, `Select.Trigger` is disabled showing `Carregando regiões...`.
    - Error: `useRegions.isError` triggers Radix Toast with `Não foi possível carregar a lista de regiões. Tente novamente.` + `Tentar novamente` action calling `refetch`.
    - Populated: `Select.Content` lists each region as `display_name` + caption `lon X.X…X.X · lat X.X…X.X` (1 decimal). `has_dataset:false` entries rendered with suffix `(sem dataset)` and disabled.
    - Default: `defaultValue="iberia_868"`; fallback to first `has_dataset:true` if iberia missing.
    - Validation: name trimmed length must be 1..64; selected region must be in current `useRegions` data AND `has_dataset:true`. Submit button disabled until both pass; inline error PT-BR copy per UI-SPEC.
    - Submit: calls `useCreateV3Project.mutate({name, region_key})`. On success: close dialog, navigate to `/projects/{id}` via `react-router`. On error: keep dialog open, render `Não foi possível criar o projeto: {error.message}` under CTA row.
    - Cancel: `Cancelar` button + ESC + overlay click all close dialog and discard state (no confirmation).
  </behavior>
  <action>
    1) Create `frontend/src/components/projects/NewProjectModal.tsx` per the UI-SPEC contract. Use ONLY `@radix-ui/themes` 3.x primitives (`Dialog.Root`, `Dialog.Trigger`, `Dialog.Content`, `Dialog.Title`, `Dialog.Description`, `Select.Root`, `Select.Trigger`, `Select.Content`, `Select.Item`, `TextField.Root`, `Button`, `Flex`, `Box`, `Text`, `Callout`). Wire `useRegions`, `useCreateV3Project`, and `useNavigate`. Component target: ~120 LOC. PT-BR copy locked per UI-SPEC §Copywriting Contract — copy strings verbatim.

    **R-07 (review) — data-testid hooks for resilient Playwright (Plan 05-10):**
       - The trigger button (rendered inside `ProjectList.tsx` via `<Dialog.Trigger asChild>...</Dialog.Trigger>` wrapper) MUST carry `data-testid="new-project-button"`.
       - The modal root (`Dialog.Content`) MUST carry `data-testid="new-project-modal"`.
       - The region selector trigger (`Select.Trigger`) MUST carry `data-testid="region-select"`.
       These attributes are non-visual; they let Playwright spec (Plan 05-10) use `page.getByTestId(...)` instead of brittle text/role selectors that break on copy changes.

    2) Modify `frontend/src/pages/ProjectList.tsx`: wrap the `Novo projeto` button in `<NewProjectModal>` as its trigger.

       **R-05 (review) — legacy `/projects/new` link fate (prescribed):**
       - Remove the `<Link to="/projects/new">` wrapper from `ProjectList.tsx` (the only outbound link must be the modal trigger after this plan ships).
       - KEEP the `/projects/new` route registration in the router AND `frontend/src/pages/ProjectNew.tsx` UNTOUCHED — direct-URL callers (bookmarks, internal Wikidata flows) must still work.
       - Do not delete `ProjectNew.tsx` and do not unmount the route.

    3) Fill `frontend/src/components/projects/__tests__/NewProjectModal.test.tsx` (6–8 cases per UI-SPEC):
       - renders title `Novo projeto` and description copy
       - shows `Carregando regiões...` when useRegions.isLoading
       - shows toast when useRegions.isError (mock `useToast` if needed)
       - lists iberia_868 + france_1066 in Select; iberia_868 selected by default
       - disables england_1216 with `(sem dataset)` suffix when has_dataset=false
       - submit button disabled when name is empty; enabled when name 5 chars + valid region
       - happy-path submit: calls useCreateV3Project.mutate with `{name, region_key}` and triggers navigate to `/projects/123`
       - cancel button closes dialog
    Use `vi.mock('../../api/useRegions')` and `vi.mock('../../api/client')` to stub hooks.
  </action>
  <verify>
    <automated>cd frontend && vitest run --no-coverage NewProjectModal 2>&1 | tail -20 && tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/projects/NewProjectModal.tsx` exists; >=120 lines.
    - `grep -n 'Novo projeto' frontend/src/components/projects/NewProjectModal.tsx` matches (PT-BR title).
    - `grep -n "defaultValue=\"iberia_868\"\\|defaultValue='iberia_868'" frontend/src/components/projects/NewProjectModal.tsx` matches.
    - `grep -n 'sem dataset' frontend/src/components/projects/NewProjectModal.tsx` matches.
    - `cd frontend && vitest run NewProjectModal` exits 0 with >=6 tests passing.
    - `cd frontend && tsc --noEmit` exits 0.
    - `cd frontend && npm run build` succeeds.
    - `grep -n 'NewProjectModal' frontend/src/pages/ProjectList.tsx` matches (modal wired into ProjectList).
    - **R-05 grep**: `grep -n 'to="/projects/new"' frontend/src/pages/ProjectList.tsx` returns 0 matches AFTER the change (legacy link removed from ProjectList).
    - **R-05 route preserved**: `grep -rn "path=\"/projects/new\"\|path=\\'/projects/new\\'" frontend/src/` returns ≥1 (the route stays registered for direct-URL callers).
    - **R-07 grep (modal root)**: `grep -nE "data-testid=\"new-project-modal\"" frontend/src/components/projects/NewProjectModal.tsx` matches ≥1.
    - **R-07 grep (trigger)**: `grep -nE "data-testid=\"new-project-button\"" frontend/src/components/projects/NewProjectModal.tsx frontend/src/pages/ProjectList.tsx` matches ≥1 (trigger lives on the button element regardless of which file renders it).
    - **R-07 grep (region select)**: `grep -nE "data-testid=\"region-select\"" frontend/src/components/projects/NewProjectModal.tsx` matches ≥1.
    - `frontend/src/pages/ProjectNew.tsx` is UNCHANGED (`git diff frontend/src/pages/ProjectNew.tsx` empty).
  </acceptance_criteria>
  <done>NewProjectModal renders + tested + wired into ProjectList; legacy ProjectNew untouched; tsc + build green.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Tampering | Client-side region_key | mitigate | Client `pattern` validation + disabled `has_dataset:false` rows are UX layer only; server-side enum check (Plan 05-04 T-05-04-01) is the security boundary. Modal cannot bypass backend rejection. |
| T-05-08-02 | XSS | display_name rendering | mitigate | React JSX text interpolation auto-escapes; no `dangerouslySetInnerHTML` in this surface. |
</threat_model>

<verification>
- Modal renders per UI-SPEC.
- vitest covers all UI-SPEC interaction table entries.
- tsc + build clean.
- Legacy ProjectNew.tsx untouched.
</verification>

<success_criteria>
- NewProjectModal ships + wired + tested.
- TypeScript + build clean.
</success_criteria>

<output>
After completion, create `.planning/phases/05-region-generalization/05-08-SUMMARY.md`
</output>
