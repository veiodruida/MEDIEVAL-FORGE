---
phase: 2.1
slug: extended-terrain-ingestion
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-29
---

# Phase 2.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Filled by gsd-planner from RESEARCH.md `## Validation Architecture`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.x (backend) + vitest (frontend, existing) |
| **Config file** | `pyproject.toml` `[tool.pytest.ini_options]` / `frontend/vitest.config.ts` |
| **Quick run command** | `pytest backend/tests/test_terrain_*.py -x` |
| **Full suite command** | `pytest backend/tests && (cd frontend && npm test -- --run)` |
| **Estimated runtime** | ~30s backend / ~10s frontend |

---

## Sampling Rate

- **After every task commit:** Run quick command for the touched plan's test file
- **After every plan wave:** Run full suite command
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 60s

---

## Per-Task Verification Map

> Populated by gsd-planner during planning. One row per task; every task either references an automated command or a Wave-0 dependency.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD     | TBD  | TBD  | GEO-XX      | —          | N/A             | TBD       | TBD               | ❌ W0       | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Per D-28..D-30, Wave 0 lands RED tests + fixtures before any GREEN implementation:

- [ ] `backend/tests/fixtures/luxon-bbox.json` — small bbox for fast Overpass replays
- [ ] `backend/tests/fixtures/responses_recorded/overpass_rivers.json`
- [ ] `backend/tests/fixtures/responses_recorded/overpass_peaks.json`
- [ ] `backend/tests/fixtures/responses_recorded/overpass_coast.json`
- [ ] `backend/tests/fixtures/responses_recorded/overpass_parishes.json`
- [ ] `backend/tests/fixtures/synthetic_dem.tif` — numpy array with 2 gaussian peaks for ridge tests
- [ ] `backend/tests/test_overpass_client.py` — RED tests for shared 3-mirror retry helper
- [ ] `backend/tests/test_terrain_overpass.py` — RED tests for rivers/peaks/coast/parishes endpoints
- [ ] `backend/tests/test_terrain_basins.py` — RED tests for HydroSHEDS clip
- [ ] `backend/tests/test_terrain_dem.py` — RED tests for tile enumeration + mosaic
- [ ] `backend/tests/test_terrain_ridges.py` — RED tests for ridge derivation against synthetic DEM
- [ ] `backend/tests/conftest.py` — `respx`/`httpx_mock` shared fixtures + `tmp_raw_dir`
- [ ] `frontend/src/features/pipeline/__tests__/TerrainDataSection.test.tsx` — RED render + interaction tests
- [ ] Add deps: `respx` (or `pytest-httpx`), `geopandas`, `rasterio>=1.4,<1.5`, `scikit-image`, `httpx[http2]`

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Real Overpass mirror rotation under live network failure | GEO-01..04 (D-02) | Cannot reliably trigger 3-mirror failure in CI | Block all 3 OVERPASS_ENDPOINTS via /etc/hosts; run any terrain step; verify "Parar ingestão" cancels cleanly via stop_event |
| Full Iberia DEM mosaic (~24 tiles, ~150MB) end-to-end | GEO-06 | Bandwidth-heavy, requires real AWS bucket | Trigger DEM step on Spain project; verify `raw/dem.tif` exists, EPSG:4326, nodata=-32768; visualize in QGIS |
| Ridge fidelity on Iberia DEM (Pyrenees + Sierra Nevada) | GEO-07 | Validation needs real terrain | Verify ≥1 ridge polygon overlaps OSM `natural=peak` clusters in both ranges (success criterion #3) |
| HydroSHEDS lv6 visual overlay (Tejo/Douro/Guadalquivir basins) | GEO-05 | Cartographic verification | Open `raw/basins.geojson` in QGIS over OSM basemap; confirm 3 named basins present |
| SSE log streaming + status badges in browser | GEO-08 | UX behavior across StepCards | Run all 4 steps from "Terrain Data" section; verify Badge transitions pendente→rodando→pronto and shared log shows the active step |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
