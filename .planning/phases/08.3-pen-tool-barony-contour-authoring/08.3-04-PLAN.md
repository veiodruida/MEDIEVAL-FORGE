---
phase: 08.3-pen-tool-barony-contour-authoring
plan: 04
type: execute
wave: 2
depends_on: ["08.3-01"]
files_modified:
  - backend/medieval_forge/api/v3/editor.py
  - backend/tests/integration/test_editor_create_handler.py
autonomous: true
requirements: [PEN-CREATE-01, PEN-ASSIGN-01]
must_haves:
  truths:
    - "POST /editor/apply accepts op_type='create' and allocates a non-aliasing original_idx (>= nb)"
    - "The existing split path now passes min_floor=nb so it no longer aliases idx 1"
    - "A create op persists ring + barony_meta + allocated_original_idx to edit_events"
    - "GET /editor/country?lat=&lon= returns {'country':'PT'|'ES'} via cfg.border_polygon (D-08)"
    - "An oversized ring is rejected (DoS cap), mirroring the landmask 50000 cap"
  artifacts:
    - path: "backend/medieval_forge/api/v3/editor.py"
      provides: "create branch in apply_op (seed min_floor=nb, allocate, persist); split passes min_floor=nb; CreateOpPayload cap; /editor/country endpoint"
      contains: "create"
  key_links:
    - from: "apply_op create branch"
      to: "allocate_next_original_idx(db, branch_id, min_floor=nb)"
      via: "Plan 01 allocator signature"
      pattern: "min_floor"
    - from: "PenDrawLayer (Plan 05) country inference"
      to: "GET /editor/country"
      via: "centroid lat/lon → PT/ES"
      pattern: "editor/country"
---

<objective>
The API surface for CREATE. Adds `op_type='create'` to POST /editor/apply (allocation + persistence),
FIXES the latent split aliasing bug by threading `min_floor=nb` into BOTH the split and create
allocations, and adds GET /editor/country for D-08 country inference (RESEARCH Open Q2 — backend is
the cheapest path because cfg.border_polygon + point_in_polygon already exist server-side; avoids
duplicating the 40-pt polygon on the frontend).

Depends on Plan 01 (the `min_floor` param on allocate_next_original_idx + the compute() create
branch). This plan does NOT touch manual_edit.py or the orchestrator — those are Plan 01. It only
wires the HTTP handler, payload validation (DoS cap), and the country endpoint.

Purpose: PEN-CREATE-01 needs the server to allocate the id and persist the op; PEN-ASSIGN-01's D-08
needs the country endpoint. The split fix (min_floor=nb on the existing split allocation at
editor.py line 254) closes the latent bug RESEARCH flagged — split has aliased idx 1 since Phase 08
but was never caught because split UATs never checked lookup_barony.png colors.
Output: extended ApplyOpBody regex + CreateOpPayload cap + create handler + split min_floor + country
endpoint + integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-RESEARCH.md
@CLAUDE.md

<interfaces>
From backend/medieval_forge/api/v3/editor.py (CURRENT):
```python
# line 145: op_type regex — ADD 'create'
op_type: str = Field(..., pattern=r"^(split|merge|translate|landmask_replace)$")
# line 254: split allocation — ADD min_floor=nb (nb from payload)
allocated_original_idx = await allocate_next_original_idx(db, body.branch_id)
# line 273: landmask DoS cap precedent (mirror at 10000 for create ring)
if len(new_coords) > 50000: raise HTTPException(422, ...)
# line 285-288: allocated_original_idx is embedded into persisted_payload before append_edit_event
# ApplyOpResponse already returns allocated_original_idx (line 165)
```

From backend/medieval_forge/services/branches/service.py (AFTER Plan 01):
```python
async def allocate_next_original_idx(db, branch_id, min_floor: int = 0) -> int  # min_floor seeds high_water
```

Country inference building blocks (backend, already present):
  - cfg.border_polygon (40-pt PT/ES border) + point_in_polygon in contracts.py
  - load the project's RegionConfig the same way other v3 editor/render endpoints do (read an
    existing endpoint in this file or generate.py for the cfg-load pattern)

create payload shape (frontend sends, RESEARCH Target 6 + Pitfall 6):
  { ring: [{lat, lon}, ...], barony_meta: {name, condado_idx, duchy_id, kingdom_id, country},
    nb: int }   # nb = current barony count, used to seed min_floor
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: create handler + split min_floor fix + DoS cap on ring</name>
  <read_first>
    - backend/medieval_forge/api/v3/editor.py (ApplyOpBody 134-149, apply_op 178-326, split branch 249-254, landmask cap 256-277, persist 283-295)
    - backend/medieval_forge/services/branches/service.py (allocate_next_original_idx — confirm Plan 01 added min_floor)
    - backend/tests/integration/test_editor_apply_persists_only.py (mirror the integration test setup: project+branch fixtures, POST /editor/apply)
    - .planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-RESEARCH.md (Target 2, §"Seeding", Pitfall 6, Security Domain ring cap)
  </read_first>
  <action>
    editor.py changes:
      1. op_type regex (line 145): `r"^(split|merge|translate|landmask_replace|create)$"`.
      2. Add a CreateOpPayload validation. Pydantic can't length-check an untyped dict field, so
         (mirroring the landmask imperative cap at line 273) in the `elif body.op_type == "create":`
         branch validate imperatively:
           - ring = body.payload.get("ring"); require list, len(ring) in [3, 10000] else 400/422
             (DoS cap 10000 — mirror of landmask 50000, smaller because rings are smaller).
           - barony_meta = body.payload.get("barony_meta", {}); require name + condado_idx present.
           - nb = int(body.payload.get("nb", 0)).
           - allocated_original_idx = await allocate_next_original_idx(db, body.branch_id, min_floor=nb)
             → guarantees idx >= nb (no alias of existing 0..nb-1).
      3. SPLIT FIX (line 254): split must ALSO seed. Add `nb` to the split payload contract and call
         `allocate_next_original_idx(db, body.branch_id, min_floor=int(body.payload.get("nb", 0)))`.
         This closes the latent split aliasing bug. If the frontend split path does not yet send nb,
         it defaults to 0 (current behavior preserved) — but document that Plan 05/existing split
         caller SHOULD send nb. Add a code comment referencing the RESEARCH bug.
      4. The persist block (285-288) already embeds allocated_original_idx into persisted_payload —
         no change needed; the ring + barony_meta ride along in body.payload automatically.
    Write backend/tests/integration/test_editor_create_handler.py (mirror
    test_editor_apply_persists_only.py fixtures):
      - test_create_allocates_above_nb: POST create with nb=251 on a fresh branch → response
        allocated_original_idx >= 251 (NOT 1).
      - test_create_persists_ring_and_meta: the edit_events row payload contains ring + barony_meta +
        allocated_original_idx.
      - test_create_ring_too_short_400: ring with 2 points → 400.
      - test_create_ring_oversized_422: ring with 10001 points → 422.
      - test_split_seeded_above_nb: POST split with nb=251 → allocated_original_idx >= 251 (regression
        for the latent split bug).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_editor_create_handler.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - editor.py op_type regex contains `create`
    - editor.py create branch calls `allocate_next_original_idx(db, body.branch_id, min_floor=` (grep `min_floor`)
    - editor.py split branch (line ~254) also passes `min_floor=` (grep count of `min_floor` >= 2)
    - editor.py create branch rejects ring len < 3 (400) and > 10000 (422)
    - test_editor_create_handler.py exists with ≥5 tests; all green
    - `cd backend && python -m pytest tests/integration/test_editor_create_handler.py -x -q` exits 0
  </acceptance_criteria>
  <done>create op allocates min_floor=nb and persists ring+meta; split allocation also seeded (latent bug fixed); ring DoS cap enforced; integration tests green.</done>
</task>

<task type="auto">
  <name>Task 2: GET /editor/country endpoint (D-08 country inference)</name>
  <read_first>
    - backend/medieval_forge/api/v3/editor.py (the GET /editor/landmask_ring endpoint ~328-380 for the cfg-load + GET route pattern)
    - backend/medieval_forge/services/pipeline/contracts.py (cfg.border_polygon + point_in_polygon)
    - .planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-RESEARCH.md (Open Q2 recommendation)
  </read_first>
  <action>
    Add `@router.get("/{project_id}/editor/country")` accepting query params `lat: float, lon: float`.
    Load the project RegionConfig the same way the landmask_ring endpoint loads cfg. Use
    `point_in_polygon((lon, lat), cfg.border_polygon)` (confirm arg order/signature by reading
    contracts.py) to decide: inside PT region → 'PT', else → 'ES' (mirror the same routing logic
    CLAUDE.md Rule #3 uses to send municipalities to the correct per-country KD-tree). Return
    `{"country": "PT"|"ES"}`. READ-ONLY, no writes. Add to test_editor_create_handler.py:
      - test_country_pt: a lat/lon known to be on the PT side → {"country":"PT"}.
      - test_country_es: a lat/lon known to be on the ES side → {"country":"ES"}.
    Use explicit numeric lat/lon fixtures (e.g. a Lisbon-ish point for PT, a Madrid-ish point for ES)
    per project memory (feedback-tests-descriptive).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_editor_create_handler.py -k country -x -q</automated>
  </verify>
  <acceptance_criteria>
    - editor.py contains `editor/country` route accepting lat + lon
    - endpoint uses `point_in_polygon` + `cfg.border_polygon` (grep `border_polygon` in the new route)
    - test_country_pt and test_country_es pass with explicit numeric fixtures
  </acceptance_criteria>
  <done>GET /editor/country returns PT/ES from centroid via cfg.border_polygon; both side tests green; no 40-pt polygon duplicated on the frontend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend → POST /editor/apply (create) | Untrusted ring geometry + barony_meta + nb cross into the pipeline persistence layer. |
| frontend → GET /editor/country | Untrusted lat/lon floats; read-only computation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.3-04-01 | Denial of Service | oversized ring → rasterize/Shapely blowup | mitigate | Imperative cap: ring len ∈ [3, 10000], else 400/422 (mirror of landmask 50000 cap, T-08-08-02). Covered by test_create_ring_oversized_422. |
| T-08.3-04-02 | Tampering | allocated_original_idx collision with existing barony | mitigate | min_floor=nb seeding (Plan 01 allocator); SELECT-then-commit monotonic per branch. Covered by test_create_allocates_above_nb + test_split_seeded_above_nb. |
| T-08.3-04-03 | Tampering | malformed barony_meta (missing condado_idx) → bad hierarchy | mitigate | Validate barony_meta has name + condado_idx in the create branch; 400 on missing. |
| T-08.3-04-04 | Denial of Service | non-numeric / out-of-range lat/lon to /editor/country | mitigate | FastAPI float query coercion (422 on non-float); point_in_polygon tolerates any float (returns PT/ES); no unbounded work. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/integration/test_editor_create_handler.py -q` green.
- `cd backend && python -m pytest tests/parity -k iberia -q` unchanged (no pipeline/manual_edit change here).
- Split regression test proves the latent aliasing bug is closed.
</verification>

<success_criteria>
- create op end-to-end at the HTTP layer (allocate + persist) with DoS cap.
- split allocation seeded (latent bug fixed).
- /editor/country returns correct PT/ES (D-08 unblocked for Plan 05).
</success_criteria>

<output>
After completion, create `.planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-04-SUMMARY.md`
</output>
