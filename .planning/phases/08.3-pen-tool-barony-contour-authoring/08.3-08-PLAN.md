---
phase: 08.3-pen-tool-barony-contour-authoring
plan: 08
type: execute
wave: 2
# depends_on 08.3-07: this plan's freehand-draw + auto-select build ON TOP of 07's carve op
# (op_type:'carve', commitCreate interior-loop detection). 07 must be executed first.
depends_on: ["08.3-07"]
files_modified:
  - frontend/src/lib/penFlatten.ts
  - frontend/src/components/canvas/PenDrawLayer.tsx
  - frontend/src/components/canvas/CanvasViewer.tsx
  - frontend/src/lib/__tests__/penFreehand.test.ts
  - frontend/src/components/canvas/__tests__/postCloseAutoSelect.test.ts
  - frontend/tests/uat/playwright/08.3-freehand-carve.spec.ts
# autonomous false: Task 5 is a checkpoint:human-verify (real-mouse freehand feel + auto-select)
autonomous: false
requirements: [FREEHAND-DRAW-01, DRAW-BUTTONS-01, POSTCLOSE-SELECT-01]
user_setup: []

must_haves:
  truths:
    - "The user can drag a continuous freehand contour (lasso) INSIDE an existing barony; the raw drag samples are simplified (Douglas-Peucker) + smoothed so the committed ring is clean, not jagged (D-20)."
    - "A freehand contour drawn inside a barony routes to 08.3-07's op_type:'carve' (the same interior-loop detection commitCreate uses for the click-pen) — the freehand path reuses the carve geometry, it does NOT reinvent it."
    - "While drawing, an on-canvas action bar shows visible buttons — Fechar contorno / Transformar em baronia / Cancelar / Desfazer último ponto — that call the SAME handlers as the keyboard shortcuts (close/commit, Esc, Ctrl+Z) (D-21)."
    - "Contextual PT-BR hint text changes with the draw state (what click/drag does, how to close, right-click deletes last point) using PenDrawLayer's existing amber status-hint style (D-22)."
    - "After a contour closes+commits (carve), the tool switches to V (already fires at PenDrawLayer.tsx:554) AND the newly-created enclave N becomes the active selection — its Bézier handles are immediately editable — fired ONLY AFTER the baroniesQ refetch settles so N exists in effectiveBaronies (D-19)."
    - "The auto-select join key is the barony NAME (effectiveBaronies feature .id === barony's name), NOT allocated_original_idx — baronies.geojson emits no original_idx (canvas_sidecars.py has zero original_idx field); the name generated in commitCreate is the only viable key."
    - "The post-close auto-select watcher lives in CanvasViewer (stays mounted), NOT in PenDrawLayer (unmounts the instant commitCreate calls selectTool('V') because PenDrawLayer mounts only when activeTool==='P')."
    - "Selection never fires optimistically: the watcher fires from an effect keyed on effectiveBaronies, so it cannot fire until the refetched barony data actually carries the new name."
    - "Iberia 10/10 byte/SSIM parity stays green on the zero-edit path — this plan is frontend-only; no backend pipeline file is touched."
  artifacts:
    - path: "frontend/src/lib/penFlatten.ts"
      provides: "sampleAndSimplifyFreehand(points, projection, opts) → PenAnchor[] | ring: Douglas-Peucker simplify + smoothing of raw freehand drag samples, pure + unit-testable"
      contains: "sampleAndSimplifyFreehand"
    - path: "frontend/src/lib/__tests__/penFreehand.test.ts"
      provides: "Vitest spec for sampleAndSimplifyFreehand: jagged 200-point input → ≤~30-point clean ring; collinear-run collapse; closed-ring invariant"
      contains: "sampleAndSimplifyFreehand"
    - path: "frontend/src/components/canvas/PenDrawLayer.tsx"
      provides: "Freehand drag-to-draw mode (sampleAndSimplifyFreehand commit) + per-state PT-BR Konva hint text; freehand commit reuses commitCreate's carve detection. (The action-bar BUTTONS are DOM and live in CanvasViewer's overlay, not here.)"
      contains: "sampleAndSimplifyFreehand"
    - path: "frontend/src/components/canvas/CanvasViewer.tsx"
      provides: "Hosts the on-canvas action-bar DOM buttons (Fechar contorno / Transformar em baronia / Cancelar / Desfazer último ponto) keyed on penDrawing; pendingSelectName state + effect keyed on effectiveBaronies that calls useUIStore.selectBarony(name) post-refetch (D-19 auto-select); onCreated(name) plumbed from PenDrawLayer"
      contains: "Transformar em baronia"
    - path: "frontend/src/components/canvas/__tests__/postCloseAutoSelect.test.ts"
      provides: "Vitest spec for the name-match selector: pendingName not in baronies → no select; pendingName present → selectBarony(name) + pending cleared"
      contains: "selectBarony"
    - path: "frontend/tests/uat/playwright/08.3-freehand-carve.spec.ts"
      provides: "Real-mouse Playwright UAT: page.mouse freehand-drag inside a barony → on-canvas buttons → Transformar em baronia → Apply → enclave appears colored + auto-selected (InspectorSidebar shows N) + Bézier handles editable. NO __forge* hooks as evidence."
      contains: "page.mouse"
  key_links:
    - from: "PenDrawLayer freehand mouseup (sampled ring)"
      to: "commitCreate interior-loop detection (08.3-07) → POST /editor/apply op_type:'carve'"
      via: "freehand sampled anchors flow into the SAME closePath/commitCreate path the click-pen uses"
      pattern: "sampleAndSimplifyFreehand|commitCreate"
    - from: "PenDrawLayer commitCreate (generated name Baronato-${Date.now()})"
      to: "CanvasViewer pendingSelectName via onCreated(name) callback"
      via: "onCreated prop fired after the name is generated inside commitCreate"
      pattern: "onCreated"
    - from: "CanvasViewer effect keyed on effectiveBaronies"
      to: "useUIStore.selectBarony(pendingSelectName) → SelectionBridge → setActiveTerritoryId + vertices → BezierEditLayer"
      via: "name-match: effectiveBaronies.some(b => b.id === pendingSelectName) → selectBarony(name)"
      pattern: "selectBarony"
---

<objective>
Deliver the **freehand-draw UX** + **on-canvas action buttons/hints** + **post-close auto-select** that sit
ON TOP of 08.3-07's carve foundation. Three locked decisions:
- **D-20** — freehand (lasso) drag-to-draw inside an existing barony; raw samples are Douglas-Peucker simplified
  + smoothed so the committed ring is clean. The freehand contour feeds 08.3-07's `op_type:'carve'` (cut the parent
  underneath) — wire the UI to the carve op, do NOT reinvent the geometry.
- **D-21** — visible on-canvas BUTTONS (Fechar contorno / Transformar em baronia / Cancelar / Desfazer último ponto)
  that call the SAME handlers as the keyboard shortcuts. The user explicitly asked for buttons, not shortcut-only.
- **D-22** — contextual PT-BR hint text per draw state, reusing PenDrawLayer's existing amber status-hint style.
- **D-19** — after the contour closes+commits, switch to V (ALREADY fires at PenDrawLayer.tsx:554 — do NOT plan
  "make V fire") AND auto-select the new enclave so its Bézier handles are immediately editable.

Purpose: The carve op (07) is invisible to the Game Designer until they can (a) draw it ergonomically (freehand +
buttons + hints) and (b) immediately refine it (auto-select hands the new enclave to the Bézier editor). This plan
closes the UX loop the live UAT requested.

Output:
- `penFlatten.ts` — `sampleAndSimplifyFreehand()` pure helper (the clean-ring algorithm, D-20).
- `PenDrawLayer.tsx` — freehand drag mode + on-canvas action-bar buttons + per-state hints; `onCreated(name)` plumb.
- `CanvasViewer.tsx` — `pendingSelectName` + the auto-select watcher (D-19, async name→selectBarony AFTER refetch).
- 2 vitest specs (freehand algorithm + auto-select name-match) + 1 real-mouse Playwright UAT.

Scope NOTE (do NOT widen): the **condado picker widget** (D-26) is plan 08.3-09. The **carve backend op** (D-23/24/25)
is plan 08.3-07. This plan is frontend-only — it touches NO backend pipeline file. Do not stub the picker here; the
freehand carve uses the parent's `condado_idx` default that 07's handler already honors. Bézier editing of a HOLED
PARENT (normalizeCandidate exterior-only limitation, 07's known limitation) is OUT of scope — auto-select targets the
new enclave N, a simple exterior-ring polygon that is fully editable without solving the parent-hole limitation.

Task-count NOTE (intentional, not an oversight): this plan has 5 implementation tasks + 1 human checkpoint. The count
is held deliberately for TDD+UAT cohesion — Tasks 1 and 4 are RED→GREEN pairs, Task 5 is the real-mouse UAT, and the
checkpoint is the feel-check. Splitting (e.g. algorithm/mode vs buttons/auto-select) would break the RED→GREEN gates or
strand the UAT, lowering quality; the plan is intentionally kept whole.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-CONTEXT.md
@.planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-UI-SPEC.md
@.planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-07-PLAN.md

<!-- The carve foundation this plan builds ON. 07 adds op_type:'carve' + commitCreate interior-loop detection.
     Read 07's Task 4 action: commitCreate computes ring centroid, finds the parent barony whose geoRing
     CONTAINS the centroid, and POSTs op_type:'carve' with parent.condado_idx. THIS plan feeds that same path
     from a freehand drag. Reuse, do NOT reimplement. -->
@frontend/src/components/canvas/PenDrawLayer.tsx
@frontend/src/components/canvas/CanvasViewer.tsx
@frontend/src/components/editor/SelectionBridge.tsx

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — do NOT re-explore to find these. -->

useUIStore selection actions (frontend/src/stores/uiStore.ts:76):
  selectBarony(id: string | null): void
    → sets selectedBaronyId = id, clears condado selection.
  SelectionBridge.tsx:62 reads selectedBaronyId, finds baronies.find(x => x.id === selectedBaronyId),
  and ONLY THEN populates useEditorStore.vertices (the handles). So auto-select MUST route through
  selectBarony(name) — calling setActiveTerritoryId directly does NOT populate vertices.

baronies.geojson feature shape (canvas_sidecars.py:233-247) — THE JOIN KEY IS NAME:
  feature.id === feature.properties.id === feature.properties.name === bname  (the barony NAME)
  There is NO original_idx field on barony features (grep canvas_sidecars.py for original_idx → 0 hits).
  effectiveBaronies (BaronyRender[]) carries .id === name. So the auto-select key is the NAME generated
  in commitCreate (`Baronato-${Date.now()}`), NOT allocated_original_idx (which is only the backend
  allocation-correctness signal, not addressable in effectiveBaronies).

commitCreate name generation (PenDrawLayer.tsx:501-507) — the name is built INSIDE commitCreate:
  const barony_meta = { name: `Baronato-${Date.now()}`, condado_idx, duchy_id, kingdom_id, country }
  To plumb the name up for auto-select, capture barony_meta.name at this point and fire onCreated(name)
  AFTER the POST /editor/apply resolves (so commitCreate owns the name; PenDrawLayer hands it up).

CanvasViewer mount gate (CanvasViewer.tsx:871):
  {activeTool === 'P' && (<PenDrawLayer .../>)}
  → PenDrawLayer UNMOUNTS the instant commitCreate calls selectTool('V') (PenDrawLayer.tsx:554).
  Therefore the auto-select watcher CANNOT live in PenDrawLayer (its effects are torn down). It lives
  in CanvasViewer, which stays mounted and already holds effectiveBaronies (line 387) + SelectionBridge
  (line 895).

Apply refetch path (useBezierApply.ts): the carve op converges via the SAME Apply→render cascade. On
  render success project.updated_at bumps → cacheVersion → useCanvasArtifacts refetches baronies.geojson →
  effectiveBaronies (CanvasViewer.tsx:387) gains the new feature. The auto-select effect keyed on
  effectiveBaronies fires when (and only when) the refetched array contains a feature with id === pendingName.

PenDrawLayer existing reusable handlers (DO NOT duplicate logic — buttons call these):
  closePath()        — PenDrawLayer.tsx:595 — validate + commitCreate/commitExtend (the Fechar + Transformar action)
  removeLastAnchor() — PenDrawLayer.tsx:307 — drop last anchor (the Desfazer último ponto + right-click + Ctrl+Z)
  Esc handler        — PenDrawLayer.tsx:325 — discards whole path (the Cancelar action)
  eventToWorld()     — PenDrawLayer.tsx:372 — getRelativePointerPosition map-space (08.1-07 G7 fix);
                       freehand sampling MUST use this, NEVER raw clientX/Y.

penFlatten.ts existing exports (frontend/src/lib/penFlatten.ts):
  flattenPenPath(anchors, projection) → ring {lat,lon}[]   (closed ring used by commitCreate)
  flattenPenPathOpen(anchors, projection) → {lat,lon}[]     (open path for live preview)
  PenAnchor type: { lat, lon, type:'straight'|'curve', cp1?, cp2? }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED→GREEN): sampleAndSimplifyFreehand() — clean-ring algorithm for freehand drag (D-20)</name>
  <files>frontend/src/lib/penFlatten.ts, frontend/src/lib/__tests__/penFreehand.test.ts</files>
  <read_first>
    - frontend/src/lib/penFlatten.ts (existing flattenPenPath/flattenPenPathOpen + PenAnchor type — the new helper lives here)
    - frontend/src/lib/bezierFlatten.ts (08.1's flattenSegment + tolerance conventions — the smoothing/sampling cadence reference)
    - frontend/src/lib/projection.ts (geoToCanvas/canvasToGeo — simplify works in canvas-px screen space then maps back to {lat,lon})
  </read_first>
  <behavior>
    Write the vitest spec FIRST (RED), then implement until GREEN. Use descriptive test names + explicit numeric
    fixtures (per project memory feedback-tests-descriptive).
    - Test 1 `sampleAndSimplifyFreehand collapses a jagged 200-point drag into a clean ≤30-point ring`:
      build an input of 200 {lat,lon} points tracing a roughly-circular path with ±small jitter; assert the
      returned ring length is ≤ ~30 and ≥ 3 (Douglas-Peucker removed the redundant points).
    - Test 2 `collinear run collapses to its endpoints`: input of 50 points on a straight line + 2 corner points →
      output keeps only the corners (assert length === 3 or 4 with the closing point).
    - Test 3 `output ring is closed (first === last) and non-self-intersecting for a simple lasso`: assert
      ring[0] equals ring[ring.length-1] within 1e-9, and the ring has ≥3 distinct points.
    - Test 4 `empty / degenerate input returns []`: < 3 input points → returns [].
  </behavior>
  <action>
    Add `export function sampleAndSimplifyFreehand(points: Array<{lat:number,lon:number}>, projection:
    ProjectionConfig, opts?: { tolerancePx?: number; maxPoints?: number }): Array<{lat:number,lon:number}>` to
    penFlatten.ts. Algorithm (Claude's discretion within "clean, not jagged" per D-20):
    1. If points.length < 3 → return [].
    2. Map each {lat,lon} to canvas px via geoToCanvas (screen-space simplify is scale-stable).
    3. Run Ramer-Douglas-Peucker simplification with tolerancePx default = 2.5 (px). Implement RDP inline
       (perpendicular-distance recursion) — do NOT add a new npm dep.
    4. Optional light smoothing pass (e.g. one round of midpoint/Chaikin or a 3-tap moving average) so corners are
       not razor-sharp; keep maxPoints (default 60) as a hard cap (decimate uniformly if still over).
    5. Map the simplified px points back to {lat,lon} via canvasToGeo.
    6. Close the ring: append a copy of the first point as the last (first === last) so it is a valid closed ring
       for commitCreate's flattenPenPath consumer.
    Keep it a PURE function (projection in, ring out — no store, no DOM). Commit when GREEN
    (feat(08.3-08): sampleAndSimplifyFreehand clean-ring helper + RED→GREEN vitest).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/__tests__/penFreehand.test.ts</automated>
  </verify>
  <done>
    sampleAndSimplifyFreehand exported from penFlatten.ts; 4 tests GREEN; a jagged 200-point input returns a
    clean ≤30-point closed ring; no new npm dependency added (grep package.json unchanged).
  </done>
</task>

<task type="auto">
  <name>Task 2: Freehand drag-to-draw mode in PenDrawLayer — reuse commitCreate carve detection (D-20)</name>
  <files>frontend/src/components/canvas/PenDrawLayer.tsx</files>
  <read_first>
    - frontend/src/components/canvas/PenDrawLayer.tsx (handleMouseDown ~644, handleMouseMove ~402, handleMouseUp
      ~678, closePath ~595, commitCreate ~458 — the carve detection 07 adds lives in commitCreate; eventToWorld ~372)
    - frontend/src/lib/penFlatten.ts (sampleAndSimplifyFreehand from Task 1)
    - .planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-07-PLAN.md (Task 4: commitCreate computes ring
      centroid → finds parent barony containing it → POST op_type:'carve' with parent.condado_idx — REUSE this path)
  </read_first>
  <action>
    Add a freehand draw mode to PenDrawLayer that accumulates a continuous drag, then commits the sampled ring
    through the EXISTING commitCreate carve path (07). Do NOT add a backend call here — freehand reuses 07's carve.
    1. Add a component-local `freehandMode` boolean (useState, default false) + a `freehandActive` toggle. Surface a
       small toggle in the action bar (Task 3) labeled "Modo livre (lasso)" so the user opts into freehand vs click-pen.
    2. Add a `freehandPointsRef = useRef<Array<{lat,lon}>>([])`. In handleMouseDown when freehandMode is on: clear the
       ref, push the first eventToWorld point, set an `isFreehandDraggingRef`. In handleMouseMove while dragging: push
       eventToWorld points into the ref (throttle to ~every 16ms or every ~3px to avoid flooding) and render a LIVE
       open polyline preview (reuse the existing green CURVE_STROKE Line, points from the raw ref). USE eventToWorld
       (getRelativePointerPosition map-space) — NEVER raw clientX/Y (08.1-07 G7 bug).
    3. In handleMouseUp when freehand dragging: `const ring = sampleAndSimplifyFreehand(freehandPointsRef.current,
       projection)`; if ring.length < 3 → setValidationError('Mínimo de 3 pontos para fechar o contorno.') and bail.
       Otherwise convert the ring back into straight PenAnchor[] (type:'straight' for each ring point, drop the closing
       duplicate) and call the SAME closePath()/commitCreate path the click-pen uses — so the centroid-in-parent carve
       detection 07 added fires unchanged. The freehand commit is a CARVE because the lasso is drawn INSIDE a parent.
    4. Keep validation identical (D-17: ≥3 anchors, no self-intersection, area ≥ threshold via the existing validate()).
    Do NOT duplicate commitCreate's carve logic — freehand only produces the anchors, the carve routing stays in 07's
    commitCreate. Commit (feat(08.3-08): freehand lasso draw feeds 07 carve via commitCreate).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run && npx tsc --noEmit -p frontend/tsconfig.json</automated>
  </verify>
  <done>
    PenDrawLayer has a freehand drag mode (grep `freehandMode` + `sampleAndSimplifyFreehand` in PenDrawLayer.tsx);
    a freehand mouseup produces a sampled ring routed through closePath/commitCreate (the carve path), NOT a new
    backend call; eventToWorld (not clientX/Y) is the only coord source; full vitest suite + tsc green.
  </done>
</task>

<task type="auto">
  <name>Task 3: On-canvas action buttons + per-state PT-BR hints (D-21, D-22) + UI-SPEC supplement note</name>
  <files>frontend/src/components/canvas/CanvasViewer.tsx, frontend/src/components/canvas/PenDrawLayer.tsx, .planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-UI-SPEC.md</files>
  <read_first>
    - frontend/src/components/canvas/PenDrawLayer.tsx (closePath ~595, removeLastAnchor ~307, Esc handler ~325,
      status-hint Text block ~1072 for the amber style; Group root ~846 for where the action bar anchors)
    - .planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-UI-SPEC.md (§Draw-Mode Status Hint, §Copywriting
      Contract, §data-testid Anchors — EXTEND these for the new buttons/hints surfaces)
  </read_first>
  <action>
    Add a visible on-canvas action bar (D-21) + contextual hints (D-22). The user explicitly asked for buttons.
    1. ACTION BAR (D-21): render a small canvas-anchored action bar that is visible WHILE drawing (anchors.length > 0
       OR freehand dragging). Implementation choice (Claude's discretion): a DOM overlay positioned over the Stage
       (absolute, top-right of canvas, pointerEvents:auto) using Radix `Button size="1"` is preferred over Konva
       buttons (real DOM is Playwright-clickable with page.mouse). Four buttons with EXACT labels + data-testids:
         - "Fechar contorno"        data-testid="pen-btn-close"    → onClick calls closePath()
         - "Transformar em baronia" data-testid="pen-btn-commit"   → onClick calls closePath() (close == commit-to-carve;
                                                                       same handler, the carve fires in commitCreate)
         - "Cancelar"               data-testid="pen-btn-cancel"   → onClick runs the SAME path as Esc (clear anchors,
                                                                       reset extend/freehand state, onDrawingStateChange(false))
         - "Desfazer último ponto"  data-testid="pen-btn-undo"     → onClick calls removeLastAnchor()
       The buttons call the SAME handlers as the keyboard shortcuts — extract the Esc body into a shared
       `cancelPath()` callback so the button and the Esc handler share one implementation (no logic divergence).
       HOST DECISION (LOCKED — the only workable choice): PenDrawLayer returns <Layer>…</Layer> and react-konva's
       reconciler REJECTS DOM children (a Radix Select/Button cannot live inside the Konva tree). Therefore the action
       bar is a DOM overlay rendered NEXT TO the Stage in CanvasViewer (absolute-positioned, keyed on the existing
       penDrawing boolean), NOT inside PenDrawLayer. The four handlers (closePath/cancelPath/removeLastAnchor) live in
       PenDrawLayer; expose them to CanvasViewer via a ref or callback props so the DOM buttons call them. This is the
       SAME DOM-overlay host plan 08.3-09's CondadoPicker reuses — decide it ONCE here so 09 stays consistent.
       NOTE: CanvasViewer.tsx is already in this plan's files_modified (Task 4 edits it for auto-select) — the action-bar
       overlay edits the same file.
    2. HINTS (D-22): replace the single static status hint with per-state PT-BR text using the existing amber style
       (fill '#f0c040', size '1' / 11px, opacity 0.75). State → copy:
         - 0 anchors, click-pen:  "Clique: âncora reta — Arraste: curva Bézier — Botão direito: desfazer ponto"
         - 0 points, freehand:    "Arraste para desenhar o contorno livre — solte para fechar"
         - ≥1 anchor:             "Clique no primeiro ponto para fechar — Botão direito: desfazer último ponto — Esc: cancelar"
       (Keep the click-pen default copy that already exists for the 0-anchor click-pen state.)
    3. UI-SPEC SUPPLEMENT (mandatory per quality gate): append a `## Phase 08.3-08 Supplement — Freehand + Action Bar`
       section to 08.3-UI-SPEC.md documenting: the four button labels + data-testids, the freehand toggle label
       ("Modo livre (lasso)"), the per-state hint copy table above, and the action-bar placement/visibility rule
       (visible while drawing only). Add the new data-testids (pen-btn-close/commit/cancel/undo, pen-freehand-toggle)
       to the §data-testid Anchors table.
    Commit (feat(08.3-08): on-canvas action buttons + per-state hints + UI-SPEC supplement).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run && npx tsc --noEmit -p frontend/tsconfig.json</automated>
  </verify>
  <done>
    Four buttons with EXACT labels Fechar contorno / Transformar em baronia / Cancelar / Desfazer último ponto exist
    with data-testids pen-btn-close/commit/cancel/undo (grep them in the source); buttons call closePath /
    cancelPath / removeLastAnchor (the same handlers as the shortcuts — no duplicated logic); per-state PT-BR hints
    present; 08.3-UI-SPEC.md has the `## Phase 08.3-08 Supplement` section with labels + testids + hint table; vitest + tsc green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4 (RED→GREEN): Post-close auto-select N — async name→selectBarony AFTER refetch (D-19)</name>
  <files>frontend/src/components/canvas/PenDrawLayer.tsx, frontend/src/components/canvas/CanvasViewer.tsx, frontend/src/components/canvas/__tests__/postCloseAutoSelect.test.ts</files>
  <read_first>
    - frontend/src/components/canvas/PenDrawLayer.tsx (commitCreate ~458 — name generated at ~502; selectTool('V')
      at ~554 ALREADY fires, do NOT add it; onPathClosed prop ~109)
    - frontend/src/components/canvas/CanvasViewer.tsx (effectiveBaronies ~387, SelectionBridge wiring ~895,
      PenDrawLayer mount ~871, baroniesQ ~361)
    - frontend/src/components/editor/SelectionBridge.tsx (line 62: reads selectedBaronyId, finds barony by id===name,
      populates vertices — this is what makes N's handles editable; selectBarony is the entry)
    - frontend/src/stores/uiStore.ts (selectBarony:76)
  </read_first>
  <behavior>
    Write the vitest selector spec FIRST (RED). The REAL work of D-19 is the async NAME → selectBarony mapping that
    fires ONLY after the baroniesQ refetch settles — NOT "make selectTool('V') fire" (it already does).
    - Test 1 `pending name absent from baronies → no select fired`: given pendingSelectName='Baronato-X' and an
      effectiveBaronies array WITHOUT that id, the selector function returns null / does not call selectBarony.
    - Test 2 `pending name present in baronies → selectBarony(name) + pending cleared`: given pendingSelectName='Baronato-X'
      and effectiveBaronies containing { id:'Baronato-X', ... }, the selector calls selectBarony('Baronato-X') exactly once
      and clears the pending name (so it does not re-fire on the next refetch).
    - Test 3 `name match is exact (id equality), not substring`: a barony id 'Baronato-X-suffix' does NOT satisfy a
      pendingSelectName 'Baronato-X'.
    Make the match a PURE function (e.g. `resolveAutoSelect(pendingName, baronies) → name | null`) so it is unit-testable
    without React, satisfying the test pyramid.
  </behavior>
  <action>
    1. PLUMB THE NAME UP: in PenDrawLayer.commitCreate, after the POST /editor/apply resolves (or in the finally), call
       a new optional prop `onCreated?.(barony_meta.name)` so CanvasViewer learns the exact name generated at
       PenDrawLayer.tsx:502. (selectTool('V') at :554 stays untouched — D-19 does NOT re-implement it.) Add `onCreated`
       to PenDrawLayerProps and pass it from CanvasViewer's <PenDrawLayer .../> (line 872).
    2. CanvasViewer (stays mounted — the watcher MUST live here, NOT in PenDrawLayer which unmounts on selectTool('V')):
       - add `const [pendingSelectName, setPendingSelectName] = useState<string | null>(null)`.
       - pass `onCreated={(name) => setPendingSelectName(name)}` to PenDrawLayer.
       - add a pure helper `resolveAutoSelect(pendingName, baronies)` (exported for the vitest spec) that returns the
         matching name (exact id equality) or null.
       - add an effect `useEffect(() => { if (!pendingSelectName) return; const match =
         resolveAutoSelect(pendingSelectName, effectiveBaronies ?? []); if (match) { useUIStore.getState().selectBarony(match);
         setPendingSelectName(null); } }, [pendingSelectName, effectiveBaronies])`. Because the effect is keyed on
         effectiveBaronies, it CANNOT fire until the post-Apply refetch actually carries the new name — selection is never
         optimistic. selectBarony(match) → SelectionBridge populates vertices → BezierEditLayer activates → N's handles editable.
    3. Vitest spec postCloseAutoSelect.test.ts on resolveAutoSelect per the behavior above (mock useUIStore.selectBarony
       and assert call/clear semantics, or test resolveAutoSelect purely + a thin wrapper that calls selectBarony).
    KNOWN LIMITATION (document, do NOT solve): auto-select targets N (a simple exterior-ring polygon) — fully editable.
    Editing the HOLED PARENT's Bézier contour hits 07's normalizeCandidate exterior-only limitation; that is out of scope
    here (07 flagged it; not one of this plan's three requirement IDs).
    Commit (feat(08.3-08): post-close auto-select N via name→selectBarony after refetch + RED→GREEN vitest).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/canvas/__tests__/postCloseAutoSelect.test.ts && npx tsc --noEmit -p frontend/tsconfig.json</automated>
  </verify>
  <done>
    resolveAutoSelect is a pure exported function; 3 vitest tests GREEN (absent→no-select, present→selectBarony+clear,
    exact-match-not-substring); CanvasViewer holds pendingSelectName + the effectiveBaronies-keyed effect that calls
    useUIStore.selectBarony(name) (grep `pendingSelectName` + `selectBarony` + `resolveAutoSelect` in CanvasViewer.tsx);
    PenDrawLayer fires onCreated(barony_meta.name) (grep `onCreated`); selectTool('V') at :554 unchanged; tsc green.
  </done>
</task>

<task type="auto">
  <name>Task 5: Real-mouse Playwright UAT — freehand → buttons → carve → auto-select → editable (NO __forge* hooks)</name>
  <files>frontend/tests/uat/playwright/08.3-freehand-carve.spec.ts</files>
  <read_first>
    - frontend/tests/uat/playwright/08.3-pen-tool.spec.ts (app-boot, project-select, navigateToWorkspace pattern,
      lookupBaronySha / findCreatedBaronyInMetadata helpers — REUSE these; but DROP the __forge* DEV-hatch usage:
      per project memory feedback-verify-ui-real-mouse, __forge* hooks alone are NOT evidence — 06's PEN-CURVE-01
      shipped broken under hook-only green)
    - frontend/tests/uat/playwright/08.3-07-carve-roundtrip.spec.ts (07's real-mouse carve UAT — mirror its pixel-sample
      hole-geometry assertions and its server restart preamble)
    - frontend/src/components/canvas/PenDrawLayer.tsx (data-testids: pen-btn-close/commit/cancel/undo, pen-freehand-toggle)
    - frontend/src/components/editor/InspectorSidebar.tsx (the real DOM surface that shows the SELECTED barony — assert
      auto-select through this DOM, NOT a __forge* read)
  </read_first>
  <action>
    Write frontend/tests/uat/playwright/08.3-freehand-carve.spec.ts driving the WHOLE chain with REAL page.mouse (no
    __forge* hooks as evidence). Steps:
    1. navigateToWorkspace; select the Iberia project; pick the Pen tool (P) via the DOM button (edit-tool-p).
    2. Select + zoom to a LARGE barony first (real-mouse freehand drag needs drawing room — freehand drag is more
       real-mouse-friendly than click-pen's 3px anchors). Enable the freehand toggle (pen-freehand-toggle click).
    3. With REAL page.mouse: mouse.move to a start point INSIDE the barony, mouse.down, a sequence of mouse.move steps
       tracing a small closed-ish loop fully inside the parent, mouse.up. This is the freehand lasso.
    4. Click the on-canvas DOM button "Transformar em baronia" (pen-btn-commit) with page.mouse → closePath → carve.
    5. Trigger the carve convergence by clicking "Aplicar edições" (the global Apply control). Do NOT manually select a
       DIFFERENT barony first — that would contradict the D-19 auto-select assertion this UAT proves (step 7). If the
       Apply control is gated behind a pre-existing barony selection in the current UI, that gating is itself a defect to
       SURFACE here (the carve must be Appliable without hijacking the selection) — note it for the Task 6 human
       checkpoint rather than papering over it by selecting an unrelated barony.
    6. Wait for the render cascade: lookup_barony.png SHA changes (reuse lookupBaronySha helper) AND the new
       "Baronato-" barony appears in territory_metadata.json with pixel_count > 0 (reuse findCreatedBaronyInMetadata).
    7. AUTO-SELECT PROOF (D-19, the load-bearing assertion — through REAL DOM, not __forge*): after the cascade settles,
       assert selection landed on N SPECIFICALLY via InspectorSidebar (VERIFIED to exist: InspectorSidebar.tsx:233 enters
       the barony branch on selectedBaronyId; line 258 the container has data-testid="inspector-barony"; line 260 renders
       <Heading>{barony.name}</Heading>). Assert: getByTestId("inspector-barony") is visible AND it contains the new
       "Baronato-..." name text. (bezier-apply-edits-btn merely being enabled proves SOME barony is selected, NOT which —
       so it is NOT sufficient for the N-specific claim; the inspector-barony name is.) Do NOT assert "handles grabbable"
       here — that is a Konva-pixel property, not DOM-assertable; it is owned by the Task 6 human checkpoint. This task's
       headline proof is: the new enclave N became the selected barony, shown by name in the inspector, with no manual click.
    8. Capture uat-08.3-08-freehand-carve.png. Real-mouse buttons + freehand + auto-select must all be exercised through
       the page, not hooks.
    Before running: shutdown + rebuild + restart backend AND frontend dev servers (per project memory
    feedback-server-restart-before-test). Commit (test(08.3-08): real-mouse freehand→carve→auto-select UAT).
  </action>
  <verify>
    <automated>cd frontend && npx playwright test 08.3-freehand-carve.spec.ts</automated>
  </verify>
  <done>
    Spec drives freehand draw via REAL page.mouse (mouse.down/move/up — grep `page.mouse` present, zero __forge* hooks
    used as evidence), clicks the on-canvas DOM buttons, commits a carve, Applies, and asserts the new enclave is
    AUTO-SELECTED through the InspectorSidebar DOM (getByTestId("inspector-barony") visible + contains the new
    "Baronato-" name — proving selection landed on N specifically, not just "some barony"). "Handles grabbable" is the
    Task 6 human checkpoint, not asserted here. Screenshot captured. Spec passes green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Human feel-check — freehand draws cleanly, buttons work, new enclave is auto-selected + editable</name>
  <files>frontend/tests/uat/playwright/08.3-freehand-carve.spec.ts</files>
  <action>Checkpoint — pause execution and ask the human to manually verify the freehand + buttons + auto-select FEEL per the how-to-verify steps (the smoothness/ergonomics judgement headless Playwright cannot fully assert). Do not proceed past this task until the human responds with the resume signal.</action>
  <what-built>
    Freehand lasso draw (Task 2) + on-canvas action buttons + per-state hints (Task 3) + post-close auto-select of the
    new enclave (Task 4) + green real-mouse Playwright UAT (Task 5). This checkpoint confirms the automated assertions
    match human perception — the gap 06's hook-only green missed.
  </what-built>
  <how-to-verify>
    1. Confirm Task 5's servers are running (rebuilt + restarted per feedback-server-restart-before-test). If not,
       shutdown + rebuild + restart backend AND frontend before opening the browser.
    2. Open the canvas, select the Iberia project, pick the Pen tool (P), enable "Modo livre (lasso)".
    3. With a REAL mouse, drag a freehand contour fully inside one barony and release. Confirm the committed contour is
       CLEAN (smoothed, not jagged) — not a noisy scribble.
    4. Confirm the on-canvas buttons (Fechar contorno / Transformar em baronia / Cancelar / Desfazer último ponto) are
       visible while drawing and each works (Desfazer removes the last point; Cancelar discards the path).
    5. Confirm the contextual PT-BR hint text changes with the draw state.
    6. Click "Transformar em baronia" → "Aplicar edições". After the render, confirm the NEW enclave is automatically
       SELECTED (the inspector shows it) and its Bézier handles are immediately grabbable/editable WITHOUT a manual click.
    Review the screenshot from Task 5 (uat-08.3-08-freehand-carve.png).
  </how-to-verify>
  <verify>Human confirms the 6 how-to-verify steps (clean freehand, working buttons, changing hints, auto-selected + editable enclave); types "approved" or reports the defect.</verify>
  <done>Human-approved: freehand draws a clean (non-jagged) contour, the on-canvas buttons all work, the hints change per state, and after Transformar em baronia + Apply the new enclave is auto-selected with immediately-editable Bézier handles.</done>
  <resume-signal>Type "approved" if freehand draws cleanly, the buttons + hints work, and the new enclave is auto-selected + Bézier-editable after Apply, or describe the defect.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user freehand drag → sampled ring | Untrusted, noisy, possibly thousands of raw drag points cross here |
| freehand ring → commitCreate carve POST | Reuses 07's POST /editor/apply op_type:'carve' trust boundary (already mitigated in 07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.3-08-01 | D (DoS) | freehand drag floods thousands of raw points | mitigate | sampleAndSimplifyFreehand caps output at maxPoints (default 60) via RDP + uniform decimation (Task 1); 07's editor.py carve handler also re-caps ring ≤10000 server-side |
| T-08.3-08-02 | T (Tampering) | freehand commit reuses click-pen carve path with wrong/missing parent | mitigate | freehand routes through the SAME commitCreate centroid-in-parent carve detection (07); no separate code path → no divergent validation |
| T-08.3-08-03 | I (Integrity) | auto-select fires optimistically before the barony exists → selects nothing / stale id | mitigate | watcher is an effect keyed on effectiveBaronies (Task 4); it cannot fire until the refetched array carries the exact name; exact id-equality match (resolveAutoSelect) prevents substring false-matches |
| T-08.3-08-04 | E (Coordinate) | freehand sampling uses raw clientX/Y → map-space drift (08.1-07 G7 bug) | mitigate | freehand sampling uses eventToWorld/getRelativePointerPosition ONLY (Task 2); never clientX/Y |
</threat_model>

<verification>
- Frontend unit: `cd frontend && npx vitest run` GREEN (sampleAndSimplifyFreehand spec + postCloseAutoSelect spec + full suite).
- Typecheck: `cd frontend && npx tsc --noEmit -p frontend/tsconfig.json` clean.
- Parity (non-negotiable): `cd backend && python -m pytest tests/parity -q` stays 10/10 — this plan is frontend-only, no backend pipeline file touched (proves the zero-edit identity contract is untouched).
- Frontend UAT: `cd frontend && npx playwright test 08.3-freehand-carve.spec.ts` GREEN with REAL page.mouse (mouse.down/move/up for freehand + DOM-button clicks), asserting auto-select through the InspectorSidebar DOM — NOT __forge* hooks.
- grep gate: `sampleAndSimplifyFreehand` in penFlatten.ts AND in PenDrawLayer.tsx; `Transformar em baronia` + `pen-btn-commit` + `pendingSelectName` + `resolveAutoSelect` + `selectBarony` in CanvasViewer.tsx (the action-bar DOM buttons + auto-select watcher both live here); `## Phase 08.3-08 Supplement` in 08.3-UI-SPEC.md.
</verification>

<success_criteria>
- The user can freehand-drag a clean (Douglas-Peucker simplified + smoothed) contour inside a barony — proven by Task 1 + the UAT.
- The freehand contour feeds 08.3-07's op_type:'carve' (reuse, not reinvented) — proven by Task 2 (no new backend call) + the carve appearing colored in the UAT.
- Visible on-canvas buttons (Fechar contorno / Transformar em baronia / Cancelar / Desfazer último ponto) call the same handlers as the shortcuts — proven by Task 3 + real-mouse button clicks in the UAT.
- Per-state PT-BR hints change with the draw state — Task 3 + the human feel-check.
- After close+commit (carve), V fires (already does) AND the new enclave N is auto-selected via name→selectBarony AFTER the refetch settles — proven by Task 4 (resolveAutoSelect + effectiveBaronies-keyed effect) + the UAT auto-select-through-DOM assertion.
- Iberia 10/10 parity unchanged on the zero-edit path (frontend-only plan).
</success_criteria>

<output>
After completion, create `.planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-08-SUMMARY.md`.

During execution, append the three requirement IDs this plan satisfies to `.planning/REQUIREMENTS.md`
(referenced in this plan's `requirements:` frontmatter):
- `FREEHAND-DRAW-01` — The user can drag a continuous freehand (lasso) contour inside an existing barony; raw samples are Douglas-Peucker simplified + smoothed into a clean closed ring that feeds 08.3-07's op_type:'carve'.
- `DRAW-BUTTONS-01` — While drawing, on-canvas buttons (Fechar contorno / Transformar em baronia / Cancelar / Desfazer último ponto) + per-state PT-BR hints drive the draw actions, calling the same handlers as the keyboard shortcuts.
- `POSTCLOSE-SELECT-01` — After a contour closes+commits, the tool switches to V and the new enclave is auto-selected (name→selectBarony fired only after the baroniesQ refetch settles), making its Bézier handles immediately editable.
</output>
