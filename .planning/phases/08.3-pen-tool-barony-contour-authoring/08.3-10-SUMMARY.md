---
phase: 08.3-pen-tool-barony-contour-authoring
plan: 10
gap_closure: true
subsystem: frontend/canvas
tags: [pen-tool, extend, vertex-graft, arc-splice, gap-closure, uat]
depends_on: ["08.3-05", "08.3-07"]
provides: [PEN-EXTEND-01]
partial: []
affects: [PenDrawLayer, manual_edit replay]
tech_stack:
  added: []
  patterns:
    - "EXTEND graft = pure spliceExtendRing(contour, arc): shorter-arc replacement (A3) between the two snapped contact points → full new ring written as ${name}#i"
    - "stale-key trap fix: buildExtendVerticesMap deletes ALL prior ${name}#* keys before writing the new contiguous ring (a spliced ring is a different length; leftover #N keys would corrupt the polygon at manual_edit.py:622-638)"
    - "no new backend op type — replay_vertex_ring rebuilds the grown barony from the ${name}#i ring on Apply; dispatch is keyed by vertices-dict keys, not the op's baronyName"
key_files:
  created:
    - frontend/src/lib/extendGraft.ts
    - frontend/src/lib/extendGraft.test.ts
    - frontend/src/components/canvas/PenDrawLayer.extend.test.tsx
    - frontend/tests/uat/playwright/08.3-pen-extend.spec.ts
  modified:
    - backend/tests/unit/test_manual_edit_create.py
    - frontend/src/components/canvas/PenDrawLayer.tsx
decisions:
  - "commitExtend was a no-op stub (passed currentVertices unchanged + synthetic extend-${i} ids); replaced with the real arc-graft. Verifier-found gap PEN-EXTEND-01."
  - "shorter-arc replacement (RESEARCH A3) with explicit tie-break; contact points = arc[0] and arc[last] of the OPEN flattened drawn path (flattenPenPathOpen)"
  - "contour read from neighborCandidates by normalizeCandidate(f).id === targetBaronyId at commit time"
  - "over-neighbour extend (D-07) NOT clipped in replay (difference() is CREATE-only) — accepted threat T-08.3-10-03; backend test grows into ocean only"
requirements-completed: [PEN-EXTEND-01]
duration: ~checkpoint-session
completed: 2026-06-01
---

# Phase 08.3 Plan 10 (gap closure): Real pen EXTEND — arc graft into existing barony Summary

**Closed the verifier-found gap PEN-EXTEND-01: `commitExtend` was a no-op stub that discarded the drawn arc. It now splices the drawn arc into the target barony's contour (shorter-arc replacement, A3), writes the full new ring as `${name}#i`, and the existing backend `replay_vertex_ring` regrows the barony on Apply.**

## Performance
- **Tasks:** 4 automated (1–4) + Task 5 human-verify checkpoint (approved by user)
- **Completed:** 2026-06-01

## Accomplishments
- **Backend round-trip test** (`test_manual_edit_create.py::TestExtendViaVertexRing`): an extended ring grows the barony into ocean with NO `-1`/`9999` sentinel and neighbour pixel counts unchanged. 2/2 GREEN. Parity unchanged (22 passed / 6 xfail / 4 xpass / 0 failed).
- **Real commitExtend** (`extendGraft.ts` + `PenDrawLayer.tsx`): pure `spliceExtendRing` (shorter-arc replacement, A3) + `buildExtendVerticesMap` (deletes stale `${name}#*`, writes contiguous `#0..#N`, logs an `op:'add'` so `vertex_ops_present=true`). 12 vitest (extendGraft) + 11 vitest (PenDrawLayer.extend) GREEN; tsc clean.
- **Routing confirmed** (Task 3): the EXTEND entry gesture (first anchor snaps to an existing contour → extendMode) and both close routes already reach `commitExtend`; only the body was broken. +3 routing-guard vitest. Full suite 650/650 GREEN.
- **Real-mouse UAT** (`08.3-pen-extend.spec.ts`): draw arc on a barony edge → Apply → reload → barony grew (lookup_barony SHA changes; region is that barony's idx; persists). Zero `__forge*` input gestures.
- **Live confirm:** orchestrator restarted both servers, verified clean boot (0 console errors with the new code). User confirmed live ("aproved"): the barony visibly extends along the drawn arc and persists on reload.

## Task Commits
1. **Task 1: backend extend round-trip test (grows into ocean, no sentinel, neighbours intact)** — `d7b65ad`
2. **Task 2: real commitExtend — spliceExtendRing + buildExtendVerticesMap + vitest** — `ae6bf86`
3. **Task 3: confirm EXTEND entry + close routing reaches commitExtend + guard tests** — `77ba2bb`
4. **Task 4: real-mouse Playwright UAT spec** — `f1b6b6d`

## Deviations from Plan
None material. Backend needed zero changes (replay_vertex_ring already handles the ring) — the backend test proved it GREEN as anticipated.

## Issues Encountered
- The stub had a double bug (synthetic keys never matched + currentVertices passed unchanged); both fixed by writing the real `${name}#i` ring and logging a vertex op.

## Next Phase Readiness
- **Phase 08.3 is now feature-complete:** CREATE (01-06) + CARVE (07) + freehand/buttons/auto-select (08) + condado picker (09) + EXTEND (10). PEN-EXTEND-01 closed.
- Deferred follow-ups (separate plans): condado-boundary editing, shared-border create-vs-extend prompt, ghost preview for border edits, and the new user ask — drag + edit a freshly pen-drawn shape pre-Apply.

---
*Phase: 08.3-pen-tool-barony-contour-authoring*
*Completed: 2026-06-01*
