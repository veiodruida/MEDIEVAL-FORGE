---
phase: 08.3-pen-tool-barony-contour-authoring
plan: 06
type: execute
wave: 4
depends_on: ["08.3-01", "08.3-05"]
files_modified:
  - frontend/tests/uat/playwright/08.3-pen-tool.spec.ts
autonomous: false
requirements: [PEN-UAT-01, PEN-PARITY-01]
must_haves:
  truths:
    - "A real-mouse pen draw of a gap-filling barony produces a new colored barony on the BaronyLayer after Apply"
    - "The new barony is present in the 12-file export (lookup_barony.png non-ocean RGB + territory_metadata.json entry)"
    - "The new barony survives a page reload"
    - "Iberia 10/10 parity + empty-log identity remain byte-equal end-to-end (zero-edit path)"
  artifacts:
    - path: "frontend/tests/uat/playwright/08.3-pen-tool.spec.ts"
      provides: "PEN-UAT-01 end-to-end pen-create-to-export-to-reload scenario"
      contains: "edit-tool-p"
  key_links:
    - from: "pen draw + Apply"
      to: "BaronyLayer colored + export"
      via: "baroniesQ refetch + /export"
      pattern: "edit-tool-p"
---

<objective>
Phase gate: the real-mouse UAT (PEN-UAT-01) and the explicit end-to-end parity-identity assertion
(PEN-PARITY-01). Proves the whole chain — Plan 05 pen UI → Plan 03 create op → Plan 04 handler →
Plan 01 convergence → colored map + 12-file export → survives reload — and that the zero-edit path
stays byte-equal after all CREATE machinery landed.

This is the only plan with a checkpoint: a human verifies the Photoshop pen FEEL (click=anchor,
drag=curve, snap across zoom) that headless Playwright cannot fully assert (VALIDATION.md
manual-only checks). The automated Playwright spec covers the happy-path artifact chain via the
DEV hatch (__forgePenState) since real ~3px-anchor mouse precision at fit scale is non-deterministic
(the plan-sanctioned DEV-hook pattern established in 08.1-04 / 04.1-05).

Purpose: closes PEN-UAT-01 + re-confirms PEN-PARITY-01 at the integration boundary.
Output: the Playwright spec + a human feel-check sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-UI-SPEC.md
@.planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-VALIDATION.md

<interfaces>
data-testids (UI-SPEC §data-testid Anchors): edit-tool-p, pen-draw-layer, pen-first-anchor,
pen-snap-vertex, pen-snap-edge, pen-validation-error, pen-rubber-band.
DEV hatch: window.__forgePenState (Plan 05 Task 2) — deterministic anchor placement for Playwright,
mirroring 08.1-04 __forgeBezierState / 04.1-05 __forgeStageScale + __forgeSelectBarony.
Existing Playwright patterns: frontend/tests/uat/playwright/ (08.1 / 08.2 specs — mirror server
restart + project-load + Apply + export-poll patterns; reuse the 08.2 "Apply edits" → baroniesQ
refetch assertion).
Parity targets: `cd backend && python -m pytest tests/parity -k "identity or iberia"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright UAT spec — pen-create → colored → export → reload</name>
  <read_first>
    - frontend/tests/uat/playwright/ (existing 08.1/08.2 specs — server restart, project load, Apply, export poll, DEV-hook usage)
    - frontend/src/components/canvas/PenDrawLayer.tsx (the __forgePenState DEV hatch + testids from Plan 05)
    - .planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-UI-SPEC.md (§data-testid Anchors, §Interaction Contract)
    - .planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-VALIDATION.md (PEN-UAT-01 row, manual-only checks)
  </read_first>
  <action>
    Create frontend/tests/uat/playwright/08.3-pen-tool.spec.ts. Follow project memory
    (feedback-server-restart-before-test): shutdown+build+restart servers before the browser run
    (or reuse the existing spec's setup harness). Scenario:
      1. Load a project, select the Pen tool (testid edit-tool-p), assert pen-draw-layer mounts.
      2. Via window.__forgePenState (DEV hatch), deterministically place ≥3 anchors forming a
         gap-filling ring near an existing barony (snap to its contour), then close on the first anchor.
      3. Assert one create op appears in the editLog (read via store DEV access).
      4. Click "Aplicar edições" (08.2 button); wait for baroniesQ refetch; assert the new barony is
         rendered colored on BaronyLayer (non-ocean fill — query the barony by its allocated name/id).
      5. Trigger export (POST /export or the UI button); assert the response/manifest includes the new
         barony in territory_metadata.json and a non-ocean RGB in lookup_barony.png colors json.
      6. Reload the page; assert the new barony still renders (survives reload).
    Real ~3px-anchor mouse precision at fit scale is non-deterministic — use the DEV hatch for anchor
    placement (plan-sanctioned, per 08.1-04 / 04.1-05). Keep one assertion that the pen-rubber-band
    testid appears during draw to prove the live overlay path.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test 08.3-pen-tool.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - 08.3-pen-tool.spec.ts exists and references `edit-tool-p` + `pen-draw-layer`
    - spec covers: pen select → draw (DEV hatch) → close → Apply → colored barony → export contains barony → reload survives
    - `cd frontend && npx playwright test 08.3-pen-tool.spec.ts` passes
  </acceptance_criteria>
  <done>Playwright proves the end-to-end pen-create chain: draw → create op → Apply → colored BaronyLayer → in 12-file export → survives reload.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end parity-identity gate (PEN-PARITY-01)</name>
  <read_first>
    - backend/tests/parity/ (the -k identity and -k iberia targets)
    - .planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-VALIDATION.md (PEN-PARITY-01 invariants)
  </read_first>
  <action>
    Run the full backend + frontend suites as the phase gate. The zero-edit identity path and Iberia
    10/10 parity MUST be byte-equal AFTER all CREATE machinery (Plans 01-05) landed — this is the
    sacred contract (CLAUDE.md, empty-log short-circuit manual_edit.py:98-101 untouched). No new test
    file; this task is the explicit gate that ties PEN-PARITY-01 to the phase boundary. If anything is
    red, the phase is not done. Confirm `cd backend && python -m pytest -q` (incl. tests/parity) and
    `cd frontend && npx vitest run` both green, plus the new Playwright spec from Task 1.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/parity -k "identity or iberia" -q && python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/parity -k "identity or iberia" -q` exits 0
    - `cd backend && python -m pytest -q` (full suite incl. parity) exits 0
    - `cd frontend && npx vitest run` exits 0
  </acceptance_criteria>
  <done>Iberia 10/10 parity + empty-log identity byte-equal end-to-end; full backend + frontend suites green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human feel-check — Photoshop pen interaction + snap across zoom</name>
  <action>Checkpoint — pause execution and ask the human to manually verify the pen FEEL per the steps in how-to-verify (VALIDATION.md manual-only checks: Photoshop pen feel + zoom-constant snap that headless Playwright cannot fully assert). Do not proceed past this task until the human responds with the resume signal.</action>
  <what-built>
    Pen tool (P) in the EditToolPalette: click = straight anchor, click-drag = Bézier curve handle,
    alt-drag adjusts the current anchor, close by clicking the first anchor (which pulses when
    closeable), Esc discards, mid-draw Ctrl+Z removes the last anchor. Snapping to neighbor vertices
    AND edges at ~12 screen-px (zoom-aware). On close, the new barony loads into the 08.1 Bézier
    editor (D-04). "Aplicar edições" colors it on the map.
  </what-built>
  <how-to-verify>
    1. Restart servers (shutdown + build + restart) before opening the browser.
    2. Open a project, press P (or click the pen button — tooltip "Caneta — desenhar contorno de
       baronato (P)").
    3. Click several straight anchors in the gap next to an existing barony; then click-drag one to
       pull a Bézier curve. Confirm the amber dashed rubber-band follows the cursor and the status
       hint "Clique: âncora reta — Arraste: curva Bézier" shows.
    4. Move the cursor near a neighbor barony's edge/vertex — confirm the amber snap marker appears
       (vertex circle or edge highlight). Zoom in and out — confirm the snap feel stays ~constant
       (12 screen-px) at any zoom.
    5. Hover the first anchor — confirm it pulses (closeable). Click it to close. Confirm the barony
       loads into the Bézier editor (amber curve-fit overlay).
    6. Click "Aplicar edições" — confirm the new barony appears COLORED on the map (not ocean), with
       the inherited neighbor's condado coloring, and the neighbor barony is NOT overwritten/missing.
    7. Reload — confirm it persists.
    8. Try an invalid close (2 anchors, or a self-crossing ring) — confirm the red overlay + PT-BR
       warning blocks the close.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what felt wrong (snap, curve pull, close, coloring).</resume-signal>
  <verify>Human confirms the 8 how-to-verify steps; types "approved" or reports issues.</verify>
  <done>Human-approved: pen feel (click/drag/alt-drag/close/Esc/Ctrl+Z), zoom-constant snap, colored new barony with neighbor unaffected, persistence on reload, invalid-close blocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright/browser → running stack | Test-only environment; DEV hatches gated on import.meta.env.DEV (08.1-04 / 04.1-05 pattern). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.3-06-01 | Elevation of Privilege | __forgePenState DEV hatch reachable in prod | mitigate | Gate the hatch on `import.meta.env.DEV` (Plan 05), same disposition as 04.1-05 __forgeStageScale. Not present in production build. |
| T-08.3-06-02 | Tampering | export writes outside intended dir (path injection) | accept | Existing export path handling unchanged by this phase; local single-user tool; export dir is server-controlled, not user-supplied. |
</threat_model>

<verification>
- Playwright 08.3 spec green; full backend (incl. parity) + frontend suites green.
- Human sign-off on pen feel + coloring + persistence.
</verification>

<success_criteria>
- PEN-UAT-01: draw gap-filling barony → colored + in export + survives reload.
- PEN-PARITY-01: Iberia 10/10 + identity byte-equal end-to-end.
- Human-verified Photoshop pen feel + zoom-constant snap.
</success_criteria>

<output>
After completion, create `.planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-06-SUMMARY.md`
</output>
