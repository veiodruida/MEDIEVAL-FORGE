---
phase: 08.3-pen-tool-barony-contour-authoring
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/stores/useEditorStore.ts
  - frontend/src/stores/useEditorStore.create.test.ts
  - frontend/src/components/editor/EditToolPalette.tsx
  - frontend/src/components/editor/__tests__/EditToolPalette.pen.test.tsx
  - frontend/src/hooks/useKeyboardShortcuts.ts
autonomous: true
requirements: [PEN-CREATE-01, PEN-CURVE-01]
must_haves:
  truths:
    - "EditTool union accepts 'P'; pressing P selects the pen tool (canvas-focused only)"
    - "EditOp union accepts a 'create' op carrying ring + allocated_original_idx"
    - "A create op committed via setVerticesAndLog produces exactly ONE zundo entry (D-13)"
    - "The Pen tool button renders in EditToolPalette with testid edit-tool-p and disables A/D/S/M when drawing"
  artifacts:
    - path: "frontend/src/stores/useEditorStore.ts"
      provides: "EditTool 'P' + EditOp 'create' variant + the splitPolygon-style allocated_original_idx patch precedent reused"
      contains: "'create'"
    - path: "frontend/src/components/editor/EditToolPalette.tsx"
      provides: "Pen tool descriptor (P, Pencil1Icon, testid edit-tool-p) + DISABLED_TOOLTIP entries for A/D/S/M"
      contains: "edit-tool-p"
    - path: "frontend/src/hooks/useKeyboardShortcuts.ts"
      provides: "'P' key → selectTool('P')"
      contains: "'P'"
  key_links:
    - from: "PenDrawLayer (Plan 05) path close"
      to: "useEditorStore create op"
      via: "setVerticesAndLog(vertices, {op:'create', ring, allocated_original_idx, barony_meta})"
      pattern: "op: 'create'"
    - from: "WorkspaceToolbar (Plan 05)"
      to: "EditToolPalette disabledTools"
      via: "penDrawing ? ['A','D','S','M'] : []"
      pattern: "disabledTools"
---

<objective>
The store + palette + keyboard CONTRACT that Plan 05 (PenDrawLayer) builds against. Interface-first:
define the `'P'` tool, the `'create'` EditOp shape, the palette button, and the P shortcut now so the
PenDrawLayer plan receives the contract instead of scavenging for it. No canvas drawing logic here —
that is Plan 05.

Purpose: PEN-CREATE-01 requires a `create` op in the editLog and a `P` tool entry; PEN-CURVE-01's
store-round-trip + single-undo-entry behavior is asserted here at unit level. The
`allocated_original_idx` post-Apply patch mirrors split's existing pattern (useEditorStore.ts
~277-289) so the backend (Plan 01/04) and frontend agree on the op shape.
Output: extended store unions + a tested create-op commit path + the Pen palette button + P shortcut.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-UI-SPEC.md
@.planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-RESEARCH.md

<interfaces>
From frontend/src/stores/useEditorStore.ts:
```typescript
// line 25:
export type EditTool = 'V' | 'A' | 'D' | 'S' | 'M' | null;   // ← add 'P'
// line ~32-43: EditOp union (op field lists move/add/delete/split/merge/translate/multi_delete) ← add 'create'
// splitPolygon (lines 227-295): the precedent. After setVerticesAndLog it POSTs /editor/apply,
// receives allocated_original_idx, and patches the LAST editLog entry (lines 279-289):
useEditorStore.setState({
  editLog: [
    ...get().editLog.slice(0, -1),
    { ...get().editLog[get().editLog.length - 1],
      allocated_original_idx: result.allocated_original_idx },
  ],
});
```
The `create` op payload (UI-SPEC §Store Extension + RESEARCH Target 6):
```typescript
{ op: 'create', ts: number,
  ring: Array<{ lat: number; lon: number }>,   // flattened closed ring
  allocated_original_idx: number | null,        // null until server allocates
  barony_meta: { name: string; condado_idx: number; duchy_id: string;
                 kingdom_id: string; country: 'PT' | 'ES' } }
```

From frontend/src/components/editor/EditToolPalette.tsx (lines 53-95):
  TOOL_DESCRIPTORS array (each: {key, testid, icon, tooltip}); DISABLED_TOOLTIP partial record;
  `disabledTools` prop already passed by WorkspaceToolbar (mechanical one-entry addition).
  Pencil1Icon is CONFIRMED present in @radix-ui/react-icons@1.3.2
  (node_modules/@radix-ui/react-icons/dist/Pencil1Icon.d.ts).

From frontend/src/hooks/useKeyboardShortcuts.ts:
  V/A/D/S/M each map a key → selectTool(key), canvas-focused only. Add 'P' the same way.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Store — add 'P' tool + 'create' EditOp + tested single-undo-entry commit</name>
  <read_first>
    - frontend/src/stores/useEditorStore.ts (EditTool line 25, EditOp union 32-43, splitPolygon 227-295 incl. allocated_original_idx patch 279-289, setVerticesAndLog chokepoint)
    - frontend/src/stores/ (existing useEditorStore test files to mirror temporal/zundo test setup, e.g. the 08.1/08.2 store tests)
    - .planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-UI-SPEC.md (§Store Extension)
  </read_first>
  <behavior>
    - Test "create op commits as single editLog entry": setVerticesAndLog(vertices, createOp) appends
      exactly one entry with op==='create', ring present, allocated_original_idx===null.
    - Test "create op is one zundo entry (D-13)": after committing one create op, temporal undo once
      removes it entirely (editLog back to prior length); pen mid-draw anchors never reached the store.
    - Test "allocated_original_idx patch (split precedent)": simulate the post-Apply patch updating
      editLog[-1].allocated_original_idx from null to a number, mirroring split lines 279-289.
    - Test "barony_meta round-trips": condado_idx/duchy_id/kingdom_id/country survive the commit.
  </behavior>
  <action>
    Edit useEditorStore.ts:
      1. `export type EditTool = 'V' | 'A' | 'D' | 'S' | 'M' | 'P' | null;`
      2. Add `'create'` to the EditOp `op` union, with the payload fields `ring`,
         `allocated_original_idx`, `barony_meta` (typed exactly per the interface block above). Make
         ring/barony_meta required and allocated_original_idx `number | null`.
      3. Do NOT add a `createPolygon` action here (the actual POST/Apply orchestration lives in
         Plan 05's PenDrawLayer + Plan 04's endpoint). This plan only widens the TYPES + proves the
         commit + undo behavior. If a thin helper is convenient, model it on splitPolygon's structure
         (setVerticesAndLog → patch editLog[-1].allocated_original_idx) but keep it out of scope unless
         a test needs it.
    Write frontend/src/stores/useEditorStore.create.test.ts with the four behaviors above. Use the
    existing temporal API pattern: `useEditorStore.temporal.getState()` (per 08.1 Rule-3 note).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/stores/useEditorStore.create.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - useEditorStore.ts EditTool union contains `'P'`
    - useEditorStore.ts EditOp union contains `'create'` with `ring` and `allocated_original_idx` fields
    - useEditorStore.create.test.ts exists with ≥4 `test`/`it` cases incl. a single-zundo-entry assertion
    - `npx vitest run src/stores/useEditorStore.create.test.ts` exits 0; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Store types widened for 'P' + 'create'; commit produces one editLog + one zundo entry; allocated_original_idx patch mirrors split; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Palette + keyboard — Pen tool button (P) + P shortcut + A/D/S/M disable</name>
  <read_first>
    - frontend/src/components/editor/EditToolPalette.tsx (TOOL_DESCRIPTORS 53-95, DISABLED_TOOLTIP, disabledTools prop)
    - frontend/src/hooks/useKeyboardShortcuts.ts (V/A/D/S/M key handlers)
    - frontend/node_modules/@radix-ui/react-icons/dist/Pencil1Icon.d.ts (confirm import)
    - .planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-UI-SPEC.md (§Tool Palette Entry, §Disabled state)
  </read_first>
  <action>
    EditToolPalette.tsx:
      - Import `{ Pencil1Icon }` from '@radix-ui/react-icons'. Add a TOOL_DESCRIPTORS entry:
        `{ key: 'P', testid: 'edit-tool-p', icon: <Pencil1Icon />,
           tooltip: 'Caneta — desenhar contorno de baronato (P)' }`.
      - Add the four DISABLED_TOOLTIP entries (PT-BR copy verbatim from UI-SPEC §Disabled state) for
        A/D/S/M (e.g. A: "Adicionar vértice indisponível enquanto desenhando — feche o caminho
        primeiro", etc.). The palette stays pen-UNAWARE — WorkspaceToolbar (Plan 05) drives the
        `disabledTools` set; this task only supplies the tooltip COPY so disabled buttons explain why.
    useKeyboardShortcuts.ts: add `'P'` (case-insensitive, canvas-focused only) → `selectTool('P')`,
      same guard pattern as V/A/D/S/M.
    Write frontend/src/components/editor/__tests__/EditToolPalette.pen.test.tsx: renders the palette,
      asserts a button with testid `edit-tool-p` exists with the pen tooltip; renders with
      `disabledTools={['A','D','S','M']}` and asserts those four buttons are disabled and V/P are not.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/editor/__tests__/EditToolPalette.pen.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - EditToolPalette.tsx imports `Pencil1Icon` and TOOL_DESCRIPTORS contains `testid: 'edit-tool-p'`
    - DISABLED_TOOLTIP has entries for 'A','D','S','M' with the UI-SPEC PT-BR strings
    - useKeyboardShortcuts.ts contains a `'P'` → selectTool('P') handler
    - EditToolPalette.pen.test.tsx passes; with disabledTools=['A','D','S','M'] those buttons are disabled, V and P are not
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Pen button renders with testid edit-tool-p + tooltip; P shortcut selects pen; A/D/S/M show disabled tooltips when disabledTools set; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard/UI → store | Local single-user; tool selection + op typing only. No untrusted network input at this layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.3-03-01 | Tampering | create op committed with allocated_original_idx left null at Apply time | mitigate | Backend reads allocated idx from the persisted edit_events row, not the frontend snapshot (RESEARCH Pitfall 6); Plan 04 enforces. Store-level patch mirrors split's proven flow. |
| T-08.3-03-02 | Denial of Service | unbounded ring in create op | accept (here) | Ring-length cap enforced in Plan 04 Pydantic model (10000) and client validation Plan 05 (D-17). This plan only types the field. |
</threat_model>

<verification>
- vitest green for both new test files; tsc clean.
- No backend changes; no parity impact.
</verification>

<success_criteria>
- EditTool 'P' + EditOp 'create' typed and committable as a single undoable entry.
- Pen palette button + P shortcut + A/D/S/M disable tooltips in place (the contract Plan 05 consumes).
</success_criteria>

<output>
After completion, create `.planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-03-SUMMARY.md`
</output>
