---
phase: 08.3-pen-tool-barony-contour-authoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/medieval_forge/services/canvas_sidecars.py
  - backend/tests/unit/test_canvas_sidecars_hierarchy.py
  - frontend/src/hooks/useCanvasArtifacts.ts
  - frontend/src/hooks/__tests__/useCanvasArtifacts.hierarchy.test.ts
autonomous: true
requirements: [PEN-ASSIGN-01]
must_haves:
  truths:
    - "Each barony Feature in baronies.geojson carries condado_idx, duchy_id, kingdom_id"
    - "BaronyFeature TS type exposes those three fields so the pen tool can inherit them (D-09)"
    - "Existing condado_id + centroid fields are unchanged (no regression)"
  artifacts:
    - path: "backend/medieval_forge/services/canvas_sidecars.py"
      provides: "barony Feature properties extended with condado_idx, duchy_id, kingdom_id"
      contains: "condado_idx"
    - path: "frontend/src/hooks/useCanvasArtifacts.ts"
      provides: "BaronyFeature.properties extended with the three hierarchy fields (optional for old artifacts)"
      contains: "condado_idx"
  key_links:
    - from: "canvas_sidecars.py barony emitter"
      to: "baronies.geojson properties"
      via: "b['condado_idx'] + duchies[duchy_id] lookup"
      pattern: "condado_idx"
    - from: "PenDrawLayer (Plan 05) D-09 inherit"
      to: "baroniesQ.data[].properties.condado_idx"
      via: "snap-neighbor lookup reads these fields"
      pattern: "condado_idx"
---

<objective>
Resolve RESEARCH Open Question 1 / Assumption A2: the canvas barony sidecar currently emits
`condado_id` (string) + `centroid` but NOT `condado_idx`, `duchy_id`, `kingdom_id`. D-09
("new barony inherits the condado/duchy/kingdom of its snap neighbor") needs those numeric/id
fields on the frontend so PenDrawLayer (Plan 05) can build the `create` op's `barony_meta` from the
snapped neighbor barony. This plan adds the three fields to the backend emitter and the frontend
BaronyFeature type.

Purpose: without these fields, the pen tool cannot inherit hierarchy and PEN-ASSIGN-01 cannot be
satisfied client-side. This is a small, parity-safe additive change (extra JSON properties + extra
optional TS fields).
Output: extended baronies.geojson Feature properties + extended BaronyFeature type, both tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-RESEARCH.md

<interfaces>
From backend/medieval_forge/services/canvas_sidecars.py (CURRENT barony emitter ~205-236):
```python
for bi, b in enumerate(bars):
    ...
    condado_idx = b["condado_idx"]
    condado_id = cfg.condados[condado_idx][0] if 0 <= condado_idx < len(cfg.condados) else ""
    ...
    features.append({
        "type": "Feature", "id": bname, "geometry": lonlat_geom,
        "properties": {
            "id": bname, "name": bname, "condado_id": condado_id,
            "centroid": [lon, lat], "pixel_count": npx,
        },
    })
```
Note: condado tuple structure — `cfg.condados[condado_idx]` is a tuple; `[0]` is the condado_id
string. `duchies` dict is in scope (used at line ~128-129: `duchy_id = c[4]`,
`kingdom_id = duchies[duchy_id][0] if duchy_id in duchies else ""`). For a barony, derive
duchy_id/kingdom_id from its condado tuple the same way the condado emitter does — read the condado
tuple fields (confirm indices by reading the condado emitter block at ~120-141).

From frontend/src/hooks/useCanvasArtifacts.ts (CURRENT BaronyFeature, line 68-85):
```typescript
interface BaronyFeature {
  type: 'Feature'; id: string
  geometry: { type: 'Polygon'; ... } | { type: 'MultiPolygon'; ... }
  properties: {
    id: string; name: string; condado_id: string; fill: string
    centroid?: [number, number]   // optional — old artifacts lack it
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — emit condado_idx + duchy_id + kingdom_id in baronies.geojson</name>
  <read_first>
    - backend/medieval_forge/services/canvas_sidecars.py (FULL file — condado emitter ~120-141 to copy the duchy/kingdom derivation, barony emitter ~205-236 to extend)
    - backend/tests/unit/ (find an existing canvas_sidecars test to mirror fixture setup; if none, create the test fresh)
  </read_first>
  <action>
    In the barony emitter loop (~205-236), add three properties to the Feature. Reuse the same
    derivation the condado emitter uses (read ~120-141 for exact tuple indices):
      - `condado_idx`: already in scope as `b["condado_idx"]` — emit it directly (int).
      - `duchy_id`: read from the condado tuple `cfg.condados[condado_idx]` at the same index the
        condado emitter uses (`c[4]` per line 128). Guard with the same `0 <= condado_idx < len(...)`.
      - `kingdom_id`: `duchies[duchy_id][0] if duchy_id in duchies else ""` (mirror line 129).
    Extend the `properties` dict with `"condado_idx": condado_idx, "duchy_id": duchy_id,
    "kingdom_id": kingdom_id`. Leave existing properties (`id`, `name`, `condado_id`, `centroid`,
    `pixel_count`) untouched. Then write backend/tests/unit/test_canvas_sidecars_hierarchy.py with a
    small fixture (2 baronies in 1 condado in 1 duchy/kingdom, explicit numeric idx) asserting the
    three new properties appear with the expected values, and that condado_id + centroid are still
    present (no regression).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_canvas_sidecars_hierarchy.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - canvas_sidecars.py barony emitter `properties` dict contains `"condado_idx"`, `"duchy_id"`, `"kingdom_id"`
    - test_canvas_sidecars_hierarchy.py exists, asserts all three new fields + condado_id + centroid present
    - `cd backend && python -m pytest tests/unit/test_canvas_sidecars_hierarchy.py -x -q` exits 0
    - `cd backend && python -m pytest tests/parity -k iberia -q` still exits 0 (additive change, parity safe)
  </acceptance_criteria>
  <done>baronies.geojson Features carry condado_idx/duchy_id/kingdom_id; existing fields intact; new unit test green; Iberia parity unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — extend BaronyFeature type with the three hierarchy fields</name>
  <read_first>
    - frontend/src/hooks/useCanvasArtifacts.ts (BaronyFeature interface 68-85)
    - frontend/src/hooks/__tests__/ (find an existing useCanvasArtifacts test to mirror, if present)
  </read_first>
  <action>
    Add to `BaronyFeature.properties`: `condado_idx?: number`, `duchy_id?: string`,
    `kingdom_id?: string` — all OPTIONAL (old artifacts on disk pre-08.3 lack them; downstream falls
    back to undefined, mirroring the existing `centroid?` comment). Do NOT remove `condado_id` or
    `centroid`. Add a small vitest
    (frontend/src/hooks/__tests__/useCanvasArtifacts.hierarchy.test.ts) that constructs a mock barony
    Feature WITH the three fields and asserts the type compiles + values read through (a type-level +
    runtime read test; the mock JSON includes condado_idx:0, duchy_id:'D1', kingdom_id:'K1').
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/__tests__/useCanvasArtifacts.hierarchy.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - useCanvasArtifacts.ts BaronyFeature.properties contains `condado_idx`, `duchy_id`, `kingdom_id` (all optional `?`)
    - condado_id and centroid still present in the interface
    - new vitest file passes; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>BaronyFeature exposes the three optional hierarchy fields; tsc clean; vitest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → baronies.geojson | Backend-generated artifact; no untrusted input. Additive JSON fields only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.3-02-01 | Information Disclosure | baronies.geojson hierarchy fields | accept | Local single-user desktop tool; hierarchy ids are non-sensitive map metadata already partially exposed (condado_id). No new exposure of concern. |
| T-08.3-02-02 | Tampering | condado_idx out of range → emitter crash | mitigate | Reuse existing `0 <= condado_idx < len(cfg.condados)` guard (already in the emitter); duchy lookup guarded with `if duchy_id in duchies`. |
</threat_model>

<verification>
- Iberia parity unchanged (additive properties don't alter lookup/visual PNGs or metadata contract).
- Frontend tsc clean; both new tests green.
</verification>

<success_criteria>
- Backend emits the three fields; frontend type exposes them optionally (PEN-ASSIGN-01 unblocked for Plan 05).
- Zero regression to existing sidecar fields or Iberia parity.
</success_criteria>

<output>
After completion, create `.planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-02-SUMMARY.md`
</output>
