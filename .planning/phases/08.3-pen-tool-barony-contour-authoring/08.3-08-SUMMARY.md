---
phase: 08.3-pen-tool-barony-contour-authoring
plan: 08
subsystem: frontend/canvas
tags: [freehand, lasso, douglas-peucker, pen-tool, action-buttons, auto-select, ghost-preview, carve, uat]
depends_on: ["08.3-07"]
provides: [FREEHAND-DRAW-01, DRAW-BUTTONS-01, POSTCLOSE-SELECT-01]
partial: []
affects: [PenDrawLayer, CanvasViewer, 08.3-09]
tech_stack:
  added: []
  patterns:
    - "freehand lasso: raw drag samples → Douglas-Peucker simplify + 3-tap smooth (sampleAndSimplifyFreehand) → existing commitCreate carve path"
    - "on-canvas action bar: DOM buttons in CanvasViewer overlay call PenDrawHandlers exposed via onHandlersReady (Konva-land handlers, DOM-land buttons)"
    - "post-close auto-select: pure resolveAutoSelect + effectiveBaronies-keyed watcher in CanvasViewer (not optimistic — fires only after refetch carries the new name)"
    - "ghost preview: pending create/carve ring rendered as dashed semi-transparent Konva overlay until Apply re-renders"
key_files:
  created:
    - frontend/src/lib/__tests__/penFreehand.test.ts
    - frontend/src/components/canvas/__tests__/postCloseAutoSelect.test.ts
    - frontend/tests/uat/playwright/08.3-freehand-carve.spec.ts
  modified:
    - frontend/src/lib/penFlatten.ts
    - frontend/src/components/canvas/PenDrawLayer.tsx
    - frontend/src/components/canvas/CanvasViewer.tsx
    - .planning/phases/08.3-pen-tool-barony-contour-authoring/08.3-UI-SPEC.md
decisions:
  - "freehand drag reuses the EXISTING commitCreate carve detection (centroid-in-parent → op_type:'carve') — geometry not reinvented (build on 08.3-07)"
  - "action-bar buttons are DOM (CanvasViewer overlay), not Konva — DOM cannot render inside PenDrawLayer's Konva tree; handlers bridged via onHandlersReady"
  - "auto-select join key is the barony NAME (Baronato-${ts}), not original_idx — baronies.geojson emits no original_idx"
  - "ghost preview (user request 2026-06-01): dashed semi-transparent ring in CanvasViewer (stays mounted; PenDrawLayer unmounts on close), cleared on Apply re-render"
  - "[Rule 1] validate() now checks pts.length not anchorsRef.current.length — fixed silent rejection of valid freehand rings"
requirements-completed: [FREEHAND-DRAW-01, DRAW-BUTTONS-01, POSTCLOSE-SELECT-01]
duration: ~checkpoint-session
completed: 2026-06-01
---

# Phase 08.3 Plan 08: Freehand draw + on-canvas buttons + post-close auto-select Summary

**Freehand lasso (Douglas-Peucker simplified) feeding the 07 carve path, on-canvas action bar + PT-BR hints, post-refetch auto-select of the new enclave, plus a user-requested ghost preview of the pending contour.**

## Performance
- **Tasks:** 5 automated (1–5) + Task 6 human-verify checkpoint (approved by user)
- **Completed:** 2026-06-01

## Accomplishments
- `sampleAndSimplifyFreehand` (penFlatten.ts): jagged drag samples → clean closed ring (Douglas-Peucker + 3-tap smooth). 4 vitest green.
- Freehand lasso draw in PenDrawLayer routes into the existing `commitCreate` carve detection (op_type:'carve') — verified live (`isCarve, parent: Toledo`).
- On-canvas action bar (DOM in CanvasViewer): `pen-btn-close` / `pen-btn-commit` / `pen-btn-cancel` / `pen-btn-undo` + `pen-freehand-toggle`; PT-BR contextual hints per draw state.
- Post-close auto-select (D-19): pure `resolveAutoSelect` (7 vitest green) + effectiveBaronies-keyed watcher in CanvasViewer (non-optimistic).
- Ghost preview of the pending edit ring (user request) — dashed semi-transparent overlay before Apply.
- Playwright UAT `08.3-freehand-carve.spec.ts` (2/2 green) drives the freehand drag with REAL `page.mouse.down/move/up` (not DEV hooks) — addresses the 06/07 hook-only-green gap.
- User confirmed live: freehand clean, buttons/hints work, ghost shows, **auto-select works** after Transformar em baronia + Aplicar edições.

## Task Commits
1. **Task 1: sampleAndSimplifyFreehand + vitest** — `944f925` (feat, RED→GREEN)
2. **Task 2: freehand lasso → commitCreate carve** — `a3caeca` (feat)
3. **Task 3: action buttons + hints + UI-SPEC supplement** — `3364417` (feat)
4. **Task 4: post-close auto-select + vitest** — `3b98ffd` (feat, RED→GREEN)
5. **Task 5: real-mouse freehand→carve→auto-select UAT** — `884fd9a` (test)

## Deviations from Plan
### Additions / auto-fixes
**1. [User request] Ghost preview of the pending edit**
- Added a dashed semi-transparent overlay of the pending create/carve ring in CanvasViewer, cleared on Apply. Requested during plan-07 UAT for pre-Apply confidence.

**2. [Rule 1 - Correctness] validate() length check**
- `validate()` checked `anchorsRef.current.length` instead of the passed `pts.length`, silently rejecting valid freehand rings. Fixed to use `pts.length`.

**Impact:** Both necessary; frontend-only; zero-edit parity unaffected.

## Issues Encountered
- D-19 auto-select was flagged uncertain (name must survive render→sidecar→refetch). Confirmed working by the user in live UAT.

## Next Phase Readiness
- Plan 09 (condado picker, Wave 3) can hook into the freehand/click carve commit path to override `barony_meta.condado_idx`.
- **Open follow-up (user request 2026-06-01):** extend the ghost/pending-preview mechanic to BORDER editing (BezierEditLayer vertex/contour edits of an existing barony), not just new-enclave carve. Out of plan-08 scope (touches the 08.1 Bézier editor) — to be scoped as a separate plan.

---
*Phase: 08.3-pen-tool-barony-contour-authoring*
*Completed: 2026-06-01*
