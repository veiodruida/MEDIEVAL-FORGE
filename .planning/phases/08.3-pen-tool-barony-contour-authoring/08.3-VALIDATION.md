---
phase: 08.3
slug: pen-tool-barony-contour-authoring
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-30
---

> nyquist_compliant: plans satisfy Nyquist design (every task has an `<automated>` verify, no
> watch-mode flags, sampling continuity holds, Wave-0 TDD pattern covers the two RESEARCH bugs).
> wave_0_complete stays false until /gsd-execute-phase actually runs Wave 1.

# Phase 08.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Filled from RESEARCH.md "## Validation Architecture". Planner refines the per-task map.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x (backend) + vitest (frontend) + Playwright (UAT) |
| **Config file** | `backend/pyproject.toml`, `frontend/vitest.config.ts`, `frontend/playwright.config.ts` |
| **Quick run command** | `cd backend && python -m pytest tests/unit -q` / `cd frontend && npx vitest run` |
| **Full suite command** | `cd backend && python -m pytest -q` (incl. tests/parity) |
| **Estimated runtime** | ~60–90s backend full, ~15s frontend unit |

---

## Sampling Rate

- **After every task commit:** Run the quick run command for the touched layer.
- **After every plan wave:** Run the full suite command.
- **Before `/gsd-verify-work`:** Full suite must be green, including non-skippable `tests/parity` (Iberia 10/10).
- **Max feedback latency:** ~90 seconds.

---

## Per-Task Verification Map

> Planner fills this from the final task breakdown. Required invariants below MUST each map to ≥1 automated test.

| Invariant | Requirement | Test Type | Automated Command (target) | Status |
|-----------|-------------|-----------|----------------------------|--------|
| Empty-log identity parity (zero-edit → byte-equal export) | PEN-PARITY-01 | parity | `pytest tests/parity -k identity` | ⬜ pending |
| Iberia 10/10 parity unchanged on zero-edit path | PEN-PARITY-01 | parity | `pytest tests/parity -k iberia` | ⬜ pending |
| `allocate_next_original_idx` seeds high-water to `nb` (no alias of existing idx) | PEN-ASSIGN-01 | unit | `pytest tests/unit -k allocator_seed` | ⬜ pending |
| CREATE op extends `bars/bc/bd/bk/nb` so allocated idx is not skipped by hierarchy/lookup/metadata | PEN-CONV-01 | unit | `pytest tests/unit -k create_index_alignment` | ⬜ pending |
| New ring rasterised (raster overpaint) into int16 grid at allocated idx | PEN-CREATE-01 / PEN-CONV-01 | unit | `pytest tests/unit -k create_overpaint` | ⬜ pending |
| CREATE clip vs neighbors never yields MultiPolygon that reopens 08.2-05 idx defect | PEN-CREATE-01 | unit | `pytest tests/unit -k create_clip_singlepoly` | ⬜ pending |
| EXTEND graft maps to add/move replay, no new backend op | PEN-EXTEND-01 | unit | `pytest tests/unit -k extend_graft` | ⬜ pending |
| `create` EditOp payload shape (ring {lat,lon} + straight/curve flags) round-trips through store | PEN-CURVE-01 / PEN-CREATE-01 | unit | `vitest run -t "create op"` | ⬜ pending |
| Mid-draw per-anchor Ctrl+Z is component-local; single `create` zundo entry post-close | PEN-CURVE-01 | unit | `vitest run -t "pen undo"` | ⬜ pending |
| Real-mouse: draw gap-filling barony → colored + in export + survives reload | PEN-UAT-01 | uat | `playwright test pen-create` | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Backend unit stubs for allocator seeding + CREATE index alignment (the two RESEARCH-identified bugs) — these are the highest-risk, parity-adjacent changes.
- [ ] Frontend vitest stub for the new `create` EditOp payload + pen-draw undo.
- [ ] Reuse existing `tests/parity` harness — no new framework install needed.

*Existing pytest/vitest/Playwright infrastructure covers all phase requirements; only new test files needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Photoshop pen feel (click=anchor, click-drag=handle, alt-drag current, close on first-anchor) | PEN-CURVE-01 | Interaction feel not fully assertable headless | Draw a mixed straight+curve closed path; confirm rubber-band + snap cues per UI-SPEC |
| Snap radius constant feel across zoom | PEN-CREATE-01 | Visual/zoom-dependent | Zoom in/out; confirm ~12px screen-constant snap |

*Playwright covers the PEN-UAT-01 happy path; the above are feel/visual checks.*

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers the two RESEARCH-identified backend bugs (Plan 01 TDD RED→GREEN)
- [x] No watch-mode flags
- [x] Feedback latency < 90s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-30 (design sign-off; wave_0_complete flips true after execution)
