# Phase 08.3: Pen tool — Photoshop-style barony contour authoring - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-30
**Phase:** 08.3-pen-tool-barony-contour-authoring
**Areas discussed:** Pen interaction + curve, Snapping + gaps, New-barony assignment, Undo granularity, Visual feedback, Snap radius, Pre-Apply validation, Tool entry

---

## Pen interaction + curve model

| Question | Options | User's choice |
|----------|---------|---------------|
| Close a new barony loop | Click first anchor ✓ / Double-click / Enter-button | **Click first anchor** |
| Cancel mid-path | Esc discards all ✓ / Esc removes last / Cancel button | **Esc discards whole path** |
| Edit an already-placed anchor mid-draw | Last only alt-drag ✓ / Any anchor / None | **Last anchor only (alt-drag)** |
| Post-close → editable by 08.1 Bézier editor | Yes reuse 08.1 ✓ / Pen creates only | **Yes, reuse 08.1** |

---

## Snapping & gap/overlap rules

| Question | Options | User's choice |
|----------|---------|---------------|
| CREATE snapping to neighbors | Vertex+edge ✓ / Vertex only / No snap | **Snap vertex AND edge** |
| EXTEND graft to existing contour | Snap 2 ends ✓ / 1 end append / Reuse 08.1 add-vertex | **Snap both ends, replace arc** |
| Overlap with existing barony | Clip ✓ / Block / Overwrite | **Clip against neighbors** |

---

## New-barony assignment policy

| Question | Options | User's choice |
|----------|---------|---------------|
| Which country | Auto by geometry ✓ / User picks / Inherit neighbor | **Auto by geometry (PT/ES border)** |
| Which condado/duchy/kingdom | Inherit snap neighbor ✓ / User picks / Re-cluster | **Inherit from snap neighbor** |
| original_idx allocation | Server allocates new ✓ / Reuse from gap | **Server allocates new, never reuse** |
| Voronoi/KD-tree coexistence | Fixed KD-tree anchor ✓ / Voronoi seed | **Fixed anchor (not re-assimilated)** |

---

## Undo granularity

| Question | Options | User's choice |
|----------|---------|---------------|
| Mid-draw Ctrl+Z | Per anchor ✓ / Whole path | **Per anchor** |
| Post-close history entry | 1 create entry ✓ / Keep per-anchor | **One `create` entry** |
| Undo after Apply | Store only re-Apply ✓ / Auto re-cascade | **Store only, re-Apply to revert map** |

---

## Visual feedback during draw

| Question | Options | User's choice |
|----------|---------|---------------|
| Draw feedback | Full (rubber-band + glyphs + pulse + snap-cue) ✓ / Minimal | **Full** |

---

## Snap radius

| Question | Options | User's choice |
|----------|---------|---------------|
| Radius + zoom | Screen-px zoom-aware ✓ / World-constant | **Screen-px, zoom-aware** |

---

## Pre-Apply validation

| Question | Options | User's choice |
|----------|---------|---------------|
| Blocking validations | All (≥3 anchors, no self-intersect, area≥merge, EXTEND 2 ends) ✓ / Critical only | **All guards** |

---

## Tool entry

| Question | Options | User's choice |
|----------|---------|---------------|
| CREATE/EXTEND in palette | One Pen tool, infer mode ✓ / Two separate buttons | **One Pen tool (P), mode inferred** |

## Claude's Discretion

- Anchor/handle glyph shapes + colors (within amber-dashed family)
- Snap-radius px value + snap-cue visual
- "Apply edits" button copy/placement (08.2 discretion)
- Whether CREATE clip reuses the split/merge Shapely boolean-op path

## Carried Forward (not re-asked)

- 08.2 D-01/02/03: two-mode convergence trigger, 04.1 cancellable render reuse, undo store-only
- 08.1: Bézier-is-UI-only, empty-log identity parity is sacred

## Deferred Ideas

- Re-clustering hierarchy on CREATE; original_idx reuse from gap; hand-drawn as Voronoi seed
  (all rejected in favor of inherit/allocate-new/fixed-anchor)
- Selective per-barony incremental render; touch/stylus input; multi-barony batch draw;
  condado-direct authoring
