---
phase: 4
slug: canvas-editing-basic
status: draft
shadcn_initialized: false
preset: not applicable
created: 2026-04-23
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none (Radix Themes is the design system) | Existing codebase — main.tsx |
| Preset | not applicable | No shadcn; Radix Themes configured in main.tsx |
| Component library | @radix-ui/themes ^3.3.0 | package.json |
| Icon library | @radix-ui/react-icons (add in this phase) | Default — cheapest Radix peer |
| Font | system-ui, sans-serif | DecorationsLayer.tsx (established in Phase 02) |
| Radix Theme config | appearance="light" accentColor="iris" radius="medium" | main.tsx — LOCKED, do not change |

No `components.json` found. shadcn gate: not applicable — Radix Themes is the established system across all prior phases.

---

## Spacing Scale

Declared values (multiples of 4; Radix Themes `gap` prop values shown in parentheses):

| Token | Value | Radix gap | Usage |
|-------|-------|-----------|-------|
| xs | 4px | gap="1" | Icon gaps, tight inline padding |
| sm | 8px | gap="2" | Compact element spacing, toolbar button gap |
| md-radix | 12px | gap="3" | Radix Theme primitive — floating panel internal spacing |
| md | 16px | gap="4" | Default element spacing, card padding |
| lg | 24px | gap="5" | Section padding, sidebar group gap |
| xl | 32px | gap="6" | Layout gaps between major regions |
| 2xl | 48px | gap="8" | Major section breaks |

Exceptions:
- **12px (gap="3")**: Radix Themes gap primitive used sparingly inside floating mini-toolbar and layer toggle panel — matches Phase 02 established usage. Not a new exception; already in production code.
- **44px minHeight**: Minimum touch target height for all interactive toolbar buttons (WCAG AA). Established in FitToViewButton.tsx Phase 02.
- **Absolute positioned overlays**: LayerTogglePanel (top: 12px, left: 12px), FitToViewButton (bottom: 12px, right: 12px) — inherit Phase 02 values unchanged.

---

## Typography

All type is rendered via Radix Themes `<Text>` and `<Heading>` components. The underlying font is `system-ui, sans-serif`. Sizes are Radix scale values (not raw px).

| Role | Radix size | Approx px | Weight | Line Height | Usage |
|------|------------|-----------|--------|-------------|-------|
| Label / caption | size="1" | 12px | regular (400) | 1.5 | Coordinate readouts, area labels, hint text, keyboard shortcut hints |
| Body | size="2" | 14px | regular (400) | 1.5 | Inspector values, toolbar labels, toast body, badge text |
| UI heading | size="3" | 16px | bold (700) | 1.2 | Panel section headings, floating toolbar heading |
| Section heading | size="5" | 20px | bold (700) | 1.2 | Inspector territory name (selected state), major panel titles |

Weights: regular (400) + bold (700) only. No intermediate weights.

Konva canvas text (territory labels): `fontFamily="system-ui, sans-serif"` `fontSize=12` `fill="#1a1a1a"` `stroke="rgba(255,255,255,0.7)"` `strokeWidth=1` — established in DecorationsLayer.tsx, unchanged in Phase 04.

---

## Color

Radix Themes semantic tokens govern all color. Raw hex values are shown for Konva canvas elements that cannot consume CSS variables.

| Role | Radix token / Hex | Usage |
|------|-------------------|-------|
| Dominant (60%) | Radix `--color-background` (light: #ffffff / near-white) | Page background, canvas container |
| Secondary (30%) | Radix `--color-panel-solid` (light: #f9f9f9) | Cards (LayerTogglePanel, InspectorSidebar, floating mini-toolbar) |
| Accent (10%) | Radix `iris` (accentColor in Theme) | Reserved for: edit mode indicator border, rubber-band selection rect, vertex drag handles (stroke), cut-line preview, active/selected toolbar button (solid variant) |
| Destructive | Radix `red` | Delete action in floating mini-toolbar (future); validation error badges |
| Warning | Radix `amber` | Validation warning badges; "Alterações não salvas" save-status badge |
| Success | Radix `green` (via Badge color="green") | "Salvo ✓" save-status badge |

Accent (iris) reserved for:
1. 2px iris-colored border on canvas container when edit mode is active
2. Rubber-band selection rectangle fill (`rgba(110, 86, 207, 0.1)`) and stroke (`1px iris-9`)
3. Vertex drag handle stroke color (#6E56CF — iris-9 approximate)
4. Cut-line preview dashed stroke (#6E56CF)
5. Active toolbar button state (Radix `variant="solid"` vs `variant="soft"` for inactive)

Konva-specific colors (cannot use CSS variables):
- Capital marker dark ring: `rgba(0, 0, 0, 0.6)` — UNCHANGED from Phase 02
- Capital marker inner fill: condado color from lookup — UNCHANGED from Phase 02
- Capital marker stroke: `#ffffff` strokeWidth=1.5 — UNCHANGED from Phase 02
- Selection outline (InteractionLayer): gold (`#FFD700`) — UNCHANGED from Phase 02
- Validation error badge (canvas overlay): `#e5484d` (Radix red-9 approximate), 8px radius circle at condado centroid
- Validation warning badge (canvas overlay): `#f76b15` (Radix amber-9 approximate), 8px radius circle at condado centroid
- Rubber-band rect: fill `rgba(110, 86, 207, 0.1)`, stroke `rgba(110, 86, 207, 0.7)`, strokeWidth=1, dash=[4,2]
- Vertex drag handle (normal): fill=`#ffffff`, stroke=`#6E56CF`, strokeWidth=1.5, radius=6px
- Vertex drag handle (hover): fill=`#ffffff`, stroke=`#6E56CF`, strokeWidth=2, radius=8px
- Cut-line preview: stroke=`#6E56CF`, strokeWidth=2, dash=[6,3]
- Ghost capital position (drag preview): fill=`rgba(110, 86, 207, 0.5)`, radius=6, no stroke

---

## Component Inventory

New components introduced in Phase 04 (all use Radix Themes primitives):

| Component | File | Type | Radix primitives used |
|-----------|------|------|----------------------|
| EditToolbar | frontend/src/components/canvas/EditToolbar.tsx | Radix Flex + ToggleGroup or Button | Flex, Button (variant="soft"/"solid"), Separator, Tooltip, SegmentedControl |
| SelectionFloatingToolbar | frontend/src/components/canvas/SelectionFloatingToolbar.tsx | Radix Card floating | Card, Flex, Button, Tooltip |
| SettingsPanel | frontend/src/components/canvas/SettingsPanel.tsx | Radix Dialog or Sheet | Dialog or Sheet, RadioGroup, Text, Flex |
| SaveStatusIndicator | frontend/src/components/canvas/SaveStatusIndicator.tsx | Inline badge | Badge (color="green"/"gray"/"amber") |
| ValidationBadge (Konva) | inside canvas layers | Konva Circle overlay | — |
| UndoRedoButtons | inside EditToolbar | Icon buttons | Button + @radix-ui/react-icons (UndoIcon, RedoIcon) |

Modified components from Phase 02 (minimal additions, no regressions):

| Component | Change | Constraint |
|-----------|--------|-----------|
| CanvasViewer.tsx | Add editMode flag, rubber-band Rect, edit mode border | Must not break Phase 02 behavior in read-only mode |
| DecorationsLayer.tsx | Capital markers become draggable in edit mode | listening=false in read-only; listening=true only in edit mode |
| TerritoryLayer.tsx | Add vertex handle overlay in vertex-edit mode | Only renders handles when editMode + vertexEditId set |
| LayerTogglePanel.tsx | No changes | Carry through unchanged |
| InspectorSidebar.tsx | Add rename field after merge/split | New text input only in post-operation state |

---

## Interaction Contract

### Edit Mode Gate (D-09)

- Canvas starts read-only (Phase 02 behavior). All Phase 02 interactions (click-select, pan, zoom, labels, fit-to-view) work identically.
- "Editar" toggle button in EditToolbar switches edit mode on/off.
- **Visual indicator when edit mode ON**: EditToolbar button renders `variant="solid"` (iris-filled) instead of `variant="soft"`. Canvas container gets a `2px solid` iris-accent border (`outline: 2px solid var(--accent-9); outline-offset: -2px`).
- In edit mode, rubber-band multi-select activates, capital markers become draggable, Split/Merge tools appear enabled in toolbar.

### Capital Drag (D-01, EDIT-01)

- Capital marker becomes draggable only in edit mode.
- During drag: ghost marker renders at drag position (`rgba(110, 86, 207, 0.5)` circle, radius=6). Original marker stays visible at original position.
- On mouseup: ghost removed, spinner/loading state on affected territories (opacity=0.6), POST to recalc endpoint, updated polygons animate in (no transition — immediate redraw per Konva batchDraw).
- Undo label: `"Mover capital de {condado.name}"` (Portuguese).

### Border Vertex Drag (D-02, EDIT-02)

- "Editar Vértices" mode enters via keyboard `V` or a button in EditToolbar (only enabled when a condado is selected in edit mode).
- On entering: polygon decimated to ~12 handle points (Douglas-Peucker); handles render as 6px circles (normal state: white fill, iris stroke 1.5px).
- Handle hover: radius expands to 8px, strokeWidth=2.
- On mouseup per handle: immediate canvas redraw; PATCH fires on Esc or "Exit vertex edit" button.
- Esc exits vertex edit mode, persists.
- Undo label: `"Editar vértice — {condado.name}"` (Portuguese).

### Rubber-Band Multi-Select (D-03, EDIT-03)

- In edit mode, click-drag on empty Stage area draws a selection Rect.
- Rect style: iris fill `rgba(110, 86, 207, 0.1)`, iris stroke `rgba(110, 86, 207, 0.7)`, strokeWidth=1, dash=[4,2].
- On mouseup: all condados with centroid inside rect are selected. InteractionLayer shows gold outline on each.
- SelectionFloatingToolbar appears: Radix Card, positioned 8px above bounding box centroid of selection, zIndex=20.
- Floating toolbar buttons: "Fundir" (Merge, Radix Button variant="solid"), future: "Excluir" (Delete, color="red").

### Territory Merge (D-03, EDIT-03)

- User presses "Fundir" in SelectionFloatingToolbar.
- If non-adjacent: Radix Toast appears: `"Territórios não adjacentes. Fundir criará um multipolígono (aviso de validação)."`. Merge proceeds.
- POST to merge endpoint. Merged condado inherits name of highest-area selection member.
- Inspector shows rename field immediately after merge (Radix TextField inline in inspector heading).
- Validation badge appears if merge created non-simple polygon.
- Undo label: `"Fundir {count} territórios"` (Portuguese).

### Territory Split (D-04, EDIT-04)

- "Dividir" tool button in EditToolbar, only enabled when exactly one condado is selected.
- On activation: segmented control appears in toolbar with 3 sub-modes:
  1. "Snap" (default) — two-point click on boundary, cursor=crosshair.
  2. "Polilinha" — click nodes, double-click finish, cursor=crosshair.
  3. "Livre" — click-drag freehand, cursor=pencil (CSS `cursor: url(...) crosshair`).
- Cut-line preview: iris dashed stroke (6px dash, 3px gap), strokeWidth=2, rendered on a dedicated Konva layer above TerritoryLayer.
- If cut line does not enter and exit polygon: Radix Toast error: `"Linha de corte deve cruzar o território em dois pontos."`. Operation cancelled.
- POST to split endpoint. Second polygon inherits base attributes; Inspector shows rename field.
- Undo label: `"Dividir {condado.name}"` (Portuguese).

### Undo/Redo (D-05, EDIT-07, EDIT-08)

- Keyboard: Ctrl+Z (undo), Ctrl+Y (redo). Cmd+Z / Cmd+Y on Mac.
- Toolbar: icon buttons using `@radix-ui/react-icons` — `CounterClockwiseClockIcon` (undo), `ClockwiseIcon` (redo).
- Undo/redo buttons are disabled (Radix `disabled` prop) when history is empty / at head.
- Tooltip on hover shows last action label: `"Desfazer: {label}"` / `"Refazer: {label}"`.
- Compound operations batched via `temporal.pause()` / `temporal.resume()` — registered as one step.

### Validation Badges (D-06)

- After each operation finalizes: re-validate affected territories.
- Error (polygon invalid, capital outside territory, empty polygon): 8px red circle at condado centroid on canvas (`#e5484d`).
- Warning (non-adjacent merge, multipolygon result): 8px amber circle at condado centroid (`#f76b15`).
- InspectorSidebar shows issue detail text when offending condado is selected.
- Export button disabled with Radix Tooltip: `"Corrija os erros de validação primeiro"` when any error-severity issue exists.

### Save-Status Indicator (D-07)

- Position: header right area, inline with project name.
- States (Radix Badge):
  - `color="green"` variant="soft": `"Salvo ✓"` — auto-save or manual save confirmed.
  - `color="gray"` variant="soft": `"Salvando…"` — debounce timer running or PATCH in flight.
  - `color="amber"` variant="soft": `"Alterações não salvas"` — explicit-save mode with pending changes.
- Settings panel (Radix Sheet from nav): radiogroup with 3 options, persisted to localStorage.
- `beforeunload` warning fires when unsaved changes exist in explicit-save mode.

---

## Keyboard Shortcut Map

| Shortcut | Action |
|----------|--------|
| E | Toggle edit mode on/off |
| V | Enter vertex-edit mode (requires: edit mode on + condado selected) |
| Esc | Exit current sub-mode (vertex-edit, split draw, rubber-band); deselect if no sub-mode |
| S | Activate split tool |
| Ctrl+Z / Cmd+Z | Undo |
| Ctrl+Y / Cmd+Y | Redo |
| Ctrl+S / Cmd+S | Manual save (explicit-save mode only; no-op in auto/per-op modes) |
| Ctrl+0 / Cmd+0 | Fit to view (carry from Phase 02) |
| Delete | Delete selected territory (future; toolbar only in this phase) |

---

## Copywriting Contract

Language: Portuguese for all new action labels and undo step labels (aligned with D-05 CONTEXT.md examples and Phase 02 layer labels). Inspector overview mixes Portuguese labels with English hierarchy names (Kingdom/Duchy) — carry Phase 02 pattern unchanged.

| Element | Copy | Source |
|---------|------|--------|
| Edit mode toggle button (off state) | "Editar" | Default (Portuguese, matches Phase 02 vocabulary) |
| Edit mode toggle button (on state) | "Editar" (variant="solid" visually indicates active) | Default |
| Vertex edit button | "Editar Vértices" | Default |
| Merge button | "Fundir" | Default (matches CONTEXT.md D-03 wording) |
| Split tool button | "Dividir" | Default |
| Split sub-mode: snap | "Snap" | Default |
| Split sub-mode: polyline | "Polilinha" | Default |
| Split sub-mode: freehand | "Livre" | Default |
| Undo button tooltip | "Desfazer: {label}" | Default |
| Redo button tooltip | "Refazer: {label}" | Default |
| Settings panel title | "Configurações de Salvamento" | Default |
| Save strategy option: auto | "Automático (1.5s após edição)" | Default |
| Save strategy option: per-op | "Por operação" | Default |
| Save strategy option: explicit | "Manual (Ctrl+S)" | Default |
| Save status: saved | "Salvo ✓" | Default |
| Save status: saving | "Salvando…" | Default |
| Save status: unsaved | "Alterações não salvas" | Default |
| Merge confirmation (non-adjacent toast) | "Territórios não adjacentes. Fundir criará um multipolígono (aviso de validação)." | Default |
| Cut-line invalid toast | "Linha de corte deve cruzar o território em dois pontos." | Default |
| Export blocked tooltip | "Corrija os erros de validação primeiro" | Default (from D-06) |
| Rename placeholder (post-merge/split) | "Nome do território…" | Default |
| Empty state (no condado selected in edit mode) | No empty state — toolbar shows disabled buttons with Tooltip: "Selecione um território para editar" | Default |
| Voronoi recalc loading state | Affected territory polygons rendered at opacity=0.6 (no text overlay) | Default |
| Undo transaction labels (set in code) | "Mover capital de {name}", "Editar vértice — {name}", "Fundir {N} territórios", "Dividir {name}" | CONTEXT.md D-05 |

---

## Layout Regions (Phase 04 additions to Phase 02 layout)

The Phase 02 two-region layout (canvas left / inspector right) is preserved. Phase 04 adds:

```
┌─────────────────────────────────────────────────────┐
│ Header: project name  ·  [Salvo ✓]   [Configurações] │
├──────────────────────────────────┬──────────────────┤
│ EDIT TOOLBAR (canvas top, full   │ INSPECTOR        │
│ width minus inspector)           │ (unchanged from  │
│  [Editar] [Editar Vértices]      │  Phase 02)       │
│  [Fundir] [Dividir: Snap/Poli/   │                  │
│  Livre] [⟲ Undo] [⟳ Redo]       │                  │
├──────────────────────────────────┤                  │
│                                  │                  │
│  CANVAS (Konva Stage)            │                  │
│  + LayerTogglePanel (top-left)   │                  │
│  + FitToViewButton (bottom-right)│                  │
│  + LegendCard (bottom-left)      │                  │
│  + SelectionFloatingToolbar      │                  │
│    (above selection, conditional)│                  │
│  + ValidationBadge dots (condado │                  │
│    centroids, conditional)       │                  │
│                                  │                  │
└──────────────────────────────────┴──────────────────┘
```

EditToolbar: Radix `Flex` with `gap="2"`, horizontal, `align="center"`, full-width, `px="3" py="2"` padding, `borderBottom="1px solid var(--gray-4)"`. Not floating — inline flow above canvas.

SelectionFloatingToolbar: Radix `Card` with `variant="surface"`, `shadow="3"`, positioned 8px above bounding box top midpoint of selection, `zIndex=20`. Contains `Flex gap="2"` with action buttons.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — no shadcn in project |
| @radix-ui/react-icons | icons only (UndoIcon equivalent, ClockwiseIcon equivalent) | not applicable — official Radix package, npm-installed |

No third-party shadcn registries. No vetting gate required.

---

## Pre-Populated From

| Source | Decisions Captured |
|--------|--------------------|
| CONTEXT.md | 9 implementation decisions (D-01 through D-09), undo labels (Portuguese), operation list, keyboard shortcuts partially |
| REQUIREMENTS.md | EDIT-01, EDIT-02, EDIT-03, EDIT-04, EDIT-07, EDIT-08 interaction expectations |
| ROADMAP.md | <500ms recalc constraint, 50-step undo, partialize+diff requirement, merge adjacency rebuild |
| main.tsx (codebase) | Radix Theme config (iris accent, light, radius=medium) — LOCKED |
| DecorationsLayer.tsx | Capital marker exact colors/sizes, label font spec |
| FitToViewButton.tsx | 44px minHeight touch target — carried forward |
| LayerTogglePanel.tsx | Absolute position values (top:12, left:12), gap="2"/"3" pattern |
| InspectorSidebar.tsx | Badge color palette (amber/blue/grass/gray), copy strings |
| uiStore.ts | Layer names, Zustand store shape |
| User input | 0 (all defaults applied; user confirmation items listed below) |

---

## Items Requiring User Confirmation

The following defaults were applied without explicit user input. User can override before this spec is promoted to `approved`:

1. **Portuguese for all new labels** — defaulted to Portuguese matching Phase 02 + D-05 undo examples. Confirm: "Editar", "Editar Vértices", "Fundir", "Dividir", "Salvo ✓", etc.
2. **Icon library: @radix-ui/react-icons** — cheapest Radix Themes peer. Confirm package addition or name an alternative.
3. **Undo/redo as toolbar icon buttons (in addition to Ctrl+Z/Y)** — ROADMAP plan 4.4 says "undo UI controls". Defaulted to icon buttons in EditToolbar. Confirm.
4. **Keyboard shortcut map** — particularly E=edit mode, V=vertex mode, S=split, M=merge. Confirm no conflicts with OS/browser shortcuts in the target environment.
5. **Edit mode visual indicator** — iris border on canvas container + solid button variant. Confirm styling approach is acceptable.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
