---
phase: 07-llm-research-as-opt-in-metadata-layer
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/medieval_forge/services/llm/__init__.py
  - backend/medieval_forge/services/llm/base.py
  - backend/medieval_forge/services/llm/schemas.py
  - backend/medieval_forge/services/llm/parse.py
  - backend/tests/unit/test_llm_schemas.py
  - backend/tests/unit/test_llm_parse.py
autonomous: true
requirements:
  - V3-LLM-OPT-IN
must_haves:
  truths:
    - "services/llm/base.py exists as NEW v3 file defining LLMProvider Protocol + AuthMethod union + HealthStatus model"
    - "services/llm/schemas.py is a LITERAL PORT of commit 87f8aab~1:backend/medieval_forge/services/llm/schemas.py (255 LOC) — byte-identical content (modulo attribution comment + import normalization) per NIT 2"
    - "services/llm/parse.py is a LITERAL PORT of commit 87f8aab~1:backend/medieval_forge/services/llm/parse.py (50 LOC) — byte-identical content per NIT 2"
    - "Import order works: schemas (no deps) -> parse (depends on schemas) -> base (independent)"
    - "test_llm_schemas.py and test_llm_parse.py both pass"
  artifacts:
    - path: "backend/medieval_forge/services/llm/__init__.py"
      provides: "Package init"
      contains: "from .base import"
    - path: "backend/medieval_forge/services/llm/base.py"
      provides: "LLMProvider Protocol + AuthMethod + HealthStatus"
      contains: "class LLMProvider(Protocol):"
    - path: "backend/medieval_forge/services/llm/schemas.py"
      provides: "ResearchResult + MapResearchResult + parse_research_json (LITERAL PORT)"
      contains: "# Literal port from commit 87f8aab~1; see D-02 in 07-CONTEXT.md"
    - path: "backend/medieval_forge/services/llm/parse.py"
      provides: "Markdown fence-stripping wrapper (LITERAL PORT)"
      contains: "# Literal port from commit 87f8aab~1; see D-02 in 07-CONTEXT.md"
  key_links:
    - from: "backend/medieval_forge/services/llm/parse.py"
      to: "backend/medieval_forge/services/llm/schemas.py:parse_research_json"
      via: "from .schemas import parse_research_json"
      pattern: "from \\.schemas import"
    - from: "backend/medieval_forge/services/llm/__init__.py"
      to: ".base / .schemas"
      via: "package-level re-exports"
      pattern: "from \\.base import"
---

<objective>
Land the FIRST half of D-02 literal ports + the NEW v3 `base.py`.

NIT 2 enforcement: literal-port files must be BYTE-IDENTICAL to `87f8aab~1` content (stripping only the prepended attribution header + minimal import-line normalization). Acceptance criterion below uses `diff` to enforce.

Purpose: D-02 + RESEARCH §Pattern 1 + §Anti-Patterns.

Output:
- `services/llm/__init__.py`
- `services/llm/base.py` (NEW)
- `services/llm/schemas.py` (LITERAL PORT)
- `services/llm/parse.py` (LITERAL PORT)
- 2 unit test files
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-CONTEXT.md
@.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Literal-port schemas.py + parse.py from 87f8aab~1 (chore) + package init</name>
  <files>
    backend/medieval_forge/services/llm/__init__.py
    backend/medieval_forge/services/llm/schemas.py
    backend/medieval_forge/services/llm/parse.py
  </files>
  <read_first>
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-CONTEXT.md (D-02)
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md §Anti-Patterns
    - 87f8aab~1 snapshots via `git show`
  </read_first>
  <action>
    1. Fetch literal ports verbatim:
       ```bash
       git show 87f8aab~1:backend/medieval_forge/services/llm/schemas.py > backend/medieval_forge/services/llm/schemas.py
       git show 87f8aab~1:backend/medieval_forge/services/llm/parse.py > backend/medieval_forge/services/llm/parse.py
       ```

    2. PREPEND attribution comment to each file (insert ABOVE the existing top-of-file content):
       ```python
       # Literal port from commit 87f8aab~1; see D-02 in 07-CONTEXT.md.
       # DO NOT MODIFY behaviorally — represents 6+ months of v1 bug-fix iteration.
       # Allowed edits: import-line adjustments only.
       ```

    3. Create `backend/medieval_forge/services/llm/__init__.py`:
       ```python
       """services/llm/ — 2-provider plugin registry (Claude + Ollama)."""
       from .schemas import ResearchResult, MapResearchResult, parse_research_json
       ```

    4. Normalize parse.py import only if needed (relative `from .schemas import`).

    5. Atomic chore commit.
  </action>
  <acceptance_criteria>
    - File `backend/medieval_forge/services/llm/__init__.py` EXISTS
    - File `backend/medieval_forge/services/llm/schemas.py` EXISTS
    - File `backend/medieval_forge/services/llm/parse.py` EXISTS
    - `grep -c "Literal port from commit 87f8aab~1" backend/medieval_forge/services/llm/schemas.py` returns 1
    - `grep -c "Literal port from commit 87f8aab~1" backend/medieval_forge/services/llm/parse.py` returns 1
    - `wc -l backend/medieval_forge/services/llm/schemas.py` reports ≥255 lines
    - `wc -l backend/medieval_forge/services/llm/parse.py` reports ≥50 lines
    - `grep -n "def parse_research_json" backend/medieval_forge/services/llm/schemas.py` returns ≥1 match
    - `grep -n "class ResearchResult" backend/medieval_forge/services/llm/schemas.py` returns ≥1 match
    - `grep -n "class MapResearchResult" backend/medieval_forge/services/llm/schemas.py` returns ≥1 match
    - `grep -n "from .schemas import" backend/medieval_forge/services/llm/parse.py` returns ≥1 match
    - **NIT 2 — byte-identical assertion**: strip prepended attribution header (first ~5 comment lines) and import-normalization edits from the new file, then `diff` against `git show 87f8aab~1:backend/medieval_forge/services/llm/schemas.py` — must show 0 lines of meaningful diff. Procedure:
      ```bash
      # Extract literal-port content sans attribution header
      tail -n +6 backend/medieval_forge/services/llm/schemas.py > /tmp/v3_schemas.py
      git show 87f8aab~1:backend/medieval_forge/services/llm/schemas.py > /tmp/v1_schemas.py
      diff /tmp/v1_schemas.py /tmp/v3_schemas.py | grep -v "^[<>] from \." | wc -l  # → 0
      ```
      Adapt the `tail -n +N` offset to skip exactly the attribution comment lines added in step 2. Document the exact diff command + result in 07-02-SUMMARY.md.
    - Same byte-identical check for parse.py (NIT 2)
    - `cd backend && python -c "from medieval_forge.services.llm.schemas import ResearchResult, MapResearchResult, parse_research_json; from medieval_forge.services.llm.parse import *; print('ok')"` prints `ok`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -c "from medieval_forge.services.llm import ResearchResult, MapResearchResult, parse_research_json"</automated>
  </verify>
  <done>Literal ports landed verbatim; byte-identical to 87f8aab~1 (modulo attribution + import normalization) per NIT 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: services/llm/base.py — NEW LLMProvider Protocol + AuthMethod union + HealthStatus</name>
  <files>
    backend/medieval_forge/services/llm/base.py
    backend/medieval_forge/services/llm/__init__.py
  </files>
  <read_first>
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md §Pattern 1
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-CONTEXT.md (D-01)
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md §Deferred (OAuthAuth deferred to v3.1)
  </read_first>
  <behavior>
    - Test 1: `isinstance(StubProvider(), LLMProvider)` is True via runtime_checkable
    - Test 2: HealthStatus round-trips
    - Test 3: ApiKeyAuth / CliAuth / NoAuth construct
  </behavior>
  <action>
    Create `backend/medieval_forge/services/llm/base.py` from RESEARCH §Pattern 1 (NEW v3 file): HealthStatus, ApiKeyAuth, CliAuth, NoAuth, AuthMethod union (OAuthAuth dropped), `@runtime_checkable class LLMProvider(Protocol)`.

    UPDATE `__init__.py` to expose new types.

    ADD `backend/tests/unit/test_llm_base.py` with 2-3 smoke cases.
  </action>
  <acceptance_criteria>
    - File `backend/medieval_forge/services/llm/base.py` EXISTS (NEW, no attribution comment)
    - `grep -n "@runtime_checkable" backend/medieval_forge/services/llm/base.py` returns 1 match
    - `grep -n "class LLMProvider(Protocol):" backend/medieval_forge/services/llm/base.py` returns 1 match
    - `grep -n "AuthMethod = ApiKeyAuth | CliAuth | NoAuth" backend/medieval_forge/services/llm/base.py` returns 1 match
    - `grep -n "OAuthAuth" backend/medieval_forge/services/llm/base.py` returns 0 matches
    - `grep -n "from .base import LLMProvider" backend/medieval_forge/services/llm/__init__.py` returns 1 match
    - `cd backend && python -c "from medieval_forge.services.llm.base import LLMProvider, HealthStatus, ApiKeyAuth, CliAuth, NoAuth"` exits 0
    - File `backend/tests/unit/test_llm_base.py` EXISTS with ≥2 tests
    - `cd backend && pytest tests/unit/test_llm_base.py -x -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd backend && pytest tests/unit/test_llm_base.py -x -q</automated>
  </verify>
  <done>NEW base.py defines protocol + auth union.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: test_llm_schemas.py + test_llm_parse.py (Wave 0 gates)</name>
  <files>
    backend/tests/unit/test_llm_schemas.py
    backend/tests/unit/test_llm_parse.py
  </files>
  <read_first>
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-VALIDATION.md (Wave 0 gates)
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md §Validation Architecture
    - backend/medieval_forge/services/llm/schemas.py (post-Task 1)
    - backend/medieval_forge/services/llm/parse.py (post-Task 1)
  </read_first>
  <behavior>
    - test_llm_schemas.py: ≥3 cases (extras strip; missing field rejected; explicit MapResearchResult fixture)
    - test_llm_parse.py: ≥2 cases (fences stripped; raw JSON parses)
  </behavior>
  <action>
    Create the two test files with descriptive names + explicit numeric/dict fixtures per project memory feedback-tests-descriptive.
  </action>
  <acceptance_criteria>
    - File `backend/tests/unit/test_llm_schemas.py` EXISTS
    - File `backend/tests/unit/test_llm_parse.py` EXISTS
    - `grep -c "^def test_" backend/tests/unit/test_llm_schemas.py` returns ≥3
    - `grep -c "^def test_" backend/tests/unit/test_llm_parse.py` returns ≥2
    - `cd backend && pytest tests/unit/test_llm_schemas.py tests/unit/test_llm_parse.py -x -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd backend && pytest tests/unit/test_llm_schemas.py tests/unit/test_llm_parse.py -x -q</automated>
  </verify>
  <done>Wave 0 test files pass.</done>
</task>

</tasks>

<context_anchors>
- **D-01** (sidecar-first — base.py is NEW)
- **D-02** (2 of 4 literal ports)
- **D-05** (Claude + Ollama only)
- **Discretion #7** (literal-port shape)
- **RESEARCH §Pattern 1** (base.py)
- **RESEARCH §Anti-Patterns** (schemas BEFORE parse)
- **VALIDATION.md Wave 0**
- **Checker NIT 2** (byte-identical diff assertion)
</context_anchors>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted LLM output → parser | LLM-returned text crosses into `parse_research_json` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | parse_research_json — output shape bypass | mitigate | Pydantic rejects out-of-shape; retry loop appends error. |
| T-07-02-02 | DoS | parse_research_json — payload size | accept | Caller-side cap; small-model output rarely >50KB. |
| T-07-02-03 | Tampering | json.loads injection | accept | Stdlib safe. |

</threat_model>

<verification>
- `cd backend && python -c "from medieval_forge.services.llm import LLMProvider, ResearchResult, MapResearchResult, parse_research_json, HealthStatus, ApiKeyAuth, CliAuth, NoAuth"` exits 0
- `cd backend && pytest tests/unit/test_llm_schemas.py tests/unit/test_llm_parse.py tests/unit/test_llm_base.py -x -q` exits 0
- `grep -c "Literal port from commit 87f8aab~1" backend/medieval_forge/services/llm/*.py` returns 2
- NIT 2: byte-identical diff vs 87f8aab~1 documented in SUMMARY.md
</verification>

<success_criteria>
- 2 of 4 D-02 ports landed
- NEW base.py defines protocol
- Wave 0 tests passing
- NIT 2 byte-identical diff = 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-02-SUMMARY.md` including the NIT 2 byte-identical diff output.
</output>
