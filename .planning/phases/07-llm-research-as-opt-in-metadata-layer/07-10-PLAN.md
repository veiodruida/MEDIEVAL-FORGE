---
phase: 07-llm-research-as-opt-in-metadata-layer
plan: 10
type: execute
wave: 4
depends_on: [08]
files_modified:
  - frontend/src/i18n/exportErrors.ts
  - frontend/src/api/useExportV3.ts
  - frontend/src/api/client.ts
  - frontend/src/components/export/ExportErrorDialog.tsx
  - frontend/src/components/workspace/WorkspaceToolbar.tsx
  - frontend/src/pages/ProjectDetail.tsx
  - frontend/src/i18n/__tests__/exportErrors.test.ts
  - frontend/src/api/__tests__/useExportV3.test.ts
  - frontend/src/components/export/__tests__/ExportErrorDialog.test.tsx
autonomous: true
requirements:
  - V3-LLM-OPT-IN
must_haves:
  truths:
    - "frontend/src/i18n/exportErrors.ts defines EXPORT_ERROR_PT_BR with EXACTLY 6 keys (SCHEMA_INVALID, COLOR_COLLISION, OCEAN_LEAK, MISSING_ORIGINAL_IDX, TERRITORY_TOO_SMALL, PIXEL_CENTER_OUT_OF_RANGE) verbatim from UI-SPEC §Copywriting §Surface 3"
    - "useExportV3 hook POSTs to /api/v3/projects/{id}/export (NOT v1 /api/projects/{id}/export); on 422 throws typed ExportValidationError"
    - "ExportErrorDialog renders the 422 envelope per UI-SPEC §Surface 3 with `Falha ao exportar` title + [badge code · file · PT-BR message] per row"
    - "Dry-run preview link `Validar antes de exportar` POSTs /export?dry_run=true and renders inline"
    - "ProjectDetail.tsx uses useExportV3 (NOT v1 client.ts:useExport — VERIFIED SWAP + LEGACY DELETED per WARNING 4)"
    - "Unknown error code falls back to server-provided e.message (forward-compat)"
    - "Network/5xx fallback uses Toast.Provider with copy `Erro ao comunicar com o servidor de exportação: ...`"
    - "Legacy `useExport` hook in client.ts is DELETED (WARNING 4 — D-V3-04 no transitional shims)"
  artifacts:
    - path: "frontend/src/i18n/exportErrors.ts"
      provides: "Single named export EXPORT_ERROR_PT_BR with 6 keys"
      contains: "EXPORT_ERROR_PT_BR"
    - path: "frontend/src/api/useExportV3.ts"
      provides: "TanStack mutation wrapping POST /api/v3/projects/{id}/export with 422 typed-error branch"
      contains: "ExportValidationError"
    - path: "frontend/src/components/export/ExportErrorDialog.tsx"
      provides: "Radix Dialog renderer for 422 envelope per UI-SPEC §Surface 3"
      contains: "Falha ao exportar"
  key_links:
    - from: "frontend/src/pages/ProjectDetail.tsx"
      to: "frontend/src/api/useExportV3.ts"
      via: "exportMut = useExportV3(projectId); handleExport calls mutateAsync"
      pattern: "useExportV3"
    - from: "frontend/src/components/export/ExportErrorDialog.tsx"
      to: "frontend/src/i18n/exportErrors.ts"
      via: "EXPORT_ERROR_PT_BR[e.code] ?? e.message"
      pattern: "EXPORT_ERROR_PT_BR"
---

<objective>
Land the deferred Phase 06 frontend work: (1) Export button swaps from v1 to `/api/v3/projects/{id}/export`; (2) structured 422 envelope renders in a Radix Dialog with PT-BR strings keyed by 6 stable error codes; (3) dry-run preview link works inline; (4) **WARNING 4 fix: delete the dead `useExport` hook in client.ts** (D-V3-04 no transitional shims).

Purpose: D-10 (absorption); D-V3-04 (no shims); UI-SPEC §Surface 3 lock; RESEARCH §Phase 06 Absorption §Required swap; RESEARCH §Example 4. UI-SPEC §Copywriting §EXPORT_ERROR_PT_BR is the authoritative 6-key map.

Output:
- 4 new frontend files + 3 modified (WorkspaceToolbar / ProjectDetail / client.ts cleanup)
- 3 vitest test files covering ≥10 cases
- Phase 07 SC #1 unblocked
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-CONTEXT.md
@.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md
@.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-UI-SPEC.md
@.planning/phases/06-export-contract-validation-gate/06-CONTEXT.md
@frontend/src/api/client.ts
@frontend/src/components/workspace/WorkspaceToolbar.tsx
@frontend/src/pages/ProjectDetail.tsx
@frontend/src/main.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: i18n/exportErrors.ts + useExportV3 hook + 422 ExportValidationError typing</name>
  <files>
    frontend/src/i18n/exportErrors.ts
    frontend/src/api/useExportV3.ts
    frontend/src/i18n/__tests__/exportErrors.test.ts
    frontend/src/api/__tests__/useExportV3.test.ts
  </files>
  <read_first>
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-UI-SPEC.md §Copywriting §EXPORT_ERROR_PT_BR
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md §Example 4 + §Example 5
    - frontend/src/api/client.ts (current Export button — to be swapped/removed)
    - .planning/phases/06-export-contract-validation-gate/06-CONTEXT.md §D-08 (envelope shape)
    - backend/medieval_forge/api/v3/export.py (Phase 06 — endpoint + dry_run=true)
  </read_first>
  <behavior>
    - exportErrors test: all 6 keys present; each maps to non-empty PT-BR; no English fallback
    - useExportV3 test: 200 returns ExportResponse; 422 throws ExportValidationError; network throws generic Error; dryRun=true posts ?dry_run=true
  </behavior>
  <action>
    Same as former Plan 10 Task 1 — create `frontend/src/i18n/exportErrors.ts` with the 6 PT-BR strings (SCHEMA_INVALID, COLOR_COLLISION, OCEAN_LEAK, MISSING_ORIGINAL_IDX, TERRITORY_TOO_SMALL, PIXEL_CENTER_OUT_OF_RANGE) verbatim from UI-SPEC; create `useExportV3.ts` with ExportValidationError class + mutation against /api/v3/projects/{id}/export with 422 branch and dry_run=true support. Create the 2 test files (≥3 + ≥4 cases).
  </action>
  <acceptance_criteria>
    - File `frontend/src/i18n/exportErrors.ts` EXISTS
    - All 6 keys present: `grep -n "SCHEMA_INVALID:\|COLOR_COLLISION:\|OCEAN_LEAK:\|MISSING_ORIGINAL_IDX:\|TERRITORY_TOO_SMALL:\|PIXEL_CENTER_OUT_OF_RANGE:" frontend/src/i18n/exportErrors.ts` returns ≥6 matches
    - File `frontend/src/api/useExportV3.ts` EXISTS
    - `grep -n "class ExportValidationError" frontend/src/api/useExportV3.ts` returns 1 match
    - `grep -n "/api/v3/projects" frontend/src/api/useExportV3.ts` returns ≥1 match
    - `grep -n "/api/projects/\${projectId}/export" frontend/src/api/useExportV3.ts` returns 0 matches
    - `grep -n "if (res.status === 422)" frontend/src/api/useExportV3.ts` returns 1 match
    - `grep -n "dry_run=true" frontend/src/api/useExportV3.ts` returns 1 match
    - `cd frontend && npm test -- --run src/i18n src/api/__tests__/useExportV3.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- --run src/i18n/__tests__/exportErrors.test.ts src/api/__tests__/useExportV3.test.ts</automated>
  </verify>
  <done>i18n + mutation hook land; 6 codes verified; v1 endpoint absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ExportErrorDialog component + dry-run inline + Toast network-fallback</name>
  <files>
    frontend/src/components/export/ExportErrorDialog.tsx
    frontend/src/components/export/__tests__/ExportErrorDialog.test.tsx
  </files>
  <read_first>
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-UI-SPEC.md §Surface 3
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md §Example 4
    - frontend/src/i18n/exportErrors.ts (Task 1)
    - frontend/src/api/useExportV3.ts (Task 1)
    - frontend/src/main.tsx (Toast.Provider)
    - frontend/src/components/projects/NewProjectModal.tsx (Dialog template)
  </read_first>
  <behavior>
    - 6 codes × 1 test each: PT-BR string from EXPORT_ERROR_PT_BR renders
    - Dry-run pass: error list replaced with passed-state heading + Exportar agora button
    - Dry-run fail: new envelope renders
    - Unknown code: falls back to server e.message
    - Close + open + z-order per UI-SPEC §Cross-surface
  </behavior>
  <action>
    Same as former Plan 10 Task 2 — full ExportErrorDialog per RESEARCH §Example 4. PT-BR strings: `Falha ao exportar`, `Validar antes de exportar`, `Validação OK — pronto para exportar`, `Exportar agora`, `Fechar`. Unknown-code fallback: `EXPORT_ERROR_PT_BR[e.code] ?? e.message`. ≥8 vitest cases.
  </action>
  <acceptance_criteria>
    - File `frontend/src/components/export/ExportErrorDialog.tsx` EXISTS
    - `grep -n "Falha ao exportar" frontend/src/components/export/ExportErrorDialog.tsx` returns ≥1 match
    - `grep -n "Validar antes de exportar" frontend/src/components/export/ExportErrorDialog.tsx` returns ≥1 match
    - `grep -n "Validação OK — pronto para exportar" frontend/src/components/export/ExportErrorDialog.tsx` returns ≥1 match
    - `grep -n "Exportar agora" frontend/src/components/export/ExportErrorDialog.tsx` returns ≥1 match
    - `grep -n "Fechar" frontend/src/components/export/ExportErrorDialog.tsx` returns ≥1 match
    - `grep -n "EXPORT_ERROR_PT_BR\[e.code\] ?? e.message" frontend/src/components/export/ExportErrorDialog.tsx` returns 1 match
    - `grep -n "style={{ maxWidth: 560 }}" frontend/src/components/export/ExportErrorDialog.tsx` returns 1 match
    - `grep -n "color=\"red\" variant=\"soft\"" frontend/src/components/export/ExportErrorDialog.tsx` returns ≥1 match
    - `grep -n "dangerouslySetInnerHTML" frontend/src/components/export/ExportErrorDialog.tsx` returns 0 matches
    - `grep -c "^(it|test)\(" frontend/src/components/export/__tests__/ExportErrorDialog.test.tsx` returns ≥8
    - `cd frontend && npm test -- --run src/components/export/__tests__/ExportErrorDialog.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/export/__tests__/ExportErrorDialog.test.tsx</automated>
  </verify>
  <done>ExportErrorDialog renders 6 codes in PT-BR; dry-run inline; unknown-code fallback; ≥8 vitest cases.</done>
</task>

<task type="auto">
  <name>Task 3: WorkspaceToolbar + ProjectDetail wire-up — swap Export button to useExportV3 + mount ExportErrorDialog + DELETE LEGACY useExport (WARNING 4)</name>
  <files>
    frontend/src/components/workspace/WorkspaceToolbar.tsx
    frontend/src/pages/ProjectDetail.tsx
    frontend/src/api/client.ts
  </files>
  <read_first>
    - frontend/src/components/workspace/WorkspaceToolbar.tsx
    - frontend/src/pages/ProjectDetail.tsx
    - frontend/src/api/client.ts (current useExport at ~lines 172-193 per RESEARCH §Example 5)
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-UI-SPEC.md §Interaction Contract `ExportErrorDialog + Export-button swap`
    - frontend/src/api/useExportV3.ts (Task 1)
    - frontend/src/components/export/ExportErrorDialog.tsx (Task 2)
    - frontend/src/main.tsx (Toast.Provider)
  </read_first>
  <action>
    1. Preserve WorkspaceToolbar `onExport: () => void` prop signature; no internal changes if signature preserved.

    2. Modify ProjectDetail.tsx:
       - Import useExportV3 + ExportErrorDialog + ExportValidationError
       - Add local state `exportEnvelope`
       - Instantiate `exportMut = useExportV3(projectId)`
       - Define `handleExport` per RESEARCH §Example 5: mutateAsync({}) → blob → URL.createObjectURL → a.download → revokeObjectURL; catch ExportValidationError → setExportEnvelope; catch generic → showToast(`Erro ao comunicar com o servidor de exportação: ...`)
       - Pass `onExport={handleExport}` to WorkspaceToolbar
       - Mount `<ExportErrorDialog />` with envelope, onDryRun → exportMut.mutateAsync({ dryRun: true }), onRetryExport → handleExport

    3. **WARNING 4 — DELETE legacy `useExport` from client.ts**:
       - Read `frontend/src/api/client.ts` carefully; locate `export function useExport` (~lines 172-193 per RESEARCH §Example 5)
       - DELETE the entire `useExport` function definition (do NOT leave it as dead code per D-V3-04 no transitional shims)
       - Also delete any related v1 export helpers if they exist (e.g., `exportProject`, `downloadExport` if they only feed `useExport`)
       - Verify no other consumer imports `useExport` — `grep -rn "useExport\b" frontend/src/` BEFORE deletion to enumerate callers. If callers exist outside ProjectDetail.tsx (and they were not migrated), surface as a blocker; otherwise delete the helper safely.

    4. Verify wiring end-to-end via vitest + tsc (manual UAT in Plan 11).
  </action>
  <acceptance_criteria>
    - `grep -n "useExportV3" frontend/src/pages/ProjectDetail.tsx` returns ≥1 match
    - `grep -n "ExportErrorDialog" frontend/src/pages/ProjectDetail.tsx` returns ≥1 match
    - `grep -n "ExportValidationError" frontend/src/pages/ProjectDetail.tsx` returns ≥1 match
    - `grep -n "/api/projects/\${projectId}/export" frontend/src/pages/ProjectDetail.tsx` returns 0 matches
    - `grep -n "Erro ao comunicar com o servidor de exportação" frontend/src/pages/ProjectDetail.tsx` returns ≥1 match
    - `grep -n "URL.createObjectURL" frontend/src/pages/ProjectDetail.tsx` returns ≥1 match
    - `grep -n "URL.revokeObjectURL" frontend/src/pages/ProjectDetail.tsx` returns ≥1 match
    - **WARNING 4 deletion verified**: `grep -rn "/api/projects/\${projectId}/export" frontend/src/` returns 0 matches
    - **WARNING 4 deletion verified**: `grep -nE "export function useExport\b" frontend/src/api/client.ts` returns 0 matches (legacy hook gone)
    - `cd frontend && npm run build` exits 0 (tsc clean — confirms no stale callers)
    - `cd frontend && npm test -- --run src/i18n src/api src/components/export src/components/research src/components/canvas/__tests__/InspectorSidebar.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build && npm test -- --run src/i18n src/api src/components/export</automated>
  </verify>
  <done>Export button calls v3 endpoint; 422 renders in ExportErrorDialog; network failure toasts; legacy useExport DELETED (WARNING 4 — D-V3-04); Phase 07 SC #1 unblocked.</done>
</task>

</tasks>

<context_anchors>
- **D-10** (Phase 07 absorbs Phase 06 frontend swap)
- **D-V3-04** (no namespace shims — direct v3 swap; legacy DELETED per WARNING 4)
- **Discretion #8** (i18n keys in `frontend/src/i18n/exportErrors.ts`)
- **UI-SPEC §Surface 3** (full ExportErrorDialog contract)
- **UI-SPEC §Copywriting §EXPORT_ERROR_PT_BR** (6-key map)
- **RESEARCH §Example 4 + §Example 5**
- **RESEARCH §Phase 06 Absorption §Validator emits 6 codes**
- **Checker WARNING 4** (delete dead useExport in client.ts)
- **CLAUDE.md** (no emojis; PT-BR UI; atomic commits)
</context_anchors>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/v3/projects/{id}/export response → ExportErrorDialog | Server-provided envelope; PT-BR map keyed by code |
| Blob download | Browser file download via URL.createObjectURL |
| Toast.Provider → user | Network-error messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10-01 | XSS | ExportErrorDialog message render | mitigate | React text children only; no dangerouslySetInnerHTML. |
| T-07-10-02 | Information Disclosure | Server message reveals stack trace | accept | Backend `e.message` is validator-generated (Phase 06 controlled). |
| T-07-10-03 | Tampering | XSRF on /export | accept | Single-user local; same-origin. |
| T-07-10-04 | Memory leak | Object URL not revoked | mitigate | `URL.revokeObjectURL(url)`. Acceptance: grep ≥1 match. |
| T-07-10-05 | UX confusion | Unknown code crashes UI | mitigate | `EXPORT_ERROR_PT_BR[e.code] ?? e.message`. |
| T-07-10-06 (WARNING 4) | Stale code path | Legacy useExport remains callable | mitigate | Hook DELETED in Task 3; grep verification proves removal. D-V3-04 compliance. |

</threat_model>

<verification>
- `cd frontend && npm test -- --run src/i18n src/api/__tests__/useExportV3.test.ts src/components/export` exits 0
- `cd frontend && npm run build` exits 0
- `grep -c "^\s*[A-Z_]*:" frontend/src/i18n/exportErrors.ts` returns ≥6
- `grep -rn "/api/projects/\${projectId}/export" frontend/src/` returns 0 matches (WARNING 4)
- `grep -nE "export function useExport\b" frontend/src/api/client.ts` returns 0 matches (WARNING 4)
- `grep -n "/api/v3/projects" frontend/src/api/useExportV3.ts` returns ≥1
</verification>

<success_criteria>
- Export button calls /api/v3/projects/{id}/export
- 422 envelope renders all 6 codes in PT-BR
- Dry-run preview inline
- Network/5xx fallback Toast
- Legacy `useExport` DELETED from client.ts (WARNING 4)
- Phase 07 SC #1 unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-10-SUMMARY.md` per the standard template.
</output>
