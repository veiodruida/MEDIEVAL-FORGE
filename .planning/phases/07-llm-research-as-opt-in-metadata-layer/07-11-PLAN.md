---
phase: 07-llm-research-as-opt-in-metadata-layer
plan: 11
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06, 07a, 07b, 08, 09a, 09b, 10]
files_modified:
  - backend/tests/parity/test_iberia_868_yaml.py
  - frontend/tests/uat/playwright/research_dialog.spec.ts
  - frontend/tests/uat/playwright/research_badge.spec.ts
  - frontend/tests/uat/playwright/export_v3_error_envelope.spec.ts
  - frontend/tests/uat/playwright/fixtures/research_overlay.json
autonomous: false
requirements:
  - V3-LLM-OPT-IN
must_haves:
  truths:
    - "tests/parity/test_iberia_868_yaml.py gains a NON-SKIPPABLE assertion `test_zero_llm_byte_identical` (D-12 — SC #1 root gate)"
    - "tests/parity/test_iberia_868_yaml.py gains `test_iberia_868_raw_metadata_uses_pipeline_slugs` asserting raw output retains pipeline-emitted slugs"
    - "Playwright research_dialog.spec.ts covers SC #2 from UI side"
    - "Playwright research_badge.spec.ts covers 'Pesquisa aplicada' badge visibility"
    - "Playwright export_v3_error_envelope.spec.ts covers all 6 error codes + dry-run pass + network-fallback Toast"
    - "fixtures/research_overlay.json — canned 3-condado overlay"
    - "Any `window.__forge*` write used by Playwright specs is wrapped `if (import.meta.env.DEV) { ... }` (NIT 4 — DEV-gated escape hatch)"
    - "Manual UAT checkpoint approved"
  artifacts:
    - path: "backend/tests/parity/test_iberia_868_yaml.py"
      provides: "Extended Iberia parity — adds D-12 zero-LLM assertion"
      contains: "test_zero_llm_byte_identical"
    - path: "frontend/tests/uat/playwright/research_dialog.spec.ts"
      provides: "Playwright UAT for ResearchDialog SSE flow"
      contains: "open dialog"
    - path: "frontend/tests/uat/playwright/research_badge.spec.ts"
      provides: "Playwright UAT for InspectorSidebar 'Pesquisa aplicada' badge"
      contains: "Pesquisa aplicada"
    - path: "frontend/tests/uat/playwright/export_v3_error_envelope.spec.ts"
      provides: "Playwright UAT for 6-code 422 envelope rendering"
      contains: "COLOR_COLLISION"
    - path: "frontend/tests/uat/playwright/fixtures/research_overlay.json"
      provides: "Canned 3-condado overlay fixture"
      contains: "oviedo"
  key_links:
    - from: "backend/tests/parity/test_iberia_868_yaml.py"
      to: "backend/medieval_forge/services/export/zip.py + Phase 06 baseline outputs"
      via: "byte-equal comparison without overlay file present"
      pattern: "test_zero_llm_byte_identical"
---

<objective>
Close the phase with all 3 layers of the test pyramid + checkpoint:human-verify for end-to-end UAT. Depends on the FULL split-and-revised plan set (07a/07b/09a/09b).

Per NIT 4: any window-escape-hatch writes (e.g., `window.__forgeSelectCondado`) added for Playwright test setup MUST be DEV-gated via `import.meta.env.DEV`.

Purpose: Phase 07 SC #1 (zero-LLM works) + SC #2 (overlay names show) + SC #3 (v3 namespace). Three-layer pyramid per CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-CONTEXT.md
@.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md
@.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-UI-SPEC.md
@.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-VALIDATION.md
@backend/tests/parity/test_iberia_868_yaml.py
@frontend/tests/uat/playwright/
@frontend/playwright.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend tests/parity/test_iberia_868_yaml.py — D-12 zero-LLM non-skippable + raw-slug + manifest assertions</name>
  <files>
    backend/tests/parity/test_iberia_868_yaml.py
  </files>
  <read_first>
    - backend/tests/parity/test_iberia_868_yaml.py
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-CONTEXT.md (D-12)
    - backend/medieval_forge/services/export/zip.py (post-Plan 08)
  </read_first>
  <behavior>
    - Test 1: `test_zero_llm_byte_identical` — Iberia without overlay → byte-identical to Phase 06 baseline. NON-skippable.
    - Test 2: `test_iberia_868_raw_metadata_uses_pipeline_slugs` — raw output has curated slugs.
    - Test 3: `test_manifest_research_overlay_applied_false_for_iberia_baseline` — no overlay → manifest.research_overlay_applied is false.
  </behavior>
  <action>
    Append 3 new test functions per former Plan 11 Task 1. Use `iberia_project_run_fixture` + `phase_06_baseline_zip` conftest fixtures.
  </action>
  <acceptance_criteria>
    - `grep -n "def test_zero_llm_byte_identical" backend/tests/parity/test_iberia_868_yaml.py` returns 1 match
    - `grep -n "def test_iberia_868_raw_metadata_uses_pipeline_slugs" backend/tests/parity/test_iberia_868_yaml.py` returns 1 match
    - `grep -n "def test_manifest_research_overlay_applied_false_for_iberia_baseline" backend/tests/parity/test_iberia_868_yaml.py` returns 1 match
    - `cd backend && pytest tests/parity/test_iberia_868_yaml.py -x -q` exits 0
    - `cd backend && pytest tests/parity/test_iberia_868_yaml.py::test_zero_llm_byte_identical -v` reports PASSED (not SKIPPED/XFAILED)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && pytest tests/parity/test_iberia_868_yaml.py -x -q</automated>
  </verify>
  <done>D-12 non-skippable assertion lands; Phase 07 root gate enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Playwright UAT specs — research dialog + badge + export 422 envelope (with DEV-gated escape hatches per NIT 4)</name>
  <files>
    frontend/tests/uat/playwright/research_dialog.spec.ts
    frontend/tests/uat/playwright/research_badge.spec.ts
    frontend/tests/uat/playwright/export_v3_error_envelope.spec.ts
    frontend/tests/uat/playwright/fixtures/research_overlay.json
  </files>
  <read_first>
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-VALIDATION.md
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-UI-SPEC.md §Surface 1 + §Surface 2 + §Surface 3
    - frontend/tests/uat/playwright/ (existing pattern)
    - frontend/playwright.config.ts
    - .planning/phases/05-region-generalization/05-12-PLAN.md (Plan 05 Playwright wrappers)
  </read_first>
  <behavior>
    - research_dialog.spec.ts ≥3 scenarios (happy / cancel / force-refresh)
    - research_badge.spec.ts ≥1 scenario
    - export_v3_error_envelope.spec.ts ≥9 scenarios (6 codes + dry-run + dry-run-fail + network-toast)
  </behavior>
  <action>
    1. Create `fixtures/research_overlay.json` with 3 condados.

    2. Create the 3 spec files per former Plan 11 Task 2.

    3. **NIT 4 — DEV-gated escape hatches**: any window-escape-hatch added for test setup (e.g., `window.__forgeSelectCondado`) MUST be wrapped:

       ```typescript
       if (import.meta.env.DEV) {
         (window as any).__forgeSelectCondado = (id: string) => { /* ... */ }
       }
       ```

       Apply when adding hatches to InspectorSidebar/Canvas. Acceptance grep enforces guard.
  </action>
  <acceptance_criteria>
    - All 4 files EXIST
    - `grep -c "^test\(" frontend/tests/uat/playwright/research_dialog.spec.ts` returns ≥3
    - `grep -c "^test\(" frontend/tests/uat/playwright/export_v3_error_envelope.spec.ts` returns ≥9
    - For each of 6 codes, PT-BR substring appears in the export spec (≥6 matches across the 6 patterns)
    - **NIT 4 verification**: `grep -nE "window\.__forge" frontend/src/` — every match must be inside an `if (import.meta.env.DEV)` block. Specifically: `grep -B1 -E "window\.__forge.*=" frontend/src/ -r | grep -c "import.meta.env.DEV"` returns ≥ (count of window.__forge assignments)
    - `cd frontend && npx playwright test research_dialog.spec.ts research_badge.spec.ts export_v3_error_envelope.spec.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx playwright test research_dialog.spec.ts research_badge.spec.ts export_v3_error_envelope.spec.ts</automated>
  </verify>
  <done>3 spec files land; ≥13 scenarios green; DEV-gated escape hatches verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT against running server — research flow + badge + Export</name>
  <files>
    .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-UAT.md
  </files>
  <read_first>
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-VALIDATION.md §Manual-Only Verifications
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-UI-SPEC.md §Copywriting
    - Project memory `feedback-server-restart-before-test`
  </read_first>
  <action>
    Executor MUST perform server-restart sequence BEFORE asking user to UAT. Sequence: kill backend + frontend → pip install / npm install if needed → npm run build → start backend + frontend → wait for both responsive → present checklist.
  </action>
  <what-built>
    Full Phase 07 surface — research dialog + InspectorSidebar badges + microcopy + Export button swap + 422 envelope renderer + dry-run preview.
  </what-built>
  <how-to-verify>
    Open running app. Pick generated Iberia project. Exercise 6 scenarios:

    1. **Zero-LLM Export (SC #1)**: click Exportar ZIP without credentials → ZIP downloads with raw slugs/Condado_NNN, never LLM names.
    2. **Research dialog open**: clear selection → click `Pesquisar metadados históricos` → Radix Dialog opens with 4 fields.
    3. **Submit + SSE**: pick Ollama + model → Iniciar pesquisa → per-stage progress → 1.2s auto-close → success Toast.
    4. **Pesquisa aplicada badge**: click covered condado → InspectorSidebar shows historical name + green badge + `Atualizar pesquisa` link.
    5. **Microcopy (BLOCKER 2 verification)**: clear selection → placeholder shows `Última pesquisa: {provider} · {model} · {YYYY-MM-DD HH:mm}` below the trigger button.
    6. **Export with overlay (SC #2)**: Exportar ZIP → unzip → condados[*].name = historical; manifest.research_overlay_applied = true.
    7. **Export 422 envelope (D-10)**: trigger broken project → Falha ao exportar dialog with PT-BR rows + Validar antes de exportar inline.

    Subjective: PT-BR copy quality + visual fidelity vs ParameterSidebar/NewProjectModal.

    Type `approved` when all scenarios pass.
  </how-to-verify>
  <acceptance_criteria>
    - File `.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-UAT.md` EXISTS documenting scenarios + outcomes
    - User typed `approved`
    - All scenarios (including BLOCKER 2 microcopy at #5) pass at least once
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-UAT.md</automated>
  </verify>
  <done>User-approved UAT; phase ready for /gsd-verify-work.</done>
  <resume-signal>Type "approved" or describe the issue.</resume-signal>
</task>

</tasks>

<context_anchors>
- **D-12** (zero-LLM parity non-skippable)
- **SC #1 + #2 + #3**
- **UI-SPEC §all 3 surfaces**
- **BLOCKER 2** (microcopy verified at UAT scenario #5)
- **NIT 4** (DEV-gated window escape hatches)
- **VALIDATION.md §Manual-Only Verifications**
- **Project memory feedback-server-restart-before-test**
- **Three-layer test pyramid** (CLAUDE.md)
</context_anchors>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test infrastructure → CI | Non-skippable tests are the contract |
| Playwright mocks → real backend behavior | Mocks must mirror real envelope shape |
| Window escape hatch (NIT 4) | DEV-only; production builds strip it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11-01 | Tampering | Skipping D-12 zero-LLM test | mitigate | Non-skippable; grep for skip-markers returns 0. |
| T-07-11-02 | Tampering | Playwright mock drift | mitigate | Mocks use envelope shapes from RESEARCH §Example 1 + Phase 06 D-08; manual UAT exercises real backend. |
| T-07-11-03 | Information Disclosure | Fixtures contain secrets | mitigate | research_overlay.json has only historical names. |
| T-07-11-04 | Tampering | Test isolation | mitigate | `page.route` scoped per test. |
| T-07-11-05 (NIT 4) | Information Disclosure | window.__forge* leaks in prod build | mitigate | All assignments wrapped `if (import.meta.env.DEV)`. Acceptance grep enforces. |

</threat_model>

<verification>
- `cd backend && pytest tests/parity/ -x -q` exits 0
- `cd backend && pytest tests/parity/test_iberia_868_yaml.py::test_zero_llm_byte_identical -v` reports PASSED
- `cd frontend && npx playwright test research_dialog.spec.ts research_badge.spec.ts export_v3_error_envelope.spec.ts` exits 0
- `cd backend && pytest tests/ -x` exits 0
- `cd frontend && npm test -- --run && npm run build` exit 0
- `grep -B1 -E "window\.__forge.*=" frontend/src/ -r | grep "import.meta.env.DEV"` count matches (NIT 4)
- User-approved 07-UAT.md exists
</verification>

<success_criteria>
- SC #1 + SC #2 + SC #3 all verified
- Three-layer test pyramid satisfied
- DEV-gated escape hatches (NIT 4)
- Phase ready for /gsd-verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-11-SUMMARY.md` and `07-UAT.md` per the standard templates.
</output>
