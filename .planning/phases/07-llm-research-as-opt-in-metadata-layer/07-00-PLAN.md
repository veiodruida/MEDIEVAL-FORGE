---
phase: 07-llm-research-as-opt-in-metadata-layer
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md
autonomous: false
requirements:
  - V3-LLM-OPT-IN
must_haves:
  truths:
    - "Reconquista Unity C# loader strictness verdict (Tolerant vs Strict) is captured in 07-Q2-UNITY-LOADER-VERDICT.md before any Plan 05 / Plan 08 task runs"
    - "Verdict drives the value of `_ZIP_BOUND_FIELDS` constant in services/research/overlay.py"
    - "Verdict file cites the exact Unity loader source file path + line number"
  artifacts:
    - path: ".planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md"
      provides: "Wave 0 Q2 gate output — Unity loader strictness verdict"
      contains: "VERDICT:"
  key_links:
    - from: ".planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md"
      to: "backend/medieval_forge/services/research/overlay.py (Plan 05)"
      via: "_ZIP_BOUND_FIELDS constant value flips based on verdict"
      pattern: "_ZIP_BOUND_FIELDS"
---

<objective>
Resolve Open Question Q2 from 07-RESEARCH.md: does the Reconquista Unity C# loader tolerate unknown JSON keys on condado entries?

Purpose: RESEARCH §Pattern 5 "Q2 verification dependency" makes this a Wave 0 gate. The verdict controls `_ZIP_BOUND_FIELDS` in Plan 05.

Output: `07-Q2-UNITY-LOADER-VERDICT.md` with the verdict + source-file evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-CONTEXT.md
@.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read Reconquista Unity loader source and produce Q2 verdict</name>
  <files>.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md</files>
  <read_first>
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md (§Pattern 5 + §Open Questions §2 + §Pitfall 8)
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-CONTEXT.md (D-03, D-04, canonical_refs §Reconquista contract)
    - CLAUDE.md
    - The Reconquista Unity loader source files under D:\Projetos_Jogo\Reconquista\Assets\Scripts\
  </read_first>
  <action>
    Read the Reconquista Unity C# loader code. Locate the deserialization call (Newtonsoft `JsonConvert.DeserializeObject<T>` or Unity's `JsonUtility.FromJson<T>`). Determine:

    A. **Tolerant** — Loader uses Newtonsoft default (`MissingMemberHandling.Ignore`), or `JsonUtility.FromJson`, or has `[JsonExtensionData]`.
    B. **Strict** — Loader uses `MissingMemberHandling.Error`, or a custom deserializer that throws on unknown fields.
    C. **Unverifiable** — Loader source not accessible. Choose conservative Strict fallback per RESEARCH §Open Questions §2.

    Write `07-Q2-UNITY-LOADER-VERDICT.md` with: VERDICT line, Evidence section, Plan 05 instruction with `_ZIP_BOUND_FIELDS` value, Rationale.
  </action>
  <acceptance_criteria>
    - File EXISTS
    - File contains one line matching `^## VERDICT: (Tolerant|Strict|Unverifiable-default-Strict)$`
    - Evidence section cites at least one `.cs` file path OR "drive not accessible" note
    - File contains the literal `_ZIP_BOUND_FIELDS: frozenset[str] = frozenset({...})` line matching the verdict
  </acceptance_criteria>
  <verify>
    <automated>grep -E "^## VERDICT: (Tolerant|Strict|Unverifiable-default-Strict)$" .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md</automated>
  </verify>
  <done>Verdict file written. Plan 05 will read this file in its read_first list.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User confirms Q2 verdict before Plan 05 lands merge_overlay</name>
  <files>.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md</files>
  <read_first>
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md (verdict + evidence written by Task 1)
  </read_first>
  <action>
    Pause for user to spot-check Task 1's verdict.
  </action>
  <what-built>
    `07-Q2-UNITY-LOADER-VERDICT.md` with verdict + `_ZIP_BOUND_FIELDS` value.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md`
    2. Read the cited evidence (Unity .cs file path + line number + literal code line)
    3. Confirm the verdict matches your knowledge of the Reconquista codebase.

    **NIT 1 retry guidance**: If Claude defaulted to `Unverifiable-default-Strict` because the D:\ drive was not accessible from the agent shell:
       a. BEFORE approving the conservative fallback, MANUALLY run the verification yourself first:
          ```
          grep -rn "territory_metadata\|MissingMemberHandling\|JsonExtensionData" D:/Projetos_Jogo/Reconquista/Assets/Scripts/
          ```
       b. If grep returns evidence (e.g., a clear `MissingMemberHandling.Ignore` or `JsonUtility.FromJson` call), EDIT the verdict file to record the correct verdict before approving.
       c. ONLY accept the `Unverifiable-default-Strict` fallback if the manual grep also fails (e.g., drive truly unmounted, .cs files encrypted/missing).
       d. The conservative fallback is safe (overlay still satisfies SC #2 — historical names show via the `name` field), but it forecloses delivering `kingdom_owner` + `historical_notes` in the zip until a future plan revisits Q2.

    4. Type `approved` to unblock Plan 05, or describe the issue.
  </how-to-verify>
  <acceptance_criteria>
    - User typed `approved` and the verdict file has not been amended after approval, OR the user edited the verdict file and re-approved
    - If verdict is `Unverifiable-default-Strict`: SUMMARY.md notes whether the user ran the NIT 1 manual grep before accepting the fallback
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md</automated>
  </verify>
  <done>User-approved verdict file is on disk; Plan 05 can begin.</done>
  <resume-signal>Type "approved" or describe the issue (e.g., "actually Tolerant, please update file").</resume-signal>
</task>

</tasks>

<context_anchors>
- **D-03** (overlay sidecar field semantics)
- **D-04** (merge at zip-build + artifact endpoint)
- **D-12** (zero-LLM parity stays green)
- **RESEARCH §Pattern 5** (Q2 verification dependency)
- **RESEARCH §Pitfall 8** (ConfigDict extra='forbid')
- **RESEARCH §Open Questions §2** (Wave 0 GATE)
- **Checker NIT 1** (retry-grep guidance for Unverifiable fallback)
</context_anchors>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disk read | Reading Reconquista .cs files — read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-00-01 | Information Disclosure | Q2 verdict file | accept | Verdict file committed; only public Reconquista source paths cited. |
| T-07-00-02 | Tampering | Q2 verdict file | accept | Single-developer; checkpoint:human-verify surfaces discrepancies. |

</threat_model>

<verification>
- `test -f .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md`
- `grep -E "^## VERDICT:" .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md` returns one match
- User typed `approved` in checkpoint Task 2
</verification>

<success_criteria>
- Plan 05 has unambiguous `_ZIP_BOUND_FIELDS` value
- No downstream plan can land merge_overlay code with the wrong constant
</success_criteria>

<output>
After completion, create `.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-00-SUMMARY.md` per the standard template.
</output>
