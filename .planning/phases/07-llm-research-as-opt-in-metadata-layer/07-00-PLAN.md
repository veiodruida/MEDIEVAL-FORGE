---
phase: 07-llm-research-as-opt-in-metadata-layer
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md
autonomous: false
requirements:
  - V3-LLM-OPT-IN
must_haves:
  truths:
    - "Reconquista Unity C# loader strictness verdict (Tolerant vs Strict) is captured in 07-Q2-UNITY-LOADER-VERDICT.md before any Plan 05 / Plan 08 task runs"
    - "Verdict drives the value of `_ZIP_BOUND_FIELDS` constant in services/research/overlay.py (Tolerant -> frozenset({\"name\",\"kingdom_owner\",\"historical_notes\"}); Strict -> frozenset({\"name\"}))"
    - "Verdict file cites the exact Unity loader source file path + line number where MissingMemberHandling / JsonExtensionData / equivalent is configured"
  artifacts:
    - path: ".planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md"
      provides: "Wave 0 Q2 gate output — Unity loader strictness verdict"
      contains: "VERDICT:"
  key_links:
    - from: ".planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md"
      to: "backend/medieval_forge/services/research/overlay.py (Plan 05)"
      via: "_ZIP_BOUND_FIELDS constant value flips based on verdict"
      pattern: "_ZIP_BOUND_FIELDS"
---

<objective>
Resolve Open Question Q2 from 07-RESEARCH.md: does the Reconquista Unity C# loader tolerate unknown JSON keys on condado entries in `territory_metadata.json`, or does it reject them?

Purpose: RESEARCH §Pattern 5 "Q2 verification dependency" makes this a Wave 0 gate. The verdict controls a single constant (`_ZIP_BOUND_FIELDS`) in Plan 05's `services/research/overlay.py`. Discovering Strict during e2e testing would force a rewrite of merge_overlay + build_unity_zip integration; resolving here costs minutes and unblocks Plans 05 + 08 deterministically.

Output:
- `07-Q2-UNITY-LOADER-VERDICT.md` with the verdict + exact source-file evidence + the `_ZIP_BOUND_FIELDS` value Plan 05 must use.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-CONTEXT.md
@.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read Reconquista Unity loader source and produce Q2 verdict</name>
  <files>.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md</files>
  <read_first>
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md (§Pattern 5 "Q2 verification dependency" + §Open Questions §2 + §Pitfall 8)
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-CONTEXT.md (D-03, D-04, canonical_refs §Reconquista contract)
    - CLAUDE.md (Rule 4 original_idx, Rule 7 byOriginalIdx, v3 Pipeline Contract)
    - The Reconquista Unity loader source files under D:\Projetos_Jogo\Reconquista\Assets\Scripts\ that consume territory_metadata.json. Discover via Glob pattern `D:/Projetos_Jogo/Reconquista/Assets/Scripts/**/*.cs` and Grep `territory_metadata|TerritoryMetadata|JsonConvert|JsonUtility|MissingMemberHandling|JsonExtensionData` on that directory.
  </read_first>
  <action>
    Read the Reconquista Unity C# loader code that parses `territory_metadata.json`. Locate the deserialization call (likely Newtonsoft `JsonConvert.DeserializeObject<T>` or Unity's `JsonUtility.FromJson<T>`). Determine which of these three states applies:

    A. **Tolerant** — Loader uses Newtonsoft default (`MissingMemberHandling.Ignore`), or `JsonUtility.FromJson` (which silently drops unknown keys), or has `[JsonExtensionData]` catch-all. Unknown JSON keys are dropped without error.
    B. **Strict** — Loader uses `MissingMemberHandling.Error` setting (rejects on unknown keys), or a custom deserializer that throws on unknown fields.
    C. **Unverifiable** — Loader source is not accessible (path missing, no .meta files, encrypted assets). In this case, choose the conservative Strict fallback per RESEARCH §Open Questions §2 ("Conservative fallback if verification is blocked: emit ONLY name in the zip").

    Write `.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md` with this exact structure:

    ```markdown
    # Q2 Verdict — Reconquista Unity Loader Strictness

    **Verdict date:** {YYYY-MM-DD}
    **Verifier:** Claude (Wave 0)

    ## VERDICT: {Tolerant | Strict | Unverifiable-default-Strict}

    ## Evidence

    - **Loader source file:** `D:\Projetos_Jogo\Reconquista\Assets\Scripts\{exact path}.cs` line {N}
    - **Deserialization call:** ```{exact line of code copied verbatim from the .cs file}```
    - **Configuration:** {e.g., "JsonSerializerSettings default — MissingMemberHandling=Ignore is the Newtonsoft default" OR "Custom settings with MissingMemberHandling = MissingMemberHandling.Error explicitly set at line X"}
    - **Field model:** {C# class name} consumes {list of fields actually read by the game, e.g., `id`, `name`, `original_idx`, `kingdom`}

    ## Plan 05 instruction

    `services/research/overlay.py:_ZIP_BOUND_FIELDS` MUST be set to:

    ```python
    # Tolerant verdict:
    _ZIP_BOUND_FIELDS: frozenset[str] = frozenset({"name", "kingdom_owner", "historical_notes"})

    # Strict verdict (also use for Unverifiable):
    _ZIP_BOUND_FIELDS: frozenset[str] = frozenset({"name"})
    ```

    Use the value matching the verdict above.

    ## Rationale

    {1-2 sentences explaining the verdict from the evidence}
    ```

    Use Read tool to fetch the .cs file contents directly (the working directory is C:\Users\veio_\Documents\Unity_Projects\MEDIEVAL-FORGE; D:\ is a separate drive — Read with absolute path D:/... should work in this environment if the drive is mounted). If D:\ is genuinely inaccessible from this shell session, record verdict = `Unverifiable-default-Strict` with that justification.
  </action>
  <acceptance_criteria>
    - File `.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md` EXISTS
    - File contains exactly one line matching the regex `^## VERDICT: (Tolerant|Strict|Unverifiable-default-Strict)$`
    - File contains an "Evidence" section with at least one `.cs` file path under `D:\Projetos_Jogo\Reconquista\Assets\Scripts\` OR an explicit "drive not accessible from this session" note
    - File contains the literal Python line `_ZIP_BOUND_FIELDS: frozenset[str] = frozenset({...})` matching the verdict
    - Verdict choice (Tolerant vs Strict/Unverifiable) drives the exact `_ZIP_BOUND_FIELDS` value Plan 05 will paste verbatim
  </acceptance_criteria>
  <verify>
    <automated>grep -E "^## VERDICT: (Tolerant|Strict|Unverifiable-default-Strict)$" .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md</automated>
  </verify>
  <done>Verdict file written and committed. Plan 05 will read this file in its read_first list and copy _ZIP_BOUND_FIELDS verbatim.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User confirms Q2 verdict before Plan 05 lands merge_overlay</name>
  <files>.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md</files>
  <read_first>
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md (verdict + evidence written by Task 1)
  </read_first>
  <action>
    Pause for the user to spot-check Task 1's verdict against their knowledge of the Reconquista codebase. The user owns the final call because they own the Unity project and may have local-only context that the agent shell cannot reach.
  </action>
  <what-built>
    `07-Q2-UNITY-LOADER-VERDICT.md` with verdict {Tolerant | Strict | Unverifiable-default-Strict} and the exact `_ZIP_BOUND_FIELDS` value Plan 05 must paste.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md`
    2. Read the cited evidence (Unity .cs file path + line number + literal code line)
    3. Confirm the verdict matches what you know about the Reconquista codebase. If you have access to the Reconquista repo locally, run `grep -rn "territory_metadata\|MissingMemberHandling\|JsonExtensionData" D:/Projetos_Jogo/Reconquista/Assets/Scripts/` and spot-check the cited lines.
    4. If Claude defaulted to `Unverifiable-default-Strict` because the D: drive was not accessible from the agent shell, decide whether to:
       a. Override the verdict by running the verification yourself and editing the file, OR
       b. Accept the conservative Strict fallback (overlay still satisfies SC #2 — historical names show; kingdom_owner + historical_notes stay UI-served-only).
    5. Type `approved` to unblock Plan 05, or describe the issue.
  </how-to-verify>
  <acceptance_criteria>
    - User typed `approved` (or equivalent confirmation) and the verdict file has not been amended after approval, OR the user edited the verdict file and re-approved.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md</automated>
  </verify>
  <done>User-approved verdict file is on disk; Plan 05 can begin.</done>
  <resume-signal>Type "approved" or describe the issue (e.g., "actually Tolerant, please update file").</resume-signal>
</task>

</tasks>

<context_anchors>
- **D-03** (overlay sidecar field semantics — name / kingdom_owner / historical_notes)
- **D-04** (merge happens at zip-build time + artifact endpoint)
- **D-12** (zero-LLM parity stays green)
- **RESEARCH §Pattern 5** (Q2 verification dependency — _ZIP_BOUND_FIELDS constant)
- **RESEARCH §Pitfall 8** (ConfigDict extra='forbid') — schema extension in Plan 05 is the strict-fallback hedge
- **RESEARCH §Open Questions §2** (Wave 0 GATE, not deferred)
</context_anchors>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disk read | Reading Reconquista .cs files — no untrusted data flows; read-only operation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-00-01 | Information Disclosure | Q2 verdict file | accept | Verdict file committed to .planning/; only public Reconquista source-code paths cited. |
| T-07-00-02 | Tampering | Q2 verdict file | accept | Single-developer project; if verdict is wrong, the checkpoint:human-verify task surfaces the discrepancy. |

</threat_model>

<verification>
- `test -f .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md`
- `grep -E "^## VERDICT:" .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-Q2-UNITY-LOADER-VERDICT.md` returns one match
- User typed `approved` in checkpoint Task 2
</verification>

<success_criteria>
- Plan 05 has an unambiguous `_ZIP_BOUND_FIELDS` value to paste from the verdict file
- No downstream plan (05, 08, 11) can land merge_overlay code with the wrong constant
</success_criteria>

<output>
After completion, create `.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-00-SUMMARY.md` per the standard template.
</output>
