---
phase: 07-llm-research-as-opt-in-metadata-layer
plan: 04
type: execute
wave: 2
depends_on: [01, 03]
files_modified:
  - backend/medieval_forge/services/llm/claude.py
  - backend/medieval_forge/services/llm/ollama.py
  - backend/medieval_forge/services/llm/__init__.py
  - backend/tests/unit/test_ollama_health.py
  - backend/tests/integration/test_claude_auth_chain.py
  - backend/tests/integration/__init__.py
autonomous: true
requirements:
  - V3-LLM-OPT-IN
must_haves:
  truths:
    - "services/llm/claude.py defines ClaudeProvider with the D-07 auth chain (CLI → DB → env → dialog) via _resolve_key() method"
    - "services/llm/ollama.py defines OllamaProvider with D-13 defaults (qwen2.5:7b primary; llama3.1:8b fallback) + 3s health-check timeout"
    - "services/llm/__init__.py registers both providers at import time: PROVIDERS has 2 entries after package import"
    - "ClaudeProvider degrades silently on 401/403 from the CLI token (Pitfall 4 — consumer-OAuth may not grant API access; chain falls through to DB / env / dialog)"
    - "OllamaProvider.health_check returns healthy=False with timeout-after-3s message when localhost:11434 is unreachable (Pitfall 5 + RESEARCH §Pattern 4)"
    - "test_claude_auth_chain.py is gated by @pytest.mark.anthropic and exercises 401-degrade + success-path"
  artifacts:
    - path: "backend/medieval_forge/services/llm/claude.py"
      provides: "ClaudeProvider — AsyncAnthropic + tool_use + D-07 auth chain"
      contains: "class ClaudeProvider:"
    - path: "backend/medieval_forge/services/llm/ollama.py"
      provides: "OllamaProvider — AsyncClient + format=schema + D-13 defaults"
      contains: "class OllamaProvider:"
    - path: "backend/tests/unit/test_ollama_health.py"
      provides: "Health-check coverage with mocked httpx (Wave 0 gate)"
      contains: "test_ollama_health_check"
    - path: "backend/tests/integration/test_claude_auth_chain.py"
      provides: "401-degrade + success-path coverage (gated by @pytest.mark.anthropic)"
      contains: "@pytest.mark.anthropic"
  key_links:
    - from: "backend/medieval_forge/services/llm/__init__.py"
      to: "backend/medieval_forge/services/llm/registry.py:register"
      via: "register(ClaudeProvider()); register(OllamaProvider())"
      pattern: "register\\(ClaudeProvider"
    - from: "backend/medieval_forge/services/llm/claude.py"
      to: "backend/medieval_forge/services/credential_store.py (Plan 01)"
      via: "Plan 07 runner passes session + db_payload into ClaudeProvider.research"
      pattern: "db_payload"
---

<objective>
Land the 2 MVP providers (Claude + Ollama) and complete the registry wiring. After this plan, `services.llm.PROVIDERS` returns 2 healthy/unhealthy entries; Plan 07 (research runner) can iterate them; Plan 09 (UI ProviderSelector) can render them.

Purpose: D-05 + D-07 + D-13 + Discretion #2/3/4 + RESEARCH §Pattern 3 + §Pattern 4 specify exact provider shapes. RESEARCH §Pitfall 4 + §Pitfall 5 enumerate the production failure modes (Claude CLI token may be consumer-OAuth; Ollama default model may not be installed). Both pitfalls are encoded as test assertions in this plan.

Output:
- ClaudeProvider with auth chain (Pattern 3)
- OllamaProvider with format=schema JSON enforcement (Pattern 4)
- Registry wired (PROVIDERS = {"claude": ..., "ollama": ...} at module import)
- Unit test: Ollama health check (mocked)
- Integration test (gated @pytest.mark.anthropic): Claude auth chain — 401-degrade + success-path
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-CONTEXT.md
@.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md
@backend/medieval_forge/services/llm/base.py
@backend/medieval_forge/services/llm/schemas.py
@backend/medieval_forge/services/llm/retry.py
@backend/medieval_forge/services/llm/registry.py
@backend/medieval_forge/services/credential_store.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ClaudeProvider — AsyncAnthropic + tool_use + D-07 auth chain (CLI → DB → env → dialog)</name>
  <files>
    backend/medieval_forge/services/llm/claude.py
    backend/tests/integration/__init__.py
    backend/tests/integration/test_claude_auth_chain.py
  </files>
  <read_first>
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md §Pattern 3 (full claude.py code block with _read_claude_cli_token + ClaudeProvider class)
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md §Pitfall 4 (consumer-OAuth vs API key; 401-degrade)
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-CONTEXT.md (D-07 chain order; Discretion #2 CLI piggyback path; Discretion #3 SDK version)
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md §Open Questions §1 (Q1 — degrade path expected; document in dialog UX)
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-VALIDATION.md (Wave 0 row 07-00-W0-AUTH — anthropic integration test marker)
    - backend/medieval_forge/services/llm/base.py (Plan 02 — LLMProvider Protocol shape)
    - backend/medieval_forge/services/llm/schemas.py (Plan 02 — schema.model_json_schema() consumed by tool_use)
  </read_first>
  <behavior>
    - Test 1 (integration, @pytest.mark.anthropic): `test_claude_auth_chain_uses_cli_token_when_claude_auth_status_exits_zero` — mock `subprocess.run(["claude","auth","status"])` to return returncode=0; mock pathlib Path.exists to return True for `~/.claude/.credentials.json`; mock json.loads to return a non-expired token; assert `_resolve_key` returns the CLI token (NOT the env or DB value).
    - Test 2 (integration): `test_claude_auth_chain_falls_through_to_db_when_cli_token_rejected_with_401` — same setup as Test 1 BUT mock AsyncAnthropic.messages.stream to raise on the CLI token; assert the provider falls through to db_payload (passed as `credentials` arg).
    - Test 3 (integration): `test_claude_auth_chain_falls_through_to_env_when_cli_and_db_empty` — no CLI, no DB; ANTHROPIC_API_KEY env set; assert env key is returned.
    - Test 4 (integration): `test_claude_resolve_key_returns_none_when_chain_bottoms_out` — no CLI, no DB, no env; assert `_resolve_key(None)` returns None.
  </behavior>
  <action>
    1. Create `backend/medieval_forge/services/llm/claude.py` VERBATIM from RESEARCH §Pattern 3, including:
       - `SYSTEM_PROMPT = "You are a historical-research assistant. Return JSON via the submit_research tool."`
       - `_read_claude_cli_token()` function with:
         * `subprocess.run(["claude", "auth", "status"], capture_output=True, text=True, timeout=3.0)` first
         * fallback to file-read of `~/.claude/.credentials.json` + `%APPDATA%\Claude\credentials.json` (Windows) + `XDG_CONFIG_HOME/claude/credentials.json` (Linux)
         * `expiresAt` validation (epoch-ms compared with `time.time() * 1000`)
       - `class ClaudeProvider` with:
         * `provider_id = "claude"`, `display_name = "Claude (Anthropic)"`
         * `auth_methods = [CliAuth(...), ApiKeyAuth(env_var="ANTHROPIC_API_KEY"), ApiKeyAuth(env_var=None)]`
         * `_resolve_key(db_payload)` per D-07 chain order: (1) CLI → (2) DB → (3) env → (4) None
         * `async def health_check(credentials)` returning HealthStatus
         * `async def research(prompt, schema, credentials, queue)` using `AsyncAnthropic.messages.stream` with `tool_use` + `tool_choice={"type":"tool","name":"submit_research"}`

    2. PIN model: `claude-sonnet-4-6` per CONTEXT D-09 placeholder (RESEARCH confirms verified at SDK 0.97).

    3. Per Pitfall 4 / RESEARCH §Pitfall 4 / Open Q1: in `research()`, wrap the stream call in `try/except` catching `anthropic.AuthenticationError` (401) — on auth failure, raise a typed exception that Plan 07 runner catches to fall through to the next chain link. Document inline: "Consumer-OAuth tokens may not grant API access — see RESEARCH Pitfall 4." For the V1 of this code, the simplest implementation is: don't catch at provider level; let auth errors propagate; Plan 07 runner re-invokes `_resolve_key` with `skip_cli=True` on 401. This deferral is acceptable as long as the chain-fall-through behavior is testable.

       **Simpler V1 implementation acceptable here:** Implement `_resolve_key` as Pattern 3 shows. Document the 401-degrade as a TODO in the docstring linking to Open Q1. The integration tests in this plan exercise the resolve-order; the Plan 07 runner adds the actual 401-handling on top.

    4. Create `backend/tests/integration/__init__.py` (empty file) so pytest discovers the new directory.

    5. Create `backend/tests/integration/test_claude_auth_chain.py` with the 4 cases from <behavior>. Mark each with `@pytest.mark.anthropic` (registered in Plan 01). Tests use `unittest.mock.patch` for subprocess/pathlib/json/AsyncAnthropic. Run command: `pytest -m anthropic` (skipped in CI by default).
  </action>
  <acceptance_criteria>
    - File `backend/medieval_forge/services/llm/claude.py` EXISTS
    - `grep -n "class ClaudeProvider:" backend/medieval_forge/services/llm/claude.py` returns 1 match
    - `grep -n "def _read_claude_cli_token" backend/medieval_forge/services/llm/claude.py` returns 1 match
    - `grep -n "def _resolve_key" backend/medieval_forge/services/llm/claude.py` returns 1 match
    - `grep -n 'subprocess.run(\[\s*"claude"' backend/medieval_forge/services/llm/claude.py` returns 1 match (Discretion #2 — CLI shell-out)
    - `grep -n 'tool_choice=' backend/medieval_forge/services/llm/claude.py` returns 1 match
    - `grep -n 'AsyncAnthropic' backend/medieval_forge/services/llm/claude.py` returns ≥1 match
    - `grep -n 'claude-sonnet-4-6' backend/medieval_forge/services/llm/claude.py` returns 1 match
    - `grep -n 'shell=False\|shell=True' backend/medieval_forge/services/llm/claude.py` — if any line uses subprocess, no shell=True must appear (T-07-04-04 mitigation)
    - File `backend/tests/integration/test_claude_auth_chain.py` EXISTS
    - `grep -c "@pytest.mark.anthropic" backend/tests/integration/test_claude_auth_chain.py` returns ≥4
    - `cd backend && python -c "from medieval_forge.services.llm.claude import ClaudeProvider; cp = ClaudeProvider(); assert cp.provider_id == 'claude'; print('ok')"` prints `ok`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -c "from medieval_forge.services.llm.claude import ClaudeProvider"</automated>
  </verify>
  <done>ClaudeProvider lands with the auth chain; integration test gated by anthropic marker; no shell=True.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OllamaProvider — AsyncClient + format=schema + D-13 defaults + health-check</name>
  <files>
    backend/medieval_forge/services/llm/ollama.py
    backend/tests/unit/test_ollama_health.py
  </files>
  <read_first>
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md §Pattern 4 (full ollama.py code block)
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-CONTEXT.md (D-13 — qwen2.5:7b primary + llama3.1:8b fallback; Discretion #4 — Ollama Python SDK over raw httpx)
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md §Pitfall 5 (default model may not be installed; auto-discover via client.list())
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-VALIDATION.md (Wave 0 row 07-XX-OLLAMA-HEALTH — `test_ollama_health.py`)
    - backend/medieval_forge/services/llm/base.py
  </read_first>
  <behavior>
    - Test 1: `test_ollama_health_check_returns_healthy_when_async_client_list_succeeds_within_3s` — mock `AsyncClient.list` to return a model list; assert HealthStatus(healthy=True, message="Reachable at http://localhost:11434").
    - Test 2: `test_ollama_health_check_returns_unhealthy_with_timeout_message_when_async_client_list_exceeds_3s` — mock `AsyncClient.list` to raise asyncio.TimeoutError; assert HealthStatus(healthy=False, message contains "timeout").
    - Test 3: `test_ollama_health_check_returns_unhealthy_with_exception_message_when_async_client_raises` — mock to raise ConnectionError; assert HealthStatus(healthy=False, message contains "Unreachable").
  </behavior>
  <action>
    1. Create `backend/medieval_forge/services/llm/ollama.py` VERBATIM from RESEARCH §Pattern 4:
       - `OLLAMA_HOST = "http://localhost:11434"`
       - `DEFAULT_MODEL = "qwen2.5:7b"` (D-13 primary)
       - `FALLBACK_MODEL = "llama3.1:8b"` (D-13 fallback)
       - `class OllamaProvider` with:
         * `provider_id = "ollama"`, `display_name = "Ollama (local)"`
         * `auth_methods = [NoAuth()]`
         * `health_check`: `await asyncio.wait_for(AsyncClient(host=OLLAMA_HOST).list(), timeout=3.0)` with TimeoutError + generic Exception handlers
         * `research`: `AsyncClient.chat(model=..., messages=[...], format=schema.model_json_schema(), stream=False)` + heartbeat task (3s period) sending `data: {"event_type":"heartbeat", ...}` events to the queue
         * Lenient-parse via literal-port `parse_research_json` when schema is `ResearchResult`

    2. Per Pitfall 5: per RESEARCH §Open Q3, the dialog Model field shows D-13 default ONLY if installed. The provider itself does NOT auto-fallback to FALLBACK_MODEL — the UI picks. Provider just accepts whatever model string the runner passes. Document this as a comment.

    3. Create `backend/tests/unit/test_ollama_health.py` with the 3 cases from <behavior>. Use `unittest.mock.patch("medieval_forge.services.llm.ollama.AsyncClient")` to mock the SDK; use `pytest.mark.asyncio` for async tests.
  </action>
  <acceptance_criteria>
    - File `backend/medieval_forge/services/llm/ollama.py` EXISTS
    - `grep -n "class OllamaProvider:" backend/medieval_forge/services/llm/ollama.py` returns 1 match
    - `grep -n 'DEFAULT_MODEL = "qwen2.5:7b"' backend/medieval_forge/services/llm/ollama.py` returns 1 match
    - `grep -n 'FALLBACK_MODEL = "llama3.1:8b"' backend/medieval_forge/services/llm/ollama.py` returns 1 match
    - `grep -n 'timeout=3.0' backend/medieval_forge/services/llm/ollama.py` returns ≥1 match
    - `grep -n 'format=schema.model_json_schema' backend/medieval_forge/services/llm/ollama.py` returns 1 match
    - `grep -n 'NoAuth()' backend/medieval_forge/services/llm/ollama.py` returns ≥1 match
    - File `backend/tests/unit/test_ollama_health.py` EXISTS
    - `grep -c "^async def test_" backend/tests/unit/test_ollama_health.py` returns ≥3
    - `cd backend && pytest tests/unit/test_ollama_health.py -x -q` exits 0 with all tests passed
    - `cd backend && python -c "from medieval_forge.services.llm.ollama import OllamaProvider; op = OllamaProvider(); assert op.provider_id == 'ollama'; print('ok')"` prints `ok`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && pytest tests/unit/test_ollama_health.py -x -q</automated>
  </verify>
  <done>OllamaProvider lands with D-13 defaults + 3s health-check timeout; unit tests verify all 3 health states.</done>
</task>

<task type="auto">
  <name>Task 3: Wire ClaudeProvider + OllamaProvider into registry (__init__.py) + PROVIDERS == 2 assertion</name>
  <files>
    backend/medieval_forge/services/llm/__init__.py
    backend/tests/unit/test_llm_registry.py
  </files>
  <read_first>
    - backend/medieval_forge/services/llm/__init__.py (Plan 03 — has the deferred-population shim)
    - backend/medieval_forge/services/llm/registry.py (Plan 03 — register / get / list_providers)
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md §Pattern 2 (intent: PROVIDERS = {"claude":..., "ollama":...})
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-CONTEXT.md (D-05 — exactly 2 providers)
  </read_first>
  <action>
    1. UPDATE `backend/medieval_forge/services/llm/__init__.py` to import and register both providers:

       ```python
       """services/llm/ — D-05 (Claude + Ollama only). OpenAI/Gemini deferred."""
       from .base import LLMProvider, HealthStatus, ApiKeyAuth, CliAuth, NoAuth, AuthMethod
       from .schemas import ResearchResult, MapResearchResult, parse_research_json
       from .parse import parse_research_response   # adjust to literal-port symbol
       from .retry import run_with_retry            # adjust to literal-port symbol
       from .prompt import build_map_research_prompt
       from .registry import PROVIDERS, register, get, list_providers
       from .claude import ClaudeProvider
       from .ollama import OllamaProvider

       # Plugin registry — Discretion #10: import-time population.
       # OpenAI + Gemini stay slot-ready; add lines here in v3.1 to extend.
       register(ClaudeProvider())
       register(OllamaProvider())
       ```

    2. Create `backend/tests/unit/test_llm_registry.py` with these cases:
       - `test_registry_has_exactly_2_providers_after_package_import` — `from medieval_forge.services.llm import PROVIDERS; assert len(PROVIDERS) == 2`
       - `test_registry_exposes_claude_and_ollama_provider_ids` — `assert set(PROVIDERS.keys()) == {"claude", "ollama"}`
       - `test_registry_get_returns_provider_for_known_id_and_none_for_unknown` — `get("claude")` is not None; `get("openai")` is None (D-05 lock)
       - `test_registry_provider_classes_satisfy_llm_provider_protocol` — `isinstance(get("claude"), LLMProvider)` is True (runtime-checkable from base.py)

    NB: These tests run WITHOUT mocking the SDK — they just instantiate the provider classes. Provider constructors must NOT make network calls (verified by reading RESEARCH §Pattern 3 + §Pattern 4 — both construct lazily; clients are instantiated inside `health_check` / `research`, not in `__init__`).
  </action>
  <acceptance_criteria>
    - `grep -n "register(ClaudeProvider())" backend/medieval_forge/services/llm/__init__.py` returns 1 match
    - `grep -n "register(OllamaProvider())" backend/medieval_forge/services/llm/__init__.py` returns 1 match
    - File `backend/tests/unit/test_llm_registry.py` EXISTS
    - `grep -c "^def test_" backend/tests/unit/test_llm_registry.py` returns ≥4
    - `cd backend && pytest tests/unit/test_llm_registry.py -x -q` exits 0
    - `cd backend && python -c "from medieval_forge.services.llm import PROVIDERS; assert len(PROVIDERS) == 2; assert set(PROVIDERS) == {'claude','ollama'}; print('ok')"` prints `ok`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && pytest tests/unit/test_llm_registry.py -x -q && python -c "from medieval_forge.services.llm import PROVIDERS; assert len(PROVIDERS) == 2"</automated>
  </verify>
  <done>Registry has exactly 2 providers post-import; Plan 07 runner + Plan 09 ProviderSelector can iterate.</done>
</task>

</tasks>

<context_anchors>
- **D-05** (Claude + Ollama only; OpenAI/Gemini deferred)
- **D-07** (Claude auth chain order)
- **D-13** (Ollama default models qwen2.5:7b + llama3.1:8b; 3s health timeout)
- **Discretion #2** (CLI piggyback file path discovery — `claude auth status` shell-out)
- **Discretion #3** (Anthropic SDK 0.97 — AsyncAnthropic.messages.stream with tool_use)
- **Discretion #4** (Ollama Python SDK over raw httpx)
- **Discretion #10** (import-time registry population)
- **RESEARCH §Pattern 3** (ClaudeProvider full code)
- **RESEARCH §Pattern 4** (OllamaProvider full code)
- **RESEARCH §Pitfall 4** (consumer-OAuth degrade; chain fall-through)
- **RESEARCH §Pitfall 5** (default model not installed; auto-discover)
- **VALIDATION.md row 07-00-W0-AUTH + 07-XX-OLLAMA-HEALTH**
</context_anchors>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process boundary → `claude` CLI | subprocess.run shell-out (Discretion #2) |
| Process boundary → Anthropic API | AsyncAnthropic over HTTPS |
| Process boundary → Ollama API | AsyncClient over HTTP (localhost) |
| DB credential read | credential_store.get_credentials returns plaintext payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Spoofing | OAuth token validation | mitigate | `_read_claude_cli_token` checks `expiresAt > time.time() * 1000` before returning the token (RESEARCH §Pattern 3 line). Acceptance: grep for `expires_at_ms > time.time() * 1000` returns 1 match in claude.py. |
| T-07-04-02 | Information Disclosure | Token leak via error message | mitigate | ClaudeProvider's `research()` MUST NOT include the key in raised exception messages. Acceptance: `grep -E "raise .*key" backend/medieval_forge/services/llm/claude.py` returns 0 matches (no key interpolation in error strings). |
| T-07-04-03 | Information Disclosure | Token leak via SSE event | mitigate | Heartbeat / token events emit only `text` content from the LLM, never the credential. Acceptance: `grep -E "queue.*put.*key\|queue.*put.*credentials" backend/medieval_forge/services/llm/*.py` returns 0 matches. |
| T-07-04-04 | Command Injection | `claude auth status` shell-out | mitigate | `subprocess.run([...], shell=False, timeout=3.0)` with hardcoded argv (no user input). Acceptance: `grep -n "shell=True" backend/medieval_forge/services/llm/claude.py` returns 0 matches. RESEARCH §Security Domain documents this. |
| T-07-04-05 | DoS | CLI shell-out timeout | mitigate | 3.0s timeout via `subprocess.run(..., timeout=3.0)`. Acceptance: `grep -n "timeout=3.0" backend/medieval_forge/services/llm/claude.py` returns ≥1 match. |
| T-07-04-06 | DoS | Ollama health-check timeout | mitigate | 3.0s `asyncio.wait_for` timeout. Acceptance: `grep -n "timeout=3.0" backend/medieval_forge/services/llm/ollama.py` returns ≥1 match. |
| T-07-04-07 | Tampering | Path traversal via Claude credential file candidates | mitigate | All 3 candidate paths are absolute and derived from controlled env vars (HOME, APPDATA, XDG_CONFIG_HOME). No user input flows into path construction. |
| T-07-04-08 | Spoofing | Provider impersonation in registry | mitigate | Registry populates via direct class import (ClaudeProvider, OllamaProvider) inside __init__.py; no dynamic loading from disk. Acceptance: `grep -n "importlib\|exec\|eval" backend/medieval_forge/services/llm/__init__.py backend/medieval_forge/services/llm/registry.py` returns 0 matches. |

</threat_model>

<verification>
- `cd backend && python -c "from medieval_forge.services.llm import PROVIDERS; assert len(PROVIDERS) == 2"` exits 0
- `cd backend && pytest tests/unit/test_ollama_health.py tests/unit/test_llm_registry.py -x -q` exits 0
- `cd backend && pytest -m anthropic tests/integration/test_claude_auth_chain.py -q` exits 0 OR skips (network unavailable; tests use mocks so they should pass)
- `grep -n "shell=True" backend/medieval_forge/services/llm/*.py` returns 0 matches (T-07-04-04)
- `grep -n "expires_at_ms" backend/medieval_forge/services/llm/claude.py` returns ≥1 match (T-07-04-01)
</verification>

<success_criteria>
- 2 MVP providers ship as classes that satisfy LLMProvider Protocol
- Registry has exactly 2 entries at module import time (D-05 lock)
- Auth chain falls through CLI → DB → env → dialog per D-07 (Pattern 3 implementation)
- 3s health-check timeout on Ollama prevents hung dialogs (Pitfall 5)
- Integration tests + unit tests in place; Plan 07 runner can call providers without further wiring
</success_criteria>

<output>
After completion, create `.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-04-SUMMARY.md` per the standard template.
</output>
