---
phase: 07-llm-research-as-opt-in-metadata-layer
plan: 03
type: execute
wave: 1
depends_on: [02]
files_modified:
  - backend/medieval_forge/services/llm/retry.py
  - backend/medieval_forge/services/llm/prompt.py
  - backend/medieval_forge/services/llm/registry.py
  - backend/medieval_forge/services/llm/__init__.py
  - backend/tests/unit/test_llm_retry.py
autonomous: true
requirements:
  - V3-LLM-OPT-IN
must_haves:
  truths:
    - "services/llm/retry.py is a LITERAL PORT of commit 87f8aab~1:retry.py (65 LOC) — attribution comment at file head"
    - "services/llm/prompt.py is a LITERAL PORT of commit 87f8aab~1:prompt.py (417 LOC) — attribution comment at file head"
    - "services/llm/registry.py exposes PROVIDERS: dict[str, LLMProvider] with exactly 2 entries (claude, ollama) — D-05 lock"
    - "test_llm_retry.py passes with 2+ cases"
    - "registry.py is import-time per Discretion #10"
    - "WARNING 3 verdict: retry.py emission path documented in <dependency_note> — either (a) writes to queue/stream OR (b) only raises exceptions that the v3 runner catches to emit structured retry events"
  artifacts:
    - path: "backend/medieval_forge/services/llm/retry.py"
      provides: "3-retry loop with error-in-prompt feedback (LITERAL PORT)"
      contains: "# Literal port from commit 87f8aab~1; see D-02 in 07-CONTEXT.md"
    - path: "backend/medieval_forge/services/llm/prompt.py"
      provides: "build_map_research_prompt + coverage rules (LITERAL PORT, 417 LOC)"
      contains: "# Literal port from commit 87f8aab~1; see D-02 in 07-CONTEXT.md"
    - path: "backend/medieval_forge/services/llm/registry.py"
      provides: "PROVIDERS dict skeleton"
      contains: "PROVIDERS:"
    - path: "backend/tests/unit/test_llm_retry.py"
      provides: "retry-loop coverage (Wave 0 gate)"
      contains: "test_run_with_retry"
  key_links:
    - from: "backend/medieval_forge/services/llm/retry.py"
      to: "backend/medieval_forge/services/llm/schemas.py (Plan 02)"
      via: "imports ResearchResult / parse_research_json"
      pattern: "from \\.schemas import"
    - from: "backend/medieval_forge/services/llm/registry.py"
      to: "backend/medieval_forge/services/llm/claude.py + ollama.py (Plan 04)"
      via: "PROVIDERS dict populated in Plan 04 via register()"
      pattern: "register"
---

<objective>
Complete D-02 literal ports (retry.py + prompt.py) and land the plugin registry skeleton.

Per WARNING 3: this plan ALSO produces a verdict in <dependency_note> on retry.py's emission path. The verdict drives Plan 09a's useResearchStream dual-shape requirement.

Purpose: D-05 + D-02 + Discretion #10 + RESEARCH §Pattern 2 + §Anti-Patterns.

Output:
- retry.py + prompt.py (literal ports with attribution)
- registry.py (skeleton; populated in Plan 04)
- test_llm_retry.py
- <dependency_note> with retry.py emission verdict (WARNING 3)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-CONTEXT.md
@.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md
@backend/medieval_forge/services/llm/base.py
@backend/medieval_forge/services/llm/schemas.py
@backend/medieval_forge/services/llm/parse.py

<dependency_note>
**WARNING 3 verdict (record in 07-03-SUMMARY.md after Task 1):**

After landing the literal-ported retry.py, grep its emission pattern:

```bash
grep -nE "yield|queue\.put|event_emit|send|asyncio\.Queue" backend/medieval_forge/services/llm/retry.py
```

Then record ONE of these verdicts in the SUMMARY:

- **Verdict (a) — retry.py writes to queue/stream**: dual-shape SSE tolerance in Plan 09a useResearchStream is JUSTIFIED. Keep the dual-shape parser. Document the grep match line.
- **Verdict (b) — retry.py only raises exceptions**: dual-shape SSE tolerance is REDUNDANT. The v3 runner (Plan 07b) catches the exception and emits a structured `event_type: "retry"` envelope. In this case, Plan 09a can SIMPLIFY useResearchStream to consume ONLY structured envelopes.

If verdict (b): Plan 09a should be revised post-Plan-03 to drop dual-shape. If verdict (a): Plan 09a's current dual-shape design is correct.

This verdict is captured in Plan 03's SUMMARY so Plan 09a's executor reads it before implementing useResearchStream.
</dependency_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Literal-port retry.py + prompt.py from 87f8aab~1 + WARNING 3 emission verdict</name>
  <files>
    backend/medieval_forge/services/llm/retry.py
    backend/medieval_forge/services/llm/prompt.py
    backend/medieval_forge/services/llm/__init__.py
  </files>
  <read_first>
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-CONTEXT.md (D-02)
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md §Anti-Patterns
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md §Pitfall 9 (PT-BR "Tentativa N/M" — DO NOT translate)
    - 87f8aab~1 snapshots via `git show`
    - backend/medieval_forge/services/llm/schemas.py
  </read_first>
  <action>
    1. Fetch literal ports:
       ```bash
       git show 87f8aab~1:backend/medieval_forge/services/llm/retry.py > backend/medieval_forge/services/llm/retry.py
       git show 87f8aab~1:backend/medieval_forge/services/llm/prompt.py > backend/medieval_forge/services/llm/prompt.py
       ```

    2. PREPEND attribution comment to each:
       ```python
       # Literal port from commit 87f8aab~1; see D-02 in 07-CONTEXT.md.
       # DO NOT MODIFY behaviorally. Allowed edits: import-line adjustments only.
       ```

    3. Per Pitfall 9: retry.py emits PT-BR "Tentativa N/M" messages. DO NOT rewrite. Keep as 87f8aab~1.

    4. Normalize imports if needed (relative `from .schemas import`).

    5. UPDATE `__init__.py` to expose new symbols (run_with_retry, build_map_research_prompt).

    6. **WARNING 3 — emission verdict**: After the literal port lands, run the grep below and record the verdict in 07-03-SUMMARY.md:

       ```bash
       grep -nE "yield|queue\.put|event_emit|send|asyncio\.Queue|\.write\(" backend/medieval_forge/services/llm/retry.py
       ```

       Verdict (a) if matches > 0: retry.py writes to a stream channel → dual-shape SSE tolerance JUSTIFIED in Plan 09a.
       Verdict (b) if matches == 0: retry.py only raises → dual-shape SSE tolerance REDUNDANT; Plan 09a should simplify post-revision.

       Record the verdict + grep evidence in 07-03-SUMMARY.md. Plan 09a executor reads it.

    7. Atomic commits per file (chore commits).
  </action>
  <acceptance_criteria>
    - File `backend/medieval_forge/services/llm/retry.py` EXISTS
    - File `backend/medieval_forge/services/llm/prompt.py` EXISTS
    - `grep -c "Literal port from commit 87f8aab~1" backend/medieval_forge/services/llm/retry.py` returns 1
    - `grep -c "Literal port from commit 87f8aab~1" backend/medieval_forge/services/llm/prompt.py` returns 1
    - `wc -l backend/medieval_forge/services/llm/retry.py` reports ≥65 lines
    - `wc -l backend/medieval_forge/services/llm/prompt.py` reports ≥417 lines
    - `grep -n "Tentativa" backend/medieval_forge/services/llm/retry.py` returns ≥1 match (Pitfall 9)
    - `grep -n "def build_map_research_prompt" backend/medieval_forge/services/llm/prompt.py` returns 1 match
    - `cd backend && python -c "from medieval_forge.services.llm.retry import run_with_retry; from medieval_forge.services.llm.prompt import build_map_research_prompt"` exits 0
    - **WARNING 3**: 07-03-SUMMARY.md contains a section `## WARNING 3 — retry.py emission verdict` with (a) or (b) + grep evidence cited
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -c "from medieval_forge.services.llm.retry import run_with_retry; from medieval_forge.services.llm.prompt import build_map_research_prompt"</automated>
  </verify>
  <done>4 of 4 D-02 ports landed; WARNING 3 verdict recorded in SUMMARY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_llm_retry.py — Wave 0 retry-loop coverage</name>
  <files>
    backend/tests/unit/test_llm_retry.py
  </files>
  <read_first>
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-VALIDATION.md
    - backend/medieval_forge/services/llm/retry.py (post-Task 1)
  </read_first>
  <behavior>
    - Test 1: `test_run_with_retry_succeeds_on_second_attempt_when_first_returns_invalid_json`
    - Test 2: `test_run_with_retry_raises_after_3_consecutive_validation_errors`
    - Test 3: `test_run_with_retry_appends_error_to_subsequent_prompt`
  </behavior>
  <action>
    Create `backend/tests/unit/test_llm_retry.py` with 3 cases using AsyncMock LLM callable.
  </action>
  <acceptance_criteria>
    - File EXISTS
    - `grep -c "^async def test_" backend/tests/unit/test_llm_retry.py` returns ≥3
    - `cd backend && pytest tests/unit/test_llm_retry.py -x -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd backend && pytest tests/unit/test_llm_retry.py -x -q</automated>
  </verify>
  <done>Wave 0 retry-loop coverage in place.</done>
</task>

<task type="auto">
  <name>Task 3: registry.py — plugin registry skeleton with deferred provider instantiation</name>
  <files>
    backend/medieval_forge/services/llm/registry.py
    backend/medieval_forge/services/llm/__init__.py
  </files>
  <read_first>
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-RESEARCH.md §Pattern 2
    - .planning/phases/07-llm-research-as-opt-in-metadata-layer/07-CONTEXT.md (D-05; Discretion #10)
    - backend/medieval_forge/services/llm/base.py
  </read_first>
  <action>
    Create `backend/medieval_forge/services/llm/registry.py` with deferred-population shim: empty PROVIDERS dict + register/get/list_providers helpers. Plan 04 will call register() for both providers.

    UPDATE `__init__.py` to expose PROVIDERS, register, get, list_providers.

    Do NOT instantiate ClaudeProvider() / OllamaProvider() here (they don't exist yet — Plan 04).
  </action>
  <acceptance_criteria>
    - File EXISTS
    - `grep -n "PROVIDERS: dict\[str, LLMProvider\] = {}" backend/medieval_forge/services/llm/registry.py` returns 1 match
    - `grep -n "def register" backend/medieval_forge/services/llm/registry.py` returns 1 match
    - `grep -n "def get(provider_id: str)" backend/medieval_forge/services/llm/registry.py` returns 1 match
    - `grep -n "def list_providers" backend/medieval_forge/services/llm/registry.py` returns 1 match
    - `cd backend && python -c "from medieval_forge.services.llm import PROVIDERS, register, get, list_providers; assert PROVIDERS == {}; print('ok')"` prints `ok`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -c "from medieval_forge.services.llm import PROVIDERS, register, get, list_providers; assert isinstance(PROVIDERS, dict)"</automated>
  </verify>
  <done>Registry skeleton with deferred-population shim.</done>
</task>

</tasks>

<context_anchors>
- **D-02** (literal ports)
- **D-05** (Claude + Ollama only)
- **Discretion #10** (import-time registry)
- **RESEARCH §Pattern 2** (registry code)
- **RESEARCH §Pitfall 9** (PT-BR + dual SSE envelope)
- **RESEARCH §Anti-Patterns** (dependency order)
- **VALIDATION.md test_llm_retry**
- **Checker WARNING 3** (emission verdict recorded in SUMMARY → drives Plan 09a dual-shape decision)
</context_anchors>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → retry loop | Untrusted LLM JSON triggers schema validation |
| Module-import order | registry.py is part of public surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | DoS | retry loop max_retries | mitigate | 3-retry hard cap (literal port). |
| T-07-03-02 | Tampering | retry.py PT-BR strings | accept | Pitfall 9 documents this. |
| T-07-03-03 | Information Disclosure | retry.py error-in-prompt feedback | accept | By design (LLM corrects its own errors). |
| T-07-03-04 | Tampering | registry deferred population | mitigate | PROVIDERS == {} at Plan 03 end; Plan 04 fills it. |

</threat_model>

<verification>
- `cd backend && python -c "from medieval_forge.services.llm import LLMProvider, PROVIDERS, register, get, list_providers, ResearchResult, MapResearchResult, parse_research_json, run_with_retry, build_map_research_prompt"` exits 0
- `cd backend && pytest tests/unit/test_llm_retry.py tests/unit/test_llm_schemas.py tests/unit/test_llm_parse.py tests/unit/test_llm_base.py -x -q` exits 0
- `grep -c "Literal port from commit 87f8aab~1" backend/medieval_forge/services/llm/*.py` returns 4
- WARNING 3: 07-03-SUMMARY.md contains the retry.py emission verdict
</verification>

<success_criteria>
- D-02 fully landed
- Registry skeleton in place
- Wave 0 test_llm_retry green
- WARNING 3 verdict recorded → drives Plan 09a dual-shape decision
</success_criteria>

<output>
After completion, create `.planning/phases/07-llm-research-as-opt-in-metadata-layer/07-03-SUMMARY.md` per the standard template. Include a section `## WARNING 3 — retry.py emission verdict` documenting (a) or (b) + grep evidence.
</output>
