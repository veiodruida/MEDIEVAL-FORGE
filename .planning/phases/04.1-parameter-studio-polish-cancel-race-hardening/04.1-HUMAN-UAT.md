---
status: diagnosed
phase: 04.1-parameter-studio-polish-cancel-race-hardening
source:
  - 04.1-VERIFICATION.md
started: 2026-05-11T14:55:00Z
updated: 2026-05-11T15:30:00Z
---

## Current Test

[testing complete — 0/3 passed, 3 issues reported; routing to gap closure]

## Pre-flight (run once before testing)

```
# Terminal 1 — backend (port 8765)
cd C:\Users\veio_\Documents\Unity_Projects\MEDIEVAL-FORGE
medieval-forge start
# fallback: cd backend && uvicorn medieval_forge.main:app --reload --port 8765

# Terminal 2 — frontend (port 5173)
cd C:\Users\veio_\Documents\Unity_Projects\MEDIEVAL-FORGE\frontend
npm run dev
```

Open http://localhost:5173/projects on a seeded Iberia 868 project. Open DevTools console (F12).

## Tests

### 1. UAT gap #1 closure — zoom/pan persistence across slider re-render
expected: Open seeded Iberia 868 project; manually zoom into a region of interest (Galiza or Andalusia); drag σ slider from 3.0 → 4.5 → 3.5; visually confirm the zoom level and pan position are preserved across every render cycle; no fit-to-view reset.
why_human: Originating gap was visual UX feedback ("annoying when comparing details after a small slider change"). Playwright spec covers the contract via __forgeStageScale read, but human UAT validates the perceived experience the user actually reported.
result: pass
resolved_by: "debug 04.1-bug-pan-reset (post-1c9dbba): stale-fallback refs in CanvasViewer prevent Stage unmount during cacheVersion-driven refetch. Root cause was TanStack v5 `placeholderData: keepPreviousData` not carrying data across queryKey changes in `useQueries` — every slider drag bumped cacheVersion → all queryKeys changed → all queries went pending → metaQ.isPending+isPending guards unmounted Stage → fresh Stage at Konva default (0,0,1) → fitToView re-centered. Fix: store last-good data in refs at CanvasViewer level; early-return guards check `effectiveMeta = data ?? lastGoodRef.current`. Verified via Playwright MCP: before {x:-400, y:50, scale:0.3} → after σ 3.0→4.5 settled: {x:-400, y:50, scale:0.3}. panPreserved:true, scalePreserved:true."

### 2. UAT gap #2 closure — hold-spacebar before/after preview gesture
expected: After a settled render at σ=3.0, drag σ to 4.5 and wait for completion. Press-and-hold spacebar: canvas swaps to the σ=3.0 raster and "Anterior" badge appears in top-left. Release: canvas restores to σ=4.5 raster and badge disappears. Gesture is responsive (no perceptible flicker — warm HTTP cache assumption holds in real browser).
why_human: Originating gap was UX comparison capability. Playwright spec uses Buffer.compare on 3 canvas screenshots, but only human can validate "feels instant, no flicker, comparable at a glance" — the actual UX goal.
result: issues
issue: "User reported: 'faz flicker. Nao vi diferenças aumentei e diminuir pametros e nao vi diferença no mapa, isso tbm se aplica ao botao space'. Two sub-issues: (a) flicker during spacebar gesture (warm-cache assumption may not hold — likely cache key mismatch or extra re-fetch path); (b) NO PERCEPTIBLE MAP DIFFERENCE between sigma values, which means either (i) σ change isn't actually re-rendering, OR (ii) the rendered output is identical despite the value change, OR (iii) canvas isn't repainting on cacheVersion change. This is concerning — Phase 04 SC-3 'pixel diff visible' was marked passed in 04-06-HUMAN-UAT but may have regressed. Spacebar shows no difference because there IS no difference between current and prior render."

### 3. UAT gap #3 closure — barony historical panel discoverability
expected: Click a frontier-region barony with a visibly large Voronoi cell (e.g., in Andalusia or the Marca). InspectorSidebar shows: name + hierarchy badges (kingdom/duchy/condado), Coordenada de origem (lat, lon to 3 decimals), Origem dos dados monospaced label `inicio/territory_data_v3.py`, collapsible "Sobre o método" expandable to PT-BR explainer mentioning "centroides históricos" and "Voronoi". The user understands WHY a barony appears large.
why_human: Originating gap was user's question "notei baronias muito grandes e nao sei se elas sao realmente assim como posso ter certeza?" — closure requires that the panel ACTUALLY satisfies the user's epistemic need. Playwright asserts DOM presence; human UAT validates the panel answers the question.
result: issues
issue: "User reported: 'nao encontrei tal painel'. The barony historical panel (Coordenada de origem + Origem dos dados + Sobre o método) is NOT visible when clicking a barony. Possible root causes: (a) BaronyLayer click handler not firing or not selecting; (b) selectedBaronyId not propagating to InspectorSidebar in dev environment; (c) baronies prop with centroid not being passed to InspectorSidebar (regression of the plumbing); (d) InspectorSidebar barony branch condition not being entered; (e) panel only renders when centroid is non-null and centroid is missing from BaronyRender. Investigation needed."

## Summary

total: 3
passed: 0
issues: 3
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Map canvas keeps current pan position when slider triggers a re-render"
  status: failed
  reason: "User reported: 'a visao e movida para canto esqurdo superior' — pan position resets to top-left on slider re-render even though scale appears preserved."
  severity: major
  test: 1
  root_cause: "Hypothesis: Plan 04.1-03's bounds-key gate prevents the auto-fit effect from firing, but Stage position (x, y) is being reset by a separate code path. Likely candidates: (a) viewport-key effect with the `Math.abs(currentScale - minScale) < 1e-6` guard from Plan 04.1-05 — when the user has zoomed, currentScale > minScale so this guard PREVENTS the auto-fit; but the same effect may still be setting Stage.x/y separately; (b) Stage component re-mount via the 'Loading map…' branch in CanvasViewer.tsx lines 499-512 — even with keepPreviousData on the queries, some transient state during slider re-render may briefly drop into loading branch and unmount the Stage; (c) a different effect that calls Stage.position({x: 0, y: 0}) or equivalent."
  artifacts:
    - path: "frontend/src/components/canvas/CanvasViewer.tsx"
      issue: "Stage position reset on slider-triggered re-render; scale is preserved but x/y are not"
  missing:
    - "Diagnostic test or console log capturing Stage.x() / Stage.y() before and after a slider drag"
    - "Vitest case: simulate user zoom + pan, then trigger metadata refetch, assert Stage position preserved (NOT just scale)"
    - "Fix: ensure pan position is tracked in the same ref-gated mechanism as scale, or persist position in localStorage / Zustand, or ensure Stage never re-mounts during slider re-render cycle"
  debug_session: ""

- truth: "Slider value changes produce visible map differences AND spacebar gesture swaps between two visibly different rasters"
  status: failed
  reason: "User reported: 'Nao vi diferenças aumentei e diminuir pametros e nao vi diferença no mapa, isso tbm se aplica ao botao space' — no perceptible map difference between sigma values, and spacebar gesture shows no difference (because there IS no difference). Phase 04 SC-3 pixel-diff regression."
  severity: critical
  test: 2
  root_cause: "Hypothesis 1 (regression of Phase 04 SC-3): the render pipeline IS being invoked on slider change but the cacheVersion update isn't producing a new image hash, OR the same raster is being served. Hypothesis 2 (UI-layer cache): canvas is not actually being repainted — useCanvasArtifacts placeholderData: keepPreviousData (added in Plan 04.1-05 Rule 1 deviation) may be holding the OLD data even after a successful refetch — verify the cacheVersion query key is changing. Hypothesis 3 (backend): the parameter override isn't being passed through the pipeline — POST /render with diffOverrides may not include the sigma value. Hypothesis 4 (visual): the σ 3.0 → 4.5 difference at Iberia scale at zoom-out is genuinely too subtle for human eye — but in this case spacebar would still need to swap between two distinct rasters. The spacebar showing no difference rules this out — confirms the canvas state is not actually updating."
  artifacts:
    - path: "frontend/src/hooks/useCanvasArtifacts.ts"
      issue: "placeholderData: keepPreviousData (Plan 04.1-05) may be masking the cacheVersion update — verify queries actually invalidate"
    - path: "frontend/src/components/canvas/CanvasViewer.tsx"
      issue: "cacheVersion prop transition effect (lines 168-185) — verify lastCacheVersionRef advances on each render completion"
    - path: "frontend/src/hooks/useParameterStudioDispatch.ts"
      issue: "POST /render diffOverrides — verify sigma value is included in the body"
    - path: "backend/medieval_forge/api/v3/render.py"
      issue: "Pipeline must consume diffOverrides.smooth_sigma — verify config override is wired to the smoothing stage"
  missing:
    - "Diagnostic: open DevTools Network, drag σ from 3.0 → 4.5, confirm POST /render body includes smooth_sigma:4.5 AND confirm GET /artifacts/visual_*.png returns a different image hash (different byte length or different first bytes)"
    - "Vitest case: simulate cacheVersion change, assert Stage.batchDraw() or equivalent fires AND the underlying Konva Image src has changed"
    - "E2E spec: σ 3.0 → 4.5 then σ 4.5 → 3.0, assert canvas pixel hash differs at both ends (not just one direction)"
    - "If the regression is in keepPreviousData: replace with a smarter staleTime / refetchOnMount strategy that still avoids the Stage unmount on transition"
  debug_session: ""

- truth: "Clicking a barony surfaces the historical discoverability panel"
  status: failed
  reason: "User reported: 'nao encontrei tal painel' — clicking a barony does NOT show the Coordenada de origem / Origem dos dados / Sobre o método sections in InspectorSidebar."
  severity: critical
  test: 3
  root_cause: "Hypothesis 1 (selection layer): BaronyLayer click handler not firing in real browser — likely Konva listening: false on a wrapper, or click event swallowed by overlay. Hypothesis 2 (selection state): useSelectionStore.selectedBaronyId not propagating to InspectorSidebar — store binding regression. Hypothesis 3 (props): InspectorSidebar's baronies prop is undefined or empty in real env — the new required prop from Plan 04.1-04 may have a wiring miss in production code path that vitest mocks bypass. Hypothesis 4 (conditional render): the new sections render only when centroid is non-null AND InspectorSidebar receives baronies; vitest sets centroid via mocked BaronyRender but real env may emit baronies WITHOUT centroid (backend canvas_sidecars.py emits it, but if a project pre-dates this it won't have it). Hypothesis 5 (visibility): the new sections render but are below the viewport fold — InspectorSidebar may need scroll."
  artifacts:
    - path: "frontend/src/components/canvas/InspectorSidebar.tsx"
      issue: "barony branch with Coordenada de origem / Origem dos dados / Sobre o método sections — verify the conditional that gates these"
    - path: "frontend/src/components/canvas/CanvasViewer.tsx"
      issue: "baronies prop forwarded to InspectorSidebar — verify it's not undefined in real env"
    - path: "frontend/src/components/canvas/BaronyLayer.tsx"
      issue: "barony click handler — verify listening:true and onClick fires"
    - path: "backend/medieval_forge/services/canvas_sidecars.py"
      issue: "baronies.geojson MUST include centroid in properties for projects rendered after Plan 04.1-04 — verify the project was re-rendered after deploy"
  missing:
    - "Diagnostic: in DevTools, click a barony, check Components/Redux DevTools for useSelectionStore.selectedBaronyId update AND InspectorSidebar.props.selectedBaronyId; check Console for any rendering errors in the barony branch"
    - "Vitest case in real-env shape: render InspectorSidebar with baronies that have NO centroid (old project) and confirm graceful fallback (— or hidden section, not crash)"
    - "Diagnostic: check if the seeded UAT project was created BEFORE Plan 04.1-04 deploy — if so, its baronies.geojson lacks centroid; re-seed required"
    - "E2E spec: use real /api endpoints (not mocks) to verify centroid propagates from backend to InspectorSidebar in production code path"
  debug_session: ""

