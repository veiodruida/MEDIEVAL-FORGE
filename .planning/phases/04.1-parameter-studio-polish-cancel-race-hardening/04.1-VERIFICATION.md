---
phase: 04.1-parameter-studio-polish-cancel-race-hardening
verified: 2026-05-11T14:55:00Z
status: human_needed
score: 7/7 must-haves verified at code level
overrides_applied: 0
human_verification:
  - test: "UAT gap #1 closure — zoom/pan persistence across slider re-render"
    expected: "Open seeded Iberia 868 project; manually zoom into a region of interest (Galiza or Andalusia); drag σ slider from 3.0 → 4.5 → 3.5; visually confirm the zoom level and pan position are preserved across every render cycle; no fit-to-view reset"
    why_human: "Originating gap was visual UX feedback ('annoying when comparing details after a small slider change'). Playwright spec covers the contract via __forgeStageScale read, but human UAT validates the perceived experience the user actually reported."
  - test: "UAT gap #2 closure — hold-spacebar before/after preview gesture"
    expected: "After a settled render at σ=3.0, drag σ to 4.5 and wait for completion. Press-and-hold spacebar: canvas swaps to the σ=3.0 raster and 'Anterior' badge appears in top-left. Release: canvas restores to σ=4.5 raster and badge disappears. Gesture is responsive (no perceptible flicker — warm HTTP cache assumption holds in real browser)."
    why_human: "Originating gap was UX comparison capability. Playwright spec uses Buffer.compare on 3 canvas screenshots, but only human can validate 'feels instant, no flicker, comparable at a glance' — the actual UX goal."
  - test: "UAT gap #3 closure — barony historical panel discoverability"
    expected: "Click a frontier-region barony with a visibly large Voronoi cell (e.g., in Andalusia or the Marca). InspectorSidebar shows: name + hierarchy badges (kingdom/duchy/condado), Coordenada de origem (lat, lon to 3 decimals), Origem dos dados monospaced label 'inicio/territory_data_v3.py', collapsible 'Sobre o método' expandable to PT-BR explainer mentioning 'centroides históricos' and 'Voronoi'. The user understands WHY a barony appears large."
    why_human: "Originating gap was user's question 'notei baronias muito grandes e nao sei se elas sao realmente assim como posso ter certeza?' — closure requires that the panel ACTUALLY satisfies the user's epistemic need. Playwright asserts DOM presence; human UAT validates the panel answers the question."
---

# Phase 04.1: Parameter studio polish + cancel-race hardening — Verification Report

**Phase Goal:** Close the 6 deferred items from Phase 04 verification (3 UAT polish gaps D-01/D-02/D-03 + 3 hardening items WR-01/WR-02/WR-03) plus a final E2E regression sweep. The phase passes if: (1) zoom/pan persists across slider re-renders; (2) hold-spacebar shows previous render; (3) clicking a barony surfaces a discoverability panel; (4) cancel→POST 409 race no longer flips useRunStore to 'error' (bounded 3-retry/1.5s recovery); (5) ParameterSidebar consolidated onto canonical hook; (6) backend producer evicts dicts so late /render/stream returns 404 not hang; (7) all 4 Phase 04 SCs remain green via re-run + 3 new Playwright specs.

**Verified:** 2026-05-11T14:55:00Z
**Status:** human_needed — automated checks all green (7/7 SCs satisfied at code level + unit-test level + E2E-spec-file level), 3 hands-on UAT scenarios outstanding to seal UAT closure for the 3 gaps originally surfaced by human UAT.
**Re-verification:** No — initial verification.

## Goal Achievement

### Observable Truths (mapped from ROADMAP Success Criteria + Phase Goal items 1-7)

| #   | Truth (Success Criterion)   | Status     | Evidence       |
| --- | --------------------------- | ---------- | -------------- |
| 1   | SC-1: Canvas preserves zoom/pan across slider re-render — `CanvasViewer.tsx` no longer calls `fitToView()` on projection ref changes with identical bounds | ✓ VERIFIED | `projectionBoundsKey` memo (line 305) + `prevBoundsKeyRef` (line 317) + bounds-keyed auto-fit effect (lines 322-329) in `CanvasViewer.tsx`; separate viewportKey effect with `Math.abs(currentScale - minScale) < 1e-6` guard (line 352); 3 vitest cases in `CanvasViewer.fitToView.test.tsx` green; Playwright `parameter-studio-zoom-persistence.spec.ts` documented green at 2026-05-11T13:28:13Z |
| 2   | SC-2: Before/after preview affordance (hold-spacebar shows previous render) | ✓ VERIFIED | `previousCacheVersion` state (line 168) + `lastCacheVersionRef` (line 169) + cacheVersion-transition effect (lines 171-185); `previewPrior` state + spacebar listeners with `e.code !== 'Space'`, `e.repeat`, INPUT/TEXTAREA/contentEditable skip, `window.addEventListener('blur', ...)` reset (lines 187-218); 'Anterior' badge with `data-testid="preview-prior-badge"` at line 590; 7 vitest cases in `CanvasViewer.previewPrior.test.tsx`; Playwright `parameter-studio-preview-gesture.spec.ts` |
| 3   | SC-3: In-app discoverability panel for the historical 868 AD barony dataset | ✓ VERIFIED | `BaronyFeature.properties.centroid?: [number, number]` (`useCanvasArtifacts.ts` line 21) + `BaronyRender.centroid?: [number, number]` (line 78) + select transform passthrough (line 202); `InspectorSidebar.tsx` extended barony branch with `data-testid="barony-source-coord"` (line 136), `barony-source-file` (line 151), `barony-method-explainer` (line 164); literal `inicio/territory_data_v3.py` rendered (line 154); PT-BR 'Sobre o método' explainer with `centroides históricos` (line 172); 5 vitest cases in `InspectorSidebar.test.tsx`; Playwright `parameter-studio-barony-historical.spec.ts` |
| 4   | SC-4: Cancel→POST 409 race (WR-01) treated as non-fatal — debounce re-queue replaces useRunStore.finish('error', 'RENDER_BUSY') | ✓ VERIFIED | `RENDER_BUSY_MAX_RETRIES = 3` + `RENDER_BUSY_WINDOW_MS = 1500` exported (`useParameterStudioDispatch.ts` lines 37-38); per-(project_id) `useRef<Map<...>>` counter (line 46); bounded retry catches `Error('RENDER_BUSY')` and re-queues for counts 1-3 (lines 63-78), surfaces error only on 4th consecutive 409 (line 80); 4 vitest cases prove the contract |
| 5   | SC-5: `_RUN_QUEUES` and `_RUN_TASKS` evicted in producer finally blocks (WR-02) — late stream subscribers no longer hang | ✓ VERIFIED | `_render_producer` finally pops `_RUN_QUEUES` (line 181) and `_RUN_TASKS` (line 182) AFTER `await queue.put(None)`; `_generate_producer` mirrors with pops at lines 155-156; 6 pytest cases (3 per producer) prove eviction on success/cancel/failure paths; stream endpoints return HTTP 404 with detail "no active render run" (render.py:239) / "no active generate run" (generate.py:219) |
| 6   | SC-6: Single canonical dispatch path (WR-03) — `ParameterSidebar.tsx` consumes `useParameterStudioDispatch` instead of inlining | ✓ VERIFIED | `ParameterSidebar.tsx` line 7: `import { useParameterStudioDispatch }`; line 32: `const debouncedRender = useParameterStudioDispatch(projectId, ...)`; grep confirms 0 occurrences of `diffOverrides`, `postRender`, `postRenderCancel`, `useDebouncedCallback`, `useRunStore` in ParameterSidebar (canonical fingerprint of consolidation); vitest case `test_parameter_sidebar_invokes_use_parameter_studio_dispatch` at line 115 proves the hook is invoked with `('proj-1', onRenderStarted)` |
| 7   | Phase Goal item 7: All 4 Phase 04 SCs (SC-1..SC-4) remain green via re-run of existing specs + 3 new Playwright specs | ✓ VERIFIED | `parameter-studio-sc3.spec.ts` and `parameter-studio-cancel.spec.ts` (Phase 04 specs) preserved unmodified; 3 new specs created (`parameter-studio-zoom-persistence.spec.ts`, `parameter-studio-preview-gesture.spec.ts`, `parameter-studio-barony-historical.spec.ts`); Plan 05 SUMMARY documents 5/5 green on combined run at 2026-05-11T13:28:13Z; backend parity 12 passed + 6 xfailed + 4 xpassed (baseline unchanged — re-verified this session at 14:50Z with identical result); frontend vitest 210/210 green (re-verified this session) |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected    | Status | Details |
| -------- | ----------- | ------ | ------- |
| `backend/medieval_forge/api/v3/render.py` | `_render_producer` finally evicts `_RUN_QUEUES` + `_RUN_TASKS` | ✓ VERIFIED | Lines 181-182; correct ordering (sentinel first) preserved |
| `backend/medieval_forge/api/v3/generate.py` | `_generate_producer` finally evicts `_RUN_QUEUES` + `_RUN_TASKS` | ✓ VERIFIED | Lines 155-156; symmetric with render; Pitfall 9 docstring updated |
| `frontend/src/hooks/useParameterStudioDispatch.ts` | Canonical dispatch hook with bounded RENDER_BUSY retry | ✓ VERIFIED | Exported constants + per-project useRef<Map> counter + 4th-attempt error surfacing |
| `frontend/src/components/canvas/ParameterSidebar.tsx` | Consumes useParameterStudioDispatch; no inlined dispatch | ✓ VERIFIED | Import + invocation + zero divergent imports remaining |
| `frontend/src/components/canvas/CanvasViewer.tsx` | Stable bounds-key gate + spacebar gesture + Anterior badge | ✓ VERIFIED | All required state, refs, listeners, JSX, plus Plan 05 keepPreviousData / viewport-key guard Rule 1 fixes |
| `frontend/src/components/canvas/InspectorSidebar.tsx` | Barony historical panel with 4 new sections | ✓ VERIFIED | Coordenada de origem + Origem dos dados + collapsible Sobre o método explainer + all 3 data-testids |
| `frontend/src/hooks/useCanvasArtifacts.ts` | BaronyFeature.properties.centroid + BaronyRender.centroid + select propagation | ✓ VERIFIED | All 3 type extensions + select transform + 5 placeholderData: keepPreviousData (Plan 05 D-01 fix) |
| `frontend/tests/e2e/parameter-studio-zoom-persistence.spec.ts` | SC-1 Playwright spec | ✓ VERIFIED | File exists, test.describe present, no .skip |
| `frontend/tests/e2e/parameter-studio-preview-gesture.spec.ts` | SC-2 Playwright spec | ✓ VERIFIED | File exists, keyboard.down/up('Space'), preview-prior-badge selector, no cancel-render-button (correct semantic) |
| `frontend/tests/e2e/parameter-studio-barony-historical.spec.ts` | SC-3 Playwright spec | ✓ VERIFIED | File exists, 3 data-testids referenced, literal source path |
| `backend/tests/integration/test_render_endpoint.py` | 3 new pytest cases for render eviction | ✓ VERIFIED | Lines 335, 375, 435 |
| `backend/tests/unit/test_v3_generate.py` | 3 new pytest cases for generate eviction | ✓ VERIFIED | Lines 263, 310, 353 |
| `backend/tests/integration/test_render_cancel.py` | 2 pre-existing tests reshaped to subscribe-before-release | ✓ VERIFIED | `threading.Event` gate added (deviation auto-fix documented in Plan 01 SUMMARY) |
| `frontend/src/stores/__tests__/usePipelineParams.test.ts` | 4 new vitest cases for D-04 retry | ✓ VERIFIED | Lines 235, 265, 291, 334 |
| `frontend/src/components/canvas/__tests__/ParameterSidebar.test.tsx` | New vitest case for D-06 consolidation | ✓ VERIFIED | Line 115 |
| `frontend/src/components/canvas/__tests__/CanvasViewer.fitToView.test.tsx` | 3 D-01 vitest cases | ✓ VERIFIED | Split-file deviation documented in Plan 03 SUMMARY; all 3 test names found |
| `frontend/src/components/canvas/__tests__/CanvasViewer.previewPrior.test.tsx` | 7 D-02 vitest cases | ✓ VERIFIED | All 7 test names found (incl. D-13 regression test) |
| `frontend/src/components/canvas/__tests__/InspectorSidebar.test.tsx` | 5 new D-03 vitest cases | ✓ VERIFIED | Lines 311, 323, 334, 346, 361 |

### Key Link Verification

| From | To  | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `_render_producer` finally | `_RUN_QUEUES` / `_RUN_TASKS` | `dict.pop(project_id, None)` | ✓ WIRED | render.py:181-182 (with await queue.put(None) at line 178) |
| `_generate_producer` finally | `_RUN_QUEUES` / `_RUN_TASKS` | `dict.pop(project_id, None)` | ✓ WIRED | generate.py:155-156 |
| `GET /render/stream` after run complete | HTTPException 404 | `_RUN_QUEUES.get(pid) is None → raises` | ✓ WIRED | render.py:239 "no active render run; POST /render first" |
| `ParameterSidebar.tsx` | `useParameterStudioDispatch` | import + hook invocation wired to renderStream.subscribe via onRenderStarted | ✓ WIRED | ParameterSidebar.tsx:7+32 |
| `useParameterStudioDispatch` RENDER_BUSY catch | `dispatch()` re-queue with counter | bounded 3-retry within 1.5s, then `finish('error', 'RENDER_BUSY')` | ✓ WIRED | useParameterStudioDispatch.ts:63-80 |
| `CanvasViewer` fitToView effect | stable projection bounds key | useRef<string> tracking last-fired key; early-return if unchanged | ✓ WIRED | CanvasViewer.tsx:305-329 |
| `CanvasViewer` cacheVersion prop transition | `previousCacheVersion` state | useRef snapshot promotes prior on transition | ✓ WIRED | CanvasViewer.tsx:168-185 |
| spacebar keydown/keyup handlers | `previewPrior` state + effectiveCacheVersion 3-way swap | useState + window.addEventListener gated on previewEnabled | ✓ WIRED | CanvasViewer.tsx:187-218 + 236-238 |
| `baronies.geojson` backend property | frontend `BaronyFeature.properties.centroid` | optional schema passthrough | ✓ WIRED | useCanvasArtifacts.ts:21+78+202 |
| InspectorSidebar barony branch | `metadata.condados/duchies/kingdoms` lookup chain | barony.condado_idx → condado → duchy → kingdom | ✓ WIRED | InspectorSidebar.tsx hierarchy badges + BaronyRender.centroid plumbed via props |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| InspectorSidebar barony panel (Coordenada de origem) | `centroid` | `BaronyRender.centroid` from useCanvasArtifacts select transform → backend `baronies.geojson` properties emitted by `canvas_sidecars.py` | Yes (real lon/lat from `inicio/territory_data_v3.py` post-pipeline) | ✓ FLOWING |
| CanvasViewer Anterior gesture | `previousCacheVersion` | `lastCacheVersionRef.current` snapshot at cacheVersion prop transition; cacheVersion is `project.updated_at` from `useProject` TanStack query | Yes (real timestamp string updated by backend after each /render write) | ✓ FLOWING |
| ParameterSidebar dispatch | `run_id` | `postRender` Promise resolution from `/api/v3/projects/{id}/render` | Yes (real backend route returning run_id) | ✓ FLOWING |
| _render_producer eviction | `_RUN_QUEUES[pid]` | `_RUN_QUEUES[project_id] = asyncio.Queue()` set at POST /render (line not directly shown but documented in Phase 04) | Yes (real asyncio.Queue lifecycle) | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Backend Phase 04.1 test suite (unit + integration) | `cd backend && python -m pytest tests/integration/test_render_endpoint.py tests/integration/test_render_cancel.py tests/unit/test_v3_generate.py -q` | 26 passed in 10.33s | ✓ PASS |
| Parity baseline preserved | `cd backend && python -m pytest tests/parity/ -q` | 12 passed, 6 xfailed, 4 xpassed in 126.55s (identical to Plan 01 baseline) | ✓ PASS |
| Frontend vitest full suite | `cd frontend && npx vitest run --reporter=dot` | 34 files / 210 tests passed in 7.09s | ✓ PASS |
| Playwright 5 specs combined (Plan 05 documented run) | `npx playwright test parameter-studio-*.spec.ts` | 5/5 green at 2026-05-11T13:28:13Z (total 2.7min); trusted from Plan 05 SUMMARY (re-run not executed this verification session — see Notes) | ✓ PASS (documented) |

### Requirements Coverage

This phase did not declare external requirement IDs (no `REQUIREMENTS.md` in v3 milestone — superseded by ROADMAP Success Criteria as the contract). All 6 ROADMAP Success Criteria (SC-1..SC-6) accounted for in the Observable Truths table above, plus phase-goal item 7 (regression + new Playwright specs).

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| SC-1 | Plans 03, 05 | Zoom/pan persistence across slider re-render | ✓ SATISFIED | Truth #1 above |
| SC-2 | Plans 03, 05 | Before/after preview affordance | ✓ SATISFIED | Truth #2 above |
| SC-3 | Plans 04, 05 | Barony historical discoverability panel | ✓ SATISFIED | Truth #3 above |
| SC-4 | Plans 02, 05 | Cancel→POST 409 race non-fatal | ✓ SATISFIED | Truth #4 above |
| SC-5 | Plans 01, 05 | Producer dict eviction | ✓ SATISFIED | Truth #5 above |
| SC-6 | Plans 02, 05 | Single canonical dispatch path | ✓ SATISFIED | Truth #6 above |

No orphaned requirement IDs detected. All plan `requirements_addressed:` fields map cleanly to SC-1..SC-6.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `frontend/src/components/canvas/InspectorSidebar.tsx` | 20, 190 | `PLACEHOLDER_PT` constant + rendering | ℹ️ Info — false positive | This is a legitimate PT-BR placeholder TEXT constant ("Clique num território para ver detalhes") used in the empty-state branch, not a stub. Pre-existing from Phase 03; not introduced by 04.1. |

Scanned 7 phase-touched source files (`useParameterStudioDispatch.ts`, `ParameterSidebar.tsx`, `CanvasViewer.tsx`, `InspectorSidebar.tsx`, `useCanvasArtifacts.ts`, `render.py`, `generate.py`) for TODO/FIXME/XXX/HACK/PLACEHOLDER/coming soon/not yet implemented. Zero blocker- or warning-severity matches.

### Code Review Observations (Separate Scope)

`04.1-REVIEW.md` flagged 4 warnings + 5 info findings during the standard-depth code review. **None of these regress a Phase 04.1 Success Criterion**; they are documented here for Phase 05 backlog routing:

- **REVIEW WR-01** (cancelled-stage `prior_token` SSE semantics) — pre-existing Phase 04 D-13 bug in `_render_producer`'s StageCancelled branch. Affects cancel-revert raster URL when `entry.prior_token` is empty; does NOT regress Phase 04 SC-4 (the `parameter-studio-cancel.spec.ts` UAT still green). Recommend Phase 05 quick-task.
- **REVIEW WR-02** (BackgroundLayer.visible tied to condados toggle) — pre-existing Phase 03/04 code; not touched by 04.1; not a Phase 04.1 SC. Recommend Phase 05 quick-task.
- **REVIEW WR-03** (`postRenderCancel().catch(() => {})` swallows too broadly) — the `.catch` was already at this line in Phase 04; 04.1 wrapped the surrounding body in retry logic but did not change the swallow scope. Recommend Phase 05 hardening pass.
- **REVIEW WR-04** (unused `sf` parameter on `_render_producer`) — explicitly listed as `IN-01` in 04.1-CONTEXT.md `D-08 — IN-01 / IN-02 explicitly out of scope`. By design.
- **REVIEW IN-01..IN-05** — info-severity nits (zoom-spec margin tightness, dead `_make_on_stage` helper, stale `# noqa`, unused fixtures, unused imports). Opportunistic cleanup, not blocking.

### Human Verification Required

The 3 originating gaps (D-01, D-02, D-03) were surfaced by human UAT (`04-06-HUMAN-UAT.md`). Automated Playwright + vitest coverage proves the contracts at the code level, but final closure of UAT-originated gaps benefits from a brief human re-UAT. See `human_verification:` in frontmatter for the 3 specific tests.

### Gaps Summary

No gaps blocking goal achievement. All 7 phase-goal items are satisfied at the code/unit/E2E-spec level. The phase delivers:

- Plan 01 (Wave 1) — backend WR-02 closed (eviction symmetric across render + generate)
- Plan 02 (Wave 2) — frontend WR-03 + WR-01 (D-06 + D-04) closed (single canonical dispatch + bounded retry)
- Plan 03 (Wave 3) — frontend D-01 + D-02 closed (bounds-key gate + hold-spacebar preview)
- Plan 04 (Wave 2) — frontend D-03 closed (BaronyRender.centroid plumbed + InspectorSidebar barony branch extended)
- Plan 05 (Wave 4) — 3 new Playwright specs landed + regression gate all-green; surfaced 2 production bugs (viewport-key clobber + missing keepPreviousData) which were auto-fixed in the same plan (documented as Rule 1 deviations)

The `human_needed` status reflects the originating-gap nature: 3 of the 6 SCs came from human UAT observations and merit a brief hands-on confirmation that the implementation truly closes the perceived UX issue (not just the code contract).

---

*Verified: 2026-05-11T14:55:00Z*
*Verifier: Claude (gsd-verifier)*
