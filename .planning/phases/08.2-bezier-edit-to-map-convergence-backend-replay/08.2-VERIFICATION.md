---
phase: 08.2-bezier-edit-to-map-convergence-backend-replay
verified: 2026-05-30T19:30:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 4/5
  gaps_closed:
    - "BEZ-CONV-05 PARTIAL — second Apply dropped the edited barony (replay_vertex_ring buffer(0) returned MultiPolygon stored without reduction guard)"
  gaps_remaining: []
  regressions: []
---

# Phase 08.2 Verification Report

**Phase Goal:** Close G8 (08.1-HUMAN-UAT) — implement barony vertex-op replay (move/add/delete) in
manual_edit.compute() + wire render cascade (or explicit Apply) so a Bézier contour edit converges
to the colored BaronyLayer and the 12-file Unity export. Iberia 10/10 byte/SSIM parity MUST stay
green (zero-edit path unchanged).

**Verified:** 2026-05-30T19:30:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (Plan 08.2-05, commits b161014 / 3e9c09e / f41c26e)

---

## Goal Achievement

### Observable Truths

| # | Truth (Requirement) | Status | Evidence |
|---|---------------------|--------|----------|
| 1 | move op replay in manual_edit.compute() (BEZ-CONV-01) | ✓ VERIFIED | `replay_vertex_ring` present at manual_edit.py:327; `vertex_ops_present` guard at line 124; 14 unit + integration tests pass (14 passed, 0 skipped, 0 xfailed); e2e gate green |
| 2 | add/delete replay in manual_edit.compute() (BEZ-CONV-02) | ✓ VERIFIED | Same `replay_vertex_ring` handles add (extra vertex key) and delete (missing key); `test_add_op_grows_barony_ring` + `test_multi_delete_op_shrinks_barony_ring` both in the 14-pass suite |
| 3 | render cascade trigger + baroniesQ refetch post-edit (BEZ-CONV-03) | ✓ VERIFIED | `useBezierApply.ts` exists with `postRender` direct call (no diffOverrides); `BezierApplyControls.tsx` has `bezier-apply-edits-btn` + `bezier-apply-auto-switch` data-testids; `temporal.pause → setState({editLog:[]}) → temporal.resume` overlay clear confirmed; 550 vitest tests pass; tsc clean |
| 4 | Iberia parity 10/10 unchanged on zero-edit path (BEZ-CONV-04) | ✓ VERIFIED | parity run: 22 passed, 6 xfailed, 4 xpassed (10/10 parity green); `_geometry_modified` short-circuit + `vertex_ops_present` guard confirmed in code at lines 124 + 198; fix is on buffer(0) heal branch only — zero-edit path never enters it |
| 5 | UAT: real-mouse Bézier edit → colored boundary changes + survives reload + appears in export (BEZ-CONV-05) | ✓ VERIFIED | Single Apply: VERIFIED (Playwright spec green, human-approved). Second Apply defect CLOSED by Plan 08.2-05: `isinstance(poly, MultiPolygon)` guard + `max(parts, key=lambda g: g.area)` reduction added in `replay_vertex_ring` after `buffer(0)` (manual_edit.py:390-394). Regression tests non-skipped and green (14 passed). |

**Score:** 5/5 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/medieval_forge/services/pipeline/manual_edit.py` | replay_vertex_ring with MultiPolygon reduction guard | ✓ VERIFIED | `from shapely.geometry import LineString, MultiPolygon, Polygon` at line 31; `isinstance(poly, MultiPolygon)` guard at lines 390-394; `max(parts, key=lambda g: g.area)` reduction confirmed |
| `backend/medieval_forge/api/v3/render.py` | _render_producer snapshot_loader injection | ✓ VERIFIED | `cfg.snapshot_loader = lambda _bid, _d=_data: _d` at line 192; `manual_edit_log_hash` fetch at line 182 |
| `backend/medieval_forge/services/pipeline/__init__.py` | barony_name_to_idx built from bars at both call sites | ✓ VERIFIED | `barony_name_to_idx = {b["name"]: i for i, b in enumerate(bars)}` at lines 428 and 652 |
| `frontend/src/stores/useEditorStore.ts` | bezierApplyMode field excluded from partialize | ✓ VERIFIED | `bezierApplyMode: 'manual'` default; `setBezierApplyMode` action; partialize covers only `vertices` + `editLog` |
| `frontend/src/hooks/useBezierApply.ts` | snapshot POST + direct postRender + overlay clear | ✓ VERIFIED | `edit_log: editLog` (snake_case) in payload; `postRender(` direct call; `temporal.pause` + `setState({editLog:[]})` + `temporal.resume`; `MAX_VERTEX_KEYS_FALLBACK` cap |
| `frontend/src/components/workspace/BezierApplyControls.tsx` | Apply button + Auto switch + Badge | ✓ VERIFIED | `bezier-apply-edits-btn` and `bezier-apply-auto-switch` data-testids; pt-BR copy confirmed |
| `frontend/src/components/canvas/BezierEditLayer.tsx` | editLog.length > 0 overlay guard | ✓ VERIFIED | `{editedRingPts.length >= 6 && editLog.length > 0 && (` |
| `frontend/tests/uat/playwright/08.2-bezier-convergence.spec.ts` | BEZ-CONV-05 un-skipped coastline UAT | ✓ VERIFIED | 0 test.skip/describe.skip; `COASTLINE_BARONY = 'Gijón'`; `page.mouse` used; 0 `__forgeBezier*` hooks |
| `backend/tests/unit/test_manual_edit_vertex_replay.py` | round-trip identity + replay + parity guard + BEZ-CONV-05 regression (9 tests) | ✓ VERIFIED | 9 test functions (7 prior + 2 new from Plan 05); 0 skips; RED-first evidence documented in SUMMARY-05; no `pytest.skip()` calls |
| `backend/tests/unit/test_render_producer_loader_injection.py` | serialize gate + loader injection tests | ✓ VERIFIED | 4 test functions; all pass |
| `backend/tests/integration/test_bezier_convergence_e2e.py` | e2e gate green (xfail removed) | ✓ VERIFIED | xfail annotation removed; 1 passed |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `_render_producer` | `cfg.snapshot_loader + cfg.manual_edit_log_hash` | DB fetch of Branch + latest Snapshot, by-value lambda | ✓ WIRED | `snapshot_loader` at render.py:192 (by-value, Pitfall 1 fixed); `manual_edit_log_hash` at line 182 |
| `manual_edit.compute` | `rasterio.features.rasterize` via `replay_vertex_ring` | _lonlat_to_rasterio_xy + Polygon rebuild + MultiPolygon guard | ✓ WIRED | `isinstance(poly, MultiPolygon)` guard reduces to largest-area Polygon before rasterization; second Apply path safe |
| `__init__.run_pipeline_incremental` | `manual_edit.compute(result, cfg, barony_name_to_idx)` | `{b["name"]: i for i, b in enumerate(bars)}` | ✓ WIRED | Lines 652-654 confirmed; same pattern at run_pipeline lines 428-430 |
| `WorkspaceToolbar / BezierApplyControls` → Apply button | `useBezierApply.handleApplyEdits` | `onClick` wired to `postRender` direct call | ✓ WIRED | `bezier-apply-edits-btn` onClick confirmed |
| `useBezierApply render-success callback` | `useEditorStore editLog reset` | `temporal.pause(); setState({editLog:[]}); temporal.resume()` | ✓ WIRED | Confirmed in useBezierApply.ts; `pendingApplyRef` guard prevents unrelated render from clearing edits |
| `BezierEditLayer overlay` | `useEditorStore.editLog` | `editLog.length > 0` guard | ✓ WIRED | BezierEditLayer.tsx confirmed |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `replay_vertex_ring` | `vertices_dict` / `polygons_by_id` | snapshot.get("vertices") via cfg.snapshot_loader (DB → deserialize) | ✓ Yes (real DB Snapshot blob) | ✓ FLOWING — MultiPolygon guard prevents second-Apply degradation; `polygons_by_id` always receives a `Polygon` |
| `BezierApplyControls` | baroniesQ (BaronyLayer re-render) | cacheVersion from project.updated_at bump after render | ✓ Yes (bump confirmed at bed660c) | ✓ FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Unit + integration tests (14) | `python -m pytest tests/unit/test_manual_edit_vertex_replay.py tests/unit/test_render_producer_loader_injection.py tests/integration/test_bezier_convergence_e2e.py -q` | 14 passed, 0 skipped, 0 xfailed (33.89s) | ✓ PASS |
| Iberia parity 10/10 | `python -m pytest tests/parity/ -q` | 22 passed, 6 xfailed, 4 xpassed (399.22s) | ✓ PASS |
| Frontend vitest suite | `cd frontend && npm run test` | 550 passed, 0 failed | ✓ PASS |
| TypeScript compilation | `cd frontend && npx tsc --noEmit` | clean (0 errors) | ✓ PASS |
| MultiPolygon guard present in replay_vertex_ring | `grep "isinstance.*MultiPolygon" manual_edit.py` | 1 match at line 390 (inside `replay_vertex_ring`, inside `if not poly.is_valid:` branch) | ✓ PASS |
| Regression test non-skipped: unit guard | `grep "pytest.skip" test_manual_edit_vertex_replay.py` | 0 matches in new test functions; `pytest.skip` appears only in fixture helper `test_lonlat_to_rasterio_xy_...` for a missing barony guard — not in BEZ-CONV-05 tests | ✓ PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| BEZ-CONV-01 | 08.2-01, 08.2-02 | move replay in manual_edit | ✓ SATISFIED | `replay_vertex_ring` + move path in compute(); unit tests green |
| BEZ-CONV-02 | 08.2-01, 08.2-02 | add/delete replay | ✓ SATISFIED | Same helper handles add (extra key) + delete (missing key); tests green |
| BEZ-CONV-03 | 08.2-01, 08.2-02, 08.2-03 | render cascade trigger + baroniesQ refetch | ✓ SATISFIED | `useBezierApply` + `_render_producer` wired; cacheVersion bump confirmed |
| BEZ-CONV-04 | 08.2-01, 08.2-02 | Iberia parity 10/10 unchanged | ✓ SATISFIED | parity: 22 passed, zero regressions; fix on buffer(0) branch only — zero-edit path unaffected |
| BEZ-CONV-05 | 08.2-01, 08.2-04, 08.2-05 | UAT: real-mouse edit → boundary changes + reload + export (including repeat-edit path) | ✓ SATISFIED | Single Apply: Playwright green + human approved. Second Apply: MultiPolygon guard closes defect — regression tests non-skipped and green (14 passed total). |

---

### Anti-Patterns Found

None — previous blocker (`buffer(0)` MultiPolygon stored without guard in `replay_vertex_ring`) is resolved by Plan 08.2-05. The fix is bounded to an in-memory geometry reduction; no new network endpoints, auth paths, or schema changes.

---

### Human Verification Required

None — BEZ-CONV-05 human checkpoint was completed in Plan 08.2-04 (user approved: "UAT funcionou — boundary muda, overlay limpa, sobrevive reload"). The second-Apply defect was a code defect verified and closed programmatically.

---

## Re-verification Summary

**Previous status:** gaps_found (4/5)

**Gap closed:** BEZ-CONV-05 PARTIAL — `replay_vertex_ring` in `manual_edit.py` now contains an
`isinstance(poly, MultiPolygon)` guard inside the `if not poly.is_valid:` branch (after `buffer(0)`).
When `buffer(0)` fragments a self-intersecting ring into multiple components, `max(parts, key=lambda g: g.area)`
selects the largest-area Polygon before storing into `polygons_by_id`. This prevents the second Apply
from producing a MultiPolygon entry, which was the root cause of the barony disappearing from
`baronies.geojson` and `territory_metadata.json` (CLAUDE.md rule #4 / `byOriginalIdx` contract).

**Regression tests added (non-skipped):**
1. `test_second_apply_self_intersecting_ring_reduces_to_polygon_not_multipolygon_in_polygons_by_id` — deterministic unit guard; confirmed RED on un-fixed code (SUMMARY-05 documents RED-first evidence), GREEN after fix.
2. `test_two_sequential_applies_same_barony_keep_gijon_in_baronies_geojson_sidecar` — symptom-level regression using real Iberia raster and `build_baronies_geojson_sidecar`; two non-collapsed `compute()` calls; Gijón must survive in `baronies.geojson` after second Apply.

**Commits:** b161014 (fix) → 3e9c09e (tests) → f41c26e (Test 1 anti-skip hardening: replaced `pytest.skip()` with `assert not poly.is_valid` to prevent vacuous test)

**No regressions** — parity unchanged (22 passed / 6 xfailed / 4 xpassed), identical to pre-fix baseline.

---

_Verified: 2026-05-30T19:30:00Z_
_Verifier: Claude (gsd-verifier)_
