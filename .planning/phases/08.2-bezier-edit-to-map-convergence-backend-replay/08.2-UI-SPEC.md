---
phase: 08.2
slug: bezier-edit-to-map-convergence-backend-replay
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-29
---

# Phase 08.2 — UI Design Contract

> Visual and interaction contract for Phase 08.2: Bézier edit-to-map convergence (backend replay).
> This is a DELTA spec — it extends Phase 08 and Phase 08.1 UI-SPECs verbatim.
> Only the convergence control (Apply button + mode toggle + in-flight state + overlay-clear
> transition) is declared here.
> Generated by gsd-ui-researcher. To be verified by gsd-ui-checker.

---

## Design System

Inherited verbatim from Phase 08 UI-SPEC. No changes.

| Property | Value |
|----------|-------|
| Tool | none — Radix UI Themes 3.x (CLAUDE.md locked) |
| Preset | not applicable |
| Component library | @radix-ui/themes 3.x |
| Icon library | @radix-ui/react-icons |
| Font | system-ui (Radix Themes default) |

> shadcn/ui is NOT used. Radix UI Themes is the locked design system. The shadcn init gate does not apply.

---

## Spacing Scale

Inherited verbatim from Phase 08 UI-SPEC. All values are multiples of 4. No new exceptions for Phase 08.2.

> This phase adds one small control group to the existing 48px horizontal WorkspaceToolbar.
> No new chrome panels, dialogs, or inspector sections are introduced.

---

## Typography

Inherited verbatim from Phase 08 UI-SPEC. No new typographic roles.

---

## Color

### Chrome palette

Inherited verbatim from Phase 08 UI-SPEC. No new Radix theme tokens.

### New badge color (cost warning)

| Element | Radix Token | Copy | Trigger |
|---------|-------------|------|---------|
| Auto-immediate cost badge | `color="orange" variant="soft"` | "Modo auto: ~3-5s por edição" | Shown only when auto-immediate switch is ON |

> The cost string is ~3-5s (not ~10s from the landmask badge) — barony cascade
> re-runs manual_edit → hierarchy → render → lookup → metadata, which is faster
> than the full landmask cascade (D-03). Do not copy the landmask badge string verbatim.

### Overlay-clear-on-converge (Konva — contract change from Phase 08.1)

**CONTEXT discretion decision:** The 08.1 amber-dashed live overlay should clear once the
colored barony re-renders to match (CONTEXT.md §Claude's Discretion, "clear-on-converge").

**Phase 08.1 overlay semantic (current):** Overlay shows whenever a barony is selected in
Bézier mode — regardless of whether there are pending edits. The guard in `BezierEditLayer`
line 870 is `editedRingPts.length >= 6` (pure geometry, no edit-log condition).

**Phase 08.2 overlay semantic (new):** Overlay shows ONLY when there are pending un-converged
edits (`editLog.length > 0`). After a successful Apply cascade, `editLog` is cleared →
overlay hides → only the converged colored raster remains visible. The "unsaved-edit"
affordance (`#f0c040` amber dashed) is now accurate: it is absent when the raster matches
the store geometry.

**Implementation:** Add `editLog.length > 0` as an additional guard in `BezierEditLayer`
at line 870:

```tsx
{editedRingPts.length >= 6 && editLog.length > 0 && (
  <Line ... />  // amber-dashed overlay unchanged
)}
```

`BezierEditLayer` must subscribe to `editLog` from `useEditorStore`:

```tsx
const editLog = useEditorStore((s) => s.editLog)
```

The post-converge `temporal.pause(); useEditorStore.setState({ editLog: [] }); temporal.resume()`
sequence then naturally clears the overlay by clearing `editLog`.

**No color change required.** The overlay hex/dash/fill values remain `#f0c040`,
`dash=[6,4]`, `fill="rgba(240,192,64,0.12)"` — unchanged from Phase 08.1.

**Test migration (planner flag):** `frontend/tests/uat/playwright/08.1-bezier-render-feedback.spec.ts`
Describe 2, line 307 asserts `editedContourPointCount > 0` on a freshly-selected, unedited barony
(before any drag). With the `editLog.length > 0` guard, this assertion changes from "overlay always
present on Bézier select" to "overlay absent until first edit." The test must be updated:
- Remove the `before > 0` precondition check (or move it to after the first drag).
- The load-bearing assertion (lines 332-335: `snapshotBefore !== snapshotAfter`) requires that
  the overlay be present AFTER the drag — this is satisfied because the drag grows `editLog`.
- The precondition should be rewritten as: make one drag → assert overlay appears → capture
  `snapshotBefore` → make second drag → assert `snapshotAfter !== snapshotBefore`.
This is a Wave 0 migration item for the planner (one of the new files: `test_bezier_convergence.spec.ts`
may absorb the clear-on-converge assertion directly).

---

## Toolbar Layout (Phase 08.2 addition to Phase 08)

The existing 48px `WorkspaceToolbar` gains one new control group between `EditToolPalette`
and Undo/Redo. Full right-zone layout after this phase:

```
[BranchPicker ▼] [EditToolPalette] | [AUTO SWITCH] [Aplicar edições] | [↩] [↪] [Regenerar] [Exportar ZIP]
                                       ↑ new in 08.2 ↑
```

### Apply Edits Control Group

**Placement:** In the right-zone `Flex align="center" gap="2"`, inserted between `EditToolPalette`
and the Undo/Redo buttons.

**Conditional visibility:** The entire control group is rendered only when:
- `editableLayer === 'baronies'` AND
- `activeTerritoryId !== null` AND
- `activeTool === 'V'`

(Same predicate as Bézier mode, derived from the same store values already read by WorkspaceToolbar.)
The mode switch is visible even when `editLog.length === 0` — user must be able to pre-select
auto mode before making edits. The Apply button is enabled only when `editLog.length > 0`.

### Mode Toggle

The mode control is a Radix `Switch` with a `Text` label — NOT a RadioGroup (CONTEXT D-01 mirrors
D-05 semantics, but the toolbar is a horizontal 48px bar; a vertical RadioGroup does not fit).

**Rationale:** The D-05 landmask control uses `RadioGroup` in a sidebar card with vertical space.
Toolbar constraints require a more compact widget. A `Switch` labeled "Auto-aplicar" conveys the
same two-mode semantics (OFF = manual, ON = auto-immediate) with one element instead of two stacked
radio items. The semantic equivalence is preserved; the widget form adapts to the layout constraint.

```jsx
<Flex align="center" gap="2">
  <Switch
    data-testid="bezier-apply-auto-switch"
    checked={bezierApplyMode === 'auto-immediate'}
    onCheckedChange={(v) => setBezierApplyMode(v ? 'auto-immediate' : 'manual')}
    size="1"
  />
  <Text size="1" color="gray">Auto-aplicar</Text>
</Flex>
```

- Default: OFF (manual mode), switch unchecked — CONTEXT D-01 locks manual as default.
- When ON: orange Badge "Modo auto: ~3-5s por edição" rendered inline, immediately after
  the switch, before the Apply button position.

### Apply Button

Rendered only in manual mode (switch OFF). Enabled only when `editLog.length > 0`.

```jsx
{bezierApplyMode === 'manual' && (
  <Button
    variant="outline"
    color="blue"
    disabled={editLog.length === 0 || isRunning}
    onClick={handleApplyEdits}
    data-testid="bezier-apply-edits-btn"
  >
    Aplicar edições
  </Button>
)}
```

- `variant="outline"` — secondary action, parallels "Aplicar landmask" (D-05 discretion call
  from Phase 08 UI-SPEC §Landmask Editor Layer Header).
- `color="blue"` — consistent with the landmask Apply button.
- `disabled` when no pending edits (`editLog.length === 0`) or render already in progress
  (`isRunning = runState === 'generating' || runState === 'rendering' || runState === 'ingesting'`).

### In-Flight State (during Apply cascade)

The Apply cascade reuses the Phase 04.1 render UX exactly (D-02). No new progress UI is needed:

- The existing `GenerateStatusBadge` (center toolbar) shows rendering progress as today.
- The existing `showCancel = runState === 'rendering'` gate replaces the badge with the red
  "Cancelar" button as today.
- `isRunning` disables the Apply button and the Gerar Mapa button simultaneously — no collision.

**There is no new spinner, no new progress bar, no new cancel UI for Apply edits.** The entire
in-flight state is inherited from the existing rendering-state machine (RunState).

---

## Store Extension

One new store field is required to hold the apply mode across re-renders.

```typescript
// Add to useEditorStore state (targeted addition)
bezierApplyMode: 'manual' | 'auto-immediate'   // default: 'manual'
setBezierApplyMode: (mode: 'manual' | 'auto-immediate') => void
```

This field is NOT part of the zundo `temporal` history (partialize must exclude it).
It is a UI preference, not an edit op.

---

## Copywriting Contract

### Primary CTA (new in 08.2)

| Element | Copy | Widget | Variant |
|---------|------|--------|---------|
| Manual apply convergence trigger | "Aplicar edições" | `Button` | `color="blue" variant="outline"` |
| Auto-immediate mode label | "Auto-aplicar" | `Text` beside `Switch` | `size="1" color="gray"` |
| Auto mode cost badge | "Modo auto: ~3-5s por edição" | `Badge` | `color="orange" variant="soft"` |
| Cancelar (in-flight, inherited) | "Cancelar" | `Button` (existing) | `color="red" variant="solid"` (inherited, unchanged) |

### Empty / Placeholder States

| Context | Copy | Mechanism |
|---------|------|-----------|
| No pending edits (editLog empty) | Apply button rendered disabled — no toast/label needed | `disabled={editLog.length === 0}` |
| No barony selected | Entire control group hidden — no message | Conditional render on Bézier mode predicate |

### Error States

| State | Copy | Widget |
|-------|------|--------|
| RENDER_BUSY retry exhausted (3 retries, inherited from 04.1) | Inherited from Phase 04.1 — no new copy | `useRunStore.finish('error', 'RENDER_BUSY')` existing path |
| Convergence render error (generic) | Inherited from Phase 04.1 RunState error path — no new copy | Existing GenerateStatusBadge error state |

### Destructive Actions

No destructive actions in this phase.

### Undo-after-converge behavior (executor note, not user-facing copy)

Ctrl+Z reverts `useEditorStore` geometry only (zundo is editor-scoped, Phase 08 D-25).
The colored map stays as last-rendered until the next Apply or auto-immediate cascade.
There is NO automatic re-cascade on undo. This is intentional (CONTEXT D-03 discretion).
No user-facing message is required — the existing undo Tooltip ("Desfazer Mover Vértice")
is sufficient. Executor must not add a "map not reverted" toast.

---

## Interaction Contract

### Manual mode (default)

1. User selects a barony in Bézier mode (V-tool + barony selected). Apply Edits control group
   appears in toolbar. Apply button is disabled (no edits yet). Overlay is HIDDEN (editLog empty).
2. User drags anchors or control handles in BezierEditLayer — commits via `op: 'move'`.
   `editLog` grows; Apply button becomes enabled; overlay appears (editLog > 0).
3. User clicks "Aplicar edições":
   a. Frontend posts snapshot with `{ ..., vertices, edit_log, barony_name_to_idx }` payload
      (snake_case `edit_log`, not camelCase).
   b. Frontend calls `postRender(projectId, {}, stageView)` directly (bypasses diffOverrides check).
   c. `useRunStore.startRender(run_id)` — existing rendering-state machine kicks in.
   d. Apply button disabled; GenerateStatusBadge shows rendering progress; Cancelar visible.
4. On render success (project `updated_at` bumped by backend):
   a. Parent refetches `project` → new `cacheVersion` → all 5 `useCanvasArtifacts` queries refetch.
   b. `baroniesQ` data updates → `BaronyLayer` re-renders with the converged colored raster.
   c. `temporal.pause(); useEditorStore.setState({ editLog: [] }); temporal.resume()`.
   d. `editLog` now empty → overlay hides → Apply button disables → only colored raster visible.
5. User remains in Bézier mode (barony still selected). Can make further edits → cycle repeats.

### Auto-immediate mode

1. User toggles the Auto-aplicar switch ON — orange cost badge appears.
2. Any prior edits (editLog non-empty): overlay already showing. First new drag commits immediately.
3. After each `setVerticesAndLog`:
   a. Frontend posts a fresh inline snapshot (with current `vertices` dict) — does NOT rely
      on the 25-edit auto-snapshot cadence (RESEARCH Target #5).
   b. Frontend calls `postRender(projectId, {}, stageView)` directly.
   c. Existing rendering-state machine handles progress + Cancelar.
4. Subsequent edits are queued or cancelled per the existing RENDER_BUSY bounded-retry logic
   (3 retries / 1.5s window, Phase 04.1).
5. After each render success: same `editLog` reset → overlay hides → colored raster converged.

### Visibility and enable state matrix

| Condition | Mode Switch | Apply Button | Badge | Overlay |
|-----------|-------------|--------------|-------|---------|
| Bézier mode inactive (no barony / not V-tool) | Hidden | Hidden | Hidden | Hidden |
| Bézier mode active, manual, editLog empty | Visible, OFF | Visible, disabled | Hidden | Hidden |
| Bézier mode active, manual, editLog non-empty | Visible, OFF | Visible, enabled | Hidden | Visible |
| Bézier mode active, auto-immediate, editLog empty | Visible, ON | Hidden | Visible | Hidden |
| Bézier mode active, auto-immediate, editLog non-empty | Visible, ON | Hidden | Visible | Visible |
| Any mode, rendering in progress (isRunning) | Visible | Hidden (auto) or disabled (manual) | Visible if auto | Visible (editLog pending) |

---

## Component Inventory (Phase 08.2 deltas)

### Files modified (targeted additions only)

| File | Change |
|------|--------|
| `frontend/src/components/workspace/WorkspaceToolbar.tsx` | Add Apply Edits control group (Switch + conditional Badge + conditional Apply button) in the right-zone Flex, between EditToolPalette and Undo/Redo. Read `bezierApplyMode`, `editLog.length` from useEditorStore. |
| `frontend/src/stores/useEditorStore.ts` | Add `bezierApplyMode` field (default: 'manual') + `setBezierApplyMode` action. Exclude from zundo `partialize`. |
| `frontend/src/components/canvas/BezierEditLayer.tsx` | Add `editLog.length > 0` guard on the overlay render (line 870). Subscribe to `editLog` from useEditorStore. This is the clear-on-converge mechanism (CONTEXT discretion). |
| `frontend/tests/uat/playwright/08.1-bezier-render-feedback.spec.ts` | Migrate Describe 2 precondition: remove "overlay present before any drag" assertion (line 307). Capture `snapshotBefore` after the first drag (not before). Load-bearing assertion (snapshotAfter != snapshotBefore) is preserved. |

### New files (may be created by planner)

| File | Description |
|------|-------------|
| `frontend/src/hooks/useBezierApply.ts` | Optional: extract Apply snapshot POST + postRender call into a dedicated hook if WorkspaceToolbar grows beyond ~250 LOC. Planner decides. |

### Files NOT modified

| File | Why untouched |
|------|---------------|
| `frontend/src/components/editor/LandmaskEditorHeader.tsx` | Landmask apply path unchanged. |
| `frontend/src/components/editor/EditToolPalette.tsx` | No changes — it remains Bézier-apply-unaware. |
| `frontend/src/hooks/useParameterStudioDispatch.ts` | No changes. Phase 08.2 calls `postRender` directly, not via the dispatch hook's diffOverrides path. |
| `backend/**` | Backend changes (manual_edit.compute() + _render_producer wiring) are specified in RESEARCH.md and PLAN. This UI-SPEC covers frontend contract only. |

---

## Konva Layer Stack

Unchanged from Phase 08.1. No new Konva layers in this phase.

```
z=0 BackgroundLayer    — terrain PNG / stage raster
z=1 TerritoryLayer     — condado polygons (read-only selection)
z=2 BaronyLayer        — barony polygons (updates on render success)
z=3 DecorationsLayer   — capitals + labels
z=4 InteractionLayer   — gold selection outlines (listening=false)
z=5 BezierEditLayer    — Bézier anchors/handles/curve overlay + amber-dashed overlay [Phase 08.1]
```

The amber-dashed live overlay (`data-testid="bezier-edited-contour"`) remains in `BezierEditLayer`.
This phase adds one render guard to it — no structural change to the layer or its Konva shapes.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — project uses Radix UI Themes |
| Third-party registries | none | not applicable |

No new npm packages are introduced. All components use existing Radix UI Themes primitives
already installed.

---

## Notes for Executor

1. **D-01: Mirror D-05 semantics, not widget.** The Apply button + mode control mirrors the
   Phase 08 D-05 landmask pattern semantically (manual default, auto-immediate toggle, outline Apply,
   orange cost badge) but uses a `Switch` instead of a `RadioGroup` because the toolbar has no vertical
   space for stacked radio items. The spec source for this decision is §Toolbar Layout above.

2. **Do NOT call `dispatch.flush()` or `dispatch()` from `useParameterStudioDispatch` for Apply edits.**
   The hook's `diffOverrides` check will short-circuit silently when no slider values changed.
   Call `postRender(projectId, {}, stageView)` directly. RENDER_BUSY retry: implement a simple
   per-projectId counter in the Apply handler (same 3-retry / 1.5s pattern as Phase 04.1).

3. **Snapshot payload extension.** The Apply snapshot must include:
   - `vertices`: the current `useEditorStore.getState().vertices` dict
   - `edit_log`: the current `useEditorStore.getState().editLog` (snake_case — NOT camelCase `editLog`)
   - `barony_name_to_idx`: built from the `effectiveBaronies` array in CanvasViewer context
   This is the non-standard extension identified in RESEARCH Target #5 (Pitfall 0 + Pitfall 5).

4. **Auto-immediate snapshot.** Each auto-immediate cascade must post a fresh snapshot before
   calling `postRender` — do NOT rely on the 25-edit auto-snapshot cadence. The 25-edit cadence
   uses `{ vertices, editLog }` (camelCase); the Apply snapshot must use `{ vertices, edit_log }`
   (snake_case) or `compute()` must normalize both (RESEARCH Pitfall 0, Option B).

5. **`bezierApplyMode` is NOT in zundo history.** Add it to the `partialize` exclusion list in
   `useEditorStore`. Undo must never change the mode toggle state.

6. **baroniesQ refetch is automatic.** No explicit `baroniesQ.refetch()` call is needed.
   After render success, `_render_producer` bumps `project.updated_at`. The parent's
   project query returns a new `cacheVersion`, which propagates via the `cacheVersion` prop
   to `useCanvasArtifacts`, triggering all 5 canvas queries including `baroniesQ`.
   (RESEARCH Target #5: "No additional baroniesQ.refetch() call needed.")

7. **Overlay clear mechanism: `editLog` guard, not selection dismiss.** Do NOT add
   `setActiveTerritoryId(null)` or `setBezierMode(false)` on Apply success. The user remains
   in Bézier mode after Apply — clearing the selection would disrupt the editing flow.
   The overlay clears by the `editLog.length > 0` guard reacting to the `editLog = []` reset.
   The user can immediately make further edits and the cycle repeats.

8. **`editLog` reset wraps zundo pause/resume.** The post-converge reset must use:
   ```typescript
   const temporal = useEditorStore.temporal.getState()
   temporal.pause()
   useEditorStore.setState({ editLog: [] })
   temporal.resume()
   ```
   This prevents the `editLog = []` write from appearing in the undo history as a separate op.

9. **Test migration is required.** `08.1-bezier-render-feedback.spec.ts` Describe 2 currently
   asserts the overlay is present on a freshly-selected, unedited barony. The new `editLog > 0`
   guard changes this: the overlay is ABSENT until the first drag. Update the test so the
   load-bearing geometry-change assertion (snapshotBefore vs snapshotAfter) runs after a drag,
   not from the pre-drag baseline. The migration changes the precondition only — the core
   regression guard (geometry changes after drag) is preserved.

10. **data-testid anchors for UAT.** The Playwright BEZ-CONV-05 spec needs:
    - `data-testid="bezier-apply-edits-btn"` (the Apply button — specified above)
    - `data-testid="bezier-apply-auto-switch"` (the mode switch — specified above)
    - `data-testid="bezier-edited-contour"` (the overlay — already exists in BezierEditLayer)
    - Assert overlay is absent before first edit (editLog empty) and present after first drag.
    - Assert overlay clears after Apply cascade completes.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
