---
phase: 08.2-bezier-edit-to-map-convergence-backend-replay
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - frontend/src/stores/useEditorStore.ts
  - frontend/src/hooks/useBezierApply.ts
  - frontend/src/components/workspace/WorkspaceToolbar.tsx
  - frontend/src/components/canvas/BezierEditLayer.tsx
  - frontend/src/stores/__tests__/useEditorStore.bezierApply.test.ts
  - frontend/src/hooks/__tests__/useBezierApply.test.ts
  - frontend/src/components/workspace/__tests__/WorkspaceToolbar.bezierApply.test.tsx
autonomous: true
requirements:
  - BEZ-CONV-03

must_haves:
  truths:
    - "An 'Aplicar edições' Button (blue outline, data-testid bezier-apply-edits-btn) renders in manual mode, enabled only when editLog.length > 0, disabled while isRunning."
    - "A Radix Switch 'Auto-aplicar' (data-testid bezier-apply-auto-switch) toggles bezierApplyMode; ON shows an orange Badge 'Modo auto: ~3-5s por edição'."
    - "Apply posts a snapshot with { vertices, edit_log (snake_case), ... } then calls postRender(projectId, {}, stageView) DIRECTLY — NEVER dispatch()/dispatch.flush() (Pitfall 3 — diffOverrides empty → silent no-op)."
    - "barony_name_to_idx is NOT built or sent by the frontend (constraint #5; backend builds it from bars). The Apply payload carries only vertices + edit_log."
    - "On render success the overlay clears via temporal.pause(); setState({editLog:[]}); temporal.resume(); the editLog.length>0 guard on the BezierEditLayer overlay then hides it (clear-on-converge)."
    - "bezierApplyMode is a UI preference EXCLUDED from zundo partialize — undo never changes it."
    - "baroniesQ refetch is automatic via cacheVersion (project.updated_at bump) — NO explicit baroniesQ.refetch() call added."
  artifacts:
    - path: "frontend/src/stores/useEditorStore.ts"
      provides: "bezierApplyMode field (default 'manual') + setBezierApplyMode action; excluded from partialize"
      contains: "bezierApplyMode"
    - path: "frontend/src/hooks/useBezierApply.ts"
      provides: "handleApplyEdits: POST snapshot (vertices + edit_log snake_case) then direct postRender + RENDER_BUSY counter; overlay clear-on-success"
      contains: "postRender"
    - path: "frontend/src/components/workspace/WorkspaceToolbar.tsx"
      provides: "Apply Edits control group (Switch + conditional Badge + conditional Apply button) per UI-SPEC visibility predicate"
      contains: "bezier-apply-edits-btn"
    - path: "frontend/src/components/canvas/BezierEditLayer.tsx"
      provides: "editLog.length > 0 guard on the amber overlay (clear-on-converge)"
      contains: "editLog.length > 0"
  key_links:
    - from: "WorkspaceToolbar Apply button onClick"
      to: "useBezierApply.handleApplyEdits"
      via: "POST snapshot then postRender(projectId, {}, stageView) direct"
      pattern: "postRender\\("
    - from: "useBezierApply render-success callback"
      to: "useEditorStore editLog reset"
      via: "temporal.pause(); setState({editLog:[]}); temporal.resume()"
      pattern: "temporal.pause"
    - from: "BezierEditLayer overlay"
      to: "useEditorStore.editLog"
      via: "editLog.length > 0 guard"
      pattern: "editLog.length > 0"
---

<objective>
Wire the frontend convergence trigger per UI-SPEC verbatim: the "Aplicar edições" Button + the
"Auto-aplicar" Switch + the orange cost Badge in WorkspaceToolbar, the snapshot-POST-then-direct-
postRender Apply flow, and the editLog>0 overlay clear-on-converge guard.

Purpose: This is the frontend half of BEZ-CONV-03. It must avoid the diffOverrides silent no-op
(Pitfall 3) by calling postRender directly, and must NOT build/send barony_name_to_idx (the backend
owns that mapping per constraint #5). baroniesQ refetch is automatic via cacheVersion.

Output: a working Apply control group + Apply flow + overlay clear-on-converge, fully unit-tested
in vitest. This plan is frontend-only and parallel-safe with Plan 02 (disjoint files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08.2-bezier-edit-to-map-convergence-backend-replay/08.2-UI-SPEC.md
@.planning/phases/08.2-bezier-edit-to-map-convergence-backend-replay/08.2-RESEARCH.md

<interfaces>
useParameterStudioDispatch (hooks/useParameterStudioDispatch.ts): exposes postRender +
RENDER_BUSY bounded-retry (3 retries / 1.5s window, per-projectId counter). DO NOT call
dispatch()/dispatch.flush() for Apply — diffOverrides({}, lastRendered) is empty → silent no-op
(Pitfall 3, RESEARCH §445-452). Call `postRender(projectId, {}, stageView)` directly then
`useRunStore.getState().startRender(run_id)`.

Apply flow (RESEARCH §Pattern 3, lines 602-628 — MINUS barony_name_to_idx per constraint #5):
```typescript
// 1. POST snapshot: body { trigger:'manual', payload: { geojson:{}, region_config:{},
//    edit_log: editLog, vertices } }   ← edit_log SNAKE_CASE (Pitfall 0). NO barony_name_to_idx.
// 2. const { run_id } = await postRender(projectId, {}, stageView)
// 3. useRunStore.getState().startRender(run_id)
```

Overlay clear (UI-SPEC Notes 7+8, RESEARCH Open Q#3):
```typescript
const temporal = useEditorStore.temporal.getState()
temporal.pause(); useEditorStore.setState({ editLog: [] }); temporal.resume()
```

Switch + Button + Badge JSX: UI-SPEC §"Mode Toggle" (152-162), §"Apply Button" (172-183),
§"New badge color" (line 63). data-testids: bezier-apply-auto-switch, bezier-apply-edits-btn,
bezier-edited-contour (overlay already exists).

Visibility predicate (UI-SPEC §"Apply Edits Control Group" 133-140):
editableLayer === 'baronies' && activeTerritoryId !== null && activeTool === 'V'.
Mode switch always visible in predicate; Apply button enabled only when editLog.length > 0.

Store extension (UI-SPEC §"Store Extension" 210-216):
bezierApplyMode: 'manual' | 'auto-immediate' (default 'manual'); setBezierApplyMode(mode).
EXCLUDE from zundo partialize.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useEditorStore bezierApplyMode field + partialize exclusion</name>
  <read_first>
    - frontend/src/stores/useEditorStore.ts (FULL — the temporal/partialize config, the existing state shape, setVerticesAndLog, editLog)
    - 08.2-UI-SPEC.md §"Store Extension" (210-216) + Note 5 (388-389) — exclude from partialize
    - CLAUDE.md (zundo temporal + partialize is the locked contract)
  </read_first>
  <behavior>
    - Test: bezierApplyMode defaults to 'manual'.
    - Test: setBezierApplyMode('auto-immediate') updates the field.
    - Test: bezierApplyMode is NOT captured in zundo history — toggling it then undo() does not revert it (it is excluded from partialize).
  </behavior>
  <action>
    Add `bezierApplyMode: 'manual' | 'auto-immediate'` (default 'manual') and
    `setBezierApplyMode: (mode) => set({ bezierApplyMode: mode })` to useEditorStore. Add
    `bezierApplyMode` to the zundo `partialize` EXCLUSION (UI-SPEC Note 5 — undo must never change
    the mode). Author the vitest cases in useEditorStore.bezierApply.test.ts.
  </action>
  <acceptance_criteria>
    - `grep -n "bezierApplyMode" frontend/src/stores/useEditorStore.ts` returns hits in both the state shape and the partialize exclusion.
    - `cd frontend && npx vitest run src/stores/__tests__/useEditorStore.bezierApply.test.ts` → all pass, including the partialize-exclusion (undo does not revert mode) test.
  </acceptance_criteria>
  <done>bezierApplyMode field + action exist, default manual, excluded from zundo history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useBezierApply hook — snapshot POST + direct postRender + overlay clear-on-success</name>
  <read_first>
    - frontend/src/hooks/useParameterStudioDispatch.ts (FULL — postRender signature, RENDER_BUSY retry counter pattern, startRender call)
    - frontend/src/stores/useEditorStore.ts (vertices, editLog, temporal.getState())
    - frontend/src/components/canvas/CanvasViewer.tsx — how stageView + projectId + branchId are sourced; the snapshots POST endpoint shape (grep for `/snapshots` POST in frontend/src)
    - 08.2-RESEARCH.md §Pattern 3 (602-628), §Pitfall 0 (667-693), §Pitfall 3 (717-727), Open Q#3 (905-908)
    - 08.2-UI-SPEC.md Notes 2,3,4,6,7,8 (372-410)
  </read_first>
  <behavior>
    - Test: handleApplyEdits POSTs a snapshot whose payload.edit_log (SNAKE_CASE) === current editLog and payload.vertices === current vertices dict.
    - Test: handleApplyEdits does NOT include barony_name_to_idx in the payload (constraint #5 — backend builds it).
    - Test: handleApplyEdits calls postRender(projectId, {}, stageView) DIRECTLY (mock postRender; assert called) and does NOT call any dispatch/flush from useParameterStudioDispatch (Pitfall 3).
    - Test: on render-success callback, editLog is reset via temporal.pause→setState→resume (mock temporal; assert pause+resume called and editLog === []).
    - Test (auto-immediate path): a fresh snapshot is posted before each postRender (does NOT rely on 25-edit cadence — RESEARCH §492-497).
    - Test (input cap, T-08.2-01): if vertices key count exceeds MAX_VERTEX_KEYS, the Apply aborts/guards (do not POST an unbounded payload).
  </behavior>
  <action>
    Create frontend/src/hooks/useBezierApply.ts exporting `handleApplyEdits(projectId, branchId,
    stageView)` (manual) and an auto-immediate trigger. Implement RESEARCH Pattern 3 MINUS
    barony_name_to_idx (constraint #5):
    1. Read `{ vertices, editLog } = useEditorStore.getState()`.
    2. Cap guard: MAX_VERTEX_KEYS = nb * 200 (ASVS V5 / T-08.2-01); if `Object.keys(vertices).length
       > MAX_VERTEX_KEYS` abort (the snapshot is malformed). Derive nb from effectiveBaronies length
       if available, else a generous constant.
    3. POST `/api/v3/projects/${projectId}/branches/${branchId}/snapshots` with body
       `{ trigger:'manual', payload: { geojson:{}, region_config:{}, edit_log: editLog, vertices } }`
       — edit_log SNAKE_CASE (Pitfall 0). Do NOT add barony_name_to_idx.
    4. `const { run_id } = await postRender(projectId, {}, stageView)` then
       `useRunStore.getState().startRender(run_id)`. RENDER_BUSY: reuse the same per-projectId
       counter pattern as Phase 04.1 (3 retries / 1.5s). Do NOT route through diffOverrides.
    5. On render SUCCESS — PINNED TRIGGER POINT: subscribe to useRunStore and fire the reset on the
       `state` transition from `'rendering'` → `'generated'` (useRunStore.ts: success sets
       state:'generated'; 'error' is the failure terminal — do NOT reset on error). Use
       `useRunStore.subscribe` with a prev/next compare (only fire when prev==='rendering' &&
       next==='generated' && there was a pending Apply this hook initiated — guard with a ref so an
       unrelated parameter-studio render does not clear a user's in-progress edits). On that
       transition clear the overlay via
       `const t = useEditorStore.temporal.getState(); t.pause();
       useEditorStore.setState({ editLog: [] }); t.resume();` (UI-SPEC Notes 7,8).
       Do NOT call setActiveTerritoryId(null) or setBezierMode(false) (UI-SPEC Note 7 — user stays
       in Bézier mode). The baroniesQ refetch is automatic (cacheVersion bump) — the editLog reset
       here is ONLY the overlay-clear, decoupled from the data refetch.
    Auto-immediate: same flow but triggered after each setVerticesAndLog when
    bezierApplyMode==='auto-immediate', posting a fresh inline snapshot each time.
    Author useBezierApply.test.ts covering all behaviours above (mock fetch, postRender, temporal).
  </action>
  <acceptance_criteria>
    - `grep -n "edit_log" frontend/src/hooks/useBezierApply.ts` returns a hit (snake_case in payload); `grep -c "editLog:" frontend/src/hooks/useBezierApply.ts` → 0 (no camelCase key in the POST payload).
    - `grep -c "barony_name_to_idx" frontend/src/hooks/useBezierApply.ts` → 0 (constraint #5 — frontend never sends it).
    - `grep -n "postRender(" frontend/src/hooks/useBezierApply.ts` returns a hit; `grep -c "dispatch.flush\|diffOverrides" frontend/src/hooks/useBezierApply.ts` → 0 (Pitfall 3).
    - `grep -n "temporal" frontend/src/hooks/useBezierApply.ts` shows pause/resume around the editLog reset.
    - `cd frontend && npx vitest run src/hooks/__tests__/useBezierApply.test.ts` → all pass.
  </acceptance_criteria>
  <done>Apply hook posts snake_case snapshot (no barony_name_to_idx), calls postRender directly, clears overlay on success via paused editLog reset; auto-immediate posts fresh snapshot each edit.</done>
</task>

<task type="auto">
  <name>Task 3: WorkspaceToolbar Apply control group + BezierEditLayer overlay editLog>0 guard</name>
  <read_first>
    - frontend/src/components/workspace/WorkspaceToolbar.tsx (FULL — the right-zone Flex layout lines 168-216; how editableLayer/activeTerritoryId/activeTool/isRunning are derived from stores)
    - frontend/src/components/canvas/BezierEditLayer.tsx — the overlay render at line ~870 (`editedRingPts.length >= 6` guard) and how it subscribes to the store
    - 08.2-UI-SPEC.md §"Toolbar Layout" (118-203), §"Overlay-clear-on-converge" (68-102), §Copywriting (222-256), Note 10 (419-424), §"Visibility and enable state matrix" (294-303)
  </read_first>
  <action>
    1. WorkspaceToolbar: insert the Apply Edits control group in the right-zone
       `Flex align="center" gap="2"` BETWEEN EditToolPalette and Undo/Redo. Render the whole group
       only when `editableLayer === 'baronies' && activeTerritoryId !== null && activeTool === 'V'`.
       - Switch (UI-SPEC 152-162): data-testid `bezier-apply-auto-switch`, checked when
         bezierApplyMode==='auto-immediate', onCheckedChange toggles via setBezierApplyMode, size="1",
         Text "Auto-aplicar" size="1" color="gray". Switch always visible in predicate.
       - When auto: orange Badge `color="orange" variant="soft"` copy "Modo auto: ~3-5s por edição"
         inline after the switch.
       - Apply Button (UI-SPEC 172-183), rendered only in manual mode: variant="outline" color="blue"
         data-testid `bezier-apply-edits-btn` copy "Aplicar edições", disabled when
         `editLog.length === 0 || isRunning`, onClick → useBezierApply.handleApplyEdits.
       All copy is pt-BR verbatim from UI-SPEC. Read bezierApplyMode + editLog.length from
       useEditorStore. If WorkspaceToolbar would exceed ~250 LOC, the Apply group may be extracted to
       a small subcomponent (UI-SPEC New Files note) — planner-permitted.
    2. BezierEditLayer: add `editLog.length > 0` to the overlay render guard (UI-SPEC 83-99):
       change `{editedRingPts.length >= 6 && (` to
       `{editedRingPts.length >= 6 && editLog.length > 0 && (`. Subscribe via
       `const editLog = useEditorStore((s) => s.editLog)`. Overlay hex/dash/fill unchanged
       (#f0c040, dash=[6,4], fill rgba(240,192,64,0.12)). Keep data-testid bezier-edited-contour.
    Author WorkspaceToolbar.bezierApply.test.tsx covering the visibility matrix (UI-SPEC 294-303):
    button hidden outside Bézier mode; disabled when editLog empty; switch toggles mode; badge shows
    only in auto mode; clicking Apply calls handleApplyEdits.
  </action>
  <acceptance_criteria>
    - `grep -n "bezier-apply-edits-btn\|bezier-apply-auto-switch" frontend/src/components/workspace/WorkspaceToolbar.tsx` returns both anchors (or in the extracted subcomponent).
    - `grep -n "Aplicar edições\|Auto-aplicar\|Modo auto: ~3-5s por edição" frontend/src/components/workspace/WorkspaceToolbar.tsx` returns the three pt-BR copy strings verbatim.
    - `grep -n "editLog.length > 0" frontend/src/components/canvas/BezierEditLayer.tsx` returns a hit on the overlay guard.
    - `cd frontend && npx vitest run src/components/workspace/__tests__/WorkspaceToolbar.bezierApply.test.tsx` → all pass (visibility matrix).
    - `cd frontend && npm run test` → full vitest suite green; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Apply control group renders per UI-SPEC visibility matrix with verbatim pt-BR copy; overlay guarded by editLog>0 (clear-on-converge); full vitest + tsc green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Apply snapshot POST → backend | `vertices` dict is the geometry the backend rasterises; client-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.2-01 | Denial of Service | Apply `vertices` payload | mitigate | useBezierApply caps at MAX_VERTEX_KEYS = nb*200 before POST (ASVS V5, RESEARCH §Security 913-918); oversized payload aborts. |
| T-08.2-05 | Tampering | edit_log key casing | mitigate | snake_case `edit_log` enforced in the payload (Pitfall 0) + backend belt-and-suspenders normalize (Plan 02). |
</threat_model>

<verification>
- `cd frontend && npm run test` → full vitest suite green (store + hook + toolbar bezierApply tests pass).
- `cd frontend && npx tsc --noEmit` → clean.
- Manual visibility-matrix smoke against UI-SPEC §294-303 covered by WorkspaceToolbar.bezierApply.test.tsx.
</verification>

<success_criteria>
- Apply button (blue outline) + Switch ("Auto-aplicar") + orange cost Badge render per UI-SPEC visibility matrix with verbatim pt-BR copy.
- Apply posts snake_case edit_log + vertices, NO barony_name_to_idx, calls postRender directly (Pitfall 3 avoided).
- Overlay clears on converge via paused editLog reset (editLog>0 guard).
- bezierApplyMode excluded from zundo; baroniesQ refetch automatic (no explicit refetch call).
</success_criteria>

<output>
After completion, create `.planning/phases/08.2-bezier-edit-to-map-convergence-backend-replay/08.2-03-SUMMARY.md`
</output>
