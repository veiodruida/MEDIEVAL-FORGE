---
phase: 08.2-bezier-edit-to-map-convergence-backend-replay
plan: 04
type: execute
wave: 2
depends_on: [02, 03]
files_modified:
  - frontend/tests/uat/playwright/08.2-bezier-convergence.spec.ts
autonomous: false
requirements:
  - BEZ-CONV-05

must_haves:
  truths:
    - "A real-mouse Bézier drag on a NAMED COASTLINE barony, followed by 'Aplicar edições', makes the colored BaronyLayer boundary pixels CHANGE (not just 'render ran')."
    - "The amber overlay is ABSENT before the first edit (editLog empty) and PRESENT after the first drag; it CLEARS after the Apply cascade completes (clear-on-converge)."
    - "The edited boundary SURVIVES a full page reload (persisted to the branch, reloaded from the colored raster)."
    - "The edited boundary APPEARS IN THE 12-file Unity export (lookup_barony.png reflects the change)."
    - "The target is a coastline barony with an open boundary (no interior neighbour to gap) — interior shared edges gap by construction (RESEARCH A3); a generic/interior target would fail."
  artifacts:
    - path: "frontend/tests/uat/playwright/08.2-bezier-convergence.spec.ts"
      provides: "BEZ-CONV-05 end-to-end Playwright scenario, un-skipped, naming the coastline barony"
      contains: "bezier-apply-edits-btn"
  key_links:
    - from: "Playwright real page.mouse drag"
      to: "BezierEditLayer op:'move' commit"
      via: "drag an anchor at a usable zoom (08.1-07 screen-space sizing; NO __forgeBezierTrigger hooks)"
      pattern: "page.mouse"
    - from: "Apply button click"
      to: "colored BaronyLayer boundary pixel change"
      via: "snapshot POST → /render cascade → cacheVersion → baroniesQ refetch"
      pattern: "bezier-apply-edits-btn"
---

<objective>
Author and run the BEZ-CONV-05 UAT: a real-mouse Bézier edit on a coastline barony that converges
to the colored map, survives reload, and appears in the export. This is the human-facing G8-closed
proof — the END-TO-END criterion that per-wire tests cannot establish.

Purpose: G8 was confirmed in-browser (08.1-HUMAN-UAT). Closing it requires a real-mouse UAT, not a
unit test. The target MUST be a coastline barony (RESEARCH A3 — interior shared edges gap by
construction in this phase's scope).

Output: 08.2-bezier-convergence.spec.ts un-skipped and green; a human-verify checkpoint confirms no
ghost overlay and the boundary visibly moves.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08.2-bezier-edit-to-map-convergence-backend-replay/08.2-VALIDATION.md
@.planning/phases/08.2-bezier-edit-to-map-convergence-backend-replay/08.2-UI-SPEC.md
@.planning/phases/08.2-bezier-edit-to-map-convergence-backend-replay/08.2-RESEARCH.md

<interfaces>
The Plan 01 scaffold already names the coastline barony + the three data-testids
(bezier-apply-edits-btn, bezier-apply-auto-switch, bezier-edited-contour). This plan fleshes out
the assertions and un-skips it.

Real-mouse drag pattern (08.1-07 established): screen-space sizing means anchors are usable at
zoom; use page.mouse.move/down/up at a usable zoom. BANNED: __forgeBezierTriggerDrag / any
__forgeBezier* test hook (08.1-07 mandate — real mouse only).

Convergence chain (RESEARCH §454-468): Apply → snapshot POST → postRender → on success
_render_producer bumps project.updated_at → cacheVersion changes → all 5 useCanvasArtifacts
queries (incl. baroniesQ) refetch → BaronyLayer re-renders. NO explicit refetch needed.

Server-restart-before-UAT (user memory): shutdown + build + restart backend AND frontend before
running the spec / asking for human verify.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flesh out + un-skip the BEZ-CONV-05 coastline UAT spec</name>
  <read_first>
    - frontend/tests/uat/playwright/08.2-bezier-convergence.spec.ts (the Plan 01 scaffold — coastline barony name + data-testids + TODO steps)
    - frontend/tests/uat/playwright/08.1-bezier-render-feedback.spec.ts (the real-mouse drag harness + page setup + how the barony is selected; reuse the coastline-barony selection approach)
    - frontend/tests/uat/playwright/ — any spec that asserts on lookup_barony.png / export ZIP contents (grep `lookup_barony` and `export` under frontend/tests) for the export-assertion harness
    - 08.2-UI-SPEC.md §"Interaction Contract" Manual mode (262-279) + Note 10 (419-424)
    - 08.2-RESEARCH.md §A3 (884-889) coastline scoping; §Target #5 baroniesQ (454-468)
  </read_first>
  <action>
    Remove the skip from 08.2-bezier-convergence.spec.ts and implement the four assertions against
    the NAMED COASTLINE barony (keep the comment `// COASTLINE barony — RESEARCH A3`). Use REAL
    page.mouse (NO __forgeBezier* hooks, 08.1-07 mandate):

    1. Load the project workspace, enter Bézier mode (V-tool), select the coastline barony at a
       usable zoom. Assert the overlay (data-testid bezier-edited-contour) is ABSENT (editLog empty,
       Plan 03 guard).
    2. Capture the colored BaronyLayer boundary BEFORE: snapshot the barony's boundary pixels (reuse
       the geometry-snapshot discriminator pattern from 08.1; or read lookup_barony.png region).
    3. Real-mouse drag an anchor (page.mouse.move/down → move → up). Assert overlay now PRESENT
       (editLog>0) and the Apply button (bezier-apply-edits-btn) is enabled.
    4. Click "Aplicar edições". Wait for the render cascade to finish (RunState back to idle /
       GenerateStatusBadge done). Assert: (a) the colored boundary AFTER differs from BEFORE
       (END-TO-END pixel change — the load-bearing criterion), and (b) the overlay CLEARS
       (bezier-edited-contour absent again — clear-on-converge).
    5. Reload the page (page.reload()). Re-select the barony. Assert the colored boundary still
       reflects the edit (SURVIVES reload).
    6. Trigger export (Exportar ZIP) and assert lookup_barony.png in the export reflects the changed
       boundary (APPEARS IN EXPORT). Reuse the export-assertion harness found in read_first; if the
       only feasible check is that the export endpoint returns a ZIP whose lookup differs from a
       zero-edit baseline, assert that.

    Per user memory: shutdown + build + restart BOTH servers before running the spec.
  </action>
  <acceptance_criteria>
    - `grep -c "test.skip\|test.describe.skip" frontend/tests/uat/playwright/08.2-bezier-convergence.spec.ts` → 0 (un-skipped).
    - Spec contains the named coastline barony string, `page.mouse`, and `grep -c "__forgeBezier" ...` → 0 (real mouse only).
    - Spec asserts boundary AFTER !== BEFORE, overlay absent→present→absent, reload persistence, and an export-contains-change assertion.
    - `cd frontend && npx playwright test tests/uat/playwright/08.2-bezier-convergence.spec.ts` → green.
  </acceptance_criteria>
  <done>The coastline UAT runs with real mouse, proves boundary change + overlay clear + reload survival + export presence; spec green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — colored boundary moves + overlay clears + survives reload</name>
  <action>After servers are rebuilt + restarted, the user manually verifies the convergence flow in the browser per the steps below; this checkpoint blocks until the user approves.</action>
  <what-built>
    The full convergence flow: backend move/add/delete replay (Plan 02) + frontend Apply control
    group + clear-on-converge overlay (Plan 03), verified end-to-end by the BEZ-CONV-05 Playwright
    spec on a coastline barony.
  </what-built>
  <how-to-verify>
    Servers were shutdown + rebuilt + restarted (per project rule). Then:
    1. Open the project workspace in the browser, enter Bézier mode, select the SAME coastline
       barony the spec targets.
    2. Confirm: no amber overlay before editing.
    3. Drag an anchor with the mouse. Confirm: amber dashed overlay appears; "Aplicar edições" button
       (blue outline) enables.
    4. Click "Aplicar edições". Confirm: rendering progress shows (GenerateStatusBadge / Cancelar),
       then the COLORED barony boundary visibly moves to match your edit, AND the amber overlay
       disappears (no doubled/ghost contour).
    5. Reload the page, re-select the barony. Confirm: the colored boundary still reflects the edit.
    6. (Optional) Toggle "Auto-aplicar" ON, confirm the orange "Modo auto: ~3-5s por edição" badge
       appears and a single drag triggers a cascade automatically.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issue (ghost overlay, boundary did not move, did not survive reload, etc.)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary introduced in this plan (test + human verification only). The vertices
payload cap (T-08.2-01) is enforced in Plan 03's useBezierApply.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.2-06 | (verification) | end-to-end convergence | mitigate | BEZ-CONV-05 UAT asserts pixel-level boundary change on real-mouse edit — the END-TO-END criterion that catches a silent NO-OP at any of the three failure points. |
</threat_model>

<verification>
- `cd frontend && npx playwright test tests/uat/playwright/08.2-bezier-convergence.spec.ts` → green.
- `cd backend && python -m pytest tests/parity/ -x -q` → Iberia 10/10 STILL green (BEZ-CONV-04 — zero-edit path untouched).
- `cd backend && python -m pytest tests/ -q && cd ../frontend && npm run test` → full suite green.
- Human-verify checkpoint approved (visual: boundary moves, overlay clears, survives reload).
</verification>

<success_criteria>
- Real-mouse Bézier edit on a coastline barony → colored boundary changes (END-TO-END pixel diff).
- Overlay absent→present→absent (clear-on-converge); edit survives reload; appears in 12-file export.
- Iberia parity 10/10 unchanged; full suite green; human-verify approved.
</success_criteria>

<output>
After completion, create `.planning/phases/08.2-bezier-edit-to-map-convergence-backend-replay/08.2-04-SUMMARY.md`
</output>
