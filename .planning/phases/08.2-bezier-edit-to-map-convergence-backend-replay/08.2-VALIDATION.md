---
phase: 08.2
slug: bezier-edit-to-map-convergence-backend-replay
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-29
---

# Phase 08.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `08.2-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.x (backend) + vitest 3.x (frontend) + Playwright (UAT) |
| **Config file** | `backend/pyproject.toml`, `frontend/vitest.config.ts`, `frontend/playwright.config.ts` |
| **Quick run command** | `cd backend && python -m pytest tests/unit/ -x -q` |
| **Full suite command** | `cd backend && python -m pytest tests/ -x -q && cd ../frontend && npm run test` |
| **Estimated runtime** | ~30s unit; parity + frontend ~2-3 min |

---

## Sampling Rate

- **After every task commit:** Run `cd backend && python -m pytest tests/unit/ -x -q`
- **After every plan wave:** Run `cd backend && python -m pytest tests/parity/ -x -q && cd ../frontend && npm run test`
- **Before `/gsd-verify-work`:** Full suite green + Iberia 10/10 parity + Bézier-convergence UAT spec green
- **Max feedback latency:** 30 seconds (unit tier)

---

## Per-Task Verification Map

> Planner populates concrete `{N}-NN-NN` task IDs once PLAN.md files exist. Requirement → test
> mapping below is the authoritative spine (from RESEARCH § Validation Architecture).

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| (tbd) | — | 0 | BEZ-CONV-04 | — | zero-edit path byte-identical (identity round-trip) | unit/parity | `pytest tests/unit/test_manual_edit_vertex_replay.py -x` + `pytest tests/parity/ -x -q` | ❌ W0 | ⬜ pending |
| (tbd) | — | 1 | BEZ-CONV-01 | — | `move` op → ring re-rasterise; pixel mask differs from identity | unit | `pytest tests/unit/test_manual_edit_vertex_replay.py -x` | ❌ W0 | ⬜ pending |
| (tbd) | — | 1 | BEZ-CONV-02 | — | `add`/`delete`/`multi_delete` → same ring re-rasterise path | unit | `pytest tests/unit/test_manual_edit_vertex_replay.py -x` | ❌ W0 | ⬜ pending |
| (tbd) | — | 1 | BEZ-CONV-03 | — | `_render_producer` fetches branch hash + injects snapshot_loader (by-value capture) | unit (mock DB) | `pytest tests/unit/test_render_producer_loader_injection.py -x` | ❌ W0 | ⬜ pending |
| (tbd) | — | 2 | BEZ-CONV-05 | input cap on `vertices` payload | real Bézier edit → colored boundary changes + survives reload + appears in export (coastline barony) | UAT | `npx playwright test tests/uat/test_bezier_convergence.spec.ts` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `backend/tests/unit/test_manual_edit_vertex_replay.py` — BEZ-CONV-01/02 + **round-trip identity test** (rasterise unedited barony's `geo_to_pixel` ring → byte-equal to existing `result == bi` mask). This identity test is the BEZ-CONV-04 prerequisite (de-risks assumption A1) and MUST pass before any replay task lands.
- [ ] `backend/tests/unit/test_render_producer_loader_injection.py` — BEZ-CONV-03 (mock DB; assert `cfg.manual_edit_log_hash`/`count` set and `cfg.snapshot_loader` returns captured data, not None).
- [ ] `frontend/tests/uat/test_bezier_convergence.spec.ts` — BEZ-CONV-05 Playwright scenario, scoped to a **coastline barony** (open boundary, no interior-gap risk per RESEARCH A3).
- [ ] Existing `tests/parity/` Iberia suite covers BEZ-CONV-04 zero-edit path (no new file).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Visual confirmation the colored `BaronyLayer` boundary visibly moves after Apply | BEZ-CONV-05 | Pixel-diff is automated in UAT, but the human "it looks right / no ghost overlay" check is visual | Real-mouse drag a coastline barony anchor → click "Apply edits" → confirm colored boundary shifts and amber overlay clears on converge |

*Playwright UAT automates the boundary-changed + survives-reload + appears-in-export assertions; the residual manual check is the visual quality gate.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (3 new test files above)
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
