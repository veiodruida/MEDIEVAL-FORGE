# Phase 08.2: Bézier edit-to-map convergence — backend replay — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-29
**Phase:** 08.2-bezier-edit-to-map-convergence-backend-replay
**Areas discussed:** Convergence trigger + cost

---

## Gray Area Selection

Presented 4 candidate gray areas; user selected **1** (Convergence trigger + cost).
Overlay lifecycle and undo-after-converge were offered but the user chose to capture
context with recorded defaults instead. The replay coordinate/vertexID mapping was
explicitly routed to research (not a user-vision question).

| Candidate area | Selected |
|----------------|----------|
| Convergence trigger + cost | ✓ |
| Live overlay lifecycle | (recorded default: clear-on-converge) |
| Undo after converge | (recorded default: revert store only) |
| Convergence scope confirm | (locked by BEZ-CONV-05) |

---

## Convergence trigger + cost

### Q1 — How does a Bézier contour edit reach the colored map + export?

| Option | Description | Selected |
|--------|-------------|----------|
| Mirror D-05 two-mode | Explicit "Apply edits" button (default, batches) + auto-immediate toggle. Same pattern as Phase 08 landmask. | ✓ |
| Apply button only | Single mode, button flushes cascade, no toggle. | |
| Auto-immediate only | Every commit cascades automatically (~3-5s each). | |
| Reuse "Generate Map" | No new button; Generate is the only trigger. | |

**User's choice:** Mirror D-05 two-mode.
**Notes:** Consistency with the landmask two-mode UX the user already chose in Phase 08.

### Q2 — During the ~3-5s convergence cascade, what UX?

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse Phase 04.1 render UX | Cancellable /render: progress, Cancelar, RENDER_BUSY retry, baroniesQ auto-refetch on success. | ✓ |
| Simple blocking spinner | Disable editor + spinner, no cancel. | |
| Fire-and-forget | Kick cascade, keep editing, refetch when it lands. | |

**User's choice:** Reuse Phase 04.1 render UX.
**Notes:** The cancel/race/RENDER_BUSY hardening is already paid for in Phase 04.1; reusing
it satisfies the BEZ-CONV-03 baroniesQ-refetch half for free.

---

## Claude's Discretion

- **Live overlay lifecycle** — default clear-on-converge (avoid ghost/doubled contour).
- **Undo after converge** — Ctrl+Z reverts useEditorStore only; no auto re-cascade-back.
- **Apply button styling/placement + toggle copy** — UI-phase / planner.

## Deferred Ideas

- Automatic re-cascade-back on undo.
- Selective per-barony incremental render (Phase 08 D-21 keeps full re-render).
- Fancier in-flight overlay transition during auto-immediate cascade.
