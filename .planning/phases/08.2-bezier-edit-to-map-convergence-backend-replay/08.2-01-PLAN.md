---
phase: 08.2-bezier-edit-to-map-convergence-backend-replay
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/unit/test_manual_edit_vertex_replay.py
  - backend/tests/unit/test_render_producer_loader_injection.py
  - backend/tests/integration/test_bezier_convergence_e2e.py
  - frontend/tests/uat/playwright/08.2-bezier-convergence.spec.ts
  - frontend/tests/uat/playwright/08.1-bezier-render-feedback.spec.ts
autonomous: true
requirements:
  - BEZ-CONV-01
  - BEZ-CONV-02
  - BEZ-CONV-03
  - BEZ-CONV-04
  - BEZ-CONV-05

must_haves:
  truths:
    - "A round-trip identity test exists and PASSES today: rasterising an unedited barony's geo_to_pixel ring is byte-equal to the existing result==bi mask (de-risks assumption A1 BEFORE any replay code)."
    - "A landmask-only parity guard test exists: a branch with non-empty manual_edit_log_hash but ZERO vertex ops returns byte-identical from compute() (the discriminating BEZ-CONV-04 test — distinct from the zero-edit Iberia parity run)."
    - "A failing end-to-end integration test exists that feeds a snapshot in the REAL wire format (edit_log snake_case + vertices dict) through the real _render_producer loader injection into the real compute(), asserting the edited barony raster DIFFERS from baseline."
    - "Per-wire unit + mock-DB test scaffolds exist (skip-marked where they depend on Wave 1 production code) so Wave 1 has red→green targets."
    - "The 08.1 overlay spec Describe 2 precondition is migrated so the editLog>0 overlay guard (added in Plan 03) does not break it."
  artifacts:
    - path: "backend/tests/unit/test_manual_edit_vertex_replay.py"
      provides: "round-trip identity test (passes now) + vertex-replay unit tests (skip until Wave 1) + landmask-only parity guard test"
      contains: "def test_round_trip_identity"
    - path: "backend/tests/unit/test_render_producer_loader_injection.py"
      provides: "mock-DB test that _render_producer sets cfg.manual_edit_log_hash/count and cfg.snapshot_loader returns captured data (skip until Wave 1)"
      contains: "snapshot_loader"
    - path: "backend/tests/integration/test_bezier_convergence_e2e.py"
      provides: "failing end-to-end test using REAL wire format; the load-bearing red→green gate for Plan 02"
      contains: "edit_log"
    - path: "frontend/tests/uat/playwright/08.2-bezier-convergence.spec.ts"
      provides: "BEZ-CONV-05 UAT scaffold scoped to a named COASTLINE barony (skip until Wave 2)"
      contains: "coastline"
  key_links:
    - from: "test_round_trip_identity"
      to: "contracts.geo_to_pixel + rasterio.features.rasterize"
      via: "rasterise unedited ring, compare to result==bi mask"
      pattern: "geo_to_pixel"
    - from: "test_bezier_convergence_e2e"
      to: "render._render_producer + manual_edit.compute"
      via: "real snapshot dict with edit_log snake_case key"
      pattern: "edit_log"
---

<objective>
Author ALL Wave 0 test scaffolds BEFORE any production replay/wiring code, per the Nyquist
validation contract (08.2-VALIDATION.md) and critical constraint #2 (round-trip identity gates
everything).

Purpose: The feature silently NO-OPs at three independent points even when every wire looks
present. Per-wire tests can pass while the feature ships G8 unfixed. This plan creates the
END-TO-END and identity-gate tests that make the silent failures loud, plus the per-wire
scaffolds Wave 1/2 will turn green.

Output:
- backend/tests/unit/test_manual_edit_vertex_replay.py (round-trip identity PASSES today;
  replay + landmask-only-guard tests skip-marked until Wave 1)
- backend/tests/unit/test_render_producer_loader_injection.py (mock-DB, skip until Wave 1)
- backend/tests/integration/test_bezier_convergence_e2e.py (REAL wire format, fails until Wave 1+2)
- frontend/tests/uat/playwright/08.2-bezier-convergence.spec.ts (coastline barony, skip until Wave 2)
- 08.1-bezier-render-feedback.spec.ts Describe 2 precondition migration
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.2-bezier-edit-to-map-convergence-backend-replay/08.2-RESEARCH.md
@.planning/phases/08.2-bezier-edit-to-map-convergence-backend-replay/08.2-VALIDATION.md
@.planning/phases/08.2-bezier-edit-to-map-convergence-backend-replay/08.2-UI-SPEC.md

<interfaces>
<!-- Extracted from live code — executor needs no codebase exploration for signatures. -->

manual_edit.compute (backend/medieval_forge/services/pipeline/manual_edit.py:39):
```python
def compute(input_array: np.ndarray, cfg: RegionConfig) -> np.ndarray
# Wave 1 adds optional param: barony_name_to_idx: dict[str, int] | None = None
# Empty-log short-circuit: if not cfg.manual_edit_log_hash: return input_array  (line 62)
# Loader: snapshot = cfg.snapshot_loader(cfg.branch_id); edit_log = snapshot.get("edit_log", [])  (lines 75-76)
# Vectorise affine: transform = from_bounds(0, 0, W, H, W, H)  (line 85)
```

contracts.geo_to_pixel (backend/medieval_forge/services/pipeline/contracts.py:176):
```python
def geo_to_pixel(lon: float, lat: float, cfg: RegionConfig, w=None, h=None) -> Tuple[int,int]
# NOTE: lon FIRST, lat SECOND. Returns (px, py), py is row (y-downward), int() truncation.
```

Snapshot vertices key scheme (SelectionBridge.tsx:75-86, BezierEditLayer.tsx:565-572):
```
vertices["<baronyName>#<i>"] = { lat, lon }   # baronyName === bars[bi]["name"]
op shape (Bézier move): { op:'move', ts, vertexIds:[...] }  — NO lat/lon
op shape (raw add):     { op:'add', ts, vertexIds:[id], lat, lon }
op shape (raw delete):  { op:'multi_delete', ts, vertexIds:[ids] }  — NO lat/lon
```

barony ordering (CONFIRMED via export.py:73 `for bi, b in enumerate(bars)`):
territory_metadata.baronies[bi] order == bars[bi] raster index. barony_name_to_idx is
built BACKEND-SIDE from bars[]: {b["name"]: i for i, b in enumerate(bars)}.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend unit scaffold — round-trip identity (PASSES now) + replay + landmask-only parity guard</name>
  <read_first>
    - backend/medieval_forge/services/pipeline/manual_edit.py (compute() lines 39-158; the vectorise→rasterise roundtrip at lines 80-150; empty-log short-circuit line 62)
    - backend/medieval_forge/services/pipeline/contracts.py lines 176-187 (geo_to_pixel: lon first, py is row, int() truncation)
    - backend/tests/ — find an existing parity/unit test that builds a RegionConfig + Iberia raster fixture to reuse the fixture harness (grep for `result ==` and `RegionConfig(` in backend/tests)
    - backend/medieval_forge/services/canvas_sidecars.py — build_baronies_geojson_sidecar (lines 207-247): the geo ring representation the frontend stores as vertices (THE A1 source-of-truth representation)
    - inicio/territory_data_v3.py — pick a real COASTLINE Iberia barony for the A1 ring (open boundary)
    - 08.2-RESEARCH.md §"Round-Trip Identity Test" (lines 854-859), §A1 (882, 887-889), §Pitfall 4 (728-743), §Pitfall 6 (755-765)
  </read_first>
  <action>
    Create backend/tests/unit/test_manual_edit_vertex_replay.py with THREE test groups. Use
    descriptive names and explicit numeric fixtures (user memory: descriptive tests + explicit
    numbers).

    GROUP A — `test_round_trip_identity_unedited_real_iberia_barony_ring_is_byte_equal_to_raster_mask`
    (MUST PASS TODAY — this is the A1 go/no-go gate, NOT skip-marked):

    CRITICAL: Use a REAL Iberia barony ring, NOT a synthetic rectangle. An axis-aligned rectangle
    round-trips through int()-truncating geo_to_pixel trivially and would PASS regardless of whether
    the assumption holds — defanging the gate. Constraint #2 mandates an UNEDITED barony's actual
    geo_to_pixel ring → byte-equal to result==bi. The risk A1 exists to catch is truncation /
    representation drift on a real DIAGONAL coastline staircase, which is exactly the ring the Plan 02
    replay path re-rasterises (including the non-edited portions of an edited barony's contour).
    - Generate (or load a cached fixture of) the real Iberia barony raster the pipeline produces
      (the same int16 array compute() receives from 'merge'). Pick a real coastline barony index bi
      and take `original = (arr == bi)` as the ground-truth mask.
    - Source the barony RING in the SAME representation SelectionBridge feeds into the snapshot
      `vertices` dict: load it from the actual `baronies.geojson` sidecar
      (services/canvas_sidecars.py output) — the exact geo ring the frontend stores as
      `<baronyName>#<i>` keys. FIRST determine whether that sidecar ring is the full pixel-boundary
      staircase or a simplified/curve-fit ring — record the finding in a test comment; this
      distinction is precisely what decides whether A1 holds.
    - Convert that ring's geo coords forward via `geo_to_pixel(lon, lat, cfg)` (lon first) to pixel
      coords, build `Polygon(pixel_coords)`, and rasterise with
      `rasterio.features.rasterize([(poly, bi)], out_shape=(H,W), transform=from_bounds(0,0,W,H,W,H),
      fill=-1, dtype=np.int16, all_touched=False)` — the SAME call compute() uses.
    - Assert `np.array_equal(rerasterised == bi, original)` (byte-equal mask).
    - If this CANNOT be made to pass on a REAL barony ring (truncation / staircase-vs-simplified
      drift), the test must `pytest.fail` with a message instructing ESCALATION — DO NOT relax the
      assertion and DO NOT fall back to a synthetic rectangle. (Per critical constraint #2 +
      advisor: A1 failure BLOCKS the phase — ring re-rasterisation would be the wrong mechanism and
      Plan 02 is unbuildable as written. Surface it loudly at Wave 0, not at Plan 04 Playwright.)

    GROUP B — vertex-replay behaviour (skip-marked `@pytest.mark.skip(reason="Wave 1: replay not
    yet implemented in compute()")` until Plan 02 lands, then Plan 02 removes the skips):
    - `test_move_op_re_rasterises_edited_barony_ring_pixels_differ_from_identity`: feed a snapshot
      `{"edit_log":[{"op":"move","ts":1,"vertexIds":["B#2"]}], "vertices":{"B#0":..,"B#1":..,
      "B#2":<moved>,..}}` and `barony_name_to_idx={"B":3}`; assert the returned raster's `==3`
      mask DIFFERS from the input mask. END-TO-END pixel-difference criterion, NOT "ran".
    - `test_add_op_grows_barony_ring`: snapshot vertices dict has an extra `B#N` key; assert mask changes.
    - `test_multi_delete_op_shrinks_barony_ring`: snapshot vertices dict missing a key; assert mask changes.
    - `test_barony_name_to_idx_built_from_bars_not_frontend`: assert compute() uses the passed
      `barony_name_to_idx` param (NOT a snapshot field) — call with the param and with a snapshot
      that has NO `barony_name_to_idx` key; replay must still work.

    GROUP C — `test_landmask_only_branch_with_nonempty_hash_returns_byte_identical` (THE
    discriminating BEZ-CONV-04 guard, advisor #3 — skip-marked until Wave 1):
    - Build cfg with a NON-EMPTY manual_edit_log_hash and a snapshot whose edit_log contains ONLY
      a `landmask_replace` op (zero vertex ops) and empty/absent vertices dict.
    - Assert `np.array_equal(compute(arr, cfg, barony_name_to_idx={...}), arr)` — byte-identical.
    - Comment: this fails if Pitfall 4 is live (ring re-rasterisation entered on a landmask-only branch).
  </action>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/unit/test_manual_edit_vertex_replay.py -k round_trip_identity -x -q` → 1 passed (GROUP A green TODAY, on a REAL Iberia coastline barony ring — not a synthetic rectangle).
    - `cd backend && python -m pytest tests/unit/test_manual_edit_vertex_replay.py -q` → exit 0 with GROUP B + GROUP C reported as skipped (not errored): grep output for `skipped` count >= 4.
    - `grep -c "def test_" backend/tests/unit/test_manual_edit_vertex_replay.py` >= 7.
    - File contains the literal strings `test_round_trip_identity`, `test_landmask_only_branch`, and `barony_name_to_idx`.
  </acceptance_criteria>
  <done>Round-trip identity test passes today; replay + landmask-only guard tests exist skip-marked; A1 escalation path is explicit in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Mock-DB render-producer loader-injection scaffold (skip until Wave 1)</name>
  <read_first>
    - backend/medieval_forge/api/v3/render.py — _render_producer (lines 102-214); the landmask_override fetch block (lines 148-167) is the pattern to mirror
    - 08.2-RESEARCH.md §Pattern 2 (lines 564-600) and §Pitfall 1 (lines 694-701) — by-value closure capture
    - backend/tests/ — grep for an existing render.py / _render_producer test to reuse the async + mock-session harness (grep `_render_producer` and `AsyncSessionLocal` in backend/tests)
  </read_first>
  <action>
    Create backend/tests/unit/test_render_producer_loader_injection.py, all tests
    `@pytest.mark.skip(reason="Wave 1: _render_producer wiring not yet added")` until Plan 02.
    - `test_branch_with_vertex_hash_sets_cfg_manual_edit_log_hash_and_count`: mock a Branch row
      with manual_edit_log_hash="abc123" / manual_edit_log_count=5; after the (Wave 1) fetch block,
      assert cfg.manual_edit_log_hash == "abc123" and cfg.manual_edit_log_count == 5.
    - `test_snapshot_loader_returns_captured_data_not_none`: mock latest Snapshot row whose
      deserialised blob is `{"edit_log":[...], "vertices":{...}}`; assert
      `cfg.snapshot_loader("any-branch")` returns that dict (NOT None) — proves by-value capture
      (Pitfall 1). Explicitly call the loader AFTER the simulated `async with` scope to catch
      reference-capture bugs.
    - `test_branch_with_empty_hash_leaves_snapshot_loader_unset`: branch row with empty hash →
      cfg.snapshot_loader stays None (zero-edit path untouched).
    Use the snake_case `edit_log` key in the mock blob (Pitfall 0 — the real wire format).
  </action>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/unit/test_render_producer_loader_injection.py -q` → exit 0, all tests skipped: grep output for `skipped` count >= 3.
    - `grep -c "def test_" backend/tests/unit/test_render_producer_loader_injection.py` >= 3.
    - File contains literals `snapshot_loader`, `manual_edit_log_hash`, and `edit_log` (snake_case).
  </acceptance_criteria>
  <done>Mock-DB scaffold exists, skip-marked, with the by-value-capture assertion (Pitfall 1) and snake_case wire key (Pitfall 0).</done>
</task>

<task type="auto">
  <name>Task 3: Failing end-to-end integration test (REAL wire format) + Playwright UAT scaffold + 08.1 spec migration</name>
  <read_first>
    - backend/medieval_forge/services/pipeline/__init__.py — run_pipeline_incremental manual_edit stage (lines 642-658); bars in scope (lines 564/582/588); compute() call line 652
    - backend/medieval_forge/api/v3/render.py — _render_producer lines 102-214
    - 08.2-RESEARCH.md §Pitfall 0 (lines 667-693) — edit_log snake_case is the real wire key; §"the full geometry chain" (lines 215-235)
    - 08.2-UI-SPEC.md §"Test migration (planner flag)" (lines 104-114) and Notes for Executor #9 (lines 412-417)
    - frontend/tests/uat/playwright/08.1-bezier-render-feedback.spec.ts — Describe 2, line 307 precondition + lines 332-335 load-bearing assertion
    - frontend/tests/uat/playwright/ — an existing 08.1 spec to copy the page-setup/coastline-selection harness
    - inicio/territory_data_v3.py — pick a real COASTLINE Iberia barony name (open boundary, no interior neighbours to gap per RESEARCH A3); record the exact name string in the spec
  </read_first>
  <action>
    (a) Create backend/tests/integration/test_bezier_convergence_e2e.py — the load-bearing red→green
    gate for Plan 02. NOT skip-marked; expected RED until Plan 02 lands (use
    `@pytest.mark.xfail(reason="Wave 1: convergence wiring not yet implemented", strict=True)` so the
    suite stays green now and Plan 02 flips it to xpass→pass by removing xfail):
    - Build a real-ish cfg + Iberia (or synthetic multi-barony) raster. Render a BASELINE
      (zero-edit) raster via the real manual_edit.compute path.
    - Construct a snapshot dict in the EXACT frontend wire format: keys `vertices` (dict of
      `"<baronyName>#<i>": {lat,lon}` with one coastline barony's ring moved) and `edit_log`
      (snake_case) holding a `{op:'move', vertexIds:[...]}`. Inject it via the same
      `cfg.snapshot_loader = lambda _bid, _d=snapshot: _d` by-value pattern _render_producer uses.
    - Set cfg.manual_edit_log_hash non-empty, manual_edit_log_count>0. Build barony_name_to_idx
      backend-side from bars (the Plan 02 mechanism).
    - Call compute() and assert the edited barony's pixel mask DIFFERS from baseline. This test
      catches Pitfall 0 (edit_log key), Pitfall 5 (mapping), and the replay gap simultaneously —
      it is the criterion that proves G8 closed, not "render ran".

    (b) Create frontend/tests/uat/playwright/08.2-bezier-convergence.spec.ts (BEZ-CONV-05),
    `test.skip` / `test.describe.skip` until Wave 2 (Plan 04 un-skips). Scaffold MUST name the
    chosen COASTLINE barony (string literal in the spec + a top comment `// COASTLINE barony:
    interior shared edges gap by construction — RESEARCH A3`). Use data-testid anchors:
    `bezier-apply-edits-btn`, `bezier-apply-auto-switch`, `bezier-edited-contour` (UI-SPEC §10).
    Outline the three assertions as TODO/skipped steps: (1) overlay absent before first edit +
    present after first drag; (2) after Apply, colored barony boundary pixels change + overlay
    clears; (3) reload page → boundary persists; export ZIP contains the changed lookup.

    (c) Migrate frontend/tests/uat/playwright/08.1-bezier-render-feedback.spec.ts Describe 2: remove
    the "overlay present on freshly-selected unedited barony" precondition (line 307); rewrite as
    "make first drag → assert overlay appears → capture snapshotBefore → second drag → assert
    snapshotAfter !== snapshotBefore" (UI-SPEC lines 109-113). Preserve the load-bearing geometry-
    change assertion. This unblocks the Plan 03 editLog>0 overlay guard.
  </action>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/integration/test_bezier_convergence_e2e.py -q` → exit 0 NOW with the test reported as `xfailed` (grep output contains `xfailed`).
    - backend/tests/integration/test_bezier_convergence_e2e.py contains literals `edit_log` (snake_case), `snapshot_loader`, and a `!= ` / `np.array_equal(...) is False`-style difference assertion on the edited barony mask.
    - frontend/tests/uat/playwright/08.2-bezier-convergence.spec.ts contains the literal `coastline` and all three data-testid strings `bezier-apply-edits-btn`, `bezier-apply-auto-switch`, `bezier-edited-contour`, and a named real barony string.
    - In 08.1-bezier-render-feedback.spec.ts: `grep -n "snapshotBefore" frontend/tests/uat/playwright/08.1-bezier-render-feedback.spec.ts` shows the capture occurs AFTER a drag (precondition migrated); the snapshotAfter !== snapshotBefore assertion still present.
  </acceptance_criteria>
  <done>E2E integration test exists in REAL wire format (xfail now, the Plan 02 gate); UAT spec scaffolds a named coastline barony with the three data-testids; 08.1 Describe 2 precondition migrated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend snapshot payload → backend compute() | `vertices` dict is untrusted input; per RESEARCH §Security, no max_length guard exists today |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.2-01 | Denial of Service | Apply snapshot `vertices` dict | accept (test-phase) | Cap deferred to Plan 03 Apply handler (MAX_VERTEX_KEYS = nb*200); this plan only authors tests. No new API surface here. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/unit/test_manual_edit_vertex_replay.py tests/unit/test_render_producer_loader_injection.py tests/integration/test_bezier_convergence_e2e.py -q` → exit 0 (identity passes, replay/loader skipped, e2e xfailed).
- `cd backend && python -m pytest tests/parity/ -x -q` → Iberia parity 10/10 STILL green (no production code touched this plan).
- `cd frontend && npx tsc --noEmit` → clean (spec migration is type-valid).
</verification>

<success_criteria>
- Round-trip identity test (A1 gate) PASSES today; failure path escalates rather than relaxes.
- Landmask-only parity guard test (BEZ-CONV-04 discriminator) exists, skip-marked.
- E2E integration test in REAL wire format exists (xfail), is the Plan 02 red→green gate.
- BEZ-CONV-05 UAT scaffold names a coastline barony + the three data-testids.
- 08.1 Describe 2 precondition migrated; Iberia parity 10/10 unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/08.2-bezier-edit-to-map-convergence-backend-replay/08.2-01-SUMMARY.md`
</output>
