---
phase: 08.2-bezier-edit-to-map-convergence-backend-replay
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - backend/medieval_forge/services/pipeline/manual_edit.py
  - backend/medieval_forge/api/v3/render.py
  - backend/medieval_forge/services/pipeline/__init__.py
  - backend/tests/unit/test_manual_edit_vertex_replay.py
  - backend/tests/unit/test_render_producer_loader_injection.py
  - backend/tests/integration/test_bezier_convergence_e2e.py
autonomous: true
requirements:
  - BEZ-CONV-01
  - BEZ-CONV-02
  - BEZ-CONV-03
  - BEZ-CONV-04

must_haves:
  truths:
    - "compute() replays move/add/delete/multi_delete by re-rasterising barony rings from the snapshot vertices dict — ONE replay_vertex_ring helper, NO per-op replay_move/replay_add functions (move payload carries no coordinates)."
    - "compute() gets barony_name_to_idx as a NEW param built BACKEND-SIDE from bars[] at both run_pipeline and run_pipeline_incremental call sites — NOT from a snapshot field, NOT from frontend array order (constraint #5; A2/A4 eliminated)."
    - "_render_producer fetches branch.manual_edit_log_hash/count and injects cfg.snapshot_loader by VALUE (lambda _bid, _d=snapshot: _d) — mirroring the existing landmask_override block."
    - "Ring re-rasterisation is entered ONLY when the edit log contains vertex-op entries; a landmask-only branch (non-empty hash, zero vertex ops) returns byte-identical (Pitfall 4 — BEZ-CONV-04 discriminator)."
    - "compute() reads edit_log via snapshot.get('edit_log') or snapshot.get('editLog') or [] (Pitfall 0 belt-and-suspenders — also fixes the camelCase auto-snapshot path)."
    - "After Plan 01's e2e gate flips green and parity stays 10/10, a real move op makes the edited barony's lookup raster DIFFER from baseline (END-TO-END, not 'render ran')."
  artifacts:
    - path: "backend/medieval_forge/services/pipeline/manual_edit.py"
      provides: "replay_vertex_ring helper + post-loop ring re-rasterisation branch + barony_name_to_idx param + vertex-op-presence guard + edit_log key normalize"
      contains: "def replay_vertex_ring"
    - path: "backend/medieval_forge/api/v3/render.py"
      provides: "_render_producer fetches branch hash/count + by-value snapshot_loader injection (mirrors landmask_override)"
      contains: "snapshot_loader"
    - path: "backend/medieval_forge/services/pipeline/__init__.py"
      provides: "barony_name_to_idx built from bars and passed to compute() at both orchestrator call sites"
      contains: "barony_name_to_idx"
  key_links:
    - from: "render._render_producer"
      to: "cfg.snapshot_loader + cfg.manual_edit_log_hash"
      via: "DB fetch of Branch + latest Snapshot, by-value closure"
      pattern: "snapshot_loader"
    - from: "manual_edit.compute"
      to: "contracts.geo_to_pixel + rasterio.features.rasterize"
      via: "replay_vertex_ring rebuilds Polygon from snapshot vertices, replaces polygons_by_id[idx]"
      pattern: "replay_vertex_ring"
    - from: "__init__.run_pipeline_incremental"
      to: "manual_edit.compute(result, cfg, barony_name_to_idx)"
      via: "{b['name']: i for i, b in enumerate(bars)}"
      pattern: "enumerate\\(bars\\)"
---

<objective>
Close BOTH missing wires that cause G8. Wire 1: manual_edit.compute() replays move/add/delete by
re-rasterising barony rings from the snapshot vertices dict. Wire 2: _render_producer fetches the
branch's manual_edit hash/count and injects a by-value snapshot_loader so compute() stops
short-circuiting to identity in production.

Purpose: Today a Bézier move commits to edit_events but compute() drops it (line 128 gap) AND
_render_producer never sets the hash/loader (08-07c known stub). Net: the colored BaronyLayer and
12-file export never change. This plan makes a real move op converge to the rasterised map while
keeping the zero-edit AND landmask-only paths byte-identical (BEZ-CONV-04).

Output: the Plan 01 e2e integration test flips from xfail to pass; the skip-marked replay +
loader-injection unit tests go green; Iberia parity stays 10/10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.2-bezier-edit-to-map-convergence-backend-replay/08.2-RESEARCH.md
@.planning/phases/08.2-bezier-edit-to-map-convergence-backend-replay/08.2-VALIDATION.md
@CLAUDE.md

<interfaces>
manual_edit.compute (current signature, manual_edit.py:39):
```python
def compute(input_array: np.ndarray, cfg: RegionConfig) -> np.ndarray
# NEW signature this plan: compute(input_array, cfg, barony_name_to_idx: dict[str,int] | None = None)
# Existing loop ends at line 129; ring re-rasterisation runs AFTER the loop (RESEARCH §521-560).
```

contracts.geo_to_pixel (contracts.py:176): `geo_to_pixel(lon, lat, cfg) -> (px, py)` — lon FIRST.

render._render_producer landmask pattern to MIRROR (render.py:148-167):
```python
async with AsyncSessionLocal() as _db:
    latest_lm = (await _db.execute(
        select(EditEvent).where(EditEvent.branch_id == branch_id,
                                EditEvent.op_type == "landmask_replace")
        .order_by(EditEvent.id.desc()).limit(1))).scalar_one_or_none()
if latest_lm is not None:
    coords = latest_lm.payload.get("new_landmask_coords")
    if isinstance(coords, list) and len(coords) >= 3:
        cfg.landmask_override = [tuple(pt) for pt in coords]
```

orchestrator call sites (__init__.py): run_pipeline line 429, run_pipeline_incremental line 652
— both call `_manual_edit.compute(result, cfg)` with `bars` in scope (lines 368/564/582/588).

barony ordering CONFIRMED (export.py:73 `for bi, b in enumerate(bars)`): metadata.baronies[bi]
order == bars[bi] raster index == raster integer value bi. So barony_name_to_idx =
{b["name"]: i for i, b in enumerate(bars)} is authoritative.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add replay_vertex_ring + post-loop ring re-rasterisation + barony_name_to_idx param + vertex-op guard + edit_log normalize</name>
  <read_first>
    - backend/medieval_forge/services/pipeline/manual_edit.py (FULL FILE — compute() 39-158, the loop 101-129, the rasterise-back block 131-158, the existing replay_* helpers 195-254)
    - backend/medieval_forge/services/pipeline/contracts.py lines 176-187 (geo_to_pixel — lon first)
    - 08.2-RESEARCH.md §Pattern 1 (lines 521-560), §"replay_vertex_ring helper" (lines 785-824), §Pitfall 0 (667-693), §Pitfall 2 (705-715), §Pitfall 4 (728-743), §Pitfall 6 (755-765)
    - .claude/skills/karpathy/SKILL.md (simplicity — ONE helper, not three)
    - backend/tests/unit/test_manual_edit_vertex_replay.py (the GROUP B + GROUP C targets from Plan 01)
  </read_first>
  <behavior>
    - Test (GROUP A, already passing): round-trip identity unaffected by the new code path.
    - Test (GROUP B): move op → edited barony mask DIFFERS from input mask.
    - Test (GROUP B): add op grows ring; multi_delete op shrinks ring; all via the same helper.
    - Test (GROUP B): barony_name_to_idx comes from the PARAM, works with a snapshot lacking that key.
    - Test (GROUP C, Pitfall 4): landmask-only branch (non-empty hash, zero vertex ops) → byte-identical output.
    - Test: empty manual_edit_log_hash → identity short-circuit unchanged (line 62, D-05).
  </behavior>
  <action>
    1. Change compute() signature to
       `def compute(input_array, cfg, barony_name_to_idx: dict[str, int] | None = None) -> np.ndarray`.
       Keep the empty-hash short-circuit at line 62 EXACTLY as-is (D-05 — do not touch).

    2. Normalize the edit-log read (Pitfall 0, belt-and-suspenders — advisor #4):
       replace line 76 `edit_log = snapshot.get("edit_log", [])` with
       `edit_log = snapshot.get("edit_log") or snapshot.get("editLog") or []`.
       This also fixes the camelCase 25-edit/auto-immediate snapshot path.

    3. In the existing op loop (lines 101-129) leave split/merge/translate UNCHANGED. The
       move/add/delete/multi_delete ops are handled AFTER the loop (they carry no usable geometry
       in the payload — RESEARCH §125-146). Do NOT add per-op helpers (anti-pattern §637-650).

    4. Add a vertex-op-presence guard (Pitfall 4 — the BEZ-CONV-04 discriminator):
       `vertex_ops_present = any(op.get("op") in ("move","add","delete","multi_delete") for op in edit_log)`
       Only enter the ring re-rasterisation block if `vertex_ops_present and barony_name_to_idx`.

    5. AFTER the loop, BEFORE the rasterise-back block (line 131), add ring re-rasterisation:
       ```python
       vertices_dict = snapshot.get("vertices") or {}
       if vertex_ops_present and barony_name_to_idx and vertices_dict:
           barony_names = {k.split('#')[0] for k in vertices_dict if '#' in k}
           for bname in barony_names:
               replay_vertex_ring(bname, barony_name_to_idx, vertices_dict, polygons_by_id, cfg)
       ```
       Iterate ALL distinct barony prefixes in vertices (not just changed ones) so coupled
       neighbours whose full ring is present re-rasterise too (RESEARCH §365-368).

    6. Add the PURE helper `replay_vertex_ring(barony_name, barony_name_to_idx, vertices_dict,
       polygons_by_id, cfg)` verbatim from RESEARCH §785-824:
       - `raster_idx = barony_name_to_idx.get(barony_name)`; return if None.
       - `ring_keys = sorted([k for k in vertices_dict if k.startswith(barony_name+'#')],
         key=lambda x: int(x.split('#')[1]) if '#' in x else 0)`.
       - Pitfall 2 guard: `if len(ring_keys) < 3: return`.
       - `pixel_coords = [geo_to_pixel(vertices_dict[k]['lon'], vertices_dict[k]['lat'], cfg) for k in ring_keys]`
         (lon FIRST — contracts.py:176).
       - `poly = Polygon(pixel_coords)` in try/except; `if poly.is_valid and not poly.is_empty:
         polygons_by_id[raster_idx] = [poly]`.
       Add `replay_vertex_ring` to `__all__`. Import `geo_to_pixel` from `.contracts`.

    7. Remove the `@pytest.mark.skip` markers Plan 01 placed on GROUP B + GROUP C in
       test_manual_edit_vertex_replay.py so those tests now RUN and PASS.
  </action>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/unit/test_manual_edit_vertex_replay.py -x -q` → all tests pass (round-trip identity + move/add/delete differ-from-baseline + landmask-only byte-identical), 0 skipped.
    - `grep -n "def replay_vertex_ring" backend/medieval_forge/services/pipeline/manual_edit.py` returns a hit; `grep -c "def replay_move\|def replay_add\|def replay_delete" backend/medieval_forge/services/pipeline/manual_edit.py` → 0 (no per-op helpers).
    - `grep -n "snapshot.get(\"edit_log\") or snapshot.get(\"editLog\")" backend/medieval_forge/services/pipeline/manual_edit.py` returns a hit (Pitfall 0).
    - `grep -n "vertex_ops_present" backend/medieval_forge/services/pipeline/manual_edit.py` returns a hit (Pitfall 4 guard).
    - `grep -n "barony_name_to_idx" backend/medieval_forge/services/pipeline/manual_edit.py` returns the param in the compute() signature.
  </acceptance_criteria>
  <done>compute() replays move/add/delete via one ring helper, gated on vertex-op presence, byte-identical on empty + landmask-only paths; replay unit tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Build barony_name_to_idx from bars and pass it to compute() at both orchestrator call sites</name>
  <read_first>
    - backend/medieval_forge/services/pipeline/__init__.py — run_pipeline manual_edit stage (lines 420-433, call line 429); run_pipeline_incremental manual_edit stage (lines 642-658, call line 652); bars assignment (lines 368, 564, 582, 588)
    - backend/medieval_forge/services/pipeline/export.py line 73 (`for bi, b in enumerate(bars)`) — confirms metadata baronies order == bars index, the authority for the mapping
    - 08.2-RESEARCH.md §"Final recommended design (revised)" (lines 226-235) — backend-side mapping eliminates A2/A4
  </read_first>
  <action>
    At BOTH manual_edit call sites in __init__.py, immediately before
    `_manual_edit.compute(result, cfg)`, build:
    `barony_name_to_idx = {b["name"]: i for i, b in enumerate(bars)}`
    and pass it: `_manual_edit.compute(result, cfg, barony_name_to_idx=barony_name_to_idx)`.

    - run_pipeline (line ~429): `bars` is in scope (line 368).
    - run_pipeline_incremental (line ~652): `bars` is in scope (lines 564/582/588 — present in all
      branches: fresh setup, cache hit, and recompute). If a branch path could leave `bars` unbound,
      build the mapping defensively (e.g. `{}` fallback) but DO NOT silently swallow — the empty-hash
      short-circuit already protects the zero-edit path, and barony_name_to_idx is only consumed when
      vertex ops are present.

    Do NOT change RegionConfig. The param default is None so the CLI run_pipeline and existing
    parity tests (which do not pass it... wait — run_pipeline DOES pass it now) stay correct: the
    empty-hash short-circuit returns before barony_name_to_idx is read on the zero-edit path, so
    parity is unaffected.
  </action>
  <acceptance_criteria>
    - `grep -n "barony_name_to_idx = {b\[\"name\"\]: i for i, b in enumerate(bars)}" backend/medieval_forge/services/pipeline/__init__.py` returns 2 hits (both call sites).
    - `grep -n "barony_name_to_idx=barony_name_to_idx" backend/medieval_forge/services/pipeline/__init__.py` returns 2 hits.
    - `cd backend && python -m pytest tests/parity/ -x -q` → Iberia parity 10/10 STILL green (zero-edit path byte-untouched, BEZ-CONV-04).
  </acceptance_criteria>
  <done>Both orchestrators build the authoritative mapping from bars and pass it to compute(); Iberia parity unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Wire _render_producer — fetch branch hash/count + by-value snapshot_loader injection</name>
  <read_first>
    - backend/medieval_forge/api/v3/render.py — _render_producer FULL (lines 102-214); the landmask_override fetch block (148-167) to mirror; the cfg replace() at lines 133-139; the project.updated_at bump (183-190)
    - backend/medieval_forge/services/branches/ — the Branch + Snapshot models + deserialize/restore_snapshot helper (grep `class Branch`, `class Snapshot`, `def deserialize` / `restore_snapshot` under backend/medieval_forge)
    - 08.2-RESEARCH.md §Pattern 2 (lines 564-600), §Pitfall 1 (694-701) by-value capture, §Open Questions (RESOLVED) Q#2 (snapshot `vertices` round-trip — RESOLVED + gated by Plan 01 Task 2; rely on it, do not re-verify)
    - backend/tests/unit/test_render_producer_loader_injection.py (the Plan 01 targets to un-skip)
  </read_first>
  <action>
    1. In _render_producer, AFTER the existing landmask_override block (lines 164-168), add the
       branch-hash + snapshot_loader fetch, mirroring the landmask pattern (RESEARCH §564-600):
       ```python
       # Phase 08.2: wire manual_edit hash + snapshot_loader for vertex-op replay
       async with AsyncSessionLocal() as _db:
           branch_row = (await _db.execute(
               select(Branch).where(Branch.id == branch_id))).scalar_one_or_none()
           _snapshot_data = None
           if branch_row and branch_row.manual_edit_log_hash:
               cfg.manual_edit_log_hash = branch_row.manual_edit_log_hash
               cfg.manual_edit_log_count = branch_row.manual_edit_log_count
               latest_snap = (await _db.execute(
                   select(Snapshot).where(Snapshot.branch_id == branch_id)
                   .order_by(Snapshot.seq.desc()).limit(1))).scalar_one_or_none()
               if latest_snap is not None:
                   _snapshot_data = deserialize(latest_snap.blob)
       if _snapshot_data is not None:
           _data = _snapshot_data
           cfg.snapshot_loader = lambda _bid, _d=_data: _d   # BY VALUE (Pitfall 1)
       ```
       Use the actual model names / Snapshot ordering column / deserialize helper discovered in
       read_first. If cfg was produced via replace() earlier (lines 133-139), setattr works since
       cfg is already a local @dataclass copy (contracts.py confirmed @dataclass); use replace() if
       the codebase convention prefers it.

    2. Open Q#2 (`vertices` survives serialize -> deserialize) is RESOLVED and GATED upstream:
       Plan 01 Task 2's `test_snapshot_serialize_round_trip_preserves_vertices_key` already
       proved (Wave 0, passes today) that the extra `vertices` key round-trips through the real
       `snapshot.py` serialize/deserialize path. RESEARCH Open Q#2 (RESOLVED) records the evidence
       chain (TypedDict not runtime-enforced; `branches.py:146` body is `dict[str,Any]`;
       `json.dumps`/`json.loads` key-preserving). So you may rely on `deserialize(latest_snap.blob)`
       returning a dict that includes `vertices` — NO re-verification needed here. (If that Wave 0
       gate had failed, this plan would not have been reachable: the gate blocks Wave 1.) Do NOT
       add a redundant runtime assertion in _render_producer; the contract is locked at Wave 0.

    3. Remove the `@pytest.mark.skip` markers on test_render_producer_loader_injection.py so its
       three tests RUN and PASS (hash/count set, by-value loader returns data not None, empty-hash
       leaves loader unset).

    4. Flip the Plan 01 e2e integration test from xfail to a real pass: remove the
       `@pytest.mark.xfail` on test_bezier_convergence_e2e.py. It must now go GREEN (a real move op
       changes the edited barony raster vs baseline) — the load-bearing G8-closed criterion.
  </action>
  <acceptance_criteria>
    - `grep -n "cfg.snapshot_loader = lambda _bid, _d=" backend/medieval_forge/api/v3/render.py` returns a hit (by-value capture, Pitfall 1).
    - `grep -n "manual_edit_log_hash" backend/medieval_forge/api/v3/render.py` returns a hit inside _render_producer.
    - `cd backend && python -m pytest tests/unit/test_render_producer_loader_injection.py -x -q` → 3 passed, 0 skipped.
    - `cd backend && python -m pytest tests/integration/test_bezier_convergence_e2e.py -x -q` → 1 passed (xfail removed; G8-closed gate GREEN).
    - `cd backend && python -m pytest tests/parity/ -x -q` → Iberia parity 10/10 STILL green.
  </acceptance_criteria>
  <done>_render_producer injects a by-value snapshot_loader + sets hash/count only when the branch has edits; the e2e G8-closed gate is green; loader-injection unit tests green; parity 10/10 unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot blob (DB) → compute() | deserialised `vertices` dict drives ring rasterisation; untrusted geometry |
| edit_events branch hash → render | branch_row.manual_edit_log_hash decides whether replay fires |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.2-02 | Tampering | snapshot `vertices` ring | mitigate | replay_vertex_ring guards `len(ring_keys) < 3` + `poly.is_valid and not poly.is_empty` before replacing polygons_by_id (Pitfall 2); invalid rings are skipped, never crash the render. |
| T-08.2-03 | Denial of Service (parity break) | landmask-only branch with non-empty hash | mitigate | vertex_ops_present guard prevents ring re-rasterisation on non-vertex branches (Pitfall 4 — BEZ-CONV-04). |
| T-08.2-04 | Information Disclosure | snapshot_loader closure | accept | closure receives pre-fetched data; no new DB access in the sync to_thread sandbox (RESEARCH §Security). |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/unit/test_manual_edit_vertex_replay.py tests/unit/test_render_producer_loader_injection.py tests/integration/test_bezier_convergence_e2e.py -x -q` → all green, 0 skipped, 0 xfail.
- `cd backend && python -m pytest tests/parity/ -x -q` → Iberia 10/10 green (BEZ-CONV-04).
- `cd backend && python -m pytest tests/ -q` → full backend suite green.
</verification>

<success_criteria>
- A real move op makes the edited barony raster DIFFER from baseline (e2e gate green) — G8 closed at the backend layer.
- Zero-edit AND landmask-only branches remain byte-identical (BEZ-CONV-04 + Pitfall 4 guard).
- ONE replay_vertex_ring helper; no per-op functions; barony_name_to_idx built backend-side from bars.
- snapshot_loader captured by value; `vertices` preservation through the snapshot round trip is already locked by Plan 01 Task 2's Wave 0 gate — this plan relies on it, no re-verification.
</success_criteria>

<output>
After completion, create `.planning/phases/08.2-bezier-edit-to-map-convergence-backend-replay/08.2-02-SUMMARY.md`
</output>
