---
phase: 01
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - backend/medieval_forge/lib/map_generator.py
  - backend/medieval_forge/lib/__init__.py
  - backend/medieval_forge/services/generator.py
  - backend/medieval_forge/services/render_modern.py
  - backend/medieval_forge/services/baronies_geojson.py
  - backend/medieval_forge/api/generate.py
  - backend/medieval_forge/main.py
  - backend/tests/test_generator_e2e.py
  - backend/tests/test_generate.py
  - backend/tests/test_terrain.py
  - backend/tests/test_baronies_geojson.py
  - backend/tests/services/test_territory_iberia_parity.py
  - backend/tests/api/test_generate_uses_cached_research.py
  - backend/tests/api/test_generate_validation.py
  - backend/tests/parity/__init__.py
  - backend/tests/parity/conftest.py
  - backend/tests/parity/test_iberia_868.py
  - backend/tests/unit/test_pipeline_module.py
  - backend/tests/unit/test_pipeline_cli.py
  - backend/tests/integration/test_app_boot.py
  - .github/workflows/ci.yml
autonomous: false
requirements:
  - V3-PIPELINE-PARITY

must_haves:
  truths:
    - "5 v1 production files deleted (lib/map_generator.py, lib/__init__.py, services/generator.py, services/render_modern.py, services/baronies_geojson.py, api/generate.py — 6 entries because lib has __init__.py too)"
    - "7 v1 test files deleted (test_generator_e2e, test_generate, test_terrain, test_baronies_geojson, services/test_territory_iberia_parity, api/test_generate_uses_cached_research, api/test_generate_validation) — D-07 delete-not-skip"
    - "main.py no longer imports from .api.generate; FastAPI app still boots and / still returns 200 (Phase 00 SC-6 invariant)"
    - "ROADMAP success criterion 1 met: pytest tests/parity/test_iberia_868.py passes (10 of 12 contract files: 2 lookup PNGs byte-equal + 4 visual PNGs SSIM ≥ 0.98 + 4 JSONs deep-equal — terrain_lookup.png + terrain_types.json deferred per P-2)"
    - "Parity test uses session-scoped pipeline_output fixture so the ~45s pipeline runs once per test session (RESEARCH §5.a)"
    - "ROADMAP success criterion 3 met: CI parity job is non-skippable — `.github/workflows/ci.yml` no longer ends the parity step with `|| (echo …; exit 0)`"
    - "Coverage gate raised from 60% → 85% in the same CI commit (per ci.yml comment 'Phase 01 raises to 85%')"
  artifacts:
    - path: "backend/tests/parity/__init__.py"
      provides: "empty package marker"
    - path: "backend/tests/parity/conftest.py"
      provides: "session-scoped pipeline_output + golden_dir fixtures"
      exports: ["pipeline_output", "golden_dir"]
    - path: "backend/tests/parity/test_iberia_868.py"
      provides: "10 parametrised parity tests across 3 functions: test_lookup_png_byte_equal (×2), test_visual_png_ssim (×4), test_json_deep_equal (×4)"
      contains: "@pytest.mark.parity, parametrise on file names, np.array_equal for lookup PNGs, ssim ≥ 0.98 for visual PNGs, deep-equal after recursive key-sort for JSONs"
    - path: "backend/tests/unit/test_pipeline_module.py"
      provides: "Verifies run_pipeline exists with the right signature"
    - path: "backend/tests/unit/test_pipeline_cli.py"
      provides: "Smoke tests `python -m medieval_forge.services.pipeline --help` and `--region iberia_868 --out X` (the latter optionally @pytest.mark.slow)"
    - path: "backend/tests/integration/test_app_boot.py"
      provides: "Asserts FastAPI / returns 200 after generator delete (Phase 00 SC-6 invariant; ROADMAP SC-3 transitively)"
    - path: ".github/workflows/ci.yml"
      provides: "pytest-parity job non-skippable (`|| exit 0` tail removed); pytest-unit gate raised to --cov-fail-under=85"
  key_links:
    - from: "backend/tests/parity/conftest.py"
      to: "medieval_forge.services.pipeline.run_pipeline + REGIONS['iberia_868']"
      via: "Session fixture calls run_pipeline once into tmp_path_factory.mktemp; tests share that output"
      pattern: "scope=\"session\""
    - from: "backend/tests/parity/test_iberia_868.py"
      to: "tests/fixtures/iberia_868/golden/"
      via: "golden_dir fixture points at REPO_ROOT / 'tests' / 'fixtures' / 'iberia_868' / 'golden'"
      pattern: "fixtures/iberia_868/golden"
    - from: "backend/medieval_forge/main.py"
      to: "(NOTHING — api.generate import removed)"
      via: "main.py no longer references api.generate; SPA catch-all keeps / serving 200"
      pattern: "api\\.generate"
---

<objective>
Wave 3 closes the phase: (a) surgical deletion of the v1 generator stack per
RESEARCH §3 import-graph trace; (b) build the parity test harness against
the Plan 01 golden fixtures; (c) flip the CI parity job to non-skippable as
the LAST commit (per RESEARCH §7 sequencing — flipping earlier would gate
in-flight port commits against themselves).

Purpose: Make Phase 01 close out cleanly. After this plan: the v1 generator
is gone, parity is locked at the file boundary (10 of 12 contract files), and
any future PR that breaks parity blocks merge.

Output: 4 commits — (1) v1 generator stack deletion + main.py edit, (2)
parity harness + unit/integration tests, (3) local green observation
(checkpoint), (4) CI flip + coverage gate raise (final).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-pipeline-parity-port-harness-together/01-CONTEXT.md
@.planning/phases/01-pipeline-parity-port-harness-together/01-RESEARCH.md
@.planning/phases/01-pipeline-parity-port-harness-together/01-VALIDATION.md
@.planning/phases/01-pipeline-parity-port-harness-together/PREFLIGHT.md
@.planning/phases/01-pipeline-parity-port-harness-together/01-01-SUMMARY.md
@.planning/phases/01-pipeline-parity-port-harness-together/01-02-SUMMARY.md
@CLAUDE.md
@.claude/skills/karpathy/SKILL.md
@.planning/quick/260507-g1v-phase-00-v3-archive-milestone-reset/V1_DELETION_CANDIDATES.md
@.github/workflows/ci.yml
@backend/medieval_forge/main.py
@backend/medieval_forge/api/generate.py
@backend/medieval_forge/services/generator.py
@pyproject.toml

<interfaces>
Per RESEARCH §3.a confirmed-delete production list (5 files + 1 directory):

```
backend/medieval_forge/lib/map_generator.py        (1094 lines)
backend/medieval_forge/lib/__init__.py             (1 line; drop the dir)
backend/medieval_forge/services/generator.py       (391 lines; uses banned sys.modules patching)
backend/medieval_forge/services/render_modern.py   (199 lines; only consumer is api/generate.py)
backend/medieval_forge/services/baronies_geojson.py (124 lines; only consumer is services/generator.py — confirmed via Grep, no other importer)
backend/medieval_forge/api/generate.py             (270 lines; only consumer is main.py)
```

Per RESEARCH §3.c confirmed-delete test list (7 files):

```
backend/tests/test_generator_e2e.py
backend/tests/test_generate.py
backend/tests/test_terrain.py
backend/tests/test_baronies_geojson.py
backend/tests/services/test_territory_iberia_parity.py
backend/tests/api/test_generate_uses_cached_research.py
backend/tests/api/test_generate_validation.py
```

main.py edit (RESEARCH §3.a final entry):

```python
# REMOVE these two lines:
from .api.generate import router as generate_router  # ~line 50
app.include_router(generate_router, prefix="/api")    # ~line 61
```

Files that explicitly SURVIVE despite CONTEXT.md tentative listing them
(RESEARCH §3.b):

```
backend/medieval_forge/services/voronoi.py            — used by api/edit.py
backend/medieval_forge/services/territories_geojson.py — used by api/edit.py
backend/medieval_forge/services/territory_builder.py   — used by api/edit.py
backend/medieval_forge/services/baronies_builder.py    — used by api/ingest.py
backend/medieval_forge/services/paths.py               — used by 11+ files
backend/tests/services/test_voronoi.py                 — tests surviving voronoi.py
backend/tests/services/test_territory_builder*.py      — tests surviving territory_builder.py
backend/tests/test_territories_geojson.py              — tests surviving territories_geojson.py
backend/tests/services/test_territories_geojson_consistency.py — tests surviving territories_geojson.py
backend/tests/test_export.py                           — survives (only imports services/paths)
```

Parity harness shape (RESEARCH §5.a):

```python
# backend/tests/parity/conftest.py
@pytest.fixture(scope="session")
def pipeline_output(tmp_path_factory):
    out = tmp_path_factory.mktemp("iberia_868_actual")
    cfg = REGIONS["iberia_868"]()
    cfg.output_dir = str(out)
    run_pipeline(cfg)
    return out

@pytest.fixture(scope="session")
def golden_dir():
    return REPO_ROOT / "tests" / "fixtures" / "iberia_868" / "golden"
```

```python
# backend/tests/parity/test_iberia_868.py
@pytest.mark.parametrize("name", ["lookup_barony.png", "lookup_condado.png"])
def test_lookup_png_byte_equal(pipeline_output, golden_dir, name):
    actual = np.array(Image.open(pipeline_output / name))
    golden = np.array(Image.open(golden_dir / name))
    assert np.array_equal(actual, golden), ...

@pytest.mark.parametrize("name", ["visual_condado.png", "visual_barony.png", "mountains_mask.png", "rivers_overlay.png"])
def test_visual_png_ssim(pipeline_output, golden_dir, name):
    actual = np.array(Image.open(pipeline_output / name).convert("RGB"))
    golden = np.array(Image.open(golden_dir / name).convert("RGB"))
    score = ssim(actual, golden, channel_axis=2, data_range=255)
    assert score >= 0.98, ...

@pytest.mark.parametrize("name", ["lookup_barony_colors.json", "lookup_condado_colors.json", "territory_metadata.json", "mountain_river_data.json"])
def test_json_deep_equal(pipeline_output, golden_dir, name):
    assert _normalise(json.loads((pipeline_output / name).read_text())) == _normalise(json.loads((golden_dir / name).read_text())), ...
```

NOTE: 4 JSON entries (not 5 from RESEARCH §5.b) because `terrain_types.json` is
deferred per P-2 (same as terrain_lookup.png).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete v1 generator stack (5 production files + 1 dir + 7 tests) and edit main.py</name>
  <read_first>
    - .planning/phases/01-pipeline-parity-port-harness-together/01-RESEARCH.md §3 (full deletion graph — confirmed-delete list, surviving files, main.py edit at lines 50 + 61)
    - .planning/phases/01-pipeline-parity-port-harness-together/01-CONTEXT.md (D-05 clean delete, D-06 surgical import-graph-driven, D-07 delete-not-skip, D-08 frontend untouched)
    - backend/medieval_forge/main.py (current state — find the api.generate import and include_router lines)
    - backend/medieval_forge/api/generate.py (just before delete — confirm no surprise consumers)
    - .planning/quick/260507-g1v-phase-00-v3-archive-milestone-reset/V1_DELETION_CANDIDATES.md (cross-reference: lib/map_generator.py listed as DO NOT DELETE in Phase 00; Phase 01 owns the delete per "Phase 01 may delete the v1 wrapper after parity is proven")
  </read_first>
  <action>
    Single atomic commit covering the production delete + main.py edit + test
    delete. Per RESEARCH §3.d, the delete and main.py edit MUST be in the SAME
    commit so no in-between commit has a broken FastAPI import. **Do NOT** run
    this task before Plan 02 Task 8 has produced a working pipeline — the
    rollback path is "revert this commit and api/generate is back."

    Production deletes (6 path entries — 5 files + 1 dir):
        rm backend/medieval_forge/lib/map_generator.py
        rm backend/medieval_forge/lib/__init__.py
        rmdir backend/medieval_forge/lib                              # if empty
        rm backend/medieval_forge/services/generator.py
        rm backend/medieval_forge/services/render_modern.py
        rm backend/medieval_forge/services/baronies_geojson.py
        rm backend/medieval_forge/api/generate.py

    main.py edit (RESEARCH §3.a — remove the two lines registering api.generate):
        - Line ~50: `from .api.generate import router as generate_router`
        - Line ~61: `app.include_router(generate_router, prefix="/api")`

    The SPA catch-all (line ~76) preserves `/` returning 200 (Phase 00 SC-6
    invariant). Confirm the catch-all is intact after the edit.

    Test deletes (7 files per RESEARCH §3.c):
        rm backend/tests/test_generator_e2e.py
        rm backend/tests/test_generate.py
        rm backend/tests/test_terrain.py
        rm backend/tests/test_baronies_geojson.py
        rm backend/tests/services/test_territory_iberia_parity.py
        rm backend/tests/api/test_generate_uses_cached_research.py
        rm backend/tests/api/test_generate_validation.py

    Surviving files NOT to touch (RESEARCH §3.b):
        services/voronoi.py, services/territories_geojson.py,
        services/territory_builder.py, services/baronies_builder.py,
        services/paths.py — all stay (used by api/edit.py, api/ingest.py).

    Editable-install hygiene: after delete, run `pip install -e .` once to
    refresh `medieval_forge.egg-info/RECORD` (RESEARCH "Runtime State Inventory").

    Smoke checks before commit:
        1. `medieval-forge start` boots — no ImportError on `from .api.generate`.
        2. `curl -sf http://localhost:8000/` returns HTML (HTTP 200) — Phase 00 SC-6.
        3. `pytest backend/tests/ -v -k "not parity and not slow" --co -q` collects without error (no orphan imports of deleted modules).

    Commit:
        chore(01): delete v1 generator stack + main.py register edit (D-05/D-06/D-07)
  </action>
  <verify>
    <automated>! test -f backend/medieval_forge/lib/map_generator.py &amp;&amp; ! test -f backend/medieval_forge/services/generator.py &amp;&amp; ! test -f backend/medieval_forge/services/render_modern.py &amp;&amp; ! test -f backend/medieval_forge/services/baronies_geojson.py &amp;&amp; ! test -f backend/medieval_forge/api/generate.py &amp;&amp; ! test -f backend/tests/test_generator_e2e.py &amp;&amp; ! test -f backend/tests/test_generate.py &amp;&amp; ! test -f backend/tests/test_terrain.py &amp;&amp; ! test -f backend/tests/test_baronies_geojson.py &amp;&amp; ! test -f backend/tests/services/test_territory_iberia_parity.py &amp;&amp; ! test -f backend/tests/api/test_generate_uses_cached_research.py &amp;&amp; ! test -f backend/tests/api/test_generate_validation.py &amp;&amp; ! grep -rn "from .api.generate\|from medieval_forge.api.generate\|from ..api.generate" backend/medieval_forge/ &amp;&amp; ! grep -rn "from .lib.map_generator\|from ..lib import map_generator\|from medieval_forge.lib" backend/medieval_forge/ &amp;&amp; ! grep -rn "from .services.generator\|from medieval_forge.services.generator\|from ..services.generator" backend/medieval_forge/ &amp;&amp; ! grep -rn "from .services.render_modern\|from ..services.render_modern" backend/medieval_forge/ &amp;&amp; ! grep -rn "from .services.baronies_geojson\|from ..services.baronies_geojson" backend/medieval_forge/ &amp;&amp; test -f backend/medieval_forge/services/voronoi.py &amp;&amp; test -f backend/medieval_forge/services/territory_builder.py &amp;&amp; test -f backend/medieval_forge/services/territories_geojson.py &amp;&amp; python -c "import medieval_forge.main"</automated>
  </verify>
  <files>
    backend/medieval_forge/lib/map_generator.py
    backend/medieval_forge/lib/__init__.py
    backend/medieval_forge/services/generator.py
    backend/medieval_forge/services/render_modern.py
    backend/medieval_forge/services/baronies_geojson.py
    backend/medieval_forge/api/generate.py
    backend/medieval_forge/main.py
    backend/tests/test_generator_e2e.py
    backend/tests/test_generate.py
    backend/tests/test_terrain.py
    backend/tests/test_baronies_geojson.py
    backend/tests/services/test_territory_iberia_parity.py
    backend/tests/api/test_generate_uses_cached_research.py
    backend/tests/api/test_generate_validation.py
  </files>
  <acceptance_criteria>
    - All 5 production files + lib/__init__.py are deleted (`! test -f` for each).
    - All 7 test files are deleted.
    - `medieval_forge/lib/` directory is gone (or only contains __pycache__ that gets cleaned).
    - No remaining import of deleted modules: `! grep -rn "from .api.generate\|from .lib.map_generator\|from .services.generator\|from .services.render_modern\|from .services.baronies_geojson" backend/medieval_forge/` returns empty.
    - Surviving files still present: `test -f backend/medieval_forge/services/voronoi.py` AND `territory_builder.py` AND `territories_geojson.py` AND `baronies_builder.py` AND `paths.py`.
    - `python -c "import medieval_forge.main"` exits 0 (FastAPI app still importable — Phase 00 SC-6).
    - `pytest backend/tests/ -v -k "not parity and not slow" --co -q` exits 0 (no broken imports in surviving tests).
    - main.py no longer contains `from .api.generate` or `include_router(generate_router`: `! grep -n "api\\.generate" backend/medieval_forge/main.py`.
    - Commit message: `chore(01): delete v1 generator stack + main.py register edit (D-05/D-06/D-07)`.
  </acceptance_criteria>
  <done>v1 generator stack is gone (production + tests + main.py wiring); FastAPI app still boots; surviving files (voronoi, territory_builder, territories_geojson, baronies_builder, paths) untouched per RESEARCH §3.b. Frontend stepper UI 404s on /api/generate are accepted per D-08 (Phase 03 owns frontend rewrite).</done>
</task>

<task type="auto">
  <name>Task 2: Build the parity harness + unit/integration tests (RESEARCH §5 + §8 Wave 0 list)</name>
  <read_first>
    - .planning/phases/01-pipeline-parity-port-harness-together/01-RESEARCH.md §5 (parity harness layout — session fixture, 3 parametrised tests across 10 files, diff-on-failure helper)
    - .planning/phases/01-pipeline-parity-port-harness-together/01-RESEARCH.md §8 (Wave 0 gaps — also need test_pipeline_module.py, test_pipeline_cli.py, test_app_boot.py)
    - .planning/phases/01-pipeline-parity-port-harness-together/01-VALIDATION.md (per-task verification map — 14 parity test invocations + 3 unit/integration ones)
    - .planning/phases/01-pipeline-parity-port-harness-together/01-CONTEXT.md (D-12 comparison rules; D-09 deployed-wins)
    - tests/fixtures/iberia_868/golden/README.md (Plan 01 Task 3 — confirms 11 files in golden, terrain_lookup.png and terrain_types.json deferred)
    - pyproject.toml (verify pytest `parity` marker is registered at line 73)
    - inicio/licoes/JORNADA_CRIACAO_MAPA.md (line ~680 — full Iberia generation runtime ~45s; informs why session-scoped fixture)
  </read_first>
  <action>
    Create six files in one atomic commit. The harness DOES NOT need to pass yet
    — Task 3 is the local-green checkpoint.

    **`backend/tests/parity/__init__.py`** — empty.

    **`backend/tests/parity/conftest.py`** — verbatim from RESEARCH §5.a:

        import pytest
        from pathlib import Path
        from medieval_forge.services.pipeline import run_pipeline
        from medieval_forge.services.pipeline.regions import REGIONS

        REPO_ROOT = Path(__file__).resolve().parents[3]  # backend/tests/parity → repo root
        GOLDEN_DIR = REPO_ROOT / "tests" / "fixtures" / "iberia_868" / "golden"

        @pytest.fixture(scope="session")
        def pipeline_output(tmp_path_factory):
            out = tmp_path_factory.mktemp("iberia_868_actual")
            cfg = REGIONS["iberia_868"]()
            cfg.output_dir = str(out)
            run_pipeline(cfg)
            return out

        @pytest.fixture(scope="session")
        def golden_dir():
            return GOLDEN_DIR

    **`backend/tests/parity/test_iberia_868.py`** — verbatim from RESEARCH §5.b
    with the P-2 deferral applied (4 JSONs not 5; 2 lookup PNGs not 3):

        import json
        import pytest
        import numpy as np
        from PIL import Image
        from skimage.metrics import structural_similarity as ssim

        pytestmark = pytest.mark.parity

        # --- Lookup PNGs: byte-equal (terrain_lookup.png deferred to Phase 06 per P-2) ---
        @pytest.mark.parametrize("name", ["lookup_barony.png", "lookup_condado.png"])
        def test_lookup_png_byte_equal(pipeline_output, golden_dir, name):
            actual = np.array(Image.open(pipeline_output / name))
            golden = np.array(Image.open(golden_dir / name))
            if not np.array_equal(actual, golden):
                diff_path = pipeline_output / f"DIFF_{name}"
                Image.fromarray(np.where(actual == golden, 0, 255).astype(np.uint8)).save(diff_path)
                pytest.fail(
                    f"{name}: pixel-mismatch.\n"
                    f"  golden: {golden_dir / name}\n"
                    f"  actual: {pipeline_output / name}\n"
                    f"  diff:   {diff_path}\n"
                )

        # --- Visual PNGs + masks: SSIM ≥ 0.98 ---
        @pytest.mark.parametrize("name", ["visual_condado.png", "visual_barony.png", "mountains_mask.png", "rivers_overlay.png"])
        def test_visual_png_ssim(pipeline_output, golden_dir, name):
            actual = np.array(Image.open(pipeline_output / name).convert("RGB"))
            golden = np.array(Image.open(golden_dir / name).convert("RGB"))
            score = ssim(actual, golden, channel_axis=2, data_range=255)
            assert score >= 0.98, (
                f"{name}: SSIM {score:.4f} < 0.98.\n"
                f"  golden: {golden_dir / name}\n"
                f"  actual: {pipeline_output / name}"
            )

        # --- JSONs: deep-equal after recursive key-sort (terrain_types.json deferred per P-2) ---
        def _normalise(obj):
            if isinstance(obj, dict):
                return {k: _normalise(obj[k]) for k in sorted(obj)}
            if isinstance(obj, list):
                return [_normalise(x) for x in obj]
            return obj

        @pytest.mark.parametrize("name", [
            "lookup_barony_colors.json", "lookup_condado_colors.json",
            "territory_metadata.json", "mountain_river_data.json",
        ])
        def test_json_deep_equal(pipeline_output, golden_dir, name):
            actual = _normalise(json.loads((pipeline_output / name).read_text(encoding="utf-8")))
            golden = _normalise(json.loads((golden_dir / name).read_text(encoding="utf-8")))
            assert actual == golden, (
                f"{name}: JSON mismatch.\n"
                f"  hint: diff <(jq -S . {golden_dir / name}) <(jq -S . {pipeline_output / name})"
            )

    **`backend/tests/unit/test_pipeline_module.py`** — verifies run_pipeline
    exists with the right signature:

        import inspect
        from medieval_forge.services.pipeline import run_pipeline, RegionConfig

        def test_run_pipeline_signature():
            sig = inspect.signature(run_pipeline)
            assert list(sig.parameters.keys()) == ["cfg"], f"expected single cfg arg, got {sig.parameters}"
            ann = sig.parameters["cfg"].annotation
            assert ann is RegionConfig or ann == "RegionConfig"

        def test_region_config_is_dataclass():
            from dataclasses import is_dataclass
            assert is_dataclass(RegionConfig), "RegionConfig must be @dataclass per RESEARCH §9 Q1"

    **`backend/tests/unit/test_pipeline_cli.py`** — smoke tests the CLI:

        import subprocess
        import sys

        def test_main_cli_help():
            result = subprocess.run(
                [sys.executable, "-m", "medieval_forge.services.pipeline", "--help"],
                capture_output=True, text=True, timeout=15,
            )
            assert result.returncode == 0
            assert "iberia_868" in result.stdout

        # The end-to-end smoke is covered by the parity session fixture — no separate
        # @pytest.mark.slow CLI run needed (would double the CI time).

    **`backend/tests/integration/test_app_boot.py`** — extends or creates the
    file. If it already exists from Phase 00, ADD the test below; if not, create
    with this body:

        import pytest
        from fastapi.testclient import TestClient
        from medieval_forge.main import app

        @pytest.mark.integration
        def test_root_returns_200():
            with TestClient(app) as client:
                r = client.get("/")
                assert r.status_code == 200, "Phase 00 SC-6 invariant: / must return 200 after generator delete"

    Local sanity checks before commit (do NOT block on parity green — that's
    Task 3's checkpoint):

        pytest backend/tests/unit/ -v
        pytest backend/tests/integration/ -v

    Commit:
        test(01): parity harness + unit + integration tests for v3 pipeline
  </action>
  <verify>
    <automated>test -f backend/tests/parity/__init__.py &amp;&amp; test -f backend/tests/parity/conftest.py &amp;&amp; test -f backend/tests/parity/test_iberia_868.py &amp;&amp; test -f backend/tests/unit/test_pipeline_module.py &amp;&amp; test -f backend/tests/unit/test_pipeline_cli.py &amp;&amp; test -f backend/tests/integration/test_app_boot.py &amp;&amp; pytest backend/tests/parity/test_iberia_868.py --collect-only -q | grep -E "test_lookup_png_byte_equal|test_visual_png_ssim|test_json_deep_equal" | wc -l | awk '$1 == 10 {exit 0} {exit 1}' &amp;&amp; pytest backend/tests/unit/test_pipeline_module.py backend/tests/unit/test_pipeline_cli.py backend/tests/integration/test_app_boot.py -v</automated>
  </verify>
  <files>
    backend/tests/parity/__init__.py
    backend/tests/parity/conftest.py
    backend/tests/parity/test_iberia_868.py
    backend/tests/unit/test_pipeline_module.py
    backend/tests/unit/test_pipeline_cli.py
    backend/tests/integration/test_app_boot.py
  </files>
  <acceptance_criteria>
    - All 6 files exist.
    - `pytest backend/tests/parity/test_iberia_868.py --collect-only -q` collects exactly 10 tests (2 + 4 + 4) — terrain_lookup.png and terrain_types.json NOT in the parametrise lists.
    - `grep -q "scope=\"session\"" backend/tests/parity/conftest.py` matches (RESEARCH §9 Q4 — session-scoped fixture).
    - `grep -q "@pytest.mark.parity" backend/tests/parity/test_iberia_868.py` OR `pytestmark = pytest.mark.parity` at module level matches.
    - `grep -E "np.array_equal" backend/tests/parity/test_iberia_868.py` matches (D-12 byte-equal for lookup PNGs).
    - `grep -E "structural_similarity|ssim" backend/tests/parity/test_iberia_868.py` matches (D-12 SSIM for visual PNGs).
    - `grep -q "0.98" backend/tests/parity/test_iberia_868.py` matches (D-12 SSIM threshold).
    - `pytest backend/tests/unit/test_pipeline_module.py backend/tests/unit/test_pipeline_cli.py backend/tests/integration/test_app_boot.py -v` — all three pass (3 unit + 1 integration tests green).
    - The parity test FILES exist but parity tests themselves are NOT yet asserted to pass — Task 3 is the local-green checkpoint for parity.
    - Commit message: `test(01): parity harness + unit + integration tests for v3 pipeline`.
  </acceptance_criteria>
  <done>Parity harness, two unit tests, and one integration test are committed. Parity collection shows 10 tests. Unit + integration tests pass. Task 3 is the next checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Local-green checkpoint — observe `pytest -m parity` exits 0 before flipping CI</name>
  <read_first>
    - .planning/phases/01-pipeline-parity-port-harness-together/01-RESEARCH.md §7 (CI flip plan — local-green observation BEFORE the flip is non-negotiable)
    - .planning/phases/01-pipeline-parity-port-harness-together/01-VALIDATION.md (sampling rate — full suite green before /gsd-verify-work)
    - .planning/phases/01-pipeline-parity-port-harness-together/PREFLIGHT.md (P-1 / Q10 verdicts that drive parity outcome)
  </read_first>
  <what-built>
    The full Phase 01 port + parity harness. This checkpoint asserts that
    parity is green LOCALLY before the CI flip. RESEARCH §7 is explicit:
    flipping CI to non-skippable while parity is red would gate Phase 01's
    own merge against itself.
  </what-built>
  <how-to-verify>
    1. From the repo root, run the full Phase 01 verification:

           pytest backend/tests/parity/ -v -m parity

       Expected: 10 tests pass (2 lookup byte-equal + 4 visual SSIM + 4 JSON deep-equal).
       Expected runtime: ~45-90 s (one ~45 s pipeline run via the session fixture
       + per-file comparisons).

    2. Run the full suite:

           pytest backend/tests/ -v -m "parity or integration or not slow"

       Expected: zero failures.

    3. Run the standalone CLI once more:

           rm -rf /tmp/iberia_final
           python -m medieval_forge.services.pipeline --region iberia_868 --out /tmp/iberia_final
           ls /tmp/iberia_final | wc -l

       Expected output: `10` (the 10 in-scope contract files).

    4. Outcomes:
       - If all three checks pass: type "approved" and proceed to Task 4 (CI flip).
       - If parity tests fail: do NOT flip CI. Triage by reading the per-test
         failure messages — they include golden-vs-actual paths and a `diff` hint.
         Common failure modes:
         * Byte mismatch on lookup_barony.png — likely an off-by-one in the RGB
           hash or a NEAREST upscale regression. Inspect `DIFF_lookup_barony.png`
           that the test wrote to tmp_path.
         * SSIM < 0.98 on visual_*.png — likely a render.py drift (e.g. `default_rng`
           seed mismatch, border-on-land guard removed, draw_names default wrong).
         * JSON deep-equal failure on territory_metadata.json — likely a P-1
           mismatch: PREFLIGHT.md Q8 verdict was "PRESENT" but export.py shipped
           without `original_idx`, or the reverse.
         Fix the issue in a follow-up commit (`fix(01): <symptom>`) and re-run.
         Re-run this checkpoint after fixes.
  </how-to-verify>
  <action>
    Manual verification — operator runs the three commands above and reports the
    outcomes. The CI flip in Task 4 MUST NOT happen until this checkpoint is
    "approved" per RESEARCH §7.
  </action>
  <verify>
    <automated>pytest backend/tests/parity/ -v -m parity --tb=short 2>&amp;1 | tee /tmp/parity_run.log | grep -E "^(PASSED|FAILED|ERROR)" || true ; pytest backend/tests/parity/ -m parity -q 2>/dev/null | grep -E "10 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/parity/ -v -m parity` reports exactly 10 passed, 0 failed, 0 errors.
    - `pytest backend/tests/ -v -m "parity or integration or not slow"` reports 0 failures.
    - The standalone CLI run produces 10 output files in `/tmp/iberia_final/`.
    - The operator types "approved" with a one-line summary of the parity outcome (e.g. "approved — 10/10 parity passed, lookup PNGs byte-equal, all SSIM ≥ 0.998").
  </acceptance_criteria>
  <resume-signal>Type "approved" once all three checks pass; or describe the failure in detail (which test, what the diff message said). If failure, do NOT proceed to Task 4 — triage and fix first.</resume-signal>
  <done>Parity is locally green. CI flip in Task 4 is now safe.</done>
</task>

<task type="auto">
  <name>Task 4: Flip CI parity job to non-skippable + raise coverage gate to 85% (FINAL commit of phase)</name>
  <read_first>
    - .github/workflows/ci.yml (current state — line 24 coverage gate at 60%; line 40 has the `|| (echo …; exit 0)` placeholder tail)
    - .planning/phases/01-pipeline-parity-port-harness-together/01-RESEARCH.md §7 (closing-commit sequence: this task is commit 4 of the closing sub-task; the LAST commit of Phase 01)
    - .planning/phases/01-pipeline-parity-port-harness-together/01-VALIDATION.md (manual-only verification: confirm CI parity job non-skippable post-flip)
    - .planning/phases/01-pipeline-parity-port-harness-together/01-CONTEXT.md (D-08 frontend untouched — vitest + playwright jobs stay scaffolded but optional)
  </read_first>
  <action>
    Two edits to `.github/workflows/ci.yml`, single atomic commit. Per RESEARCH
    §7 this MUST be the last commit of Phase 01.

    **Edit 1 — pytest-unit coverage gate (line 24):**

        - run: pytest backend/tests/unit/ -v --cov=medieval_forge --cov-fail-under=60
        + run: pytest backend/tests/unit/ -v --cov=medieval_forge --cov-fail-under=85

    Update the comment immediately above line 24 to reflect the bump:

        - # Coverage gate starts at 60% for Phase 00; Phase 01 raises to 85%.
        + # Coverage gate raised from 60% (Phase 00) to 85% (Phase 01) per ROADMAP.

    **Edit 2 — pytest-parity job non-skippable (line 40):**

        - run: pytest backend/tests/parity/ backend/tests/integration/ -v -m "parity or integration" --no-header || (echo "No parity/integration tests yet (Phase 00) — passing as placeholder"; exit 0)
        + run: pytest backend/tests/parity/ backend/tests/integration/ -v -m "parity or integration" --no-header

    Drop the `|| (echo …; exit 0)` tail entirely. Update the comment above:

        - # Non-skippable from Phase 01 forward. Phase 00 may have no parity tests yet (will pass empty).
        + # Non-skippable from Phase 01. Any parity-test failure blocks merge.

    Sanity check: re-read the file, confirm line 24 ends with `--cov-fail-under=85`
    (no trailing characters) and line ~40 ends with `--no-header` (no `|| ` tail).

    Commit (THE LAST COMMIT OF THE PHASE):
        ci(01): flip pytest-parity job to required + raise coverage gate to 85%

    Karpathy compliance: this is a 2-line CI edit, not a refactor. Do NOT
    touch the vitest or playwright-uat jobs (D-08 — frontend untouched in
    Phase 01).
  </action>
  <verify>
    <automated>grep -q "cov-fail-under=85" .github/workflows/ci.yml &amp;&amp; ! grep -q "cov-fail-under=60" .github/workflows/ci.yml &amp;&amp; ! grep -q "exit 0" .github/workflows/ci.yml &amp;&amp; ! grep -q "passing as placeholder" .github/workflows/ci.yml &amp;&amp; grep -q "Non-skippable from Phase 01" .github/workflows/ci.yml</automated>
  </verify>
  <files>.github/workflows/ci.yml</files>
  <acceptance_criteria>
    - `grep -q "cov-fail-under=85" .github/workflows/ci.yml` matches.
    - `! grep -q "cov-fail-under=60" .github/workflows/ci.yml` (the old 60% gate is gone).
    - `! grep -q "exit 0" .github/workflows/ci.yml` (the `|| (echo …; exit 0)` tail is gone — no other exit-0 escape hatches added by accident).
    - `! grep -q "passing as placeholder" .github/workflows/ci.yml` (the placeholder comment is gone).
    - `grep -q "Non-skippable from Phase 01" .github/workflows/ci.yml` matches (updated comment).
    - The vitest and playwright-uat jobs are unchanged: `git diff HEAD~1 -- .github/workflows/ci.yml` shows edits ONLY in lines belonging to pytest-unit and pytest-parity jobs (D-08 — frontend untouched).
    - This is the LAST commit on the Phase 01 branch — no commits follow.
    - Commit message: `ci(01): flip pytest-parity job to required + raise coverage gate to 85%`.
  </acceptance_criteria>
  <done>CI parity job is non-skippable; coverage gate raised to 85%. ROADMAP success criterion 3 met. Phase 01 closes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem deletes (Task 1) | Surgical removal of v1 generator stack. Risk: surviving consumer ImportError if the deletion graph trace missed an importer. RESEARCH §3.b enumerates all surviving consumers. |
| Test fixture reads (Task 2) | `tests/fixtures/iberia_868/golden/` is committed in-tree (Plan 01 Task 3). Tests read; never write. |
| CI workflow edit (Task 4) | `.github/workflows/ci.yml` runs in the CI environment; no local secrets exposed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Path traversal in pipeline `--out` (used by parity conftest via tmp_path_factory) | mitigate | `tmp_path_factory.mktemp` returns a sandboxed path under pytest's tmp tree; no user input. Severity: low. |
| T-03-02 | Tampering | Fixture tampering — golden PNG/JSON edited in-tree | mitigate | D-10 explicit `docs(parity): refresh iberia_868 baseline` commit convention; CI parity reads the committed fixture; any silent edit fails PR review. Severity: medium → mitigation acceptable. |
| T-03-03 | Tampering | LFS pointer integrity for PT GeoJSON (consumed by run_pipeline via session fixture) | mitigate | Broken LFS smudge yields a 134-byte text file → `decode_topojson_municipalities` raises on first load → parity test fails fast within 45 s with a clear error message. Severity: medium → mitigation acceptable. |
| T-03-04 | Denial of service | Malformed input GeoJSON/TopoJSON | accept | Local-only tool; `MemoryError` on the 28 MB GeoJSON parse acceptable. Severity: low. |
| T-03-05 | Tampering | CI workflow privilege escalation via the parity job | mitigate | The flip removes only the `|| exit 0` escape hatch; it does NOT add new permissions, secrets, or steps. The job continues to run pytest only. Severity: low. |
| T-03-06 | Information disclosure | None — phase ships zero secrets, no network call in test path | accept | Phase 01 is offline-only. No mitigation needed. |

**Block-on threshold:** high. None of the above hit high. Gate passes.
</threat_model>

<verification>
After all 4 tasks complete:

1. `pytest backend/tests/parity/ -v -m parity` reports 10 passed (2 byte-equal + 4 SSIM + 4 deep-equal). [ROADMAP SC-1]
2. `python -m medieval_forge.services.pipeline --region iberia_868 --out /tmp/X` exits 0 with 10 files. [ROADMAP SC-2]
3. `.github/workflows/ci.yml` no longer contains `exit 0` or `cov-fail-under=60`. [ROADMAP SC-3]
4. `medieval-forge start` boots and `/` returns 200. [Phase 00 SC-6 invariant]
5. `pytest backend/tests/ -v -m "parity or integration or not slow"` exits 0.
6. `pytest backend/tests/unit/ --cov=medieval_forge --cov-fail-under=85` exits 0 (the new gate passes locally).
7. `git log --oneline | head -20` shows 4 commits with prefixes: `chore(01): delete v1 generator stack`, `test(01): parity harness`, `ci(01): flip pytest-parity`. (Task 3 is a checkpoint, no commit.)
</verification>

<success_criteria>
- 5 v1 production files + lib/__init__.py + 7 test files deleted.
- main.py edit removes the api.generate import + include_router; FastAPI app still boots.
- 6 new test files committed (parity __init__ + conftest + test_iberia_868 + unit/test_pipeline_module + unit/test_pipeline_cli + integration/test_app_boot).
- Parity test passes locally (10/10).
- CI parity job is non-skippable (no `|| exit 0`); coverage gate at 85%.
- ROADMAP success criteria 1-3 all met.
- Phase 01 closes with 4 commits in this plan (3 atomic + 1 final CI flip).
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-parity-port-harness-together/01-03-SUMMARY.md` with:
- Final list of deleted files (production + tests) with line counts.
- Parity test outcomes (10/10 passed; SSIM scores per visual PNG; JSON sizes).
- CI flip diff (the 2-line ci.yml edit).
- Phase 01 closing notes: ROADMAP SC-1/2/3 status, what Phase 02 inherits (run_pipeline + RegionConfig + REGIONS + parity harness still gating).
- Update `.planning/STATE.md` to mark Phase 01 complete and set Phase 02 (Ingestion adapter) as the next-up phase.
</output>
