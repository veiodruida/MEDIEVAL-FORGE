---
phase: 01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/iberia_868/golden/lookup_barony.png
  - tests/fixtures/iberia_868/golden/lookup_barony_colors.json
  - tests/fixtures/iberia_868/golden/lookup_condado.png
  - tests/fixtures/iberia_868/golden/lookup_condado_colors.json
  - tests/fixtures/iberia_868/golden/territory_metadata.json
  - tests/fixtures/iberia_868/golden/visual_condado.png
  - tests/fixtures/iberia_868/golden/visual_barony.png
  - tests/fixtures/iberia_868/golden/mountains_mask.png
  - tests/fixtures/iberia_868/golden/rivers_overlay.png
  - tests/fixtures/iberia_868/golden/mountain_river_data.json
  - tests/fixtures/iberia_868/golden/README.md
  - data/regions/iberia_868/inputs/pt_concelhos_wgs84.geojson
  - data/regions/iberia_868/inputs/es-atlas-pkg/package/es/municipalities.json
  - data/regions/iberia_868/inputs/mountain_river_data.json
  - .gitattributes
  - backend/medieval_forge/data/__init__.py
  - backend/medieval_forge/data/regions/__init__.py
  - backend/medieval_forge/data/regions/iberia_868/__init__.py
  - backend/medieval_forge/data/regions/iberia_868/territory_data.py
  - backend/medieval_forge/services/pipeline/__init__.py
  - backend/medieval_forge/services/pipeline/contracts.py
  - backend/medieval_forge/services/pipeline/regions.py
  - backend/medieval_forge/services/pipeline/landmask.py
  - backend/medieval_forge/services/pipeline/border.py
  - backend/medieval_forge/services/pipeline/voronoi.py
  - backend/medieval_forge/services/pipeline/cleanup.py
  - backend/medieval_forge/services/pipeline/render.py
  - backend/medieval_forge/services/pipeline/lookup.py
  - backend/medieval_forge/services/pipeline/export.py
  - backend/medieval_forge/services/pipeline/__main__.py
  - .planning/phases/01-pipeline-parity-port-harness-together/PREFLIGHT.md
autonomous: false
requirements:
  - V3-PIPELINE-PARITY

must_haves:
  truths:
    - "Wave 0 preflight resolves P-1 (original_idx in deployed territory_metadata.json) before any port code runs"
    - "Wave 0 preflight resolves Q10 (draw_names True/False against deployed visual_condado.png) before any port code runs"
    - "Repo is self-contained for Iberia 868 — clone + install yields golden fixtures + pipeline inputs on disk under tests/fixtures/iberia_868/golden/ and data/regions/iberia_868/inputs/"
    - "Territory data lives inside the package at backend/medieval_forge/data/regions/iberia_868/territory_data.py with KINGDOMS, DUCHIES, CONDADOS importable via `from ...data.regions.iberia_868.territory_data import KINGDOMS, DUCHIES, CONDADOS`"
    - "9-submodule pipeline package exists at backend/medieval_forge/services/pipeline/ with empty stubs for landmask/border/voronoi/cleanup/render/lookup/export plus contracts.py carrying RegionConfig dataclass"
    - "RegionConfig is an @dataclass (NOT pydantic) carrying every inicio field plus the 4 new fields (kingdoms, duchies, condados, rng_seed, draw_names)"
    - "pipeline/__init__.py exports run_pipeline; pipeline/__main__.py provides argparse CLI; pipeline/regions.py exports REGIONS = {'iberia_868': iberia_config}"
  artifacts:
    - path: "tests/fixtures/iberia_868/golden/"
      provides: "11 deployed Reconquista files (12 minus terrain_lookup.png deferred per P-2) for parity comparison"
      contains: "lookup_barony.png, lookup_condado.png, lookup_barony_colors.json, lookup_condado_colors.json, visual_condado.png, visual_barony.png, mountains_mask.png, rivers_overlay.png, territory_metadata.json, mountain_river_data.json, README.md (notes terrain_lookup.png and terrain_types.json deferred to Phase 06)"
    - path: "data/regions/iberia_868/inputs/"
      provides: "Pipeline inputs in-tree so pytest tests/parity/ runs without external paths"
      contains: "pt_concelhos_wgs84.geojson (LFS), es-atlas-pkg/package/es/municipalities.json, mountain_river_data.json"
    - path: ".gitattributes"
      provides: "LFS pointer for the 29.7 MB PT GeoJSON"
      contains: "data/regions/iberia_868/inputs/pt_concelhos_wgs84.geojson filter=lfs diff=lfs merge=lfs -text"
    - path: "backend/medieval_forge/data/regions/iberia_868/territory_data.py"
      provides: "KINGDOMS, DUCHIES, CONDADOS as Python module-level constants; identical content to inicio/territory_data_v3.py"
      exports: ["KINGDOMS", "DUCHIES", "CONDADOS"]
    - path: "backend/medieval_forge/services/pipeline/__init__.py"
      provides: "run_pipeline(cfg) entry point; placeholder body raising NotImplementedError until Plan 02 fills it"
      exports: ["run_pipeline"]
    - path: "backend/medieval_forge/services/pipeline/contracts.py"
      provides: "RegionConfig @dataclass + geo_to_pixel / pixel_to_geo / point_in_polygon shared transforms"
      exports: ["RegionConfig", "geo_to_pixel", "pixel_to_geo", "point_in_polygon"]
    - path: "backend/medieval_forge/services/pipeline/regions.py"
      provides: "REGIONS = {'iberia_868': iberia_config} factory registry per D-04"
      exports: ["REGIONS", "iberia_config"]
    - path: ".planning/phases/01-pipeline-parity-port-harness-together/PREFLIGHT.md"
      provides: "Recorded resolution of Open Q8 (P-1: original_idx) and Open Q10 (draw_names) so Plan 02 implementers cannot drift"
      contains: "P-1 verdict, Q10 verdict, jq output excerpt, ES TopoJSON sourcing method, LFS configuration"
  key_links:
    - from: "backend/medieval_forge/services/pipeline/contracts.py"
      to: "backend/medieval_forge/data/regions/iberia_868/territory_data.py"
      via: "iberia_config() in regions.py imports KINGDOMS/DUCHIES/CONDADOS via 'from ...data.regions.iberia_868.territory_data import ...'"
      pattern: "from \\.\\.\\.data\\.regions\\.iberia_868\\.territory_data import"
    - from: "backend/medieval_forge/services/pipeline/regions.py"
      to: "data/regions/iberia_868/inputs/"
      via: "iberia_config() points municipality_pt_geojson, municipality_es_topojson, mountain_river_json at the in-repo paths under data/regions/iberia_868/inputs/ (D-11)"
      pattern: "data/regions/iberia_868/inputs"
    - from: "backend/medieval_forge/services/pipeline/__main__.py"
      to: "backend/medieval_forge/services/pipeline/regions.py"
      via: "argparse --region looks up REGIONS[args.region]() then calls run_pipeline(cfg)"
      pattern: "REGIONS\\[args\\.region\\]"
---

<objective>
Wave 0 of Phase 01: pre-flight verification and scaffolding. Resolve the two parity-affecting unknowns (P-1 `original_idx`, Q10 `draw_names`), source ES TopoJSON, configure LFS, snapshot golden fixtures, move territory data into the package, and lay down the empty 9-submodule `pipeline/` skeleton with `RegionConfig`. The port itself happens in Plan 02.

Purpose: Plan 02's verbatim port cannot start until (a) we know which P-1/Q10 path produces parity, (b) the golden fixtures and pipeline inputs exist in-tree, and (c) the package skeleton + RegionConfig dataclass anchor every port submodule. Splitting these from the port also keeps each plan inside the ~50% context budget.

Output: 11 golden fixtures + 3 pipeline inputs + LFS + territory_data move + 9-submodule skeleton + RegionConfig + iberia_config + PREFLIGHT.md recording the verdicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pipeline-parity-port-harness-together/01-CONTEXT.md
@.planning/phases/01-pipeline-parity-port-harness-together/01-RESEARCH.md
@.planning/phases/01-pipeline-parity-port-harness-together/01-VALIDATION.md
@CLAUDE.md
@.claude/skills/karpathy/SKILL.md
@inicio/map_generator.py
@inicio/territory_data_v3.py
@pyproject.toml

<interfaces>
<!-- RegionConfig contract from RESEARCH §2.a — every field listed verbatim with default + line ref. -->

From inicio/map_generator.py:52-112 (the port target):

```python
@dataclass
class RegionConfig:
    name: str = "iberia"
    map_w: int = 1920
    map_h: int = 1080
    upscale: int = 2
    lon_min: float = -13.2
    lon_max: float = 8.2
    lat_min: float = 35.4
    lat_max: float = 44.6
    lon_scale: float = field(default=None)  # __post_init__ derives cos(center_lat)
    output_dir: str = "../Assets/StreamingAssets/Maps"
    municipality_pt_geojson: str = None
    municipality_es_topojson: str = None
    mountain_river_json: str = None
    border_polygon: list = field(default_factory=list)  # list[(lon,lat)]
    pt_duchies: set = field(default_factory=set)
    kingdom_colors: dict = field(default_factory=dict)
    ocean_near: tuple = (45, 90, 140)
    ocean_far: tuple = (70, 130, 180)
    ocean_gradient_dist: float = 150.0
    coast_inner_width: int = 2
    coast_inner_color: tuple = (15, 10, 5)
    island_min_px: int = 300
    fragment_min_px: int = 600
    blob_merge_px: int = 200
    median_passes: int = 8
    smooth_sigma: float = 3.0
    mountain_threshold: int = 1500
    mountain_color_visual: tuple = (118, 100, 80)
    mountain_color_lookup: tuple = (50, 45, 40)
    mountain_noise: int = 20
    river_color: tuple = (74, 144, 217)
    river_width: int = 2

    def __post_init__(self):
        if self.lon_scale is None:
            center_lat = (self.lat_min + self.lat_max) / 2
            self.lon_scale = math.cos(math.radians(center_lat))
```

NEW fields the port adds (per RESEARCH §2.a "New fields the port introduces"):

```python
    # D-14: territory data lives directly on cfg
    kingdoms: dict = field(default_factory=dict)
    duchies: dict = field(default_factory=dict)
    condados: list = field(default_factory=list)
    # CLAUDE.md: np.random.default_rng(cfg.rng_seed) replaces hardcoded 42 at inicio lines 537 + 904
    rng_seed: int = 42
    # D-03: generate_maps(draw_names=...) becomes cfg.draw_names; default determined by Q10 verification
    draw_names: bool = False  # adjust per PREFLIGHT.md Q10 verdict
```

Shared transforms (RESEARCH §1, inicio §2 lines 149-185) — placeholder signatures (bodies in Plan 02):

```python
def geo_to_pixel(lon: float, lat: float, cfg: RegionConfig) -> tuple[int, int]: ...
def pixel_to_geo(x: int, y: int, cfg: RegionConfig) -> tuple[float, float]: ...
def point_in_polygon(point: tuple[float, float], polygon: list[tuple[float, float]]) -> bool: ...
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Preflight verification — P-1 (original_idx) + Q10 (draw_names)</name>
  <read_first>
    - .planning/phases/01-pipeline-parity-port-harness-together/01-RESEARCH.md (§6 Pitfall P-1 lines around `original_idx`; §6 Pitfall P-2 for terrain_lookup defer; §9 Open Q8 + Q10)
    - inicio/map_generator.py lines 676-726 (export_metadata — confirms `original_idx` is NOT emitted by inicio)
    - CLAUDE.md (non-negotiable rule #4 — `original_idx` in every territory)
    - .planning/phases/01-pipeline-parity-port-harness-together/01-CONTEXT.md (D-09 deployed-wins)
  </read_first>
  <what-built>
    Two one-time inspections of the deployed Reconquista source-of-truth that
    cannot be automated:
    (a) Run `jq '.condados[0] | keys' D:/Projetos_Jogo/Reconquista/Assets/StreamingAssets/Maps/territory_metadata.json`
        and `jq '.baronies[0] | keys' D:/Projetos_Jogo/Reconquista/Assets/StreamingAssets/Maps/territory_metadata.json`
        to determine whether `original_idx` is present per condado/barony entry.
    (b) Open `D:/Projetos_Jogo/Reconquista/Assets/StreamingAssets/Maps/visual_condado.png` in
        an image viewer (Windows Photos / IrfanView / equivalent) and visually inspect
        whether condado labels (e.g. "Najera", "Astorga", "Coimbra") are drawn on the
        map. Record yes/no.
  </what-built>
  <how-to-verify>
    1. Open a shell where `jq` is available (Git Bash + jq installed, or WSL).
    2. Run:
       `jq '.condados[0] | keys' "D:/Projetos_Jogo/Reconquista/Assets/StreamingAssets/Maps/territory_metadata.json"`
       Record the output (list of keys present in the first condado entry).
    3. Run the same for `.baronies[0]` and `.condados | map(select(.original_idx != null)) | length`.
    4. Open `D:/Projetos_Jogo/Reconquista/Assets/StreamingAssets/Maps/visual_condado.png`
       and check whether territory names are rendered as text on the map.
    5. Create `.planning/phases/01-pipeline-parity-port-harness-together/PREFLIGHT.md` with:
       - **Q8 (P-1) verdict:** `original_idx PRESENT` or `original_idx ABSENT` + the literal jq output.
       - **Q10 verdict:** `draw_names = True` or `draw_names = False` + one-line note on what was visible.
       - These verdicts feed Plan 02 Task `Port §11 export.py` (P-1) and Plan 02 Task `Configure iberia_config()` (Q10).
    6. Commit: `docs(01): record P-1 and Q10 preflight verdicts`.
  </how-to-verify>
  <action>
    Manual-only — operator runs the jq commands and visual inspection per the steps
    above. The verdicts go into PREFLIGHT.md and are referenced in subsequent
    task acceptance criteria. **DO NOT proceed to Task 2 until PREFLIGHT.md is
    committed.**
  </action>
  <verify>
    <automated>test -f .planning/phases/01-pipeline-parity-port-harness-together/PREFLIGHT.md &amp;&amp; grep -q "Q8 (P-1) verdict" .planning/phases/01-pipeline-parity-port-harness-together/PREFLIGHT.md &amp;&amp; grep -q "Q10 verdict" .planning/phases/01-pipeline-parity-port-harness-together/PREFLIGHT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/01-pipeline-parity-port-harness-together/PREFLIGHT.md` exists.
    - Grep `"Q8 (P-1) verdict"` matches and the line states one of: `original_idx PRESENT` / `original_idx ABSENT`.
    - Grep `"Q10 verdict"` matches and the line states one of: `draw_names = True` / `draw_names = False`.
    - The PREFLIGHT.md includes a literal jq output excerpt (verbatim copy of the keys list).
    - Commit `docs(01): record P-1 and Q10 preflight verdicts` exists in git log.
  </acceptance_criteria>
  <resume-signal>Type "approved" once PREFLIGHT.md is committed; type the verdicts inline (e.g. "P-1 PRESENT, draw_names False") so the next task acceptance criteria reference them.</resume-signal>
  <done>PREFLIGHT.md committed with both verdicts; Plan 02 has unambiguous guidance for export.py and iberia_config().</done>
</task>

<task type="auto">
  <name>Task 2: Source ES TopoJSON + commit pipeline inputs (D-11) + LFS for PT GeoJSON (Q12)</name>
  <read_first>
    - .planning/phases/01-pipeline-parity-port-harness-together/01-RESEARCH.md §4.b (ES TopoJSON sourcing options + LFS recommendation) and §9 Open Q11/Q12
    - inicio/licoes/MAPA_V2_GEOJSON_BRIEFING.md (line ~30 references the es-atlas GitHub raw URL — historical context only)
    - inicio/map_generator.py lines 122-124 (where iberia_config originally pointed at `es-atlas-pkg/package/es/municipalities.json` and `mountain_river_data.json`)
    - .planning/phases/01-pipeline-parity-port-harness-together/01-CONTEXT.md (D-11 inputs location)
  </read_first>
  <action>
    Three sub-steps in this single atomic commit:

    (1) Initialise git-lfs and add the LFS attribute for the PT GeoJSON. Add to repo
        root `.gitattributes` (create if missing) the line:

            data/regions/iberia_868/inputs/pt_concelhos_wgs84.geojson filter=lfs diff=lfs merge=lfs -text

        Then run `git lfs install` once locally. If `git lfs` is unavailable, fall
        back to direct commit of the 29.7 MB file and update `.gitattributes` accordingly
        (record the fallback in PREFLIGHT.md "Q12 LFS verdict" line).

    (2) Copy inputs into `data/regions/iberia_868/inputs/`:
        - `pt_concelhos_wgs84.geojson` (29.7 MB) from
          `D:/Projetos_Jogo/Reconquista/Assets/Downloads/handoff/pt_concelhos_wgs84.geojson`.
        - `mountain_river_data.json` (19 KB) from `inicio/mountain_river_data.json`.

    (3) Source the ES TopoJSON (Open Q11). Pick ONE method:
        - Method A (preferred for reproducibility): `npm pack es-atlas` in a scratch
          directory, extract `package/es/municipalities.json`, copy to
          `data/regions/iberia_868/inputs/es-atlas-pkg/package/es/municipalities.json`
          (preserve the `es-atlas-pkg/package/es/` subpath because inicio's
          `iberia_config()` line 123 expects exactly that string).
        - Method B (fallback): direct download from
          `https://raw.githubusercontent.com/martgnz/es-atlas/master/data/es/municipalities.json`
          to the same target path. Record the commit SHA of the source in PREFLIGHT.md
          "Q11 source" line for reproducibility.

    Commit:
        chore(01): commit Iberia 868 pipeline inputs + LFS for PT GeoJSON

    Per RESEARCH §7 closing-commit sequence, this is one of the three "fixture & input"
    commits before the parity test lands.
  </action>
  <verify>
    <automated>test -f data/regions/iberia_868/inputs/pt_concelhos_wgs84.geojson &amp;&amp; test -f data/regions/iberia_868/inputs/mountain_river_data.json &amp;&amp; test -f data/regions/iberia_868/inputs/es-atlas-pkg/package/es/municipalities.json &amp;&amp; grep -q "data/regions/iberia_868/inputs/pt_concelhos_wgs84.geojson" .gitattributes</automated>
  </verify>
  <files>
    .gitattributes
    data/regions/iberia_868/inputs/pt_concelhos_wgs84.geojson
    data/regions/iberia_868/inputs/mountain_river_data.json
    data/regions/iberia_868/inputs/es-atlas-pkg/package/es/municipalities.json
  </files>
  <acceptance_criteria>
    - `test -f data/regions/iberia_868/inputs/pt_concelhos_wgs84.geojson` exits 0.
    - `test -f data/regions/iberia_868/inputs/mountain_river_data.json` exits 0.
    - `test -f data/regions/iberia_868/inputs/es-atlas-pkg/package/es/municipalities.json` exits 0.
    - The path `data/regions/iberia_868/inputs/es-atlas-pkg/package/es/municipalities.json` is verbatim — required because inicio line 123 hardcodes `es-atlas-pkg/package/es/municipalities.json`.
    - `grep -q "pt_concelhos_wgs84.geojson filter=lfs" .gitattributes` matches OR PREFLIGHT.md records "Q12 LFS verdict: direct commit fallback" with reason.
    - PT GeoJSON file size: byte length matches source (29,705,375 B per RESEARCH §4.b) — verify with `wc -c data/regions/iberia_868/inputs/pt_concelhos_wgs84.geojson` (or under LFS, `git lfs ls-files` shows the file with the right size).
    - mountain_river_data.json size = 19,307 B per RESEARCH §4.b.
    - Commit message: `chore(01): commit Iberia 868 pipeline inputs + LFS for PT GeoJSON`.
  </acceptance_criteria>
  <done>Three inputs are in-tree under `data/regions/iberia_868/inputs/`; LFS configured for the 29.7 MB PT GeoJSON (or fallback documented in PREFLIGHT.md); paths match inicio's hardcoded relative paths so Plan 02's `iberia_config()` can point at them with one substitution.</done>
</task>

<task type="auto">
  <name>Task 3: Snapshot Iberia 868 golden fixtures (D-10) — 11 files, terrain_lookup deferred per P-2</name>
  <read_first>
    - .planning/phases/01-pipeline-parity-port-harness-together/01-RESEARCH.md §4.a (golden fixture inventory — 12 files listed with sizes)
    - .planning/phases/01-pipeline-parity-port-harness-together/01-RESEARCH.md §6 Pitfall P-2 (terrain_lookup.png + terrain_types.json deferred)
    - .planning/phases/01-pipeline-parity-port-harness-together/01-CONTEXT.md D-10 fixture location convention
    - CLAUDE.md (12-file Unity export contract — terrain_lookup.png is contract file #5 + terrain_types.json #6)
  </read_first>
  <action>
    Copy ten deployed Reconquista files plus the mountain_river_data.json output
    snapshot from `D:/Projetos_Jogo/Reconquista/Assets/StreamingAssets/Maps/` into
    `tests/fixtures/iberia_868/golden/`. The eleven files (10 contract files + 1
    README) are:

        lookup_barony.png            (54 KB)
        lookup_barony_colors.json    (5 KB)
        lookup_condado.png           (37 KB)
        lookup_condado_colors.json   (2 KB)
        territory_metadata.json      (64 KB)
        visual_condado.png           (455 KB)
        visual_barony.png            (493 KB)
        mountains_mask.png           (12 KB)
        rivers_overlay.png           (46 KB)
        mountain_river_data.json     (19 KB) — also in inputs/, byte-identical
        README.md                    (~30 lines, see below)

    Do **NOT** copy `terrain_lookup.png` or `terrain_types.json` — both are
    deferred to Phase 06 per RESEARCH §6 P-2 (inicio's pipeline never produces
    them; reproducing them would require porting code beyond inicio's 944 lines,
    which violates D-01 verbatim).

    Create `tests/fixtures/iberia_868/golden/README.md` with this exact content
    (substitute the date):

        # Iberia 868 Golden Fixtures

        Snapshot of `D:/Projetos_Jogo/Reconquista/Assets/StreamingAssets/Maps/`
        captured on YYYY-MM-DD for parity testing.

        ## Contract status (Phase 01)

        Phase 01 parity covers **10 of 12** contract files. The two deferred:
        - `terrain_lookup.png` — defer to Phase 06; inicio's pipeline does not produce it.
        - `terrain_types.json` — defer to Phase 06; companion of terrain_lookup.png.

        ## Refresh policy

        Updates are explicit: a single `docs(parity): refresh iberia_868 baseline`
        commit. Never silently re-snapshot — PR review catches drift.

    Commit:
        chore(01): commit Iberia 868 golden fixtures (10 of 12 contract files)

    Per RESEARCH §7, this is the second of the three fixture-and-input commits.
  </action>
  <verify>
    <automated>test -d tests/fixtures/iberia_868/golden &amp;&amp; ls tests/fixtures/iberia_868/golden/ | wc -l | awk '$1 == 11 {exit 0} {exit 1}' &amp;&amp; test -f tests/fixtures/iberia_868/golden/README.md &amp;&amp; ! test -f tests/fixtures/iberia_868/golden/terrain_lookup.png &amp;&amp; ! test -f tests/fixtures/iberia_868/golden/terrain_types.json</automated>
  </verify>
  <files>
    tests/fixtures/iberia_868/golden/lookup_barony.png
    tests/fixtures/iberia_868/golden/lookup_barony_colors.json
    tests/fixtures/iberia_868/golden/lookup_condado.png
    tests/fixtures/iberia_868/golden/lookup_condado_colors.json
    tests/fixtures/iberia_868/golden/territory_metadata.json
    tests/fixtures/iberia_868/golden/visual_condado.png
    tests/fixtures/iberia_868/golden/visual_barony.png
    tests/fixtures/iberia_868/golden/mountains_mask.png
    tests/fixtures/iberia_868/golden/rivers_overlay.png
    tests/fixtures/iberia_868/golden/mountain_river_data.json
    tests/fixtures/iberia_868/golden/README.md
  </files>
  <acceptance_criteria>
    - Directory `tests/fixtures/iberia_868/golden/` exists.
    - Exactly 11 entries inside (10 contract files + README.md). `ls tests/fixtures/iberia_868/golden/ | wc -l` returns 11.
    - File `terrain_lookup.png` does NOT exist in the directory: `! test -f tests/fixtures/iberia_868/golden/terrain_lookup.png` exits 0.
    - File `terrain_types.json` does NOT exist in the directory: `! test -f tests/fixtures/iberia_868/golden/terrain_types.json` exits 0.
    - README.md mentions both deferred files by name and references Phase 06.
    - File sizes match RESEARCH §4.a inventory within ±1 byte (filesystem tolerance for line-ending normalisation should not apply — these are binary PNGs and JSONs with `\n` only; verify with `wc -c`).
    - Commit message: `chore(01): commit Iberia 868 golden fixtures (10 of 12 contract files)`.
  </acceptance_criteria>
  <done>11-file snapshot in `tests/fixtures/iberia_868/golden/` with explicit deferral notes for terrain_lookup.png and terrain_types.json; ready for Plan 03's parity harness to consume via `golden_dir` fixture.</done>
</task>

<task type="auto">
  <name>Task 4: Move territory data into the package (D-13) + package markers</name>
  <read_first>
    - inicio/territory_data_v3.py (full file — KINGDOMS, DUCHIES, CONDADOS constants)
    - .planning/phases/01-pipeline-parity-port-harness-together/01-CONTEXT.md (D-13 + D-14 — static import from in-package module)
    - .planning/phases/01-pipeline-parity-port-harness-together/01-RESEARCH.md §2.c (recommended path: `backend/medieval_forge/data/regions/iberia_868/territory_data.py`)
    - pyproject.toml (verify `[tool.setuptools.packages.find]` already covers `medieval_forge*`)
  </read_first>
  <action>
    Create the in-package data tree and copy `inicio/territory_data_v3.py` into it:

        backend/medieval_forge/data/__init__.py                           # empty
        backend/medieval_forge/data/regions/__init__.py                   # empty
        backend/medieval_forge/data/regions/iberia_868/__init__.py        # empty
        backend/medieval_forge/data/regions/iberia_868/territory_data.py  # COPY of inicio/territory_data_v3.py, byte-identical content; only the file path changes

    Do NOT modify the constants `KINGDOMS`, `DUCHIES`, `CONDADOS` — D-13 is a
    move, not a rename. The original `inicio/territory_data_v3.py` stays in
    place; inicio remains the read-only gold-standard reference per CLAUDE.md.

    Verify the package import path works:

        python -c "from medieval_forge.data.regions.iberia_868.territory_data import KINGDOMS, DUCHIES, CONDADOS; print(len(KINGDOMS), len(DUCHIES), len(CONDADOS))"

    Per RESEARCH §2.c, no `pyproject.toml` change is needed because
    `[tool.setuptools.packages.find]` already includes `medieval_forge*` and
    each new directory has an `__init__.py`.

    Commit:
        feat(01): move territory data into package (D-13)
  </action>
  <verify>
    <automated>test -f backend/medieval_forge/data/regions/iberia_868/territory_data.py &amp;&amp; test -f backend/medieval_forge/data/__init__.py &amp;&amp; test -f backend/medieval_forge/data/regions/__init__.py &amp;&amp; test -f backend/medieval_forge/data/regions/iberia_868/__init__.py &amp;&amp; python -c "from medieval_forge.data.regions.iberia_868.territory_data import KINGDOMS, DUCHIES, CONDADOS; assert len(KINGDOMS) > 0 and len(DUCHIES) > 0 and len(CONDADOS) > 0"</automated>
  </verify>
  <files>
    backend/medieval_forge/data/__init__.py
    backend/medieval_forge/data/regions/__init__.py
    backend/medieval_forge/data/regions/iberia_868/__init__.py
    backend/medieval_forge/data/regions/iberia_868/territory_data.py
  </files>
  <acceptance_criteria>
    - All four files exist (three empty `__init__.py` + the territory_data.py).
    - `diff inicio/territory_data_v3.py backend/medieval_forge/data/regions/iberia_868/territory_data.py` shows zero differences (the file is a byte-for-byte copy).
    - The Python import succeeds: `python -c "from medieval_forge.data.regions.iberia_868.territory_data import KINGDOMS, DUCHIES, CONDADOS"` exits 0.
    - The constants are non-empty: KINGDOMS, DUCHIES, CONDADOS all have len > 0.
    - `inicio/territory_data_v3.py` remains in place (NOT deleted — inicio is read-only per CLAUDE.md).
    - Commit message: `feat(01): move territory data into package (D-13)`.
  </acceptance_criteria>
  <done>Territory data importable via `from medieval_forge.data.regions.iberia_868.territory_data import KINGDOMS, DUCHIES, CONDADOS`. Plan 02's `iberia_config()` uses this exact import path.</done>
</task>

<task type="auto">
  <name>Task 5: Scaffold pipeline package skeleton + RegionConfig dataclass + iberia_config + CLI shim</name>
  <read_first>
    - inicio/map_generator.py lines 49-145 (Section 1 — RegionConfig + iberia_config — the verbatim source for contracts.py + regions.py)
    - inicio/map_generator.py lines 149-185 (Section 2 — geo_to_pixel, pixel_to_geo, point_in_polygon — placeholder stubs, bodies in Plan 02)
    - .planning/phases/01-pipeline-parity-port-harness-together/01-RESEARCH.md §1 (submodule mapping table) + §2.a (RegionConfig fields including the 4 new ones) + §2.b (dataclass over pydantic) + §1's `pipeline/__main__.py` shim
    - .planning/phases/01-pipeline-parity-port-harness-together/01-CONTEXT.md (D-03 orchestrator location, D-04 hard-coded REGIONS, D-13 + D-14 territory data on cfg)
    - .planning/phases/01-pipeline-parity-port-harness-together/PREFLIGHT.md (Q10 verdict — sets `draw_names` default in iberia_config)
  </read_first>
  <action>
    Create the 9-submodule pipeline skeleton. Wave 1 (Plan 02) fills the bodies;
    Wave 0 (this task) only creates the files with correct imports, signatures,
    and `RegionConfig` + `iberia_config` populated. This guarantees that when
    Plan 02 ports each section, the import wiring is already correct and the
    dataclass contract cannot drift.

    File-by-file:

    **`backend/medieval_forge/services/pipeline/__init__.py`** (5-10 lines):

        """v3 deterministic pipeline — verbatim port of inicio/map_generator.py.

        Wave 0 stub. Wave 1 (Plan 02) implements run_pipeline.
        """
        from .contracts import RegionConfig

        def run_pipeline(cfg: RegionConfig) -> None:
            """Entry point per D-03. Body filled by Plan 02 (port of inicio §13)."""
            raise NotImplementedError("Plan 02 wires this to landmask → border → voronoi → cleanup → render → lookup → export")

        __all__ = ["RegionConfig", "run_pipeline"]

    **`backend/medieval_forge/services/pipeline/contracts.py`** (full RegionConfig
    dataclass per RESEARCH §2.a; verbatim port of inicio §1 lines 52-112 PLUS
    the 4 new fields). Use the EXACT field list and defaults from the
    `<interfaces>` block above. Use `@dataclass` decorator from `dataclasses` (NOT
    pydantic) — RESEARCH §9 Open Q1 recommendation. Field types and defaults
    must mirror inicio line-for-line.

    Also include placeholder bodies for the three Section 2 transforms:

        from typing import Tuple, List
        import math
        from dataclasses import dataclass, field

        @dataclass
        class RegionConfig:
            # ... [all 26 inicio fields + 5 new fields per <interfaces>] ...

        def geo_to_pixel(lon: float, lat: float, cfg: RegionConfig) -> Tuple[int, int]:
            """Verbatim port pending — Plan 02 §2 task fills body from inicio:152-167."""
            raise NotImplementedError

        def pixel_to_geo(x: int, y: int, cfg: RegionConfig) -> Tuple[float, float]:
            """Verbatim port pending — Plan 02 §2 task fills body from inicio:170-178."""
            raise NotImplementedError

        def point_in_polygon(point, polygon) -> bool:
            """Verbatim port pending — Plan 02 §2 task fills body from inicio:181-185."""
            raise NotImplementedError

    The `draw_names` default in RegionConfig is `False` if PREFLIGHT.md Q10
    verdict says "draw_names = False", else `True`. **The actual setting goes in
    `iberia_config()`** via explicit kwarg, but the dataclass default mirrors
    the verdict so all regions share it.

    **`backend/medieval_forge/services/pipeline/regions.py`**: REGIONS registry
    + verbatim port of `iberia_config()` from inicio §1 lines 115-145, with
    these substitutions:

    - `output_dir` left as inicio's default (Plan 03 conftest overrides at runtime).
    - `municipality_pt_geojson="data/regions/iberia_868/inputs/pt_concelhos_wgs84.geojson"`
    - `municipality_es_topojson="data/regions/iberia_868/inputs/es-atlas-pkg/package/es/municipalities.json"`
    - `mountain_river_json="data/regions/iberia_868/inputs/mountain_river_data.json"`
    - Add territory data: `kingdoms=KINGDOMS, duchies=DUCHIES, condados=CONDADOS`
      via `from ...data.regions.iberia_868.territory_data import KINGDOMS, DUCHIES, CONDADOS`.
    - Add `rng_seed=42` (matches inicio's hardcoded seed; promoted to cfg per CLAUDE.md non-negotiable rule for determinism).
    - Add `draw_names=<verdict>` per PREFLIGHT.md Q10 (literal True or False, no string).
    - The 38-point `border_polygon` is copied verbatim from inicio lines 132-143; do NOT reformat or truncate.
    - The `kingdom_colors` dict and `pt_duchies` set are copied verbatim from inicio lines 125-131.

    Then:

        REGIONS = {"iberia_868": iberia_config}  # factory callables per D-04, NOT pre-built configs

    **`backend/medieval_forge/services/pipeline/__main__.py`** — verbatim from
    RESEARCH §1 (the 14-line argparse shim):

        import argparse
        from . import run_pipeline
        from .regions import REGIONS

        if __name__ == "__main__":
            p = argparse.ArgumentParser(prog="python -m medieval_forge.services.pipeline")
            p.add_argument("--region", required=True, choices=list(REGIONS.keys()))
            p.add_argument("--out", required=True)
            args = p.parse_args()
            cfg = REGIONS[args.region]()
            cfg.output_dir = args.out
            run_pipeline(cfg)

    **Empty stubs** for the remaining six submodules (each file is a one-line
    docstring + an `__all__ = []`; bodies arrive in Plan 02):

        backend/medieval_forge/services/pipeline/landmask.py
        backend/medieval_forge/services/pipeline/border.py
        backend/medieval_forge/services/pipeline/voronoi.py
        backend/medieval_forge/services/pipeline/cleanup.py
        backend/medieval_forge/services/pipeline/render.py
        backend/medieval_forge/services/pipeline/lookup.py
        backend/medieval_forge/services/pipeline/export.py

    Each stub:

        """Stub — Plan 02 fills this with the verbatim port of inicio §<N>."""
        __all__ = []

    Commit:
        feat(01): scaffold pipeline package skeleton with RegionConfig + iberia_config

    Karpathy compliance: this task is a SKELETON only. Do not pre-emptively
    write algorithm bodies; that's Plan 02's job. The acceptance criteria check
    for `NotImplementedError` raises in run_pipeline + transforms.
  </action>
  <verify>
    <automated>test -f backend/medieval_forge/services/pipeline/__init__.py &amp;&amp; test -f backend/medieval_forge/services/pipeline/contracts.py &amp;&amp; test -f backend/medieval_forge/services/pipeline/regions.py &amp;&amp; test -f backend/medieval_forge/services/pipeline/__main__.py &amp;&amp; test -f backend/medieval_forge/services/pipeline/landmask.py &amp;&amp; test -f backend/medieval_forge/services/pipeline/border.py &amp;&amp; test -f backend/medieval_forge/services/pipeline/voronoi.py &amp;&amp; test -f backend/medieval_forge/services/pipeline/cleanup.py &amp;&amp; test -f backend/medieval_forge/services/pipeline/render.py &amp;&amp; test -f backend/medieval_forge/services/pipeline/lookup.py &amp;&amp; test -f backend/medieval_forge/services/pipeline/export.py &amp;&amp; python -c "from medieval_forge.services.pipeline import run_pipeline, RegionConfig; from medieval_forge.services.pipeline.regions import REGIONS, iberia_config; cfg = iberia_config(); assert cfg.name == 'iberia'; assert cfg.map_w == 1920 and cfg.map_h == 1080 and cfg.upscale == 2; assert cfg.smooth_sigma == 3.0; assert cfg.median_passes == 8; assert cfg.rng_seed == 42; assert len(cfg.border_polygon) == 38; assert len(cfg.kingdoms) > 0 and len(cfg.duchies) > 0 and len(cfg.condados) > 0; assert 'iberia_868' in REGIONS"</automated>
  </verify>
  <files>
    backend/medieval_forge/services/pipeline/__init__.py
    backend/medieval_forge/services/pipeline/contracts.py
    backend/medieval_forge/services/pipeline/regions.py
    backend/medieval_forge/services/pipeline/__main__.py
    backend/medieval_forge/services/pipeline/landmask.py
    backend/medieval_forge/services/pipeline/border.py
    backend/medieval_forge/services/pipeline/voronoi.py
    backend/medieval_forge/services/pipeline/cleanup.py
    backend/medieval_forge/services/pipeline/render.py
    backend/medieval_forge/services/pipeline/lookup.py
    backend/medieval_forge/services/pipeline/export.py
  </files>
  <acceptance_criteria>
    - All 11 files exist under `backend/medieval_forge/services/pipeline/`.
    - `python -c "from medieval_forge.services.pipeline import run_pipeline, RegionConfig"` exits 0.
    - `python -c "from medieval_forge.services.pipeline.regions import REGIONS, iberia_config"` exits 0.
    - `iberia_config()` returns a RegionConfig with: `cfg.name == "iberia"`, `cfg.map_w == 1920`, `cfg.map_h == 1080`, `cfg.upscale == 2`, `cfg.smooth_sigma == 3.0` (default — valid range [3.0, 4.5] per CLAUDE.md rule #2), `cfg.median_passes == 8`, `cfg.rng_seed == 42` (per CLAUDE.md determinism), `len(cfg.border_polygon) == 38`, `len(cfg.kingdom_colors) == 4`, `cfg.pt_duchies == {"d_portucale", "d_gharb", "d_fronteira"}`, `len(cfg.kingdoms) > 0`, `len(cfg.duchies) > 0`, `len(cfg.condados) > 0`.
    - `run_pipeline(cfg)` raises NotImplementedError (skeleton; bodies in Plan 02).
    - `geo_to_pixel`, `pixel_to_geo`, `point_in_polygon` all raise NotImplementedError.
    - `REGIONS["iberia_868"]` is a callable, NOT a pre-built RegionConfig: `callable(REGIONS["iberia_868"])` is True (D-04 — factory callables).
    - `grep "from dataclasses import dataclass" backend/medieval_forge/services/pipeline/contracts.py` matches AND `grep -i "pydantic" backend/medieval_forge/services/pipeline/contracts.py` does NOT match (RESEARCH §9 Q1 — `@dataclass` not pydantic).
    - `grep "draw_names" backend/medieval_forge/services/pipeline/regions.py` matches and the literal value (True or False) matches the PREFLIGHT.md Q10 verdict.
    - The 6 stub submodules (landmask/border/voronoi/cleanup/render/lookup/export — `wc -l` ≤ 5 each).
    - `python -m medieval_forge.services.pipeline --help` exits 0 and shows `--region {iberia_868}` in the output (NotImplementedError fires only when `--region iberia_868 --out X` is run, not on --help).
    - Commit message: `feat(01): scaffold pipeline package skeleton with RegionConfig + iberia_config`.
  </acceptance_criteria>
  <done>The pipeline package skeleton + RegionConfig contract + iberia_config + REGIONS registry + CLI shim are committed. Plan 02 starts with every import path resolved and only the algorithm bodies left to fill. The package import contract is locked: subsequent plans can rely on `from medieval_forge.services.pipeline.contracts import RegionConfig` and `from medieval_forge.services.pipeline.regions import REGIONS`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → in-package code (Task 4) | inicio/territory_data_v3.py is the source; copy is a byte-identical clone. |
| Filesystem → in-repo data tree (Tasks 2-3) | External `D:/Projetos_Jogo/Reconquista/` and the npm `es-atlas` package supply binary inputs and golden artifacts. |
| LFS pointer integrity (Task 2) | `pt_concelhos_wgs84.geojson` is checked-in via LFS; broken smudge = 134-byte text → parity tests fail fast in Plan 03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Path traversal in pipeline `--out` (Task 5 CLI) | mitigate | `argparse` accepts the path string; Plan 02 will pass it through `pathlib.Path(args.out).resolve()` before write. Severity: low (local-only tool). |
| T-01-02 | Denial of service | Malformed input GeoJSON/TopoJSON in `data/regions/iberia_868/inputs/` | accept | Local-only tool; `MemoryError` on a 28 MB GeoJSON parse is acceptable. Severity: low. No mitigation. |
| T-01-03 | Tampering | Fixture tampering — golden PNG/JSON edited in-tree (Task 3) | mitigate | D-10 explicit `docs(parity): refresh iberia_868 baseline` commit convention; Phase 03's CI parity reads the committed fixture. Any silent edit fails PR review by commit-message convention. Severity: medium → mitigation acceptable. |
| T-01-04 | Tampering | LFS pointer integrity for PT GeoJSON (Task 2) | mitigate | Broken LFS smudge yields a 134-byte text file → `decode_topojson_municipalities` and PT GeoJSON loaders raise on first load → parity test fails fast in Plan 03 with a clear error message. Plan 03's parity test surfaces this within 45 s. Severity: medium → mitigation acceptable. |
| T-01-05 | Information disclosure | None — phase ships zero secrets, no auth, no network | accept | Phase 01 is offline-only. No mitigation needed. |

**Block-on threshold:** high. None of the above hit high. Gate passes.
</threat_model>

<verification>
After all 5 tasks complete:

1. `pytest backend/tests/unit/ -v --cov=medieval_forge --cov-fail-under=60` — must pass (Phase 00's 60% coverage gate is still in effect; Plan 03 raises to 85%).
2. `python -c "from medieval_forge.services.pipeline.regions import iberia_config; cfg = iberia_config(); print(cfg)"` — exits 0, prints a populated RegionConfig.
3. `python -m medieval_forge.services.pipeline --help` — exits 0, shows `--region iberia_868`.
4. `git lfs ls-files` (if LFS used) — lists `data/regions/iberia_868/inputs/pt_concelhos_wgs84.geojson`.
5. `ls tests/fixtures/iberia_868/golden/ | wc -l` returns 11.
6. `medieval-forge start` (smoke) — server still boots; root returns 200 (Phase 00 SC-6 invariant — confirms Wave 0 didn't accidentally break anything).
</verification>

<success_criteria>
- All 5 tasks committed as 5 atomic commits with `(01)` scope.
- PREFLIGHT.md committed with P-1 + Q10 + Q11 + Q12 verdicts.
- 11-file golden snapshot in `tests/fixtures/iberia_868/golden/` with terrain_lookup.png + terrain_types.json explicitly excluded and the README documenting the deferral to Phase 06.
- 3 input files in `data/regions/iberia_868/inputs/` with paths matching inicio's hardcoded relative paths.
- Territory data importable from `medieval_forge.data.regions.iberia_868.territory_data`.
- 9-submodule pipeline package skeleton compiles, RegionConfig is `@dataclass` (NOT pydantic), iberia_config returns a populated config with all 38 border points + 3 territory dicts, REGIONS["iberia_868"] is a callable.
- `medieval-forge start` still boots (Phase 00 SC-6 invariant).
- Plan 02 has every prerequisite locked: contract, paths, fixtures, verdicts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-parity-port-harness-together/01-01-SUMMARY.md` capturing:
- The PREFLIGHT verdicts (P-1 ABSENT/PRESENT, Q10 True/False, Q11 method, Q12 LFS yes/no).
- The 11-file fixture inventory + sizes.
- The pipeline package layout.
- A note on what Plan 02 must reference: PREFLIGHT.md, the iberia_config field values, the RegionConfig dataclass.
</output>
