---
phase: 07.1
slug: period-numeric-inputs-llama-cpp-re-add
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-15
revised: 2026-05-15
---

# Phase 07.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.4 (backend) + vitest 3.x (frontend) + Playwright (UAT) |
| **Config file** | `pyproject.toml` (pytest), `frontend/vitest.config.ts`, `frontend/playwright.config.ts` |
| **Quick run command** | `cd backend && python -m pytest tests/unit/ -x` (backend) / `cd frontend && pnpm vitest run` (frontend) |
| **Full suite command** | `cd backend && python -m pytest && cd ../frontend && pnpm vitest run && pnpm playwright test` |
| **Estimated runtime** | ~90s backend + ~25s frontend + ~120s UAT |

---

## Sampling Rate

- **After every task commit:** Run targeted test file (`pytest tests/unit/test_<x>.py` or `vitest run <x>`)
- **After every plan wave:** Run quick suite (unit + parity)
- **Before `/gsd-verify-work`:** Full suite must be green including Playwright UAT
- **Max feedback latency:** 30s (per-task) / 180s (per-wave) / 360s (full)

---

## Per-Task Verification Map

> One row per task across all 11 plans (00..10). Verification cells reference the
> `<automated>` command embedded in each plan task's `<verify>` block. Tasks that
> are pure scaffolding or setup (no behavioral assertion) are marked "—".

| Plan | Task | Verification Cell | What Is Verified |
|------|------|-------------------|------------------|
| 07.1-00 | Task 1 | `pytest tests/unit/test_period_numeric_validation.py tests/unit/test_prompt_period_range.py tests/unit/test_llamacpp_provider.py tests/unit/test_llamacpp_launcher.py tests/unit/test_research_cache_key.py --co` collects 35 skip-marked tests | Wave 0 scaffold: 5 backend pytest files exist with descriptive `@pytest.mark.skip` tests; pytest can collect them |
| 07.1-00 | Task 2 | `pytest tests/e2e/test_llamacpp_launch_lifecycle.py --co` (5 skipped) + `vitest run ResearchDialog.numeric.spec.tsx AuthSetupSheet.spec.tsx` (16 skipped) + `grep -c "test.skip" llamacpp_launch.spec.ts` returns 3 | Wave 0 scaffold: backend e2e + frontend vitest + Playwright skeleton files exist with `it.skip` / `test.skip` |
| 07.1-01 | Task 1 | `pytest tests/unit/test_llm_base_health_status.py -v` reports 6 passed | HealthStatus pydantic model gains optional `available_models: list[str] \| None = None`; Claude/Ollama default unchanged; serialization round-trip |
| 07.1-02 | Task 1 | `pytest tests/unit/test_llamacpp_launcher.py -v` reports 16 passed | llamacpp_launcher.py: models_dir resolution, binary resolution, port auto-pick via socket.bind(0), path-traversal rejection, .gguf suffix enforcement, launch idempotency by model path, conflict-on-different-model raises LlamacppLaunchConflict, missing-binary raises LlamacppBinaryMissing, shutdown_sync kill-within-5s, atexit registered once, reset_state_for_tests |
| 07.1-03 | Task 1 | `pytest tests/unit/test_llamacpp_provider.py tests/unit/test_llm_registry.py -v` reports 7+ passed including llamacpp registration | LlamaCppProvider implements provider_id="llamacpp", health() returns HealthStatus with available_models from launcher.list_models(), research() streams via httpx mock to queue; registry.py exposes llamacpp as third entry |
| 07.1-04 | Task 1 | `grep -r "period_label" backend/medieval_forge/ --include='*.py' \| wc -l` returns 0 AND `grep -rn "period_label" backend/tests/` returns 0 lines | Atomic backend swap: Alembic 0007 truncate migration + StartResearchBody field swap + all 6 runner.py callsites + cache.py signature + meta sidecar shape in a single commit; backend-wide period_label residual is exactly 0 |
| 07.1-04 | Task 2 | `pytest tests/unit/test_period_numeric_validation.py tests/unit/test_prompt_period_range.py tests/unit/test_research_cache_key.py -v` reports 12 passed | SC-1a..d: 5 period numeric validation tests; SC-2: 3 prompt builder integer-acceptance tests + 4 cache_key signature tests with inspect.signature |
| 07.1-05 | Task 1 | `pytest tests/e2e/test_llamacpp_launch_lifecycle.py -v` reports collection success after router mount | api/v3/llamacpp.py router: POST /api/v3/llm/llamacpp/launch + DELETE handler with exception mapping (BinaryMissing → 503, LaunchConflict → 409, path-traversal → 400); main.py lifespan + atexit fallback registered |
| 07.1-05 | Task 2 | `pytest tests/e2e/test_llamacpp_launch_lifecycle.py -v` reports 5 passed | End-to-end: test_post_launch_then_get_status_then_delete_process_gone, test_post_launch_idempotent_same_model_returns_200, test_post_launch_conflict_different_model_returns_409, test_delete_launch_when_no_server_returns_200_was_running_false, test_post_launch_path_traversal_returns_400 |
| 07.1-06 | Task 1 | `pnpm tsc --noEmit` reports no new errors | useProject TanStack Query hook exposes Project.period_start / period_end for ResearchDialog seed (D-03) |
| 07.1-06 | Task 2 | `pnpm tsc --noEmit` reports no new errors | useLlamacppHealth TanStack useQuery for /api/v3/research/providers filtered to llamacpp + useLlamacppLaunch + useLlamacppShutdown mutations |
| 07.1-07 | Task 1 | `pnpm tsc --noEmit` reports no new errors AND data-testid hooks present (research-period-start, research-period-end, period-error-start-gt-end, auth-setup-sheet-trigger) | ResearchDialog single TextField period_label → two TextField.Root type="number" Início + Fim swap; wasOpenRef seed gate (Pitfall 10); useResearchStream body shape change from period_label to period_start/end as JSON integers; "Configurar provedores" rodapé link placeholder onClick |
| 07.1-07 | Task 2 | `pnpm vitest run src/components/research/__tests__/ResearchDialog.numeric.spec.tsx` reports 8 passed | SC-1e..f frontend coverage: renders type=number min=1 max=2100, seeds from Project, empty when null, disables CTA on start>end / start<1 / end>2100, payload carries integers not strings, rodapé link onOpenChange wired |
| 07.1-08 | Task 1 | `pnpm tsc --noEmit` reports no new errors AND data-testid hooks present (auth-setup-sheet, tab-llamacpp, tab-claude-disabled, tab-ollama-disabled, llamacpp-model-select, launch-button, shutdown-button, empty-models-hint, binary-missing-warning, server-status-line, launch-error, shutdown-error) | AuthSetupSheet.tsx Radix Dialog (maxWidth 480) with Tabs.Root (Llama.cpp active + Claude/Ollama disabled per D-05); dropdown sources from useLlamacppHealth().available_models; PT-BR microcopy verbatim per UI-SPEC §Surface 2; ResearchDialog rodapé link wires setAuthSheetOpen(true) |
| 07.1-08 | Task 2 | `pnpm vitest run src/components/research/__tests__/AuthSetupSheet.spec.tsx` reports 8 passed | SC-5a..c frontend coverage: Tabs shell renders, dropdown populates alphabetical, dropdown disabled with empty hint, binary-missing orange warning, launch click calls mutation with selected model, button swap to Parar servidor on running, HTTP 409 inline PT-BR error, shutdown DELETE click |
| 07.1-09 | Task 1 | `pnpm playwright test tests/uat/playwright/llamacpp_launch.spec.ts --reporter=list` reports 3 passed | SC-5d UAT: happy-path (open dialog → AuthSetupSheet → select model → Levantar servidor → status active → Parar servidor → idle), empty-models hint, HTTP 409 conflict — all with page.route() mocked backend |
| 07.1-09 | Task 2 | `pnpm playwright test tests/uat/playwright/research_period_numeric.spec.ts --reporter=list` reports 2 passed | SC-1 e2e: numeric inputs render with type=number/min=1/max=2100 and seed from Project; submit payload carries period_start + period_end as JSON integers, not period_label string |
| 07.1-09 | Task 3 | Human checkpoint sign-off "approved — all 4 verifications green" | Manual UAT on dev machine with real llama-server: real spawn produces /health 200; dropdown filename rendering; empty-models hint after removing files; app-exit cleanup |
| 07.1-10 | Task 1 | `pytest tests/parity/test_iberia_868_yaml.py -v` exits 0 AND `grep -rn "period_label" backend/ frontend/src/ frontend/tests/` returns 0 hits AND full pytest + vitest + Playwright suites green | Phase closure gate: D-12 zero-LLM parity preserved; global grep gates clean; no new dependencies; VERIFICATION.md generated with all 6 SC and 13 D-XX closure rows; ROADMAP.md plans marked [x] |

*Planner expanded from `07.1-RESEARCH.md` §Validation Architecture + per-plan `<verify>` blocks (2026-05-15 revision).*

---

## Wave 0 Requirements

> Test files RESEARCH.md flagged as MISSING. Wave 0 of the plan must create stubs/scaffolds
> before later waves can run.
>
> **Path correction (2026-05-15 revision):** Filenames below match what plan 07.1-00
> actually produces (not the early RESEARCH.md draft naming).

- [x] `backend/tests/unit/test_llamacpp_provider.py` — health() + research() stubs (7 skip-marked tests)
- [x] `backend/tests/unit/test_llamacpp_launcher.py` — spawn/idempotent/cleanup stubs (16 skip-marked tests)
- [x] `backend/tests/unit/test_period_numeric_validation.py` — start<=end Pydantic validator stub (5 skip-marked tests)
- [x] `backend/tests/unit/test_prompt_period_range.py` — prompt template composition stub (3 skip-marked tests)
- [x] `backend/tests/unit/test_research_cache_key.py` — cache_key signature stub (4 skip-marked tests)
- [x] `backend/tests/e2e/test_llamacpp_launch_lifecycle.py` — POST/DELETE lifecycle stub (5 skip-marked tests)
- [x] `frontend/src/components/research/__tests__/ResearchDialog.numeric.spec.tsx` — input render + validation (8 it.skip cases)
- [x] `frontend/src/components/research/__tests__/AuthSetupSheet.spec.tsx` — gguf dropdown (8 it.skip cases)
- [x] `frontend/tests/uat/playwright/llamacpp_launch.spec.ts` — Playwright end-to-end launch flow (3 test.skip cases)

*Existing infrastructure (pytest + vitest + Playwright) already covers framework setup; only test files are new.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `llama-server` actual spawn produces working `/health` endpoint | SC-4 | Subprocess interaction with native binary depends on local install; CI may not have llama.cpp | 1. Ensure `llama-server` on PATH. 2. Drop a `.gguf` in `~/.medieval-forge/models/`. 3. POST `/api/v3/llm/llamacpp/launch` with model name. 4. Curl `http://localhost:<port>/health` → expect 200. 5. DELETE endpoint → process exits within 5s. |
| Visual: AuthSetupSheet dropdown populates after model dir creation | SC-5 | Eye check that dropdown renders gguf filenames cleanly (no `.gguf` extension polluting label) | 1. Create empty `~/.medieval-forge/models/`. 2. Open AuthSetupSheet llama.cpp tab. 3. Verify "Nenhum modelo encontrado" hint. 4. Drop `Qwen3-7B-Q4.gguf`. 5. Refresh — verify dropdown shows "Qwen3-7B-Q4". |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all 9 MISSING test files above (filenames corrected to plan 00 actual outputs)
- [x] No watch-mode flags (`--watch`, `--ui`) in CI commands
- [x] Feedback latency < 30s per task
- [x] `nyquist_compliant: true` set in frontmatter after planner expanded Per-Task Verification Map

**Approval:** approved (revision 2026-05-15 — addresses plan-checker BLOCKER 2)
</content>
