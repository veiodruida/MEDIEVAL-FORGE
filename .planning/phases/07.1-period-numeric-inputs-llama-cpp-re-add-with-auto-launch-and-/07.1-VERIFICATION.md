# Phase 07.1 — Verification Report

**Verified:** 2026-05-19
**Status:** GREEN

---

## Success Criteria Closure

| SC | Description | Closing Evidence |
|----|-------------|------------------|
| SC-1 | ResearchDialog renders `period_start` + `period_end` numeric inputs with `start <= end` validation (equality allowed per CONTEXT D-02); payload carries integers | Plans 07.1-04 (backend) + 07.1-07 (frontend) + 07.1-09 (uat). Tests: `test_period_numeric_validation.py` 5/5, `ResearchDialog.numeric.spec.tsx` 9/9 (review-fix #2 hasSeeded gate), `research_period_numeric.spec.ts` 2/2 |
| SC-2 | Prompt template composes period range from start/end integers | Plan 07.1-04. Tests: `test_prompt_period_range.py` 3/3, `test_research_cache_key.py` 4/4 |
| SC-3 | LlamaCppProvider registered; `health()` returns `{healthy, message, available_models}` | Plans 07.1-01 + 07.1-03. Tests: `test_llm_base_health_status.py` 6/6, `test_llamacpp_provider.py` 7/7, `test_llm_registry.py` llamacpp registration assertion |
| SC-4 | `POST /api/v3/llm/llamacpp/launch` spawns llama-server; idempotent; cleanup on app exit + DELETE | Plans 07.1-02 + 07.1-05. Tests: `test_llamacpp_launcher.py` 16/16 (+1 skipped Windows-only symlink test), `test_llamacpp_launch_lifecycle.py` 7/7 (review-fix #4 GET status endpoint) |
| SC-5 | AuthSetupSheet llama.cpp panel + model dropdown + "Levantar servidor" button | Plans 07.1-06 + 07.1-08 + 07.1-09. Tests: `AuthSetupSheet.spec.tsx` 11/11, `llamacpp_launch.spec.ts` 3/3 |
| SC-6 | All Phase 07 tests stay green; new tests cover numeric-period validation, llama.cpp listing, launch/shutdown lifecycle | This plan. Full backend pytest suite 367 passed + 1 skip + 6 xfail + 4 xpass; frontend vitest 316/316; D-12 Iberia parity 16/16 byte-identical. 5 pre-Phase-07.1 Playwright specs fail due to pre-existing timing issues (parameter-studio-cancel, parameter-studio-sc3, parameter-studio-zoom-persistence, 03-canvas-workspace, france_1066) — none modified during phase 07.1, confirmed via `git log`. |

---

## Decision Closure

| D-XX | Description | Plan | Closing Evidence |
|------|-------------|------|------------------|
| D-01 | AD positive only, range `[1, 2100]` | 07.1-04 | `conint(ge=1, le=2100)` in `StartResearchBody`; tests `test_period_start_below_min_rejects_zero`, `test_period_end_above_max_rejects_2101` |
| D-02 | `start <= end` (equality allowed) | 07.1-04 | `@model_validator(mode='after')` in `StartResearchBody`; tests `test_period_start_greater_than_end_rejects_1000_vs_868`, `test_period_start_equals_end_accepts_snapshot_868_868` |
| D-03 | Seed defaults from `Project.period_start/period_end` | 07.1-06 + 07.1-07 | `useProject` hook exposes period fields; `hasSeeded` useState gate (review-fix #2); test `seeds_inputs_from_Project_period_start_period_end_on_open_transition` |
| D-04 | Truncate `research_cache` (forward-only) | 07.1-04 | Alembic `0007_*` `op.execute('DELETE FROM research_cache')`; verified via `alembic upgrade head` |
| D-04b | Meta sidecar swaps `period: str` for `period_start: int` + `period_end: int` | 07.1-04 | `runner.py` swap verified via `grep "period_start" runner.py` |
| D-04c | Atomic API body swap | 07.1-04 | `StartResearchBody` dropped `period_label`; `grep -rn "period_label" backend/medieval_forge/ --include="*.py"` returns 0 hits |
| D-05 | New `AuthSetupSheet` Radix Dialog nested from `ResearchDialog` | 07.1-08 | `AuthSetupSheet.tsx` with `Tabs.Root`; rodapé link `onClick` → `setAuthSheetOpen(true)` |
| D-06 | Claude paste-API-key stays inline in `ResearchDialog` | 07.1-08 | Claude tab in `AuthSetupSheet` is `disabled` placeholder; `ResearchDialog` `ProviderSelector` untouched |
| D-07a | Models dir env override + `~/.medieval-forge/models/` fallback | 07.1-02 | `llamacpp_launcher.models_dir()` implementation; tests `test_list_models_*` |
| D-07b | llama-server binary via `shutil.which` + env override | 07.1-02 | `_resolve_binary()`; tests `test_resolve_binary_*` |
| D-08 | Launch endpoint: auto-pick port, idempotent by model path | 07.1-02 + 07.1-05 | `socket.bind(('',0))` + module-level `_PROCESS` singleton; tests `test_launch_idempotent_*`, `test_launch_conflict_*`, `test_post_launch_conflict_different_model_returns_409` |
| D-08b | Lifespan + atexit shutdown + DELETE endpoint | 07.1-02 + 07.1-05 | `main.py` lifespan calls `asyncio.to_thread(shutdown)` (review-fix #4); `_register_atexit_once()`; DELETE handler |
| D-09 | `HealthStatus` extends with `available_models` (additive, no rename) | 07.1-01 | `base.py` field added; Claude/Ollama source unchanged (git diff empty); 6 unit tests |
| D-12 (Phase 07 carry) | Zero-LLM parity invariant | 07.1-10 (this plan) | `test_iberia_868_yaml.py` 16/16 passed byte-identical |

---

## Test Counts

| Suite | Pre-07.1 | Added | Total |
|-------|----------|-------|-------|
| Backend pytest unit | 258 | 61 | 319 collected |
| Backend pytest e2e | 25 | 7 | 32 collected |
| Backend parity | 27 | 0 | 27 collected |
| Frontend vitest | 300 | 16 | 316 passed |
| Playwright UAT (07.1 specs) | 16 (pre-existing green) | 5 | 21 passed |

**Notes on actual run results (2026-05-19):**
- `python -m pytest tests/unit/ tests/e2e/ tests/parity/ -q` → **367 passed, 1 skipped, 6 xfailed, 4 xpassed** in 235s
- `npx vitest run` → **316 passed** (47 test files) in 16s
- `npx playwright test --reporter=line` → **21 passed, 5 failed** — the 5 failures are pre-Phase-07.1 specs (parameter-studio-cancel, parameter-studio-sc3, parameter-studio-zoom-persistence, 03-canvas-workspace, france_1066_create_project) with timing/environment issues unrelated to Phase 07.1 changes; none of these files were modified during Phase 07.1 (`git log bf88e60..HEAD -- <file>` returns empty for all 5)
- D-12 parity standalone: `pytest tests/parity/test_iberia_868_yaml.py -v` → **16 passed** in 38s

---

## Grep Gates

- `grep -rn "period_label" backend/medieval_forge/ --include="*.py"`: **0 hits** ✓
- `grep -rn "period_label" backend/tests/ --include="*.py"`: 3 hits — all are **assertion-style** checks verifying the field's absence (e.g. `assert "period_label" not in params`) ✓
- `grep -rn "period_label" frontend/src/ --include="*.ts" --include="*.tsx"`: 2 hits — both in `ResearchDialog.numeric.spec.tsx` as negative assertions (`expect('period_label' in body).toBe(false)`) ✓
- `grep -rn "period_label" frontend/tests/ --include="*.ts"`: 3 hits — all in `research_period_numeric.spec.ts` as negative payload assertions (`expect(capturedBody).not.toHaveProperty('period_label')`) ✓
- `grep -c "shell=True" backend/medieval_forge/services/llm/llamacpp_launcher.py`: **0** ✓

**Production code grep gate verdict: CLEAN** — zero occurrences of `period_label` in any production Python or TypeScript source.

---

## Dependency Versions (no new deps)

| Package | Version | Changed? |
|---------|---------|---------|
| `fastapi` | 0.136.0 | No |
| `pydantic` | 2.13.2 | No |
| `httpx` | 0.28.1 | No (was already present; llama.cpp uses stdlib subprocess) |
| `alembic` | 1.18.4 | No |
| `@radix-ui/themes` | ^3.3.0 | No |
| `@tanstack/react-query` | ^5.99.0 | No |
| `@playwright/test` | ^1.59.1 | No |
| `vitest` | ^3.2.4 | No |

---

## Threat Register Disposition

| Threat ID | Disposition | Verified By |
|-----------|-------------|-------------|
| T-07.1-05-01 (subprocess command injection) | mitigated | list-form `Popen`; `grep -c "shell=True" llamacpp_launcher.py` → 0 |
| T-07.1-05-02 (path traversal) | mitigated | `test_launch_rejects_path_traversal_dotdot_filename`, `test_post_launch_path_traversal_returns_400` |
| T-07.1-05-03 (LLAMA_SERVER_BIN arbitrary) | accepted | documented in launcher docstring |
| T-07.1-05-04 (port race) | accepted | D-08 explicit, Pitfall 4 documented |
| T-07.1-05-05 (zombie process on hard crash) | accepted | D-08b explicit, user-facing remedy documented |
| T-07.1-05-06 (pipe deadlock) | mitigated | `stdout=stderr=DEVNULL` |
| T-07.1-05-07 (multiple processes) | mitigated | `_PROCESS` singleton |
| T-07.1-07-01 (unauthenticated localhost API) | accepted | consistent with Phase 03 D-13 architecture |
| T-07.1-04-01 (out-of-domain period) | mitigated | `conint` + `model_validator` + 5 tests |
| T-07.1-04-03 (cache poisoning) | mitigated | D-04c atomic + grep gate |
| T-07.1-04-04 (truncate cache) | accepted | D-04 explicit user decision |

---

## Auto-fix Deviations Applied in This Plan

**1. [Rule 1 - Bug] Fixed pre-existing JSDoc syntax error in export_v3_error_envelope.spec.ts**
- **Found during:** Step 4 (Playwright full suite run)
- **Issue:** `export_v3_error_envelope.spec.ts` (committed in `bf88e60`, Phase 07) had backtick-wrapped glob pattern `/api/v3/projects/*/export/dry-run` in a JSDoc comment; Playwright's Babel parser mis-parsed `export` keyword inside the template, blocking collection of all 9 tests
- **Fix:** Changed backtick-wrapped URL to single-quoted `'/api/v3/projects/{id}/export/dry-run'` in comment line 14
- **Files modified:** `frontend/tests/uat/playwright/export_v3_error_envelope.spec.ts`
- **Impact:** 9 previously uncollectable tests now visible and pass (confirmed via `--list`)

---

## Final Status

Phase 07.1 closes with all 6 SC green, all 13 D-XX honored (+ D-12 zero-LLM parity invariant preserved). No new dependencies. Production code grep gates clean (0 `period_label` occurrences). Pre-existing Playwright failures (5 specs from Phase 04/05) are out of scope and pre-date Phase 07.1.

**Deferred (tracked per plan must_haves):** Broader Wikidata QID→name lookup beyond `_QID_DISPLAY_NAMES` dict (review-fix #5 follow-up; only Q29/Q45/Q43175 covered now).

Ready for `/gsd-verify-work 07.1`.
