---
phase: "07.1"
plan: "01"
subsystem: "backend/services/llm"
one_liner: "HealthStatus carries optional `available_models: list[str] | None = None` per D-09; Claude/Ollama source untouched"
tags: [llm, pydantic, schema, health-status, d-09, sc-3, sc-6]
requirements: [V3-LLM-OPT-IN, SC-3, SC-6]
dependency_graph:
  requires: ["07.1-00"]
  provides: ["HealthStatus.available_models field (D-09 schema prerequisite for 07.1-04)"]
  affects: ["backend/medieval_forge/services/llm/base.py", "api/v3/research.py /providers builder (wire-compatible)"]
tech_stack:
  added: []
  patterns: ["pydantic v2 optional field with default None", "additive-only schema extension (D-09)"]
key_files:
  created:
    - backend/tests/unit/test_llm_base_health_status.py
  modified:
    - backend/medieval_forge/services/llm/base.py
    - backend/tests/unit/test_llm_base.py
decisions:
  - "D-09 additive-only: available_models field added with default None; healthy stays canonical (no rename to ok)"
  - "Rule 1 fix: test_llm_base.py round-trip assertion updated to include new field in model_dump() output"
metrics:
  duration_minutes: 5
  completed_date: "2026-05-16"
  tasks_completed: 1
  tasks_total: 1
  files_changed: 3
---

# Phase 07.1 Plan 01: Extend HealthStatus with available_models field (D-09) Summary

## What Was Built

Added `available_models: list[str] | None = None` to `HealthStatus` pydantic model in `backend/medieval_forge/services/llm/base.py`. This is the Wave 1 schema prerequisite for `LlamaCppProvider` (plan 07.1-04), which will populate this field with sorted `.gguf` filenames from the local models directory.

The `/providers` builder in `api/v3/research.py` (lines 231-232) already conditionally surfaces `available_models` when present — confirmed wire-compatible with this additive change.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Extend HealthStatus with available_models optional field | 0a64dee | base.py, test_llm_base_health_status.py (created), test_llm_base.py |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed test_llm_base.py round-trip assertion after schema extension**
- **Found during:** Task 1 verification (full unit suite run)
- **Issue:** `test_health_status_round_trips_with_healthy_true_and_message_string` compared `model_dump()` against `{"healthy": True, "message": "Credentials present"}` — after adding `available_models`, `model_dump()` returns the new field with `None` value, causing AssertionError.
- **Fix:** Updated assertion to include `"available_models": None` in the expected dict. Added inline comment referencing the plan that introduced the change.
- **Files modified:** `backend/tests/unit/test_llm_base.py`
- **Commit:** 0a64dee (same atomic commit as the feature)

## Verification Results

- `cd backend && python -m pytest tests/unit/test_llm_base_health_status.py -v` — 6 passed
- `cd backend && python -m pytest tests/unit/` — 274 passed, 35 skipped (baseline 268 + 6 new)
- `git diff --stat backend/medieval_forge/services/llm/claude.py backend/medieval_forge/services/llm/ollama.py` — empty (D-09 additive-only invariant confirmed)
- `grep -c "available_models" backend/medieval_forge/services/llm/base.py` — 3 (docstring x2 + field declaration)
- `python -c "from medieval_forge.services.llm.base import HealthStatus; h = HealthStatus(healthy=True); assert h.available_models is None; print('ok')"` — prints `ok`

## Known Stubs

None — this plan adds only a schema field. No UI rendering, no data source wiring needed at this layer.

## Threat Flags

No new trust boundaries introduced. Field is populated server-side only from provider implementations. T-07.1-01-02 mitigated: pydantic v2 rejects non-string elements (test `test_health_status_rejects_non_string_elements_in_available_models` enforces).

## Self-Check: PASSED
