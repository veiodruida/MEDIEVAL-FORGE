---
phase: "07.1"
plan: "03"
subsystem: "backend/llm-provider"
tags: ["llamacpp", "httpx", "sse-streaming", "registry", "provider", "security"]
dependency_graph:
  requires:
    - phase: "07.1-01"
      provides: "HealthStatus.available_models field (D-09)"
    - phase: "07.1-02"
      provides: "llamacpp_launcher: list_models(), status(), _resolve_binary(), _reset_state_for_tests()"
  provides:
    - "LlamaCppProvider class with health()/health_check()/research() methods"
    - "LlamaCppProviderError typed exception"
    - "llamacpp registered as third entry in PROVIDERS dict"
  affects:
    - "07.1-04 (auth setup sheet — consumes health().available_models for dropdown)"
    - "07.1-05 (router — maps LlamaCppProviderError to HTTP 503/409)"
tech-stack:
  added: []
  patterns:
    - "Pattern 2 — literal-port with three v3 deviations (D-V3-04 rewrite under v3 patterns)"
    - "Top-level schema import for mockable parse_research_json in tests"
    - "test-only seam via _test_only_base_url instance attribute (never user input)"

key-files:
  created:
    - backend/medieval_forge/services/llm/llamacpp.py
  modified:
    - backend/medieval_forge/services/llm/__init__.py
    - backend/tests/unit/test_llamacpp_provider.py
    - backend/tests/unit/test_llm_registry.py

key-decisions:
  - "parse_research_json imported at module top level (not inline inside research()) — required for test monkeypatching via patch('...llamacpp.parse_research_json')"
  - "Registry count tests updated from 2->3 (Rule 3 deviation: adding LlamaCppProvider directly breaks existing len()==2 assertions)"
  - "Test 7 verifies queue receives delta strings during streaming, not a None sentinel — provider returns parsed ResearchResult, not None"
  - "ResearchResult fixture JSON uses {kingdoms:{}, duchies:{}, condados:[], baronies:{}} (correct schema) not {territories:[]} (wrong)"

patterns-established:
  - "Provider test pattern: _test_only_base_url instance attribute set by test fixture; never wired from user input; production path goes through launcher.status()"
  - "SSE streaming: chunks buffered via list append + ''.join(); malformed JSON raises LlamaCppProviderError (no silent skip)"

requirements-completed:
  - V3-LLM-OPT-IN
  - SC-3
  - SC-6

duration: 20min
completed: "2026-05-16"
---

# Phase 07.1 Plan 03: LlamaCppProvider + Registry Registration Summary

**LlamaCppProvider (223 LOC) with three D-V3-04 deviations — launcher-sourced base_url, Ollama-shape health() dict, no (server-default) sentinel — registered as third provider entry; 11 tests + 1 registry extension green.**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-05-16T17:18:55Z
- **Completed:** 2026-05-16T17:28:21Z
- **Tasks:** 1 (TDD: 2 commits — RED + GREEN)
- **Files modified:** 4

## Accomplishments

- `LlamaCppProvider` implemented per RESEARCH §Pattern 2 with all three D-V3-04 deviations enforced
- `LlamaCppProviderError` typed exception guarding SSRF (no localhost:8080 fallback) and malformed SSE
- `PROVIDERS["llamacpp"]` registered at import time in `__init__.py` alongside claude + ollama
- 11 provider tests green (SC-3a identity, SC-3b health shapes × 5, SC-3c streaming × 4 + review-fix #8)
- 5 registry tests green (3 updated + 1 pre-existing + 1 new `test_llamacpp_registered_at_import`)
- 302/302 unit tests pass, 13 skipped — zero Phase 07 regressions (SC-6 invariant held)

## Task Commits

1. **RED — add 12 failing tests** — `6c6775d` (test)
2. **GREEN — implement LlamaCppProvider + register** — `9ea9a34` (feat)

## Files Created/Modified

- `backend/medieval_forge/services/llm/llamacpp.py` — LlamaCppProvider class (223 LOC), LlamaCppProviderError, module-level imports for mockability
- `backend/medieval_forge/services/llm/__init__.py` — added LlamaCppProvider/LlamaCppProviderError imports + `register(LlamaCppProvider())`
- `backend/tests/unit/test_llamacpp_provider.py` — 11 real tests replacing Wave 0 scaffold (0 @pytest.mark.skip)
- `backend/tests/unit/test_llm_registry.py` — count tests updated 2→3; `test_llamacpp_registered_at_import` added

## Decisions Made

- `parse_research_json` moved to top-level import in `llamacpp.py` (was inline in `research()`) so `patch("...llamacpp.parse_research_json")` works correctly in Test 8.
- Registry count assertion tests updated from `len == 2` to `len == 3` as Rule 3 deviation (adding LlamaCppProvider at import directly causes them to fail — blocking issue caused by this task).
- `registry.py` itself unchanged — registration happens in `__init__.py` following the established claude/ollama pattern.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] Registry count tests updated 2->3**
- **Found during:** Task 1 (GREEN — registry registration)
- **Issue:** Existing `test_llm_registry_has_exactly_two_providers_after_package_import` and `test_llm_registry_list_providers_returns_sorted_ids` asserted `len == 2` and `set == {"claude", "ollama"}`. Adding `register(LlamaCppProvider())` at import time directly causes both to fail.
- **Fix:** Updated count assertions to `len == 3`, set to `{"claude", "ollama", "llamacpp"}`, list to `["claude", "llamacpp", "ollama"]` (sorted).
- **Files modified:** `backend/tests/unit/test_llm_registry.py`
- **Commit:** 9ea9a34

**2. [Rule 1 — Bug] Test fixtures used invalid ResearchResult JSON**
- **Found during:** Task 1 (GREEN — first test run)
- **Issue:** Tests 7, 8, 10 used `{"territories": []}` or plain strings as mock SSE content. `ResearchResult` requires `kingdoms`, `duchies`, `condados`, `baronies` — `territories` is not a valid field (raises pydantic ValidationError).
- **Fix:** Updated fixtures to use `{"kingdoms": {}, "duchies": {}, "condados": [], "baronies": {}}` (minimal valid JSON). Test 8 mock uses `parse_research_json` monkeypatch so arbitrary string content can be asserted for buffering behavior.
- **Files modified:** `backend/tests/unit/test_llamacpp_provider.py`
- **Commit:** 9ea9a34

**3. [Rule 1 — Bug] parse_research_json imported inline, breaking mock path**
- **Found during:** Task 1 (GREEN — Test 8 failure)
- **Issue:** `from .schemas import ResearchResult, parse_research_json` inside `research()` body means `patch("medieval_forge.services.llm.llamacpp.parse_research_json")` raises `AttributeError: module has no attribute 'parse_research_json'`.
- **Fix:** Moved import to module top level in `llamacpp.py`.
- **Files modified:** `backend/medieval_forge/services/llm/llamacpp.py`
- **Commit:** 9ea9a34

---

**Total deviations:** 3 auto-fixed (1 Rule 3 blocking, 2 Rule 1 bug)
**Impact on plan:** All fixes necessary for tests to pass. No scope creep. D-V3-04 compliance unaffected.

## D-V3-04 Compliance Evidence (grep-verified)

| Check | Result |
|-------|--------|
| `grep -c "(server-default)" llamacpp.py` | 4 — all in comments/docstrings only (Deviation #3 doc) |
| `grep -c "localhost:8080" llamacpp.py` | 2 — both in comments (review-fix #8 doc) |
| `grep -c "DEFAULT_BASE_URL" llamacpp.py` | 2 — both in comments (Deviation #1 doc) |
| `grep -c "from .llamacpp_launcher import" llamacpp.py` | 1 — Deviation #1 dependency |
| `grep -c "@pytest.mark.skip" test_llamacpp_provider.py` | 1 — in module docstring only (not a decorator) |
| `wc -l llamacpp.py` | 223 (≥100 required) |

## Threat Register Dispositions Verified

| Threat ID | Status |
|-----------|--------|
| T-07.1-03-01 SSRF via crafted base_url | Mitigated — `base_url` from `launcher.status()` only; no user-supplied path; test-only override seam never wired from user input |
| T-07.1-03-02 DoS via malformed JSON | Mitigated — `json.JSONDecodeError` raises `LlamaCppProviderError` (Test 9 green) |
| T-07.1-03-03 gguf filename disclosure | Accepted — single-user local app, consistent with Ollama pattern |
| T-07.1-03-04 provider impersonation | Accepted — `provider_id` hardcoded at import, PROVIDERS dict is module-private |

## Known Stubs

None — module fully implemented. No placeholder data flowing to UI (provider is backend-only; UI surface is plan 07.1-04's responsibility).

## Self-Check: PASSED

- `backend/medieval_forge/services/llm/llamacpp.py` — FOUND (223 lines)
- `backend/tests/unit/test_llamacpp_provider.py` — FOUND (11 tests, 0 static skips)
- `backend/tests/unit/test_llm_registry.py` — FOUND (5 tests, 1 new)
- Commit 6c6775d — RED (confirmed)
- Commit 9ea9a34 — GREEN (confirmed)
- 302 unit tests passing — VERIFIED
