---
phase: "07.1"
plan: "07"
subsystem: "frontend/research"
tags: ["research-dialog", "numeric-inputs", "vitest", "SC-1", "D-01", "D-02", "D-03", "D-04c"]
dependency_graph:
  requires: ["07.1-00", "07.1-04", "07.1-06"]
  provides: ["ResearchDialog two-input numeric period", "9 vitest cases SC-1e/SC-1f", "rodapé placeholder for 07.1-08"]
  affects: ["frontend/src/components/research/ResearchDialog.tsx", "frontend/src/hooks/useResearchStream.ts"]
tech_stack:
  added: []
  patterns: ["hasSeeded useState gate (review-fix #2)", "vi.mock + vi.mocked pattern for module-level mocks in vitest"]
key_files:
  created: []
  modified:
    - "frontend/src/components/research/ResearchDialog.tsx"
    - "frontend/src/components/research/__tests__/ResearchDialog.numeric.spec.tsx"
    - "frontend/src/components/research/__tests__/ResearchDialog.test.tsx"
decisions:
  - "useResearchStream.ts not modified: period_label was in ResearchDialog.tsx handleSubmit, not in the hook (plan incorrectly identified the file; fixed inline as Rule 3 deviation)"
  - "vi.mock with vi.mocked pattern chosen over vi.spyOn per-test to avoid vi.restoreAllMocks killing module mocks"
  - "ResearchDialog.test.tsx obsolete period-text seed test replaced with Início/Fim label presence check (Rule 1)"
metrics:
  duration: "~25 min"
  completed_date: "2026-05-16"
  tasks_completed: 2
  files_changed: 3
---

# Phase 07.1 Plan 07: Numeric Period Inputs + 9 Vitest Cases Summary

**One-liner:** ResearchDialog two-input numeric period swap (Início/Fim, type=number, min=1 max=2100) with hasSeeded seed-race fix, integer payload, rodapé placeholder, and 9 vitest cases covering SC-1e/SC-1f.

## What Was Built

### Task 1 — ResearchDialog two-input swap + rodapé link + useResearchStream payload

**File:** `frontend/src/components/research/ResearchDialog.tsx` (+124 / -49 LOC)

- Replaced single free-text `Período` TextField with two `<TextField.Root type="number">` inputs:
  - Labels "Início" and "Fim" per UI-SPEC §Copywriting Surface 1
  - `data-testid="research-period-start"` + `data-testid="research-period-end"`
  - `min={1}` `max={2100}` `step={1}` `inputMode="numeric"` on both
  - Helper text "Anos AD (1–2100). Início ≤ Fim." always rendered
  - Inline PT-BR error "O início deve ser menor ou igual ao fim." when start > end and both set

- `hasSeeded` useState gate (review-fix #2 — Gemini HIGH):
  - Seeds from `useProject(projectId).period_start/period_end` only when `(open && project && !hasSeeded)`
  - `hasSeeded` resets to `false` on `!open` to arm for next open
  - Old `wasOpenRef` pattern never existed in this file version; gate implemented clean

- `canSubmit` memo extended with `periodValid`: 1–2100 range + start ≤ end; deps updated to `[periodStart, periodEnd]`

- Submit JSON body: `period_start: periodStart, period_end: periodEnd` (integers); `period_label` removed

- Rodapé `justify="between"` + "Configurar provedores" placeholder link with `data-testid="auth-setup-sheet-trigger"` (onClick: console.log stub; plan 07.1-08 wires the real open-state)

- Pre-existing TS errors fixed: added `useRef` to React import; removed stale `period`/`setPeriod` state references

**Commit:** `2ce1b31` — `feat(07.1-07): ResearchDialog two-input numeric period swap (D-01/D-02/D-03)`

### Task 2 — ResearchDialog.numeric vitest (9 cases)

**File:** `frontend/src/components/research/__tests__/ResearchDialog.numeric.spec.tsx` (+276 / -14 LOC replacing Wave 0 scaffold)

| # | Test | Result |
|---|------|--------|
| 1 | renders Início and Fim number inputs with min=1 max=2100 step=1 | PASS |
| 2 | seeds inputs from Project.period_start and period_end on open transition | PASS |
| 3 | renders empty inputs when Project.period_start is null | PASS |
| 4 | disables CTA when periodStart > periodEnd with PT-BR error message | PASS |
| 5 | disables CTA when periodStart is 0 (below min) | PASS |
| 6 | disables CTA when periodEnd is 2101 (above max) | PASS |
| 7 | submit payload carries period_start and period_end as JSON integers not strings | PASS |
| 8 | Configurar provedores rodapé link rendered with auth-setup-sheet-trigger | PASS |
| 9 | period_inputs_seed_when_project_resolves_after_open (review-fix #2) | PASS |

**Also updated:** `ResearchDialog.test.tsx` — replaced obsolete `renders Período seeded from region display_name` test (tested removed behavior) with `renders Início and Fim labels for the numeric period inputs`

**Commit:** `ae4b7c1` — `test(07.1-07): ResearchDialog numeric vitest (9 cases)`

## Verification Evidence

```
# TypeScript clean
npx tsc --noEmit → 0 errors

# period_label key grep gate
grep -rn "period_label:" frontend/src/ → 0 hits (exit 1)
# (mentions in test assertion comment + expect('period_label' in body) are not payload usage)

# 9 cases green
npx vitest run src/components/research/__tests__/ResearchDialog.numeric.spec.tsx
→ Tests: 9 passed (9)

# Full suite no regressions
npx vitest run
→ Test Files: 46 passed | 1 skipped (47)
→ Tests: 305 passed | 8 skipped (313)
```

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Pre-existing TypeScript errors in ResearchDialog.tsx**
- **Found during:** Task 1 baseline check
- **Issue:** File had 5 TS errors from a prior partial edit: `useRef` missing from import, `period`/`setPeriod` state references still in `handleSubmit` and form body despite state having been renamed to `periodStart`/`periodEnd` in an earlier partial session
- **Fix:** Added `useRef` to React import; removed all `period`/`setPeriod` references; replaced with `periodStart`/`periodEnd` integers
- **Files modified:** `frontend/src/components/research/ResearchDialog.tsx`
- **Commit:** `2ce1b31`

**2. [Rule 1 - Bug] Plan incorrectly identified useResearchStream.ts as the file containing period_label**
- **Found during:** Task 1 Step 2
- **Issue:** Plan said "find the JSON.stringify body for /api/v3/research/start in useResearchStream.ts" — but `useResearchStream.ts` is a pure EventSource consumer with no POST. The `period_label` key was in `ResearchDialog.tsx` `handleSubmit` (confirmed by grep)
- **Fix:** Applied the `period_label → period_start/period_end` swap in `ResearchDialog.tsx` only; `useResearchStream.ts` left untouched
- **Files modified:** `frontend/src/components/research/ResearchDialog.tsx` (not useResearchStream.ts)
- **Commit:** `2ce1b31`

**3. [Rule 1 - Regression] Pre-existing ResearchDialog.test.tsx test tested removed behavior**
- **Found during:** Task 2 full suite run
- **Issue:** `renders Período seeded from region display_name` used `getByDisplayValue('Iberia 868 AD')` — this tested the old single-text Período field behavior that no longer exists
- **Fix:** Replaced with `renders Início and Fim labels for the numeric period inputs` — verifies the new label presence; full numeric contract covered by ResearchDialog.numeric.spec.tsx
- **Files modified:** `frontend/src/components/research/__tests__/ResearchDialog.test.tsx`
- **Commit:** `ae4b7c1`

**4. [Rule 3 - Blocking] vi.restoreAllMocks() in afterEach killed module-level vi.mock registrations**
- **Found during:** Task 2 first test run (8/9 failing with "Cannot destructure property 'data' of useProviders")
- **Issue:** Initial test used `vi.spyOn` per-test + `vi.restoreAllMocks()` in afterEach; `restoreAllMocks` reverts module mocks to undefined, breaking subsequent tests
- **Fix:** Switched to `vi.mock(...)` at module level + `vi.mocked()` references; `beforeEach` calls `mockReturnValue` to reset per-test defaults; `afterEach` uses `vi.unstubAllGlobals()` only (not restoreAllMocks)
- **Files modified:** `frontend/src/components/research/__tests__/ResearchDialog.numeric.spec.tsx`
- **Commit:** `ae4b7c1`

## Known Stubs

- **Configurar provedores onClick** — `console.log('AuthSetupSheet trigger clicked')` in `ResearchDialog.tsx` line ~381. Intentional stub; plan 07.1-08 replaces with `() => setAuthSheetOpen(true)` when `AuthSetupSheet` component is built. `data-testid="auth-setup-sheet-trigger"` is in place for plan 07.1-08 to grep.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. Client-side validation in `canSubmit` is UX-only (per T-07.1-07-01); security boundary is backend pydantic `conint` from plan 07.1-04.

## Self-Check: PASSED

| Check | Result |
|-------|--------|
| ResearchDialog.tsx exists | FOUND |
| ResearchDialog.numeric.spec.tsx exists | FOUND |
| 07.1-07-SUMMARY.md exists | FOUND |
| commit 2ce1b31 exists | FOUND |
| commit ae4b7c1 exists | FOUND |
| `period_label:` grep gate (frontend/src/) | 0 hits — PASS |
| `hasSeeded` occurrences in ResearchDialog.tsx | 5 hits — PASS |
| `wasOpenRef` as code (not comment) | 0 functional uses — PASS (1 mention in explanatory comment only) |
