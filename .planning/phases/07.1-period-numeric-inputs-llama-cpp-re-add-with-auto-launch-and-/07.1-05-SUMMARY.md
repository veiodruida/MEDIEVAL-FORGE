---
phase: "07.1"
plan: "05"
subsystem: backend
tags: [llamacpp, fastapi, router, lifespan, e2e-tests, subprocess]
dependency_graph:
  requires: ["07.1-02", "07.1-03"]
  provides: ["POST /api/v3/llm/llamacpp/launch", "DELETE /api/v3/llm/llamacpp/launch", "GET /api/v3/llm/llamacpp/launch", "SC-4e-closed"]
  affects: ["main.py lifespan", "07.1-06 useLlamacppStatus hook consumer"]
tech_stack:
  added: []
  patterns:
    - "asyncio.to_thread wrapping sync shutdown to avoid await-on-bool TypeError"
    - "APIRouter prefix=/v3/llm; main.py adds /api at mount"
    - "Exception hierarchy -> HTTP status code mapping (400/404/503/409)"
key_files:
  created:
    - backend/medieval_forge/api/v3/llamacpp.py
    - (scaffold replaced) backend/tests/e2e/test_llamacpp_launch_lifecycle.py
  modified:
    - backend/medieval_forge/main.py
decisions:
  - "7 tests implemented (not 5): plan objective was stale; must_haves.artifacts and behavior listed 7 — review-fix #4 added GET-running-false + GET-running-true"
  - "import logging + logger added to main.py (Rule 3 blocker fix): lifespan snippet used logger.info/warning without prior definition"
metrics:
  duration_minutes: 10
  completed_date: "2026-05-16"
  tasks_completed: 2
  files_changed: 3
---

# Phase 07.1 Plan 05: llamacpp Router + Lifespan + e2e Tests Summary

**One-liner:** FastAPI router POST/DELETE/GET `/api/v3/llm/llamacpp/launch` wired from `llamacpp_launcher` with exception→HTTP mapping; lifespan D-08b shutdown hook; 7 e2e tests green.

## Tasks Completed

| # | Task | Commit | Key Files |
|---|------|--------|-----------|
| 1 | api/v3/llamacpp router + main.py mount + lifespan | `1e42b02` | `api/v3/llamacpp.py` (128 LOC), `main.py` |
| 2 | 7 e2e tests (Wave 0 scaffold replaced) | `143e9bf` | `tests/e2e/test_llamacpp_launch_lifecycle.py` |

## What Was Built

### Task 1 — Router + main.py

**`backend/medieval_forge/api/v3/llamacpp.py`** (128 LOC):
- `POST /api/v3/llm/llamacpp/launch` — calls `launcher_launch(body.model)`; returns `{ok, base_url, pid, model, started_at}`
- `DELETE /api/v3/llm/llamacpp/launch` — `await asyncio.to_thread(launcher_shutdown)`; returns `{ok: True, was_running: bool}`
- `GET /api/v3/llm/llamacpp/launch` (review-fix #4) — canonical `{running, pid, model, base_url, started_at}` sourced from `launcher_status()`
- Exception mapping: `LlamacppInvalidModelFilename` → 400, `LlamacppModelNotFound` → 404, `LlamacppBinaryMissing` → 503, `LlamacppLaunchConflict` → 409

**`backend/medieval_forge/main.py`** edits:
- Added `import asyncio`, `import logging`, `logger = logging.getLogger(__name__)` at module top
- Router mount: `app.include_router(llamacpp_v3.router, prefix="/api")`
- Lifespan D-08b hook: `was_running = await asyncio.to_thread(llamacpp_shutdown)` with logger.info + except guard

### Task 2 — 7 e2e Tests

All 7 tests pass in 0.14s using `fake_subprocess` fixture (no real llama-server needed):
1. `test_post_launch_then_get_status_then_delete_process_gone` — full lifecycle
2. `test_post_launch_idempotent_same_model_returns_200` — idempotency, same pid
3. `test_post_launch_conflict_different_model_returns_409` — 409 with PT-BR "ativo" + "DELETE"
4. `test_delete_launch_when_no_server_returns_200_was_running_false` — Discretion #7
5. `test_post_launch_path_traversal_returns_400` — T-07.1-05-02 path-traversal guard
6. `test_get_launch_returns_running_false_when_no_process` (review-fix #4)
7. `test_get_launch_reflects_active_process` (review-fix #4)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocker] Missing `logger` in main.py lifespan snippet**
- **Found during:** Task 1, before writing
- **Issue:** Plan's lifespan snippet used `logger.info()` and `logger.warning()` but main.py had no `import logging` nor `logger` definition — would NameError on app shutdown
- **Fix:** Added `import asyncio`, `import logging`, `logger = logging.getLogger(__name__)` to top of main.py
- **Files modified:** `backend/medieval_forge/main.py`
- **Commit:** `1e42b02`

**2. [Rule 3 - Plan count alignment] 7 tests implemented, not 5**
- **Found during:** Task 2 planning
- **Issue:** Plan `<objective>` and `<output>` section said "5 e2e tests" but `must_haves.artifacts`, `<behavior>`, and `<acceptance_criteria>` all specified 7 tests (review-fix #4 added GET-running-false + GET-running-true)
- **Fix:** Implemented 7 tests per the authoritative must_haves spec; the "5" in objective was stale
- **Files modified:** `backend/tests/e2e/test_llamacpp_launch_lifecycle.py`
- **Commit:** `143e9bf`

## Verification Results

```
backend/tests/e2e/test_llamacpp_launch_lifecycle.py  7 passed in 0.14s
backend/tests/unit/ + backend/tests/e2e/             350 passed, 1 skipped
```

- `python -c "from medieval_forge.main import app; print([(r.methods, r.path) for r in app.routes if 'llamacpp' in r.path])"` → POST, DELETE, GET all on `/api/v3/llm/llamacpp/launch`
- `grep -n "await llamacpp_shutdown()"` → 0 hits (review-fix #4 gate: no raw await on bool)
- `grep -n "shutdown_sync"` in llamacpp.py → 0 code hits (only doc comment)

## Known Stubs

None — all endpoints fully wired to `llamacpp_launcher`.

## Threat Flags

None — no new network surface beyond what is documented in the plan's threat model.

## Self-Check: PASSED

- `backend/medieval_forge/api/v3/llamacpp.py` — EXISTS (128 LOC, ≥80 required)
- `backend/medieval_forge/main.py` contains `app.include_router(llamacpp_v3.router` — CONFIRMED
- `backend/medieval_forge/main.py` contains `was_running = await asyncio.to_thread(llamacpp_shutdown)` — CONFIRMED
- `backend/tests/e2e/test_llamacpp_launch_lifecycle.py` contains 7 test functions — CONFIRMED
- Commits `1e42b02` and `143e9bf` exist — CONFIRMED
