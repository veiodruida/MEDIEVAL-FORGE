---
phase: 07.1
plan: 10
type: execute
wave: 8
depends_on: ["07.1-04", "07.1-05", "07.1-07", "07.1-08", "07.1-09"]
files_modified:
  - ".planning/ROADMAP.md"
  - ".planning/phases/07.1-period-numeric-inputs-llama-cpp-re-add-with-auto-launch-and-/07.1-VERIFICATION.md"
autonomous: true
requirements:
  - V3-LLM-OPT-IN
  - SC-6

must_haves:
  truths:
    - "Iberia 868 YAML parity test stays byte-identical (D-12 zero-LLM invariant from Phase 07)"
    - "All Phase 07 regression tests stay green (SC-6)"
    - "Global frontend grep gate: grep -rn 'period_label' frontend/src/ frontend/tests/ returns 0 hits"
    - "Global backend grep gate: grep -rn 'period_label' backend/ returns 0 hits"
    - "Full backend pytest + frontend vitest + Playwright UAT all green in one sweep"
    - "No new dependencies introduced (httpx, pydantic, FastAPI, Radix Themes, TanStack Query versions unchanged)"
    - "ROADMAP SC-1 wording reconciled with CONTEXT D-02 — reads `start <= end` (review-fix #3)"
    - "review-fix uptake recorded: all 10 ranked fixes from 07.1-REVIEWS.md applied — #1 (AuthSetupSheet isRunning from useLlamacppStatus, mutations reset on close), #2 (ResearchDialog hasSeeded gate replacing wasOpenRef), #3 (ROADMAP SC-1 wording), #4 (single sync shutdown + GET /launch status endpoint), #5 (runner _QID_DISPLAY_NAMES — broader Wikidata lookup tracked DEFERRED), #6 (useLlamacppStatus hook + anchored Playwright selectors), #7 (provider partial-SSE + malformed-SSE + final-payload-shape tests), #8 (no production localhost:8080 fallback — strict launcher-state-only), #9 (symlink-escape launcher test), #10 (empty-models hint loading guard)"
    - "Tracked DEFERRED for future phase: broader Wikidata QID→name lookup beyond the hardcoded _QID_DISPLAY_NAMES dict (review-fix #5 follow-up; only Q29, Q45, Q43175 covered now)"
  artifacts:
    - path: ".planning/phases/07.1-period-numeric-inputs-llama-cpp-re-add-with-auto-launch-and-/07.1-VERIFICATION.md"
      provides: "Cross-cutting verification report; phase closure gate"
      contains: "D-12"
  key_links:
    - from: "Phase 07.1 changes"
      to: "Phase 07 D-12 zero-LLM parity invariant"
      via: "test_iberia_868_yaml.py re-run"
      pattern: "test_iberia_868_yaml"
---

<objective>
Wave 8: phase closure gate. Runs the parity invariant (D-12 from Phase 07: zero-LLM Iberia byte-identical), the full backend + frontend + UAT suites, and the global `period_label` grep gates one final time. Produces a VERIFICATION.md report listing every SC + every D-XX with its closing evidence.

Purpose: SC-6 closed (no regressions). The Phase 07 zero-LLM parity test must remain byte-identical despite Phase 07.1's cache schema change — because Phase 07.1's changes are research-path only, not geometric-path. If parity breaks, the diff is the bug.

Output: 1 VERIFICATION.md report + ROADMAP.md plan checklist updated. No production code modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-period-numeric-inputs-llama-cpp-re-add-with-auto-launch-and-/07.1-CONTEXT.md
@.planning/phases/07.1-period-numeric-inputs-llama-cpp-re-add-with-auto-launch-and-/07.1-RESEARCH.md
@backend/tests/parity/test_iberia_868_yaml.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Full-suite verification + grep gates + VERIFICATION.md report</name>
  <read_first>
    - backend/tests/parity/test_iberia_868_yaml.py (D-12 invariant test from Phase 07)
    - .planning/phases/07.1-period-numeric-inputs-llama-cpp-re-add-with-auto-launch-and-/07.1-CONTEXT.md (D-01..D-09 list to verify each closure)
    - .planning/ROADMAP.md (SC-1..SC-6 list to verify each closure)
    - All 07.1-NN-SUMMARY.md files from completed plans (NN = 00 through 09)
  </read_first>
  <files>
    .planning/phases/07.1-period-numeric-inputs-llama-cpp-re-add-with-auto-launch-and-/07.1-VERIFICATION.md
    .planning/ROADMAP.md
  </files>
  <action>
**Step 1: Run the parity invariant (D-12 zero-LLM byte-identical).**

```bash
cd backend && python -m pytest tests/parity/test_iberia_868_yaml.py -v 2>&1 | tail -10
```
Expected: passes. The Iberia 868 YAML pipeline does NOT touch research_cache, runner.py's research path, or the LLM stack. Phase 07.1 changes were isolated to opt-in metadata; parity stays green.

**If parity fails:** STOP. Diff the output PNGs/JSONs against the Reconquista golden. The most likely cause is an accidental side-effect from runner.py edits in plan 07.1-04. Open a gap-closure plan.

**Step 2: Run the full backend test suite.**

```bash
cd backend && python -m pytest tests/unit/ tests/e2e/ tests/parity/ 2>&1 | tail -10
```
Expected: all green. Sum should be `pre-07.1 count + 16 (test_llamacpp_launcher) + 7 (test_llamacpp_provider) + 1 (test_llm_registry extension) + 6 (test_llm_base_health_status) + 5 (test_period_numeric_validation) + 3 (test_prompt_period_range) + 4 (test_research_cache_key) + 5 (test_llamacpp_launch_lifecycle)` = pre-07.1 + 47 new tests.

**Step 3: Run the full frontend vitest suite.**

```bash
cd frontend && pnpm vitest run 2>&1 | tail -10
```
Expected: all green. Sum should be `pre-07.1 count + 8 (ResearchDialog.numeric) + 8 (AuthSetupSheet)` = pre-07.1 + 16 new tests.

**Step 4: Run the full Playwright UAT suite.**

```bash
cd frontend && pnpm playwright test 2>&1 | tail -10
```
Expected: all green. Phase 07 + Phase 05 + Phase 04 specs all remain green; plan 07.1-09 added 5 new specs.

**Step 5: Run global grep gates.**

```bash
grep -rn "period_label" backend/ && echo "FAIL backend grep" && exit 1 || echo "backend grep OK"
grep -rn "period_label" frontend/src/ frontend/tests/ && echo "FAIL frontend grep" && exit 1 || echo "frontend grep OK"
```
Both MUST return 0 hits. If any residual found, surgically remove before declaring phase done.

**Step 6: Verify no new dependencies introduced.**

```bash
cd backend && pip freeze | grep -iE "httpx|pydantic|fastapi|alembic" | tee /tmp/deps_after.txt
cd frontend && cat package.json | grep -E "radix-ui/themes|tanstack/react-query|playwright|vitest"
```
Compare against pre-07.1 versions (recorded in 07.1-RESEARCH.md §Standard Stack table). NO version bumps; NO new packages.

**Step 7: Generate `07.1-VERIFICATION.md`.**

Create `.planning/phases/07.1-period-numeric-inputs-llama-cpp-re-add-with-auto-launch-and-/07.1-VERIFICATION.md`:

```markdown
# Phase 07.1 — Verification Report

**Verified:** 2026-05-15
**Status:** {GREEN | RED}

## Success Criteria Closure

| SC | Description | Closing Evidence |
|----|-------------|------------------|
| SC-1 | ResearchDialog renders period_start + period_end numeric inputs with `start <= end` validation (snapshot-style equality allowed per CONTEXT D-02 + review-fix #3 ROADMAP reconciliation); payload carries integers | Plans 07.1-04 (backend) + 07.1-07 (frontend) + 07.1-09 (uat). Tests: test_period_numeric_validation.py 5/5, ResearchDialog.numeric.spec.tsx 9/9 (+1 review-fix #2), research_period_numeric.spec.ts 2/2 |
| SC-2 | Prompt template composes period range from start/end | Plan 07.1-04. Tests: test_prompt_period_range.py 3/3, test_research_cache_key.py 4/4 |
| SC-3 | LlamaCppProvider registered; health() returns {ok, message, available_models} | Plans 07.1-01 + 07.1-03. Tests: test_llm_base_health_status.py 6/6, test_llamacpp_provider.py 7/7, test_llm_registry.py llamacpp registration assertion |
| SC-4 | POST /api/v3/llm/llamacpp/launch spawns llama-server; idempotent; cleanup on app exit + DELETE | Plans 07.1-02 + 07.1-05. Tests: test_llamacpp_launcher.py 16/16, test_llamacpp_launch_lifecycle.py 5/5 |
| SC-5 | AuthSetupSheet llama.cpp panel + model dropdown + "Levantar servidor" | Plans 07.1-06 + 07.1-08 + 07.1-09. Tests: AuthSetupSheet.spec.tsx 8/8, llamacpp_launch.spec.ts 3/3 |
| SC-6 | All Phase 07 tests stay green; new tests cover numeric-period validation, llama.cpp listing, launch/shutdown lifecycle | This plan. Phase 07 backend + frontend + Playwright suites all green; D-12 Iberia parity byte-identical. |

## Decision Closure

| D-XX | Description | Plan | Closing Evidence |
|------|-------------|------|------------------|
| D-01 | AD positive only, range [1, 2100] | 07.1-04 | `conint(ge=1, le=2100)` in StartResearchBody; tests test_period_start_below_min_rejects_zero, test_period_end_above_max_rejects_2101 |
| D-02 | start <= end (equality allowed) | 07.1-04 | `@model_validator(mode='after')` in StartResearchBody; tests test_period_start_greater_than_end_rejects_1000_vs_868, test_period_start_equals_end_accepts_snapshot_868_868 |
| D-03 | Seed defaults from Project.period_start/period_end | 07.1-06 + 07.1-07 | useProject hook exposes period fields; wasOpenRef gates seed (Pitfall 10); test seeds_inputs_from_Project_period_start_period_end_on_open_transition |
| D-04 | Truncate research_cache (forward-only) | 07.1-04 | Alembic 0007 `op.execute('DELETE FROM research_cache')`; verified with `alembic upgrade head` |
| D-04b | Meta sidecar swaps period: str for period_start: int + period_end: int | 07.1-04 | runner.py line 224 swap (verified via grep "period_start": runner.py) |
| D-04c | Atomic API body swap | 07.1-04 | StartResearchBody dropped period_label; grep -rn period_label backend/ returns 0 |
| D-05 | New AuthSetupSheet Radix Dialog nested from ResearchDialog | 07.1-08 | AuthSetupSheet.tsx with Tabs.Root; rodapé link onClick → setAuthSheetOpen(true) |
| D-06 | Claude paste-API-key stays inline in ResearchDialog | 07.1-08 | Claude tab in AuthSetupSheet is `disabled` placeholder; ResearchDialog ProviderSelector untouched |
| D-07a | Models dir env override + ~/.medieval-forge/models/ fallback | 07.1-02 | llamacpp_launcher.models_dir() implementation; tests test_list_models_* |
| D-07b | llama-server binary via shutil.which + env override | 07.1-02 | _resolve_binary(); tests test_resolve_binary_* |
| D-08 | Launch endpoint: auto-pick port, idempotent by model path | 07.1-02 + 07.1-05 | socket.bind(('',0)) + module-level _PROCESS singleton; tests test_launch_idempotent_*, test_launch_conflict_*, test_post_launch_conflict_different_model_returns_409 |
| D-08b | Lifespan + atexit shutdown + DELETE endpoint | 07.1-02 + 07.1-05 | main.py lifespan calls llamacpp_shutdown(); _register_atexit_once(); DELETE handler |
| D-09 | HealthStatus extends with available_models (additive, no rename) | 07.1-01 | base.py field added; Claude/Ollama source unchanged (git diff empty) |
| D-12 (Phase 07 carry) | Zero-LLM parity invariant | 07.1-10 (this plan) | test_iberia_868_yaml.py passes byte-identical |

## Test Counts

| Suite | Pre-07.1 | Added | Total |
|-------|----------|-------|-------|
| Backend pytest unit | {N} | 47 | {N+47} |
| Backend pytest e2e | {M} | 5 | {M+5} |
| Backend parity | {P} | 0 | {P} |
| Frontend vitest | {V} | 16 | {V+16} |
| Playwright UAT | {U} | 5 | {U+5} |

## Grep Gates

- `grep -rn "period_label" backend/`: 0 hits ✓
- `grep -rn "period_label" frontend/src/ frontend/tests/`: 0 hits ✓
- `grep -c "shell=True" backend/medieval_forge/services/llm/llamacpp_launcher.py`: 0 ✓

## Threat Register Disposition

| Threat ID | Disposition | Verified By |
|-----------|-------------|-------------|
| T-07.1-05-01 (subprocess command injection) | mitigated | list-form Popen, grep shell=True returns 0 |
| T-07.1-05-02 (path traversal) | mitigated | test_launch_rejects_path_traversal_dotdot_filename, test_post_launch_path_traversal_returns_400 |
| T-07.1-05-03 (LLAMA_SERVER_BIN arbitrary) | accepted | documented in launcher docstring |
| T-07.1-05-04 (port race) | accepted | D-08 explicit, Pitfall 4 documented |
| T-07.1-05-05 (zombie process on hard crash) | accepted | D-08b explicit, user-facing remedy documented |
| T-07.1-05-06 (pipe deadlock) | mitigated | stdout=stderr=DEVNULL |
| T-07.1-05-07 (multiple processes) | mitigated | _PROCESS singleton |
| T-07.1-07-01 (unauthenticated localhost API) | accepted | consistent with Phase 03 D-13 architecture |
| T-07.1-04-01 (out-of-domain period) | mitigated | conint + model_validator + 5 tests |
| T-07.1-04-03 (cache poisoning) | mitigated | D-04c atomic + grep gate |
| T-07.1-04-04 (truncate cache) | accepted | D-04 explicit user decision |

## Final Status

Phase 07.1 closes with all 6 SC green, all 13 D-XX honored, and D-12 zero-LLM parity invariant preserved. No new dependencies. Frontend + backend grep gates clean.

Ready for `/gsd-verify-work 07.1`.
```

Fill in actual test counts by parsing the pytest/vitest/playwright output.

**Step 8: Update ROADMAP.md plan checklist.**

In `.planning/ROADMAP.md` §"Phase 07.1", mark all 11 plans as `[x]` (00 through 10). Update Status from "planned" to "complete".

**Step 9: Commit.**
```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(07.1-10): phase verification report + ROADMAP closure" --files .planning/phases/07.1-period-numeric-inputs-llama-cpp-re-add-with-auto-launch-and-/07.1-VERIFICATION.md .planning/ROADMAP.md
```
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/parity/test_iberia_868_yaml.py -v 2>&1 | tail -5 && echo "---" && grep -rn "period_label" backend/ frontend/src/ frontend/tests/ 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/parity/test_iberia_868_yaml.py -v` exits 0 (D-12 invariant green)
    - `cd backend && python -m pytest tests/ -q 2>&1 | grep -E "(passed|failed|error)"` reports no failures
    - `cd frontend && pnpm vitest run 2>&1 | grep -E "(passed|failed)"` reports no failures
    - `cd frontend && pnpm playwright test --reporter=line 2>&1 | tail -3` reports no failures
    - `grep -rn "period_label" backend/ frontend/src/ frontend/tests/` returns 0 hits across both trees
    - `.planning/phases/07.1-period-numeric-inputs-llama-cpp-re-add-with-auto-launch-and-/07.1-VERIFICATION.md` exists with all 6 SC and 13 D-XX rows filled in
    - `.planning/ROADMAP.md` Phase 07.1 section shows all 11 plans marked `[x]` and Status "complete"
  </acceptance_criteria>
  <done>Phase 07.1 closed; verification report committed; ROADMAP updated; ready for `/gsd-verify-work 07.1`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none-new | Verification plan; reads test outputs only, makes no production code change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-10-01 | none | Verification report | accept | Documentation-only; no behavior change. Final phase closure gate. |
</threat_model>

<verification>
- Parity test green (D-12 invariant preserved)
- Full backend + frontend + Playwright suites green
- Grep gates clean (zero period_label hits anywhere)
- VERIFICATION.md exists with all SC + all D-XX closure rows
- ROADMAP.md Phase 07.1 marked complete
</verification>

<success_criteria>
- SC-6 fully closed (no regressions)
- D-12 zero-LLM parity invariant preserved
- Phase 07.1 ready for `/gsd-verify-work 07.1`
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-period-numeric-inputs-llama-cpp-re-add-with-auto-launch-and-/07.1-10-SUMMARY.md` with final phase-closure facts:
- Test counts table (pre-07.1 → post-07.1)
- D-12 parity verdict
- All 6 SC + all 13 D-XX status (green/needs-followup)
- Commit hash for `docs(07.1-10): phase verification + ROADMAP closure`
</output>
</content>
</invoke>