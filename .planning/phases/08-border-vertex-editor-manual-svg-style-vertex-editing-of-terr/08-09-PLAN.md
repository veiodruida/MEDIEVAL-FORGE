---
phase: 08
plan: 09
type: execute
wave: 7
depends_on: [08-03b, 08-04]
autonomous: true
requirements: [BRANCH-01, BRANCH-02, BRANCH-04, DAG-03]
files_modified:
  - frontend/src/components/editor/BranchPicker.tsx
  - frontend/src/components/editor/NewBranchDialog.tsx
  - frontend/src/components/editor/RenameBranchDialog.tsx
  - frontend/src/components/editor/DeleteBranchDialog.tsx
  - frontend/src/components/editor/CopyBranchToMainDialog.tsx
  - frontend/src/components/editor/SliderConflictDialog.tsx
  - frontend/src/components/editor/SnapshotTimeline.tsx
  - frontend/src/components/workspace/WorkspaceToolbar.tsx
  - frontend/src/components/editor/__tests__/BranchPicker.test.tsx
  - frontend/src/components/editor/__tests__/SliderConflictDialog.test.tsx

must_haves:
  truths:
    - "BranchPicker = Radix Select dropdown in toolbar, left of 'Generate Map' (D-13)"
    - "Dropdown items sorted by updated_at desc; format 'main (12 edits)' per UI-SPEC Discretion"
    - "Below Separator: 'Nova ramificação...', 'Renomear...', 'Excluir' (Excluir disabled on main per D-15)"
    - "NewBranchDialog: TextField + Criar/Cancelar buttons; calls useCreateBranch"
    - "RenameBranchDialog: TextField default = current name"
    - "DeleteBranchDialog: red confirm copy per UI-SPEC"
    - "CopyBranchToMainDialog: red confirm; calls POST replace-main endpoint OR client-side fetch+restore (planner picks; recommend backend endpoint added inline)"
    - "SliderConflictDialog: opens AFTER auto-snapshot succeeds (Pitfall 9) — frontend POSTs snapshot first; only on 201 opens dialog"
    - "SnapshotTimeline = floating Card listing snapshots reverse-chronological with Restore button each (RESEARCH Open Q3)"
  artifacts:
    - path: "frontend/src/components/editor/BranchPicker.tsx"
      provides: "Radix Select dropdown"
      min_lines: 60
    - path: "frontend/src/components/editor/SliderConflictDialog.tsx"
      provides: "Conflict modal per D-19 + Pitfall 9 ordering"
      contains: "snapshot_id"
  key_links:
    - from: "WorkspaceToolbar"
      to: "BranchPicker"
      via: "toolbar adds branch zone left of Generate Map button per UI-SPEC §Toolbar Layout"
      pattern: "BranchPicker"
---

<objective>
Wave 7 branching UX. All branch + snapshot dialogs + the toolbar picker + slider-conflict modal (D-19). Per UI-SPEC §Modals: 6 dialogs total. Per Pitfall 9: SliderConflictDialog opens AFTER snapshot 201, never before.

Output: 7 new components + WorkspaceToolbar extension + 2 test files filled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/08-.../08-CONTEXT.md §D-10..D-15, D-19
@.planning/phases/08-.../08-UI-SPEC.md §Branch Picker Zone + §Modals/Dialogs (verbatim)
@.planning/phases/08-.../08-RESEARCH.md §Pitfall 9 (snapshot-before-modal ordering) + §Open Q3 (snapshot timeline)
@frontend/src/components/workspace/WorkspaceToolbar.tsx
@frontend/src/api/branches.ts
@frontend/src/api/snapshots.ts
@frontend/src/stores/useEditorStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BranchPicker + 4 branch dialogs + WorkspaceToolbar wiring</name>
  <files>frontend/src/components/editor/BranchPicker.tsx, frontend/src/components/editor/NewBranchDialog.tsx, frontend/src/components/editor/RenameBranchDialog.tsx, frontend/src/components/editor/DeleteBranchDialog.tsx, frontend/src/components/editor/CopyBranchToMainDialog.tsx, frontend/src/components/workspace/WorkspaceToolbar.tsx, frontend/src/components/editor/__tests__/BranchPicker.test.tsx</files>
  <read_first>
    - frontend/src/components/workspace/WorkspaceToolbar.tsx (existing toolbar zones)
    - .planning/phases/08-.../08-UI-SPEC.md §Branch Picker Zone + §New Branch Dialog + §Rename + §Delete + §Copy to Main
    - frontend/src/api/branches.ts (from 08-04)
    - Wave 0 stub: BranchPicker.test.tsx
  </read_first>
  <behavior>
    - Test: BranchPicker renders Radix Select trigger with current branch name
    - Test: dropdown lists all branches from useBranches, sorted by updated_at desc; format "name (N edits)" includes edits_since_snapshot
    - Test: clicking "Nova ramificação..." opens NewBranchDialog
    - Test: clicking "Excluir" with main branch active → button disabled; with non-main → opens DeleteBranchDialog
    - Test: NewBranchDialog Criar button calls useCreateBranch; on success closes dialog + sets active branch + calls switchBranch
    - Test: SliderConflictDialog only opens after snapshot 201 (Pitfall 9 — verified via fetch mock sequencing)
  </behavior>
  <action>
**Step 1 — `BranchPicker.tsx`:** Radix `Select.Root` driven by useBranches + useEditorStore.activeBranchId. Trigger shows current name. Items: branches + Separator + 3 action items. Wire `onValueChange` to switchBranch (08-04). Use UI-SPEC Discretion: include `(N edits)` per item.

**Step 2 — `NewBranchDialog.tsx`:** Radix Dialog with TextField + Criar/Cancelar. Body of useCreateBranch mutation. UI copy verbatim from UI-SPEC.

**Step 3 — `RenameBranchDialog.tsx`:** Dialog + TextField default = current name; PATCH via useRenameBranch.

**Step 4 — `DeleteBranchDialog.tsx`:** Red confirm; DELETE via useDeleteBranch. Closed automatically if branch was main (disabled in picker, never opens).

**Step 5 — `CopyBranchToMainDialog.tsx`:** Red confirm. Implementation: fetches source snapshot payload → restore → POST snapshot to main branch with trigger='manual' + payload from source. Wholesale replace per D-14.

**Step 6 — `WorkspaceToolbar.tsx`:** Add Branch Picker zone left of "Gerar Mapa" button per UI-SPEC §Toolbar Layout.

**Step 7 — fill BranchPicker.test.tsx** with 6 tests.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/editor/__tests__/BranchPicker.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 6 tests pass
    - 5 dialog files exist + BranchPicker
    - WorkspaceToolbar shows picker
    - main branch Excluir item disabled
    - tsc clean
  </acceptance_criteria>
  <done>Branch UX (picker + 4 dialogs) committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SliderConflictDialog (D-19/Pitfall 9) + SnapshotTimeline + Slider integration</name>
  <files>frontend/src/components/editor/SliderConflictDialog.tsx, frontend/src/components/editor/SnapshotTimeline.tsx, frontend/src/components/canvas/ParameterSidebar.tsx, frontend/src/components/editor/__tests__/SliderConflictDialog.test.tsx</files>
  <read_first>
    - frontend/src/components/canvas/ParameterSidebar.tsx (Phase 04 slider dispatch — useParameterStudioDispatch)
    - .planning/phases/08-.../08-UI-SPEC.md §Slider Conflict Modal (verbatim copy)
    - .planning/phases/08-.../08-RESEARCH.md §Pitfall 9 (sequencing)
    - frontend/src/api/snapshots.ts (from 08-04 — useCreateSnapshot)
    - Wave 0 stub: SliderConflictDialog.test.tsx
  </read_first>
  <behavior>
    - Test: dialog renders only when prop open=true
    - Test: pre-snapshot fetch ordering — when slider changes with editLog.length > 0, ParameterSidebar calls useCreateSnapshot FIRST; only on 201 opens dialog
    - Test: snapshot 5xx → no dialog opens; toast shown instead
    - Test: dialog "Confirmar e regenerar" red button calls slider POST /render; dialog "Cancelar" reverts slider value
    - Test: SnapshotTimeline lists snapshots reverse-chronological with Restore button each
    - Test: Restore button calls useRestoreSnapshot → switchBranch with payload
  </behavior>
  <action>
**Step 1 — `SliderConflictDialog.tsx`:** Radix Dialog with copy verbatim from UI-SPEC §Slider Conflict Modal. Props: `{open, branchName, snapshotSeq, onConfirm, onCancel}`. Buttons: red "Confirmar e regenerar", outline "Cancelar".

**Step 2 — `SnapshotTimeline.tsx`:** Per RESEARCH Open Q3 recommendation: Card listing snapshots reverse-chronologically with Restore button each. Renders inside InspectorSidebar's editor panel (or floating Card — planner picks; UI-SPEC §Editor Panel mentions "Histórico de Edições" section — extend there).

**Step 3 — `ParameterSidebar.tsx`:** Intercept slider change in useParameterStudioDispatch (extend): when `useEditorStore.getState().editLog.length > 0`, instead of immediately POSTing render, first POST snapshot (trigger='pre_slider_change') with current state payload. On 201 → open SliderConflictDialog. On user Confirm → proceed with render POST. On user Cancel → revert slider value (handled in dialog onCancel).

**Step 4 — fill SliderConflictDialog.test.tsx** with 6 tests including fetch sequencing assertions via vi.fn().
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/editor/__tests__/SliderConflictDialog.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 6 tests pass
    - Snapshot-before-modal ordering verified
    - Cancel reverts slider
    - SnapshotTimeline render + Restore wired
  </acceptance_criteria>
  <done>Slider conflict + snapshot timeline integrated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ branch+snapshot APIs | dialogs trigger mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09-01 | Tampering | XSS via branch name in dropdown | accept | React Text component auto-escapes; never use dangerouslySetInnerHTML. |
| T-08-09-02 | Tampering | Copy-to-main race during edits | mitigate | Backend create_snapshot creates atomic Snapshot row before main is updated; on failure rollback. |
| T-08-09-03 | Logic | Slider-conflict modal opens before snapshot succeeds (Pitfall 9) | mitigate | UI ordering: await useCreateSnapshot 201 BEFORE setOpen(true). Test asserts sequence. |
</threat_model>

<verification>
- 6 dialogs + BranchPicker + SnapshotTimeline
- D-19 sequencing enforced
- 12 tests pass
</verification>

<success_criteria>
Branch UX + snapshot timeline + slider conflict resolved per D-10..D-15 + D-19.
</success_criteria>

<output>
After completion, create `.planning/phases/08-.../08-09-SUMMARY.md`.
</output>
