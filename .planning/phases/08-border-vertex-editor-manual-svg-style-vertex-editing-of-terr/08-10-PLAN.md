---
phase: 08
plan: 10
type: execute
wave: 7
depends_on: [08-03b]
autonomous: true
requirements: [BRANCH-05]
files_modified:
  - backend/medieval_forge/services/export/schemas.py
  - backend/medieval_forge/services/export/zip.py
  - backend/medieval_forge/api/v3/export.py
  - backend/tests/parity/test_iberia_868_yaml.py
  - backend/tests/e2e/test_export_gate_iberia.py

must_haves:
  truths:
    - "MANIFEST_SCHEMA_VERSION bumped 2 → 3 in services/export/schemas.py"
    - "ManifestSchema extended with branch_name: str | None, snapshot_id: str | None, snapshot_timestamp: datetime | None (Pitfall 4 — None defaults preserve backward-compat with non-branch exports)"
    - "POST /api/v3/projects/{pid}/export accepts {branch_id, snapshot_id} body extension (D-16)"
    - "Iberia parity test asserts schema_version == 3"
    - "Iberia gold path with branch_id=null still passes gate (D-17 carry-forward: optional fields)"
    - "Branch export: manifest carries actual branch_name + snapshot metadata; ZIP filename suffix '-{branch}-{snapshot_seq}.zip'"
  artifacts:
    - path: "backend/medieval_forge/services/export/schemas.py"
      provides: "ManifestSchema with branch fields + bumped version"
      contains: "MANIFEST_SCHEMA_VERSION = 3"
    - path: "backend/medieval_forge/services/export/zip.py"
      provides: "build_unity_zip threads branch metadata into manifest"
      contains: "branch_name"
  key_links:
    - from: "POST /export"
      to: "build_unity_zip with branch context"
      via: "endpoint resolves branch + latest snapshot → passes to zip builder"
      pattern: "branch_id\\|snapshot_id"
---

<objective>
Wave 7 export manifest extension. Per D-16 + Pitfall 4: bump MANIFEST_SCHEMA_VERSION 2→3 to add `branch_name`, `snapshot_id`, `snapshot_timestamp` without breaking the Phase 06 parity test (uses Optional fields with `None` default for non-branch exports).

Output: schema + zip builder + endpoint extension + 2 test updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/08-.../08-CONTEXT.md §D-16
@.planning/phases/08-.../08-RESEARCH.md §Pitfall 4
@.planning/phases/06-.../06-CONTEXT.md §D-07 (manifest shape)
@backend/medieval_forge/services/export/schemas.py
@backend/medieval_forge/services/export/zip.py
@backend/medieval_forge/api/v3/export.py
@backend/tests/parity/test_iberia_868_yaml.py
@backend/tests/e2e/test_export_gate_iberia.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema bump + endpoint extension + parity update</name>
  <files>backend/medieval_forge/services/export/schemas.py, backend/medieval_forge/services/export/zip.py, backend/medieval_forge/api/v3/export.py, backend/tests/parity/test_iberia_868_yaml.py, backend/tests/e2e/test_export_gate_iberia.py</files>
  <read_first>
    - backend/medieval_forge/services/export/schemas.py (current ManifestSchema; MANIFEST_SCHEMA_VERSION constant location)
    - backend/medieval_forge/services/export/zip.py (build_unity_zip signature)
    - backend/medieval_forge/api/v3/export.py (POST handler signature)
    - backend/tests/parity/test_iberia_868_yaml.py (schema_version assertion)
    - .planning/phases/08-.../08-RESEARCH.md §Pitfall 4 (verbatim mitigation)
  </read_first>
  <behavior>
    - Test: ManifestSchema accepts {branch_name:None, snapshot_id:None, snapshot_timestamp:None} (Iberia gold path)
    - Test: ManifestSchema accepts {branch_name:"experiment", snapshot_id:"<uuid>", snapshot_timestamp:"2026-..."}
    - Test: ManifestSchema with schema_version=2 → ValidationError (must be 3 now); update Phase 06 parity test to assert 3
    - Test: Iberia parity (test_iberia_868_yaml.py) still passes with schema_version=3 + null branch fields
    - Test: POST /export with body {branch_id:"<uuid>"} resolves latest snapshot, injects metadata into manifest
    - Test: e2e test_export_gate_iberia asserts manifest.branch_name is None (no branch passed → backward-compat)
  </behavior>
  <action>
**Step 1 — `schemas.py`:**
- `MANIFEST_SCHEMA_VERSION: int = 3`
- Extend ManifestSchema:
  ```python
  branch_name: str | None = None
  snapshot_id: str | None = None
  snapshot_timestamp: datetime | None = None
  ```
- Bump `schema_version` Field default to 3 if currently typed as Literal[2].

**Step 2 — `zip.py`:** Extend `build_unity_zip(project_id, ...)` signature with `branch_name: str | None = None, snapshot_id: str | None = None, snapshot_timestamp: datetime | None = None`. Thread these into the assembled manifest dict.

**Step 3 — `api/v3/export.py`:** Extend POST body schema with optional `branch_id: str | None = None`. If set: fetch latest Snapshot for that branch via services.branches.service.list_snapshots → pass first snapshot's id+created_at + branch.name into build_unity_zip. ZIP filename: append `-{branch_name}-seq{seq}.zip` when branch present.

**Step 4 — `test_iberia_868_yaml.py`:** Update existing schema_version assertion 2 → 3. Add assertion: `manifest.branch_name is None` (Iberia legacy path).

**Step 5 — `test_export_gate_iberia.py`:** Same updates.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/parity/test_iberia_868_yaml.py tests/e2e/test_export_gate_iberia.py tests/e2e/test_export_gate_france.py tests/e2e/test_export_gate_broken.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - All Phase 06 export tests still pass
    - `grep -c "MANIFEST_SCHEMA_VERSION = 3" backend/medieval_forge/services/export/schemas.py` returns 1
    - `grep -c "branch_name" backend/medieval_forge/services/export/schemas.py` returns 1+
    - Iberia parity (test_iberia_868_yaml.py) green; schema_version=3 verified
    - Optional fields preserve backward-compat (None default works)
  </acceptance_criteria>
  <done>Manifest v3 + branch export ready; Phase 06 parity unbroken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /export | branch_id from client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10-01 | Tampering | Invalid branch_id | mitigate | pydantic UUID format check; backend 404 if branch missing. |
| T-08-10-02 | Information Disclosure | Branch name in ZIP manifest leaked to Unity build | accept | Branch names are user-chosen; no sensitive context. |
</threat_model>

<verification>
- Schema version 3 + 3 optional fields
- Phase 06 parity + e2e green
- Manifest extension matches D-16 verbatim
</verification>

<success_criteria>
Branch metadata flows from active branch through manifest into Unity ZIP. Phase 06 contracts preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/08-.../08-10-SUMMARY.md`.
</output>
