---
phase: 08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr
plan: 13
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - frontend/src/components/editor/EditToolPalette.tsx
  - frontend/src/components/workspace/WorkspaceToolbar.tsx
autonomous: true
requirements: [UX-01]
must_haves:
  truths:
    - "A visible tool palette renders V/A/D/S/M + Esc buttons in the workspace toolbar"
    - "Clicking a tool button calls useEditorStore.selectTool and sets activeTool"
    - "The active tool button is visually distinct (solid/blue) from inactive (soft)"
    - "Keyboard shortcuts V/A/D/S/M still drive the SAME activeTool the palette shows"
  artifacts:
    - path: "frontend/src/components/editor/EditToolPalette.tsx"
      provides: "Radix IconButton group bound to useEditorStore.activeTool/selectTool"
      min_lines: 50
    - path: "frontend/src/components/workspace/WorkspaceToolbar.tsx"
      provides: "Mounts <EditToolPalette/> between BranchPicker and Generate badge"
      contains: "EditToolPalette"
  key_links:
    - from: "EditToolPalette buttons"
      to: "useEditorStore.selectTool"
      via: "onClick handlers"
      pattern: "selectTool"
    - from: "useEditorStore.activeTool"
      to: "EditToolPalette button variant"
      via: "active-state styling"
      pattern: "activeTool"
---

<objective>
Close GAP-C (no visible edit-tool UI). The five edit tools are keyboard-only
(useKeyboardShortcuts V/A/D/S/M) with default `activeTool=null` and no visible
affordance — the editor is undiscoverable. Add a visible tool palette in the
WorkspaceToolbar (per 08-UI-SPEC §Tool Palette Zone) bound to the SAME
`useEditorStore.activeTool/selectTool` state the keyboard shortcuts already set.

Purpose: make edit mode discoverable + surface the active tool (UX-01 affordance).
Output: EditToolPalette component + toolbar mount. FRONTEND-ONLY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr/08-VERIFICATION.md
@.planning/phases/08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr/08-UI-SPEC.md

<interfaces>
From frontend/src/stores/useEditorStore.ts:
```typescript
export type EditTool = 'V' | 'A' | 'D' | 'S' | 'M' | null;
activeTool: EditTool;               // default null (line 174)
selectTool: (t: EditTool) => void;  // line 184 — set({ activeTool: t })
```

From frontend/src/hooks/useKeyboardShortcuts.ts (already wired — DO NOT EDIT):
```typescript
// V/A/D/S/M -> useEditorStore.getState().selectTool(TOOL_KEYS[key])  (lines 68-79)
// Esc -> selectTool(null) when a tool is active (lines 93-102)
// These already drive activeTool; the palette must read/write the SAME state.
```

From 08-UI-SPEC.md §Tool Palette Zone (lines 141-160) — LOCKED placement + style:
- Position: WorkspaceToolbar center, between BranchPicker and the Generate badge.
- Layout: `Flex align="center" gap="1"` of `IconButton size="2" variant="soft"`.
- Active tool: `variant="solid" color="blue"`; inactive: `variant="soft"`.
- Each button wrapped in Radix `Tooltip` (delayDuration 300ms) with PT-BR label + shortcut.
- Icon/tooltip map (use @radix-ui/react-icons; fall back to listed alternates if a name is missing):
  | Tool | Icon | Tooltip |
  | V | CursorArrowIcon | "Selecionar vértice (V)" |
  | A | PlusIcon | "Adicionar vértice na aresta (A)" |
  | D | TrashIcon | "Excluir vértice selecionado (Del/D)" |
  | S | Scissors1Icon (or ScissorsIcon) | "Dividir território — desenhe um corte (S)" |
  | M | MixIcon (or MixerHorizontalIcon) | "Mesclar territórios adjacentes (M)" |
  | Esc | Cross2Icon | "Cancelar ação atual (Esc)" |

From frontend/src/components/workspace/WorkspaceToolbar.tsx:
```typescript
// Right cluster (lines 155-198): <BranchPicker/>, Undo/Redo, Gerar Mapa, Exportar ZIP.
// Per UI-SPEC the palette goes between BranchPicker and the Generate badge.
// The Generate badge is the center element (GenerateStatusBadge, lines 137-153).
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EditToolPalette component bound to activeTool/selectTool</name>
  <files>frontend/src/components/editor/EditToolPalette.tsx</files>
  <read_first>
    - frontend/src/stores/useEditorStore.ts (EditTool type:25; activeTool:174; selectTool:184)
    - frontend/src/components/workspace/WorkspaceToolbar.tsx (Radix import patterns, IconButton/Tooltip usage)
    - .planning/phases/08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr/08-UI-SPEC.md lines 141-160 (icon/tooltip map)
  </read_first>
  <behavior>
    - Renders 6 buttons in order V, A, D, S, M, Esc.
    - Reading activeTool='S' → the S button has variant="solid" color="blue"; all
      others variant="soft".
    - Clicking the A button calls useEditorStore.selectTool('A') (activeTool becomes 'A').
    - Clicking the Esc button calls useEditorStore.selectTool(null) (activeTool becomes null).
    - After a keyboard 'V' press sets activeTool='V', the palette V button shows the
      active (solid) variant — i.e. the palette is a pure reflection of store state,
      no local tool state.
    - Each button carries data-testid `edit-tool-{V|A|D|S|M|esc}` for UAT targeting.
  </behavior>
  <action>
    Create frontend/src/components/editor/EditToolPalette.tsx:
    1. Import: `Flex, IconButton, Tooltip` from '@radix-ui/themes'; the icons listed
       in the interfaces map from '@radix-ui/react-icons' (use the primary names;
       if `Scissors1Icon`/`MixIcon` aren't exported in the installed version, use
       `ScissorsIcon`/`MixerHorizontalIcon` and note the substitution in a comment).
    2. `const activeTool = useEditorStore((s) => s.activeTool)`;
       `const selectTool = useEditorStore((s) => s.selectTool)`.
    3. Define an ordered array of tool descriptors: `{ key: EditTool|'ESC', testid, icon, tooltip }`
       for V, A, D, S, M, then an Esc entry whose action is `selectTool(null)`.
    4. Render `<Flex align="center" gap="1" data-testid="edit-tool-palette">` mapping each
       descriptor to a `<Tooltip content={tooltip} delayDuration={300}>` wrapping an
       `<IconButton size="2" data-testid={testid}
          variant={activeTool === key ? 'solid' : 'soft'}
          color={activeTool === key ? 'blue' : 'gray'}
          aria-pressed={activeTool === key}
          aria-label={tooltip}
          onClick={() => selectTool(key === 'ESC' ? null : key)}>{icon}</IconButton>`.
       (For the Esc button, active-state is never shown — it is a momentary cancel.)
    Top doc comment: this palette closes GAP-C; it is a pure reflection of
    useEditorStore.activeTool — keyboard shortcuts and palette share one state source.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest run EditToolPalette</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/src/components/editor/EditToolPalette.tsx exists.
    - The 6 testids `edit-tool-{v,a,d,s,m,esc}` are produced via the descriptor map (`data-testid={testid}`); a vitest assertion MUST verify all 6 render in the DOM (this is the gate, not a source grep, since the testids are templated).
    - `grep -n "selectTool" frontend/src/components/editor/EditToolPalette.tsx` ≥1 hit.
    - `grep -n "activeTool === " frontend/src/components/editor/EditToolPalette.tsx` ≥1 hit (active-state styling).
    - A vitest spec asserts: activeTool='A' → A button has variant solid (e.g. aria-pressed=true); clicking M calls selectTool('M'); clicking Esc calls selectTool(null).
    - `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>EditToolPalette renders 6 tool buttons reflecting + driving useEditorStore.activeTool; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Mount EditToolPalette in WorkspaceToolbar</name>
  <files>frontend/src/components/workspace/WorkspaceToolbar.tsx</files>
  <read_first>
    - frontend/src/components/workspace/WorkspaceToolbar.tsx (center badge lines 137-153; right cluster 155-198)
    - frontend/src/components/editor/EditToolPalette.tsx (from Task 1)
  </read_first>
  <action>
    In frontend/src/components/workspace/WorkspaceToolbar.tsx:
    1. Import: `import { EditToolPalette } from '../editor/EditToolPalette'`.
    2. Mount `<EditToolPalette />` in the toolbar, positioned per UI-SPEC between the
       BranchPicker and the Generate badge. Concretely: render it inside the existing
       right-cluster `<Flex align="center" gap="2">` immediately AFTER `<BranchPicker .../>`
       and BEFORE the Undo/Redo Tooltip block — this places the palette between the
       branch zone and the generate CTA, matching the UI-SPEC center→right ordering.
    No other toolbar logic changes.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest run WorkspaceToolbar</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "EditToolPalette" frontend/src/components/workspace/WorkspaceToolbar.tsx` returns import AND JSX mount (≥2 hits).
    - The JSX `<EditToolPalette` appears after `<BranchPicker` in the file (verify ordering by line numbers from grep -n).
    - `cd frontend && npx tsc --noEmit` exits 0; `npx vitest run WorkspaceToolbar` green.
  </acceptance_criteria>
  <done>Toolbar renders the visible tool palette; edit mode is discoverable.</done>
</task>

</tasks>

<verification>
- Palette renders in the running toolbar; clicking tools changes activeTool; keyboard
  shortcuts and palette stay in sync (both read/write useEditorStore.activeTool).
- `cd frontend && npx tsc --noEmit` exits 0; `cd frontend && npx vitest run` green.
</verification>

<success_criteria>
- Visible V/A/D/S/M + Esc palette in the toolbar per 08-UI-SPEC §Tool Palette Zone.
- Active tool visually surfaced; palette and keyboard share one state source.
- No backend/DAG/store-logic changes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr/08-13-SUMMARY.md`
</output>
