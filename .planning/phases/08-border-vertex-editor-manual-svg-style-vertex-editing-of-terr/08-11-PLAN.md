---
phase: 08
plan: 11
type: execute
wave: 8
depends_on: [08-06b, 08-07, 08-08, 08-09, 08-10]
autonomous: false
requirements: [EDIT-VERTEX-01, EDIT-POLYGON-01, EDIT-POLYGON-02, DAG-01, DAG-03, BRANCH-01, PERF-01]
files_modified:
  - frontend/tests/uat/08-vertex-drag.spec.ts
  - frontend/tests/uat/08-slider-conflict.spec.ts
  - frontend/tests/uat/08-perf-drag-60fps.spec.ts
  - frontend/tests/uat/08-branch-flow.spec.ts
  - backend/tests/parity/test_phase08_edit_roundtrip.py

must_haves:
  truths:
    - "Iberia parity (12-file Unity contract) still byte-equal with empty edit log + branch_id='main' (D-17 carry-forward)"
    - "Phase 06 + 07 + 07.1 + 07.2 tests all green (no regressions)"
    - "5 Playwright UAT specs pass: vertex drag commit, slider conflict modal flow, 60fps drag (manual confirm gate if CI flaky), branch create/switch/copy-to-main flow, snapshot timeline restore"
    - "Manual UAT checkpoint: user confirms snap visual + purple shared-edge + invalid drag red feedback"
  artifacts:
    - path: "frontend/tests/uat/08-vertex-drag.spec.ts"
      provides: "filled E2E for EDIT-VERTEX-01"
    - path: "frontend/tests/uat/08-branch-flow.spec.ts"
      provides: "E2E for BRANCH-01..05"
    - path: "backend/tests/parity/test_phase08_edit_roundtrip.py"
      provides: "empty-log identity parity assertion"
  key_links:
    - from: "Wave-0 stubs"
      to: "Wave-8 filled tests"
      via: "Nyquist gate closes — every REQ-ID now has executable coverage"
      pattern: ".*"
---

<objective>
Wave 8 verification + sign-off. Fills the remaining Wave-0 UAT stubs, adds the empty-log identity parity test, runs the full pyramid, and includes a `checkpoint:human-verify` for the 3 visual gates listed in VALIDATION.md §Manual-Only Verifications.

Output: 4 Playwright UAT specs filled + 1 parity test + 1 checkpoint task. Phase 08 done after this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/08-.../08-CONTEXT.md
@.planning/phases/08-.../08-VALIDATION.md §Manual-Only Verifications
@frontend/tests/uat (existing Playwright config + test patterns)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fill 4 Playwright UAT specs + parity empty-log test</name>
  <files>frontend/tests/uat/08-vertex-drag.spec.ts, frontend/tests/uat/08-slider-conflict.spec.ts, frontend/tests/uat/08-perf-drag-60fps.spec.ts, frontend/tests/uat/08-branch-flow.spec.ts, backend/tests/parity/test_phase08_edit_roundtrip.py</files>
  <read_first>
    - frontend/tests/uat (existing specs as templates — Phase 04/05/07 patterns)
    - frontend/playwright.config.ts
    - backend/tests/parity/test_iberia_868.py (parity pattern)
    - .planning/phases/08-.../08-VALIDATION.md (PERF-01 manual confirmation note)
    - Wave 0 stubs for all 4 frontend spec files
  </read_first>
  <action>
**Step 1 — `08-vertex-drag.spec.ts`:** Open Iberia 868 project → click Select tool (V) → click a barony → drag a vertex handle to a valid position → assert mouseup commits via assertion on canvas pixels (use `__forgeStageScale` / `__forgeSelectBarony` dev escape hatches from Phase 04.1) OR assert via backend snapshot fetch that editLog grew by 1.

**Step 2 — `08-slider-conflict.spec.ts`:** Make N edits on a non-main branch → drag σ slider → expect SliderConflictDialog appears AFTER snapshot save (verify via API spy on /branches/{bid}/snapshots POST returning 201 before dialog visible).

**Step 3 — `08-perf-drag-60fps.spec.ts`:** Use `page.evaluate` to measure rAF deltas during a 2s drag; assert median frame interval < 20ms. Tolerate CI jitter — mark `test.fail()` only if median > 33ms (30fps floor). Add manual-confirmation flag in test description per PERF-01 note.

**Step 4 — `08-branch-flow.spec.ts`:** Create branch "experiment" → switch → make edit → switch back to main (assert canvas reverts) → switch to experiment (assert canvas restores) → copy experiment → main (assert main now matches experiment) → delete experiment (assert 204) → delete main (assert disabled/409).

**Step 5 — `test_phase08_edit_roundtrip.py`:** Parity: run pipeline on Iberia with `manual_edit_log_hash=""` and `branch_id="main"` → assert 12 contract files byte-equal to pre-Phase-8 gold. This is D-17 carry-forward as a non-skippable parity gate.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/parity/ -x && cd ../frontend && npx playwright test tests/uat/08-vertex-drag.spec.ts tests/uat/08-slider-conflict.spec.ts tests/uat/08-branch-flow.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist with no `test.skip` / `pytestmark = skip` markers
    - Parity test passes
    - 3 of 4 Playwright specs pass; 08-perf may be marked manual-confirm per PERF-01
    - Full pytest + vitest + playwright suite green (counting manual-confirm tests as pass)
  </acceptance_criteria>
  <done>UAT + parity filled; suite green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Cross-surface gate — full pyramid + grep cleanups</name>
  <files>(verification only — no file modifications expected; if grep finds dead helpers, fix in place)</files>
  <read_first>
    - .planning/phases/08-.../08-VALIDATION.md (sampling rate + sign-off section)
  </read_first>
  <action>
Run full pyramid:
```
cd backend && python -m pytest tests/ -q
cd frontend && npm test -- --run && npm run test:e2e
```

Grep cleanups (RESEARCH §Anti-Patterns):
- No v1 restoration: `grep -rn "VertexHandlesLayer\\|useEditKeyboardMap\\|useUndoShortcut" frontend/src` → must return 0
- No global voronoi: `grep -rn "global.*kdtree\\|single.*kd_tree" backend/medieval_forge` → must return 0 hits in changed files
- NEAREST upscale preserved: `grep -n "BICUBIC\\|BILINEAR" backend/medieval_forge/services/pipeline/render.py` → must return 0 (only Image.NEAREST)
- branch_id in cache layer: `grep -n "cache_get\\|cache_put" backend/medieval_forge/services/pipeline/__init__.py | grep -v branch_id` → must return 0

If any grep fails, fix the offending file in-place as a Rule 1 deviation, document in commit.

Then update VALIDATION.md `Checker Sign-Off` section: tick all dimensions. Update STATE.md "Current Position" to "Phase 08 complete — ready for verification".
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/ -q 2>&1 | tail -5 && cd ../frontend && npm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Backend pytest exit code 0; total tests reported; no unexpected failures vs. pre-Phase-8 baseline
    - Frontend vitest exit code 0
    - Playwright exit code 0 (or manual-confirm flag for 08-perf only)
    - All 4 grep gates pass
    - VALIDATION.md sign-off complete
  </acceptance_criteria>
  <done>Pyramid green; phase verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual visual verification (3 gates from VALIDATION.md)</name>
  <files>(human inspection)</files>
  <what-built>
    - VertexEditLayer with viewport-culled handles + RAF-throttled drag
    - Snap target indicator (#eab308 yellow circle)
    - Shared-edge purple highlight (#a855f7)
    - Invalid drag red feedback (#ef4444)
    - Branch picker + dialogs + snapshot timeline
    - Landmask 2-mode toggle
  </what-built>
  <how-to-verify>
    1. Run `medieval-forge start` and open http://localhost:8000 → choose Iberia 868 project
    2. Verify desktop banner does NOT show on your dev machine
    3. Click Select tool (V); click a barony; observe vertex handles render only in viewport
    4. Drag a vertex slowly across screen → confirm 60fps perceived smoothness; coord tooltip follows cursor with 6-decimal lat/lon
    5. Drag a vertex near a neighbouring vertex → confirm yellow #eab308 snap circle appears; hold Alt → snap disabled
    6. Hover an edge shared between two baronies → confirm purple #a855f7 highlight + enlarged endpoint handles
    7. Drag a vertex to create self-intersection → confirm polygon turns red; mouseup → vertex snaps back; Callout "Geometria inválida..." shows 3s
    8. Open BranchPicker → create "experiment" branch → edit a vertex → switch back to main → confirm canvas reverts → switch to experiment → confirm canvas restores
    9. Drag σ slider while edits exist on experiment branch → confirm SliderConflictDialog appears mentioning auto-snapshot N
    10. Open Landmask toggle in LayerTogglePanel → switch Auto imediato → confirm orange Badge "Modo auto: ~10s por edição"
    11. Open Help panel (?) → confirm shortcut table renders
  </how-to-verify>
  <action>Pause for human inspection. Operator performs the steps in &lt;how-to-verify&gt; on a live dev server (medieval-forge start) and signs off via resume-signal. No automated steps.</action>
  <verify>
    <automated>echo "checkpoint:human-verify — manual sign-off required"</automated>
  </verify>
  <done>Operator typed "approved" or all 11 steps in how-to-verify confirmed working as specified.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan; no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11-01 | (none) | — | accept | All threats handled by upstream plans; this plan verifies their mitigations land. |
</threat_model>

<verification>
- Full pyramid green
- 4 grep gates pass
- Manual UAT signed off
- Iberia 12-file parity preserved
</verification>

<success_criteria>
Phase 08 complete: 37 decisions implemented, 30 REQ-IDs covered, all parity contracts preserved, manual UAT confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/08-.../08-11-SUMMARY.md` and `.planning/phases/08-.../08-VERIFICATION.md` summarizing per-REQ outcomes.
</output>
