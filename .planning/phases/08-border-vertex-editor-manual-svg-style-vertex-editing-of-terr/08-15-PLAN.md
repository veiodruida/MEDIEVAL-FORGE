---
phase: 08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr
plan: 15
type: execute
wave: 3
gap_closure: true
depends_on: [12, 13, 14]
files_modified:
  - frontend/src/components/canvas/VertexEditLayer.tsx
  - frontend/tests/uat/08-editor-reachable.spec.ts
autonomous: true
requirements: [EDIT-VERTEX-01, UX-01, EDIT-POLYGON-01, LANDMASK-01, PERF-01]
must_haves:
  truths:
    - "Driving the REAL workspace UI: selecting a barony renders vertex handles (count > 0)"
    - "Clicking the visible tool palette sets activeTool (same state keyboard sets)"
    - "After a vertex drag the editor store editLog grows (op committed end-to-end)"
    - "The Landmask Editor header is reachable in the running app"
  artifacts:
    - path: "frontend/src/components/canvas/VertexEditLayer.tsx"
      provides: "Dev-only __forgeEditorState() escape hatch for deterministic UAT inspection"
      contains: "__forgeEditorState"
    - path: "frontend/tests/uat/08-editor-reachable.spec.ts"
      provides: "End-to-end spec proving editor reachability via the real workspace UI"
      min_lines: 60
  key_links:
    - from: "__forgeSelectBarony"
      to: "vertex handles render"
      via: "SelectionBridge -> activeTerritoryId+vertices -> VertexEditLayer.visibleEntries"
      pattern: "__forgeEditorState"
    - from: "EditToolPalette button click"
      to: "useEditorStore.activeTool"
      via: "selectTool"
      pattern: "edit-tool-"
---

<objective>
Prove the editor is REACHABLE end-to-end through the running app — the exact thing
the existing isolated unit/parity/Playwright tests all passed while missing. The
existing 08-*.spec.ts files are "contract-literal": they `page.goto('/')` and `fetch`
endpoints directly without ever driving the UI, so they never noticed the editor was
dead at the integration layer.

This spec drives the REAL workspace: navigate to a seeded project, select a barony via
the established `__forgeSelectBarony` dev hatch, assert vertex handles render, click the
visible tool palette, simulate a vertex drag, and assert `editLog` grew. To inspect
Konva-canvas state deterministically (Konva paints to <canvas>, not DOM), add a small
dev-only `__forgeEditorState()` escape hatch mirroring the existing `__forgeStageScale`
/ `__forgeSelectBarony` pattern.

Purpose: a regression gate that fails loudly if any integration wire (GAP-A/B/C) breaks
again. FRONTEND-ONLY (one dev-gated hatch + one spec).
Output: __forgeEditorState hatch, 08-editor-reachable.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr/08-VERIFICATION.md
@.planning/phases/08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr/08-VALIDATION.md

<interfaces>
Existing dev escape hatches (the established UAT pattern — gated on import.meta.env.DEV):
```typescript
// frontend/src/components/canvas/BaronyLayer.tsx:64-75
window.__forgeSelectBarony = (name: string) => useUIStore.getState().selectBarony(name)
// frontend/src/components/canvas/CanvasViewer.tsx:423-431
window.__forgeStageScale = currentScale   // number
```

From frontend/src/components/canvas/VertexEditLayer.tsx (state to expose):
```typescript
const activeTerritoryId = useEditorStore((s) => s.activeTerritoryId);
const vertices = useEditorStore((s) => s.vertices);            // Record<id,{lat,lon}>
const visibleEntries: Array<{id;lat;lon}>;                     // line 402 — handles actually rendered
// editLog length: useEditorStore.getState().editLog.length
// Barony handles render only when !isLandmaskMode && visibleEntries non-empty (line 787)
```

Existing UAT harness reference (mock + real-nav pattern):
- frontend/tests/uat/08-vertex-drag.spec.ts (stubBranchEndpoints helper; route mocking)
- frontend/tests/uat/playwright/03-canvas-workspace.spec.ts (real workspace navigation +
  how a seeded project is reached; copy its nav + artifact-stub approach)

Playwright config testDir includes ./tests (per Phase 04 STATE.md decision), so a spec at
frontend/tests/uat/08-editor-reachable.spec.ts is discovered.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dev-only __forgeEditorState() escape hatch to VertexEditLayer</name>
  <read_first>
    - frontend/src/components/canvas/VertexEditLayer.tsx (visibleEntries:402; activeTerritoryId:273; vertices:274; isLandmaskMode:314; existing effects ~700-714)
    - frontend/src/components/canvas/BaronyLayer.tsx (lines 64-75 — the exact DEV-gated window-hatch + cleanup pattern to mirror)
  </read_first>
  <action>
    In frontend/src/components/canvas/VertexEditLayer.tsx, add a useEffect (near the
    other effects, AFTER visibleEntries is declared) that — gated on
    `import.meta.env.DEV` — installs a window function returning current editor state:
    ```ts
    useEffect(() => {
      if (!import.meta.env.DEV) return
      ;(window as unknown as { __forgeEditorState?: () => unknown }).__forgeEditorState = () => ({
        activeTerritoryId,
        vertexCount: Object.keys(vertices).length,
        visibleHandleCount: isLandmaskMode ? 0 : visibleEntries.length,
        editLogLength: useEditorStore.getState().editLog.length,
        activeTool: useEditorStore.getState().activeTool,
      })
      return () => {
        delete (window as unknown as { __forgeEditorState?: unknown }).__forgeEditorState
      }
    }, [activeTerritoryId, vertices, isLandmaskMode, visibleEntries])
    ```
    This mirrors BaronyLayer's `__forgeSelectBarony` pattern exactly (DEV-gated, cleanup
    on unmount). It ships no code in production builds.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest run VertexEditLayer</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "__forgeEditorState" frontend/src/components/canvas/VertexEditLayer.tsx` returns the install AND the delete-cleanup (≥2 hits).
    - `grep -n "import.meta.env.DEV" frontend/src/components/canvas/VertexEditLayer.tsx` ≥1 hit (DEV-gated).
    - `cd frontend && npx tsc --noEmit` exits 0; `npx vitest run VertexEditLayer` green (no regression).
  </acceptance_criteria>
  <done>Dev-only __forgeEditorState() exposes activeTerritoryId/vertexCount/visibleHandleCount/editLogLength/activeTool for UAT.</done>
</task>

<task type="auto">
  <name>Task 2: 08-editor-reachable.spec.ts — drive the REAL workspace UI end-to-end</name>
  <read_first>
    - frontend/tests/uat/08-vertex-drag.spec.ts (stubBranchEndpoints + route-mock helpers — REUSE)
    - frontend/tests/uat/playwright/03-canvas-workspace.spec.ts (how the seeded workspace is navigated + how artifacts/status are stubbed to reach the CanvasViewer branch)
    - frontend/src/components/editor/EditToolPalette.tsx (data-testid `edit-tool-{v|a|d|s|m|esc}` from 08-13)
    - frontend/src/components/canvas/LayerTogglePanel.tsx (data-testid `layer-toggle-panel`; landmask header from LandmaskEditorHeader)
  </read_first>
  <action>
    Create frontend/tests/uat/08-editor-reachable.spec.ts. Unlike the existing
    contract-literal specs, this MUST drive the real UI. Reuse the route-stubbing
    approach from 03-canvas-workspace.spec.ts + 08-vertex-drag.spec.ts so the workspace
    reaches the CanvasViewer branch (status has_artifacts true, branches list returns a
    main branch, geojson/metadata artifacts stubbed with at least one barony whose
    geometry has a multi-point ring).

    Write these tests (each navigates to the real project workspace route, NOT just '/'):

    Test 1 — "selecting a barony renders vertex handles (GAP-A)":
      - Navigate to the workspace; wait for `[data-testid="canvas-stage"]`.
      - `await page.evaluate((name) => window.__forgeSelectBarony(name), '<seeded-barony-name>')`.
      - `await expect.poll(() => page.evaluate(() => window.__forgeEditorState?.()))`
        asserts `activeTerritoryId` is non-null AND `vertexCount > 0` AND
        `visibleHandleCount > 0`. (If culling makes visibleHandleCount flaky, first
        reset zoom via Ctrl+0 / fit-to-view so the whole barony is in view; assert
        vertexCount > 0 as the hard gate and visibleHandleCount >= 1.)

    Test 2 — "tool palette is visible and drives activeTool (GAP-C)":
      - Assert `[data-testid="edit-tool-palette"]` is visible.
      - Click `[data-testid="edit-tool-a"]`; poll `__forgeEditorState().activeTool === 'A'`.
      - Click `[data-testid="edit-tool-v"]`; poll `activeTool === 'V'`.

    Test 3 — "vertex edit commits end-to-end — editLog grows (GAP-A + store sink)":
      - Select a barony (Test 1 setup). Capture `editLogLength0 = __forgeEditorState().editLogLength`.
      - Trigger a committed vertex op. Driving a raw Konva canvas drag is brittle; the
        deterministic, integration-honest path is to invoke a committed store op the way
        the UI would after a drag mouseup. Add (if not already present from 08-06a) a
        DEV hatch OR call the store directly via page.evaluate:
        `window.__forgeEditorState` is read-only; instead use page.evaluate to call the
        real store action used by a drag commit:
        `await page.evaluate(() => { const s = (window as any); /* trigger via real path */ })`.
        Preferred: select the V tool, then dispatch a Konva drag on the stage canvas at
        the first handle's screen position using mouse.move/down/up (compute the handle
        screen coord from __forgeEditorState's first vertex via projection is complex —
        instead assert the realistic integration: after selecting a barony with the V
        tool active, perform `page.mouse` drag across the canvas center and poll that
        editLogLength > editLogLength0). If canvas-coordinate drag proves flaky in CI,
        fall back to asserting the wired chokepoint fires by calling the SAME store
        action a drag-end calls (`useEditorStore.getState().moveVertex(firstId, lat, lon)`)
        via page.evaluate, then poll editLogLength === editLogLength0 + 1. Document which
        path was used in a spec comment.
      - Assert `editLogLength` increased by ≥1 (op committed through the real store).

    Test 4 — "Landmask Editor header is reachable (GAP-B)":
      - Assert `[data-testid="layer-toggle-panel"]` is visible.
      - Assert the landmask mode toggle / "Aplicar landmask" affordance from
        LandmaskEditorHeader is present (query by its visible PT-BR text
        "Aplicar landmask" or a header testid). This proves projectId/branchId reached
        the panel (without them in the pre-fix code the header was effectively dead).

    Use `expect.poll` for all store assertions (React effects settle asynchronously).
    Keep the spec self-contained: all backend routes mocked; no live server required.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test tests/uat/08-editor-reachable.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/tests/uat/08-editor-reachable.spec.ts exists with ≥4 `test(` blocks.
    - `grep -n "__forgeSelectBarony" frontend/tests/uat/08-editor-reachable.spec.ts` ≥1 hit (drives REAL selection, not raw fetch).
    - `grep -n "__forgeEditorState" frontend/tests/uat/08-editor-reachable.spec.ts` ≥1 hit (inspects real editor state).
    - `grep -n "edit-tool-" frontend/tests/uat/08-editor-reachable.spec.ts` ≥1 hit (drives the visible palette).
    - `grep -n "editLogLength" frontend/tests/uat/08-editor-reachable.spec.ts` ≥1 hit (proves commit end-to-end).
    - The spec does NOT rely solely on `page.goto('/')` + raw `fetch` to "prove" behavior — it MUST select a barony through the UI hatch and assert rendered editor state (anti-pattern guard: this is the failure mode of the existing 08-vertex-drag.spec.ts).
    - `cd frontend && npx playwright test tests/uat/08-editor-reachable.spec.ts` passes all blocks.
  </acceptance_criteria>
  <done>End-to-end spec proves: barony select renders handles, palette drives activeTool, vertex edit grows editLog, landmask header reachable — all through the real workspace.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx playwright test tests/uat/08-editor-reachable.spec.ts` green — the
  editor is reachable end-to-end through the running app.
- `cd frontend && npx tsc --noEmit` exits 0.
- Full suite unaffected: `cd frontend && npx vitest run` green.
</verification>

<success_criteria>
- A regression gate exists that fails if GAP-A/B/C wiring breaks again.
- Proof is via the REAL UI (barony select → handles → tool palette → committed edit →
  landmask header), not contract-literal fetches.
- No backend/DAG/store-logic/parity changes (one DEV-gated hatch only).
</success_criteria>

<output>
After completion, create `.planning/phases/08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr/08-15-SUMMARY.md`
</output>
