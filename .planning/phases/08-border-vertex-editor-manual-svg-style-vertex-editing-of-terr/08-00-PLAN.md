---
phase: 08
plan: 00
type: execute
wave: 0
depends_on: []
autonomous: true
requirements: [EDIT-VERTEX-01, EDIT-VERTEX-02, EDIT-VERTEX-03, EDIT-VERTEX-04, EDIT-POLYGON-01, EDIT-POLYGON-02, EDIT-POLYGON-03, LANDMASK-01, LANDMASK-02, BRANCH-01, BRANCH-02, BRANCH-03, BRANCH-04, BRANCH-05, DAG-01, DAG-02, DAG-03, DAG-04, DAG-05, TOPO-01, TOPO-02, TOPO-03, TOPO-04, PERF-01, UX-01, UX-02, UNDO-01, PERSIST-01, PERSIST-02, TELEM-01]
files_modified:
  - backend/tests/unit/test_topology_validate.py
  - backend/tests/unit/test_manual_edit_simplify.py
  - backend/tests/unit/test_manual_edit_split.py
  - backend/tests/unit/test_manual_edit_merge.py
  - backend/tests/unit/test_shared_vertex_coupling.py
  - backend/tests/unit/test_dag_manual_edit.py
  - backend/tests/unit/test_stage_cache_branch.py
  - backend/tests/unit/test_models_branches.py
  - backend/tests/integration/test_branches_endpoint.py
  - backend/tests/integration/test_landmask_edit.py
  - backend/tests/integration/test_landmask_cascade.py
  - backend/tests/integration/test_snapshot_persistence.py
  - backend/tests/integration/test_editor_validate_endpoint.py
  - backend/tests/integration/test_render_with_branch.py
  - frontend/src/stores/__tests__/useEditorStore.test.ts
  - frontend/src/components/canvas/__tests__/VertexEditLayer.test.tsx
  - frontend/src/components/editor/__tests__/BranchPicker.test.tsx
  - frontend/src/components/editor/__tests__/VertexCapBadge.test.tsx
  - frontend/src/lib/__tests__/snap.test.ts
  - frontend/src/lib/__tests__/sharedVertex.test.ts
  - frontend/src/hooks/__tests__/useKeyboardShortcuts.phase08.test.ts
  - frontend/src/components/__tests__/DesktopRequiredBanner.test.tsx
  - frontend/src/components/editor/__tests__/SliderConflictDialog.test.tsx
  - frontend/src/components/editor/__tests__/LandmaskEditorHeader.test.tsx
  - frontend/tests/uat/08-vertex-drag.spec.ts
  - frontend/tests/uat/08-slider-conflict.spec.ts
  - frontend/tests/uat/08-perf-drag-60fps.spec.ts

must_haves:
  truths:
    - "Every Wave-1+ task has an executable <automated> target (no MISSING references)"
    - "All 28 skip-marked test files commit cleanly with no production imports"
    - "Full pytest + vitest + playwright suite passes (skipped tests count as pass)"
  artifacts:
    - path: "backend/tests/unit/test_topology_validate.py"
      provides: "EDIT-VERTEX-01 + TOPO-01 unit harness"
      contains: "@pytest.mark.skip"
    - path: "frontend/src/stores/__tests__/useEditorStore.test.ts"
      provides: "UNDO-01 + EDIT-VERTEX-03 vitest harness"
      contains: "it.skip"
    - path: "frontend/tests/uat/08-vertex-drag.spec.ts"
      provides: "EDIT-VERTEX-01 Playwright harness"
      contains: "test.skip"
  key_links:
    - from: "VALIDATION.md per-task table"
      to: "08-00-PLAN.md test scaffolds"
      via: "Nyquist sampling guarantee — every later task has an <automated> path"
      pattern: "tests/.*test_.*\\.py|spec\\.ts"
---

<objective>
Wave 0 BLOCKER. Land all 28 skip-marked test scaffold files BEFORE Wave 1 implementation so every subsequent task's `<verify>` block has an executable target (Nyquist). Pattern mirrors Phase 04 04-00-PLAN.md and Phase 07.1 07.1-00-PLAN.md verbatim.

Purpose: close VALIDATION.md `wave_0_complete: false` gap; flip to `true`.
Output: 28 new test files committed; full suite still green (skipped tests pass).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr/08-CONTEXT.md
@.planning/phases/08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr/08-RESEARCH.md
@.planning/phases/08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr/08-VALIDATION.md
@.planning/phases/04-parameter-studio-live-re-render/04-00-PLAN.md
@.planning/phases/07.1-period-numeric-inputs-llama-cpp-re-add-with-auto-launch-and-/07.1-00-PLAN.md

<interfaces>
Backend test marker pattern (verified in pyproject.toml):
  @pytest.mark.unit       — fast unit tests
  @pytest.mark.integration — DB + endpoint
  @pytest.mark.parity     — non-skippable Iberia/France
  @pytest.mark.skip(reason="Wave 0 stub: implemented in plan 08-XX") — Wave 0 only

Frontend test pattern (vitest):
  import { describe, it } from 'vitest';
  describe('SUITE', () => { it.skip('TODO Wave X', () => {}); });

Playwright test pattern:
  import { test } from '@playwright/test';
  test.skip('TODO Wave X', async ({ page }) => {});
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create 15 backend test scaffolds</name>
  <files>backend/tests/unit/test_topology_validate.py, backend/tests/unit/test_manual_edit_simplify.py, backend/tests/unit/test_manual_edit_split.py, backend/tests/unit/test_manual_edit_merge.py, backend/tests/unit/test_shared_vertex_coupling.py, backend/tests/unit/test_dag_manual_edit.py, backend/tests/unit/test_stage_cache_branch.py, backend/tests/unit/test_models_branches.py, backend/tests/integration/test_branches_endpoint.py, backend/tests/integration/test_landmask_edit.py, backend/tests/integration/test_landmask_cascade.py, backend/tests/integration/test_snapshot_persistence.py, backend/tests/integration/test_editor_validate_endpoint.py, backend/tests/integration/test_render_with_branch.py</files>
  <read_first>
    - .planning/phases/08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr/08-VALIDATION.md (Wave 0 gaps section)
    - .planning/phases/04-parameter-studio-live-re-render/04-00-PLAN.md (stub pattern)
    - backend/pyproject.toml (pytest markers)
  </read_first>
  <action>
Create each file with this exact skeleton (substitute REQ + plan refs per the VALIDATION.md table):

```python
"""Wave 0 stub for Phase 08 — implemented in plan 08-XX.

Covers REQ-IDs: {EDIT-VERTEX-01, TOPO-01, ...}
See: .planning/phases/08-.../08-VALIDATION.md
"""
import pytest

pytestmark = pytest.mark.skip(reason="Wave 0 stub — implementation lands in plan 08-XX")


def test_placeholder_for_req_xxx():
    """Test will be filled by plan 08-XX per VALIDATION.md per-task table."""
    pass
```

For each of the 14 files, include 2-4 stub functions with descriptive names matching the REQ-ID behaviours (per user memory `feedback-tests-descriptive.md`). Examples for `test_topology_validate.py`:
  - `test_self_intersection_polygon_returns_SELF_INTERSECT_code`
  - `test_disjoint_neighbour_returns_NEIGHBOUR_GAP_code`
  - `test_valid_post_edit_polygon_returns_True_None`

For `test_stage_cache_branch.py`: this file ANNOUNCES the rewrite of the existing `test_stage_cache.py` — but does NOT delete the old file yet (plan 08-02 does the atomic swap). Skip-mark so it's a no-op.

For `test_models_branches.py`: stubs for Branch, Snapshot, EditEvent ORM round-trip.

Do NOT import any production module that does not yet exist. Skip marker means pytest collection succeeds even with bad imports — but be defensive: only `import pytest`.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/test_topology_validate.py tests/unit/test_manual_edit_simplify.py tests/unit/test_manual_edit_split.py tests/unit/test_manual_edit_merge.py tests/unit/test_shared_vertex_coupling.py tests/unit/test_dag_manual_edit.py tests/unit/test_stage_cache_branch.py tests/unit/test_models_branches.py tests/integration/test_branches_endpoint.py tests/integration/test_landmask_edit.py tests/integration/test_landmask_cascade.py tests/integration/test_snapshot_persistence.py tests/integration/test_editor_validate_endpoint.py tests/integration/test_render_with_branch.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - All 14 files exist
    - `pytest --collect-only` reports each test as `SKIPPED` not `ERROR`
    - No import of production modules that don't exist
    - `pytest backend/tests -q` exit code 0; suite delta = +N skipped, 0 failed
  </acceptance_criteria>
  <done>14 backend stub files committed; pytest collection clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create 10 frontend vitest scaffolds + 3 Playwright UAT scaffolds</name>
  <files>frontend/src/stores/__tests__/useEditorStore.test.ts, frontend/src/components/canvas/__tests__/VertexEditLayer.test.tsx, frontend/src/components/editor/__tests__/BranchPicker.test.tsx, frontend/src/components/editor/__tests__/VertexCapBadge.test.tsx, frontend/src/lib/__tests__/snap.test.ts, frontend/src/lib/__tests__/sharedVertex.test.ts, frontend/src/hooks/__tests__/useKeyboardShortcuts.phase08.test.ts, frontend/src/components/__tests__/DesktopRequiredBanner.test.tsx, frontend/src/components/editor/__tests__/SliderConflictDialog.test.tsx, frontend/src/components/editor/__tests__/LandmaskEditorHeader.test.tsx, frontend/tests/uat/08-vertex-drag.spec.ts, frontend/tests/uat/08-slider-conflict.spec.ts, frontend/tests/uat/08-perf-drag-60fps.spec.ts</files>
  <read_first>
    - .planning/phases/08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr/08-VALIDATION.md
    - frontend/src/components/canvas/__tests__ (existing patterns)
    - frontend/tests/uat (existing Playwright patterns)
  </read_first>
  <action>
Vitest skeleton (each .test.ts/.test.tsx file):

```typescript
/* Wave 0 stub for Phase 08 — implemented in plan 08-XX.
 * Covers REQ-IDs: {UNDO-01, EDIT-VERTEX-03, ...}
 * See: .planning/phases/08-.../08-VALIDATION.md
 */
import { describe, it } from 'vitest';

describe('Phase 08 — useEditorStore (UNDO-01, EDIT-VERTEX-03)', () => {
  it.skip('TODO plan 08-04: zundo temporal wraps store; pastStates grows on moveVertex', () => {});
  it.skip('TODO plan 08-04: history cap = 100 oldest evicted on push', () => {});
  it.skip('TODO plan 08-04: switchBranch() clears temporal history', () => {});
});
```

Use descriptive test names per user memory (`feedback-tests-descriptive.md`). 3-6 stub `it.skip` per file aligned with VALIDATION.md REQ map.

Playwright skeleton (each .spec.ts file):

```typescript
/* Wave 0 stub for Phase 08 — implemented in plan 08-11. */
import { test } from '@playwright/test';

test.skip('TODO plan 08-11: vertex drag commits on mouseup with valid topology', async ({ page }) => {});
test.skip('TODO plan 08-11: vertex drag snaps back on self-intersect', async ({ page }) => {});
```

Do NOT import any production component/module that doesn't exist (skip marker prevents execution but imports still resolve at parse time — keep imports to test framework only).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/stores/__tests__/useEditorStore.test.ts src/components/canvas/__tests__/VertexEditLayer.test.tsx src/components/editor/__tests__/BranchPicker.test.tsx src/components/editor/__tests__/VertexCapBadge.test.tsx src/lib/__tests__/snap.test.ts src/lib/__tests__/sharedVertex.test.ts src/hooks/__tests__/useKeyboardShortcuts.phase08.test.ts src/components/__tests__/DesktopRequiredBanner.test.tsx src/components/editor/__tests__/SliderConflictDialog.test.tsx src/components/editor/__tests__/LandmaskEditorHeader.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 10 vitest files + 3 playwright files exist
    - `npx vitest run` reports all stubs as `skipped`, 0 failed
    - `npx playwright test tests/uat/08-*.spec.ts --list` reports each test as skipped
    - Full vitest + playwright suite green (delta = +N skipped, 0 failed)
  </acceptance_criteria>
  <done>13 frontend stub files committed; vitest + playwright collection clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update VALIDATION.md — flip nyquist_compliant and wave_0_complete to true</name>
  <files>.planning/phases/08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr/08-VALIDATION.md</files>
  <read_first>
    - .planning/phases/08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr/08-VALIDATION.md
  </read_first>
  <action>
Update frontmatter: `nyquist_compliant: false` → `true`; `wave_0_complete: false` → `true`. Update the "Wave 0 Requirements" checklist: tick every box now that all 28 files exist. Update the per-task table by populating one row per Wave-0 file with `File Exists: ✅ Wave 0`. Replace the placeholder row with the actual per-plan tasks 08-01 through 08-11 (skeletal — each later plan fills its own rows during execution).
  </action>
  <verify>
    <automated>grep -c "nyquist_compliant: true" .planning/phases/08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr/08-VALIDATION.md && grep -c "wave_0_complete: true" .planning/phases/08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr/08-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `nyquist_compliant: true` present in frontmatter
    - `wave_0_complete: true` present in frontmatter
    - Every checkbox under "Wave 0 Requirements" is `- [x]`
  </acceptance_criteria>
  <done>VALIDATION.md committed with Wave 0 sign-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Wave 0 produces only test scaffolds — no production code, no API surface, no DB writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-00-01 | Tampering | Test files | accept | Tests are skip-marked stubs; cannot execute production code. No DB or network surface. |

Security enforcement N/A for stub-only plan; threat register documented for completeness.
</threat_model>

<verification>
- 14 backend + 10 vitest + 3 Playwright stub files exist
- Full suite (pytest + vitest + playwright) exits 0, delta = +N skipped, 0 failed
- VALIDATION.md `nyquist_compliant: true` and `wave_0_complete: true`
- 3 atomic commits (per Phase 07.1 precedent: backend stubs; frontend stubs; VALIDATION update)
</verification>

<success_criteria>
Every Wave-1+ plan task can reference a real test file in its `<verify><automated>` block. Zero MISSING placeholders downstream.
</success_criteria>

<output>
After completion, create `.planning/phases/08-border-vertex-editor-manual-svg-style-vertex-editing-of-terr/08-00-SUMMARY.md`.
</output>
